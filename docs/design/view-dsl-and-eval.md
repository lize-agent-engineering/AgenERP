# 视图 DSL 与渲染器 · 状态快照与评测

> 生成出来的界面长什么样、由什么渲染，以及怎么证明它没坏。

| | |
|---|---|
| 来源 | 由 `ARCHITECTURE.md`（草案 v0.6，69KB/1159 行）于 2026-08-20 按语义拆分而来 · 证据仓 `1c622c8` |
| 原文 | `${XM}/docs/next/ARCHITECTURE.md`（**冻结，不再更新**；本文件是唯一在演进的版本） |
| 索引 | [docs/architecture/README.md](../architecture/README.md) |

> **章节编号保持原样**（§1、§7、§11.2 …），因为主计划的 `REF:` 表按标题原文定位。**改标题等于断链**，要改先改 [REF 表](../masterplan/README.md)。

---

## 10. 视图 DSL 与渲染器

### 10.1 为什么需要 DSL

三条路的比较：

| 方案 | 成本 | 天花板 | 「每家不一样」 |
|---|---|---|---|
| A. 留在 Desk 改 Workspace | 最低 | 只能放链接/卡片/图表 | 未解决 |
| B. frappe-ui 手写页面 | 中 | 高 | **未解决**（每加一个视图要写代码） |
| **C. DSL + 渲染器 + LLM 生成** | 高（一次性） | 高 | **解决** |

C 让「UI 太不直观」和「每家公司流程不一致」变成同一个问题的同一个解。

### 10.2 v0 只支持五种块（只读）

| 块 | 用途 |
|---|---|
| `list` | 列表 + 筛选 + 排序 |
| `detail` | 单据明细 |
| `metric` | 数字卡，**必须支持基准与对比** |
| `chart` | 图表 |
| `explain` | **解释性文本块** —— 回答「为什么这单还是待出货」，Workspace 永远做不到 |

**写操作 v0 不做，全部留在 Desk。** 理由：业务管理端本来就只读，风险最低，而「看不懂」是当前最大痛点。

### 10.3 渲染器职责边界

- 渲染器**只负责画**，数据与权限全部走 Frappe API 由后端强制
- 前端权限只做提示（「你看不到这个，因为…」），**绝不做判定**
- 未支持的块/字段类型 → 落回 Desk

#### 🔴 术语层 v0 已落地（P2.7，2026-08-27）

**基线是零，不是少**：活站点导出实读，业务 app 全量 **6,350 个字段，label 含中文的 = 0**。
`Item Reorder.warehouse` 的 label 逐字是「**Request for**」—— 字段名说仓库、标签说请求给。

落地：`agenerp/i18n/terms.zh.json`（本地 Ollama `qwen3:14b` 生成，**API 成本 0**）+
`agenerp/i18n/load_terms()`。车间工人覆盖面 **317/317 = 100%**，渲染器表头已换成中文。
判据 `tests/i18n/`（`pytest tests/i18n -q` 退 0，12 passed）。

🔴 **这套判据的上限，必须和它一起读**：它守得住的全是**规则面** ——
覆盖率 · 逐条指回真实字段 · 含中文 · 不抄英文 · 不抄 fieldname · 不带单据名前缀 ·
`Check` 字段读成是非说法。**它验的是「有没有离谱地错」，不是「翻译得好不好」。**

后者没有规则面的判法，而硬约束 ③（D-15）反过来也成立：**不许让模型判自己的产物。**

⚠️ **这个上限有多低，有实测**：第一版用 `qwen3.5:0.8b`，T1/T2/T3 **全过**，
而 `Item.has_batch_no`（一个勾选框）被起成「**批次号**」、`Item.item_code` 被起成
「**项目代码**」（制造业语境该是物料）。**T3 全中却漏掉了它们。**
补了一条 `Check` 类型的规则判据才抓出来 —— 当场显示 52 个 Check 字段里
只有 38.5% 读成了是非问句。换 `qwen3:14b` 后修正为「是否批次管理」「物料编码」。

⇒ **用它之前先知道：判据绿不代表译得对。**

⚠️ 只覆盖路线 C 的 317 个字段，整站 6,350 个未做。

---

#### v0 已落地的形状（P2.1，2026-08-26）

代码在 `agenerp/dsl/`，判据在 `tests/dsl/`（`pytest tests/dsl -q` → **42 passed，退出码 0**）。

| 文件 | 管什么 |
|---|---|
| `blocks.py` | 声明格式（`View` / `Block`），`BLOCK_TYPES` / `AGGREGATES` / `CHART_KINDS` / `FILTER_OPERATORS` 全是**封闭元组** |
| `schema.py` | `SchemaView` —— 校验器问「这个字段存在吗」的那个对象，**注入进来的只读快照，本层不连站点** |
| `validate.py` | **两层校验器**：L1 结构 + 🔴 L2 字段存在性 |
| `fallback.py` | 「未支持的一律落回 Desk」，**封闭表驱动，源码里没有任何模型入口** |

🔴 **三条硬约束在这一版的落点**：

1. **「DSL 校验过 ≠ 字段真的存在」**（roadmap 硬约束 ①/④）——
   L2 逐条问 `SchemaView`，且**按块自己的 `doctype` 查**，不是「这个字段名在整个
   schema 里出现过吗」。后者正是 P2.0R 实测出的头号错法：**语义对了，单据错了**
   （`qty` 在 `Sales Order Item` 上真实存在，但不在 `Sales Order` 上）。
2. **`validate(view, None)` 抛 `SchemaUnavailable`，不返回 ok。**
   验不了的东西不许算过 —— 这是 P1 那个 `or` 让空答案照过的直接对策。
   空的 `SchemaView` 是「站点什么都没有」，**不是**「别查了」。
3. **落回判定是规则面**（D-15 / 硬约束 ③）。判据里有一条**读 `fallback.py` 源码**
   的静态断言，确认它不含任何模型/路由入口 —— 读的是纸面上的字，不是自报。

**`validate` 严格、`plan_render` 兜底，两者管的不是同一件事**：前者管「作者写对没有」
（不认识的块类型当场拒），后者管「这一版渲染器画得了没有」（读到比自己新的 DSL
时**落回 Desk**，不炸、不静默丢弃）。落回是**块粒度**的，且**每一次都带原因**。

**判据有牙，四条变异逐条验过**（D-26，不许只验绿）：把 L2 改成恒真 → **6 条红**；
把未知块类型改成静默忽略 → 3 条红；把块类型集合放开 → 1 条红；
把「拿不到 schema」改成返回通过 → 1 条红。**复原后三个实现文件 `sha256` 逐字节相同。**

⚠️ **`metric` 的「基准与对比」v0 只落声明位（`baseline`），不求值** —— 求值要连站点，属 P2.2。

---

### 10.4 行为不可配置 —— 视图 DSL 之外的独立缺口

实测（`demo.localhost` / Frappe v16.31.0）：ERPNext 的**页面**能被无代码搭出来，**行为**不能。

| 用户想要的 | 载体 | 无代码？ |
|---|---|---|
| 菜单 | `Workspace` 记录（`parent_page` / `sequence_id` / `roles`） | ✅ |
| 新表、子表 | `DocType`（`custom:1`） | ✅ |
| 字段 | `Custom Field`，43 种 fieldtype 可选 | ✅ |
| 按钮 | `Button` fieldtype | ✅ 加得上 |
| **按钮点了干什么** | **Client Script / Workflow** | ❌ **要写 JS** |
| **校验、自动计算、跨单据联动** | **Server Script** | ❌ **要写 Python** |

原生的无代码行为通道只有四条：Workflow、Notification、Assignment Rule、Auto Repeat。校验、自动计算、按钮动作、跨单据联动都不在其中。

**附带发现**：`custom: 1` 的 DocType **绕过 developer_mode**（Frappe `DocType.before_insert` 中对 `custom=1` 短路了 developer_mode 校验）。站点 `developer_mode` 关闭时，形态 Agent 仍能在生产站点建整张新表 + 整套页面——但 `export_customizations` 要求 developer_mode（§11.1）。**能建，导不出**：L3 动作可以造出治理体系看不见的东西。这比 §11.1 的 revert 缺口更靠前一环，待 Spike 12 收口。

→ 对 §10.1 那张 A/B/C 表的补充：**即使接受 Workspace 的天花板，行为层仍然无解。** 视图 DSL 若只覆盖呈现，定制包就只治理了一半。对策见 §10.5。

### 10.5 行为层：不做运行期编码 Agent

Frappe 的 Server Script 并非任意代码执行——它跑在 RestrictedPython 沙箱里（`frappe/utils/safe_exec.py`）：`compile_restricted` + 自定义 `FrappeTransformer`（:133）、白名单全局、`UNSAFE_ATTRIBUTES` 封死帧/生成器/traceback 内省（:776）、`frappe.db.sql` 被换成只放行 `select`/`explain` 的 `read_sql`（:724）。且 `server_script_enabled` 默认关闭。

**但沙箱防的是越狱，不防在权限内做错事**——而后者才是 ERP 定制的主要死法。因此运行期不引入编码 Agent：

| | 编码 Agent | 行为 DSL |
|---|---|---|
| 表达力 | 高 | 覆盖大部分「防错」需求 |
| 可 diff / revert | ❌ 两段脚本的语义差不是文本差 | ✅ |
| 人批 L2/L3 时 | 人得读代码 | 读一句声明 |
| 注入面 | 大 | 枚举内 |
| **工程量** | **小** | **大（要写解释器）** |

诚实记录这个取舍的形状：**编码 Agent 是便宜的那条路。** 它与 §10.1 的 A/B/C 完全同构，而当时选了贵的 C。行为层若选便宜的，会让 C 的投入白费——视图可治理、行为不可治理，定制包只覆盖一半，§11 的承诺整体失效。

**构建期编码 Agent 另论**：面向 ① 系统管理端、产物进 git 走 PR 人审、不自动 apply 到运行时。这类工具已经存在（通用编码助手），**不属于 AgenERP 的交付范围**。要决策的只有运行期那一个。

**逃生舱**：Client Script 风险显著低于 Server Script——它跑在浏览器端，后端仍会再判权限与校验，改不了数据的合法性；代价是能篡改用户所见（→ §7.5）。**即便开放也只作为人工逃生舱：由人写、走 git，Agent 不生成。**

行为 DSL 的边界与最小动作词表需专门设计，**不早于 P2 跑通**——届时才有真实需求清单可反推动作词表。

---

## 13. 状态快照与评测

### 13.1 state-diff 判定

Agent 动作前后各打一次快照，客观判定「改对了没有」——不看轨迹像不像，看**状态是否变成预期的样子**。

现有 `runtime_snapshot()` 已是雏形，输出包括：GL 借贷与差额、负库存数、各 DocType 已提交数、毛利构成。需扩展为：任意时刻打快照 + 两快照 diff + 断言 DSL。

### 13.2 关键局限（必须写进文档，不能自欺）

实测案例：系统自制入库 1,000 米 + 外协收货 1,000 米 = 2,000 米，发货 990 米，成品仓结余 **1,010 米、价值 6,450 元**。

而所有自动化门禁**全部通过**：GL 借贷平衡 ✅、负库存 0 ✅、毛利与凭证差额 < ¥0.01 ✅。

> **state-diff 能保证账是平的，保证不了业务是对的。**

Agent 一旦自动推单据，犯的恰恰是这种错：**每一步都合规，合起来毫无道理**。

→ 因此评测必须有第二层：**行业包提供的业务合理性规则**（如「成品库存无对应订单且 > N 天 → 异常」）。这也是洞察 Agent 存在的意义。

**Spike 08 把这一条推进了一步：不只是 state-diff 保证不了，自由巡检的 LLM 也保证不了。**

给老板身份、34 个业务 DocType 只读权限、开放式指令「你自己找异常」，Agent 找到了
应收逾期 ¥18,612 与应付逾期 ¥2,200（都对、都有用），**但漏掉了 1,010 米积压**，
并主动把该区域判为「✅ 正常」，依据是 `achievement_rate = 100%`。

它同时诚实标注了「库存/Stock Entry 明细未逐条核对」。**它不是查错了，是不知道该去查。**

→ 结论：**1,010 米积压就是行业包规则的 test case #1。** 且 P1 的验收标准应写成可归因的形式：

> 行业包 v0 声明「成品库存无对应订单」规则后，洞察 Agent 无需指令即能报出该异常；
> **并且移除该规则后能复现漏报**——以此证明是规则在起作用，不是模型碰巧猜到。

---
