# P1-7 逐条对照 `predictions.md`（吻合/不吻合逐格照实记，不吻合的保留不修饰）

> ⚠️ `predictions.md` 自 2026-08-26 10:12 写死后**一个字未改**（`git log --follow` 可核）。

| 预测 | 结果 | 判定 |
|---|---|---|
| **1a** 判定步形如「门禁 N 项：红 R，绿 G，跳过 S」，R ∈ {1,2}，跳过恒 0 | 10 个 run 全是 `门禁 54 项`，R = 2 / 1 / 1，跳过全 0 | **吻合** |
| **1b** 红的判据名只落在那两条内 | 三次红共 4 条判据名，全部落在那两条内，无第三条 | **吻合** |
| **1c** 异常是 `TimeoutError`，抛点在 `recv_into` / `readline` | `758b7bc` 与 `82a144a` 逐字相同：`socket.py:718 recv_into` ← `client.py:298 readline`，`TimeoutError: timed out` | **吻合**（⚠️ `e3afd77` 无此数据 —— `失败取证` 步是 `758b7bc` 才引入的，`git log -S"失败取证"` 实证） |
| **1d 正侧** 红 run 有 `Up` < `CREATED`、绿 run 没有 ⇒ 方向 ③ 坐实 | **不成立** | **不吻合** |
| **1d 反侧** 绿 run 同样出现 ⇒ 不足以单独解释红 | **成立，10/10 同形**，且同一 sha 的绿 attempt 也同形 | **吻合（命中反侧）** |
| **1d 第三种可能** ps 不带 `CREATED` 列 ⇒ 取不到 | 未发生，`CREATED` 列一直在 | **未触发** |
| **（预测未覆盖）** | 🔴 **那个差值根本不是重启**：CI backend 日志 10/10 只有一套启动序列 · 本机 `RestartCount` 全 `0` · `Up` 落后的那组恰好等于（传递地）等 `create-site` 的那组 | **预测未覆盖，照实新增** |
| **2a** 从 `agenerp-serve` 最近 60 行找到请求起止时间戳 | **找不到** —— 该容器日志逐行只有 `POST /agenerp/explain` 之类，**无时间戳** | **不吻合** |
| **2b** 若找得到，单次真解释 > 30s | 前提未成立 | **未触发** |
| **2c** 若 B3 被证伪，预期看到「墙钟 < 30s 而判据仍红」 | **未看到**（红的那次确实 > 30s）。⚠️ **但预测的二分法本身过窄**：实测是**同时**出现「正常那些次 ≪ 30s」与「红那次 > 30s」 ⇒ **「解释系统性地慢」这个前提被证伪，而「回包超时」这个形态成立** | **部分不吻合 —— 预测把两件事绑成了一件** |
| **2d** 可能拿不到时间戳 ⇒ 写「未测出」 | **命中**，改由步级墙钟反解（`p1-3` 给出算式与假设） | **吻合** |
| **（预测未覆盖）** | 🔴 **服务端侧的 `BrokenPipeError`**（`app.py:377`，栈帧在 `_respond(200, payload)` 之后），次数与红的条数逐次相等（1↔1、2↔2） | **预测未覆盖，照实新增 —— 这是本轮最重的一条证据** |
| **3a** 本机没有模型密钥 ⇒ 发不出真解释 | **不吻合**：宿主 `env` 与 `.env` 里确实没有，**但本机 `agenerp-serve` 容器内三件套全非空**（`docker inspect` 实取）。**写预测时只看了宿主、没看容器，是我漏了一格。** | **不吻合** |
| **3b** 本机能拿到 `RestartCount` / `StartedAt` / `Health` | 13 个容器全部取到 | **吻合** |
| **3c** 本机会复现「起栈后再重启一次」 | **不复现**，`RestartCount` 全 `0`；按反侧预期写成「本机不复现」，未写成「被证伪」 | **不吻合（反侧口径已按预测执行）** |
| **4a** ps 里看得到起动次序线索 | 看得到，且与 compose 依赖图逐一对上 | **吻合** |
| **4b** `agenerp-serve` 起得比 `backend` 早，但不造成红 | **前半句不吻合**：两者是 `create-site` 同一道闸上的平级容器，10 个 run 的 `Up` 值逐次**完全相同**，不存在「早」。**后半句吻合**：不造成红 | **前半不吻合、后半吻合** |
| **4c** 若红是 502 / `BadStatusLine` 则起栈时序坐实 | 未发生 | **吻合（反侧未触发）** |
| **5a** P1-6 在探路闸内跑不完（预期红 ⇒ 每次 4 次真解释，两次 8 > 6） | **不吻合**：两次重跑**都绿**，每次只花 **2** 次 ⇒ 合计 4 次，**闸内跑完了**。**我把「预期红」当成了必然。** | **不吻合** |
| **5b** 重跑预期红 | **2 次全绿** | **不吻合** |
| **6a** 三路径逐对多为空 | **9 对全空** | **吻合** |
| **6b** backend 日志有 `Linking fresh assets` 线索但给不出根因 | 命中，且它反过来成了「**没重启**」的证据 | **吻合（且方向相反地更强）** |
| **6c** 第四机制查不出具名的一条，结论「无」 | 结论「无（说明查过哪些）」，逐条列了 5 个候选 | **吻合** |
| **7a** 至少有一格不吻合 | **7 格不吻合、2 格预测未覆盖** | **吻合** |
| **7b** 预期给不出完整机制陈述，Phase 1 以「证据不足」+ 停机（C 或 D）收尾 | **不吻合**：证据比预期强，**机制陈述给得出来**（`p1-8`）。Phase 1 **以完成收尾**；停机发生在 **Phase 2**，且是**分支 A**（唯一修法落在红线 1），不是分支 D | **不吻合** |

## 一句话总结

**预测里最保守的那几格（1d 反侧、2d、6a、6c）全中；最自信的那几格（3a 本机没密钥、5a/5b 重跑必红、7b 拿不到机制陈述）全错。**
两条**完全没预测到**的证据（服务端 `BrokenPipeError`、以及「重启」根本没发生）**恰好是本轮承重最多的两条**。
