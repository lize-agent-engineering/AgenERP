# Development Log - 2026-08-22

### 2026-08-22 · plan `2325-1` Phase 1–2 —— `ACC_OPERATING` 修好了，`strip_abbr` 从「容忍」改成「失败即停」

- **Phase 1（先立判据）**：新建 `tests/unit/test_seed_model_constants.py`（3 条），
  对 `model.py` 里全部 `ACC_*` / `WH_*` 常量做机械核对——**遍历模块属性取清单，不手抄**，
  且**不经由 `strip_abbr` / `site_name_of` 求值**（拿容忍它的那段代码开证明会空转）。
  修复之前实测 `python3 -m pytest tests/unit -q` → **exit 1**，`3 failed, 285 passed`，
  新判据的失败信息逐字点名 `ACC_OPERATING = '生产费用（计入估值）- XM' 不以 ' - XM' 结尾`。
- **两条钉住缺陷本身的单测是改写，不是删除**：`test_account_name_mismatch_is_reported_not_swallowed`
  与 `test_the_mismatch_check_is_silent_for_every_other_constant` 输入换成**测试内构造的畸形 `Step`**
  （`monkeypatch` 掉 `plan_steps`），修复前后**都绿**；「真实产品数据零 mismatch」那半覆盖
  **拆成净新增的第三条** `test_the_real_master_data_plan_reports_no_mismatch_at_all`（修复前红、修复后绿）。
  **覆盖面只增不减**：2 条 → 4 条（含 `strip_abbr` 抛异常那条）。
- **⚠️ 与 plan 预期红条数的偏差照实记**：plan Phase 1 第 5 项预期红 4 条，实测 **3 条**。
  差异全在「改写中的两条覆盖」这一类——同 Phase 的 Exit Criteria 逐字要求它们「修复前后都绿」，
  两处要求互斥时按 Exit Criteria 执行。**没有改判据去凑**，覆盖拆分见上一条。
- **Phase 2（修）**：`agenerp/seed/model.py:60` 只加一个空格 →
  `生产费用（计入估值） - XM`；`agenerp/seedsite.py` 的 `strip_abbr` 改成不匹配 ` - {ABBR}` 即
  `raise ValueError`（异常信息含那个串本身、所要求的形状 `<name> - XM`、以及机械判据的路径）。
  `Decision` 的三个候选与残余风险写进 `docs/architecture/module-boundaries.md` **§12.11**（新增段）。
- **`LoadReport.mismatches` 一行未动，刻意保留**：它是「站点回名 vs 本仓预期」的通用对账，
  不是为这一个常量造的；§12.11 写明它此刻**没有已知的活触发点**以及它仍然值得留着的理由。
- **§12.9 那条现在时陈述按三句分开处置，原文一句不删**：①「永远命不中」→ 加改准块（现已为假）；
  ②「报告不静默但不退非 0」→ **仍然为真**，只补「机制保留、无活触发点」的准确说法；
  ③「本次不修」→ 历史陈述为真，**不加改准**，只加指向本 plan 的前向指引。
- **Phase 2 收尾复跑（全绿）**：`python3 -m pytest tests/unit -q` → **exit 0**（`288 passed in 0.61s`，
  不低于修复前的 283）· `ruff check agenerp tests/unit tests/contracts` → **exit 0** ·
  `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 19 项：预期红 7，绿 12，跳过 0`）·
  `python3 -m agenerp.seed --seed 42 --verify` → **exit 0**。
  红线自查：`git diff --numstat -- tests/gates .github/workflows docs/masterplan/DECISIONS.md` **无输出**。

### 2026-08-22 · plan `2107-2` **独立关闭审计** —— 两条 blocking，其中一条是执行者当场引入的真缺陷

- **审计方**：独立关闭审计子代理（fresh session，agent `a091942e83e9a237d`），**非本 plan 的执行者**。
  方法是不读执行者的结论当证据，在活仓与活站点（18080）上逐条复跑，并**自选四条变异**做证伪尝试。
- **复跑退出码（与执行者所记一致）**：`python3 -m pytest tests/unit -q` → **0**（`283 passed in 0.66s`）·
  `ruff check agenerp tests/unit tests/contracts` → **0** · `python3 -m agenerp.seed --seed 42 --verify` → **0** ·
  `… --verify-site --site frontend` → **0**（`9 项，通过 9，失败 0`）·
  `… --load-documents --site frontend`（**第三跑**）→ **0**，`合计：新建 0 / 已存在 17 / 提交 0`
  —— **幂等在审计侧独立复现** · `AGENERP_LIVE=1 … check_expected_red.py` → **0**，`门禁 19 项：红 0，绿 19，跳过 0`。
- **审计方自选的四条变异全部拿到「转红 → 点名 → 复原 → 转绿」**：① `_close` 容差改 `1e9` → `3 failed`；
  ② `CH.EXPECTED_BACKLOG_VALUE` 换成字面量 → `2 failed`，点名两条防作弊断言；
  ③ **专挑执行者自己写明的那处盲点**（裸整数 `"basic_rate": 5`）→ `1 failed`，点名
  `test_manufacture_entries_never_send_the_finished_item_rate` —— **那处盲点实际上被结构断言兜住了，
  执行者当时把覆盖面写得比实际保守**；④ 复现 R3 ③ → `4 failed, 279 passed`，与 plan 所记逐字一致。
- **⚠️ blocking ① · 执行者把 plan 文件弄坏了 382 行，照实记，不是一路顺过来的**：
  收尾勾 Closure Gates 时用了一处**下标弄反**的脚本编辑（`s[:a] + s[b:]` 在 `b < a` 时会**复制** `[b, a)`），
  结果 `## Closure Gates` 那个标题被写成 `## Deferred But Adjudicated\``，
  从 front matter 尾部到 `## Draft Review Record` 的整段被复制了第二份（含四个 Phase 的全部勾选），
  文件 495 → **917 行**。**自动检查器看不见这种重复**（它只数复选框），是审计逐节读全文时撞上的
  —— 与 plan `2107-1` 关闭审计撞到的那次**同一形态**，值得记进教训面。
  **已修复**：删重复段、恢复标题，文件回到 **535 行**；机械核对
  `diff <(git show HEAD:<plan> | 抹掉勾选与 Status) <(现文件 | 同样抹掉)` **只剩 `## Closure` 段的填写**，
  **没有任何一条 gate 文本被改写**；章节结构与 HEAD 逐字一致；`## Deferred But Adjudicated` 出现次数 **1**。
- **blocking ②**：`text consistency verified` 在缺陷 ① 存在时被勾了。已随 ① 的修复重新成立并重新机械核对。
- **非阻塞三条，审计判为「说法比实际强/窄」，已就地记准（改说法，不改判据）**：
  ① **「9 项」是打印行数，独立约束只有 8 条** —— 第 7 项「毛利」= 第 5 项 − 第 6 项，
  而 `EXPECTED_GROSS_PROFIT` 本身就是 `18612 − 4950`，故它不可能在前两项都绿时单独发红；
  ② **`EXPECTED_RECEIVABLE_OVERDUE` 被复用在了它声明含义之外的一处**（声明是「应收**逾期**合计」，
  第 5 项拿它当「GL 收入贷方合计」的期望；两者此刻相等只因那张发票 100% 未收款）——
  **没有违反本 plan 的要求**（不得引入新的期望值字面量，此处确实没有），但 `出处` 标注比实际含义窄；
  ③ **「派生量零命中」的可判形式是 `M.<NAME>` / `model.<NAME>`，不是裸名** ——
  裸名 `grep` 在 `agenerp/seedsite.py` 里**有命中**（三处 `CH.EXPECTED_*`，全在**对账侧**）。
  三条各留一份在 plan 的 `## Status Note` 第 5/6/7 条与 `module-boundaries.md` §12.10 末尾，
  并改准了 `agenerp/seedsite.py` 里那条自己写错口径的注释。**Closure Gate 的文字一个字未改**
  —— 改判据去迁就实现是本 plan 逐字禁止的。
- **裁定：接受关闭。** 修复后收尾复跑：`python3 -m pytest tests/unit -q` → **exit 0**（`283 passed`）·
  `ruff check agenerp tests/unit tests/contracts` → **exit 0** · `python3 -m agenerp.seed --seed 42 --verify` → **exit 0** ·
  `… --verify-site --site frontend` → **exit 0**（`9 项，通过 9，失败 0`）·
  `AGENERP_LIVE=1 … check_expected_red.py` → **exit 0**（`红 0，绿 19，跳过 0`）·
  `python3 tools/gates/check_expected_red.py`（默认判定环境）→ **exit 0**（`预期红 7，绿 12，跳过 0`）。
  **verification scope limited 这句话保留**——上列命令全部 scoped，其中活站点那几条 `GATE_VERIFY` 复跑不到；
  本 plan 交付的行为仍**没有属于自己的门禁**，站点侧三条 L2 断言仍是提案文本，采纳者是人。

### 2026-08-22 · plan `2107-2` Phase 4 —— owner doc 对齐 + 把门禁提案交回给人

- **`module-boundaries.md` §12.7 就地改准**：补「改准二」——`--verify-site` 已落地，站点侧那三条断言
  现在有了**可执行形式**（CLI 退出码 + 三条变异实测）。
  **但上一段那句「站点侧那一半仍然没有门禁」原样保留、未删** ——
  它是一条 CLI 退出码，不是 `tests/gates/**` 下的测例，`GATE_VERIFY` 与 CI 都复跑不到它。
  **没有写「工作项 7 已有站点侧门禁」——那是假的。**
- **§12.8 就地补一条本仓站点的实读事实**：`Stock Settings.valuation_method = "FIFO"`、
  `allow_negative_stock = 0`、`Item[XM-LACE-1000].valuation_method = ""`；
  同站点由站点自己算出 `1010.0 米 / ¥6,450.00`（`valuation_rate 6.386138614`）。
  **原来那条「冻结证据仓那个站点的计价方法仍是推断」明确保留并加了一句「不得写成已闭合」**
  —— 红线 6 下本仓取不到它的新证，两者是两个站点，不要合并读。
- **新增 §12.10「单据装载与站点侧对账在本仓的落点」**，含六节：选定的单据链与**被否掉的两条**
  （收窄链 / ERPNext v15 原生外协链，后者是实测撞死的 417 原文）· **站点算了 §12.1 哪 6 段、
  喂进去哪 7 类输入的逐字列举** · 幂等口径（含那张 DocType → 幂等键的表，以及补提交那一格）·
  干净站点循环为什么是强制前置（含 `bench backup` 的字节数）· 达成率那一项为什么算不出来 ·
  判据归属与验证范围。**两条结论强度限定写在里面**：外协批的 DocType 不同、工序成本由装载器搬运了一次。
- **`project-context.md` 验证命令表新增一行**「种子数据站点侧对账」，四条口径齐备
  （端口 18080 / **干净站点循环是强制前置且 `down -v` 丢整站数据** / env 三个变量由命令给 /
  **不在 `commands.test` 里**），并写明它是 CLI 退出码不是门禁、达成率那项不在 9 项里、
  以及 `Overdue` 依赖真实时钟。
- **门禁提案 `gate-proposal-seed-dataset.md` 逐条标注三条拟断言的现状**（`Status: proposed` **未改**，不代人采纳）：
  ① 与 ③「实现已就绪，站点上实测通过，期望值 = 实测值」，附实测数与两条变异证据；
  ② **按 D2 拆开标注** —— 三项可查（`0.00` / `0` / `13662.00`），
  **达成率那一项写明「当前口径下站点算不出来」及其原因**（`Loss Review` 是本仓虚构 DocType，
  站点实测 `per_delivered = 99.0`，建它要发 DDL），并给人列了三个可选处置。
  **否则人采纳提案时会拿到一条注定红的门禁。**
- **roadmap 工作项 7 处只追加一条现状行**：`git diff --numstat` → `19  0`，**删除列为 0**，
  一个字未改写既有行。内容含：B 半两段均已落地 · 判据形态是 CLI 退出码而非门禁 ·
  达成率那一项的裁定 · 外协批 DocType 的限定 · **工作项 7 仍保持 `planned`** 及其真实卡点
  （L1 门禁从未进过名单，「划掉」没有对象）。**未自行改工作项状态值。**
- **Closure Gates 机械核对，逐条实跑**：
  `git status --porcelain tests/gates tools/gates/expected-red.txt` → **0 行** ·
  `git diff --stat agenerp/seed/model.py agenerp/seed/checks.py tests/gates/test_seed_dataset_absurdity.py` → **0 行**（三份期望值副本一个未改）·
  `grep -c "M\.\(BACKLOG_QTY\|…\|SUBCON_VALUE\)" agenerp/seedsite.py` → **0**（八个派生量在装载输入侧零命中，
  取 Closure Gate 二选一里的 **(i)**）· `document_steps` / `load_documents` 段内 `CH.` 命中 → **0**
  （期望值只出现在对账侧）· `.github/workflows` / `missions` / `DECISIONS.md` → **0 行** ·
  `STATE.md` `git diff --numstat` → `14  0`（纯追加）· 工作项 7 仍逐字 `planned`。
- **`closure audit was independent` 这一格未勾**：它不由执行者自证，留给独立关闭审计填实。

### 2026-08-22 · plan `2107-2` Phase 3 —— **站点自己算出了 1,010 米 / ¥6,450**（R1–R5 实跑）

- **首次 `down -v` 之前的备份，字节数照抄**（plan Infrastructure 的硬要求）：
  `docker compose exec -T backend bench --site frontend backup` → exit 0，
  `20260822_201846-frontend-database.sql.gz` **832,967 B**、
  `20260822_201846-frontend-site_config_backup.json` **94 B**（容器内 `ls -l` 实读）。
- **R1 · 干净站点循环**：`down -v` → exit 0；`up -d --wait --wait-timeout 300` → **exit 0，59.1 秒**。
  装载**前**取本机基线：`AGENERP_LIVE=1 … check_expected_red.py` → **exit 0**，`门禁 19 项：红 0，绿 19，跳过 0`。
  随后 `--load-masters` → **exit 0**（`合计：新建 40 / 已存在 0`，含那条 `ACC_OPERATING` 名字不符告警，未被静默）；
  `--load-documents` → **exit 0**，`合计：新建 17 / 已存在 0 / 提交 11`。
  **原样再跑一次** → **exit 0**，`合计：新建 0 / 已存在 17 / 提交 0` —— **幂等的判据是第二跑的计数，不是「没报错」**。
- **R2 · 承重判据：`python3 -m agenerp.seedsite --verify-site --site frontend` → exit 0，9 项全过。**
  站点自己算出来的两个数，原样抄在这里：
  · `✅ Bin(XM-LACE-1000, XM 成品仓 - XM).actual_qty = 1010.00 / expected = 1010.00（出处：agenerp.seed.checks.EXPECTED_BACKLOG_QTY）`
  · `✅ Bin(XM-LACE-1000, XM 成品仓 - XM).stock_value = 6450.00 / expected = 6450.00（出处：agenerp.seed.checks.EXPECTED_BACKLOG_VALUE）`
  另七项：`GL 借贷差额 0.00` · `负库存条目数 0` · `GL 收入贷方 18612.00` · `GL 成本借方 4950.00` ·
  `毛利 13662.00` · `Sales Invoice 逾期合计 18612.00（ACC-SINV-2026-00001）` ·
  `Purchase Invoice 逾期合计 2200.00（ACC-PINV-2026-00001）`。
  **本仓一行 `Bin` / `Stock Ledger Entry` / `GL Entry` 都没构造** —— 这三类行全部由站点自己产生。
  **门禁提案的拟断言 ① 与 ③ 到此在站点上实测成立**，② 的三项站点算得出来的也全绿；达成率那一项按 D2 不在里面。
- **R3 · 三条变异验证，每条都从干净站点重来，逐条留痕**：
  · **① 发货量 990 → 980**（`agenerp/seed/model.py:34`，Non-Goals 写明的唯一例外，临时实验）。
    冷起 `down -v` → 0 / `up --wait` → 0，`--load-masters` → 0，`--load-documents` → 0。
    `--verify-site` → **exit 1**，`站点侧对账：9 项，通过 3，失败 6`，第一条逐字
    `❌ Bin(XM-LACE-1000, XM 成品仓 - XM).actual_qty = 1020.00 / expected = 1010.00`
    —— **站点实得 1020、期望 1010，与 plan 预写的一模一样。**
    同轮 `--seed 42 --verify` → **exit 1**（`销售订单达成率应为 100%…实为 99.00%`），**这是预期的**，
    **没有据此放宽任何断言，也没有把任何门禁加进 `tools/gates/expected-red.txt`**。
  · **② 外协服务费 2200.0 → 2100.0**（`agenerp/seed/model.py:42`）。冷起 → 装载 → `--verify-site` → **exit 1**，
    `站点侧对账：9 项，通过 7，失败 2`，逐字
    `✅ …actual_qty = 1010.00`（**数量仍对**）/ `❌ …stock_value = 6350.00 / expected = 6450.00`
    —— **红在金额那一条，不是红成一片**，说明判据分得开。
    同轮 L1 门禁 `tests/gates/test_seed_dataset_absurdity.py` → `1 failed, 5 passed`，
    点名 `test_backlog_absurdity_is_present_and_exact` —— **预期，且 `tests/gates/**` 一个字节未改。**
  · **③ 删掉 `--verify-site` 的 `stock_value` 断言**（纯逻辑，不起站点）→ `tests/unit` **4 failed, 279 passed**，
    点名 `test_verify_site_passes_on_the_numbers_the_plan_exists_to_prove` /
    `test_verify_site_prints_the_actual_value_and_the_expected_value_with_its_source` /
    `test_verify_site_goes_red_when_the_site_computed_a_different_backlog` /
    `test_verify_site_takes_every_expectation_from_checks_and_never_from_a_local_literal`。复原后 `283 passed`。
  · **代码复原不等于站点复原**：①② 复原后**又跑了一次完整干净站点循环**（`down -v` → 0、`up --wait` → 0、
    `--load-masters` → 0、`--load-documents` → 0），`--verify-site` 回到 **exit 0，9 项全过**。
    `git diff --stat agenerp/seed/` **无输出**（三条变异的代码痕迹清零）。
- **R4 · 计价条件留痕**：本仓 `frontend` 站点实读 `Stock Settings.valuation_method = "FIFO"`、
  `allow_negative_stock = 0`；`Item[XM-LACE-1000].valuation_method = ""`（空 → 回落全局）。
  **`¥6,450` 是在这个 `valuation_method` 下由站点实算成立的。** 装载后四条 `Bin` 原样：
  `XM-LACE-1000 / XM 成品仓 - XM → 1010.0 / 6450.0 / 6.386138614` ·
  `XM-YARN-WHITE / XM 原料仓 - XM → 60.0 / 2100.0 / 35.0` · 在制品仓与外协仓各 `0.0 / 0.0 / 35.0`。
  ⚠️ **不写「§12.8 已闭合」**：那条推断说的是**冻结证据仓**那个站点（红线 6，只读），本仓取不到它的新证。
- **R5 · 不弄脏既有判据**：装载**前**（R1 第二步）与装载**后**各一次
  `AGENERP_HTTP_PORT=18080 AGENERP_LIVE=1 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin python3 tools/gates/check_expected_red.py`
  → **两次都 exit 0**，逐字 `门禁 19 项：红 0，绿 19，跳过 0` / `✅ live 判定：全部门禁绿，零 red、零 skip`。
- **收尾复跑**：`python3 -m pytest tests/unit -q` → **exit 0**（`283 passed`）·
  `ruff check agenerp tests/unit tests/contracts` → **exit 0** · `python3 -m agenerp.seed --seed 42 --verify` → **exit 0**。
  红线自查：`tests/gates` / `tools/gates/expected-red.txt` / `agenerp/seed` / `.github/workflows` /
  `missions` / `docs/masterplan/DECISIONS.md` 六条路径 `git status --porcelain` **零行**。

### 2026-08-22 · plan `2107-2` Phase 2 —— 单据装载段 + 站点侧对账命令（纯逻辑半落地）

- **`SiteClient.submit_doc(doctype, name)`**：`PUT /api/resource/<dt>/<name>` 带 `{"docstatus": 1}`。
  **回值 `docstatus` 不为 1 时主动抛 `SiteError`**——200 但没提交上去与提交成功长得一模一样，
  那正是本模块第 1 条约束（不伪装成功）要挡的形状。
  名字里含 `submit`（在 `WRITE_VERBS` 里），**扫描守卫看得见它**，仍逐条登记进
  `tests/unit/test_site_client.py` 的 `WRITE_METHOD_ALLOWLIST`（写面 3 条 → **4 条**）。
  ⚠️ **它比另外三个更进一步：不可逆**——本模块没有 cancel/amend，提交过的单据在站点侧回不去。
- **`agenerp/seedsite.py` 新增单据段**：`document_steps()` 是**纯函数**（不联网、不读环境），
  站点才知道的值以占位符（`{{sales_order}}` / `{{so_detail}}` / `{{work_order_*}}` /
  `{{delivery_note}}` / `{{operating_cost}}`）出现在载荷里，由 `load_documents` 边跑边绑。
  **`--load-masters` 的既有行为一个字节未动**：两段的 DocType 集合交集为空（有单测钉死）。
- **工序附加成本是从站点读回来的，不是本仓算的**：`operating_cost_per_unit()` 读回
  站点自己算的 `BOM.operating_cost / BOM.quantity`，再按 **ERPNext 自己那一行**
  （`bom.py:add_operations_cost` → `operating_cost_per_unit * fg_completed_qty`，容器内实读）换算成整批。
  单测把这条钉死：**站点把 `operating_cost` 改成 1234，送出去的附加成本必须跟着变**。
- **⚠️ 单测当场抓出一处真错，照实记**：起草时把 `{{operating_cost}}` 绑成了**每单位**成本（0.8），
  而载荷那一格要的是**整批**（800）。`test_the_operating_cost_comes_from_the_site_not_from_this_repo`
  直接判红（`assert 1.234 == 1.234 * 1000`）。这条判据不是仪式。
- **幂等键按 E4 结论用业务字段过滤**（站点不采纳显式 `name`）：
  `Stock Entry` → `(company, purpose, posting_date)`（五张分录两两不同，有单测）·
  `Work Order` → `(company, production_item, wip_warehouse)`（**两张工单只有在制仓不同**，
  不靠它分开的话第二张永远命中第一张）· `Sales Order` / `Delivery Note` / `Sales Invoice`
  → `(company, customer, transaction_date|posting_date)` · `Purchase Invoice` → `(company, supplier, posting_date)`。
- **半装状态有专门的一格**：`find_one` 不分 `docstatus`，命中 `docstatus 0` 的草稿时**补提交**，
  计入 `submitted` 而**不**计入 `created`。没有这一格，一次中途失败留下的草稿会让第二跑「新建 0」照样绿。
- **`--verify-site` 的期望值一律 `from agenerp.seed import checks as CH` 取**，本模块里没有任何新的期望字面量。
  输出**成功与失败都**打带出处的实得值与期望值，形如
  `✅ Bin(XM-LACE-1000, XM 成品仓 - XM).stock_value = 6450.00 / expected = 6450.00（出处：agenerp.seed.checks.EXPECTED_BACKLOG_VALUE）`。
  覆盖 9 项：结存数量 / 结存金额 / GL 借贷差额 / 负库存条目数 / GL 收入贷方 / GL 成本借方 / 毛利 /
  应收逾期 / 应付逾期。**达成率那一项按 D2 明确不在里面。**
- **两条防作弊结构断言**（`tests/unit/test_seedsite_documents.py`）：
  ① 八个派生量（`BACKLOG_*` / `COGS_VALUE` / `GROSS_PROFIT` / `INHOUSE_RATE` / `SUBCON_RATE` /
  `INHOUSE_VALUE` / `SUBCON_VALUE`）在 `agenerp/seedsite.py` 里**零命中**（`M.<NAME>` 文本 + AST 字面量两路判）；
  ② `--verify-site` 引用到的每个 `EXPECTED_*` 都必须在 `agenerp.seed.checks` 上真实存在，
  且源码里不得出现等于任一 `EXPECTED_*` 的数字字面量。第三条测例给这两条喂了一份伪造源码，确认它们不空转。
  ⚠️ **口径限定照实写**：`INHOUSE_RATE` 恰好等于 `5.0`，而裸整数 `5` 在本模块里是日期偏移（`M.day(5)`），
  故 AST 那一路**只判带小数点的 `float` 或 ≥ 100 的整数**。代价是「把 `5` 裸写再乘出来」这条路它抓不到，
  由文本断言与关闭时的人工 `grep` 兜。不粉饰成「全覆盖」。
- **验证退出码**：`python3 -m pytest tests/unit -q` → **exit 0**（248 → **283 passed**）·
  `ruff check agenerp tests/unit tests/contracts` → **exit 0** ·
  `python3 -m agenerp.seed --seed 42 --verify` → **exit 0**（A 半未被打坏）。
  `agenerp/seed/**` 与 `tests/gates/**` **一个字节未改**。

### 2026-08-22 · plan `2107-2` Phase 1 —— 隔离探针上跑通单据链（E1–E4）+ 三条 `Decision`

- **全程在隔离探针上做，一次都没碰 `XM-LACE-1000`**（plan Current Baseline 第 8 条：站点没有单据撤销手段，
  在真物料上试跑会让 Phase 3 的 `Bin` 变成 2020，承重判据按构造不可达）。
  探针物料 `XM-PROBE-CHAIN` / `XM-PROBE-RAW` / `XM-PROBE-SERVICE`、探针仓
  `XM 探针原料仓 / 在制仓 / 成品仓 / 外协仓 - XM`，**四个探针名从不被 `agenerp/seed/model.py` 引用**。
  这些探针对象会在 Phase 3 的 `docker compose down -v` 冷起里被整站丢掉。

**E1 · 链路实测（每一步的站点原样回值）**

- **写路径实测确认：`PUT /api/resource/<doctype>/<name>` 带 `{"docstatus": 1}` 即是提交**，
  站点回 200 且 `data.docstatus == 1`。这就是 Phase 2 `SiteClient.submit_doc` 的落点。
- **站点起不来的三个结构前置（本仓建站命令没有 setup wizard，站点上一条都没有）**，
  逐条是实测撞出来的，不是照着文档抄的：
  · 无 `Fiscal Year` → 提交任何分录回 417 `FiscalYearError: Date 02-02-2026 is not in any active Fiscal Year`；
  · 无 `Stock Entry Type` + 载荷缺 `purpose` → 建 `Material Receipt` 回 417
    `ValidationError: Source warehouse is mandatory for row 1`（`purpose` 不由 `stock_entry_type` 自动带出）；
  · 无 `Price List` → 建 `Sales Order` 回 417 `MandatoryError: selling_price_list, price_list_currency, plc_conversion_rate`。
- **期初入库**：`Material Receipt` 300 Kg @ ¥35 → 提交后站点回 `valuation_rate 35.0` / `amount 10500.0`，
  `Bin(XM-PROBE-RAW, 探针原料仓) actual_qty 300.0 / stock_value 10500.0`。
- **BOM 的成本三项由站点自己算**：探针 BOM 建成即回
  `raw_material_cost 4200.0 / operating_cost 800.0 / total_cost 5000.0`（本仓载荷里这三项一个都没送）。
  ⚠️ 对照**真 BOM**（`BOM-XM-LACE-1000-001`，前置 plan 装的）此刻实读：
  `raw_material_cost 0.0 / operating_cost 800.0 / total_cost 800.0`、`rm_cost_as_per "Valuation Rate"`，
  三条工序各自 `operating_cost 400 / 240 / 160`。**即 `600 分钟 ÷ 60 × ¥80/小时 = ¥800` 这一段
  是站点自己汇总的**；`120 Kg × ¥35 = ¥4,200` 那一段在建 BOM 时站点无库存可估、要等装载期初库存后
  由 `Manufacture` 分录按实际估值算（前置 plan 的 STATE 行早已把这条记为「等第二个 plan」）。
- **自制批：站点自己算出 ¥5.00/米**。`Work Order` 提交后站点自行从 BOM 派生
  `required_items[0] = {item_code: XM-PROBE-RAW, required_qty 120.0, rate 35.0, amount 4200.0}`（本仓没送这张表）；
  `Material Transfer for Manufacture` 120 Kg 提交后站点自填 `basic_rate 35.0`（本仓**没送** `basic_rate`）；
  `Manufacture` 分录带 `additional_costs.amount = 站点 BOM.operating_cost ÷ quantity × 1000 = 800.0` 提交后，
  成品行回 `basic_rate 4.2 / additional_cost 800.0 / valuation_rate 5.0 / amount 5000.0`。
  ⚠️ **反证也拿到了**：同一条链**不送** `additional_costs` 时，成品行回 `valuation_rate 4.2`
  （`Bin stock_value 4200.0`）——即 `get_items()`/`add_operations_cost` 只在服务端 `make_stock_entry`
  路径上跑，走 `/api/resource` 建档时**不会**自动带出工序成本，必须由装载器把站点算出的那个数搬进去。
- **外协批：站点自己算出 ¥6.40/米**。`Work Order` 的 `wip_warehouse` 取外协仓，原料 120 Kg 转入外协仓，
  `Manufacture` 分录带 `additional_costs.amount = 2200.0`（`SUBCONTRACT_FEE`，§12.1 的**输入**）→
  成品行回 `basic_rate 4.2 / additional_cost 2200.0 / valuation_rate 6.4 / amount 6400.0`。
- **FIFO 分层由站点自己做**：探针成品仓三层（1000@4.2、1000@5.0、1000@6.4）发货 990 米后，
  `Delivery Note` 行回 `incoming_rate 4.2`（**最早那一层**），
  `Bin actual_qty 2010.0 / stock_value 11442.0`（= 10×4.2 + 1000×5.0 + 1000×6.4，逐分对上）。
  **这正是 §12.1 说的那种分层，只是探针的费率不是 5.00/6.40。**
- **两张发票提交即为逾期**：`Sales Invoice` 回 `status "Overdue" / grand_total 18612.0 / outstanding 18612.0`，
  `Purchase Invoice` 回 `status "Overdue" / grand_total 2200.0 / outstanding 2200.0`。
  ⚠️ 照实记一条限定：`status` 是站点拿**真实时钟**跟 `due_date` 比出来的，不是拿数据集的 `as_of` 比的。
  种子日期固定在 2026-02/03，今天已越过，故恒成立；但这条断言的成立条件是「今天 > `due_date`」，不写出来会被误读成结构性成立。
- **COGS 落哪个科目要显式指定**：`Delivery Note` 行不带 `expense_account` 时 GL 落
  `Cost of Goods Sold - XM`（公司默认），带 `expense_account: 主营业务成本 - XM` 时 GL 逐字落
  `主营业务成本 - XM 借 4.2 / 成品 - XM 贷 4.2`。装载器因此显式送 `expense_account`。

**E2 · 计价方法实读（只记不改）**

- `Stock Settings.valuation_method = "FIFO"`；`item_naming_by = "Item Code"`；`allow_negative_stock = 0`。
- `Item[XM-LACE-1000 / XM-YARN-WHITE / XM-DYE-SERVICE].valuation_method = ""`（空 → 回落到全局 FIFO）。
- **一个字节都没改站点配置**：这一段只有 `GET`。`¥6,450` 是**在这个配置下**成立的（见 Phase 3 R4）。

**E3 · 拟断言 ② 四项指标的读取路径，逐项给结论**

- **GL 借贷差额** → 可查：`GET /api/resource/GL Entry`（`limit_page_length=0`）滤掉 `is_cancelled`，
  `sum(debit) - sum(credit)`。探针上实测 `24 行 / 借 67870.0 / 贷 67870.0 / 差额 0.0`。
- **负库存条目数** → 可查：`Stock Ledger Entry.qty_after_transaction < 0` 计数（探针实测 **0**），
  `Bin.actual_qty < 0` 计数（探针实测 **0**）。
- **毛利与凭证差额** → 可查：GL 上 `主营业务收入 - XM` 的贷方合计 − `主营业务成本 - XM` 的借方合计。
  探针实测 `收入贷 18612.0`，COGS 因未指定科目落在 `Cost of Goods Sold - XM 借 4158.0`（已按上面那条改准）。
- **销售订单达成率 100%** → ⚠️ **站点上算不出来**。探针发货 990 / 订单 1000 后
  `Sales Order.per_delivered` 实测 **99.0**、`status "To Deliver and Bill"`。
  站点词汇表里没有「已审批损耗」这个概念（`Loss Review` 是本仓虚构 DocType，`xm_loss_review` 是虚构自定义字段），
  给站点建它们要发 DDL，本 plan 逐字禁止。→ 走 `D2`。

**E4 · 单据幂等键**

- **站点不采纳显式 `name`**，实测：送 `name: "MAT-STE-9999-88888"` 建 `Stock Entry`，站点回 `'MAT-STE-2026-00009'`
  （命名序列胜出）。`Sales Order` / `Work Order` / `Delivery Note` / `Sales Invoice` / `Purchase Invoice` 同理，
  全部由 `naming_series:` 派生。**⚠️ 冷起空站点上这些序列号恰好等于 `agenerp/seed/names.py` 的那几个字面量，
  但那是「按顺序建」的巧合，不是站点承诺，幂等不许押在它上面。**
- **结论：幂等键用业务字段过滤**（`SiteClient.find_one` 已支持多字段 `=` 过滤，实测按
  `(company, purpose, posting_date)` 能精确查回 `MAT-STE-2026-00005`）。逐个 DocType 的键见 `module-boundaries.md` 新增小节。
- **半装状态的处置**：`find_one` 不分 `docstatus`，命中一份 `docstatus 0` 的草稿时装载器**补提交**（记为「补提交」不记为「新建」），
  否则一次中途失败会让第二跑「新建 0」在**留着草稿**的情况下照样绿。

**D1 · 单据链裁定：取「站点承担成本汇总最多」的那条 —— 完整制造链（含外协批走 `Manufacture` + 服务费为附加成本）**

- **选定的链**（依赖顺序即装载顺序）：
  `Fiscal Year` / `Stock Entry Type` ×4 / `Price List` ×2（结构前置）→ `Stock Entry(Material Receipt)` 期初原料 300 Kg
  → `Sales Order` 1,000 米 → `Work Order`#1（wip=在制品仓）→ `Stock Entry(Material Transfer for Manufacture)` 120 Kg
  → `Stock Entry(Manufacture)` + 工序附加成本 → `Work Order`#2（wip=外协仓）→ `Stock Entry(MTfM)` 120 Kg
  → `Stock Entry(Manufacture)` + 外协服务费附加成本 → `Delivery Note` 990 米 → `Sales Invoice` → `Purchase Invoice`。
- **站点算了 §12.1 的哪几段，逐字列举**：
  ① `600 分钟 ÷ 60 × ¥80/小时 = ¥800`（站点在 BOM 上按三条工序汇总，本仓载荷未送该数）；
  ② `120 Kg × ¥35 = ¥4,200`（站点在 `Manufacture` 分录上按原料仓的**实际 FIFO 估值**算，本仓未送 `basic_rate`）；
  ③ `(¥4,200 + ¥800) ÷ 1,000 = ¥5.00/米` 自制批单位成本（站点算，本仓未送）；
  ④ `(¥4,200 + ¥2,200) ÷ 1,000 = ¥6.40/米` 外协批单位成本（站点算，本仓未送）；
  ⑤ FIFO 发货 990 米全部出自制批、结余 `10 × 5.00 + 1,000 × 6.40`（站点算，本仓未送 `incoming_rate`）；
  ⑥ `Bin` / `Stock Ledger Entry` / `GL Entry` 三类行全部由站点自己产生（本仓一行都不构造）。
- **哪几段是喂进去的，同样逐字列举**（都是 §12.1 的**输入**，不是它的结论）：
  原料单价 `RAW_RATE = ¥35/Kg`（期初入库 `basic_rate`）· 期初量 `OPENING_RAW_QTY = 300 Kg` ·
  BOM 用量 `BOM_RAW_QTY = 120 Kg` 与三条工序分钟数、工位费率 `WORKSTATION_HOUR_RATE = ¥80/小时`（前置 plan 装的主数据）·
  外协服务费 `SUBCONTRACT_FEE = ¥2,200` · 售价 `SALES_RATE = ¥18.8/米` · 各单据的数量与日期 ·
  以及**从站点读回来再送回去**的 `BOM.operating_cost`（站点自己算出的 ¥800，装载器只做搬运，不重算）。
- **`INHOUSE_RATE` / `SUBCON_RATE` / `INHOUSE_VALUE` / `SUBCON_VALUE` 四个费率派生量一个都没进载荷**
  —— 取 Closure Gate 那条二选一里的 **(i) 零命中**。
- **被否掉的备选，记名不省略**：
  · **(b) 收窄链**（`Stock Entry` 直接按批次价 ¥5.00 / ¥6.40 入库）—— **否决**：那把 §12.1 里
    *产生*这两个费率的成本汇总整段喂给站点，站点只剩最后一次 FIFO 减法，结论比 Goals 宣称的弱得多（D1 判定依据 ②）。
  · **(a′) ERPNext v15 原生外协链**（`Purchase Order(is_subcontracted)` → `Subcontracting Order` → `Subcontracting Receipt`）
    —— **实测撞死，不是嫌麻烦**：建 PO 回 417
    `ValidationError: Row #1: Finished Good Item XM-PROBE-CHAIN must be a sub-contracted item`，
    即成品 `Item` 必须带 `is_sub_contracted_item = 1`（且 `Subcontracting Order.purchase_order` 是必填）。
    给成品加这个字段要改 `--load-masters` 的 `Item` 载荷，而本 plan 逐字要求
    「`--load-masters` 的既有行为逐字节不变」。**因此外协批改用 `Stock Entry(Manufacture)` + 服务费附加成本**
    ——它复现的正是 ERPNext 自己给外协收货算成本的那道公式（`rm_cost_per_qty + service_cost_per_qty`），
    但**用的 DocType 不是 `Subcontracting Receipt`**。这一点是具名残余风险，写进 owner doc 与 `## Deferred But Adjudicated`，
    **不得据此宣称「站点验证了 ERPNext 的外协单据链」**。

**D2 · 达成率那一项：采纳 (c)**

- E3 没有给出第三条路：站点上没有任何字段能表达「1,000 米订单 − 10 米已审批损耗 → 达成率 100%」。
- 处置：`--verify-site` 只覆盖拟断言 ② 中站点算得出来的三项（GL 借贷差额 0 / 负库存 0 / 毛利差额 < ¥0.01），
  **达成率那一项明确移出本 plan 结果面**并进 `## Deferred But Adjudicated`（重开事件：人裁定是否允许为 `Loss Review` 建 DocType / 自定义字段时）。
- **同时更新门禁提案**，写明该项在当前口径下站点算不出来及其原因 —— 否则人采纳提案时会拿到一条注定红的门禁。

**D3 · 「站点算出来的数与期望对不上」的固定处置**：逐字沿用 plan Phase 1 `D3` 写死的四步与六条具名禁止做法，本轮无需动用。

**Phase 1 的验证退出码**（此时尚无实现改动）：
`python3 -m pytest tests/unit -q` → **exit 0**（`248 passed`）· `ruff check agenerp tests/unit tests/contracts` → **exit 0** ·
`python3 -m agenerp.seed --seed 42 --verify` → **exit 0** ·
`AGENERP_HTTP_PORT=18080 AGENERP_LIVE=1 … python3 tools/gates/check_expected_red.py` → **exit 0**，`门禁 19 项：红 0，绿 19，跳过 0`。

### 2026-08-22 · plan `2107-1` 关闭审计回填的 **full-green verification**

- 提交前在干净仓上原样跑 mission 配置的门禁命令，退出码照抄，不自报通过：
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**，
  `门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致` / `248 passed in 0.66s`。
- **本轮无门禁转绿**，`tools/gates/expected-red.txt` 因此一行未动（名单只能变短，这轮没有可划的）。
- 本轮改动**仅两份文档**（`docs/logs/2026/08-22.md` 与 plan `2107-1`），无实现改动，因此只出一个 `docs(...)` 提交。
- 红线自查：`tests/gates` / `.github/workflows` / `docs/masterplan` / `missions` / `tools/gates/expected-red.txt`
  五条路径 `git status --porcelain` **无输出**。

### 2026-08-22 · plan `2107-1` **独立关闭审计** —— 九条判据当场复跑，并当场修掉 plan 文件自身的两处缺陷

- **审计方**：独立关闭审计子代理（fresh session，`2026-08-22-185331-mission-driver`），**非本 plan 的执行者**。
  方法是**不读执行者的结论当证据**，在活仓（sha `da048a2`，工作树干净）与活站点（18080）上逐条复跑。
- **复跑取得的退出码**（命令原文与关键输出全部抄进 plan 的 `## Closure` 审计段）：
  `python3 -m pytest tests/unit -q` → **0**（`248 passed in 0.58s`）·
  `ruff check agenerp tests/unit tests/contracts` → **0** ·
  `python3 -m agenerp.seed --seed 42 --verify` → **0** ·
  `python3 tools/gates/check_expected_red.py` → **0**（`预期红 7，绿 12，跳过 0`）·
  `… python3 -m agenerp.seedsite --load-masters --site frontend` → **0**，`合计：新建 0 / 已存在 40`
  （**这是第三跑，幂等在审计侧独立复现**）· `AGENERP_LIVE=1 … check_expected_red.py`（装载之后）→ **0**，
  `门禁 19 项：红 0，绿 19，跳过 0`。
- **变异验证四条逐条独立复跑，`## Closure` 写的四条全部为真**（改动均已复原，复原后 `248 passed`）：
  `find_one` 吞 `SiteError` → `4 failed` 并点名那四个参数化用例；删 `create_doc` / `delete_custom_field` 登记
  → 各 `1 failed` 且 `AssertionError` 逐字点名；删 `ensure_doc` 登记 → `1 failed`，点名
  `test_deliberately_registered_write_methods_stay_registered`。
  **那道被自查证伪后补出来的 `NON_VERB_WRITE_METHODS` 判据，在审计侧确实兜住了扫描守卫看不见的名字。**
- **红线自查**：`tests/gates` / `tools/gates/expected-red.txt` / `agenerp/seed` / `.github/workflows` / `missions` /
  `docs/masterplan/DECISIONS.md` 六条路径 `git diff --stat 478392e..HEAD` **无输出**；
  `STATE.md` 的删除行计数为 **0**（纯追加）。
- **`ACC_OPERATING` 缺陷在审计侧当场复现且确未被静默**：第三跑原样打出那条 `⚠️ Account 的站点名与 …常量不符`。
  它有 bug note（`Status: open`）+ `Successor Required: yes` + 运行时告警 + 两条单测，
  **不是藏在 Follow-up 里的在范围缺陷**；plan 的 `Follow-up` 逐字为「无」，判定属实。
- **⚠️ 审计当场发现并修掉 plan 文件自身的两处缺陷，照实记，不是一路顺过来的**：
  ① 本文件在 `da048a2` 里**整段正文被重复了一份** —— `## Deferred But Adjudicated` 后误插了一份
  从 front matter 到 `## Draft Review Record` 的完整副本（两份 `diff` 逐字节相同，939 行里 397 行是重复）。
  自动检查器**看不见这种重复**（它只数复选框），是审计逐节读全文时才撞上的。已删除重复段。
  ② `Closure Gates` 的「closure audit was independent」与 `Auditor / Agent` 仍是待填占位，已由本次审计填实。
- **裁定：接受关闭。** `node tools/mission-driver/src/plan-check.mjs … --strict` → **exit 0**，
  `totalChecked 65 / totalUnchecked 0`。**verification scope limited 这句话保留**——上列命令全部 scoped，
  其中三条活站点命令 `GATE_VERIFY` 复跑不到；本 plan 交付的行为仍**没有属于自己的门禁**，
  站点侧三条 L2 断言仍是提案文本，采纳者是人。

### 2026-08-22 · plan `2107-1` 关闭自查 —— 一条 Closure Gate 当场被实测证伪，补齐而不是勾掉

- **逐条机械核对 Closure Gate 时发现一条不成立**，照实记：
  该 gate 逐字是「**删掉任一登记项** → `tests/unit` 必红并点名」。
  实测**删掉 `SiteClient.ensure_doc` 的登记 → `246 passed`，一声不响照样绿**。
  原因不是实现坏了，正是 D2 自己写过的那件事：`ensure_doc` 名字里**一个 `WRITE_VERB` 都没有**
  （`create`/`write`/`submit`/`cancel`/`delete`/`amend`），扫描守卫**根本看不见它**。
  也就是说 Phase 2 那句「主动登记」在落地时**只是一句自觉，不是判据**。
- **处置：补齐判据，不是把 gate 勾掉、也不是把 gate 改宽。**
  `tests/unit/test_site_client.py` 新增 `NON_VERB_WRITE_METHODS = ("SiteClient.ensure_doc",)`
  与两条测例：`test_deliberately_registered_write_methods_stay_registered`（名字里没有 verb 的写方法，
  删掉登记必须转红）与 `test_every_allowlisted_name_resolves_to_a_real_method`（白名单不许留陈迹）。
  `246 → 248 passed`。
- **三条登记项现在逐条都有牙齿，各自实测「转红 → 点名 → 复原转绿」**：
  · 删 `create_doc` → `1 failed`，`AssertionError: 未登记的写方法：['SiteClient.create_doc']`（Phase 2 已记）
  · 删 `ensure_doc` → `1 failed`，点名 `test_deliberately_registered_write_methods_stay_registered`
  · 删 `delete_custom_field` → `1 failed`，`AssertionError: 未登记的写方法：['SiteClient.delete_custom_field']`
  三条复原后均 **`248 passed`**，`ruff check agenerp tests/unit tests/contracts` → **exit 0**。
- **⚠️ 这一条如果只按「读一遍 gate、觉得合理、打勾」走，就会漏过去**——
  它长得像一条早已满足的形式条款，实测才发现三分之一不成立。记在这里，不记成「一切照常」。
- **其余 gate 的机械核对结果**：
  · `git diff --stat 478392e..HEAD -- tests/gates tools/gates/expected-red.txt` → **无输出**；
    `git status --porcelain` 同路径 → **无输出**（红线 1 与账本均未触及）
  · `git diff --stat 478392e..HEAD -- agenerp/seed` → **无输出**；`git status --porcelain` 同路径 → **无输出**
  · roadmap 工作项状态块第 26 行逐字仍是 `- 7. 种子数据（…）: \`planned\``，**未置 `done`**
  · `git diff docs/backlog/p0-foundation-roadmap.md | grep '^-'` → **恰好两行**，
    且两行**都在 `## 当前基线`**，状态块与对照表**零删除**
- **`closure audit was independent` 那一框执行器不自勾**，留给独立关闭审计（裁判规则 1/2）。

### 2026-08-22 · plan `2107-1` Phase 4 —— 三处确认漂移逐条就地改准 + §12.9 落地 + 验证命令表补一行

- **`module-boundaries.md` §12.7 两处假陈述就地改准**（Minimum Rule 14，一条都没降级成 follow-up）：
  ① 「工作项『种子数据』本身**仍然没有绑定的门禁测试**」→ 改准为：有一条 **L1**
  （`test_seed_dataset_absurdity.py`，6 条，人于 2026-08-21 补齐，断的是**生成器**，从未进过名单），
  **但站点侧那一半仍无门禁**，提案 `Status: proposed` 待人采纳。
  **改准句里逐字写了「不得把本段读成『工作项 7 站点侧已有门禁』——那是假的」**，防的是下一次读者读反。
  ② 「装载进活站点与站点侧断言那一半**被红线 1 挡着**」→ 改准为：红线 1 挡的只是
  「站点侧断言**作为一条门禁**」；**装载器在 `agenerp/seedsite.py`，红线 1 不挡它**，2026-08-22 已落地。
  「门禁仍需人批」那一半**保留未抹**。
- **`gate-proposal-seed-dataset.md:11`「工作项 7 是八项中唯一……写着『尚无门禁』的」改准**，
  并补了一段「那这份提案为什么还在」（人补的是 **L1 断生成器**，本提案三条是 **L2 断站点算出来的数**，
  判的不是同一件事）+ 主数据段现状（指向本批两个 plan）。
  **`Status: proposed` 一个字未改，不代人采纳。**
- **`p0-foundation-roadmap.md` `## 当前基线` 的假陈述就地改准**（Phase 4 的 `Decision`，取备选 (c)：
  把「只许追加」的自查**收窄到「工作项状态块」与「工作项 → 门禁测试对照表」**那两处引擎/判据落点，
  `## 当前基线` 的假话就地改准 —— 挂着一句**已被同一份文件第 65 行自我推翻**的假话不是「保守」）。
  **机械判据实测**：`git diff --numstat docs/backlog/p0-foundation-roadmap.md` → **`21	2`**；
  `git diff | grep '^-'` 的两行**全部**是 `## 当前基线` 里那两行原文，
  **工作项状态块与对照表零删除**。另外 21 行插入 = 改准文本 + D4 的追加说明块。
- **`module-boundaries.md` 新增 §12.9「主数据装载在本仓的落点」**（排在 §12.8 之后）：
  模块归属（为什么**不在** `agenerp/seed/` 内，D1 的理由）· 40 步依赖顺序（含 `Workstation`/`Operation`
  与 `Warehouse Type: Transit` 的位置和缺它的报错原文）· 站点没跑 setup wizard 这条前提 ·
  幂等口径（`ensure_doc` 只建不改 + 重开事件）· **装载器自有纯结构字段清单（表格，五类，不参与断言）** ·
  命名隔离（由装载前后两次 live 判定**判**出来）· **「装了就留在站点上、无代码级 teardown、
  复位靠 `down -v` 冷起或人工 `restore`」这条代价** · 两处不粉饰的实测（BOM `raw_material_cost = 0.0`
  与上游 `rm_cost_as_per: "Manual"` 的 500；`ACC_OPERATING` 永远命不中）· 判据归属（本段无自有门禁）。
- **§11.7 就地改准**：`SiteClient` 那一行由「读方法 + **一条**写方法」改为「**三条**」并补上 `find_one`；
  末尾加一段逐字写明「本节标题里的『只读』与原 docstring 里的『只覆盖结构定制』两句都已不再成立」，
  **「这是扩张，就写成扩张，不写成『一直如此』」**；三条可判的代偿逐条列出，
  并把「这三条不阻止将来出现第二个调用方、也不约束写的是什么」当作残余风险写死。
  另补一段解释 `find_one` 为什么不走「GET 单文档 + 404 判不存在」（E3 的派生名实测）。
- **`project-context.md` 验证命令表新增「种子主数据装载」一行**，四条口径齐备
  （端口 18080 / 必须先起栈 / env 三个变量必须由命令给 / ⚠️ 不在 `commands.test` 里，`GATE_VERIFY` 复跑不到），
  并写明幂等判据是第二跑的「新建 0」、以及无 teardown 这条代价。
  ⚠️ **动这张表触发了 plan `2026-08-22-1041-1` 登记的 Deferred「验证命令表整体臃肿」的重开事件**，
  **没有静默略过**：处置按本 plan `## Deferred But Adjudicated` 里事先写死的那条执行（只新增一行、
  不动既有行结构），并把实测 `git diff --numstat` → **`3	1`**（那 1 行删除是表头说明句折行，
  不是任何一条命令行）回填进该条目。重构该表仍待人裁定，本 plan 不代人裁定。
- **收尾复跑**：`python3 -m pytest tests/unit -q` → **exit 0**（`246 passed`）·
  `python3 tools/gates/check_expected_red.py` → **exit 0**（`✅ 与预期红名单完全一致`）。

### 2026-08-22 · plan `2107-1` Phase 3 —— 主数据装载段落地，从**冷起的空站点**连跑两次幂等

- **新模块 `agenerp/seedsite.py`**（D1 的落点，`agenerp/seed/**` 一个字节未改）。
  纯函数 `plan_steps()` 把依赖顺序**写死在代码里**、且不读环境不联网，所以顺序本身是可被单测直接判的：
  `Warehouse Type` → `Company` → `Account`(11) → `Warehouse`(4) → `Item Group`(4) → `UOM`(3) →
  **`Workstation`(1) → `Operation`(3)** → `Item`(3) → 客商分组(6) → `Customer`/`Supplier` → `BOM`。
  **共 40 步。**
- **⚠️ 前置 fixture 是 Phase 1 实测逼出来的，不是起草时的清单**：站点没跑过 setup wizard，
  `Warehouse Type: Transit`（不建它 `POST Company` 直接 417）· `Item Group` 树根 · `UOM` ·
  `Customer Group`/`Territory`/`Supplier Group` 的根与非组叶子，全都得自己补。名字**照抄 ERPNext 标准 fixture 名**。
- **常量归属（Phase 3 的 `Decision`，依赖 E2 的科目表实测结果）**：
  参与断言的数值一律从 `agenerp.seed` 取（`M.COMPANY` / `M.CUSTOMER` / `M.SUPPLIER` / `M.WH_*` /
  `M.ACC_*` / `M.WAREHOUSE_ACCOUNT` / `M.WORKSTATION_HOUR_RATE` / `masters.items()` / `masters.bom()` / `names.BOM`）；
  **纯 ERPNext 结构字段由装载器自己拥有**：`ABBR=XM` · `CNY` · `China` · `Standard Template`/`Standard` ·
  `ACCOUNT_SHAPE` 的 11 组 `(root_type, account_type, parent_account)` · 六个 fixture 名 · `WORKSTATION`。
  E2 实测「建公司站点自己生成 82 条英文科目」，与本仓 11 个中文科目**零重合**，所以那 11 条**必须补建**——
  这一条是 `Decision` 依赖 E2 的具体含义。
- **成本三项不进载荷**：`raw_material_cost` / `operating_cost` / `total_cost` 是站点算的派生量，
  塞进去等于用本仓的数覆盖站点的数，而 B 半存在的全部理由正是「让站点自己算」。
- **CLI** `python3 -m agenerp.seedsite --load-masters --site <site>`：
  打印每个 DocType 的「新建 N / 已存在 M」+ 合计；任一步抛 `SiteError` **立即停并退 1**；
  缺 `--load-masters` 或缺 `--site` 退 **2**（两条都实跑确认）。
- **单测 11 条**（`tests/unit/test_seedsite_loader.py`，FakeTransport 假站点、不连站点）：
  依赖顺序（含 `Workstation`/`Operation` 在 `BOM` 之前，以及另外 9 组先后关系）· 数值全部来自 `agenerp.seed` ·
  仓载荷用 `warehouse_name` 而**不是** `name`（E3）· 科目载荷只含结构 · BOM 已提交且不覆盖成本 ·
  **第一跑建满 40 / 第二跑零 POST** · **失败即停**（`Company` 失败时只尝试过 `Warehouse Type` + `Company`）·
  名字不符必须报告 · 不符检查不空转（只有 1 条报） · CLI 两条退 2。
  `python3 -m pytest tests/unit -q` → **exit 0**，`235 → 246 passed`。

**活站点半（每一条都是命令原文 + 退出码，冷起后按顺序一次跑完）**

| # | 命令 | 退出码 | 输出 |
|---|---|---|---|
| ① | `AGENERP_HTTP_PORT=18080 docker compose -f docker-compose.yml down -v` | **0** | 卷 `agenerp_db-data` / `agenerp_logs` 已移除 |
| ① | `AGENERP_HTTP_PORT=18080 docker compose -f docker-compose.yml up -d --wait --wait-timeout 300` | **0** | **59.6 秒**，九个容器 Healthy |
| ② | `AGENERP_HTTP_PORT=18080 AGENERP_LIVE=1 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin python3 tools/gates/check_expected_red.py` | **0** | 逐字 `门禁 19 项：红 0，绿 19，跳过 0` —— **本机基线** |
| ③ | `… python3 -m agenerp.seedsite --load-masters --site frontend` | **0** | `合计：新建 40 / 已存在 0` |
| ④ | **原样再跑一次** | **0** | `合计：新建 0 / 已存在 40` —— **幂等的判据是这一行** |
| ⑥ | 同 ② 的整目录 live 判定，**装载后** | **0** | 逐字 `门禁 19 项：红 0，绿 19，跳过 0`，与 ② **逐字节相同** |

- ⚠️ **这一轮是第二次冷起。** 第一次冷起（同样 exit 0 / 59.6 秒 / 装载 40 条 / 第二跑新建 0）跑在
  「不符检查还在拿派生名跟自己比」的那一版代码上，那版**永远报不出不符**。发现后改了比对基准，
  **并从 `down -v` 重跑了整条序列**，上表全部取自最终代码那一轮。**旧那一轮的数据不混进上表。**
- **⑤ 读回证据（原样输出）**：
  · `Company`：`{"abbr":"XM","country":"China","default_currency":"CNY","name":"XM 演示纺织有限公司"}`
  · `Warehouse` 4 条，名字与 `model.py` 常量**逐字相等**，且 `account` 挂对：
    `XM 原料仓 - XM → 原材料 - XM` · `XM 在制品仓 - XM → 在制品 - XM` ·
    `XM 成品仓 - XM → 成品 - XM` · `XM 外协仓 - XM → 外协物料 - XM`，四条 `parent_warehouse` 均为 `All Warehouses - XM`
  · `Item` 3 条：`XM-LACE-1000`（成品/Meter/`is_stock_item=1`）· `XM-YARN-WHITE`（原材料/Kg/1）·
    `XM-DYE-SERVICE`（服务/Nos/**0**）
  · `Operation` 3 条（织造 / 定型 / 成品检验），三条 `workstation` 均为 `XM 织造机台`；
    `Workstation`：`{"hour_rate":80.0,"hour_rate_labour":80.0}`
  · `BOM`：`{"name":"BOM-XM-LACE-1000-001","docstatus":1,"is_active":1,"is_default":1,"quantity":1000.0,`
    `"uom":"Meter","currency":"CNY","operating_cost":800.0,"raw_material_cost":0.0,"total_cost":800.0}`；
    三行工序 `400.0 + 240.0 + 160.0 = 800.0`，与 `model.OPERATION_MINUTES / 60 * WORKSTATION_HOUR_RATE` 逐字相等
  · `Customer`：`{"customer_group":"Commercial","territory":"Rest Of The World"}` ·
    `Supplier`：`{"supplier_group":"Local"}` · `Item Group` 4 条 · `UOM` 3 条
  · `Account`：11 条中文科目全部在站点上，`root_type`/`account_type`/`parent_account` 逐条如 `ACCOUNT_SHAPE` 所写。
- **⚠️ `raw_material_cost = 0.0`，照实记，不粉饰**：BOM 默认 `rm_cost_as_per = "Valuation Rate"`，
  此刻站点无库存故为 0；`model.INHOUSE_VALUE = 5000.0` 里的 `120 × 35 = 4200` 那一半
  **要等第二个 plan 装完期初库存后由站点自己算出来**。E4 试过 `rm_cost_as_per: "Manual"`——
  ERPNext v15.119.3 直接 500（`UnboundLocalError: ... 'rate' ...`，`_exc_source: erpnext`），**上游缺陷，不绕**。
- **⚠️ `M.ACC_OPERATING` 的不符在活站点上被打出来了**（这是它该有的样子，不是失败）：
  逐字 `⚠️ Account 的站点名与 agenerp.seed 常量不符：常量 '生产费用（计入估值）- XM' ≠ 站点 '生产费用（计入估值） - XM'`。
  装载器**不因此退非 0**——科目建成功了，这不是装载失败。取舍与重开事件见
  `docs/bugs/01-acc-operating-constant-can-never-match-a-live-account-name.md`。
- **子表读不到，照实记**：`GET /api/resource/BOM Operation` → **403 `PermissionError`**（Frappe 不许直查子表）。
  三行工序的证据改由父文档 `GET /api/resource/BOM/BOM-XM-LACE-1000-001` 带出来，**不是省掉了这一条**。
- **收尾复跑（exit 码取自命令本身）**：`python3 -m agenerp.seed --seed 42 --verify` → **exit 0**
  （`✅ 种子 42：两次生成 diff 为空，场景断言全过`，**A 半未被打坏**）·
  `python3 -m pytest tests/unit -q` → **exit 0**（`246 passed`）·
  `ruff check agenerp tests/unit tests/contracts` → **exit 0** ·
  默认判定环境 `python3 tools/gates/check_expected_red.py` → **exit 0**（`预期红 7，绿 12，跳过 0`，逐字节不变）。
- **红线自查**：`git diff --stat 478392e -- tests/gates tools/gates/expected-red.txt agenerp/seed` → **无输出**。

### 2026-08-22 · plan `2107-1` Phase 2 —— `SiteClient` 的写入面落地，两条变异验证各拿到「转红 → 点名 → 复原转绿」

- **三个方法落地**（`agenerp/site.py`，`+83 / -6`）：
  · `create_doc(doctype, payload) -> dict` —— `POST /api/resource/<DocType>`，回 `data`。
    **不吞任何错误**：409 `DuplicateEntryError` 照抛，**不判成「已经有了、算成功」**。
    docstring 里写死了 E3 的实测结论：**`name` 由站点说了算**，显式塞 `name` 会被静默忽略，
    调用方要真名就得读返回值的 `data.name`。
  · `find_one(doctype, filters) -> dict | None` —— **按 E3 的结论选的形态**（站点对半数 DocType
    不采纳显式 `name`，「GET 单文档 + 404 判不存在」对它们无从下手）。走列表端点 + `filters` +
    `limit_page_length=1`，**只把「查得到、但零行」判成不存在**，其余一律抛。
  · `ensure_doc(doctype, key, payload) -> tuple[dict, bool]` —— 先查后建；**只建不改**。
- **`WRITE_METHOD_ALLOWLIST` 从 1 条变 3 条**：新增 `SiteClient.create_doc` 与 `SiteClient.ensure_doc`。
  ⚠️ **`ensure_doc` 的名字里一个 `WRITE_VERB` 都没有，守卫扫不到它——它是主动登记的。**
  只登记守卫扫得到的，等于让守卫替人决定该留什么痕。`find_one` 是纯读，不登记。
  `:261` 那条「断言不空转」的测例**逐字保留，未动**。
- **两句已不成立的话就地改准，不默默扩**：`agenerp/site.py` 模块头第 4 条的「写只有一条」与
  「本模块的写面只该覆盖『结构定制』」——后者 2026-08-22 起为假（`create_doc`/`ensure_doc` 是**通用建档面**）。
  `SiteClient` 类 docstring 的「写只有一条」同步改准为「写三条」。文档侧（§11.7）归 Phase 4。
- **`tests/unit/test_site_client.py` 补 11 个测函数 / 14 条测例**（`+143 / -0`）：
  create 成功且断言 POST 载荷 / create 非 2xx 抛且消息含 `409` + `DuplicateEntryError` /
  create 载荷无 `data` 抛 / find_one 命中 / find_one 未命中回 `None` /
  **find_one 遇 401·403·500·502 必须抛（4 条参数化，不得判成不存在）** / find_one 载荷无 `data` 抛 /
  **ensure_doc 命中时零 POST**（断言 `[r.method] == ["GET"]`）/ ensure_doc 未命中时恰好一次 POST /
  **ensure_doc 不改已存在文档**（回的是站点上的旧值，不是载荷里的新值）/ 三个方法在缺凭据时都指名环境变量。
  `python3 -m pytest tests/unit -q` → **exit 0**，`221 → 235 passed`。
- **变异验证 ①（find_one 的错误分流）**：把 `find_one` 里那次 `self.get(...)` 包进
  `try: ... except SiteError: return None`（= 「任何非 2xx 都判不存在」）→
  **`4 failed, 231 passed`**，逐字点名
  `test_find_one_raises_on_any_non_2xx_and_never_reads_it_as_absent[401]/[403]/[500]/[502]`，
  失败原文 `Failed: DID NOT RAISE <class 'agenerp.site.SiteError'>`。**复原后 `235 passed`。**
- **变异验证 ②（白名单守卫的牙齿）**：把 `"SiteClient.create_doc"` 从 `WRITE_METHOD_ALLOWLIST` 删掉 →
  **`1 failed, 234 passed`**，点名 `test_site_module_exposes_no_unlisted_write_method`，
  失败原文逐字 `AssertionError: 未登记的写方法：['SiteClient.create_doc']；白名单=('SiteClient.delete_custom_field', 'SiteClient.ensure_doc')`。
  **复原后 `235 passed`。** 这条同时证明了守卫**确实没有**因为新方法而空转。
- **收尾复跑（exit 码取自命令本身，不取自管道末端）**：
  `python3 -m pytest tests/unit -q` → **exit 0**（`235 passed in 0.53s`）·
  `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 19 项：预期红 7，绿 12，跳过 0` /
  `✅ 与预期红名单完全一致`）· `ruff check agenerp tests/unit tests/contracts` → **exit 0**。
- **红线自查**：`git diff --stat 478392e..` 触及 `tests/gates` / `tools/gates/expected-red.txt` → **无输出**；
  `agenerp/seed/**` → **无输出**。

### 2026-08-22 · plan `2107-1` Phase 1 —— 站点写语义四组实测（E1–E4）+ 五条裁定（D1–D5）

**全部输出取自活站点 `frontend@http://127.0.0.1:18080`（栈已起，`AGENERP_HTTP_PORT=18080`），
基线 sha `478392e`。以下每一行都是实跑贴回来的，不是回忆。**

**E1 · REST 写语义**

- ① **不需要 `X-Frappe-CSRF-Token`**：纯 cookie 会话下 `POST /api/resource/Item Group` 直接走到**业务校验**
  （`HTTP 417 frappe.exceptions.LinkValidationError: Could not find Parent Item Group: All Item Groups`），
  不是 403/CSRF。`agenerp/site.py` 的 `UrllibTransport` 自带 cookie jar，登录后即可写。
- ② **建重名回 `HTTP 409 DuplicateEntryError`**，逐字例：
  `{"exception":"frappe.exceptions.DuplicateEntryError: ('Item', 'XM-LACE-1000', IntegrityError(1062, \"Duplicate entry 'XM-LACE-1000' for key 'PRIMARY'\"))"}`；
  `Operation` / `Warehouse` 同为 409（后者报的是**派生名** `ZZ 只派生 - XM`）。
- ③ **`PUT /api/resource/<DocType>/<name>` 改已存在文档回 200 + 整份文档**（实测 `Operation/织造` 改 `description`）；
  改不存在的回 **404 `DoesNotExistError`**。
- ④ **无害探针建 + 读回走得通**：`POST /api/resource/Item Group {"item_group_name":"All Item Groups","is_group":1}`
  → `200 name='All Item Groups'`；随后 `GET` 同名回全文。
- ⑤ **未认证时一律 403 `PermissionError`**（漏调 `_ensure_authenticated()` 时 19 个 DocType 全部 403）——
  这条是踩出来的：403 与「站点上没有」长得完全不一样，`find_one` 因此**只能把「查得到但零行」判成不存在**。

**E1 附带的重大事实：这个站点是真空的，`bench new-site --install-app erpnext` 没跑 setup wizard**

- 实测（已认证）：`UOM` **0** · `Item Group` **0** · `Account` **0** · `Company` **0** · `Warehouse` **0** ·
  `Customer` **0** · `Supplier` **0** · `Operation` **0** · `Workstation` **0** · `Customer Group` **0** ·
  `Supplier Group` **0** · `Territory` **0** · `Fiscal Year` **0** · `Item` **0**；
  只有 frappe/erpnext 装包期就写死的三类有行：`Currency` **149** · `Country` **250** · `Domain` **2**。
- **后果**：装载器要建的不止 plan 列的那几类，还得先把 ERPNext setup wizard 本该建的**前置 fixture** 补上
  （`Warehouse Type: Transit` · `Item Group` 树根 · `UOM` · `Customer Group`/`Territory`/`Supplier Group` 根与非组子节点）。
  这一条是起草时不知道的，Phase 3 的依赖顺序据此加长。

**E2 · `Company` 与自动科目表**

- **第一跑失败**：`POST /api/resource/Company` → `HTTP 417 LinkValidationError: Could not find Warehouse Type: Transit`。
  先 `POST /api/resource/Warehouse Type {"name":"Transit"}` → 200，再建公司才过。
- **第二跑成功**（载荷 `company_name` / `abbr=XM` / `default_currency=CNY` / `country=China` /
  `create_chart_of_accounts_based_on=Standard Template` / `chart_of_accounts=Standard`，耗时 0.7s）：
  `data.name = "XM 演示纺织有限公司"`，并回填 `default_cash_account=Cash - XM` ·
  `default_receivable_account=Debtors - XM` · `default_payable_account=Creditors - XM` ·
  `default_expense_account=Cost of Goods Sold - XM` · `default_income_account=Sales - XM` · `cost_center=Main - XM`。
- **站点自己生成了 82 条 `Account`**（Standard 模板，全部英文名 + ` - XM` 后缀），
  与本仓 `model.py` 的 11 个中文科目**零重合**——**装载器必须自己补建那 11 条**。
  其中可用作父节点的组节点：`Stock Assets - XM` · `Stock Expenses - XM` · `Stock Liabilities - XM` ·
  `Accounts Payable - XM` · `Accounts Receivable - XM` · `Direct Income - XM`。
- 建公司还**顺带**生成 **5 个 `Warehouse`**（`All Warehouses - XM` 组 + `Finished Goods` / `Goods In Transit` /
  `Stores` / `Work In Progress`）与 **2 个 `Cost Center`**（`Main - XM` · `XM 演示纺织有限公司 - XM`）。
- **`Fiscal Year` 仍为 0**——建公司不生成会计年度。本 plan 不装单据，够用；**记给第二个 plan**。

**E3 · 自动命名（本 plan 最关键的一条）**

| DocType | 显式 `name` | 站点最终 `name` | 判据 |
|---|---|---|---|
| `Warehouse` | 传 `"ZZ 显式名测试"`、同时传 `warehouse_name="ZZ 派生名测试"` | **`ZZ 派生名测试 - XM`** | **不采纳**，`warehouse_name + " - " + abbr` |
| `Account` | 传 `"ZZ 显式科目"`、同时传 `account_name="ZZ 派生科目"` | **`ZZ 派生科目 - XM`** | **不采纳**，`account_name + " - " + abbr` |
| `Item` | 传 `"ZZ-EXPLICIT"`、同时传 `item_code="ZZ-PROBE"` | **`ZZ-PROBE`** | **不采纳**，`Stock Settings.item_naming_by = "Item Code"`（实读单文档） |
| `BOM` | 传 `"BOM-XM-LACE-1000-001"` | `BOM-XM-LACE-1000-001` | **不采纳**——第二条同 item 的 BOM 显式传 `"ZZ-BOM-PROBE3"` 回的是 **`BOM-XM-LACE-1000-002`**，证明走的是序列 `BOM-{item}-{###}`，第一条只是**恰好撞上** |
| `Operation` | 传 `"织造"` | `织造` | **采纳** |
| `UOM` / `Item Group` / `Customer Group` / `Territory` / `Supplier Group` / `Workstation` / `Customer` / `Supplier` | `<x>_name` 即 name | 同 `<x>_name` | 采纳（等价于按名字段命名） |

- **结论（plan Phase 2 要的那一条）：幂等 __不能__ 无条件靠「把 name 塞进载荷」判**。
  站点对半数 DocType 用派生名。因此存在性判断取 **`find_one(doctype, filters)`** 形态
  （`GET /api/resource/<DocType>?filters=[["name","=",...]]&limit_page_length=1`），
  **查得到但零行 = 不存在**；实测命中回 `{"data":[{"name":"织造"}]}`，未命中回 `{"data":[]}`，两者都是 **HTTP 200**。
- 装载器因此**自己算派生名**（`f"{x} - {abbr}"`），再拿算出来的名字去 `find_one`。

**E3 顺带发现的一处真缺陷（`agenerp/seed/model.py`，本 plan 不许改它）**

- `ACC_OPERATING = "生产费用（计入估值）- XM"` —— **少一个空格**，`- XM` 前没有空格，其余 10 个 `ACC_*` 都是 ` - XM`。
- ERPNext v15 的 `Account.autoname` 走 `" - ".join([account_name, abbr])`，**只可能产出 `生产费用（计入估值） - XM`**。
  实测建出来的就是 `'生产费用（计入估值） - XM'`。**这个常量在活站点上永远命不中。**
- 本 plan 的 Closure Gate 逐字要求 `agenerp/seed/**` 一个字节未改，**因此不在这里修**：
  装载器改为**报告实际站点名与常量不符**（打印告警行，不粉饰），并登记进 `docs/bugs/` 与本 plan 的
  `## Deferred But Adjudicated`，重开事件交给第二个 plan 的对账。

**E4 · BOM 的前置**

- `BOM.operations[].operation` **是 Link 到 `Operation` DocType**：`Operation` 不存在时 417 `LinkValidationError`。
- **`workstation` 非强制**：实测 `POST /api/resource/Operation {"name":"定型"}`（不带 workstation）→ 200。
  但 `BOM` 的 operation 行给 `workstation` 更贴 ERPNext 口径，装载器给。
- **最小可提交载荷**：`Workstation {"workstation_name": ..., "hour_rate_labour": 80.0}` →
  回 `hour_rate=80.0`（⚠️ 直接传 `hour_rate` **会被站点算掉，回 0.0**——实测两次，
  `hour_rate` 是 `hour_rate_labour + 电 + 耗材 + 租金` 的派生量）；
  `Operation {"name": ..., "workstation": ...}`。
- **BOM 的 `operations[].hour_rate` 站点采纳**：三行 300/180/120 分钟 × 80 元/时 → `operating_cost = 800.0`，
  与 `model.OPERATION_MINUTES / 60 * WORKSTATION_HOUR_RATE` 逐字相等。
- **`raw_material_cost` 回 `0.0`**：BOM 默认 `rm_cost_as_per = "Valuation Rate"`，此刻站点无库存故为 0。
  ⚠️ **试过 `rm_cost_as_per: "Manual"` —— ERPNext v15.119.3 直接 500**：
  `UnboundLocalError: cannot access local variable 'rate' where it is not associated with a value`（`_exc_source: erpnext`）。
  **这是上游缺陷，本 plan 不绕它**：装载器沿用默认口径，原料成本等第二个 plan 装完期初库存后由站点自己算出来。
- **`docstatus: 1` 可以随 `POST` 一起提交**（实测回 `docstatus=1`）。装载器据此把 BOM 建成**已提交**——
  `masters.bom()` 逐字给了 `is_active: 1` / `is_default: 1`，草稿 BOM 上这两个字段没有意义，
  且第二个 plan 的 `Work Order` 必须挂已提交 BOM。
- `Customer` **必须挂非组 `Customer Group`**：用 `All Customer Groups`（组）→
  `417 ValidationError: Cannot select a Group type Customer Group.`；建一个非组子节点后过。
  `Supplier` 挂组 `Supplier Group` 反而放行（ERPNext 两边校验不对称，照实记）。

**D1 · 装载器放哪个模块 → 采纳 (c)：新建独立模块 `agenerp/seedsite.py`**

- 否决 (a) `agenerp/seed/loader.py` 与 (b) 改松不变量：`docs/architecture/module-boundaries.md:746` 逐字
  「模块：`agenerp.seed`（包，非单文件）。零第三方依赖，纯标准库，**不读时钟、不读环境、不联网**」。
  装载器两件都要做。**保住这条好的不变量，代价是多一个模块**。
- 落点：**`agenerp/seedsite.py`**（单文件模块，CLI 入口 `python3 -m agenerp.seedsite` 就在该文件内）。
  先例：`agenerp/oob.py` 已是同级单文件模块。
- 残余风险：多一处文档落点（Phase 4 的 §12.9 认领）。

**D2 · 写方法命名与守卫登记 → 采纳 (c)：方法名带 verb 并逐条登记**

- `tests/unit/test_site_client.py:38` 的 `WRITE_METHOD_ALLOWLIST` 按 `agenerp/contracts.py:40` 的
  `WRITE_VERBS = ("create","write","submit","cancel","delete","amend")` **扫方法名**。
  叫 `insert` / `ensure` 的方法名里一个 verb 都没有，**守卫会空转变绿**。
- 采纳：`create_doc`（含 `create`）与 `ensure_doc`（**不含任何 verb，但仍主动登记**，
  理由是它是写路径的门面，漏登记等于把「加写面要付一次留痕」这条规矩掏空）。
  `find_one` 是纯读，不登记。
- 同时裁定：写面从**结构定制**扩到**业务主数据**这件事本身，`agenerp/site.py` 模块 docstring 与
  `module-boundaries.md` §11.7 都写着「只覆盖结构定制」，**Phase 2/4 就地改准，不默默扩**。
- 残余风险：`SiteClient` 从此是通用写客户端，误用面变大；缓解见 D3 + 登记守卫 + 装载器是唯一调用方。

**D3 · `ai-autonomy-policy.md` Protected Areas 加严一行 → 采纳 (c)**

- 新增一行「对活站点的**非破坏性写**（建 / 改）」，定级 `plan-first`，Required Evidence 三条
  （独立草案评审 · 独立关闭审计 · **一条对可逆性说话的**）。方向是**加严**，不是放宽。
- 残余风险：文档级约束对拿着 shell 的执行器没有强制力（该文件自述），本条不改变这一点。

**D4 · 工作项 7 将有 3 个 plan → 采纳 (d)：roadmap 工作项 7 处追加一条说明行**

- 不新增工作项行（那是引擎的选取输入）；不把 B 半压成一个 plan（会让 Minimum Rule 4
  「One plan, one result surface」失效）；不静默违规。

**D5 · 命名隔离 → 沿用 `model.py` 既有的 `XM` 前缀常量，不新造命名规则**

- 装载器**自己拥有**的纯结构名（不参与任何断言，Phase 4 的 §12.9 逐条列名）：
  `Warehouse Type: Transit` · `Item Group: All Item Groups` · `Customer Group: All Customer Groups` / `Commercial` ·
  `Territory: All Territories` / `Rest Of The World` · `Supplier Group: All Supplier Groups` / `Local` ·
  `Workstation: XM 织造机台` · 公司缩写 `XM` · 币种 `CNY` · 国别 `China` ·
  科目表模板 `Standard Template` / `Standard` · 11 条科目的 `root_type` / `account_type` / `parent_account`。
  前六项是 ERPNext setup wizard 的**标准 fixture 名**，照抄不自造。
- 隔离是否成立**由 Phase 3 的「装载前后各跑一次整目录 live 判定」判**，不靠声明。

### 2026-08-22 · plan `1206-2` 收尾第二推 —— 终止规则第三步，终点判据成立

- **推前守卫预检**：判定器谓词**无输出**（情形 ①）· trailer 计数 `0` · 区间只含
  `docs/logs/**` 与 `docs/plans/p0-foundation/**`。
- **`git push origin main` → exit 0**，`2b151fb..92cae80`。
- **按三步终止规则，这一推只记 run id 与 `conclusion`**（与第一推跑的是逐字相同的 workflow，明细无新信息量）：
  run **`32573555156`**（event `push`，head `92cae80`）→ **`success`**。
- **终点判据成立（此后不再推、不再记）**：`git merge-base --is-ancestor origin/main main` → exit 0 ·
  `git rev-list --count origin/main..main` → **`1`**（≤ 1）· 那一个提交只含
  `docs/logs/2026/08-22.md` 与本 plan 文件。**闭幕判据用这一条，不用「两值相等」。**
- **CI 运行终值 6 / 7，未超预算**：`32572388207` · `32572416547` · **`32572618933`（权威运行）** ·
  `32573304204` · `32573555156`（第 0 步 / PR #3 / 落地 / 收尾第一推 / 收尾第二推），机动 1 次未用。

### 2026-08-22 · plan `1206-2` 收尾第一推 —— `main` push 运行九个 job 全绿，plan 置 `completed`

- **推前守卫预检（⚠️ 守卫此刻已活在 `main` 上，这一推与之前所有推都不同）**：
  `git diff --name-only origin/main main -- tools/gates/check_expected_red.py tools/gates/gate-verify.mjs`
  → **无输出**（情形 ①）· trailer 计数 `0` · `git diff --name-only origin/main main` → 八个文件**全在 `docs/**` 下**。
- **`git push origin main` → exit 0**，`3503f2c..2b151fb`。
  **当场实测 `git rev-parse main origin/main` 两值相等**（均 `2b151fb…`）。
- **该次 `main` push 运行**：run **`32573304204`**（event `push`，head `2b151fb`）→ **`success`**，
  **九个 job 全部 `success`**：`循环联动冒烟` `97031875185` · `roadmap 引擎可解析` `97031875245` ·
  `预期红名单只能变短` `97031875249` · **`L2 全量 live 判定（19 条）` `97031875254`** ·
  `门禁未被改动` `97031875269` · `主计划引用不断链` `97031875274` · `L1 快门禁` `97031875281` ·
  **`判定器未被改动` `97031875306`** · `L2 慢门禁（零依赖启动）` `97031875323`。
- ⚠️ **这是 `gates-l2-live` 在 `main` 上的第二次绿，但权威运行只有一个**——
  NB2 钉的是落地那一次（`32572618933`），本次是收尾推送的附带结论，**两者不混**。
- **Plan Status 置 `completed`**，`## Closure Gates` 十八框勾了十七框；
  **`closure audit was independent` 那一框执行器不自勾**，留给独立关闭审计（裁判规则 1/2）。
- **CI 运行 5 / 7**（`32572388207` · `32572416547` · `32572618933` · `32573304204`，外加第二推）。
- **本 plan 期间零红**，停机纪律三条走向一条都没走到。

### 2026-08-22 · plan `1206-2` Phase 3 —— owner-doc 漂移逐条裁定并就地改准，`0027-2` 两项欠账还清

- **那条 grep 命中 10 行 / 4 个文件，逐条裁定（不是逐条照改）**：
  **9 条判为确认漂移，全部就地改准**（`project-context.md:58,59` · `system-baseline.md:448,457,461,521` ·
  `p0-foundation-roadmap.md:62,64,69`），**没有一条被降级成 follow-up**（Minimum Rule 14）；
  **1 条判为非漂移**——`docs/architecture/module-boundaries.md:305`「无活站点不等于错误……
  让它抛异常，门禁就会永远红在环境而不是红在实现上」是一条**正确的架构原则**，
  命中只因共用「红在实现」这个词组，与两个 job 在不在 `main` 上无关。**记明位置与不改的理由，未静默略过、更未照改。**
- **改准的内容一律不写成比证据更强的说法**：写的是落地 sha `3503f2c`、权威运行 `32572618933`
  九个 job 全绿、`gates-l2-live` job `97030229667` 日志逐字；
  ⚠️ run `32509351108` 那次**可复现的红是永久证据，逐字保留、未被抹掉**；
  守卫 `push` 路径写的是「能跑通、走到未触及分支」，**没写成「已有牙齿」**。
  另有一条**与本次落地无关的独立确认漂移**一并改准：`system-baseline.md:461`「红在实现，不是红在判据：
  `apply_pack` 的清除面在 runner 全新站点上不成立」——它早已被 plan `2026-08-22-0228-2` 推翻。
- **`docs/masterplan/` 另跑同一 pattern → 7 条**（`STATE.md:50,54,112,113,115,127` ·
  `archive/STATE-2026-08-22.md:177`）。**一处都没就地改**（红线 5），改准内容走
  「在 `STATE.md` §2/§3 各追加一条事实行」的既有先例；归档文件**连追加都不做**。
  实测 `git diff --numstat … -- docs/masterplan/STATE.md` → **`19	0`**，`grep '^-'` → **无输出**。
- **`0027-2` 两项欠账还清**：① 在 `2026-08-21-2220-2` 的 `## Deferred But Adjudicated` 两条下各**追加**一行
  （`git diff --numstat` → **`2	0`**，**删除列为 0**），**逐字点名两个 plan 的完整 id**
  （`2026-08-22-1206-1-verdict-guard-mutation-proof` / `2026-08-22-1206-2-gates-l2-live-lands-on-main`）
  与权威运行 `32572618933`；② `0027-2` 的 `Closure Audit Log` 追加一行（`11	0`，纯追加），**只写本 plan 还的那一项**。
- **`0027-2` 未被代关闭，四条计数改动前后完全相同**：锚定未勾 `19` · 未勾总计数 `26` · 锚定已勾 `36` ·
  `Plan Status: deferred` 计数 `1`。⚠️ 追加时未勾总计数一度由 `26` 变 `27`（追加文本里写了一对字面方括号），
  **当场按该判据收回**——这正是「只钉锚定式的 19 不够」那条判据存在的理由。
- **roadmap「9 现状」改准**，含权威运行九个 job 的 id 与结论、守卫四条实验（**含「同一 sha 上不可复现」的限定**）、
  与工作项 8 的包含关系（**9 绿不代表 8 可置 `done`**）。
  ⚠️ **`## Work Item Status` 一个字未动**，工作项 9 仍 `planned`——置状态由引擎在 closure 审计后回写；
  该 roadmap 不使用 `❌ / ✅` 标记，没有可翻转的标记。
- **本机五条命令全部 exit 0**：`python3 tools/gates/check_expected_red.py` →
  `门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致` ·
  `python3 -m pytest tests/unit -q` → `221 passed` · `python3 -m pytest tests/contracts -q` → `151 passed` ·
  `ruff check agenerp tests/unit tests/contracts` → `All checks passed!` ·
  `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/gates.yml'))"` → exit 0。
  ⚠️ **验证范围照实说**：本仓无全量套件，证据面是「CI 上九个 job + 本机这五条」，**不得报成「全量验证通过」**。
- **红线自查五条（锚开工 sha `79184e5`）全部为期望值**：`tests/gates` / `agenerp` / `DECISIONS.md` /
  `missions` / `tools/gates` 的 `diff --stat` 与 `status --porcelain` **均无输出**；
  `expected-red.txt` **一行未动**；`STATE.md` **只追加**；`2026-08-21-2220-2` **删除列为 `0`**。
  另记：`gates.yml` 本 plan 全程 `118	0`（纯追加零删除）；**证据仓 `${XM_PATH}` 全程未写入**。

### 2026-08-22 · plan `1206-2` Phase 2 —— 两个 job 落到 `main`，权威运行九个 job 全绿（工作项 9 的关闭判据第一次在 `main` 上成立）

- **第 0 步（推平 `origin/main`）**：推前守卫预检
  `git diff --name-only origin/main main -- tools/gates/check_expected_red.py tools/gates/gate-verify.mjs`
  → 无输出（情形 ①），trailer 计数 `0`；`git push origin main` → exit 0，`10da9e7..bb83b20`；
  **实测 `git rev-parse main origin/main` 两值相等**（均 `bb83b20…`）。
  该次 push 运行 **`32572388207`** → `success`，**七个 job**（此刻仍是 7 个）全绿。
- **第一步（新分支 + 新 PR）**：`$RECUT` = **`bb83b2039efd9c733ff9c9aa0bba85b4bbb11edf`**；
  118 行走**再生路径**从 `origin/ci/0027-2-l2-full-live-gate` 取（`/tmp` 跨会话已失），
  再生自检 job 键恰为两行、与前驱分支 tail 逐字相同；`git switch -c ci/1206-2-l2-live-land main` 后
  `gates.yml` **190 → 308** 行，单提交 **`3503f2c`**（**不含** trailer），普通 push（**无 force**）→ **PR #3**。
  **六条判据全中**：`is-ancestor` exit 0 · `git diff --numstat $RECUT` 恰好一行 `118	0	.github/workflows/gates.yml` ·
  区间提交数 `1` · **`baseRefOid` = `bb83b20…` 逐字等于 `$RECUT`** · 读数 `1 / 118 / 0` · trailer `0`。
  **前驱分支 `ci/1206-1-verdict-guard-proof` 与 PR #2 一个字节未动**（用的是 `switch -c` + 新名，非 `-C`）。
- **PR #3 全绿**：run **`32572416547`** → `success`，**九个 job 全绿**，
  `L2 全量 live 判定（19 条）` job `97029725467` 日志逐字 `门禁 19 项：红 0，绿 19，跳过 0` /
  `✅ live 判定：全部门禁绿，零 red、零 skip`；守卫 job `97029725459` 日志
  `BASE="bb83b20…"; HEAD="3503f2c…"` → `✅ 未触及判定器`，**与 Phase 1 预检算定的情形 ① 一致**。
- **`--ff-only` 落地**：推前重跑守卫预检仍为情形 ①；落地时 `git rev-parse main` 仍等于 `$RECUT`
  （**切分支到 ff-merge 之间 `main` 零新提交**）；`git merge --ff-only` → `Updating bb83b20..3503f2c` /
  `Fast-forward` / `1 file changed, 118 insertions(+)`；`git push origin main` → exit 0。
  **落地 sha = `3503f2c`，与 PR #3 的 head 逐字相同**。
- **红线 2 自查五条**：(a) 前缀性 `diff` **无输出**（既有 190 行逐字节未动、零删除，连「往既有 job 里插一行」都挡得住）·
  (b) job 键 **9 个**，原 7 个原序原名 + 末尾两个新的 · (c) 禁用词**零命中** · (d) `on:` 块逐字未动 ·
  (e) ⚠️ **收窄说法**：`|| true` 实测**两处**（`:36` 既有 `gates-untouched`、`:293` 新守卫体内，同款写法），
  是**真实的假阴入口**；`if: always()` 六处全在取证/拆栈步骤，判定步骤无 `continue-on-error`。
  **正确结论是「判定与守卫的 `exit 1` 路径没有被吞掉」，不是「零吞噬」。**
- **NB2 的权威运行**：run **`32572618933`**（event **`push`**，head `3503f2c`）→ **`success`**，
  **九个 job 全部 `success`**：`门禁未被改动` `97030229573` · `循环联动冒烟` `97030229628` ·
  `L1 快门禁` `97030229662` · **`L2 全量 live 判定（19 条）` `97030229667`** · `roadmap 引擎可解析` `97030229671` ·
  `主计划引用不断链` `97030229672` · `L2 慢门禁（零依赖启动）` `97030229696` ·
  **`判定器未被改动` `97030229697`** · `预期红名单只能变短` `97030229729`。
  **⇒ 工作项 9 的关闭判据第一次在 `main` 上成立。**
- **守卫 `push` 路径首次实测**：`BASE="bb83b20…"（github.event.before）; HEAD="3503f2c…"（github.sha）`
  → `✅ 未触及判定器`，与预检一致。⚠️ **只证明 push 路径能跑通并走到「未触及」分支，
  不证明守卫在 push 上已有牙齿**——正向变异要在 `main` 上故意改判定器，代价过高，登记为残余。
- **三个 PR 终局**：PR #3 → **`MERGED`**（不变式 `git merge-base --is-ancestor 3503f2c origin/main` exit 0，
  **不拿 `mergeCommit` 当判据**）· PR #2 → **`CLOSED`** + 说明评论（head 仍 `b7348bf`，与前驱记录逐字相同）·
  PR #1 → **`CLOSED`** + 说明评论（`32509351108` `failure` 与 `32533449466` `success` 实测仍可查，
  **那条可复现的红没有被抹掉**）。**未删除任何远程分支**（`git ls-remote --heads origin` 五条都在）。
- **CI 运行 4 / 7，未超预算；本 plan 期间零红**，停机纪律三条走向一条都没走到。
- **本机复跑**：`python3 tools/gates/check_expected_red.py` → exit 0 · `python3 -m pytest tests/unit -q` → exit 0（`221 passed`）。

### 2026-08-22 · plan `1206-2` Phase 1 —— 前置八条核对全成立，四个 `Decision` 定死，闸门放行

- **开工 sha `79184e5`**，`git status --porcelain` 无输出。`git rev-parse main origin/main`
  → `79184e5…` / `10da9e7…`，本地领先 **2** 个文档提交（**这正是前置核对 ⑥ 预料的形态**，
  不是「两值相等」——那个形式在本仓 loop 节奏下不可满足）。
- **八条前置核对全部成立**：① 前驱 plan `Plan Status: completed` + **四条**实验证据在文件里
  （`32570222139` 红 / `32570426423` 绿 / `32570691388` 绿 / `32570942284` attempt 2 绿，
  第四条「触及 + 带 trailer 放行」在，缺它就停）· ② `git diff --numstat $CUT ci/1206-1-verdict-guard-proof`
  → 恰好一行 `118	0	.github/workflows/gates.yml`（`$CUT` = `f689d0e7cde…`）·
  ③ `gh pr view 2` → `OPEN / 1 / 118 / 0` · ④ 该 head 最近一次运行 **`32571266013`** → `success`，
  **九个 job 全绿**，`L2 全量 live 判定（19 条）` job `97026942737` `success` ·
  ⑤ `grep -n '2026-08-22-1206' …2220-2….md` → **零命中**（`0027-2` 那项欠账实测确实还欠着）·
  ⑥ `git merge-base --is-ancestor origin/main main` 成立 且 `git diff --name-only origin/main main`
  只含 `docs/logs/**` 与 `docs/plans/p0-foundation/**` · ⑦ `gh pr view 2 --json baseRefOid`
  → `f689d0e7cde…`，**逐字等于 `$CUT`**（C5 那颗钉子）· ⑧ `gh pr view 1` → `OPEN` / `c2c688b…` / `7b0f585…`，原封未动。
- **守卫预测预检（推之前算的，不是事后解释）**：`git fetch origin main` 后
  `git diff --name-only origin/main ci/1206-1-verdict-guard-proof -- tools/gates/check_expected_red.py tools/gates/gate-verify.mjs`
  → **无输出** ⇒ **情形 ①**（期望 `✅ 未触及判定器` / `success`）；trailer 计数 `0`，与 ① 自洽。
  `pull_request` 那一路同解（`BASE = baseRefOid = $RECUT`，两式退化成 `main` vs `main`+118 行）。
  **有约束力的是 Phase 2 落地推之前的重算。**
- **四个 `Decision` 定死**：落地失败 → **停机等人**（`revert` 只有人能选，执行器无授权）·
  落地方式 → **新分支 `ci/1206-2-l2-live-land` + 新 PR + `--ff-only`**（不碰前驱分支与 PR 一个字节）·
  PR 终局 → PR #1 与 PR #2 **均 `gh pr close` + 留说明评论，不删任何远程分支** ·
  授权面 → 沿用 `2026-08-21-2220-2` 先例（纯追加 = 加严），`ai-autonomy-policy.md` 措辞待人裁定继续挂着。
- **本机复跑**：`python3 tools/gates/check_expected_red.py` → exit 0（`门禁 19 项：预期红 7，绿 12，跳过 0` /
  `✅ 与预期红名单完全一致`）· `python3 -m pytest tests/unit -q` → exit 0（`221 passed`）。
- **本阶段一个字节都没碰 `.github/workflows/`**，改动只有本 plan 文件与本日志。

### 2026-08-22 · plan `1206-1` Phase 4 收尾推送 —— `main` push 运行七个 job 全绿，CI 终值 10 / 12

- **推前四条预检全为期望值**（无条件跑）：`git diff --stat origin/main main -- .github/workflows/` → 无输出 ·
  `git diff --name-only origin/main main -- tests/gates tools/gates/expected-red.txt` → 无输出 ·
  同上 `-- docs/masterplan/DECISIONS.md` → 无输出 ·
  `git diff origin/main main -- docs/masterplan/STATE.md | grep '^-' | grep -v '^---'` → 无输出。
- **`git push origin main` → exit 0**，`f689d0e..10da9e7`，**6 个文件全部在 `docs/**` 下**。
- **那次 `main` push 运行**：run **`32571701925`**（event `push`，head `10da9e7`）→ **`success`**，
  **七个 job 全部 `success`**：`gates-untouched` `97027947547` · `expected-red-ratchet` `97027947522` ·
  `gates-l1` `97027947498` · `masterplan-links` `97027947492` · `roadmap-parseable` `97027947439` ·
  `loop-wiring` `97027947500` · `gates-l2` `97027947541`。
- ⚠️ **这一轮只有 7 个 job 本身就是一条证据**：`gates-l2-live` 与 `verdict-tool-untouched`
  **仍不在 `main` 上**。所以这次 `main` push 运行**不构成**守卫 `push` 路径的实测，
  **也不构成工作项 9 判据的成立**——两者都归本批第二个 plan `2026-08-22-1206-2`。
- Phase 3 最终清理那次 force-push 开出的 run **`32571266013`**（head `b7348bf`）→ `success`，
  是「清理后 head 上有一次全绿」的第三次冗余复现。
- **CI 运行次数终值 10 / 12，未超声明预算**：`32569935835` · `32570222139` · `32570426423` ·
  `32570657720` · `32570691388` · `32570916073` · `32570942284` attempt 1 与 attempt 2 ·
  `32571266013` · `32571701925`。
- **收尾收敛判据成立**：`git merge-base --is-ancestor origin/main main` → 成立；
  `git diff --name-only origin/main main` → 只含 `docs/logs/**` 与 `docs/plans/p0-foundation/**`
  （本回填提交本身，**未推**，留给独立关闭审计那次回填一起走）。
  **不用「两值相等」**：本 plan 关闭还需一次独立审计回填提交，那会让「两值相等」在姊妹 plan
  开工那一刻必然为假，两个 plan 会死锁。

### 2026-08-22 · plan `1206-1` Phase 4 —— 留痕、红线自查与收尾推送（plan 置 `completed`，交独立审计）

- **§14.4 追加守卫实证结论**：`docs/architecture/system-baseline.md` 559 → **614 行，纯追加**。
  四条实验的 run id / job id / conclusion / 日志逐字表，外加**分开写的四句结论 + 同等醒目的第五条**：
  ① `pull_request` 四条出口里**两条稳定实证**、第三条只证到「可达」· ② `push` 路径未实证 ·
  ③ 全零 sha 分支永不可测 · ④ `|| true` 假阴入口仍在且**本批新引入** ·
  ⑤ ⚠️ 放行出口**同一 sha 上不可复现**。**没有写成比证据更强的说法。**
- **`2026-08-22-0027-2` 只被追加一行**：`git diff --numstat` → `13	0`（纯追加）；
  `grep -c '^\s*-\s\[ \]'` 仍 **19**；`Plan Status` 仍 `deferred`。**不代它打勾**。
- **四条本机命令全 exit 0**：`python3 tools/gates/check_expected_red.py` →
  `门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致` ·
  `python3 -m pytest tests/unit -q` → `221 passed` · `python3 -m pytest tests/contracts -q` → `151 passed` ·
  `ruff check agenerp tests/unit tests/contracts` → `All checks passed!`。
  **判定器默认环境输出的「前 / 后」两次实跑逐字节相同**（`diff /tmp/verdict-before.txt /tmp/verdict-after.txt`
  → 无输出）——Protected Areas 对判定器要求的那一条。本 plan 对判定器**净改动为零**。
- **红线自查四条全为期望值**（锚 `$CUT` = `f689d0e`，**不是** plan 原文写死的 `aba9a5f`——
  那个 sha 已被 `main` 甩在身后三个提交，照原文跑会因为**别人的提交**误判成红线违规）：
  `git diff --stat "$CUT"..HEAD -- tests/gates agenerp docs/masterplan/DECISIONS.md missions tools/gates` → **无输出** ·
  `git status --porcelain -- <同 pathspec>` → **无输出** ·
  `git diff "$CUT"..HEAD -- .github/workflows/` → **无输出**（**对 `main` 的 workflow 零改动**，118 行只在分支上）·
  `git diff "$CUT"..HEAD -- docs/masterplan/STATE.md | grep '^-' | grep -v '^---'` → **无输出**（只追加，`20	0`）。
- **一件明确不做的事，写下来免得下一轮以为漏了**：**不改 roadmap 工作项 9 的状态**。
  它的关闭判据要求那两个 job 在 `main` 的 `gates.yml` 上，实测
  `git show main:.github/workflows/gates.yml | grep -cE 'gates-l2-live|verdict-tool-untouched'` → `0`。
  **判据是红的，改它就是把红报成绿。** 该项归本批第二个 plan `2026-08-22-1206-2`。
- **plan 置 `completed`**，`## Closure Gates` 十七框留空，**由独立 `CLOSURE_VERIFY` 步回填** ——
  执行器自勾即自审，不算。
- **验证范围（scoped，不得读成 full green）**：本仓无全量套件（无 build、无 typecheck）。
  CI 侧本 plan 期间共 **10 次运行**（声明预算 12，**未超**）。守卫的 `push` 路径未实证、
  全零 sha 分支永不可测、`|| true` 假阴入口仍在。

### 2026-08-22 · plan `1206-1` Phase 3 —— 守卫的四次变异实证：①②③ 干净成立，④ 同一 sha 上一红一绿

- **实验 ①（正向，必须红）✅**：判定器末尾加空行、不带 trailer，提交 `47e0069`。
  run **`32570222139`** → run 结论 `failure`，**红 job 集合恰好 `{verdict-tool-untouched}`**
  （job **`97024540387`** `failure`），七个点名 job 全 `success`（`gates-l2-live` 也绿，不参与判定）。
  日志真实输出逐字 `本次改动触及判定器：` / `tools/gates/check_expected_red.py` /
  `❌ 改动了门禁判定器却没有人工批准。`。**「预期红」三条判据全中，归预期红，不计真红。**
  **守卫第一次被证明有牙齿**——此前它所有的绿都可能只是「从不触发」。
- **实验 ②（复原，必须绿）✅**：`git revert` 掉 ①，提交 `4516e7f`，仍不带 trailer。
  run **`32570426423`** → 九个 job 全 `success`，守卫 job **`97025008659`** 日志逐字 `✅ 未触及判定器`。
  **证明守卫比的是 `BASE..HEAD` 的累积 diff 而不是逐提交**（区间里明明有一个改判定器的提交仍放绿）。
- **实验 ③（边界，必须不触发）✅**：只给 `expected-red.txt` 加一行 `#` 注释，提交 `a8e8305`。
  run **`32570691388`** → 九个 job 全 `success`；守卫 job **`97025611324`** `✅ 未触及判定器`，
  棘轮 job **`97025611265`** `✅ 名单没有变长`（`grep -vE '^\s*(#|$)' | wc -l` 前后都是 `7`）。
  **「账本不在守卫路径清单内、注释不计数」这条硬边界第一次拿到实证。**
- **实验 ④（放行路径）⚠️ 不可复现**：判定器末尾加空行 + trailer
  `Gates-Change-Approved-By: EXPERIMENT-ONLY-NOT-AN-APPROVAL`，提交 `cf73d90`。
  run **`32570942284`** 两次 attempt **输入逐字节相同、结果相反**：
  attempt 1 守卫 job **`97026197943`** → **`failure`**（`❌ 改动了门禁判定器却没有人工批准。`）；
  attempt 2（`gh run rerun --failed` 原样复跑）守卫 job **`97026657710`** → **`success`**
  （`✅ 找到人工批准 trailer，放行`）。已机械核对两次的 merge ref（`1b3e5ea2…`）、`fetch` 命令行、
  插值出的 `BASE`/`HEAD`（`f689d0e7cde…` / `cf73d90c0dd…`）**逐字相同**，attempt 1 日志**无任何 `fatal:`/`error:` 行**。
  **按裁判规则 3 记「不可复现」，不猜根因。**
  **交付边界照实写**：放行出口**可达但不可靠**——`pull_request` 路径四条出口里，
  「未触及」「触及无 trailer 必红」两条**稳定实证**，「触及带 trailer 放行」只证到**可达**。
  这比 plan 立这条实验时担心的情形**更糟**（原担心是「打不开」，实测是「有时打得开有时打不开」）。
- **停机纪律归类**：attempt 1 的红不满足预期红第 (c) 条（head sha 不是实验 ① 的），
  **归真红 #1**；attempt 2 绿，连续真红计数在 1 处断掉，**未达「连续 2 轮真红即停机」**，
  因此继续执行——按规则算的，不是酌情放过。已把该缺陷登记进 plan 的
  `## Deferred But Adjudicated`（`Classification: needs-human` / `Successor Required: yes`）与 `STATE.md` §3。
- **一条本仓此前写着「没实测过」的事实实测出来了**：**force-push 回一个已跑过绿的 sha，每一次都会触发一次完整运行**
  （`32570657720` / `32570916073` / `32571266013` 三次全在 `b7348bf` 上）。按最坏情形算预算这个决定是对的。
- **清理七条机械判据全为期望值**：分支 sha 回到 `b7348bf`（= Phase 2 sha）· `git diff --numstat "$CUT" <分支>`
  仍恰好一行 `118	0	.github/workflows/gates.yml` · 判定器/账本净 diff **空** · 区间 trailer 计数 **0** ·
  PR #2 读数仍 `1 / 118 / 0` · `baseRefOid` 仍 `f689d0e…` · `headRefOid` = `b7348bf`。
  ⚠️ 措辞：`reset` + force-push **不等于「抹掉」**，四个实验提交按 sha 在 GitHub 上仍可访问；
  成立的说法只有「不进 `main`、不留在 PR 最终形态里」。
- **清理后的 head 上有一次全绿**：`headRefOid` = `b7348bf`，即 Phase 2 那次 run **`32569935835`**（九 job 全绿），
  未制造新的；`32570657720` / `32570916073` 是同一 sha 上的两次冗余复现。
- **读日志的硬规矩就地改准**：Phase 2 记录里给的 `sed 's/\x1b\[[0-9;]*m//g'` **在 macOS BSD sed 上不生效**，
  本阶段实际用的是 `perl -pe 's/\e\[[0-9;]*m//g'`，已实测可用。
- **CI 滚动计数 9 / 12**（rerun 的 attempt 按一次算）。

### 2026-08-22 · plan `1206-1` Phase 2 —— `gates-l2-live` 在当前 `main` 基线上重新拿到绿

- **「前」的本机判定基线**（Protected Areas 要求的两次实跑之一）：
  `python3 tools/gates/check_expected_red.py` → **exit 0**，
  `判定模式：default …` / `门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致`。
- **PR #2 上的运行 `32569935835`**（event `pull_request`，head `b7348bf`，run 结论 **`success`**）：
  **九个 job 全部 `success`**。`gates-l2-live` job **`97023867883`**，
  `verdict-tool-untouched` job **`97023868017`**，其余七个 job id 逐字记在 plan 的 Phase 2 执行记录里。
- **`gates-l2-live` 日志逐字**：`判定模式：live（AGENERP_LIVE=1）…` / `门禁 19 项：红 0，绿 19，跳过 0` /
  `✅ live 判定：全部门禁绿，零 red、零 skip`。**Baseline C4 那处过期的绿补成有效的了**——
  run `32533449466` 跑的是 `0228-1` 改判定器**之前**的版本，本次跑的是 `main` 上当前这一份。
- **`verdict-tool-untouched` 日志逐字** `✅ 未触及判定器`（基线出口）。
  ⚠️ **一条读日志的硬规矩**：`gh run view --job … --log` 会把脚本正文一起回显，那些行带 `\x1b[36;1m`
  前缀，且**同时含三条互斥出口的字面**。**必须先 `sed 's/\x1b\[[0-9;]*m//g'` 再判**，
  否则任何一次运行的日志都能「命中」全部出口。四条变异实验的判定一律按这条规则读。
- **首跑即绿**，`gh run rerun --failed` 未触发，无真红计数。CI 滚动计数仍 **1 / 12**（Phase 1/2 共用一次运行）。
- `<Phase 2 sha>` 钉死为 **`b7348bf3a1eb1eccbe1c032af8bd73ed808ed4af`**，Phase 3 三次 `reset --hard` 以它为目标。

### 2026-08-22 · plan `1206-1` Phase 1 —— 新建实验载体分支与 PR #2，`baseRefOid` 钉在 `$CUT` 上

- **开工第一件事是基线改准**：plan 里写的 `$CUT` 期望值 `4d7b311…` 已过期，`main` 又前进一个提交。
  实测 `git rev-parse main origin/main` → 两值相等，均为 **`f689d0e7cde3f2733b044b004f7a314f14958973`**，
  `git rev-list --left-right --count origin/main...main` → `0	0`。**`$CUT = f689d0e…`**，全篇判据以它为锚
  （plan 第 5 轮评审已把锚点从字面值改成 `$CUT` 符号，因此没有一条判据失效）。
- **核验相等 → 本阶段无 `main` push 运行**，四条推前预检未触发（触发条件是「核验不等而补推」）。
- **保命闸**：`git show origin/ci/0027-2-l2-full-live-gate:.github/workflows/gates.yml | tail -n 118 > /tmp/two-jobs.yml`
  （本 plan 唯一一次读那条分支，只读）。前缀性 `diff <(git show main:…) <(git show origin/ci/…:… | head -n 190)` → **无输出**。
- **动作三步**：`git switch -c ci/1206-1-verdict-guard-proof main` → 追加 118 行 → 单提交
  **`b7348bf3a1eb1eccbe1c032af8bd73ed808ed4af`**（`1 file changed, 118 insertions(+)`，message **不带**
  `Gates-Change-Approved-By:`）→ `git push -u origin …`（exit 0）→ `gh pr create` → **PR #2**。
  紧贴 `gh pr create` 之前又核了一次 `origin/main == main`，仍是 `f689d0e…`。
- **形态判据五条全中**：merge-base 逐字等于 `$CUT` · `git diff --numstat "$MB" <分支>` → 恰好一行
  `118	0	.github/workflows/gates.yml` · 区间提交数 `1` · 前缀 `diff` 无输出 · `diff /tmp/two-jobs.yml
  <(tail -n 118 …)` 无输出。**第五条（最承重）**：`gh pr view 2 --json baseRefOid` →
  `f689d0e7cde3f2733b044b004f7a314f14958973`，**逐字等于 `$CUT`**——C5 那个「`base.sha` 建 PR 时钉死」的陷阱躲开了。
- **trailer 洁净度**：`git log --format=%B "$CUT"..ci/1206-1-verdict-guard-proof | grep -c '^Gates-Change-Approved-By:'` → `0`。
- **PR 读数**：`gh pr view 2` → `OPEN` / `changedFiles 1` / `additions 118` / `deletions 0`（硬判据，无豁免）。
  **PR #1 一个字节未动**：`gh pr view 1 --json state,headRefOid,baseRefOid` → `OPEN` / `c2c688b7f6…` / `7b0f585f7c…`，与开工时逐字相同。
- **一处照实记的偏差**：开工时工作树带着前序会话未提交的
  `M docs/plans/p0-foundation/2026-08-22-1206-2-….md`（姊妹 plan 第 4 轮草案评审的改写）。
  Phase 3 要做三次 `git reset --hard`，它会**静默吞掉**这个已跟踪文件的改动，因此按「全程硬规矩」
  随本阶段文档提交一起提到 `main`，**内容一字未改**。后果：Phase 1 收敛判据原文要求差集只含
  `docs/logs/**` 与本 plan 文件，实际多一个姊妹 plan 文件——**不改判据去迁就，照实登记**，
  它仍在 `docs/plans/p0-foundation/**` 之内，与 Phase 4 那条更宽的收敛判据相容。
- **CI 消耗**：`gh pr create` 触发一次 `pull_request` run **`32569935835`**（head `b7348bf`）；
  推分支本身不触发（`on: push` 限定 `branches: [main]`）。滚动计数 **1 / 12**。

### 2026-08-22 · plan `1041-1` 独立关闭审计回填 —— Closure Gates 十一框实测勾上

- **审计方**：独立关闭审计步 MISSION_DRIVER `2026-08-22-112156-mission-driver`，fresh session，
  非本 plan 的执行器会话。审计基线为关闭时的 HEAD `70e67ad`；**全部命令由审计方自己复跑**，
  不采信 plan 内已记录的任何输出。
- **四条基线命令自行复跑，全 exit 0**：`ruff …` → `All checks passed!` ·
  `pytest tests/unit -q` → `221 passed` · `pytest tests/contracts -q` → `151 passed` ·
  `python3 tools/gates/check_expected_red.py` → `门禁 19 项：预期红 7，绿 12，跳过 0` /
  `✅ 与预期红名单完全一致`。与 Baseline 逐字一致。
- **红线自查自行复跑**：含 `agenerp` 的 pathspec `git diff --name-only 4ac3517 HEAD -- …` → **无输出**；
  `git status --porcelain` → **无输出**；全部变更 8 个文件全在 `docs/**` 下。
- **交付面对着活仓逐条核验（不采信 `[x]`）**：`sed -n '574,582p' … | grep "只有读方法\|未做"` → exit 1 无输出（D2/D3）·
  §11.6 `:334`–`:338` 未被顺手改动 · `project-context.md:60` 旧行逐字节是新行前缀（804→1525，纯追加，D4）·
  `ai-autonomy-policy.md:87` 行内三格程序化比对：Rule 格逐字节相同、Required Evidence 旧三条逐字仍在只增一条、
  Area 只增 `drop_columns`（**注意旧格不是新格的连续子串**，判据是逐条片段的超集判定）·
  Decision 三要素（候选表 / 否决 (b)(c) 理由 / 残余风险）确实落在 `ai-autonomy-policy.md` 本身 ·
  backlog 新条目存在（8865 字节，`触发条件` 命中）· roadmap `:62`/`:64` 纯追加且「保持 `planned`」两侧都在。
- **零 CI 消耗复核**：`git rev-parse origin/main` → `508c75b…`，与 Phase 4 记录逐字节相同；
  `git rev-list --count origin/main..main` → **9**（Phase 4 当时是 8，差的是其后置 `completed` 的
  `70e67ad` 这一个 `docs/**` 提交，**没有提交被推走**）。
- **回填结果**：Closure Gates **11 框**全部实测勾上（plan 内原写「十二框」，实际 11 条，已照实改准）；
  Closure Audit Evidence 与 Follow-up 写实。行首锚定判据
  `grep -B5 '^- \[ \]' <plan> | grep '^Status: completed'` → 无输出；全文 `grep -c "^- \[ \]"` → **0**。
  `node tools/mission-driver/src/plan-check.mjs … --strict` → **exit 0**，`"passed": true`，`totalUnchecked: 0`。
- **验证范围（照实写）**：本仓无全量套件；本轮不跑 L2、不起 docker 栈、不连活站点、**CI 未跑** ——
  不得读成「CI 已验证」或 full green。

### 2026-08-22 · plan `1041-1` Phase 4 —— 全量复跑与红线自查（plan 关闭，交独立审计）

- **四条基线命令原样复跑，四条全 exit 0，结论与 Baseline 逐字一致**（本 plan 不改代码，条数必须不变）：
  · `ruff check agenerp tests/unit tests/contracts` → **exit 0**（`All checks passed!`）
  · `python3 -m pytest tests/unit -q` → **exit 0**（`221 passed in 0.55s`）
  · `python3 -m pytest tests/contracts -q` → **exit 0**（`151 passed in 0.09s`）
  · `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 19 项：预期红 7，绿 12，跳过 0` /
    `✅ 与预期红名单完全一致`，判定行逐字节不变）
- **红线自查（基线 sha 钉死 `4ac3517`，不用裸 `git diff`）**：
  `git diff --name-only 4ac3517 HEAD -- tests/gates .github/workflows missions docs/masterplan/DECISIONS.md
  tools/gates/expected-red.txt tools/gates/check_expected_red.py agenerp` → **无输出** ✅
  （`agenerp` 也在清单里 —— 本 plan 明示不改一行代码）；`git status --porcelain` → **无输出**。
- **全部变更 8 个文件，无一例外在 `docs/**` 下**；三个提交 `e91f56e`（P1）· `9ed1d41`（P2）· `3b9fc9b`（P3）。
  iteration 2 评审要求入库的 durable 证据 `docs/audits/p0-foundation/2026-08-22-1041-1-draft-review-iteration-1.md`
  已随 `e91f56e` 入库。
- **零 `git push`，在线证据不是离线替代**：`git rev-list --count origin/main..main` → **8**
  （开工 5 + 本 plan 三个提交，没有提交被推走）；`git ls-remote origin main` → `508c75b0c8a0b2af30285d293b5ab694922d3eaa`，
  与 `git rev-parse origin/main` **逐字节相同**；`git reflog show --date=iso origin/main` 最新一条停在
  `2026-08-22 05:55:52 +0800`，**本轮无新条目**。`gates.yml` 的 `push: branches: [main]` 一次都没被触发。
- **验证范围（照实写，不得报成 full green）**：本仓**无全量套件**（无 build、无 typecheck）；本 plan
  **不跑 L2、不起 docker 栈、不连活站点**。**CI 未跑，且本 plan 明示不跑** —— 不得读成「CI 已验证」。
- **plan 置 `completed`**。**Closure Gates 十二框与 Closure Audit Evidence 留空，由独立 `CLOSURE_VERIFY`
  步回填** —— 执行器自勾即自审，不算。

### 2026-08-22 · plan `1041-1` Phase 3 —— 「不可逆 DDL 无代码级前置」登记进 backlog，交人裁定

- **新建 `docs/backlog/irreversible-ddl-has-no-code-level-precondition.md`**，形态照本目录既有条目
  `gate-fixtures-pollute-the-live-site.md`（`Status:` / `Created:` / 事实 / 现状 / 触发条件 / 可选处置 / 现在就能做的）。
- **事实四条，全部可判**：调用链一路自动无人工确认点（`pack.py:153` → `apply.py:251` → `:254` → `oob.py:255`）·
  `DROP COLUMN` 不可逆 · `grep -rn "backup" agenerp/*.py` → **零命中** ·
  被删列可能带真实业务数据（收窄口径只管「是不是本次残留」，**不管列里有没有数据**）。
  **不是假想的**：`0228-2` 那次 817012 字节 `.sql.gz` 备份是**人手敲的**，不是代码跑的。
- **为什么 loop 不能自己做（两条硬拦，都写进了条目）**：① 判据先行的豁免不适用——
  roadmap 工作项 9 那一格**逐字**排除了「绑着 `test_zero_dep_boot.py` 具体断言」的情形，
  而本路径归工作项 5/6、绑的是 `test_customization_roundtrip_delete.py` 的具体断言；
  补新门禁在**红线 1** 内，要 `Gates-Change-Approved-By:`。
  ② `1922-3` 的 Deferred 第一条逐字含「**站点侧的回滚只能手工做**」，
  那是**已裁定状态**，重开事件（出现用包在站点上建字段的调用方）**未发生**。
  条目里同时标清：「豁免只适用于 4/7/9」是**推论**不是原文，逐字的只有那段排除句。
- **触发条件三条**（任一即触发）：人选定处置档 / 人出具 `Gates-Change-Approved-By:` 补门禁 /
  `1922-3` Deferred 第一条的重开事件发生。**可选处置四档，每档标出代价**：
  (a) 前置备份（要动 `ALLOWED_CALLS`，全站备份对删一列过重）·
  (b) 列级取证（取证产物本身是业务数据落盘；**不是备份**，只把不可逆变难逆）·
  (c) fail-closed 开关（**会让 `::test_no_orphan_column_left_behind` 立刻转红**，要它继续绿就得动
  `tests/gates/**` —— 红线 1）· (d) 维持现状（文档级约束无强制力，P2 第一次踩到就是数据丢失）。
  **loop 不替人选。**
- **roadmap「5 现状」/「6 现状」各行内追加一句**，指向本 plan 与新条目。**`planned` 一个字未改**
  （不划 `expected-red.txt`，`done` 的字面条件依旧不可满足，沿用人在 STATE §2 11:20Z 的裁定）。
- **判据（roadmap 按「行内比对」判，不数 diff 加减行——in-place 追加必然产出一 `-` 一 `+`）**：
  · `git diff --stat 4ac3517 -- docs/backlog/p0-foundation-roadmap.md` → **1 file changed, 2 insertions(+), 2 deletions(-)**（恰好那两行）
  · 旧行去掉行尾 `|` 后**逐字节是新行前缀**：`5 现状` 3720→4787 字符、`6 现状` 4110→5177 字符，两行均 **True**
  · diff 中含「保持 `planned`」的 `-` 行数 = **2**、`+` 行数 = **2**，**两值相等**（状态词没被动过）
  · `grep -n "1041-1" docs/backlog/p0-foundation-roadmap.md` → 命中 `:62` 与 `:64`，各一处
  · `grep -c "触发条件" docs/backlog/irreversible-ddl-has-no-code-level-precondition.md` → **1**（≥1）
  · `python3 tools/gates/check_expected_red.py` → **exit 0**，判定行逐字节不变
  · `python3 -m pytest tests/unit -q` → **exit 0**，`221 passed in 0.54s`
- **验证范围**：本机 L1 判定环境。**不跑 L2、不起 docker 栈、不连活站点、零 CI 消耗。**

### 2026-08-22 · plan `1041-1` Phase 2 —— Protected Areas 具体化到落点一级（D1，**加严**）

- **性质先说清：不是「补一处漏掉的落点」。** `agenerp/apply.py` · `execute_plan` 早已在
  `docs/context/ai-autonomy-policy.md:87` 的落点列表里，而 `execute_plan`（`apply.py:251`）
  → `drop_orphan_columns`（`:254`）→ `agenerp/oob.py` · `drop_columns`（`:304`）是**一条调用链**，
  区域意义上本来就罩着。本次补的是**落点名字**与**一条对不可逆性说话的证据要求**。
- **Decision 取 (a)**：落点列表点名 `drop_columns` + 补一条 Required Evidence，**Rule 格仍是 `plan-first`**。
  否决 (b)`ask first` / (c)`blocked` 的理由：`apply_pack` 的清除面是工作项 5/6 已交付的**活路径**，
  门禁 `::test_no_orphan_column_left_behind` 正在判它；锁死等于让一条绿着的门禁的实现面
  进入不可维护状态。**候选、否决理由、残余风险三样都写进了 `ai-autonomy-policy.md` 本身**，不是只写在 plan 里。
- **新增的那条 Required Evidence 说的是「删错了能不能回来」**：动 `drop_columns` 这条不可逆路径的 plan，
  必须逐字写明改动后**站点侧回滚仍然只能手工做**（含手工前置命令原文），或写明它交付了什么
  代码级前置/取证并给出实跑证据 —— **两者取其一，不许略过不谈**。原三条 Evidence **逐字未动**。
- **只加严不放宽的自查，按「行内三格」比对**（in-place 改行必然产出一条 `-` 与一条 `+`，
  数 diff 加减行的判据在这里恒不可满足）：
  ① **Rule 格** 旧 `plan-first` / 新 `plan-first`，**逐字节相同**；
  ② **Required Evidence 格** 新值是旧值的**超集**（旧三条 + 补行自述 + 加严自述全部逐字仍在）；
  ③ **Area 格** 旧的两处仍在，新增 `agenerp/oob.py` · `drop_columns`。三条全过。
- **残余风险照实写进文档**：文档级约束对拿着 shell 的执行器**没有强制力**；
  且本行加严之后 `drop_columns` 在**代码侧仍然零备份零取证**（`grep -rn "backup" agenerp/*.py` → 零命中），
  那条真实风险由 Phase 3 的 backlog 条目承接，**本行不假装解决了它**。
- **判据**：
  · `grep -n "drop_columns" docs/context/ai-autonomy-policy.md` → 命中 `:87`（此前**零命中**）
  · `python3 -m pytest tests/unit -q` → **exit 0**，`221 passed in 0.55s`
  · `python3 tools/gates/check_expected_red.py` → **exit 0**，判定行逐字节不变
- **验证范围**：本机 L1 判定环境。**不跑 L2、不起 docker 栈、不连活站点、零 CI 消耗。**

### 2026-08-22 · plan `1041-1` Phase 1 —— 破坏性写这一族的文档面：两处假陈述改准（D2/D3）+ 一处效力范围具体化（D4）

- **`agenerp/**` 一行未改。** 本阶段全部落在 `docs/**`，产品行为零变更。
- **D2 · `docs/architecture/module-boundaries.md` §11.7 落点表 `:577`**：`SiteClient` 的状态格此前写
  「已实现（**只有读方法**）」，与 `agenerp/site.py:196` 的 `delete_custom_field` 直接冲突，
  也与同一份文档 §11.6（`:338`）的「已实现（B 半）」互相矛盾。已改准为
  「读方法 + **一条**写方法 `delete_custom_field`」，白名单口径指向 `agenerp/site.py:16-17`。
- **D3 · 同表 `:581`**：写 / 删方法那一格此前是一句否定态状态词，**从 2026-08-21 起就是假的**——
  交付它的 plan `2026-08-21-1922-3` 当天关闭，方法已落地并在活站点上实测删过字段。
  已改准为「已实现（判据 `tests/unit/test_site_client.py` 的 `WRITE_METHOD_ALLOWLIST`）」，
  **照实标注为「改准一句假陈述」而不是「新增一项」**，并写明它假了整整一天。
  行内**刻意不复述那个否定词**：本 plan 的机判判据要求 §11.7 表行范围内不再出现它，
  逐字原文的取法写在行内（`git show 4ac3517:… | sed -n '581p'`），不是抹掉证据。
- **D4 · `docs/context/project-context.md:60`（具体化，不是改判对错）**：末尾「动它之前先 `bench backup`」
  是一条**祈使句**，作为给手敲命令的人的操作建议**没有错，本次不删不弱化**。补的是**效力范围**：
  `apply_pack` 自动跑那条链（`pack.py:153` → `apply.py:251` → `:254` → `oob.py:255`）
  **没有任何东西执行它**——实测 `grep -rn "backup" agenerp/*.py` → **零命中**。
  该行同时指向 Phase 3 新建的 backlog 条目。**纯追加**：旧行逐字节仍是新行的前缀。
- **判据（本阶段不改代码，结论必须与基线逐字一致）**：
  · `sed -n '574,582p' docs/architecture/module-boundaries.md | grep -n "只有读方法\|未做"` → **无输出**（exit 1）
  · `grep -n "已实现（B 半）" docs/architecture/module-boundaries.md` 仍命中 `:338`（§11.6 那行未被顺手改动）
  · `sed -n '60p' docs/context/project-context.md | grep -c "bench --site frontend backup"` → **1**；
    同行 `grep -c "apply_pack"` → **1**（改动前该行为 **0**）
  · `python3 -m pytest tests/unit -q` → **exit 0**（`221 passed`，条数不变）
  · `python3 tools/gates/check_expected_red.py` → **exit 0**，判定行逐字节不变
    （`门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致`）
- **验证范围**：本机 L1 判定环境。**不跑 L2、不起 docker 栈、不连活站点、零 CI 消耗（不 `git push`）。**

### 2026-08-22 · plan `0228-2` —— 孤儿列门禁在**全新站点**上的红因：清干净了反而红（`agenerp/oob.py`）

- **红因原文，不是推断。** 本机 `docker compose down -v` 冷起后单跑该门禁 → **exit 1**，
  `1 failed, 3 passed in 8.53s`，逐字
  `agenerp.oob.OobError: frappe.model.meta.trim_table 的输出不是 JSON：''`（冷起站点 3 跑红 3 次）。
  与 CI run `32509351108` 两次 attempt 的点名一致。
- **分流 (b) 档，落点 `agenerp/oob.py` `run_json` 的空 stdout 分支。** 另两档已被独立证据排掉：
  `information_schema` 直查证明探针列**不在** `tabItem` 上（排 (a)：断言本身不会失败）；
  `drop_orphan_columns` 一条跳过 WARNING 都没打、DDL 真发了（排 (c)）。
  **清除面从来没坏过，红在巡检的表达能力。**
- **机制（容器内实读，不是猜）**：`apps/frappe/frappe/commands/utils.py:285` 逐字 `if ret:` 才打印——
  `bench execute` 对**假值返回一个字都不打印**。所以「该 DocType 上一条孤儿列都没有」
  （`trim_table` 回 `[]`）在这条通道上就是**零字节 stdout**，旧代码把它判成「载荷不是 JSON」。
- **它逐字解释了此前那个「方向相反」的实测差异**：本机常驻站点长期躺着别的门禁留下的残留
  （冷起前实测 `schema_drift("Item")` → `["agenerp_gate_probe"]`），清完自己的探针后集合**仍非空** → 绿；
  **全新站点没有残留 → 清完必然归零 → 必然红**（runner 2/2、本机冷起 3/3）。本机 6 跑红 1 次同源。
- **修法：`run_json` 对「exit 0 且 stdout 全空」返回哨兵 `FALSY_RESULT`**，由调用方按自身返回类型翻译
  （`schema_drift` → `()`）。**不吞异常、不放宽收窄**：`narrow_deletes` / `drop_orphan_columns` 的交集口径
  一字未改，非零退出仍抛，非空非 JSON 载荷仍抛。**刻意不用 `None` / `[]`**——`json.loads("null")` 就是 `None`。
- **「不是放宽」是实证的**：冷起站点上三种真故障逐条实跑，**全部非零退出**、走不到新分支——
  函数不存在 → exit 1 `AttributeError`；函数内部抛错 → exit 1 `pymysql.err.ProgrammingError`；
  站点不存在 → exit 1。
- **验证命令原文与退出码**：
  · `ruff check agenerp tests/unit tests/contracts` → **exit 0**（`All checks passed!`）
  · `python3 -m pytest tests/unit -q` → **exit 0**（`221 passed in 0.59s`，新增 4 条判据）
  · `python3 -m pytest tests/contracts -q` → **exit 0**（`151 passed in 0.06s`）
  · `python3 tools/gates/check_expected_red.py`（默认判定环境）→ **exit 0**（`预期红 7，绿 12，跳过 0`）
  · **全新站点 live 整目录判定连跑 3 次** → **exit 0 / 0 / 0**，均 `门禁 19 项：红 0，绿 19，跳过 0`
  · **多轮累积站点（不 `down -v`）第 4 跑** → **exit 0**，同一行输出
- **变异验证有牙齿，A/B 两跑都在各自冷起的全新站点上做**：删掉那两行 → **exit 1**，
  `门禁 19 项：红 1，绿 18，跳过 0`，点名集合**恰好一条**
  `tests/gates/test_customization_roundtrip_delete.py::test_no_orphan_column_left_behind`（与 CI 逐字一致）；
  单测同时 `2 failed, 219 passed`。复原后单测 `221 passed`、冷起站点判定 **exit 0**。
- **`0027-2` 指名要的证据在案**：实跑前后全量 `capture` 对照，两侧站点各一次，均为
  `entries added/removed: []` / `columns added/removed: []`；非空累积站点上 `agenerp_gate_probe`
  **原样保留**（一列都没多删），本次探针 `agenerp_gate_roundtrip` 经 `information_schema` 确认不在表上。
- **照实记的两条**：① 冷起前常驻站点的历史孤儿列实测是 **1 条**（`agenerp_gate_probe`），
  不是此前记的 5–6 条；② 冷起后孤儿列集合从空集变成 1 条，那一条由**另一条门禁**创建
  （`grep` 实证 `tests/gates/test_snapshot_diff_structured.py:39`），归既有 backlog
  `gate-fixtures-pollute-the-live-site.md`，不是本次 apply 造成的。
- **站点侧不可回滚，已按 plan 先备份并拷出卷**：`bench --site frontend backup` → exit 0，
  `docker compose cp … ./.backups-2026-08-22` → exit 0（817012 字节 `.sql.gz`，已入 `.gitignore`）。
- **`git diff` 未触及** `tests/gates/**` · `tools/gates/expected-red.txt` · `docs/masterplan/DECISIONS.md` ·
  `missions/**` · `.github/workflows/**`（实跑 `git diff --name-only HEAD | grep -E …` 无命中）。
- **Phase 4 · CI 实跑 1 次（硬上限 2）→ 绿**（补记，`d27c9a2` 那轮的回写此前只落在 STATE 与 plan 里）：
  推之前实跑 `gh pr view 1 --json state,baseRefOid` → `OPEN` / `7b0f585f7c…`（两条硬前置成立）；
  分支形态**只 cherry-pick `agenerp/oob.py` 与 `agenerp/snapshot.py` 两个文件**，未把 `main` 合进分支，
  `git diff --name-only 7b0f585 c2c688b -- tools/gates/check_expected_red.py tools/gates/gate-verify.mjs` → **无输出**。
  `git push origin ci/0027-2-l2-full-live-gate` → **exit 0**（`9a8832f..c2c688b`）。
  **run `32533449466`**（PR #1 `pull_request` synchronize，head `c2c688b`）→ `gh run view … --json conclusion`
  逐字 `"conclusion":"success"`，**九个 job 全绿**：`gates-l2-live`（job `96929876654`）日志
  `门禁 19 项：红 0，绿 19，跳过 0` / `✅ live 判定：全部门禁绿，零 red、零 skip`；
  `verdict-tool-untouched`（job `96929876658`）`success`。
  实跑次数核对 `gh run list --branch ci/0027-2-l2-full-live-gate` 推前 1 条、推后 2 条，**差 1**。
- **Phase 4 · 就地改准两处已确认的 owner-doc 漂移**（Minimum Rule 14，不降级为 follow-up）：
  `docs/backlog/p0-foundation-roadmap.md`「5/6/9 现状」三行与 `docs/context/project-context.md` 两行里
  「红在**实现**（清除面在 runner 的全新站点上不成立）」的表述已被本轮推翻，就地改准为
  「清除面从来没坏过，真红因在**巡检的表达能力**」，并注明依据 run `32533449466`
  （`grep -c 32533449466` → roadmap **3**、`project-context.md` **2**）。三张表结构未破。
  **仍不置任何工作项为 `done`**：`done` 要求「从 `expected-red.txt` 划掉」，默认判定环境无 `AGENERP_LIVE`、
  L2 恒红，条件不可满足（人在 STATE §2 2026-08-21T11:20Z 的裁定），与工作项 4/5/7/8/9 同一情形。
- **Phase 4 · 只追加不改写的三处**：`docs/masterplan/STATE.md` §2 一条证据行 + §3 那条 `[open]` 行**下方**
  一条 `[resolved]` 处置事实行（`git diff -U0 … | grep -c '^-[^-]'` → **0**，`17 insertions(+), 0 deletions`，
  `[open]` 行一字未动）；`2026-08-22-0027-2-…md` 的 `Closure Audit Log` 追加第五条
  （既有行零删除，**不代它改 `Plan Status`、一个勾未改**）。`docs/masterplan/DECISIONS.md` **一行未动**。
- **停机解除**：`AGENTS.md` 裁判规则 4 的「CI 连续 2 轮红」由本次同分支同 job 第 3 次实跑 `success` 解除；
  但**重开条件满足 ≠ `0027-2` 可以关闭**——它自己欠的守卫三次变异实证仍无 run id，19 个 `[ ]` 仍是真未做。

### 2026-08-22 · `GATE_VERIFY` —— plan `0228-1` 关闭审计回填入库 + **full-green verification**

- **本轮验证命令原文与退出码**：`python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q`
  → **exit 0**（`判定模式：default` / `门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致` /
  `217 passed in 0.57s`）。**名单一致即无测试转绿**，`tools/gates/expected-red.txt` 本轮**未改**（名单只能变短，本轮不该短）。
- **本轮唯一写入面**：`docs/plans/p0-foundation/2026-08-22-0228-1-...md` 的 `## Closure` / `## Closure Gates`
  两节（独立 `CLOSURE_VERIFY` 的审计结论回填）与本日志文件。`git status --porcelain -- tests/gates
  .github/workflows tools/gates/expected-red.txt missions docs/masterplan` → **输出为空**。
- **对回填内容做了哪些独立核对**（只核对便宜可复跑的，不替审计者背书）：
  · `git show --name-only --pretty=format: 57ad6d5` 共 8 个文件，逐个比对**无一**落在
    `tests/gates/` · `.github/workflows/` · `tools/gates/expected-red.txt` · `missions/` ·
    `docs/masterplan/DECISIONS.md` —— 红线自查复核通过；
  · `git show 57ad6d5 -- docs/architecture/system-baseline.md | grep '^-[^-]'` → 无输出、**exit 1**，纯追加属实；
  · `python3 tools/gates/explain_last_gate_failures.py` → **exit 0**，首行为出处行
    （`报告：…/.pytest-gates.xml｜junit timestamp=2026-08-22T06:13:39.778670+08:00｜文件 mtime=…`），
    次行 `红 7 条，逐条原文如下：` —— 取证出口在真实报告上确实出证。
- **口径限定，写清楚免得后人误读**：回填里那条「审计侧自行复跑的变异验证」（改 `failure_details()`
  首行 → `5 failed, 212 passed` → 复原）**本轮没有再跑一遍**，它是 `CLOSURE_VERIFY` 审计侧的记录，
  本轮只负责入库，不对它作独立背书。本轮这条绿**只覆盖默认判定环境与 `tests/unit`**，L2 live **没有跑**。
- **零 CI 消耗**：本轮不推送、不触发任何 workflow；交付提交 `57ad6d5` 仍未推送（`origin/main` = `508c75b`）。

### 2026-08-22 · plan `0228-1` —— 门禁判定失败取证：junit 报告不再被丢弃（**纯本机，零 CI**）

- **要解决的失效逐字记一遍**：CI run `32509351108`（job `96857746484`）失败步骤的**全部**日志，
  只有判定器那几行加一条 nodeid —— 没有断言原文、没有异常类型、没有 `agenerp.apply` 的 WARNING。
  本机同样取不到。**原因不是采集不足，是判定器把唯一的取证载体删了**：
  它 `-q --tb=no --junitxml=<JUNIT>` + `capture_output=True` 起 pytest，红因**只**在 junit 里，
  而报告解析完就被 `unlink`。（`--tb=no` 不影响 junit —— 它只压 pytest 自己的终端回显。）
- **交付三件**：① `tools/gates/check_expected_red.py` 的 `JUNIT.unlink(missing_ok=True)`
  **从解析之后挪到起 pytest 之前**，报告判定完留在盘上；② 判定器新增纯函数 `failure_details()`，
  与 `classify()` 共用同一套 `nodeid()` 拼法（不开第二套口径）；
  ③ 新增只读小工具 `tools/gates/explain_last_gate_failures.py`。
- **`unlink` 是前移不是删掉，这一条是本次最容易做错的地方。** pytest 收到未知参数时参数解析就失败、
  **不写**新报告，上一轮的报告会原样留在盘上（`timestamp=` 不变）。若只是去掉那句 `unlink`，
  判定器会拿**上一轮**的报告判出一个根本没发生过的结果，可能打出 `✅ 与预期红名单完全一致` 并 exit 0；
  触发路径是现成的（`main()` 把 `sys.argv[1:]` 原样转发给 pytest）。
  前移之后保命闸**不但没削弱，还多覆盖一路**：盘上留着旧报告时仍 `exit 2`（实测在案）。
- **取证出口做成独立小工具而非判定器子命令**：判定器把 `sys.argv[1:]` 原样转发给 pytest，
  加自有开关就要在转发面上开例外，而那正是保命闸的触发路径 —— 等于在受保护面上加风险。
- **陈旧可见是这次新加的一条口径**：报告长期驻盘之后，「拿旧证当新证」取代「取不到证」成为新失效面。
  取证出口的**第一行恒为出处行**（报告路径 + junit `timestamp` + 文件 mtime），`timestamp` 缺失打显式占位；
  **报告不存在时报错退 2**，绝不打印「没有失败」——那会让「取不到证」长得像「没红」。它只读，不写不删。
- **受保护面四条强制项，逐条在案**（判定器是 `ai-autonomy-policy.md` 的 `plan-first` 面）：
  ① 独立草案评审两轮（第二轮 `accept`，记在 plan）；② 独立关闭审计**未做，留给 loop 的 CLOSURE_VERIFY 步**
  —— 执行器不得自审受保护面；③ **默认判定环境前后两次实跑逐字节相同**：
  `python3 tools/gates/check_expected_red.py` 前后各一次均 **exit 0**，
  `diff /tmp/verdict-before.txt /tmp/verdict-after.txt` **无输出**；
  ④ **变异验证**：把 `failure_details` 改成恒返回空 dict → `pytest tests/unit -q` **exit 1**
  且逐字点名 5 条新判据（`..._keeps_message_and_body_of_a_failure` / `..._of_an_error` /
  `..._uses_explicit_placeholders_instead_of_empty_strings` / `..._and_classify_agree_on_nodeids` /
  `test_explain_prints_every_red_verbatim_and_never_touches_the_report`），复原后 **exit 0**。
- **保命闸前后对照**（每次实跑前先 `rm -f .pytest-gates.xml`，否则环境残留会让「前后相同」假红）：
  `python3 tools/gates/check_expected_red.py --this-arg-does-not-exist` 改动前 **exit 2**、改动后 **exit 2**。
- **取证出口两路实跑**：报告在 → `exit 0`，首行
  `报告：…/.pytest-gates.xml｜junit timestamp=2026-08-22T06:06:44.657199+08:00｜文件 mtime=2026-08-22T06:06:44`，
  随后打出 7 条红的原文（本机默认环境那 7 条是 setup error，逐字为
  `failed on setup with "Failed: compose_stack 需要 AGENERP_LIVE=1`）；报告不在 → `exit 2` 且 stdout 为空。
- **判据全部建在合成 junit 字符串上**：本机那 7 条 setup error 里根本没有断言原文，冒充不了取证判据。
  `tests/unit/test_gate_verdict.py` 从 12 条加到 24 条（`pytest tests/unit -q` 217 passed）。
- **验证（全绿，scope 说明见下）**：`python3 -m pytest tests/unit -q` exit 0（217 passed）·
  `python3 -m pytest tests/contracts -q` exit 0（151 passed）·
  `ruff check agenerp tests/unit tests/contracts` exit 0 ·
  `ruff check tools/gates/check_expected_red.py tools/gates/explain_last_gate_failures.py` exit 0
  （`tools/` 不在本仓惯用 lint 面内，新文件否则一次都不会被 lint 到；**不扩到整个 `tools/gates`**——
  该目录另有 3 条与本次无关的既存告警）。
  **⚠️ verification scope limited**：本仓无全量套件，以上**全部为本机 scoped 验证，CI 一次未跑**。
- **零 CI 消耗，这是刻意的**：`AGENTS.md` 裁判规则 4 的停机线仍在生效（STATE §3 写死的重开条件是
  「successor 修好 `agenerp` 侧清除面后推一次」，本 plan 不碰 `agenerp/**`，不满足）；
  且 `.github/workflows/**` 在 `ai-autonomy-policy.md` 定级 `blocked`。
  CI 侧取证消费面（`gates-l2-live` 上加 `if: failure()` 步骤）**AI 无合法通路**，
  记在 plan 的 `## Human Handoff`，**不是被降级的 in-scope 项**。
- **工作项 9 仍是 `planned`**：它的关闭判据逐字是「CI 上有 job 判 19 条并 `success`」，本次交付的是取证面。
- **红线自查**：`git status --porcelain` 未触及 `tests/gates/` · `.github/workflows/` ·
  `tools/gates/expected-red.txt` · `missions/` · `docs/masterplan/DECISIONS.md`（命令输出在 plan 内）。
  `system-baseline.md` §14.4 为**纯追加**（`git diff -- … | grep '^-[^-]'` 无输出）。

### 2026-08-22 · plan `0027-2` Phase 2/3 —— 两个新 job 落地、CI 第一次判 19 条、**连续两轮红，停机**

- **交付**（提交 `9a8832f`）：`.github/workflows/gates.yml` **末尾追加两个 job，纯新增零删除**——
  **`gates-l2-live`**（runner 上起栈后用 `python3 tools/gates/check_expected_red.py` 对**全部 19 条**做 live 判定）
  与 **`verdict-tool-untouched`**（判定器纳入服务端「未经批准不得改动」复核，
  **路径清单不含 `tools/gates/expected-red.txt`**——账本只能变短，归 `expected-red-ratchet` 管）。
- **红线 2 自查五条全部为期望值**：(a) 旧文件是新文件的**行前缀**（`diff` 无输出，一次覆盖
  「7 个 job 一行不改」「`on:`/`permissions:` 不动」「零删除」三件事）；(a2) job 键集合恰好 7+2=9；
  (b) 禁用词 0 命中；(c) 新增块 4 处 `if:` 全是取证/拆栈上的 `if: always()`；
  (d) 2 处多行 `run:` 首行均 `set -euo pipefail`，判定步骤是单条命令、无 `||`/`;`/尾随 `exit`。
  静态自检两条 exit 0（`yaml.safe_load` / `docker compose config -q`）。
- **CI 结论：红，且可复现。** run `32509351108`（`pull_request`，PR #1，head `9a8832f`）两次 attempt，
  `gates-l2-live` **都 `failure`**，判定器输出逐字完全相同：
  `判定模式：live（AGENERP_LIVE=1）—— 契约为全部门禁绿、零 skip，不读预期红名单` /
  `门禁 19 项：红 1，绿 18，跳过 0` /
  `❌ live 判定契约是全部门禁绿，下列门禁红了：` /
  `   tests/gates/test_customization_roundtrip_delete.py::test_no_orphan_column_left_behind`。
  **其余 8 个 job 两次全部 `success`。**
- **按 `AGENTS.md` 裁判规则 4（CI 连续 2 轮红即停机）停机。**
  第二跑是 `gh run rerun --failed` 的**原样复跑**（裁判规则 3：复跑优先于分析）——**它复现了**，
  所以结论**不是**「首轮红、复跑绿、不可复现」，**更不是**「CI 已验证」。
- **红因分流：红在实现，不是红在判据。** 那条门禁断言 `apply_pack` 删掉 Custom Field 之后必须连物理列一起清掉。
  断言在 runner 上不成立，**判据没问题，不成立的是被判的实现**。
  `agenerp/**` 是本 plan 的 Non-Goal，**一行没改**；交一个专门的 successor plan
  （必须带 Protected Areas 末行要求的「实跑前后全量 `capture` 对照」证据）。**不猜根因。**
- **本次最有价值的一条新事实**：同一条门禁**本机 6 跑红 1 次**（前驱 `0027-1`，复跑 4 次全绿，记「不可复现」），
  **runner 2 跑红 2 次**。plan 起草时写的推理「runner 的全新站点没有历史孤儿列，方向恰好**有利**」
  **被实测证伪**。**这次停机是这条 plan 兑现了它存在的理由，不是它失败了**——
  CI 第一次跑就抓出一条本机独证掩盖着的**稳定红**。
- **PR 路径首测**（本仓 91 个提交 0 个 merge commit，此前从未跑过）：往分支推送**不触发任何运行**
  （`on: push` 限定 `branches: [main]`），只有开 PR 的 `pull_request` 事件触发；
  `gates-untouched` / `expected-red-ratchet` 在 PR 上均 `success`，`base.sha`/`head.sha` 路径实测可用。
  **`Decision` 二选 PR 的可逆性在这里兑现**：回滚 = 不合并，`main` 全程干净。
- **五处确认的 owner-doc 漂移就地改准**（Minimum Rule 14，没降级成 follow-up），
  **全部按 plan 事先钉死的第三种写法**（「CI 上实跑过，结论是红，红因是 X」）：
  roadmap「5 现状」「6 现状」「9 现状」、`project-context.md` 两行、`system-baseline.md` §14.4
  （后者加了**上线状态段**：两个 job 不在 `main` 上，PR 未合并；并把「空窗期终点」「残余风险已收口」两句改准）。
  **`2026-08-21-2220-1` 的 Closure Gates 一个字未改。**
- **站点污染那条 Deferred 已裁定：维持 watch-only。** CI 日志实测拆栈删掉 12 个容器 +
  **5 个卷全部 `Removed`** + 网络（⚠️ 判定步骤是红的那一跑，`if: always()` 仍然执行了），
  CI 站点仍是一次性的、残留不累积。触发条件改绑为「当 CI 的 L2 站点不再是一次性的时」。
  **那条红不是站点污染问题**——CI 站点是全新的，没有历史孤儿列可污染。
- **守卫尚未交付，照实记**：`verdict-tool-untouched` 两次 attempt 都 `success`，
  但那证明不了它有牙齿。三次变异实验要再跑三轮 CI，停机线已触发，**没有做**。
  因此**也没有**去前驱 plan `2026-08-21-2220-2` 里追加「已了结」那一行——写它就是比证据更强的说法。
- **保护面自查全部为空**：`agenerp/**` / `tests/gates/**` / `DECISIONS.md` / `missions/**` /
  `tools/gates/check_expected_red.py` / `tools/gates/expected-red.txt` ——
  `git diff --stat 7b0f585..HEAD` 与 `git status --porcelain` 两条命令对该 pathspec **均无输出**。
  `STATE.md` **只追加**（`git diff --numstat` → `23	0`）。
- 验证：`check_expected_red.py && pytest tests/unit -q` → **exit 0**（`205 passed`）；
  `pytest tests/contracts -q` → **exit 0**（`151 passed`）；`ruff check` → **exit 0**。
  **本仓无全量套件，这些绿是 scoped verification；而本 plan 真正要交付的那条 CI 判定是红的。**
- `Plan Status` 置 **`deferred`**（plan 自己列明的三条自置 deferred 路径命中了两条）。
  needs-human 已追加进 `docs/masterplan/STATE.md` §3。

### 2026-08-22 · plan `0027-2` Phase 1 —— 前置五条核对、四个 `Decision`、`## 14.4` 补上 CI 用法那半

- **开工 sha `7b0f585f7c8082a64902da65e6e3314cb239dc9f`**，`git status --porcelain` 无输出。
- **前置五条全部成立**：前驱 `0027-1` 已 `completed`；默认环境
  `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 19 项：预期红 7，绿 12，跳过 0`）；
  `grep -n '^## 14.4' docs/architecture/system-baseline.md` → 第 **383** 行有命中；
  前驱 live 整目录已被证明能返回 exit 0（六跑五绿一红，那一红不可复现）。
- **四个 `Decision` 落定**：① 两个新 job 全部**追加在 `gates.yml` 末尾**（保住「行前缀」这条机械判据）；
  ② CI 落地走 **PR** 而非直推 `main`（决定性理由：守卫的 experiment ① 必须红，PR 把它挡在 `main` 之外）；
  ③ 主判定 job `gates-l2-live` 用 `python3 tools/gates/check_expected_red.py` 判 19 条，
  env 四条、`AGENERP_HTTP_PORT` 不设；④ 取证三条一律 `-f` + `if: always()`，
  判定步骤 `run:` 是单条命令，新增块的多行 `run:` 一律 `set -euo pipefail`。
- **落地路径带出一个起草时没预见到的必做动作**：开工时 `origin/main` 停在 `8b1e95c`，本地领先 6 个提交，
  而那 6 个提交**改过 `tools/gates/check_expected_red.py`**。不先推 `main`，PR 的 base 就是陈旧的，
  新守卫会因为看见前驱的改动而报**假阳**。处置：先 `git push origin main`（`8b1e95c..7b0f585`），再开分支。
- **`## 14.4` 补上两个小节**：「CI 怎么用这个判定模式」（含 `AGENERP_SITE=frontend` 把
  `::test_two_snapshots_of_unchanged_site_diff_empty` 与 `::test_diff_is_structured_not_text`
  从离线翻到活站点上这条副作用、以及 live 契约比棘轮更紧因而覆盖了那条残余风险）
  与「判定器的 CI 侧守卫与它的实证边界」（守卫只实证了 `pull_request` 路径）。
  同时把节内三处因本次交付而失效的措辞就地改准（「本节只写判定契约」「守卫还没上线」「收口归工作项 9」）。
- **一处行号照实更正**：plan 里写的 `check_expected_red.py:59/:61` 是起草时的行号，
  前驱改过该文件，实测现在是 **`:67`/`:69`**；引用的事实没变，按活文件写。
- 验证：`python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**。

### 2026-08-22 · plan `0027-1` Phase 3 —— live 整目录实测（本仓第一次）、三条变异验证、收尾

- **本仓第一次让 19 条门禁在 live 判定环境下一次跑完**（此前 live 绿证都是按文件分开跑的，只覆盖 10/19）。
  命令：`AGENERP_HTTP_PORT=18080 AGENERP_LIVE=1 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080
  AGENERP_ADMIN_PASSWORD=admin python3 tools/gates/check_expected_red.py`。
- **六跑，五绿一红**：
  - 第 1 跑 → **exit 1**，`门禁 19 项：红 1，绿 18，跳过 0`，逐字点名
    `tests/gates/test_customization_roundtrip_delete.py::test_no_orphan_column_left_behind`
  - 第 2–5 跑（**原样复跑，命令一字未改**）→ **exit 0**，`门禁 19 项：红 0，绿 19，跳过 0` /
    `✅ live 判定：全部门禁绿，零 red、零 skip`
  - 第 6 跑（变异 ① 复原之后）→ **exit 0**，同上
- **按 `AGENTS.md` 裁判规则 3 记为「不可复现」，不猜根因。** 不写「是门禁互相干扰」、也不写「是环境抖动」——
  两者都没有证据。**没有用 `-p no:randomly` / `-x` / 收窄目录之类的手段掩盖。**
  已立一条 `watch-only residual`（重开事件：再次观察到同一条门禁在整目录 live 判定下红，本机或 CI 皆算），
  并作为**已知风险**交办给本批第二个 plan —— CI 上一次红就是一次红，没有人在旁边复跑。
- **`agenerp/apply.py` 真删除路径跑了两次，两次都带 Protected Areas 末行要求的证据**：
  前后全量 `capture("doctypes")` 差集**都为空**（10 条进、10 条出，added / removed 均为 `[]`）——
  连门禁自己的探针都没剩下。`capture` 没有 CLI，按 plan `1922-3` 的先例用一次性脚本，**该脚本不提交**。
- **三处变异窗口全部复原，零产品行为发布**：
  - `agenerp/pack.py` 的 `normalize` 改恒等（Phase 2）→ `GATE_VERIFY` 字面命令 exit 1，点名三条 normalizer 门禁
  - `agenerp/apply.py` 删除路径改 no-op → live 判定 exit 1，**点名 nodeid 集合之差恰好一条**
    （`::test_removing_from_pack_actually_deletes_on_site`）；判据是集合之差，不是退出码
  - **判定器自己**的 `verdict()` `skipped` 分支删掉 → `python3 -m pytest tests/unit -q` exit 1 且逐字点名
    `test_default_skip_fails_even_when_everything_else_matches` 与 `test_live_any_skip_fails`
  三处均 `git checkout` 复原并复跑回 exit 0。
- **一处确认的 owner-doc 漂移就地改准**（Minimum Rule 14，没降级成 follow-up）：
  `docs/context/project-context.md` 写着「`tests/gates/conftest.py` 全文不设这个变量」——**那是错的**，
  `conftest.py:274` 在 `live_site` fixture 内部会设它。正确说法更窄：
  `test_snapshot_diff_structured.py` 那两条不取任何 fixture、直接调 `capture()`，走不到那行。
- 同表新增一行「L2 live 门禁（**整目录判定**）」，**如实收录了第一跑的 exit 1 与「不可复现」**；
  **没有**把诊断用的 `--ignore` 写进表里（本次压根没跑它——正向对照由整目录全绿满足）。
- roadmap 补「9 现状」行，工作项 9 停在 `planned`（关闭判据是「CI 上跑绿」，本 plan 不碰 CI）。
- **口径限定，不含糊其辞：live 只在本机做过，CI 未验证。** 本机 Docker 29.2.1 / Compose v5.0.2，端口 18080。
  本仓此刻没有全量套件，上面这些绿是 scoped verification。

### 2026-08-22 · plan `0027-1` Phase 2 —— 判定器的 live 判定模式 + 纯函数接缝 + 12 条单测

- `tools/gates/check_expected_red.py`：分类与判定从 `run_pytest()` / `main()` 里抽成两个纯函数
  —— `classify(junit_xml)` 只吃 junit 文本、`verdict(outcomes, expected_red, live)` 只吃分类结果与名单。
  `main()` 退化成「组装 + 打印」。`healed_env()`、pytest 调用参数、junit 文件名**未改**。
- **新增 live 判定模式**：`AGENERP_LIVE=1` 选中（与 `tests/gates/conftest.py` 的 `_require_live()`
  同一个开关），契约是「全部门禁绿、零 red、零 skip」，**不读** `expected-red.txt`。
  两种模式都打印一行模式行。判定契约与偏离记录见 `docs/architecture/system-baseline.md` §14.4。
- **默认判定环境的判定行逐字节不变**：`门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致`，
  与开工前（`084c9c4`）逐字一致；新增的只有那行模式行。
- 新建 `tests/unit/test_gate_verdict.py`（12 条，输入是手写 junit XML 片段，不起 pytest 子进程），
  覆盖两模式共八态 —— 其中 **skip 判定第一次有判据覆盖**（`pytest` 对全部 skip 的一轮照样退 0，判定器不然）。
- 验证：
  - `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**（`205 passed in 0.55s`）
  - `ruff check agenerp tests/unit tests/contracts` → **exit 0**（`All checks passed!`）
  - `bash tools/gates/smoke-loop-wiring.sh` → **exit 0**（回归守卫；该脚本从不调用判定器）
  - **负向对照有牙齿**：把 `agenerp/pack.py` 的 `normalize` 临时改成恒等 → 组合命令 **exit 1**，
    判定器逐字点名 `test_normalizer_idempotent.py` 三条；`git checkout` 复原后复跑回 **exit 0**。
- 口径限定：本轮**没有跑 L2**。live 模式此刻只有单测证据，**尚未在活站点上跑过**，归 Phase 3。

### 2026-08-22 · plan `0027-1` Phase 1 —— 判定契约、判定器的保护、工作项 9

- 开工 sha `084c9c443cd7db6c3f9189addcad59edf0c191ff`，进场工作树只有本批两个未跟踪的 plan 文件。
- **纯文档轮**：`docs/context/ai-autonomy-policy.md`（Protected Areas 新增判定器本体一行，**加严**）·
  `docs/backlog/p0-foundation-roadmap.md`（新增工作项 9 + 对照表一行）·
  `docs/architecture/system-baseline.md`（原第 381–393 行那个误挂在 `## 14.3` 下的不带编号 `###`
  提升为独立的 `## 14.4` 并改写）。**判定器本身本轮一个字节没改。**
- 三个 `Decision` 落地：live 契约写死为「全部绿、零 red、零 skip」（不加名单文件，偏离已记）·
  模式开关取环境变量 `AGENERP_LIVE=1`（与 `conftest.py` 的 `_require_live()` 同一个开关）·
  roadmap 新增工作项 9（规则「一个工作项 = 1–2 个 plan」自己指名的处置）。
- **本轮登记的既有洞**：判定器三层皆无保护（`gates-untouched` 只 diff `tests/gates/**`、
  `gate-verify.mjs` 的 `PROTECTED = ["tests/gates/"]`、`expected-red-ratchet` 只数 txt 行数），
  而 `gates-l1` 跑的就是判定器本身——改废后会在 CI 上自证为绿。文档层已加严，
  CI 侧守卫（`verdict-tool-untouched`）指名交给 plan `2026-08-22-0027-2`，空窗期写进 `## 14.4`。
- 复跑：`python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致` / `193 passed in 0.52s`）。
- 口径限定：本轮**没有跑 L2**，不对 live 门禁的当前状态作任何断言。

### 2026-08-22 · `GATE_VERIFY` 干净树独立复跑 —— **full-green verification**（零改动轮）

- **本轮没有任何代码或文档实现改动**：进场时 `git status --porcelain` 输出为空，
  `HEAD` = `8b1e95cf1995b9b1ed2fa683a136ad3be26008ae`（`docs(p0-foundation): 2220-2 补记关闭审计改动的
  CI 第二次复跑`）。所以本条不是交付记录，只是给未来调试留一个**已知good基线**（AGENTS.md 第 200 条）。
- 复跑的就是 mission 配置里的那条，未改写：
  - `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
    - 前半：`门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致`
    - 后半：`193 passed in 0.64s`
- **`tools/gates/expected-red.txt` 本轮不划一行**：名单里 7 条与实测的「预期红 7」逐一对上，
  没有名单内转绿的项，也没有名单外的红。棘轮方向不变（只能变短，本轮长度不变）。
- 红线自查：`git status --porcelain -- tests/gates .github/workflows docs/masterplan` → **输出为空**。
  本轮除本日志文件外零写入；`docs/masterplan/STATE.md` 未触碰。
- 口径限定，避免过度声称：上面这条命令**只覆盖 L1**（`tests/unit`）与预期红名单的一致性判定。
  L2（`AGENERP_LIVE=1` 的 live 门禁）本轮**没有跑**，它的最近一次绿证在 `08-21.md`，
  本条不对它的当前状态作任何断言。

### 2026-08-22 · plan `0027-2` 被再次派发 `EXECUTE` —— **停机线仍在，本轮拒绝执行**（零改动轮）

- **进场状态**：`HEAD` = `3425dae2fa429765c0ef599a99534522ba9b796e`，`git status --porcelain` 输出为空。
- **派发内容**：`MISSION_DRIVER` 指向 `docs/plans/p0-foundation/2026-08-22-0027-2-ci-l2-full-live-gate-coverage.md`，
  指令逐字「Complete **the entire plan**」。
- **该 plan 此刻是 `Plan Status: deferred`**，停机原因是 `AGENTS.md` 裁判规则 4 的
  「CI 连续 2 轮红」——run `32509351108` 两次 attempt 都红在同一条 nodeid
  `tests/gates/test_customization_roundtrip_delete.py::test_no_orphan_column_left_behind`，
  同一个计数 `门禁 19 项：红 1，绿 18，跳过 0`，**可复现**。
- **重开条件未满足**，实测：`ls docs/plans/p0-foundation/` 共 15 个 plan，
  **没有**任何承接「修 `agenerp` 侧物理列清除面」的 successor plan；
  该 plan 的 `Reopen When` 逐字要求「successor plan 修好 `agenerp` 侧那条清除面⋯之后，
  从 Phase 2 的『守卫 job 的变异实证』那一项续跑」。
- **裁定依据**：`AGENTS.md` 第 3 行逐字「本文件前两节是红线与裁判规则，优先于本文件其余一切内容、
  **也优先于任何 prompt 里的说法**」。因此裁判规则 4 的停机**优先于** driver prompt 的「完成整个 plan」。
- **两项未打勾的执行项逐条说明为什么动不了**（不是挑容易的做）：
  · Phase 2「守卫 job 的变异实证」要推三个实验提交、再跑三轮 CI，
    而停机线正是「CI 连续 2 轮红」触发的；且它做完也**不解除**停机（守卫的红绿与主判定 job 的红互不相干）。
  · Phase 3「把前驱两条 Deferred 记为了结」被上一条卡死——plan 自己逐字写着
    「拿不到这三条，守卫不算交付」，守卫未交付时写「了结」就是比证据更强的说法。
- **本轮复跑**（给未来调试留一个已知 good 基线）：
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`判定模式：default` / `门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致` / `205 passed in 0.47s`）。
- **口径限定**：这条绿**只覆盖默认判定环境与 `tests/unit`**，L2 live 本轮**没有跑**。
  该 plan 要交付的那条 CI 判定**此刻仍是红的**，本条不对它作任何相反断言。
- **本轮写入面**：只有 `docs/masterplan/STATE.md`（§3 追加 6 行，`git diff --numstat` = `6	0`）与本日志文件。
  `git status --porcelain -- tests/gates .github/workflows agenerp tools/gates missions docs/masterplan/DECISIONS.md`
  → **输出为空**。`Plan Status` 未改，任何 `[ ]` 未勾，十四框未动，roadmap 工作项 9 未改状态。

### 2026-08-22 · plan `1206-1` 关闭审计回填后的 full-green verification（验证轮）

- **进场状态**：`HEAD` = `d1d30c5`，`git status --porcelain` 只有一行
  ` M docs/plans/p0-foundation/2026-08-22-1206-1-verdict-guard-mutation-proof.md`
  （上一步 `CLOSURE_VERIFY` 回填的 17 个勾与 Closure Audit Evidence，**代码面零改动**）。
- **本机验证命令逐字与退出码**：
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`判定模式：default —— 按 tools/gates/expected-red.txt 判定` /
  `门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致` / `221 passed in 0.74s`）。
- **`expected-red.txt` 本轮未改**：判定器输出「与预期红名单完全一致」，无门禁转绿，名单无可划项。
- **红线自查**：`git status --porcelain -- tests/gates .github/workflows agenerp tools/gates missions docs/masterplan`
  → **输出为空**。裁判与 workflow 一字未动。
- **口径限定**：这条绿**只覆盖默认判定环境与 `tests/unit`**，本轮**没有**跑 L2 live、`tests/contracts`、ruff
  （mission 配置里只有这一条命令非空）。**不得读作全量验证通过。**

### 2026-08-22 · plan `1206-2` 独立关闭审计回填后的 full-green verification（验证轮）

- **进场状态**：`HEAD` = `7aee223`，`git status --porcelain` 只有一行
  ` M docs/plans/p0-foundation/2026-08-22-1206-2-gates-l2-live-lands-on-main.md`
  （上一步独立关闭审计回填的 `closure audit was independent` 一勾 + `Closure Audit Evidence` 正文
  + CI 计数由 `6 / 7` 改准为 `5 / 7`，**代码面零改动**，`git diff --stat` = `1 file changed, 29 insertions(+), 6 deletions(-)`）。
- **本机验证命令逐字与退出码**：
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`判定模式：default —— 按 tools/gates/expected-red.txt 判定` /
  `门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致` / `221 passed in 0.61s`）。
- **`expected-red.txt` 本轮未改**：判定器输出「与预期红名单完全一致」，无门禁转绿，名单无可划项。
- **`plan-check --strict`**：`node tools/mission-driver/src/plan-check.mjs <本 plan> --strict` → `passed: true`
  （`planStatus: completed`，`totalChecked: 67`，`totalUnchecked: 0`）。
- **红线自查**：`git status --porcelain -- tests/gates .github/workflows agenerp tools/gates missions docs/masterplan`
  → **输出为空**。裁判、workflow、判定器名单、masterplan 一字未动。
- **口径限定**：这条绿**只覆盖默认判定环境与 `tests/unit`**，本轮**没有**跑 L2 live、`tests/contracts`、ruff / mypy / docker
  （mission 配置里只有这一条命令非空）。**不得读作全量验证通过。**
