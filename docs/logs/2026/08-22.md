# Development Log - 2026-08-22

### 2026-08-22 · plan `0027-1` Phase 2 —— 判定器的 live 判定模式 + 纯函数接缝 + 12 条单测

- `tools/gates/check_expected_red.py`：分类与判定从 `run_pytest()` / `main()` 里抽成两个纯函数
  —— `classify(junit_xml)` 只吃 junit 文本、`verdict(outcomes, expected_red, live)` 只吃分类结果与名单。
  `main()` 退化成「组装 + 打印」。`healed_env()`、pytest 调用参数、junit 文件名**未改**。
- **新增 live 判定模式**：`AGENERP_LIVE=1` 选中（与 `tests/gates/conftest.py` 的 `_require_live()`
  同一个开关），契约是「全部门禁绿、零 red、零 skip」，**不读** `expected-red.txt`。
  两种模式都打印一行模式行。判定契约与偏离记录见 `docs/architecture/system-baseline.md` §14.4。
- **默认判定环境的判定行逐字节不变**：`门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致`，
  与开工前（`084c9c4`）逐字一致；新增的只有那行模式行。
- 新建 `tests/unit/test_gate_verdict.py`（12 条，输入是手写 junit XML 片段，不起 pytest 子进程），
  覆盖两模式共八态 —— 其中 **skip 判定第一次有判据覆盖**（`pytest` 对全部 skip 的一轮照样退 0，判定器不然）。
- 验证：
  - `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**（`205 passed in 0.55s`）
  - `ruff check agenerp tests/unit tests/contracts` → **exit 0**（`All checks passed!`）
  - `bash tools/gates/smoke-loop-wiring.sh` → **exit 0**（回归守卫；该脚本从不调用判定器）
  - **负向对照有牙齿**：把 `agenerp/pack.py` 的 `normalize` 临时改成恒等 → 组合命令 **exit 1**，
    判定器逐字点名 `test_normalizer_idempotent.py` 三条；`git checkout` 复原后复跑回 **exit 0**。
- 口径限定：本轮**没有跑 L2**。live 模式此刻只有单测证据，**尚未在活站点上跑过**，归 Phase 3。

### 2026-08-22 · plan `0027-1` Phase 1 —— 判定契约、判定器的保护、工作项 9

- 开工 sha `084c9c443cd7db6c3f9189addcad59edf0c191ff`，进场工作树只有本批两个未跟踪的 plan 文件。
- **纯文档轮**：`docs/context/ai-autonomy-policy.md`（Protected Areas 新增判定器本体一行，**加严**）·
  `docs/backlog/p0-foundation-roadmap.md`（新增工作项 9 + 对照表一行）·
  `docs/architecture/system-baseline.md`（原第 381–393 行那个误挂在 `## 14.3` 下的不带编号 `###`
  提升为独立的 `## 14.4` 并改写）。**判定器本身本轮一个字节没改。**
- 三个 `Decision` 落地：live 契约写死为「全部绿、零 red、零 skip」（不加名单文件，偏离已记）·
  模式开关取环境变量 `AGENERP_LIVE=1`（与 `conftest.py` 的 `_require_live()` 同一个开关）·
  roadmap 新增工作项 9（规则「一个工作项 = 1–2 个 plan」自己指名的处置）。
- **本轮登记的既有洞**：判定器三层皆无保护（`gates-untouched` 只 diff `tests/gates/**`、
  `gate-verify.mjs` 的 `PROTECTED = ["tests/gates/"]`、`expected-red-ratchet` 只数 txt 行数），
  而 `gates-l1` 跑的就是判定器本身——改废后会在 CI 上自证为绿。文档层已加严，
  CI 侧守卫（`verdict-tool-untouched`）指名交给 plan `2026-08-22-0027-2`，空窗期写进 `## 14.4`。
- 复跑：`python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致` / `193 passed in 0.52s`）。
- 口径限定：本轮**没有跑 L2**，不对 live 门禁的当前状态作任何断言。

### 2026-08-22 · `GATE_VERIFY` 干净树独立复跑 —— **full-green verification**（零改动轮）

- **本轮没有任何代码或文档实现改动**：进场时 `git status --porcelain` 输出为空，
  `HEAD` = `8b1e95cf1995b9b1ed2fa683a136ad3be26008ae`（`docs(p0-foundation): 2220-2 补记关闭审计改动的
  CI 第二次复跑`）。所以本条不是交付记录，只是给未来调试留一个**已知good基线**（AGENTS.md 第 200 条）。
- 复跑的就是 mission 配置里的那条，未改写：
  - `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
    - 前半：`门禁 19 项：预期红 7，绿 12，跳过 0` / `✅ 与预期红名单完全一致`
    - 后半：`193 passed in 0.64s`
- **`tools/gates/expected-red.txt` 本轮不划一行**：名单里 7 条与实测的「预期红 7」逐一对上，
  没有名单内转绿的项，也没有名单外的红。棘轮方向不变（只能变短，本轮长度不变）。
- 红线自查：`git status --porcelain -- tests/gates .github/workflows docs/masterplan` → **输出为空**。
  本轮除本日志文件外零写入；`docs/masterplan/STATE.md` 未触碰。
- 口径限定，避免过度声称：上面这条命令**只覆盖 L1**（`tests/unit`）与预期红名单的一致性判定。
  L2（`AGENERP_LIVE=1` 的 live 门禁）本轮**没有跑**，它的最近一次绿证在 `08-21.md`，
  本条不对它的当前状态作任何断言。
