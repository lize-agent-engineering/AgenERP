# 2026-08-28 · P2.3 视图 Agent 落地

> `docs/plans/p2-views/2026-08-28-P2.3-view-agent.md` 的执行记录。

## 交了什么

自然语言 → 视图 DSL 的控制循环（`agenerp/views/`），外加三层判据。
**校验由循环无条件执行，模型没有绕过它的路。**

| 层 | 命令 | 结果 |
|---|---|---|
| 离线本体 | `pytest tests/agents/test_view_agent.py -q` | **33 passed**（WBS §5 第 103 行那条验收） |
| 判据目录整体 | `pytest tests/agents -q -m "not live"` | **48 passed, 13 deselected** |
| 活体端到端 | `pytest -m live tests/agents/test_view_agent_live.py -q -rs` | **13 passed，零 skip** |
| 量化评测 | `run_eval.py`（18 条，双层判分） | 主集 **10/12 = 83.3%**、域外 **6/6 = 100%** |

全量离线 **1530 passed / 38 skipped**（基线 1436）· `check_expected_red.py` **29 项预期红 0** ·
`pytest -m live tests/render` **18 passed**（快照改动没带坏 P2.2/P2.6）· ruff 干净。

## 人当天裁定的六条

1. 判据分层（离线本体 + live 门禁 + 量化评测）
2. 双层判分（硬判把门 + 判官只答「切题吗」）
3. `dsl.validate`/`dsl.preview` **不进模型工具面**（偏离 owner doc §5.1 字面，已加注解）
4. 开发期数字就够 · 题目我拟 · live 先写在 `tests/agents/`
5. **放宽「agent 循环只许一处」的门禁白名单**（`c6e5b4b`，带 `Gates-Change-Approved-By:`）
6. **借 LangChain 官方 Skill 的思想、不引依赖**（D-22/D-22.1 不翻）

## 🔴 这一场值得下一个人知道的四件

### ① 门禁按设计把我拦住了，而那正是它的价值

`tests/gates/test_agent_seam_stays_swappable.py`「agent 循环只许一处」在我写完
`agenerp/views/loop.py` 的当天变红。它白名单上方那句「改这里会**逼人正面回答
为什么需要第二处循环**」——逼出来的回答逐字写进了白名单上方的注释，供后来人复核。
**放宽之后验过它还咬得动**：临时加第三处 `for … adapter.chat(…)` → 门禁红。

⚠️ 同时暴露我 baseline 的漏读：查了 WBS 与 roadmap，**没有逐条读 `tests/gates/**`**
⇒ 人是在不知道有这道门禁的情况下点头 D1 的。**这条进 P2.8 复盘。**

### ② 活体判据第一次真跑抓到六条，**五条是我们自己的 harness**

与 handoff §3④ 完全同族：每一次「能力失败」往下查都是 harness。最值钱的是第五条 ——
**Frappe 标准字段（`name`/`creation`/`docstatus`…）一个都不在 schema 快照里**，
于是模型交 `count(Stock Entry.name)`（正解）被判「字段不存在」，退到 `naming_series`。
修完同题：3 次打回 / 42,344 token → **0 次打回 / 21,091 token**。

### ③ 「我在实现 harness 时总是在出错」——错在**结构化输出**，不在循环

人 2026-08-28 指出这一点。核下来是对的，但要说准：六条里有两条出在
**手搓「模型输出 → 结构化对象」**，那正是 `with_structured_output` 替人做掉的事。
⇒ 借思想不引依赖：`wire.view_json_schema()` 由 DSL 封闭取值**生成** schema，
当作 `view.submit` 的工具参数发出去，**端点先挡一道形状**。提示词随之从
「块结构散文」瘦到 **307 字符**，漂移判据从提示词挪到 schema。

**D-22 / D-22.1 不翻**，理由被这次读到的官方 Skill 原文逐字确认：
「CANNOT Configure — Core middleware removal（TodoList / **Filesystem** / SubAgent
always present）」⇒ `write_file`/`edit_file` 删不掉、打的是容器文件系统不是 Frappe，
会给一个刚收成三个只读工具的工具面凭空开一条绕过 `has_permission` 的写通道。
D-22.3 的三条翻案条件本项一条都不触发。

### ④ 双层判分不是理论补位 —— 它真的抓到了一条

**in-08**「库存转移是哪天过账的」：硬判**过**（字段真实、落回 0），
判官判**不切题**：「filters 为空，会把入库、出库、制造等所有 Stock Entry 都列出来」。

## 🔴 独立收口审计找到九条，一条报人

收口时派了一个**独立子 agent**（干净上下文，被要求「找窟窿不是确认它做得好」、
不许采信 plan 自述、须自做变异）。**它找到了真东西**，九条已整改各带判据 + 变异见血：
混合 `tool_calls` 时其余 call 无人应答（会被端点 400 杀掉整轮）· 打分函数里两个恒真的
check · 打分函数零判据覆盖 · 三个从没被读过的死参数（而 plan 图里写着「开场注入」）·
三份文档里的 `27 passed` 是旧数 · 评测 schema 的「真」只靠自述 · 等。逐条见 plan §14。

### 🔴 报人的那一条：CI 门禁⑦ 已经红了两天

`.github/workflows/gates.yml` 第 ⑦ 步「没有测试目录被漏在 CI 之外」用一张写死的
`COVERED` 表比对 `tests/` 下的目录。本地原样复现那段 shell：**它会红。**

```
COVERED 最后一次设定  2026-08-24  b0ad632
tests/dsl    出现     2026-08-26（P2.1）← 从这天起就红了
tests/i18n   出现     2026-08-27（P2.7）
tests/render 出现     2026-08-27（P2.2）
tests/agents 出现     2026-08-28（P2.3）
```

**这道门禁恰恰在做它被造出来要做的事，而它连着咬了四个工作项没人看见** ——
因为 mission 的 `commands.test` 是 `check_expected_red.py && pytest tests/unit`，**不跑 CI**。
连带后果：**WBS 第 103 行那条 P2.3 验收命令，CI 上从来没跑过。**

⚠️ 修它要改 `.github/workflows/**` = 红线 ②。**loop 不动，报人。**

## 留给后面的三笔账

1. **live 判据现在不在 `tests/gates/`**（人裁定先写在 `tests/agents/`）。
   将来落地**必须在最终位置重跑** —— §3⑤：换位置本身就是一个变量。
2. **生产模型上的数字没有**（人：开发期数字就够）。重开条件：P2.4 开工，或人要求。
   在那之前，本项任何数字**不得被表述为「视图 Agent 在生产上的表现」**。
3. **评测集的 `must_fields` 编码的是「我想到的答案」不是「答案集合」** ——
   P2.0R 的独立集有 `acceptable`/`why_acceptable` 两列正为此。人 2026-08-28 裁定不改。
