# Development Log - 2026-08-24

### 2026-08-24 · plan `P1.6` **收口** —— 行业包 v0（离散制造）落地：包在盘上、校验器判得动它，M1–M8 全红

- **交付面逐字声明（最容易被读错的一句先写）**：本轮交付的是**包与校验器**，
  **不是「行业包已装载进工具面」**。`agenerp/tools/queries.py` 的 `rule.lookup`
  **仍然指名报错**（只改了 docstring 的说法，报错行为一个字未动），
  理由从「本仓没有行业包」变成「**包在盘上、未接线，接线待人裁定**」——
  三处判据钉着它的现行行为，其中 `tests/gates/test_tool_execution_live.py:119` 是**裁判**。
  接线的两条出路与各自代价已交人，见 `docs/masterplan/STATE.md` §3。
- **命令原文 + 退出码 + sha**（裁判规则 2）：基线 sha `928a888`（§0 执行期重取，四条基线命令全退 0）；
  实现提交 sha **`6682b68`**。
  · `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **退出码 0**
  （`门禁 11 项：预期红 0，绿 11，跳过 0` · 414 → **453 passed**，新增 **39** 条判据）
  · `python3 -m pytest tests/contracts -q` → **0**（`151 passed`）
  · `python3 -m pytest tests/tools -q` → **0**（`81 passed, 12 skipped`）
  · `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments` → **0**
  · `python3 -m agenerp.packs validate --pack discrete` → **0**
- **落点**：制品 `industry-packs/discrete/pack.json`（三条规则，**每条带 `test_case`**）·
  装载面与校验器 `agenerp/packs/` · CLI `python3 -m agenerp.packs validate --pack discrete` ·
  判据 `tests/unit/test_industry_pack.py`（39 条）· 坏包夹具 `tests/unit/pack_fixtures/`
  （**不放进 `industry-packs/`** —— 产品制品目录里不许躺着故意写坏的包）。
  落点节是 `docs/architecture/module-boundaries.md` **§7.10**（五条 `Decision` D1–D5 与被否决的备选都在里面）。
- **四种输入四种可区分的处置**：`0` 健康 / `3` 查无此包 / `4` 缺 `test_case` / `5` 测例跑不过
  （`2` 留给 argparse，不复用），且消息各自指名到具体对象。
  ⚠️ 三个非零码分开不是洁癖：合成同一个「非零」时，**「查无此包」会被读成「这个包有问题」**，
  那是最贵的一种假绿。
- **扩了两个算子（D3），日期算术明确不在本期**：`equals` / `not_equals` 行过滤，
  各带一个字面量（`ROW_FILTER_KEYS` 加一位 `value`），并同时交付求值判据与**四种拒载判据**
  （未知算子名 / 缺字面量 / `truthy` 夹带 `value` / 非标量字面量）。
  外协那条 v0 按「**已发料 − 已收货 > 0**」这种**不含时间**的形态表达。
  **本期表达不了的三类**（触发侧合取 · 按组计数 · 日期算术）照实列进 §7.10，
  **没有**先写进包里、判据以后补。
- **出处落到 `Hit.pack_id`（D5）**，来源那一层是 `Pack.pack_id` → `run()` / `inspect_site()` 的形参，
  **不进 `Rule`** —— 同一条 `rule_id` 挂在两个 `pack_id` 下时，两次命中的出处必须不同，
  这条判据在被否决的 (C)「把 `pack_id` 编进 `rule_id` 前缀」上根本构造不出来。
- **M1–M8 八个变异逐个复跑，全部由绿转红**，且由**预期的那条判据**打红：
  M4（把某条规则的判据改成永远命中）由**阴性对照**打红，**消融对它是绿的** ·
  M5（命中量写死 1010）由**第二个数据集**打红，固定测例那条对它是绿的 ·
  M8（CLI 退出码恒为 0）**只被子进程判据打红，函数级判据对它是绿的** —— 这正是两种都写的理由。
- ⚠️ **两条不许被读大的声称**：① **消融判据是恒真的那一侧**（`without()` + `run()` 下抽掉规则自然零命中），
  它证明「引擎读清单」，不证明「规则有判别力」——发现力由每条规则的阳性/阴性对照证明；
  ② **外协那条规则未在真实数据上验证过命中** —— 种子的外协链是完整的（发多少收多少），
  两侧零命中是正确行为，能验的只有「它不误报」，且那个零命中必须是**算出来的零**。
- ⚠️ **活站点核对（H4）实测「部分一致」，照实记，不改规则去迁就站点**：
  `frontend@http://127.0.0.1:18080` 跑整份包（9 次只读请求）——
  `discrete/finished-goods-backlog` 两侧**逐字一致**（`1010.0`，命中集合非空且含它，H4 的防伪前提成立）·
  外协那条两侧都零命中 · 但 `discrete/closed-order-short-delivered` **离线命中 10、站点零命中**。
  **这是 `DECISIONS.md` D-12 预言的失败形态被抓到了，不是本包的缺陷**：
  站点上 `Sales Order.status` 是 `To Deliver and Bill`，`agenerp/seed/model.py:57` 写的是 `Closed`，
  而 `agenerp/seedsite.py` 全文没有写这个 `status` 的地方（先原样复跑一次，结果相同）。
  **到此为止不再往下猜根因**（ERPNext 提交时会不会自己重算 `status` 未取证）。
  已记 `docs/bugs/02-live-site-sales-order-is-not-closed-so-the-account-green-trap-is-absent.md`，
  归属（种子装载面，不是行业包）交人，见 STATE §3。
- **WBS §4 P1.6 行的验收命令字符串定稿**：`python` → `python3`，形状不变，
  定稿证据行已追加到 `docs/masterplan/STATE.md` §2；`docs/masterplan/` 已有行只有人能改（红线 5），
  归属并入 §3 那条 needs-human 的第二个 bullet，**loop 不代改**。
- **红线复核**：`git diff --name-only 928a888 HEAD -- tests/gates .github/workflows missions docs/masterplan/DECISIONS.md`
  → 无输出；`docs/masterplan/STATE.md` 的 numstat 为 `14	0`（**删除列为 0**，只追加）。红线 1–7 无一触碰。

### 2026-08-24 · plan `P1.5` **独立关闭审计补做** —— 结论「接受收口」，`closure audit was independent` 由留白转勾

- 首次收口时这条 gate **留白**（执行环境不具备独立子代理，执行者自审不算独立；plan §10 照实记）。
  本轮由 mission-driver 派发的**独立收口审计器**（session `2026-08-24-203159-mission-driver`，
  fresh session、不带实现上下文、非执行者自审）在基线 sha `090700a` 上补做，结论**接受收口**。
  补做记录**追加**在 plan 的 `## Closure` 末尾，原 `Closure Audit Evidence` 块**一个字未改写**。
- **命令原文 + 退出码 + sha**（裁判规则 2）：审计与本次落账均在 `090700a` 的工作树上；
  · `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **退出码 0**
  （`门禁 11 项：预期红 0，绿 11，跳过 0` · `414 passed`）—— **full-green verification**，
  与 P1.5 收口条目声称的数字逐字吻合。
  · 审计侧另跑 `tests/contracts` → **0**（`151 passed`）、`tests/tools` → **0**（`81 passed, 12 skipped`）、
  `ruff check …` → **0**、`python3 -m pytest tests -q -m "not live"` → **0**（`880 passed, 12 skipped, 21 deselected`），
  逐条见 plan 补做记录。
- **审计侧独立变异复算**（不只读断言）：把 `agenerp/inspection/rules.py` 的 `_reject_unknown` 改成 `return`（M12 变异）
  → `python3 -m pytest tests/unit/test_inspection_rules.py -q` → **`1 failed, 15 passed`**，
  红在 `test_the_loader_refuses_keys_it_does_not_know`；复原后 `git status --porcelain` 无输出。
- **红线复核（审计侧独立跑）**：`git diff --name-only 04aa9ea HEAD -- tests/gates .github/workflows missions docs/masterplan/DECISIONS.md`
  → 无输出；`docs/masterplan/STATE.md` 的 numstat 为 `8	0`（**删除列为 0**，只追加）。红线 1–7 无一触碰。
- ⚠️ **本次补做不扩大任何声称**：证据表第 ⑤ 行（活站点巡检）审计环境不具备 compose 站点与凭据，
  **未复跑**，采信执行者记录但不加盖审计印；`## Closure` 第 4 条
  「归因那一半没有在活端点上跑过」的诚实限制**继续有效**。
- ⚠️ §11 的两条后继**未被审计器代人处置**：一条归人创建 `tests/gates/**` 下的两条 🔴 门禁文件（红线 1），
  一条归 P1.6 起草（`rule.lookup` 的 `watch-only residual`）。

### 2026-08-24 · plan `P1.5` **收口** —— 巡检器（纯规则引擎，零 LLM）+ 洞察 Agent（归因）落地，M1–M12 全红（M7 第一轮有缺口，已补断言）

- **命令原文 + 退出码 + sha**（裁判规则 2）：基线 sha `04aa9ea`（§0 执行期重取；起草基线 `5ffb8fb` 已过时 —— 当时 `agenerp/explain/` 还不存在）。
  · `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **退出码 0**
  （`门禁 11 项：预期红 0，绿 11，跳过 0` · `414 passed`，基线 389 → +25）
  · `python3 -m pytest tests/contracts -q` → **0**（`151 passed`，与基线逐字相同）
  · `python3 -m pytest tests/tools -q` → **0**（`81 passed, 12 skipped`，与基线逐字相同，零回归）
  · `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments` → **0**（`All checks passed!`）
  · 活站点巡检一次：`AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin python3 -c "…inspect_site(minimal_rules(), client_from_env('frontend'))…"` → **0**
- **D-15 落成代码**：巡检（按清单逐条查、命中即报）是**代码**，落 `agenerp/inspection/`；
  归因（为什么会这样 / 要不要紧）才是**模型**，落 `agenerp/insight/`。两者分开，不合并。
  落点节 `docs/architecture/module-boundaries.md` **§7.9**（新增），`agents-and-roles.md` §5.1 只补指针。
- **规则是数据，不是代码**（D1）：声明式数据 + 有限算子（行过滤 2 个 / 度量 4 个 / 触发 2 个），
  因此可 diff、可迁移。装载器**未知键与缺 `test_case` 一律拒载**，且 `test_case` 会被引擎**真跑**。
- **消融判据跑两个数据集**：固定测例（由 `agenerp/seed/` 派生，一行数据未手写）命中 `on_hand = 1010.0`
  （`== EXPECTED_BACKLOG_QTY`，取自 `checks.py` 的**期望侧**）；把 `INHOUSE_QTY→800` / `SUBCON_QTY→600` /
  `DELIVERY_QTY→500` 换过参数的第二个数据集命中 `900.0`（判据写死的字面量）——**数随数据集变**。
  抽掉那条规则 → 零命中；把它的 `trigger.value` 由 `0.5` 改到 `10.0` → 也零命中。
- **「零 LLM」判在两个可观测量上，不判源码文本**：① 进程级探针把 `ChatAdapter` 的
  `__init__` / `chat` / `_send` / `_post`、`_ssl_context`、`urllib.request.urlopen` 整体换成一被碰就抛的替身，
  整条巡检路径跑完仍绿，**并配阳性对照**（同模块里 `route("explain", …)` 必须被同一探针打红）；
  ② 全新解释器里 `import agenerp.inspection` 之后 `agenerp.routing` **不在 `sys.modules`**
  —— 巡检器**根本没有模型接缝**，不是「留了口子没用」。
- **归因走 P1.4 的循环，不另起一条**（D3，符号重绑为 **`agenerp.explain.explain`**）：
  取证不足时归因**被证据充分性门禁拒**（`accepted is False`、`answer == ""`，L3 点名两张入库凭证
  `MAT-SCR-2026-00001` / `MAT-STE-2026-00003`，`missing_count == 2`）；补齐取证后同一条归因被放行。
- **巡检结论不可被模型改写**：给替身模型一个与命中**相反**的输出（「其实一台都没积压，这条命中应当撤销」），
  最终 `attribution.hit is hit`、命中记录逐字不变、报告也一字未动。边界执行另有落点 `ensure_unchanged` 并有直接判据。
- ⚠️ **执行期查出一处判据缺口，照实记**：变异自查第一轮 **M7 打不红** —— 装载器**静默丢弃未知键**，
  夹带在未知键里的单号在源码里明晃晃写着、序列化后却看不见，而结构判据只判后者。
  已补两条断言（结构判据**源声明与装载后两侧都判** + 装载器**未知键拒载**），登记为 **M12**
  （M9–M11 已被 Phase 2 占用）。补后 **M1–M12 逐个复跑，全部由绿转红**。
- ⚠️ **D3 的残余风险实测确认，不擅自绕开**：命中的 `subject` 里 `HRD-PACK-5K` 是三段全大写数字，
  正好落进 `agenerp/explain/gate.py` 的 `DOC_NAME`，于是 **L1 把物料号当成「问题点名的单据」**并要求 `doc.links`。
  它误报的方向是**更严**（保守侧），门禁措辞归 owner doc 与人的裁定面。
- ⚠️ **两条 🔴 门禁文件本轮未创建**（红线 1，交人），**未声称 WBS §4 P1.5 的 🔴 验收已满足**；
  **未交付行业包 v0**，`rule.lookup` 的报错行为**未翻转**（重开事件仍是 P1.6）；
  `anomaly.scan` / `benchmark.compare` **仍未实现**。交接写进 `docs/masterplan/STATE.md` §3（只追加）。
- ⚠️ **归因文本的质量本 plan 没有任何判据**：判自由文本要先跑通
  `tests/unit/test_answer_judging_fixture.py` 的 24 条人工标注，属另一个交付面。
  **活站点那一次跑的是巡检那一半；归因那一半没有在活端点上跑过，也不声称跑过。**

### 2026-08-24 · plan `P1.4` **独立关闭审计补做** —— 结论「接受收口」，`closure audit was independent` 由留白转勾

- 首次收口时这条 gate **留白**（当时执行环境不具备独立子代理，plan §10 与 `## Closure` 照实记）。
  本轮由 mission-driver 派发的**独立收口审计器**（fresh session，不带实现上下文，非执行者自审）
  在基线 sha `a39070f` 上补做，结论**接受收口**；plan 的 diff 基线仍是 `6b07889`。
- **命令原文 + 退出码 + sha**（裁判规则 2）：审计基线 `a39070f`，本次记录复跑于同一 sha 的工作树上。
  · `python3 tools/gates/check_expected_red.py` → **0**（`门禁 11 项：预期红 0，绿 11，跳过 0`）
  · `python3 -m pytest tests/unit -q` → **0**（`389 passed`）
  · `python3 -m pytest tests -q -m "not live"` → **0**（`849 passed, 12 skipped, 21 deselected`）
  · `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments` → **0**（`All checks passed!`）
  四条数字与 plan §10 / roadmap 工作项 6 声称的**逐字吻合**，未发现被修饰的数字。
- **红线复核（本轮独立跑，不采信自述）**：
  `git status --porcelain -- tests/gates .github/workflows missions docs/masterplan/DECISIONS.md` → **无输出**；
  `git diff --numstat -- docs/masterplan/STATE.md` → **无输出**（本轮未动 STATE，首次收口那 `7  0`（删除列为 0）只追加的结论不变）。
- **落点**：plan 的 `## 12. Closure` 改为裸标题 `## Closure`（`plan-check` 的 `CLOSURE_HEADER_RE` 只认裸标题，
  仓内其余 plan 也是这个写法），全文唯一一处 `§12` 引用随之改准；`## Closure` 末尾**追加**
  「独立关闭审计补做记录」一节，上方原 `Closure Audit Evidence` 块**一个字未改写**。
  `node tools/mission-driver/src/plan-check.mjs <plan> --strict` → **exit 0**（`passed: true` · `totalUnchecked: 0` · `details: []`）。
- **roadmap 工作项 6** 那行的 ⚠️「独立关闭审计未做」**原文保留、不改写**，其后追加一句补记指向本节。
- ⚠️ **本次审计不代人处置 plan §11 的两条 Deferred**：`tests/gates/test_evidence_gate_blocks_single_hop.py` 归人（红线 1），
  `commands.test` 覆盖面归 STATE §3 已有的 `[open]` 条目。两条都**不是**本 plan 确认的活体缺陷。

### 2026-08-24 · plan `P1.4` **收口** —— 解释 Agent 控制循环本体 + 证据充分性门禁在作答面落地，M1–M8 八个全红（无 M9）

- **命令原文 + 退出码 + sha**（裁判规则 2）：基线 sha `6b07889`（§1.0 二次重取；
  `b86244f..6b07889` 之间人又提了五笔，其中两笔动 `.github/workflows/**`，沿用旧基线会把它们算成本 plan 触红线）。
  · `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **退出码 0**
  （`门禁 11 项：预期红 0，绿 11，跳过 0` · `389 passed`，基线 373 → +16）
  · `python3 -m pytest tests/tools -q` → **0**（`81 passed, 12 skipped`，与基线**逐字相同**，零回归）
  · `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments` → **0**（`All checks passed!`）
  · `python3 -m pytest tests -q -m "not live"` → **0**（`849 passed, 12 skipped, 21 deselected`）
- Phase 1–2 `agenerp/explain/`（`gate.py` / `loop.py` / `__init__.py`）：控制循环本体
  **形状继承 `tools/experiments/p1_entry_gate/loop.py`，换掉四个零件**（模型侧 `routing.route()`、
  开场侧 `orchestration.open_session()`、会话侧 `context.session`、熔断侧 `DenialBreaker`），
  实验设施**一行没动、一个文件没复制**。导出面只有 `explain` 与 `ExplainResult`。
- **门禁在两处求值，事实采集面只有一份**（D2）：① 工具前置卡「作答类工具能不能调」（P1.0a 已在，一个字未改），
  ② 作答前卡「这个 answer 能不能被接受」（新增）。只有 ② 拦得住 Spike 02 那条
  「不调作答类工具、直接凭 `doc.get` 的返回值报数字」。同源性靠 `EvidenceSurface` 自己的
  `surface_id` + `uses` 留痕**可断言**，不靠人复核。
- **§6 的 H1–H4 逐条对照，六行全部吻合，假设一个字未改**：H1 三条（默认路径拒 · 消融侧同一答案被收 ·
  默认路径下 ② 门禁确实是开的）· H2（文字正确但取证不足**仍然拒**，两段答案文本不同而事实字典**逐字相等**）·
  H3（一批给 6 个调用、实测只发出 **5** 次 `execute`，文案 + **非空且点名 DocType** 的 `read:GL Entry` 清单）·
  H4 见下。
- **变异自查 M1–M8 八个全部被打红，没有一个需要补断言，因此没有 M9**。M2（② 处换另一份采集面）
  打红的正是 D2 的绑定断言；M6（开场包标志位写死为 True、站点上不发注入请求）打红的是
  「断言落在**假站点请求记录**上」那一条 —— 沿用 P1.3 M6 的教训，不用相等断言。
- **判据落 `tests/unit/`**（D5）：`test_evidence_gate_single_hop_body.py`（H1 三条 + H2 两条）+
  `test_explain_loop.py`（11 条）+ `explain_fakes.py`（共用假件）。**`tests/tools/**` 一个字未改** ——
  工具执行层那份 `FakeSite` **按路径加载**，不搬家、不复制。
  ⚠️ **实测补出先例里没有的一行**：`tests/gates/test_tool_execution_live.py` 的 `_load_sibling_module`
  **没有**把模块塞进 `sys.modules`，而 `tests/tools/conftest.py` 模块级有 `@dataclass`，那样加载**当场炸**
  （`dataclasses._is_type` 反查 `sys.modules[cls.__module__]` 拿到 `None`）。必须先注册再 `exec_module`。
- **H4 活端点实跑一次**（轨迹 `docs/evidence/p1-explain/live-run-01.json` + README）：
  `usage` 三项均 > 0（prompt 40,885 / completion 4,310 / reasoning 2,784），
  七次调用的账目**逐次**对得上端点自报的 `raw["usage"]`（**不是**恒真的 `prompt + completion == total`）。
  **L3 在真实数据上抓到了它被设计来抓的那件事**：答案报 1,010 台 → 门禁从库存流水反查出
  `MAT-SCR-2026-00001`（外协入库，正是「沿订单查得再深也看不见」的那张）与 `MAT-STE-2026-00003`。
  ⚠️ **那一跑没走到 ② 门禁的拒绝路径**（模型第一次作答就已取证充分，`forced_continues` 为空），
  **也没触发熔断**（Administrator 不撞 403）—— 那两件由 `tests/unit` 判据证明，**不由那一跑证明**。
  ⚠️ 单跑 **45,195 token**，低于 roadmap P1.7 节记的 9.7 万–12.8 万；**不修饰成「优化了」**，
  本 plan 没做任何成本工作，差异成因未测量。
- owner doc：`module-boundaries.md` **新增 §7.8**（三文件职责 · D1–D7 · 两处求值分工 · 判据缺口，
  D6/D7 的残余风险逐条写在「判据缺口与验证范围」小节里）· **五处失效归属一并改准**：
  §7.4 末尾两条 ⚠️（熔断接线 / 审计写入）· `module-boundaries.md` §7.6 那句「尚未接到任何真实控制循环上」·
  `agenerp/orchestration/__init__.py` 模块头两句 + §7.6a 的「接缝只有单侧」· `agenerp/contracts.py` 模块头
  「P0 阶段还没有控制循环去消费它们」。**§7.4 表里的六行落地形态与「35 次」那条 D-16 警示一个字未动。**
- STATE §3 **追加**一条 needs-human（`git diff --numstat` → `7	0`，删除列为 0）：
  (a) 本 plan **未创建** `tests/gates/test_evidence_gate_blocks_single_hop.py`，**也未声称那个 🔴 已满足**；
  (b) 与 P1.0a 先例不同的两处需人复核（basename 必须不同 · 无 live 语义因此没有 skip 可收严）；
  (c) 加载器必须先注册进 `sys.modules`；(d) H4 的验证范围与那个低于预期的 token 数。
- **红线自查干净**：`git diff --name-only 6b07889..` 对 `tests/gates/**` · `.github/workflows/**` ·
  `missions/**` · `docs/masterplan/DECISIONS.md` **四个 pathspec 均无输出**；`docs/masterplan/STATE.md` 只有追加行。
  ⚠️ 执行期间人另提了两笔（`dc7077c` 动 `tools/loop-supervisor.sh`、`548cca6` 动 `docs/masterplan/04-RUNBOOK.md`），
  **两笔都不属本 plan，loop 一个字未动它们** —— 这正是 §1.0「开工先重取基线」要防的那种混算。

### 2026-08-24 · plan `P1.3` **full-green verification** —— mission 门禁命令原样复跑，退出码 0

- 命令原文（`missions/p1-insight.json` 的 `commands.test`，未自造）：
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **退出码 0**
  （`判定模式：default` · `门禁 11 项：预期红 0，绿 11，跳过 0` · `✅ 与预期红名单完全一致` · `373 passed in 1.00s`）。
- `tools/gates/expected-red.txt` **本轮无需划行**：名单自 2026-08-23 起已为 0 条，无「名单内转绿」项。
- 工作区本轮只有**文档**改动（plan §12/§13.5/§13.7 + 本日志），`agenerp/**`、`tests/**` 零改动；
  `tests/gates/**`、`.github/workflows/**`、`missions/**`、`docs/masterplan/**` **一律未动**。

### 2026-08-24 · plan `P1.3` **独立关闭审计补做** —— 结论 `approved`，A2 探针绿但裁为「不是缺陷」

- 首次收口时 `closure audit was independent` 这条 gate **留白**（当时无独立子代理，plan §13.5 照实记）。
  本轮由**独立关闭审计者**（独立会话，未参与起草与执行）在基线 sha `44ce646` 上补做，结论 **`approved`**，
  记录写进 plan **§13.7**；§13.5 的旧记录**保留不改写**（沿用 P1.2 的做法）。
- **七条验证命令逐条原样复跑，退出码与 §13.2 记的逐条相同**：`pytest tests/tools/test_navigation.py -q` → 0（`32 passed`）·
  `pytest tests/tools -q` → 0（`81 passed, 12 skipped`）· `check_expected_red.py && pytest tests/unit -q` → 0（`预期红 0，绿 11` / `373 passed`）·
  `pytest tests/contracts -q` → 0（`151 passed`）· `pytest tests/context -q` → 0（`53 passed`）· `pytest tests/routing -q` → 0（`151 passed, 1 skipped`）·
  `ruff check ...` → 0。**没有一条对不上。**
- **独立探针 A1–A3**（审计者自出，不复用 M1–M8，逐个按字节还原）：A1 熔断阈值 5→6 **红**；
  A3 `run_metric` 把开场注入从计数里去掉 **红**；**A2** `_verified` 的 `all("can_read" in row …)`→`any(…)` **绿**。
  A2 裁定为**不是缺陷**：Phase 1 写死的判据逐字是「产物里含 `can_read` 行」，`any` 正是这句的字面实现，
  `all` 是实现方向上的加严 —— 判据钉的是 plan 的口径，不是这层加严。**照实记成残余，不降级、也不谎报成缺陷。**
- **sha 归属经 `git log --diff-filter=A` 复核属实**：`opening.py` 进仓于 `659b41f`、`navigation.py`/`circuit.py` 进仓于 `1c61089`，
  两个都是**人**的 CI 提交扫进去的（`1c61089` 里一个 workflow 文件都没有，与其 message 不符）。plan 没把它们算成自己的，写法与 `git log` 对得上。
- **红线复核干净**：`git diff --numstat 20d5adc~1 44ce646 -- tests/gates .github/workflows missions docs/masterplan/DECISIONS.md` **输出为空**；
  同区间 STATE 为 `7	0`（删除列 0）。`659b41f` 里那处 workflow 改动是**人**做的且是**收紧**（补 seed 前置、零 skip），不属本 plan。
- ⚠️ **`verification scope limited` 仍然成立，审计不消除它**：`tests/tools` 不在 `missions/p1-insight.json` 的 `commands.test` 里，
  `GATE_VERIFY` 复跑不到本层的 32 条判据 —— 已在 STATE §3 (a) 排队等人。**残余风险不消除。**

### 2026-08-24 · plan `P1.3` **收口** —— 导航质量判据 + 权限拒绝熔断落地，M1–M8 八个全红（M6 是补出来的）

- Phase 2 `agenerp/orchestration/navigation.py`：确定性导航策略 `ScopeFirstStrategy` + 度量骨架
  `run_metric`。**on / off 两组共用同一个策略对象**，骨架把实际用过的那个记进 `MetricRun.strategy`，
  判据断言 `on.strategy is off.strategy` —— **同一性，不是相等**。没有这条，M3 那种
  「骨架内部按开场包分叉、off 那一路 `deepcopy` 一个」的假实现会恒绿（两份相同代码产出相同数字）。
- **§6 的 H1–H4 四条全部吻合，假设一个字未改、题一道未换**（本仓夹具实测，非站点实测）：
  · ① 受限身份问不可见 DocType：on **1** 次 `execute()` / off **5** 次 → H1 吻合、H2（≤2）吻合
  · ② 单跳 2/1 · ③ 多跳 4/3 → H3（注入不是免费的）吻合
  · H4 空包变体（人工置真 `opening_injection_verified`）三题逐位等于 off 组 → H1 不再成立，吻合
- **照实记两件不好看的事**：① **站点请求那一栏 on 组是净亏的，连它赢的那道题也是**（① 题 10 对 5）——
  「更省」只在 `execute()` 次数这一栏成立；② off 组那 5 次由题面决定（① 题写死 4 条备选路线），
  与 §7.4 记的 35 次**不是同一个量**，不得互相引用为佐证。
- Phase 3 `agenerp/orchestration/circuit.py`：`DenialBreaker`（N=5，**连续**不是累计，
  **只有 HTTP 403** 算权限拒绝，非 403 **既不计数也不清零**）。两处 403 口径靠一条**行为级**断言绑定 ——
  拿 `FakeSite` 驱动 `doctypes_with_data` 两次（403 进 `unreadable` / 非 403 原样抛出）取行为作基准，
  **不比源码文本**；非 403 那种输入现成夹具造不出来，判据自己包了一层 transport。
- **变异自查 M1–M8 八个全部被打红**，但 **M6 第一轮是绿的（`79 passed`）**，照实写：
  原判据只有 `pack.facts[...] == pack.result.facts[...]`，而「装配路径上把它写死成 `'has_permission'`」
  让**两边同时**变成那个字面量，相等断言照样成立。就地补两条 —— 用 `str` 子类实例（相等但不同对象，
  子类不被驻留）断言 `is`，外加「执行体没推出这条事实时开场包不许凭空补一个」。
  补完后 M6a（守卫内写死）/ M6b（无条件补一个）复跑均 exit 1。
- owner doc：`module-boundaries.md` §7.4 末尾追加熔断落点（N / 连续语义 / 403-only / 非 403 的处理 /
  所需权限清单 / **接线归 P1.4** 的限定）· 新增 **§7.6a 编排层在本仓的落点**（三文件职责、D1/D2/D3、
  两段机制表、导航数字并标注「本仓夹具实测，非站点实测」、判据缺口）· §7.6 那句
  「熔断仍未做…归 P1.0 的控制循环」**改准**（P1.0 交付的是实验设施且已 `done`，那是一条责任人空缺的悬空条目）。
- STATE §3 **追加**一条 needs-human（`git diff --numstat` → `7	0`，删除列为 0）：
  (a) `commands.test` 这一半 —— **CI 那一半已由 `b0ad632` 解除，本行不重复登记**；
  (b) 熔断未接真实循环；(c) 本期数字与「35 → 1」不是同一个量。
- 验证（sha `f4f686b`）：`pytest tests/tools/test_navigation.py -q` → **exit 0**（`32 passed`）·
  `pytest tests/tools -q` → **exit 0**（`81 passed, 12 skipped`）·
  `check_expected_red.py && pytest tests/unit -q` → **exit 0**（`373 passed`）·
  `pytest tests/contracts -q` → **exit 0**（`151 passed`）· `pytest tests/context -q` → **exit 0**（`53 passed`）·
  `pytest tests/routing -q` → **exit 0**（`151 passed, 1 skipped`）· `ruff check`（七个目录）→ **exit 0**。
- ⚠️ **独立关闭审计没做，`closure audit was independent` 这条 gate 留白**：本轮执行环境不具备独立子代理。
  P1.2 的先例说明这不是形式 —— 那轮独立审计另出的 A5 打中了起草期 M1–M8 挑不到的盲区。
- ⚠️ **三句话不能说反**：`injected_at_session_start` 仍是**调用方自证的软断言**（加强的是编排面不是契约面）·
  熔断**尚未接到任何真实循环上** · 本仓**没有复现「35 → 1」**，也没打算复现。
- **红线复核**：`tests/gates/**`、`.github/workflows/**`、`missions/**`、`DECISIONS.md`、
  `tools/experiments/**`、证据仓一律未动；八个变异探针按字节还原后 `git status --porcelain` 为空。
- ⚠️ **sha 归属有一处与直觉相反，照实记**：`agenerp/orchestration/opening.py` 首次进仓是
  **`659b41f`**、`navigation.py` / `circuit.py` 是 **`1c61089`** —— 那是**人**在同一工作树上做
  CI 修复时 `git add` 扫进去的，两个 commit 的信息里没有一个字提到编排层。
  **不把它们算成本 plan 的提交，也不假装代码落在 `20d5adc` / `f4f686b` 里**（那样写与 `git log` 对不上）。
  「全部产物同时为绿」的权威 sha 是 **`e3764fc`**。

### 2026-08-24 · plan `P1.3` Phase 1 **会话开场装配器落地** —— 注入真的发生，且写死标志骗不过去

- 新包 `agenerp/orchestration/`（D1 选定 (B)：编排是独立一层，判据落既有的 `tests/tools/`）。
  Phase 1 只交付 `opening.py` 的 `open_session(...)`：在任何模型消息之前执行一次
  `execute("permission.scope", ...)`，产物 + 代价 + 推导事实进开场包。
- **两段机制照 §1.1a 分名分述，这是本阶段的核心**：
  · 契约面 `injected_at_session_start` —— 装配器**必须**交进 `ReadOnlyContext`
    （`runtime.py:359` 的 `{**caller_facts, **outcome.facts}` + `contracts.py:102` 的
    「事实缺席即判否」，不交必然在 `postconditions` 上 abort）。**这一段本层不宣称被加强**：
    绕过 `agenerp/orchestration/` 直接调 `execute` 的人照样能把它填成 `True`。
  · 开场包面 `opening_injection_verified` —— 由 `_verified()` 从 `ToolResult` **推导**，
    五条判据写死在代码里（产物存在 · `tool == "permission.scope"` · `ok is True` ·
    `request_count > 0` · 产物里含 `can_read` 行）。**调用方传进来的同名值一律不进开场包。**
- 新判据文件 `tests/tools/test_navigation.py`（WBS §4 第 82 行点名的那个），**15 条**：
  注入真的发生（判据落 `FakeSite.requests` 上的 `has_permission` 条数，不落标志位）·
  产物含 `can_read: False` 的行 · `readable()` 把「不知道」与「读不到」分开 ·
  `permission_probe_method` 来自执行体未被覆盖 · 反测 A（只写标志不注入）·
  反测 B（带候选集不得走发现式，元数据枚举请求必须为 0、探测次数恰等于候选集大小）·
  注入代价与实际请求条数对得上（换一组候选集数字必须跟着变，常量实现在这里红）。
  ⚠️ 现成的 `fake_site` 夹具 `permissions` / `forbidden` 都是空的、对一切回 `True`，
  判据自己建了 `worker_site` 把三个 DocType 置 `False` —— 不这么做第 ② 条验不出东西。
- 开场包同时收 ① 即时上下文（P1.2 的 `ImmediateContext`，原样摆进不改字段）并把注入记进
  ② 对话会话的「已执行动作」档（走 `ConversationSession.with_action`，不另拼一套记法）。
- 验证（基线 sha `1f8d595`）：`python3 -m pytest tests/tools/test_navigation.py -q` → **exit 0**
  （`15 passed`）· `python3 -m pytest tests/tools -q` → **exit 0**（`64 passed, 12 skipped`）·
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 11 项：预期红 0，绿 11` / `373 passed`）·
  `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments`
  → **exit 0**。
- **红线复核**：本轮只新增 `agenerp/orchestration/**` 与 `tests/tools/test_navigation.py`，
  改 plan 与本日志。`tests/gates/**`、`.github/workflows/**`、`docs/masterplan/**`、
  `missions/**`、证据仓一律未动。

### 2026-08-24 · plan `P1.2` **A5 缺口补齐，M9 打红** —— plan 回 `completed`，只补判据、live 代码零改动

- 上一条日志记的 A5 是：**常量原封不动**、只把 `trim()` 里 `b.tier not in UNTRIMMABLE_TIERS`
  改成 `b.tier != TIER_DOCUMENT` 的假实现在 `tests/context` 上全绿。本轮把它补上，登记为 **M9**。
- `tests/context/test_immediate.py` 加两条（判据 **51 → 53**）：
  · `test_the_audit_record_blocks_all_survive_once_memory_and_schema_are_gone` —— **行为级正向**：
    ③ / ④ 全裁光后 ② 档的块一个不少、`tier` 与 `payload` 逐字相等。
    ⚠️ **它在 M9 变异下仍然绿**（`trim` 的循环在预算够时提前 `break`，轮不到裁 ② 档），照实写。
  · `test_it_raises_rather_than_dropping_the_audit_record_to_fit_the_budget` —— **反测**：
    预算比「① + ②」差一个字符时必须抛 `ContextBudgetExceeded`、文案点名 `② 已执行动作/actions`。
    **打红 M9 的只有这一条。** 只写正向那条的话，A5 的缺口原样还在。
- **最该记的一点**：A5 之所以溜过去，不只是「缺行为断言」，还因为既有那两条 trim 测试用的是
  `_assembled()` —— 空 `actions` 的块只有 2 个字符，裁不裁都碰不到预算。
  **构造得太小的输入会让一条本该管用的断言变成摆设**，新加的 `_with_actions()` 把这个理由写在了注释里。
- M9 复跑：植入 → `python3 -m pytest tests/context -q` → **exit 1**（`1 failed, 52 passed`），
  唯一红的正是反测那条，与 Phase 3 变异表的预言逐字一致；按字节还原后
  `git status --porcelain agenerp/` 无输出。
- 全量复跑（基线 sha `d5f0a04`）：`pytest tests/context -q` → **exit 0**（`53 passed`）·
  `check_expected_red.py && pytest tests/unit -q` → **exit 0**（`门禁 11 项：预期红 0，绿 11` / `373 passed`）·
  `pytest tests/contracts -q` → **exit 0**（`151 passed`）· `pytest tests/routing -q` → **exit 0**
  （`147 passed, 1 skipped`）· `pytest tests/tools -q` → **exit 0**（`49 passed, 12 skipped`）·
  `ruff check agenerp tests/unit tests/contracts tests/routing tests/context` → **exit 0**。
- plan 回 `Plan Status: completed`、Phase 1 回 `completed`、Closure Gate
  `in-scope behavior is complete` 勾上，证据进 §12.9。
- ⚠️ **补齐本身没有再经独立审计**：§12.8 那轮独立审计验的是 51 条那一版，这 2 条新断言只经执行者自查。
  `closure audit was independent` 这条 gate 记的仍是「§12.8 那一轮是独立做的」，不得读作补齐也被独立复核过。
- **红线复核**：本轮只改 `tests/context/test_immediate.py`、plan、`docs/backlog/p1-insight-roadmap.md`
  与本日志四个文件。`agenerp/` **一个字节未改**；`tests/gates/**`、`.github/workflows/**`、
  `docs/masterplan/**`（STATE 本轮一行未加）、证据仓一律未动。

### 2026-08-24 · plan `P1.2` **独立关闭审计补做，结论 `issues`** —— 另出的 A5 打出一处判据盲区，plan 回退 `active`

- 审计者是独立会话（未参与起草与实现），不采信任何 `[x]`，在 sha `3337d69` 上逐条对 live 仓库复核。
  §12.7 的每条命令原样复跑，退出码逐条一致：`pytest tests/context -q` → **exit 0**（`51 passed`）、
  `check_expected_red.py && pytest tests/unit -q` → **exit 0**（`373 passed`）、
  `pytest tests/contracts -q` → **exit 0**（`151 passed`）、`pytest tests/routing -q` → **exit 0**
  （`147 passed, 1 skipped`）、`ruff check agenerp tests/unit tests/contracts tests/routing tests/context`
  → **exit 0**。`git status --porcelain` 无输出。
- 审计另出 **A1–A5 五个变异**（都不在起草期挑的 M1–M8 里）。A1–A4 全部被打红：
  keep-stuffing、`UNTRIMMABLE_TIERS` 删档、落盘丢 `reasoning`、超预算静默返回。
  · **A5 全绿，51 passed** —— **常量原封不动**，只把 `trim()` 里的
    `b.tier not in UNTRIMMABLE_TIERS` 改成 `b.tier != TIER_DOCUMENT`。
    也就是说，一个「② 已执行动作的审计记录会被裁掉」的假实现，今天的 `tests/context` 一条都不响。
- **性质说准**：live 代码是对的（`trim()` 用的就是常量），**这不是 live 缺陷**；缺的是**判据**——
  钉住 ② 档的只有一条常量声明断言，验的是常量长什么样，不是 `trim()` 的行为。
  这正是 plan §12.2 自己写下的那句话应验：「M1–M8 是起草期自己挑的变异，挑不到自己没想到的地方」。
- **处置不降级**（指南 Minimum Rule 14，② 档在 Phase 1 执行项里是逐字写明的 in-scope 行为）：
  Phase 1 新增一条 `Proof`（补 ② 档的**行为级**正向断言 + 反测，登记为 **M9**），
  Phase 1 `Status` → `in progress`，`Plan Status` → `active`，Closure Gate
  `in-scope behavior is complete` 取消勾选。`closure audit was independent` 这条**改为已勾**——
  它说的是「审计是独立做的」，不是「审计通过了」，两者不能混为一谈。
- **红线复核**：本轮只改 plan 文件与本日志；`tests/gates/**`、`.github/workflows/**`、
  `docs/masterplan/**`、证据仓一律未动，五个变异探针全部按字节还原后 `git status --porcelain` 为空。

### 2026-08-24 · plan `P1.2` 收口：M1–M8 八个变异全被打红，但独立关闭审计**没做**，这条 gate 留白

- Phase 3 交付：会话 DocType **声明**落 git（`agenerp/context/doctype/agent_conversation_session.json`，
  不含任何 apply 逻辑）+ `tests/context/test_doctype_declaration.py`（同构断言 + 零站点写扫描）。
  owner doc 两处更新：`module-boundaries.md` **§7.7 新节**、`context-and-memory.md` §8.2 的 ① / ② 两行
  （**③ / ④ 一个字未动**）。STATE §3 **追加**一条 needs-human，`git diff --numstat` 该文件 `6 0`，删除列为 0。
- **M1–M8 八个变异逐个植入 → 跑 → 原样还原，八个全部被打红，无一需要就地补断言。**
  三条最值得记的：
  · **M4 只红了键序那一条** —— 保真与「两次落盘字节相等」都还是绿的。round-trip 与确定性
    确实是两个性质，合成一条断言的话按插入序写盘的实现整个溜过去。
  · **M3 红了两条** —— 算术断言与 `Usage.plus` 计数断言各一条。光有算术断言验不出「必须调 `plus()`」：
    一份手写但算得对的三项加法能满足全部算术断言。
  · **M8 只红了零站点写扫描那一条** —— 它就是 plan §5.2 可逆性声明的机械兑现。
- **但这不等于判据没有盲区，照实说**：M1–M8 是起草期自己挑的变异，挑不到自己没想到的地方。
  P1.1 的收口审计正是靠**另出四个变异**抓出「出站请求那一层完全没有判据」。
  本 plan 的同类盲区**没有被独立方检验过**。
- ❌ **`closure audit was independent` 这条 Closure Gate 未勾，本轮没做独立关闭审计。**
  本次执行会话被明确约束不得派发子代理，solo 自查按 `AGENTS.md` 的 Reviewer-Availability Fallback
  顶替不了一个 `Audit: required` 的 plan 的独立审计。**不勾它是唯一诚实的处置**，待人安排或授权。
  **本 plan 的收口强度因此低于 P1.1。**
- ⚠️ **会话在活站点上还没有落处。** 本 plan 未在任何站点建过表、未发出任何 DDL。
  残余照实记：AST 扫描挡不住 `getattr(client, "create_" + "doc")` 这类拼名调用，
  **不得说成「已证明不可能写站点」**。
- ⚠️ **本轮有另一个并发会话在同一工作树上提交**，把本 plan 的代码产物卷进了它自己的两笔提交
  （`3ce0410` · `38b5124`，提交信息讲的是 P1.0 的判定与标注集）。产物内容完好、判据全绿，
  但本 plan 的交付没有属于自己的提交信息。照实记，不粉饰。
- **命令原文 + 退出码**：
  `python3 -m pytest tests/context -q` → **exit 0**（`51 passed`）·
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 11 项：预期红 0，绿 11，跳过 0` / `373 passed`）·
  `python3 -m pytest tests/contracts -q` → **exit 0**（`151 passed`）·
  `python3 -m pytest tests/routing -q` → **exit 0**（`147 passed, 1 skipped`）·
  `python3 -m pytest tests/tools -q` → **exit 0**（`49 passed, 12 skipped`）·
  `python3 -m ruff check agenerp tests/unit tests/contracts tests/routing tests/context` → **exit 0**。


### 2026-08-24 · plan `P1.2` Phase 2 —— ② 会话层落地；M4 印证了「round-trip 与确定性是两个性质」

- 新增 `agenerp/context/session.py` · `agenerp/context/store.py` · `tests/context/_scan.py`
  · `tests/context/test_session.py` · `tests/context/test_store.py`（本 Phase 30 条判据）。
- **命名消歧写进类名**：`ConversationSession`（对话会话）vs `agenerp.tools.runtime.Session`
  （工具会话，执行体与站点之间的唯一通道）。同名不同义，两者无继承、无组合、不可互换。
- **用量聚合归本层，逐项语义沿用 P1.1**：`usage_total` 从空 `Usage()` 起逐轮调
  `Usage.plus()`，**N 轮恰好 N 次**（判据 monkeypatch 数次数，写死成 `== 3` 不写「至少一次」）。
  `reasoning` 是 `completion` 的细分，`total = prompt + completion`，**reasoning 不参与求和**。
- **最该记的一条 —— M4 当场印证了 plan 起草期第二轮评审的那句话**：把 `sort_keys=True` 去掉后，
  「落盘→读回保真」与「连续两次落盘字节相等」**两条都还是绿的**，
  只有**键序恰等于 `sorted(keys)`** 那一条红。round-trip 与确定性确实是两个性质，
  合成一条断言的话这个坏实现会整个溜过去。承载它的是每轮工具调用的 `params`
  （自由键字典，样例里插入序刻意写成 `zeta / mid / alpha`）——dataclass 的字段序是固定的，
  拿它验不出插入序问题。
- **权限/风险红线有机械判据，不是 code review**：`tests/context/_scan.py` 一套 AST 扫描，
  三条具体禁令（不 import `agenerp.contracts`、不碰契约求值面、不构造 `facts` 交给 `execute`）。
  ⚠️ 黑名单**刻意不写成**「禁 import `agenerp.tools.runtime`」——① 层正要 import 它的
  `wrap_free_text`，一条过宽的禁令会把 Phase 1 当场打红，而那与红线毫无关系。
- **三个变异当场验过**（逐个植入 → 跑 → 原样还原）：M3 改成自己写三项加法且把 reasoning
  加进 total → **红 2**（算术断言 + `Usage.plus` 计数断言）；M4 去掉 `sort_keys` → **红 1**（键序）；
  M7 `from agenerp.contracts import ReadOnlyContext` + 构造 `facts` → **红 3**。
- ⚠️ **快照那一项记的是「取证前后的只读快照」，不是写操作的回滚点** —— ② 端 v0 只有只读工具，
  没有写动作。不要读成「已经在记回滚点了」。
- **命令原文 + 退出码**：
  `python3 -m pytest tests/context -q` → **exit 0**（`42 passed`）·
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 11 项：预期红 0，绿 11，跳过 0` / `345 passed`）·
  `python3 -m pytest tests/contracts tests/routing tests/tools -q` → **exit 0**
  （`347 passed, 13 skipped`）·
  `python3 -m ruff check agenerp tests/unit tests/contracts tests/routing tests/context` → **exit 0**。


### 2026-08-24 · plan `P1.2` Phase 1 —— ① 即时上下文装配落地；首稿那条「已包过就不再包」被就地作废

- 新增 `agenerp/context/immediate.py` + `tests/context/test_immediate.py`（12 条判据，零网络）。
- **最该记的一条**：起草期发现首稿写的「自由文本已被 `wrap_free_text` 包过就不再包」是**反的**。
  那等于给攻击者一条「自己写一对标记 → 装配面认为已包过 → 载荷落在数据边界之外」的路，
  正是 Spike 01 探针 5 的形状。改成**复用** `agenerp.tools.runtime.wrap_free_text` 的现行语义：
  **先转义、后无条件包**。裁定、备选、残余风险全部落 `module-boundaries.md` §7.7。
- **`keep` 取 `STRUCTURAL_KEYS`，`name` 不加。** 理由不是「`name` 不重要」，而是本层把身份摆在
  `CurrentDocument.doctype` / `.name` 两个专用字段上，字段表里那一份没有「不包就会失效的判定面」；
  而 prompt 命名的 DocType 上 `name` 是人写的自由文本 —— 按保守口径它该被包。
- **判据不写成自指的那一条**：「与 `wrap_free_text` 输出逐字相等」单独看是空的
  （实现把 `keep` 塞成全部字段名，「什么都没包」与「逐字相等」同时成立）。因此 `keep`
  在测试里**写死成字面量**，另加两条正向断言（开头/结尾是边界标记、值内标记串出现转义串），
  「逐字相等」降为补充判据。
- **三个变异当场验过**（逐个植入 → 跑 → 原样还原，`git diff --stat` 回到零）：
  M1 值超长即截断 → **红 3**（含 `test_every_field_value_survives_verbatim_not_just_the_key_set`）；
  M2 删掉 `wrap_free_text` 调用 → **红 4**；M6「已包过就不再包」 → **红 1**（转义断言）。
- ⚠️ **verification scope limited**：`tests/context` 既不在 `commands.test` 也不在任何 CI job 里，
  **`GATE_VERIFY` 复跑不到本层的主判据**。缺口照实记在 §7.7 末节，代偿是变异自查 + 独立关闭审计 +
  收口时往 STATE §3 追加 needs-human。
- **命令原文 + 退出码**：
  `python3 -m pytest tests/context -q` → **exit 0**（`12 passed`）·
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 11 项：预期红 0，绿 11，跳过 0` / `345 passed`）·
  `python3 -m ruff check agenerp tests/unit tests/contracts tests/routing tests/context` → **exit 0**。


### 2026-08-24 · plan `P1.1` 收口：独立审计另出四个变异，抓出一层**完全没有判据**的代码

- **审计判 `APPROVED WITH CONDITIONS`**（独立子代理，非自审），1 Blocker + 5 Major + 4 Minor，
  **已逐条处置完毕，无一条降级为 follow-up**。
- **最该记的一条**：审计**没有采信** plan 的 M1–M5 结果表，而是自己另出四个变异 ——
  把 `messages` 换成空数组、让 `tools` 永不转发、拿掉 `context=_ssl_context()`、
  把 `Authorization` 换成别的头。**四个全部绿着过去，一条判据都没红。**
  根因：每条单测都注入 transport，`_post` 与 `_ssl_context` **一次都没被执行过** ——
  「能发请求」那一层在判据上完全是空的，而本 mission 的硬约束第一条正是「判据不许只验『调得通』」。
  **M1–M5 五个变异全打在 router 与 usage 解析上，没有一个打在出站请求上**，这就是自查的盲区形状：
  自己挑的变异，挑不到自己没想到的地方。已补 8 条判据直判发出去的那个 `Request`，
  四个变异现在**逐一打红**（各 1 条）。
- **四处"比证据更强的话"被改准**：① §12.5 写 `qwen3.6-plus` 是 `2/6`，同句括号里却是
  `1/3 + 0/3`（= `1/6`），**自相矛盾** —— 改按复核表逐格数取 `1/6`，而 `2/6` 出自 D-11 复验注记
  与 STATE §3（**两处都是人写的，红线 3/5 不许 loop 改**），分歧追加进 §3 待人裁定；
  ② 「人侧复核已声明取代那份审计文件」是 loop 自己的判断 —— `ffcf7c1` 逐字只写了
  「予以确认，依据比循环写的更强」，已改成「这是本节按 D-16 作的取舍，不是人已下的裁定」；
  ③ 凭据「绝不进日志」盖不住 `asdict` / `vars` —— `LlmConfig` 改为 `__slots__` 且不再是
  dataclass，并**照实写明挡不住**带 locals 的 traceback 打印器；
  ④ 「人可以增删模型档案表而不改任何代码路径」不成立，它就是产品源码。
- **两条交付次序上的问题，照实认，不辩解**：roadmap 工作项 3 先转 `done` 才有审计结论；
  plan 直到实现完成才第一次进 git，草案评审**无 git 可验的先后**（P1.0 / P1.0a 是分多次提交的
  正确形态，后续照那个来）。两条都写进了 plan `## Closure`。
- **STATE §3 本轮共追加 6 条 needs-human**，两笔提交 `git diff --numstat` 各 `6 0`，
  **删除列为 0，纯追加**。
- ⚠️ **verification scope limited**：`tests/routing` 既不在 `commands.test` 也不在任何 CI job 里，
  **`GATE_VERIFY` 复跑不到本 plan 的主判据**；本轮**零 CI 轮次**；
  审计方**无模型凭据**，活端点那次冒烟它没复跑过且**轨迹未落盘**，那三个 token 数在本仓内不可复现。
- **命令原文 + 退出码 + sha**（sha `4edb868`）：
  `python3 -m pytest tests/routing -q` → **exit 0**（`143 passed, 1 skipped`）·
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 11 项：预期红 0，绿 11，跳过 0` / `345 passed`）·
  `python3 -m pytest tests/contracts -q` → **exit 0**（`151 passed`）·
  `python3 -m pytest tests/tools -q` → **exit 0**（`49 passed, 12 skipped`）·
  `python3 -m ruff check agenerp tests/unit tests/contracts tests/routing` → **exit 0** ·
  `node tools/mission-driver/src/plan-check.mjs <plan> --strict` → 打勾后 **exit 0**（`45/0`）。

### 2026-08-24 · plan `P1.1` Phase 2–3 —— adapter + 路由器落地；冒烟推翻了自己写的一句话

- **交付**：`agenerp/routing/` 五个模块（`capabilities` / `config` / `adapter` / `router` / `errors`）
  + 6 个名字的导出面。路由是**纯确定性规则**，零模型参与（D-15）：集合包含判定，
  不满足就抛，异常文本逐条说**缺哪几项能力、缺在哪个模型上**。
  `requested` 走同一条校验 —— 「我点名要它」不是豁免，静默降级最常见的形态就是这个。
- **判据 132 passed, 1 skipped**（那 1 条是 `-m live`，无凭据时 skip 并打印理由）。
  **M1–M5 假实现变异自查全部被打红**（16 / 18 / 8 / 2 / 14 条），还原后复跑 132 passed：
  M1 忽略校验回默认模型 · M2 校验永远通过 · M3 `requested` 跳过校验 ·
  M4 reasoning 记进 completion · M5 失败回空 `Reply`。
  **M4 第一轮只打红 1 条**，一条判据托一个承重结论太薄，**当场补**
  `test_reasoning_is_never_folded_into_completion`，复跑打红 2 条。
- **活端点冒烟跑过，并且推翻了本 plan 起草时写的一句话** —— 这是本轮最该记的一条。
  实读回包逐字：`{"prompt_tokens": 15, "completion_tokens": 178, "total_tokens": 193,
  "completion_tokens_details": {"reasoning_tokens": 173}}`。`15 + 178 = 193` 对得上，
  即 **`reasoning` 是 `completion` 的一个细分，不是第四个桶**。
  代码原先的 docstring 与一条断言消息写的是「`total` 不含 reasoning」，
  **读起来像"reasoning 在 completion 之外"，是错的**，已就地改准。
  `total = prompt + completion` 这个**算法本身没错**，与端点自报的 `total_tokens` 一致。
  开工前写死的预期 `usage.reasoning > 0`（D-11「回两个字也烧约 195」）**吻合**：实测 188 / 194。
- **收尾自查（`development-wisdom-gate-prompt.md`）不是走过场，当场修掉两件事**：
  ① 回包 `choices[0]` 缺 `message`、或既无 `content` 又无 `tool_calls` 时，原实现**回一个空
  `Reply`** —— 那正是模块头拒绝的那种降级（`finish_reason='length'` 的截断会被当成一次"回答"）。
  改为抛并带上 `finish_reason` 与 usage，配三条判据，含一条**反向判据**：只回工具调用、
  不回文本仍是**正常**的一轮，不许矫枉过正。
  ② 「部分兼容端点对空数组会 400」是 `guessed`，本仓没验过，改写成「这是取舍，不是实测」。
  ③ 第 5 条 first-principles 实做：没采信 plan 转述的 CI 事实，重读 `gates.yml` ——
  `pip install pytest` 在 98/164/223/443 行，`ruff==0.14.1` 在 **480** 行（plan 写的是 479，差一行），
  作用域 446/449/486 行。**`tests/routing` 确实两边都不在。**
- **执行期间承重事实被人侧改写了一次，照实记**：Phase 1 写进 owner doc 的 P1.0 逐格计数
  （弱 on 2/3、强 3/3）在 Phase 2 提交之后被 `main` 上的 **`ffcf7c1`（人侧独立复核）取代**——
  正确的是 0/3 · 1/3 · 1/3 · 0/3，合计 **2/12**，且**强模型半边有反向证据**。
  同一提交立了 **D-16「以本项目的实测为准」**。owner doc §12.3/§12.5 **已改到复核的数**，
  并**逐字点名**循环自写的审计文件与它不同、复核已声明取代它 ——
  两处结论标签一致（`被削弱`），**分歧只在逐格计数，不合并成一个数、不悄悄改掉一边**。
  plan §1.4 与 Phase 1 原文**一个字未改写**（那是当时的实读记录）。
- **三条 needs-human 已追加进 `STATE.md` §3（纯追加，`git diff --numstat` = `6 0`）**：
  ① `tests/routing` 与 `tests/tools` 都不在 `commands.test` 与任何 CI job 上，
  连带 `pyproject.toml` 未声明 `dependencies`（`certifi` 至今是未声明依赖）；
  ② `qwen3.6-plus` 的 `multi_hop` 声明建立在本项目 **2/6** 的实测上 ——
  `route('lineage', ...)` 今天会放行它，而它在本项目实测下答对不到一半，**这是有意的取舍且已逐字登记**；
  ③ roadmap 工作项 2 的逐格计数与同文件 P1.7 警示表自相矛盾，**loop 不代改**。
- ⚠️ **verification scope limited，收口时不得读成「CI 已覆盖」**：`tests/routing`
  既不在 `missions/p1-insight.json` 的 `commands.test` 里，也不在任何 CI job 里。
  代偿控制只有三条：同构判据、降级反测、M1–M5 变异自查。**残余风险不消除。**
- **命令原文 + 退出码 + sha**（sha `5a0f87a`）：
  `python3 -m pytest tests/routing -q` → **exit 0**（`132 passed, 1 skipped`）·
  `set -a && . ./.env && set +a && AGENERP_LLM_API_KEY="$DASHSCOPE_API_KEY" AGENERP_LLM_BASE_URL="$DASHSCOPE_BASE_URL" python3 -m pytest tests/routing -q -m live -s`
  → **exit 0**（`1 passed, 132 deselected`；key 未进任何文件）·
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 11 项：预期红 0，绿 11，跳过 0` / `345 passed`）·
  `python3 -m pytest tests/contracts -q` → **exit 0**（`151 passed`）·
  `python3 -m pytest tests/tools -q` → **exit 0**（`49 passed, 12 skipped`）·
  `python3 -m ruff check agenerp tests/unit tests/contracts tests/routing` → **exit 0**。

### 2026-08-24 · plan `P1.1` Phase 1 —— 能力声明落地，顺手修掉 §12.3 那处确证漂移

- **交付**：`agenerp/routing/capabilities.py` 三份声明（能力枚举 4 项 / 任务类目→最低能力 4 档 /
  本仓实测过的 3 份模型档案）+ 声明自身的校验器；`agenerp/routing/errors.py` 两个失败类型
  （`DeclarationError` 是声明写错了，`RoutingError` 是运行时不满足，分开是因为该由不同的人在
  不同时刻修）。
- **判据不是「能 import」**：`tests/routing/test_capabilities.py` 把声明与
  `docs/architecture/model-management.md` §12.5 的三张 `machine-read` 表**逐行同构**判定，
  外加四类畸形声明各打红一次、`lineage` 最低能力换空集必须红、`lineage` 必须严格严于 `explain`。
  **同构判据当场做了变异实证**：把 §12.5 里 `lineage` 那行改成 `tool_calling` 单项 →
  `test_task_matrix_is_isomorphic_to_the_owner_doc` **红**；原样还原 → 18 passed。
  ⚠️ 同构管不了「两边一起写错」，这条残余风险已逐字写进 §12.5，不消除。
- **确证漂移的改准（`Fix`，不是 Follow-up）**：§12.3 结论表第二行原文「需强模型，或**由更强的
  循环门禁补偿**（见 §5.0 ①）」——P1.0 实测把这条替代路径削弱了：同模型下门禁 0/3 → 2/3
  **有效**，但**没有**让弱模型追上强模型无门禁的 3/3。改准连同 2×2 网格与
  ⚠️「只覆盖一道题、两个模型、每格 3 次有效运行，不得外推」一起写进文档。
  **被削弱的是「门禁能补偿能力」这个推论，不是门禁本身**：`agents-and-roles.md` §5.0 ①
  的规则文本一个字未动，`docs/masterplan/**` 一个字未动。
- **两个 `Decision` 的备选与否决理由进了文档，不留在 chat**：否决 `vision`/`embedding`（本期无
  消费者，声明了也无人校验）、否决把速度/价格当能力（那是选型偏好，混进来会让「不满足则失败」
  退化成「按价格排序」）；否决按 Agent 分类目（Agent 还没有）、否决只留强/快两档（那正是
  §12.3 结论 3 要推翻的一刀切）。
- **两条残余风险照实登记，不写成已校验**：① `multi_hop` 今天没有低成本的自动判定法，取值来自
  **人写的档案**；② 四档边界只从一次实验加四道探针推出来，**P1.4 后很可能重划**。
- **命令原文 + 退出码 + sha**（sha `1dae31b`）：
  `python3 -m pytest tests/routing/test_capabilities.py -q` → **exit 0**（`18 passed`）·
  `python3 -m ruff check agenerp tests/unit tests/contracts tests/routing` → **exit 0** ·
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 11 项：预期红 0，绿 11，跳过 0` / `345 passed`）。

### 2026-08-24 · plan `P1.0a` 收口：独立收口审计跑通，plan-check 从红转绿

- **卡在哪**：`P1.0a` 的 `CLOSURE_VERIFY` 连续判 FAIL，两条原因，一条是格式一条是实质。
- **格式那条（真缺陷，不是刁难）**：`plan-check.mjs` 认 `## Closure` 的正则是
  `/^#{2,4}\s+Closure\s*$/im`，本 plan 写的是编号形式 `## 12. Closure`，**匹配不上** →
  `hasClosureSection = false` → 判「completed plan missing Closure evidence」。
  按指南 `docs/plans/00-plan-authoring-and-execution-guide.md` §模板的写法改回
  `## Closure Gates` / `## Closure`（仓内其余 20 份 plan 都是这个形态），并顺手改正两处
  指向错节的交叉引用（「假实现变异自查」在 §10 不在 §11）。
- **实质那条**：唯一未打勾的 `- [ ] closure audit was independent` 是**上一轮刻意留空的**
  ——执行者与实现者是同一个，自审不算独立。**代打勾即伪造关闭证据**，所以这一轮真派了
  一个独立子代理复核，而不是把框勾上了事。审计判 **APPROVED**，六条命令原样复跑：
  `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 11 项：预期红 0，绿 11，跳过 0`）·
  `python3 -m pytest tests/unit -q` → **exit 0**（`345 passed`）·
  `python3 -m pytest tests/contracts -q` → **exit 0**（`151 passed`）·
  `python3 -m pytest tests/tools -q` → **exit 0**（`49 passed, 12 skipped`）·
  `python3 -m ruff check agenerp tests/unit tests/contracts tests/tools` → **exit 0** ·
  `node tools/mission-driver/src/plan-check.mjs <plan> --strict` → 打勾后 **exit 0**（`58/0`）。
- **审计的限制照实记**：审计环境**无站点凭据**，12 条活站点合规断言全部 skip，
  **没有观测到任何一条活站点断言真跑**。活站点那一层只在 CI 的「L2 种子链」job 里成立。
- **顺带对齐了三处已经过期的话**：`3b6d071`（人，带 `Gates-Change-Approved-By:`）已经把
  `tests/gates/test_tool_execution_live.py` 补上并接进 CI，而 plan §9 / `## Closure` 的
  Follow-up 与 roadmap 第 1 项都还写着「待人做」。三处改成实况，**上方原口径一字未改，只补记**。
  仍未做的那一条（`tests/tools` 未进 `unit-and-contracts` / `lint` 的路径）保留为 ⬜。
- **审计带回三条非阻塞旁证，已登记进 plan `## Closure`**：① `check_expected_red.py` 的 exit 0
  只覆盖 31 项里的 11 项（default 模式追加 `-m "not live"`），**不等于「全部门禁绿」**；
  ② `schema_search` 的 `returns_candidate_list_not_single_pick` 在代码里近乎恒真，
  检出能力实际在测试侧，§10 那句「已改出恒真」只对了一半；③ `Plan Status: completed` 是在
  独立审计之前置上的，次序与指南相反，本轮事后补齐使其成立。

### 2026-08-24 · plan `P1.0` Phase 3–4 —— 2×2 网格跑完了，承重命题的结论是**被削弱**，照实写

- **网格结果**（每格 3 次有效运行，判定对象只有最终答案文本）：

  | 格位 | 正确 | 不完全 | 错误 |
  |---|---|---|---|
  | `qwen-plus`（弱）· 门禁 **off** | **0** | 2 | 1 |
  | `qwen-plus`（弱）· 门禁 **on** | **2** | 1 | 0 |
  | `qwen3.6-plus`（强）· 门禁 **off** | **3** | 0 | 0 |
  | `qwen3.6-plus`（强）· 门禁 **on** | **3** | 0 | 0 |

- **承重命题「确定性门禁能否补偿模型能力不足」的处置：`被削弱`**。
  门禁的效果是真的（同模型同提示词，0/3 → 2/3，唯一变量是门禁），
  **但它没有让弱模型追上强模型无门禁的 3/3**。本题支持的最强表述到此为止，
  一个字都不再往前推。
- **H1 判 `不吻合`，而且是有意思的那种不吻合**：H1 逐字预测弱模型「不会自发地从 Bin
  反查入库来源」。实测 run-02 与 run-13 **查了**——它们把两张入库凭证逐笔列进了答案
  （run-13 逐字「2 月 5 日入库 1000 → 2 月 7 日又入库 1000（总计 2000）→ 出库 990」），
  却把根因写成「销售订单还有 10 台没发」。**失败在归因，不在取证。**
  这解释了为什么现有 L3 有效：它判的是「答案里报了库存数 → 入库来源必须逐张覆盖」，
  逼的正是「把查到的东西用上」，而不是「去查」。
- **H4 吻合，但别读成「门禁便宜」**：工具调用中位数 `qwen-plus` 5 → 10（上限 15）、
  `qwen3.6-plus` 10 → 10（上限 30），方向收敛。**token 不在判据里**：
  `qwen-plus` 25,586 → 78,915（**约 3.1 倍**），`qwen3.6-plus` 84,942 → 85,182（约 1.0 倍）。
  弱模型侧门禁的 token 代价已顶到判据给调用次数设的那条 3× 线，而判据没约束它。
- **两次无效运行照实记，没有静默丢弃**：`run-09`（`qwen-plus`/off）与 `run-10`
  （`qwen3.6-plus`/on）均 token 超限（上限 200,000），未产出答案。
  按 plan §2 补跑 `run-13` / `run-14`，两份无效轨迹**原样留在仓库**。
  同格无效最高 1 次，未触及「> 3 次即停下写 STATE §3」的上限。
- ⚠️ **盲判没做到，写在最显眼的地方**：判定者与执行者是同一个循环，且在打勾前
  已看过 `run_id → 模型/门禁` 的汇总表。`verdicts.md` 抬头与审计文件 §5.2 逐字记了这件事。
  判据本身是跑之前冻结的、逐条命中/未命中全部落盘，但「事后挪标准」这一风险
  **只被削弱，没有被消除**。
- ⚠️ **一个悬而未决的口径交人定**：判据 ③「点名两批生产中的至少一批是多余的」
  没要求机制正确。run-04 点名了外协那批多余（命中），但把机制说成「重复记账」，
  而站点上是真有两批实物。**若加严口径，H2 从 `吻合` 翻成 `不吻合`**——翻转点
  写进了审计文件 §2，本轮不自行加码。
- **判据复跑（命令原文 + 退出码）**：
  - `python3 tools/gates/check_expected_red.py` → **exit 0**，`门禁 11 项：预期红 0，绿 11，跳过 0`
  - `python3 -m pytest tests/unit -q` → **exit 0**，`345 passed`
  - `python3 -m pytest tests/experiments -q` → **exit 0**，`10 passed`
  - `python3 -m ruff check agenerp tests/unit tests/contracts tests/tools tests/experiments tools/experiments`
    → **exit 0**，`All checks passed!`
  - `AGENERP_HTTP_PORT=18080 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin python3 -m agenerp.seedsite --verify-site --site frontend`
    → **exit 0**，`站点侧对账：30 项，通过 30，失败 0`
  - 补跑两次运行的命令原文（`.env` 提供 `DASHSCOPE_*`，key 不进 git/日志）：
    `AGENERP_HTTP_PORT=18080 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin python3 -m tools.experiments.p1_entry_gate.run --model qwen-plus --gate off --max-tokens 200000 --seed-run-id run-13`
    → **exit 0**；同一条把 `--model qwen3.6-plus --gate on --seed-run-id run-14` 换上 → **exit 0**
  - sha `2c3ba6b`
- ⚠️ **verification scope limited**：**零 CI 轮次、零 `AGENERP_LIVE=1` 整目录判定**。
  本轮的绿是本机的绿加 14 次真实 LLM 运行；`tests/experiments/` 仍不在 CI 的任何 job 里
  （与 `tests/tools/` 同因，红线 2）。


### 2026-08-24 · plan `P1.0` Phase 2 —— 入口关口实验的最小控制循环跑起来了

- **落在 `tools/experiments/p1_entry_gate/`**，不进 `agenerp/`：这是为一次实验而建的设施，
  不承担产品的向后兼容义务。模型路由的产品形态是 P1.1。
  ⚠️ **目录名与 plan 原文差一个字符**（`p1-entry-gate` → `p1_entry_gate`）：
  带连字符的目录不是合法 Python 包名，判据 import 不到被测对象。为一个连字符
  把判据写成 `importlib` 按路径加载不值得。**执行者的就地判断，已记进 plan。**
- **门禁在两处起作用，而且是同一组 `Condition`**（`agenerp/tools_readonly.py` 的 `EVIDENCE_GATE`）：
  ① 作答类工具（`query.read` / `snapshot.read`）的**前置**——契约本来就把门禁挂在它们身上；
  ② 模型给出最终答案时的**答案面判定**——L3 在这里才判得动。
  `gate.py` 里**一条规则原文都没有**，它只负责把五条事实凑齐。
- **L3 的触发口径是「答案里的数字」，不是「模型查过 Bin」**：后者会让一个从不查 Bin、
  却照样报出 1,010 的回答绕过 L3。判据独立于模型行为——直接拿答案里的数字比对站点的
  `Bin.actual_qty`，命中就取该 `(物料, 仓库)` 的全部入库凭证。
  **残余口子照实记**：只说「积压」而不报数字的回答 escape 得掉 L3，
  因为规则原文写的就是「作答涉及某个仓库的库存**数量**」——不擅自扩大它。
- **两处就地裁定**（都写进 plan 的 Phase 2）：
  - 强制续跑消息**只说规则和缺几件，不说缺哪几件**。说出单号等于把答案递过去，
    那测到的就成了「告诉模型答案有没有用」。**代价：H2 的结果是下界**——
    若 H2 不成立，只能推出「这种强度的门禁在这道题上不够」，推不出「门禁无用」。
  - `permission.scope` **不进工具面**（9 个而非 10 个）：它的后置断言
    `injected_at_session_start` 断的是编排面的事实（P1.3 的开场自动注入），
    本设施没实现那件事，递 `True` 进去就是断言一件不成立的事。
- **冒烟跑通了，而且门禁在轨迹里真的改变了路径**（`qwen-plus` / `--gate on`）：
  第 1–3 轮 `doc.get` 订单 → `doc.links` → `doc.get` 发货单；
  **第 4 轮 `query.read` 被 L2 挡下**（`未覆盖 ['MFG-WO-2026-00001']`）；
  第 5 轮补上那张工单的 `doc.get`，随后才查库存流水、逐张取入库凭证的证，第 10 轮作答，门禁绿。
  9 次工具调用，token `prompt 61209 / completion 1348 / reasoning 0`
  （`qwen-plus` 不是推理模型，`reasoning 0` 是**正确读数**，不是没记）。
- **判据复跑（命令原文 + 退出码）**：
  - `python3 -m pytest tests/experiments -q` → **exit 0**，`10 passed`
  - `python3 -m ruff check agenerp tests/unit tests/contracts tests/tools tests/experiments tools/experiments`
    → **exit 0**，`All checks passed!`
  - sha `55bab42`
- ⚠️ **`tests/experiments/` 同样不在 CI 的任何 job 里**（与 `tests/tools/` 同因，红线 2）。

### 2026-08-24 · plan `P1.0a` Phase 1–4 —— 十条只读契约有执行体了，`permission.scope` 的判别力有真反例了

- **为什么这一轮跑的是 P1.0a 而不是派发给我的 P1.0**：P1.0 Phase 2 的 `Prereqs` 逐字要求
  「P1.0a 工具执行层完成」，而 P1.0 的 Non-Goals 第 3 条又逐字禁止在那个 plan 里实现执行体。
  `STATE.md` §3 的 2026-08-24T04:10Z `[open]` 行已把处置写死：**「下一轮改派 P1.0a……
  这件事 loop 可以自己做，不需人定」**。本轮按那条处置执行，**没有绕过任何一条 Non-Goal**。
- **交付了什么**（sha `35313cb`）：
  - `agenerp/tools/runtime.py` —— **唯一执行入口**，四步序：前置不满足 → `abort_and_report`
    且**一个请求都不发** → 打站点 → 按 `returns` 裁剪（剥框架管道字段 / `max_rows` 截断 /
    `must_keep` 核对 / §7.5 数据边界标记）→ 后置断言。**③ 与 ④ 不可颠倒**：后置判的是
    **给出去的东西**，先判后裁的话被裁掉的字段参与了判定却不在返回值里。
  - `agenerp/tools/{site_scope,documents,queries}.py` —— 十个执行体，逐条照契约原文实现。
  - `agenerp/seedusers.py` —— 受限身份「车间工人」的**幂等**装载（只读 3 个 DocType）。
  - `tests/tools/` —— 四组判据：执行入口 11 · 十执行体 34 · 双向注册 4 · **活站点合规 12**。
- **一处起草时没料到的设计，照实记**：后置断言里有一类是**过程约束**
  （`permission.scope` 必须逐个调 `has_permission`、`query.read` 不得跨表拼装），
  **从返回值上看不出来**。执行入口因此多了一个 `Session`：执行体与站点之间的唯一通道，
  逐条记调用留痕，事实**从留痕上推**而不由执行体自报。
  残余弱点不消除：它验得了「调没调」，验不了「每次调用的参数语义都对」。
- **判别力有实据了**（STATE §3 那条 2026-08-24T03:18Z `[open]` 就此收口）：
  以 `worker@hrd.example.com` 跑 `permission.scope`，候选 6 个 → **可读 3
  （`Work Order` / `Stock Entry` / `Item`）、不可读 3（`Sales Order` / `GL Entry` / `Account`）**。
- **三条实测限制**（已写进 `docs/architecture/module-boundaries.md` §7.6）：
  - **受限身份枚举不出 DocType 清单**：stock Frappe 只把 `DocType` 的读权限给
    System Manager / Administrator（实读它的 `DocPerm` 只有这两条），且对它建
    `Custom DocPerm` **不生效**（授了 read 之后 `has_permission("DocType")` 仍为 `False`）。
    → `permission.scope` 的候选集**可由调用方给**。**没有靠给工人发 System Manager 绕过去**
    ——那等于把「受限」这件事取消掉，判别力也就没了。
  - **REST 面没有批量计数端点**：「只回有数据的 DocType」要对每个候选调一次
    `frappe.client.get_count`，本站点 **239 次、约 2 秒**。虚拟 DocType 必须排掉——
    它们没有物理表，计数回 `{}`（无 `message`）而不是 0（实测 `Bulk Transaction Log`）。
  - **`Bulk Transaction Log` 那个 `{}`**：不排掉的话，一个纯粹的结构事实会被读成「站点答不上话」。
- **假实现变异自查（七个，全部被打红）**：永远回 true 的 `permission.scope`（2 failed）·
  不剥框架字段的裁剪（1）· 不套数据边界标记（3）· 只留已提交的 `doc.links`（2）·
  不回溯父单据的 `lineage.trace`（3）· 只挑第一名的 `schema.search`（1）·
  不逐个探的 `permission.scope`（3）。还原后复跑全绿。
  **自查当场加强过一条断言**：`schema.search` 的 `returns_candidate_list_not_single_pick`
  起初写成恒真表达式，在「只挑第一名」下会照样绿。
- **一次被判据挡下来的分层错误，照实记**：`agenerp/seedusers.py` 初版直接读环境变量取口令，
  被 `tests/unit/test_seed_deterministic.py` 判红（`agenerp/seed*.py` 整片被当「生成路径」扫）。
  **没有改判据**，而是把凭据读取挪进 `agenerp/site.py` 的 `credential_from_env`
  ——那本来就是 §11.7 里凭据该待的地方。
- **判据复跑（命令原文 + 退出码）**：
  - `python3 tools/gates/check_expected_red.py` → **exit 0**，`门禁 11 项：预期红 0，绿 11，跳过 0`
  - `python3 -m pytest tests/unit -q` → **exit 0**，`345 passed`
  - `python3 -m pytest tests/contracts -q` → **exit 0**，`151 passed`
  - `python3 -m pytest tests/tools -q` → **exit 0**，`49 passed, 12 skipped`（skip 理由逐条打印）
  - `AGENERP_HTTP_PORT=18080 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin AGENERP_WORKER_PASSWORD=… python3 -m pytest tests/tools -q`
    → **exit 0**，`61 passed`
  - `python3 -m ruff check agenerp tests/unit tests/contracts tests/tools` → **exit 0**，`All checks passed!`
  - `AGENERP_HTTP_PORT=18080 … python3 -m agenerp.seedsite --verify-site --site frontend`
    → **exit 0**，`站点侧对账：30 项，通过 30，失败 0`
  - `AGENERP_HTTP_PORT=18080 … AGENERP_WORKER_PASSWORD=… python3 -m agenerp.seedusers --load-users --site frontend`
    → **exit 0**，`合计：新建 0 / 已存在 5`（原样复跑一致）
- ⚠️ **verification scope limited**：`tests/tools/` **不在 CI 的任何 job 里**
  （`unit-and-contracts` 与 `lint` 的路径都不含它）。接进去要改 `.github/workflows/**`（红线 2），
  只能由人做。本轮的绿是**本机的绿**：零 CI 轮次、零 `AGENERP_LIVE=1` 整目录判定。
- ⚠️ **WBS §4 第 78 行的 🔴 判据只满足了一半**：`tests/gates/test_tool_execution_live.py`
  **本轮没有创建**（红线 1）。同等强度的断言在 `tests/tools/test_live_conformance.py`，
  提升与接线是**人的动作**，已挂 STATE §3 needs-human 队列。
- ⚠️ **本机站点上留了两条探路残余**：探路阶段给「车间工人」建过 `DocType` 与 `Module Def`
  的 `Custom DocPerm`（前者对 Frappe **不生效**，后者生效但装载器不建它）。
  装载器只建三条业务 DocType 的读权限，**全新栈上不会有这两条**；判据不依赖它们。

### 2026-08-24 · plan `P1.0` Phase 1 —— owner-doc 漂移消除，证据门禁 L3 的出处不再指向一个不存在的条款

- **改的是什么**：`docs/design/agents-and-roles.md` §5.0 ①（解释 Agent 的证据充分性门禁）
  与 §9（风险分级）。四件事按 plan 的 Phase 1 Fix 项逐条落地，见 sha `1f88581`。
- **① 移除 `LOSS-00003` 案例**。它是 XM 自建的 custom DocType，已按 D-9 退役。
  **L1/L2 的措辞一个字未动**——失效的是推出它们的案例，不是规则。原句「那 10 米是
  `LOSS-00003` 已审批的合理损耗」整段删掉，改写成失败**形状**（停在第一层证据上 /
  看到了下游单却没打开），并加一条注记说明 Spike 02/08 跑在退役前的 XM 数据集上、
  数量骨架 1,000/990/1,010 原样保留、单位随样板公司改「台」。
  ⚠️ 原文的「188 元未开票」是 XM 价格量级专属的数字，**直接去掉，没有另编一个 HRD
  价格的替身**——编一个就是把实测记录改写成没跑过的东西。
- **② 把证据门禁 L3 写进该节**。此前 `EVIDENCE_GATE_L3.source` 逐字指向
  「`agents-and-roles.md` §5.0 ① 规则 L3」，而**该节根本没有 L3**——代码引用了一个
  不存在的条款。现三条规则的 `text` 在该节逐字可查，已机械核对：
  `EVIDENCE_GATE_L1/L2/L3.text` 与文中 `规则 Lx：…` 三行**逐字相等**（`in doc` 全 True）。
- **③ 单位「米」→「台」**：§5 表里洞察 Agent 的例句 + §5.0 ② 的正文与表头两处。
  `grep -n "LOSS-00003" docs/design/agents-and-roles.md` → **无输出（exit 1）**；
  余下两处「米」是解释单位改换来历的注记本身，不是旧口径。
- **④ 与风险分级的 L3 消歧**。§5.0 ① 与 §9 各加一条「命名消歧」注记：前者是
  **证据门禁 L1/L2/L3**（决定什么时候允许停下来），后者是**风险档 L0–L3**（决定
  要不要人批）；§9 的「L3 中的 Server Script 特别处置」改为「**风险档 L3** 中的…」。
  ⚠️ **plan 正文把风险分级记作「§6」，实读在 §9**（§6 是角色 × Agent 矩阵）——
  按实读处置，不按 plan 的行号处置。
- **Exit Criteria 的活站点那一条已实测**（从 `SAL-ORD-2026-00001` 出发能看到什么）：
  站点上指向 `Sales Order` 的 Link 字段共 **18 个，其中 14 个在子表**；对 `SAL-ORD-2026-00001`
  真正命中的只有两条，子表命中已回溯父单据 ——
  - `Delivery Note MAT-DN-2026-00001` ← `Delivery Note Item.against_sales_order`
  - `Work Order MFG-WO-2026-00001` ← `Work Order.sales_order`（T0 `ef71196` 补送的字段）
  - **外协侧仍不可见，且是结构性的**：`Subcontracting Order` / `Subcontracting Receipt`
    上指向 `Sales Order` 的 Link 字段**各 0 个**（站点各 1 张单：`SC-ORD-2026-00001` /
    `MAT-SCR-2026-00001`）。**这正是实验要的形状，不是缺陷，不要去修它。**
- **判据复跑（命令原文 + 退出码）**：
  - `python3 -m pytest tests/unit/test_evidence_gate_l3.py -q` → **exit 0**，`5 passed`
  - `python3 -m pytest tests/unit -q` → **exit 0**，`340 passed`
  - `python3 -m pytest tests/contracts -q` → **exit 0**，`151 passed`
  - `python3 tools/gates/check_expected_red.py` → **exit 0**，`门禁 11 项：预期红 0，绿 11，跳过 0`
  - `AGENERP_HTTP_PORT=18080 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin python3 -m agenerp.seedsite --verify-site --site frontend`
    → **exit 0**，`站点侧对账：30 项，通过 30，失败 0`
  - `bash tools/check-state-consistency.sh` → **exit 0** · `bash tools/check-masterplan-links.sh` → **exit 0**
  - `node tools/check-doc-references.mjs` → **exit 1**。⚠️ **照实记**：53 行报告里
    `agents-and-roles` **零命中**，全部是本次改动之前既有的 line-ref 债（`system-baseline.md` /
    `module-boundaries.md`）。**不是本次引入，也没有被本次修好。**
- **本轮的提交 sha（STATE §2 那条「该提交 sha 见 `docs/logs/`」指的就是这两个）**：
  - `1f88581` —— owner doc 重述（`docs/design/agents-and-roles.md`）
  - `9e4f763` —— Phase 1 收口落账（本日志 + STATE 纯追加 12 行 + plan 打勾）
  - 另有 `b286112` —— **与本项无关**：P1.0a 方案的起草评审改动，本轮开工前就躺在
    工作区未提交，单独一个提交入库，不混进 `9e4f763`
- ⚠️ **scoped verification**：本轮零 CI 轮次、零 `AGENERP_LIVE=1` 整目录判定，
  只在本机默认判定面 + 一次活站点只读对账上跑过。**不得据此宣称任何 CI 侧结论。**

### 2026-08-24 · plan `P1.0` Phase 2–4 —— 被自己写死的前置卡住，未开工

- **Phase 2 的 `Prereqs` 逐字写着「P1.0a 工具执行层完成」**，而 `docs/plans/p1-insight/2026-08-24-P1.0a-tool-execution-layer.md`
  四个 Phase **全部为 `planned`**，`agenerp/tools/` 与 `tests/tools/` 两个目录都不存在。
- **不能绕**：P1.0 的 Non-Goals 第 3 条逐字写着「**不**实现 10 个工具的执行体（那是 **P1.0a**）」。
  在控制循环里顺手写十个执行体 = 在一个实验设施的 plan 里交付契约层的 B 半，
  既违反本 plan 自己的 Non-Goals，也绕过 P1.0a 的独立收口审计。
- **Phase 3 / Phase 4 顺次被 Phase 2 挡住**（各自的 `Prereqs` 是前一个 Phase）。
- **处置**：Phase 1 打勾转 `completed`，Phase 2–4 保持 `planned` 未开工；本轮**不**把
  `Plan Status` 改成 `completed`。执行顺序的账在 `docs/backlog/p1-insight-roadmap.md`：
  P1.0a 是第 1 项、P1.0 是第 2 项，**roadmap 的顺序是对的，这轮派发的顺序反了**。
