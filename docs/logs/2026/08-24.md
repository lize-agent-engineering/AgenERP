# Development Log - 2026-08-24

### 2026-08-24 · plan `P1.1` 收口：独立审计另出四个变异，抓出一层**完全没有判据**的代码

- **审计判 `APPROVED WITH CONDITIONS`**（独立子代理，非自审），1 Blocker + 5 Major + 4 Minor，
  **已逐条处置完毕，无一条降级为 follow-up**。
- **最该记的一条**：审计**没有采信** plan 的 M1–M5 结果表，而是自己另出四个变异 ——
  把 `messages` 换成空数组、让 `tools` 永不转发、拿掉 `context=_ssl_context()`、
  把 `Authorization` 换成别的头。**四个全部绿着过去，一条判据都没红。**
  根因：每条单测都注入 transport，`_post` 与 `_ssl_context` **一次都没被执行过** ——
  「能发请求」那一层在判据上完全是空的，而本 mission 的硬约束第一条正是「判据不许只验『调得通』」。
  **M1–M5 五个变异全打在 router 与 usage 解析上，没有一个打在出站请求上**，这就是自查的盲区形状：
  自己挑的变异，挑不到自己没想到的地方。已补 8 条判据直判发出去的那个 `Request`，
  四个变异现在**逐一打红**（各 1 条）。
- **四处"比证据更强的话"被改准**：① §12.5 写 `qwen3.6-plus` 是 `2/6`，同句括号里却是
  `1/3 + 0/3`（= `1/6`），**自相矛盾** —— 改按复核表逐格数取 `1/6`，而 `2/6` 出自 D-11 复验注记
  与 STATE §3（**两处都是人写的，红线 3/5 不许 loop 改**），分歧追加进 §3 待人裁定；
  ② 「人侧复核已声明取代那份审计文件」是 loop 自己的判断 —— `a810c0c` 逐字只写了
  「予以确认，依据比循环写的更强」，已改成「这是本节按 D-16 作的取舍，不是人已下的裁定」；
  ③ 凭据「绝不进日志」盖不住 `asdict` / `vars` —— `LlmConfig` 改为 `__slots__` 且不再是
  dataclass，并**照实写明挡不住**带 locals 的 traceback 打印器；
  ④ 「人可以增删模型档案表而不改任何代码路径」不成立，它就是产品源码。
- **两条交付次序上的问题，照实认，不辩解**：roadmap 工作项 3 先转 `done` 才有审计结论；
  plan 直到实现完成才第一次进 git，草案评审**无 git 可验的先后**（P1.0 / P1.0a 是分多次提交的
  正确形态，后续照那个来）。两条都写进了 plan `## Closure`。
- **STATE §3 本轮共追加 6 条 needs-human**，两笔提交 `git diff --numstat` 各 `6 0`，
  **删除列为 0，纯追加**。
- ⚠️ **verification scope limited**：`tests/routing` 既不在 `commands.test` 也不在任何 CI job 里，
  **`GATE_VERIFY` 复跑不到本 plan 的主判据**；本轮**零 CI 轮次**；
  审计方**无模型凭据**，活端点那次冒烟它没复跑过且**轨迹未落盘**，那三个 token 数在本仓内不可复现。
- **命令原文 + 退出码 + sha**（sha `29f7a69`）：
  `python3 -m pytest tests/routing -q` → **exit 0**（`143 passed, 1 skipped`）·
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 11 项：预期红 0，绿 11，跳过 0` / `345 passed`）·
  `python3 -m pytest tests/contracts -q` → **exit 0**（`151 passed`）·
  `python3 -m pytest tests/tools -q` → **exit 0**（`49 passed, 12 skipped`）·
  `python3 -m ruff check agenerp tests/unit tests/contracts tests/routing` → **exit 0** ·
  `node tools/mission-driver/src/plan-check.mjs <plan> --strict` → 打勾后 **exit 0**（`45/0`）。

### 2026-08-24 · plan `P1.1` Phase 2–3 —— adapter + 路由器落地；冒烟推翻了自己写的一句话

- **交付**：`agenerp/routing/` 五个模块（`capabilities` / `config` / `adapter` / `router` / `errors`）
  + 6 个名字的导出面。路由是**纯确定性规则**，零模型参与（D-15）：集合包含判定，
  不满足就抛，异常文本逐条说**缺哪几项能力、缺在哪个模型上**。
  `requested` 走同一条校验 —— 「我点名要它」不是豁免，静默降级最常见的形态就是这个。
- **判据 132 passed, 1 skipped**（那 1 条是 `-m live`，无凭据时 skip 并打印理由）。
  **M1–M5 假实现变异自查全部被打红**（16 / 18 / 8 / 2 / 14 条），还原后复跑 132 passed：
  M1 忽略校验回默认模型 · M2 校验永远通过 · M3 `requested` 跳过校验 ·
  M4 reasoning 记进 completion · M5 失败回空 `Reply`。
  **M4 第一轮只打红 1 条**，一条判据托一个承重结论太薄，**当场补**
  `test_reasoning_is_never_folded_into_completion`，复跑打红 2 条。
- **活端点冒烟跑过，并且推翻了本 plan 起草时写的一句话** —— 这是本轮最该记的一条。
  实读回包逐字：`{"prompt_tokens": 15, "completion_tokens": 178, "total_tokens": 193,
  "completion_tokens_details": {"reasoning_tokens": 173}}`。`15 + 178 = 193` 对得上，
  即 **`reasoning` 是 `completion` 的一个细分，不是第四个桶**。
  代码原先的 docstring 与一条断言消息写的是「`total` 不含 reasoning」，
  **读起来像"reasoning 在 completion 之外"，是错的**，已就地改准。
  `total = prompt + completion` 这个**算法本身没错**，与端点自报的 `total_tokens` 一致。
  开工前写死的预期 `usage.reasoning > 0`（D-11「回两个字也烧约 195」）**吻合**：实测 188 / 194。
- **收尾自查（`development-wisdom-gate-prompt.md`）不是走过场，当场修掉两件事**：
  ① 回包 `choices[0]` 缺 `message`、或既无 `content` 又无 `tool_calls` 时，原实现**回一个空
  `Reply`** —— 那正是模块头拒绝的那种降级（`finish_reason='length'` 的截断会被当成一次"回答"）。
  改为抛并带上 `finish_reason` 与 usage，配三条判据，含一条**反向判据**：只回工具调用、
  不回文本仍是**正常**的一轮，不许矫枉过正。
  ② 「部分兼容端点对空数组会 400」是 `guessed`，本仓没验过，改写成「这是取舍，不是实测」。
  ③ 第 5 条 first-principles 实做：没采信 plan 转述的 CI 事实，重读 `gates.yml` ——
  `pip install pytest` 在 98/164/223/443 行，`ruff==0.14.1` 在 **480** 行（plan 写的是 479，差一行），
  作用域 446/449/486 行。**`tests/routing` 确实两边都不在。**
- **执行期间承重事实被人侧改写了一次，照实记**：Phase 1 写进 owner doc 的 P1.0 逐格计数
  （弱 on 2/3、强 3/3）在 Phase 2 提交之后被 `main` 上的 **`a810c0c`（人侧独立复核）取代**——
  正确的是 0/3 · 1/3 · 1/3 · 0/3，合计 **2/12**，且**强模型半边有反向证据**。
  同一提交立了 **D-16「以本项目的实测为准」**。owner doc §12.3/§12.5 **已改到复核的数**，
  并**逐字点名**循环自写的审计文件与它不同、复核已声明取代它 ——
  两处结论标签一致（`被削弱`），**分歧只在逐格计数，不合并成一个数、不悄悄改掉一边**。
  plan §1.4 与 Phase 1 原文**一个字未改写**（那是当时的实读记录）。
- **三条 needs-human 已追加进 `STATE.md` §3（纯追加，`git diff --numstat` = `6 0`）**：
  ① `tests/routing` 与 `tests/tools` 都不在 `commands.test` 与任何 CI job 上，
  连带 `pyproject.toml` 未声明 `dependencies`（`certifi` 至今是未声明依赖）；
  ② `qwen3.6-plus` 的 `multi_hop` 声明建立在本项目 **2/6** 的实测上 ——
  `route('lineage', ...)` 今天会放行它，而它在本项目实测下答对不到一半，**这是有意的取舍且已逐字登记**；
  ③ roadmap 工作项 2 的逐格计数与同文件 P1.7 警示表自相矛盾，**loop 不代改**。
- ⚠️ **verification scope limited，收口时不得读成「CI 已覆盖」**：`tests/routing`
  既不在 `missions/p1-insight.json` 的 `commands.test` 里，也不在任何 CI job 里。
  代偿控制只有三条：同构判据、降级反测、M1–M5 变异自查。**残余风险不消除。**
- **命令原文 + 退出码 + sha**（sha `b6f4a5a`）：
  `python3 -m pytest tests/routing -q` → **exit 0**（`132 passed, 1 skipped`）·
  `set -a && . ./.env && set +a && AGENERP_LLM_API_KEY="$DASHSCOPE_API_KEY" AGENERP_LLM_BASE_URL="$DASHSCOPE_BASE_URL" python3 -m pytest tests/routing -q -m live -s`
  → **exit 0**（`1 passed, 132 deselected`；key 未进任何文件）·
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 11 项：预期红 0，绿 11，跳过 0` / `345 passed`）·
  `python3 -m pytest tests/contracts -q` → **exit 0**（`151 passed`）·
  `python3 -m pytest tests/tools -q` → **exit 0**（`49 passed, 12 skipped`）·
  `python3 -m ruff check agenerp tests/unit tests/contracts tests/routing` → **exit 0**。

### 2026-08-24 · plan `P1.1` Phase 1 —— 能力声明落地，顺手修掉 §12.3 那处确证漂移

- **交付**：`agenerp/routing/capabilities.py` 三份声明（能力枚举 4 项 / 任务类目→最低能力 4 档 /
  本仓实测过的 3 份模型档案）+ 声明自身的校验器；`agenerp/routing/errors.py` 两个失败类型
  （`DeclarationError` 是声明写错了，`RoutingError` 是运行时不满足，分开是因为该由不同的人在
  不同时刻修）。
- **判据不是「能 import」**：`tests/routing/test_capabilities.py` 把声明与
  `docs/architecture/model-management.md` §12.5 的三张 `machine-read` 表**逐行同构**判定，
  外加四类畸形声明各打红一次、`lineage` 最低能力换空集必须红、`lineage` 必须严格严于 `explain`。
  **同构判据当场做了变异实证**：把 §12.5 里 `lineage` 那行改成 `tool_calling` 单项 →
  `test_task_matrix_is_isomorphic_to_the_owner_doc` **红**；原样还原 → 18 passed。
  ⚠️ 同构管不了「两边一起写错」，这条残余风险已逐字写进 §12.5，不消除。
- **确证漂移的改准（`Fix`，不是 Follow-up）**：§12.3 结论表第二行原文「需强模型，或**由更强的
  循环门禁补偿**（见 §5.0 ①）」——P1.0 实测把这条替代路径削弱了：同模型下门禁 0/3 → 2/3
  **有效**，但**没有**让弱模型追上强模型无门禁的 3/3。改准连同 2×2 网格与
  ⚠️「只覆盖一道题、两个模型、每格 3 次有效运行，不得外推」一起写进文档。
  **被削弱的是「门禁能补偿能力」这个推论，不是门禁本身**：`agents-and-roles.md` §5.0 ①
  的规则文本一个字未动，`docs/masterplan/**` 一个字未动。
- **两个 `Decision` 的备选与否决理由进了文档，不留在 chat**：否决 `vision`/`embedding`（本期无
  消费者，声明了也无人校验）、否决把速度/价格当能力（那是选型偏好，混进来会让「不满足则失败」
  退化成「按价格排序」）；否决按 Agent 分类目（Agent 还没有）、否决只留强/快两档（那正是
  §12.3 结论 3 要推翻的一刀切）。
- **两条残余风险照实登记，不写成已校验**：① `multi_hop` 今天没有低成本的自动判定法，取值来自
  **人写的档案**；② 四档边界只从一次实验加四道探针推出来，**P1.4 后很可能重划**。
- **命令原文 + 退出码 + sha**（sha `c7086ee`）：
  `python3 -m pytest tests/routing/test_capabilities.py -q` → **exit 0**（`18 passed`）·
  `python3 -m ruff check agenerp tests/unit tests/contracts tests/routing` → **exit 0** ·
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 11 项：预期红 0，绿 11，跳过 0` / `345 passed`）。

### 2026-08-24 · plan `P1.0a` 收口：独立收口审计跑通，plan-check 从红转绿

- **卡在哪**：`P1.0a` 的 `CLOSURE_VERIFY` 连续判 FAIL，两条原因，一条是格式一条是实质。
- **格式那条（真缺陷，不是刁难）**：`plan-check.mjs` 认 `## Closure` 的正则是
  `/^#{2,4}\s+Closure\s*$/im`，本 plan 写的是编号形式 `## 12. Closure`，**匹配不上** →
  `hasClosureSection = false` → 判「completed plan missing Closure evidence」。
  按指南 `docs/plans/00-plan-authoring-and-execution-guide.md` §模板的写法改回
  `## Closure Gates` / `## Closure`（仓内其余 20 份 plan 都是这个形态），并顺手改正两处
  指向错节的交叉引用（「假实现变异自查」在 §10 不在 §11）。
- **实质那条**：唯一未打勾的 `- [ ] closure audit was independent` 是**上一轮刻意留空的**
  ——执行者与实现者是同一个，自审不算独立。**代打勾即伪造关闭证据**，所以这一轮真派了
  一个独立子代理复核，而不是把框勾上了事。审计判 **APPROVED**，六条命令原样复跑：
  `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 11 项：预期红 0，绿 11，跳过 0`）·
  `python3 -m pytest tests/unit -q` → **exit 0**（`345 passed`）·
  `python3 -m pytest tests/contracts -q` → **exit 0**（`151 passed`）·
  `python3 -m pytest tests/tools -q` → **exit 0**（`49 passed, 12 skipped`）·
  `python3 -m ruff check agenerp tests/unit tests/contracts tests/tools` → **exit 0** ·
  `node tools/mission-driver/src/plan-check.mjs <plan> --strict` → 打勾后 **exit 0**（`58/0`）。
- **审计的限制照实记**：审计环境**无站点凭据**，12 条活站点合规断言全部 skip，
  **没有观测到任何一条活站点断言真跑**。活站点那一层只在 CI 的「L2 种子链」job 里成立。
- **顺带对齐了三处已经过期的话**：`f76b07c`（人，带 `Gates-Change-Approved-By:`）已经把
  `tests/gates/test_tool_execution_live.py` 补上并接进 CI，而 plan §9 / `## Closure` 的
  Follow-up 与 roadmap 第 1 项都还写着「待人做」。三处改成实况，**上方原口径一字未改，只补记**。
  仍未做的那一条（`tests/tools` 未进 `unit-and-contracts` / `lint` 的路径）保留为 ⬜。
- **审计带回三条非阻塞旁证，已登记进 plan `## Closure`**：① `check_expected_red.py` 的 exit 0
  只覆盖 31 项里的 11 项（default 模式追加 `-m "not live"`），**不等于「全部门禁绿」**；
  ② `schema_search` 的 `returns_candidate_list_not_single_pick` 在代码里近乎恒真，
  检出能力实际在测试侧，§10 那句「已改出恒真」只对了一半；③ `Plan Status: completed` 是在
  独立审计之前置上的，次序与指南相反，本轮事后补齐使其成立。

### 2026-08-24 · plan `P1.0` Phase 3–4 —— 2×2 网格跑完了，承重命题的结论是**被削弱**，照实写

- **网格结果**（每格 3 次有效运行，判定对象只有最终答案文本）：

  | 格位 | 正确 | 不完全 | 错误 |
  |---|---|---|---|
  | `qwen-plus`（弱）· 门禁 **off** | **0** | 2 | 1 |
  | `qwen-plus`（弱）· 门禁 **on** | **2** | 1 | 0 |
  | `qwen3.6-plus`（强）· 门禁 **off** | **3** | 0 | 0 |
  | `qwen3.6-plus`（强）· 门禁 **on** | **3** | 0 | 0 |

- **承重命题「确定性门禁能否补偿模型能力不足」的处置：`被削弱`**。
  门禁的效果是真的（同模型同提示词，0/3 → 2/3，唯一变量是门禁），
  **但它没有让弱模型追上强模型无门禁的 3/3**。本题支持的最强表述到此为止，
  一个字都不再往前推。
- **H1 判 `不吻合`，而且是有意思的那种不吻合**：H1 逐字预测弱模型「不会自发地从 Bin
  反查入库来源」。实测 run-02 与 run-13 **查了**——它们把两张入库凭证逐笔列进了答案
  （run-13 逐字「2 月 5 日入库 1000 → 2 月 7 日又入库 1000（总计 2000）→ 出库 990」），
  却把根因写成「销售订单还有 10 台没发」。**失败在归因，不在取证。**
  这解释了为什么现有 L3 有效：它判的是「答案里报了库存数 → 入库来源必须逐张覆盖」，
  逼的正是「把查到的东西用上」，而不是「去查」。
- **H4 吻合，但别读成「门禁便宜」**：工具调用中位数 `qwen-plus` 5 → 10（上限 15）、
  `qwen3.6-plus` 10 → 10（上限 30），方向收敛。**token 不在判据里**：
  `qwen-plus` 25,586 → 78,915（**约 3.1 倍**），`qwen3.6-plus` 84,942 → 85,182（约 1.0 倍）。
  弱模型侧门禁的 token 代价已顶到判据给调用次数设的那条 3× 线，而判据没约束它。
- **两次无效运行照实记，没有静默丢弃**：`run-09`（`qwen-plus`/off）与 `run-10`
  （`qwen3.6-plus`/on）均 token 超限（上限 200,000），未产出答案。
  按 plan §2 补跑 `run-13` / `run-14`，两份无效轨迹**原样留在仓库**。
  同格无效最高 1 次，未触及「> 3 次即停下写 STATE §3」的上限。
- ⚠️ **盲判没做到，写在最显眼的地方**：判定者与执行者是同一个循环，且在打勾前
  已看过 `run_id → 模型/门禁` 的汇总表。`verdicts.md` 抬头与审计文件 §5.2 逐字记了这件事。
  判据本身是跑之前冻结的、逐条命中/未命中全部落盘，但「事后挪标准」这一风险
  **只被削弱，没有被消除**。
- ⚠️ **一个悬而未决的口径交人定**：判据 ③「点名两批生产中的至少一批是多余的」
  没要求机制正确。run-04 点名了外协那批多余（命中），但把机制说成「重复记账」，
  而站点上是真有两批实物。**若加严口径，H2 从 `吻合` 翻成 `不吻合`**——翻转点
  写进了审计文件 §2，本轮不自行加码。
- **判据复跑（命令原文 + 退出码）**：
  - `python3 tools/gates/check_expected_red.py` → **exit 0**，`门禁 11 项：预期红 0，绿 11，跳过 0`
  - `python3 -m pytest tests/unit -q` → **exit 0**，`345 passed`
  - `python3 -m pytest tests/experiments -q` → **exit 0**，`10 passed`
  - `python3 -m ruff check agenerp tests/unit tests/contracts tests/tools tests/experiments tools/experiments`
    → **exit 0**，`All checks passed!`
  - `AGENERP_HTTP_PORT=18080 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin python3 -m agenerp.seedsite --verify-site --site frontend`
    → **exit 0**，`站点侧对账：30 项，通过 30，失败 0`
  - 补跑两次运行的命令原文（`.env` 提供 `DASHSCOPE_*`，key 不进 git/日志）：
    `AGENERP_HTTP_PORT=18080 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin python3 -m tools.experiments.p1_entry_gate.run --model qwen-plus --gate off --max-tokens 200000 --seed-run-id run-13`
    → **exit 0**；同一条把 `--model qwen3.6-plus --gate on --seed-run-id run-14` 换上 → **exit 0**
  - sha `91bc24c`
- ⚠️ **verification scope limited**：**零 CI 轮次、零 `AGENERP_LIVE=1` 整目录判定**。
  本轮的绿是本机的绿加 14 次真实 LLM 运行；`tests/experiments/` 仍不在 CI 的任何 job 里
  （与 `tests/tools/` 同因，红线 2）。


### 2026-08-24 · plan `P1.0` Phase 2 —— 入口关口实验的最小控制循环跑起来了

- **落在 `tools/experiments/p1_entry_gate/`**，不进 `agenerp/`：这是为一次实验而建的设施，
  不承担产品的向后兼容义务。模型路由的产品形态是 P1.1。
  ⚠️ **目录名与 plan 原文差一个字符**（`p1-entry-gate` → `p1_entry_gate`）：
  带连字符的目录不是合法 Python 包名，判据 import 不到被测对象。为一个连字符
  把判据写成 `importlib` 按路径加载不值得。**执行者的就地判断，已记进 plan。**
- **门禁在两处起作用，而且是同一组 `Condition`**（`agenerp/tools_readonly.py` 的 `EVIDENCE_GATE`）：
  ① 作答类工具（`query.read` / `snapshot.read`）的**前置**——契约本来就把门禁挂在它们身上；
  ② 模型给出最终答案时的**答案面判定**——L3 在这里才判得动。
  `gate.py` 里**一条规则原文都没有**，它只负责把五条事实凑齐。
- **L3 的触发口径是「答案里的数字」，不是「模型查过 Bin」**：后者会让一个从不查 Bin、
  却照样报出 1,010 的回答绕过 L3。判据独立于模型行为——直接拿答案里的数字比对站点的
  `Bin.actual_qty`，命中就取该 `(物料, 仓库)` 的全部入库凭证。
  **残余口子照实记**：只说「积压」而不报数字的回答 escape 得掉 L3，
  因为规则原文写的就是「作答涉及某个仓库的库存**数量**」——不擅自扩大它。
- **两处就地裁定**（都写进 plan 的 Phase 2）：
  - 强制续跑消息**只说规则和缺几件，不说缺哪几件**。说出单号等于把答案递过去，
    那测到的就成了「告诉模型答案有没有用」。**代价：H2 的结果是下界**——
    若 H2 不成立，只能推出「这种强度的门禁在这道题上不够」，推不出「门禁无用」。
  - `permission.scope` **不进工具面**（9 个而非 10 个）：它的后置断言
    `injected_at_session_start` 断的是编排面的事实（P1.3 的开场自动注入），
    本设施没实现那件事，递 `True` 进去就是断言一件不成立的事。
- **冒烟跑通了，而且门禁在轨迹里真的改变了路径**（`qwen-plus` / `--gate on`）：
  第 1–3 轮 `doc.get` 订单 → `doc.links` → `doc.get` 发货单；
  **第 4 轮 `query.read` 被 L2 挡下**（`未覆盖 ['MFG-WO-2026-00001']`）；
  第 5 轮补上那张工单的 `doc.get`，随后才查库存流水、逐张取入库凭证的证，第 10 轮作答，门禁绿。
  9 次工具调用，token `prompt 61209 / completion 1348 / reasoning 0`
  （`qwen-plus` 不是推理模型，`reasoning 0` 是**正确读数**，不是没记）。
- **判据复跑（命令原文 + 退出码）**：
  - `python3 -m pytest tests/experiments -q` → **exit 0**，`10 passed`
  - `python3 -m ruff check agenerp tests/unit tests/contracts tests/tools tests/experiments tools/experiments`
    → **exit 0**，`All checks passed!`
  - sha `9fe8acb`
- ⚠️ **`tests/experiments/` 同样不在 CI 的任何 job 里**（与 `tests/tools/` 同因，红线 2）。

### 2026-08-24 · plan `P1.0a` Phase 1–4 —— 十条只读契约有执行体了，`permission.scope` 的判别力有真反例了

- **为什么这一轮跑的是 P1.0a 而不是派发给我的 P1.0**：P1.0 Phase 2 的 `Prereqs` 逐字要求
  「P1.0a 工具执行层完成」，而 P1.0 的 Non-Goals 第 3 条又逐字禁止在那个 plan 里实现执行体。
  `STATE.md` §3 的 2026-08-24T04:10Z `[open]` 行已把处置写死：**「下一轮改派 P1.0a……
  这件事 loop 可以自己做，不需人定」**。本轮按那条处置执行，**没有绕过任何一条 Non-Goal**。
- **交付了什么**（sha `5a712a7`）：
  - `agenerp/tools/runtime.py` —— **唯一执行入口**，四步序：前置不满足 → `abort_and_report`
    且**一个请求都不发** → 打站点 → 按 `returns` 裁剪（剥框架管道字段 / `max_rows` 截断 /
    `must_keep` 核对 / §7.5 数据边界标记）→ 后置断言。**③ 与 ④ 不可颠倒**：后置判的是
    **给出去的东西**，先判后裁的话被裁掉的字段参与了判定却不在返回值里。
  - `agenerp/tools/{site_scope,documents,queries}.py` —— 十个执行体，逐条照契约原文实现。
  - `agenerp/seedusers.py` —— 受限身份「车间工人」的**幂等**装载（只读 3 个 DocType）。
  - `tests/tools/` —— 四组判据：执行入口 11 · 十执行体 34 · 双向注册 4 · **活站点合规 12**。
- **一处起草时没料到的设计，照实记**：后置断言里有一类是**过程约束**
  （`permission.scope` 必须逐个调 `has_permission`、`query.read` 不得跨表拼装），
  **从返回值上看不出来**。执行入口因此多了一个 `Session`：执行体与站点之间的唯一通道，
  逐条记调用留痕，事实**从留痕上推**而不由执行体自报。
  残余弱点不消除：它验得了「调没调」，验不了「每次调用的参数语义都对」。
- **判别力有实据了**（STATE §3 那条 2026-08-24T03:18Z `[open]` 就此收口）：
  以 `worker@hrd.example.com` 跑 `permission.scope`，候选 6 个 → **可读 3
  （`Work Order` / `Stock Entry` / `Item`）、不可读 3（`Sales Order` / `GL Entry` / `Account`）**。
- **三条实测限制**（已写进 `docs/architecture/module-boundaries.md` §7.6）：
  - **受限身份枚举不出 DocType 清单**：stock Frappe 只把 `DocType` 的读权限给
    System Manager / Administrator（实读它的 `DocPerm` 只有这两条），且对它建
    `Custom DocPerm` **不生效**（授了 read 之后 `has_permission("DocType")` 仍为 `False`）。
    → `permission.scope` 的候选集**可由调用方给**。**没有靠给工人发 System Manager 绕过去**
    ——那等于把「受限」这件事取消掉，判别力也就没了。
  - **REST 面没有批量计数端点**：「只回有数据的 DocType」要对每个候选调一次
    `frappe.client.get_count`，本站点 **239 次、约 2 秒**。虚拟 DocType 必须排掉——
    它们没有物理表，计数回 `{}`（无 `message`）而不是 0（实测 `Bulk Transaction Log`）。
  - **`Bulk Transaction Log` 那个 `{}`**：不排掉的话，一个纯粹的结构事实会被读成「站点答不上话」。
- **假实现变异自查（七个，全部被打红）**：永远回 true 的 `permission.scope`（2 failed）·
  不剥框架字段的裁剪（1）· 不套数据边界标记（3）· 只留已提交的 `doc.links`（2）·
  不回溯父单据的 `lineage.trace`（3）· 只挑第一名的 `schema.search`（1）·
  不逐个探的 `permission.scope`（3）。还原后复跑全绿。
  **自查当场加强过一条断言**：`schema.search` 的 `returns_candidate_list_not_single_pick`
  起初写成恒真表达式，在「只挑第一名」下会照样绿。
- **一次被判据挡下来的分层错误，照实记**：`agenerp/seedusers.py` 初版直接读环境变量取口令，
  被 `tests/unit/test_seed_deterministic.py` 判红（`agenerp/seed*.py` 整片被当「生成路径」扫）。
  **没有改判据**，而是把凭据读取挪进 `agenerp/site.py` 的 `credential_from_env`
  ——那本来就是 §11.7 里凭据该待的地方。
- **判据复跑（命令原文 + 退出码）**：
  - `python3 tools/gates/check_expected_red.py` → **exit 0**，`门禁 11 项：预期红 0，绿 11，跳过 0`
  - `python3 -m pytest tests/unit -q` → **exit 0**，`345 passed`
  - `python3 -m pytest tests/contracts -q` → **exit 0**，`151 passed`
  - `python3 -m pytest tests/tools -q` → **exit 0**，`49 passed, 12 skipped`（skip 理由逐条打印）
  - `AGENERP_HTTP_PORT=18080 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin AGENERP_WORKER_PASSWORD=… python3 -m pytest tests/tools -q`
    → **exit 0**，`61 passed`
  - `python3 -m ruff check agenerp tests/unit tests/contracts tests/tools` → **exit 0**，`All checks passed!`
  - `AGENERP_HTTP_PORT=18080 … python3 -m agenerp.seedsite --verify-site --site frontend`
    → **exit 0**，`站点侧对账：30 项，通过 30，失败 0`
  - `AGENERP_HTTP_PORT=18080 … AGENERP_WORKER_PASSWORD=… python3 -m agenerp.seedusers --load-users --site frontend`
    → **exit 0**，`合计：新建 0 / 已存在 5`（原样复跑一致）
- ⚠️ **verification scope limited**：`tests/tools/` **不在 CI 的任何 job 里**
  （`unit-and-contracts` 与 `lint` 的路径都不含它）。接进去要改 `.github/workflows/**`（红线 2），
  只能由人做。本轮的绿是**本机的绿**：零 CI 轮次、零 `AGENERP_LIVE=1` 整目录判定。
- ⚠️ **WBS §4 第 78 行的 🔴 判据只满足了一半**：`tests/gates/test_tool_execution_live.py`
  **本轮没有创建**（红线 1）。同等强度的断言在 `tests/tools/test_live_conformance.py`，
  提升与接线是**人的动作**，已挂 STATE §3 needs-human 队列。
- ⚠️ **本机站点上留了两条探路残余**：探路阶段给「车间工人」建过 `DocType` 与 `Module Def`
  的 `Custom DocPerm`（前者对 Frappe **不生效**，后者生效但装载器不建它）。
  装载器只建三条业务 DocType 的读权限，**全新栈上不会有这两条**；判据不依赖它们。

### 2026-08-24 · plan `P1.0` Phase 1 —— owner-doc 漂移消除，证据门禁 L3 的出处不再指向一个不存在的条款

- **改的是什么**：`docs/design/agents-and-roles.md` §5.0 ①（解释 Agent 的证据充分性门禁）
  与 §9（风险分级）。四件事按 plan 的 Phase 1 Fix 项逐条落地，见 sha `f66f3e9`。
- **① 移除 `LOSS-00003` 案例**。它是 XM 自建的 custom DocType，已按 D-9 退役。
  **L1/L2 的措辞一个字未动**——失效的是推出它们的案例，不是规则。原句「那 10 米是
  `LOSS-00003` 已审批的合理损耗」整段删掉，改写成失败**形状**（停在第一层证据上 /
  看到了下游单却没打开），并加一条注记说明 Spike 02/08 跑在退役前的 XM 数据集上、
  数量骨架 1,000/990/1,010 原样保留、单位随样板公司改「台」。
  ⚠️ 原文的「188 元未开票」是 XM 价格量级专属的数字，**直接去掉，没有另编一个 HRD
  价格的替身**——编一个就是把实测记录改写成没跑过的东西。
- **② 把证据门禁 L3 写进该节**。此前 `EVIDENCE_GATE_L3.source` 逐字指向
  「`agents-and-roles.md` §5.0 ① 规则 L3」，而**该节根本没有 L3**——代码引用了一个
  不存在的条款。现三条规则的 `text` 在该节逐字可查，已机械核对：
  `EVIDENCE_GATE_L1/L2/L3.text` 与文中 `规则 Lx：…` 三行**逐字相等**（`in doc` 全 True）。
- **③ 单位「米」→「台」**：§5 表里洞察 Agent 的例句 + §5.0 ② 的正文与表头两处。
  `grep -n "LOSS-00003" docs/design/agents-and-roles.md` → **无输出（exit 1）**；
  余下两处「米」是解释单位改换来历的注记本身，不是旧口径。
- **④ 与风险分级的 L3 消歧**。§5.0 ① 与 §9 各加一条「命名消歧」注记：前者是
  **证据门禁 L1/L2/L3**（决定什么时候允许停下来），后者是**风险档 L0–L3**（决定
  要不要人批）；§9 的「L3 中的 Server Script 特别处置」改为「**风险档 L3** 中的…」。
  ⚠️ **plan 正文把风险分级记作「§6」，实读在 §9**（§6 是角色 × Agent 矩阵）——
  按实读处置，不按 plan 的行号处置。
- **Exit Criteria 的活站点那一条已实测**（从 `SAL-ORD-2026-00001` 出发能看到什么）：
  站点上指向 `Sales Order` 的 Link 字段共 **18 个，其中 14 个在子表**；对 `SAL-ORD-2026-00001`
  真正命中的只有两条，子表命中已回溯父单据 ——
  - `Delivery Note MAT-DN-2026-00001` ← `Delivery Note Item.against_sales_order`
  - `Work Order MFG-WO-2026-00001` ← `Work Order.sales_order`（T0 `3992737` 补送的字段）
  - **外协侧仍不可见，且是结构性的**：`Subcontracting Order` / `Subcontracting Receipt`
    上指向 `Sales Order` 的 Link 字段**各 0 个**（站点各 1 张单：`SC-ORD-2026-00001` /
    `MAT-SCR-2026-00001`）。**这正是实验要的形状，不是缺陷，不要去修它。**
- **判据复跑（命令原文 + 退出码）**：
  - `python3 -m pytest tests/unit/test_evidence_gate_l3.py -q` → **exit 0**，`5 passed`
  - `python3 -m pytest tests/unit -q` → **exit 0**，`340 passed`
  - `python3 -m pytest tests/contracts -q` → **exit 0**，`151 passed`
  - `python3 tools/gates/check_expected_red.py` → **exit 0**，`门禁 11 项：预期红 0，绿 11，跳过 0`
  - `AGENERP_HTTP_PORT=18080 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin python3 -m agenerp.seedsite --verify-site --site frontend`
    → **exit 0**，`站点侧对账：30 项，通过 30，失败 0`
  - `bash tools/check-state-consistency.sh` → **exit 0** · `bash tools/check-masterplan-links.sh` → **exit 0**
  - `node tools/check-doc-references.mjs` → **exit 1**。⚠️ **照实记**：53 行报告里
    `agents-and-roles` **零命中**，全部是本次改动之前既有的 line-ref 债（`system-baseline.md` /
    `module-boundaries.md`）。**不是本次引入，也没有被本次修好。**
- **本轮的提交 sha（STATE §2 那条「该提交 sha 见 `docs/logs/`」指的就是这两个）**：
  - `f66f3e9` —— owner doc 重述（`docs/design/agents-and-roles.md`）
  - `053252f` —— Phase 1 收口落账（本日志 + STATE 纯追加 12 行 + plan 打勾）
  - 另有 `6466639` —— **与本项无关**：P1.0a 方案的起草评审改动，本轮开工前就躺在
    工作区未提交，单独一个提交入库，不混进 `053252f`
- ⚠️ **scoped verification**：本轮零 CI 轮次、零 `AGENERP_LIVE=1` 整目录判定，
  只在本机默认判定面 + 一次活站点只读对账上跑过。**不得据此宣称任何 CI 侧结论。**

### 2026-08-24 · plan `P1.0` Phase 2–4 —— 被自己写死的前置卡住，未开工

- **Phase 2 的 `Prereqs` 逐字写着「P1.0a 工具执行层完成」**，而 `docs/plans/p1-insight/2026-08-24-P1.0a-tool-execution-layer.md`
  四个 Phase **全部为 `planned`**，`agenerp/tools/` 与 `tests/tools/` 两个目录都不存在。
- **不能绕**：P1.0 的 Non-Goals 第 3 条逐字写着「**不**实现 10 个工具的执行体（那是 **P1.0a**）」。
  在控制循环里顺手写十个执行体 = 在一个实验设施的 plan 里交付契约层的 B 半，
  既违反本 plan 自己的 Non-Goals，也绕过 P1.0a 的独立收口审计。
- **Phase 3 / Phase 4 顺次被 Phase 2 挡住**（各自的 `Prereqs` 是前一个 Phase）。
- **处置**：Phase 1 打勾转 `completed`，Phase 2–4 保持 `planned` 未开工；本轮**不**把
  `Plan Status` 改成 `completed`。执行顺序的账在 `docs/backlog/p1-insight-roadmap.md`：
  P1.0a 是第 1 项、P1.0 是第 2 项，**roadmap 的顺序是对的，这轮派发的顺序反了**。
