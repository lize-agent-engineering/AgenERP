# Development Log - 2026-08-27

### 2026-08-27 · P2.7 术语层落地：317/317 有中文名，表头已换（分支 `p2-views`）

- **`python3 -m pytest tests/i18n -q` → 退出码 0，12 passed**（WBS §5 那条验收）。
  活体 `pytest -m live tests/render -q` 仍退 0（**16 passed**，表头已是中文）。
  **API 成本 = 0**，全程本地 Ollama。
- **基线是零不是少**：整站 6,350 个业务字段，label 含中文的 = **0**。
  `Item Reorder.warehouse` 的 label 是「Request for」—— 字段名说仓库、标签说请求给。
- T1 覆盖 **317/317 = 100%** · T2 含中文 **100%** · T3「不该错」**15/15**。
- **🔴 中途换过一次模型，而换模型是判定表指的不是我挑的**：
  第一版 `qwen3.5:0.8b`，**T1/T2/T3 全过**，而术语层里躺着实打实的错 ——
  `Item.has_batch_no`（勾选框）→「**批次号**」、`Item.item_code` →「**项目代码**」。
  **T3 全中却漏掉了它们**，因为「项目代码」含「代码」，我的关键词放行了。
  ⇒ **判据绿 ≠ 判据在测它名字说的那件事，这次发生在我自己刚写的判据上。**
  处置是**补判据不是改判据**：`Check` 是规则能覆盖的一整类，补了「Check 字段要读成
  是非说法」，当场红 —— **52 个 Check 字段里只有 38.5% 读成了是非问句**。
  往 prompt 补两条领域事实后 **0.8b 跟不动、T3 反而掉了** ⇒ 命中事先写死的
  「T3 < 13/15 ⇒ 换模型」那一格。换 `qwen3:14b`（仍本地 $0）后修正为
  「是否批次管理」「物料编码」「目标仓库」「默认费用科目」。
- **另两处执行期修的，都修在生成器一侧，判据一个字没动**：
  ① 覆盖率卡在 218/317 —— 小模型把提示里的分隔符抄回来（`是否成品 | Is Finished Item`），
     **中文答案本身是对的** ⇒ 解析问题不是质量问题；
  ② 20/317 条带单据名前缀（`工单工序.工作站`）—— 同样先补判据再修生成器。
  还有一处**选重试而不是加剥离规则**：剩下的失败是「中文 English」用空格分隔，
  按 ASCII 切会误伤「BOM 编号」，而本地重试成本≈0、误伤不可见。
- **变异 8 条**：删术语 / 换英文 / Check 起成名词 / 指不回真实字段 / 带前缀 /
  抄 fieldname / 拿掉渲染面的术语层 —— **7 条见血**。
  ⚠️ **M6（把 `Item.item_code` 改成「东西」）没见血** —— T3 阈值 `≥13/15`
  **刻意留了 2 条容错**。补了 M6b 把边界量出来：**错 1 条绿、错 2 条绿、错 3 条红**。
  **这里不事后收紧阈值**（看到结果再改阈值就是调判据），把容错量出来写进纪要。
- **两条既有判据因为术语层生效而红，红得正确**：原来断言表头含 `item_code` / `conversion_factor`
  （英文 fieldname），现在表头是中文。已改成断言「物料编码」「换算系数」并记明原因。
- **P2.0R 的 bonus 这一轮量不了**：术语层范围是路线 C 的 317 个字段，
  而 P2.0R 评测集打的是整站 6,350 个字段里的目标字段，两者几乎不重叠。转入 §11。
- **红线自证**（`vs 012385a`）：`git diff --name-only 012385a -- tests/gates/ .github/workflows/ missions/ docs/masterplan/`
  → **无输出**。**未碰 `main`。**

### 2026-08-27 · P2.2 渲染器落地：车间工人落回 = 0（分支 `p2-views`）

- **`python3 -m pytest -m live tests/render -q` → 退出码 0，14 passed**（WBS §5 那条验收）。
  真站点 · 真权限（车间工人的真会话）· 真浏览器 · 真渲染器。
- **人 2026-08-27 选定路线 C（按角色做到 100%）**。可达性是**算出来的**不是推的：
  工人覆盖面 15 张表 / 317 字段，动工前已覆盖 **94.3%**，**只缺 4 种 fieldtype、18 个字段**。
  三个日常视图的落回 **3 → 0**（R1 落在 `957ac06`，实现之前，git 可查）。
- **R3/R4/R5 全中**：`<img src=x onerror=…>` 喂进去 **不执行**；
  `javascript:`/`data:`/`vbscript:`/`//host` 四种 URL **全部不产出可点击元素**，
  而 `/files/…` 照画（白名单不是把功能关掉）；工人打 `Sales Order` → **后端回 403**，
  前端只显示「你看不到这个」，一行数据都没编。
- **🔴 活体判据抓到两件我没主动发现的事**：
  ① **`detail` 块的子表永远是空的** —— Frappe 的**列表接口从不返回子表**，
     而我的子表展开是从列表响应里取的。改成先取一条 `name` 再打单据详情接口。
     **这是「渲染出来 ≠ 渲染对了」的活标本**：表画出来了，只是永远空着。
  ② **变异 M6 第一轮没见血** —— 把落回卡片整段改成静默丢弃，**12 条判据一条都没红**。
     根因刺眼：**路线 C 把落回压到 0 ⇒ 落回渲染那条路在活体里一次都没走过**。
     ⇒ **「落回 = 0」不等于「落回画对了」，我把两件事当成了一件。**
     已补两条判据（用真的会落回的探针视图，计划仍由真 `plan_payload()` 算），补完 M6 见血。
- **变异六条全见血**（D-26）：拿掉 URL 白名单 → 4 红；富文本不剥标签 → 1 红；
  `//host` 当站内 → 1 红；`detail` 改回用列表响应 → 6 红；后端拒绝时静默留白 → 1 红；
  落回卡片静默丢弃 → 1 红（补判据后）。**复原后 `render.js`/`fallback.py`/`app.py`
  三个 `sha256` 与施加前逐字节相同**，复原后重跑退 0。
- **⚠️ 与 WBS 字面偏离，照实记**：WBS 写「渲染器（**frappe-ui**）」，实际用零构建 vanilla JS。
  理由：仓内没有前端构建链（无 `package.json`），引入会牵动零依赖启动门禁而**那条在红线 1 内我改不了**；
  且路线 C 之后覆盖面只有 4 种 fieldtype + 3 种块，不是组件库量级的问题。
  **可逆性也写了**：DSL 与落回判定都与 UI 库无关。**这是我自己的判断，人只裁了路线 C。**
- **⚠️ 活体没覆盖到的两处，不掩盖**：`/agenerp/view` 的**路由接线**与 `/agenerp/app` 壳页
  经 nginx 发出来这件事。原因：活栈的 `agenerp-serve` 挂的是**主工作树**的 `agenerp/`
  （`docker inspect` 实读），而**往主工作树写会污染那儿正在跑的循环**。
  ⇒ 用 `page.route` 兜住那一跳，**但填的 JSON 是真 `view_plan()` 算的**，只桩掉 HTTP 传输。
- **两处执行期订正**：① 守「严格模式」那条原本 `pytest.raises(Exception)` ——
  `pytest.fail` 抛的 `Failed` 继承自 **`BaseException`**，捕不到；
  ② `Stock Entry Detail.image` 是 **`Attach` 不是 `Attach Image`**，渲染成 `<a>` 不是 `<img>`
  —— **红在我的假设上，不在实现上**。
- **站点侧**：跑了 `python3 -m agenerp.seedusers --site frontend --load-users`
  （Role 1 / User 1 / Custom DocPerm 3，**只建不改，不写任何业务数据**）——
  这是 plan §6 列的前置，之前站点上这个身份根本不存在。
- **成本**：**零 LLM 调用**（本项不碰 `explain`）。
- **全量**：`pytest -m "not live"` → **1415 passed**，error 23 → 35。
  多出的 12 条是新增活体断言体，**与既有 `test_desk_sidebar_body.py`(11) 同一形态**，非新问题。
- **红线自证**（`vs 012385a`）：`git diff --name-only 012385a -- tests/gates/ .github/workflows/ missions/ docs/masterplan/`
  → **无输出**；`tools/nginx/` 一个字未动。**未碰 `main`。**

### 2026-08-27 · P2.0R 第三轮：⛔ 分数融合也被推翻，但看清了为什么（分支 `p2-views`）

- **开工授权来自人 2026-08-26** —— 第二轮的判定表逐字是「不得选路」，
  这一轮不是我自己从判定表里读出来的。预测在 `9ff97bf` 落盘。
- **F1 设施自检过**：α=1.0 的 Top-5 **恰好等于基线 65.0%**。
- **⛔ 最好一档 Top-5 = 65.0%，与基线一模一样，融合一分不加。** F2/F3/F4 三格全未中
  （F4 甚至比基线**多**错一条：12 vs 11）。按判定表「≤ 65% ⇒ 分数融合被推翻」。
- **全部 11 档 α 的曲线都报了**（plan §2.1 第 1 条事先写死：只报最优值 =
  把一次网格搜索包装成一个发现）。曲线**单调**：族信号加得越重越差，65.0 → 42.5。
- **🔴 `Top-1` 在整条曲线上纹丝不动，恒为 30.0 —— 这一格解释了全部。**
  `famscore` 取族内 max，对族里分数最高的字段而言**族分就等于它自己的分**
  ⇒ **融合从未改变过谁是第一名**，只是把同族其他字段抬上来挤占名额。
- **三轮之后的那句话**：第二轮的族聚合、第三轮的分数融合，**都是拿同一批 embedding
  再算一遍 —— 从同一口井里再打一次水**。第一轮 oracle 能跳 35 个点，是因为它
  从外面注入了一条这批向量里没有的信息（正确答案在哪张单上）。
  **任何不注入新信息的重排，结构上都够不着那 35 个点。**
  ⚠️ 这是观察不是选路（D-16）：剩下的候选（术语层 · 行业包约束）都会引入新信息，未被触动。
- **判据草稿已交人**（人 2026-08-26 裁定「你来写」）：
  `tools/experiments/p2_schema_retrieval/GATE-DRAFT-test_schema_retrieval_recall.py.txt`。
  **后缀刻意是 `.txt`、路径刻意不在 `tests/gates/` 下** —— 红线 1，我不碰那个目录。
  草稿头部写了四条落之前必读的实测教训，其中第 ④ 条最重：
  **评测集不该长期是我出的那 40 条** —— 我已在同一份上跑了三轮、还看过每轮的 miss，
  要它当裁判，至少得有一份不是我出的。
- **成本**：零 LLM API 调用。字段向量已落盘缓存（`/tmp/p2_vectors.json`，带
  `texts_sha` 与模型名校验），后续轮次不必再等 145 秒。
- **红线自证**（`vs 012385a`）：`git diff --name-only 012385a -- tests/gates/ .github/workflows/ missions/ docs/masterplan/`
  → **无输出**；`agenerp/**` 本轮一字未动（只测量不改造）。**未碰 `main`。**
