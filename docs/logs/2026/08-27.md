# Development Log - 2026-08-27

### 2026-08-27 · P2.0R 第三轮：⛔ 分数融合也被推翻，但看清了为什么（分支 `p2-views`）

- **开工授权来自人 2026-08-26** —— 第二轮的判定表逐字是「不得选路」，
  这一轮不是我自己从判定表里读出来的。预测在 `9ff97bf` 落盘。
- **F1 设施自检过**：α=1.0 的 Top-5 **恰好等于基线 65.0%**。
- **⛔ 最好一档 Top-5 = 65.0%，与基线一模一样，融合一分不加。** F2/F3/F4 三格全未中
  （F4 甚至比基线**多**错一条：12 vs 11）。按判定表「≤ 65% ⇒ 分数融合被推翻」。
- **全部 11 档 α 的曲线都报了**（plan §2.1 第 1 条事先写死：只报最优值 =
  把一次网格搜索包装成一个发现）。曲线**单调**：族信号加得越重越差，65.0 → 42.5。
- **🔴 `Top-1` 在整条曲线上纹丝不动，恒为 30.0 —— 这一格解释了全部。**
  `famscore` 取族内 max，对族里分数最高的字段而言**族分就等于它自己的分**
  ⇒ **融合从未改变过谁是第一名**，只是把同族其他字段抬上来挤占名额。
- **三轮之后的那句话**：第二轮的族聚合、第三轮的分数融合，**都是拿同一批 embedding
  再算一遍 —— 从同一口井里再打一次水**。第一轮 oracle 能跳 35 个点，是因为它
  从外面注入了一条这批向量里没有的信息（正确答案在哪张单上）。
  **任何不注入新信息的重排，结构上都够不着那 35 个点。**
  ⚠️ 这是观察不是选路（D-16）：剩下的候选（术语层 · 行业包约束）都会引入新信息，未被触动。
- **判据草稿已交人**（人 2026-08-26 裁定「你来写」）：
  `tools/experiments/p2_schema_retrieval/GATE-DRAFT-test_schema_retrieval_recall.py.txt`。
  **后缀刻意是 `.txt`、路径刻意不在 `tests/gates/` 下** —— 红线 1，我不碰那个目录。
  草稿头部写了四条落之前必读的实测教训，其中第 ④ 条最重：
  **评测集不该长期是我出的那 40 条** —— 我已在同一份上跑了三轮、还看过每轮的 miss，
  要它当裁判，至少得有一份不是我出的。
- **成本**：零 LLM API 调用。字段向量已落盘缓存（`/tmp/p2_vectors.json`，带
  `texts_sha` 与模型名校验），后续轮次不必再等 145 秒。
- **红线自证**（`vs 012385a`）：`git diff --name-only 012385a -- tests/gates/ .github/workflows/ missions/ docs/masterplan/`
  → **无输出**；`agenerp/**` 本轮一字未动（只测量不改造）。**未碰 `main`。**
