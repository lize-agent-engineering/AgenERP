# Development Log - 2026-08-26

### 2026-08-26 · mission-driver 验证/提交步：full-green verification + 起草步遗留的 `deferred` plan `2026-08-26-2213-2` 落盘

**本步是 `MISSION_DRIVER:2026-08-26-205222-mission-driver` 的验证/提交步。** 进场时 `git status --porcelain` 只有一行——
未跟踪文件 `docs/plans/p1-insight/2026-08-26-2213-2-case-ledger-marker-drift.md`（506 行，起草步的产物）。
**本步不执行它、不改它一个字、不给它派工**，只做验证与把它落进版本库，免得这份判词随工作区丢失。

- **本步自己复跑的 verification（命令原文 + 退出码）**：
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`判定模式：default` · `门禁 29 项：预期红 0，绿 29，跳过 0` · `✅ 与预期红名单完全一致` ·
  `847 passed, 17 skipped in 13.69s`）。**full-green verification**。
  进场 HEAD = `dfde59c`。
- **`tools/gates/expected-red.txt` 本轮零改动**：`grep -v '^\s*#' | grep -c .` → **0**，名单本就为空（29 行全是注释），
  没有「该划掉的行」；名单只能变短这条约束本轮空转。
- **`847` 与上一条日志记的 `839` 不同值**：中间落了 `1d9b196`（`tests/unit` 新增 ci-coverage 双向同构判据）
  与 `a52a5d7` 的解析修补，**是这几条把数抬上去的，不是抖动**。
- **plan `2026-08-26-2213-2` 的状态照抄不动**：`> Plan Status: deferred` · `> Work Item: 无合法归属`——
  该 plan 自述其归属被独立评审第 1 轮 BL-4 推翻，且来源 `STATE.md` 的原话是「**由人指派**」。
  **谁来指派、指不指派，是人的事**，本步不代判、不代挂工作项、不动 `docs/backlog/`。
- **红线自证**：`git diff HEAD --name-only -- tests/gates/ .github/workflows/ docs/masterplan/ missions/` → **无输出**；
  本轮改动面仅「新增 `docs/plans/p1-insight/2026-08-26-2213-2-*.md`」+ 本条日志两个文件，零删除、零改写。
- ⚠️ **verification scope limited**：只跑 mission 配置里那一条命令 —— ruff / mypy / docker 三格仍是空的（P0 交付物未到），
  未起 docker 栈、未跑任何 `-m live` 门禁、未跑 `tests/tools` / `tests/routing` / `tests/contracts` / `tests/context` / `tests/ui`、
  **未经 CI 服务端复跑**。

### 2026-08-26 · §7.7 那句「CI 复跑不到本层判据」实测与仓库相反 —— 把「哪些测试目录被谁复跑得到」钉成单一真相源（plan `2026-08-26-2213-1`）

同一个病的第二例（第一例是同日的 `2026-08-26-2101-1`）：人在 `b0ad632`（2026-08-24）
把 `tests/tools` / `tests/routing` / `tests/context` / `tests/experiments` 接进 CI、
在 `f795e47`（2026-08-26）把 `tests/ui` 接进去，而 owner doc 里写着「它不在任何 job 的作用域里」的登记文字
**一处都没跟上**。CI 侧早有一条防漏接的元判据（`gates.yml:628` 第 ⑦ 步），**owner doc 侧一条都没有**。

- **Phase 1 · 先实测判定面，再落表**：四张来源逐条读出来（五条命令全是「读」，`git diff --name-only -- .github/ missions/` 无输出）——
  ① `ls -d tests/*/` → **九个目录** · ② `unit-and-contracts`（job 键 `:586`）里 **六条裸目录 `pytest` 步骤**
  （`:604 :607 :615 :618 :621 :624`），**`if:` / `continue-on-error:` 在 `586–642` 区间零命中**
  （对照：同一份文件 `:211` 与 `:367-368` 两种写法都实际存在 ⇒ 「本仓不用这两个词」不是理由）·
  ③ `lint`（job 键 `:663`）的 `run:` 行 `:682` 逐字**八个目录**（七个 `tests/` + `agenerp`）·
  ④ `commands.test` 逐字**只有 `tests/unit`** ⇒ **`GATE_VERIFY` 确实复跑不到其余八个，这一半是真的，没被一并改掉** ·
  ⑤ `:631` 的 `COVERED` **九项、与实扫逐字相等** ⇒ 今天推送第 ⑦ 步**不红**。
- **纳管口径裁定 = 目录，不是文件**（否决「按文件」：表会退化成全量测试清单；否决「只纳管有缺口的」：
  新增目录忘登记不会红，登记表没理由比 CI 松）。残余风险照实记：`tests/fixtures` 是数据目录、没有 `def test_*`，
  它在表里占一行是**口径的结果，不是缺口**。
- **落点**：`module-boundaries.md` **新增 §7.26**（文末，**§7.25 及其之前一个字未改** ——
  `git diff --numstat` 删除列 **0**），含成对 `<!-- machine-read: ci-coverage -->` 标记、九行六列表，
  外加三节散文：第 2 列为何写「**本 job 里**不跑」（`tests/ui` 在 `gates-l2-live` `:337`、
  `tests/gates` 在 `gates-l2` `:208` / `gates-l2-seed` `:561`，**少了这句就会复刻「低估已有覆盖」那个病**）·
  **这张表不管什么**（不管 live 面三个 job 在测什么、不管 `check_expected_red.py` 的判定域、不管条数、不管断言质量）·
  **它是单一真相源，今天仍在别处重复登记同一事实的六处逐处点名并说明归谁**。
- **`:4270`（§7.23.1 探针 `H1`）只加一句时点限定，不改写内容** —— 它是**带日期的探针快照**，
  按本仓惯例是追加式账本。不加这句，「单一真相源」会在同一份 owner doc 里被自己的副本当场推翻。
- **Phase 2 · 判据 `tests/unit/test_ci_coverage_registration.py`**（**七条断言、八个 `def test_`** ——
  ② 的两个方向按要求分开写死）。落 `tests/unit/` 是有代价的选择：它是今天**唯一**同时进
  `commands.test` 与 CI 的目录 ⇒ **人正当改 CI 之后本条会红在 `GATE_VERIFY` 上**。
  这是已知代价不是遗漏，缓解只有一条 —— **每条失败都逐字打印「表说什么 · 仓里实际是什么 · 该改哪个文件的哪一列」**；
  **不接受「把判据挪出 `commands.test` 以免拖红」**（那是用降低判别力换绿）。
  - ① 目录集合**三向**同构（表 == 实扫 == `COVERED`）· ②a 表→CI · ②b CI→表 · ③ 只认 `lint` 的 `run:` 行
    （**不许匹配 `name:` 行** —— 它逐字是 `ruff check（agenerp + tests/ 全部非门禁目录）`，首匹配解析器会读出 `tests/`）·
    ④ 读 `commands.test` 里**全部**目录参数（不是只读第一个）· ⑤ 存活守卫（其余六条各自对空表短路，
    使「整表删空」**只红在 ⑤ 上**）· ⑥ 第 6 列日期可解析 + 证据路径存在（**钉不住新鲜度**，照实声明）·
    ⑦ 步骤未被 `if:` / `continue-on-error:` 悄悄关掉，job 本身也不带 `if:`（⑦ **跳过**「表里点名而步骤查不到」的目录，
    那种情形归 ②a 报）。
  - 解析器是**纯文本行扫描，不 import `yaml`** —— 理由是 CI 的实装面：`gates.yml:601` 逐字
    `pip install pytest certifi`，**runner 上没有 `yaml`**，import 它会让 `unit-and-contracts` 当场 `error`。
  - 两条形状约束：**认 job 边界** + **目录参数必须是裸目录**（否则 `:208` 的
    `pytest tests/gates/test_zero_dep_boot.py` 会把 `gates` 算成一步）。
  - **省掉了先例的 `_REGISTERED_FILES` 纳管常量，并在文件头写明为什么省得掉**：本条的 `B` 三路
    （实扫 / `gates.yml` 实读 / `missions` 实读）**都不由表导出** ⇒ `A == B == ∅` 按构造不成立。
    **存活守卫本身没有省。**
- **Phase 3 · 变异自查 N1–N10 全部在 `/tmp` 的整仓副本上施加，活仓工作树零改动**
  （每条施加后丢弃整份副本重新拷贝，比逐条复原更难出错）。**十条与预测吻合**，含两条 must-stay-green
  （N8 在别的 job 里插一条裸目录步骤 · N10 在本 job 里插一条**文件路径**步骤）**真的保持绿**，
  且**各自的约束一去掉就转红**（N7b 去 job 边界→转绿 · N8b 去 job 边界→红 · N10b 去裸目录约束→红）
  ⇒ 「两条形状约束互为替身」这件事在本条上**不成立**。N9 三个变体（`continue-on-error: true` /
  `if: false` / job 级 `if:`）各红一次，全部单点红在 ⑦。
- 🔴 **N3 首跑与预测不符，照实记**：预测「整表删空 → 仅断言⑤红」，实测 **`8 failed`** ——
  **解析器只用了成对标记的起始那一半**，表删空后往下读到 §7.26.2 那张四列表，先红在「行必须六列」上，
  **存活守卫从未被触发**。⚠️ **成对标记本来就是为这件事加的。** 当场补「在闭合标记处截断」+
  一条「闭合标记缺失」断言（`a52a5d7`），复跑 N3 → `1 failed`（仅 `test_05`），全表重跑无回归。
  **本轮没有出现「打不红」的变异**；另有三处「红得比预测多」（N1 多红 ②b③ · N8b/N10b 多红 ②a），
  成因逐条写进证据 §3.2 —— **判据比预测更严，不是更松**。
- **改准落地，两种口径分开、不互相套用**（判断依据：**该段是不是带时点的账本**）：
  - **断言性散文 → 就地删被证伪的从句 + 加指针，重述零个事实**：§7.7 `:485-488` · §12.5 七行 ·
    §7.23.6 五条 · `project-context.md:52`。判「真」的逐字保留 —— §12.5 的 `:374`/`:379`/`:385` 三句、
    §7.7 的「不得因为本层测试自己是绿的就说『已被门禁覆盖』」、§7.23.6 的「六件全落在 workflows 里 ⇒ 红线 2，归人」。
  - **带时点的账本 / 交付记录 / 探针快照 → 一个字不改写，只加时点限定 + 指针**：`:4270` 探针 `H1` ·
    `system-baseline.md` §14.7 / §14.10（**删除列 0**）· `roadmap:41` · `roadmap:108-109` ·
    `tools-dir-has-no-static-check-coverage.md`（补扫查出）。**roadmap 三行已有字零改动，`git diff` 逐字核过。**
- ⚠️ **`missions/**` 不在红线内，本轮一并改准了两处把它写成红线的话**（§7.7 `:488` 与 §12.5 `:378` 同句式）：
  `AGENTS.md` 七条红线实读无它，禁令出处是 `ai-autonomy-policy.md:87` 的 Protected Areas（标 `blocked`）。
  **`.github/workflows/**` 那半也已由 `b0ad632` 接完，一并删掉。**
- **补扫（十二关键词 × 四个目录，170 行命中）逐条判词记进证据 §5**，三分口径 `成立` / `行号漂移` / `已过期`，
  **判词是集合不是单选，只有含 `已过期` 的才进改准范围**。判 `行号漂移` 留在原地
  （`module-boundaries.md:4417` 引的 `gates.yml:567` → `:601`；`leak-env:37` 引的 `:570-584` → `:603-624`）。
  ⚠️ **它仍然是关键词扫描、已经漏过两次，不许写成「扫完就没有了」**（边界见 plan 的 `D4`）。
- **归属取舍连同两轮相反判读一并写进 `STATE.md` §3**（纯追加，删除列 0），含**一键推翻方式**：
  人若不认同「一个 plan 可以给别的工作项的落点节加纯指针」，`git revert` 掉 §12.5 那一处 hunk 即可。
- **收尾复跑（与起草期 B5 逐条同一条命令）**：`check_expected_red.py` → **exit 0**
  （`门禁 29 项：预期红 0，绿 29，跳过 0`，**本 plan 不新增门禁**）· `pytest tests/unit tests/tools -q` → **exit 0**
  （`928 passed, 29 skipped`，基线 `920` 只增不减）· `pytest tests/contracts tests/routing tests/context -q` → **exit 0**
  （`386 passed, 1 skipped`）· `ruff check …` → **exit 0**。
- ⚠️ **verification scope limited**：未跑整仓 `pytest tests -q -m "not live"`（已知基线即红）·
  未起 docker 栈 · 未跑任何 `-m live` · **未经 CI 服务端复跑**。
  ⚠️ **独立收口审计由收口审计步执行，本轮执行者不代跑、不代批、不预填结论。**

### 2026-08-26 · F8 的登记面与它的处置对不上 —— §12.5 那句「没有任何判据拦得住」实测已假一年半个月（plan `2026-08-26-2101-1`）

处置早就做完了（人 `d18c05c` 2026-08-25 落的静态判据，至今绿着），**登记没跟上**：
`docs/architecture/model-management.md:295` 今天仍逐字写着「没有任何判据拦得住这条路」。
一个只读 owner doc 的人，今天会得出「这个接缝完全没人守」的结论 —— 而它有人守，只是守得有边界。

- **Phase 1 · 先测牙口，再改字**：六条变异的预测表**先落盘、单独一个 commit**（`2840052`），
  之后才施加第一条 —— `git log` 可验先后。**六条预测与实测逐条吻合，无一条不符**：
  - **M1**（`agenerp/explain/loop.py` 内裸名构造）→ 门禁 **exit 1**，失败文案逐字点名
    `{'agenerp/explain/loop.py': [681]}` ⇒ **它真的盖住了这一种形态**。
  - **M2**（`as _CA` 别名）/ **M3**（`_r.ChatAdapter(...)` 属性式）→ 门禁 **exit 0 · `2 passed`**
    ⇒ 两条都逃出匹配形状（判据只认 `ast.Call` + `ast.Name` + `id == "ChatAdapter"`）。
  - **M4 零施加** → 门禁 **exit 0**，而 `tools/experiments/p1_insight_live/run.py:159` 逐字
    `self._poster = None if inner is not None else ChatAdapter(config)` **今天就在仓里**，
    该文件 `:151` 逐字写明「打真端点」⇒ **域外逃逸不是假想，是一个会真发请求的现存实例。**
  - **M5**（`agenerp/routing/` 之内新增构造）→ 门禁 **exit 0** ⇒ 允许面成立，不是空话。
  - **M6**（把 `ChatAdapter` 整体改名为 `ChatCaller`）→ 门禁 **exit 0 · `2 passed`**、
    `pytest tests/routing -q` **exit 2**（`2 errors during collection`，**ImportError 不是断言失败**），
    `python3 -c "import agenerp.explain.loop"` **exit 1** ⇒ **判据自身没有存活守卫**：
    守护对象一消失，`offenders` 恒空、它永久静默绿。循环那条 `:74` 有 `assert found`，adapter 这条 `:100` 没有。
  - **复原口径**：逐文件按施加前 `sha256` 比对，**不用 `git checkout -- .` 兜底**；
    五条施加变异共 7 份文件 `RESTORED OK`，`git status --porcelain -- agenerp/ tools/ tests/` → **零行**。
  - 全部逐字落 `docs/evidence/p1-routing-guard-registration/README.md`。
  - ⚠️ **顺带看见但不代改**：`model-management.md:320-327` 那句「`tests/routing` 也不在 `gates.yml` 的任何 job 里」
    同样已假（`gates.yml:617-618` 步骤 ④），但它在 Phase 2 判定面（`:293-297` + 新增表）之外，**登记交人**。
- **Phase 1 附加 · L1/L2**：`routing-guards` 表按**文件级**纳管，同一文件里的**两条**判据都要占一行，
  而第 5 列逐字是「**实测**日期」—— 没实测过就把日期写上去，正是本 plan 要修的那种病。故补测两条
  （预测同样先落盘、单独一个 commit `cfe03ba`，再施加）：**L1**（新增第二处 agent 循环）→ **exit 1**，
  文案点名 `{'agenerp/judging/judge.py': [101]}`；**L2**（把 `.chat` 整体改名）→ **exit 1**，
  捕获者正是 `:74` 那句 `assert found`。⇒ **同一个文件里的两条判据，在「守护对象消失」这一格上表现相反** ——
  循环那条**红**，adapter 那条**绿**。第 3/4 列因此必须逐条分开写实，不能按文件笼统写一句。
- **Phase 2 · 把 §12.5 那段登记改成今天的真话**（`git diff` **单个 hunk**，`56 insertions, 3 deletions`，
  全部落在 `:295-297` 那三行与新增表内；三张既有 `machine-read` 表零改动 ——
  `pytest tests/routing/test_capabilities.py -q` → **exit 0 · `19 passed`** 自证）：
  - `:295` 那句**就地改写**（选定的 `Decision`；备选「保留原句 + 追加改准」被否 —— 本段不是状态行而是**事实陈述**，
    留一句已不成立的事实陈述在那里正是本次要修的病）。**原句逐字抄进证据文件留痕。**
  - `:297` 那条预期照实判「**没兑现**」：P1.4 已 `done`，唯一入口没出现，`route()` 今天两个产品调用点。
  - 新增 `<!-- machine-read: routing-guards -->` 表，**五列**：判据文件 · 判据函数名 · 盖住的形态 ·
    **明确不盖的形态** · **实测日期 + 证据路径**。第 5 列的意义是让读者一眼看出**这条覆盖断言有多老**。
    前言四条规矩写死：文件级纳管 · 本期只纳管一个文件（要加第二个必须重开 `Decision`）·
    表名不等于「只登记 routing 语义」· **第 3/4 列是散文、没有判据钉着**（残余风险照实登记）。
  - **另两处同源登记也改准，用的是追加而不是改写**（红线 5 与仓内惯例决定的，不是上面那条 `Decision` 的结果）：
    `STATE.md` §3 追加一条更正行，点名 `:751` 那条 `[open] 2026-08-24T08:12Z` 的两句已被 `d18c05c` 与工作项 6 的 `done` 证伪，
    **仍挂 `[open]`** —— 被证伪的只是描述，四种覆盖缺口本身还在，堵它们要动 `tests/gates/**`（红线 1）。
    `git diff --numstat 7ed23c8 -- docs/masterplan/STATE.md` → **`7 0`，删除列为 0**。
    `docs/backlog/p1-insight-roadmap.md` 工作项 3 那一行**下方**追加结清记录，**行首 `done` 一个字未动**，
    `--numstat` → **`4 0`**。那句 `verification scope limited` **两半分开说**：
    `commands.test` 那一半**仍真**（`GATE_VERIFY` 退 0 不等于新判据被跑过），
    「任何 CI job」那一半**已假**（`gates.yml:617-618` 步骤 ④ + `:631` `COVERED` 含 `routing`）。
  - **原句未被改写这件事怎么判的**：不用 `--numstat`（行内追加必然让整行进 diff，判不出），
    也不用 grep 片段（片段由谁来挑没写死，挑错地方同一次改写照样绿），改用**子串不变式** ——
    `old` 从 `git show 7ed23c8:…` 按工作项前缀取（**命中 1 行**）· 砍行尾前先 `assert old.rstrip().endswith("）")` ·
    判 `core in 全文`。实跑四格：**行内追加 PASS · 改写原句 FAIL · 翻状态词 FAIL · 本次真实交付 PASS**
    ⇒ 一条不变式同时覆盖「原句任意处被改写」与「状态词被翻」，并天然容纳 `1618-1` 那个「行尾 `）` 右移」的合法位移。
- **Phase 3 · 钉住登记面**：新增 `tests/routing/test_routing_guard_registration.py`（**只读 `tests/gates/**`，一个字节不写**）。
  它断言的是**双向同构 + 存活守卫**，五条：① 纳管文件集合写死在 `_REGISTERED_FILES`（**不由表自己导出**）·
  ② `A`（表里的 `(文件, 函数名)`）`== B`（纳管文件内全部顶层 `def test_*`）· ③ `assert A` 存活守卫 ·
  ④ 表里出现未纳管文件即红 · ⑤ 第 5 列的证据路径必须存在、日期必须逐字 `YYYY-MM-DD` 且可 `fromisoformat`。
  **③ 与 ④ 互相咬住** ⇒ `_REGISTERED_FILES` 无法被悄悄放松成空集。模块头逐字声明
  「**本条只验存在性同构，不验语义**，『那条判据到底盖住什么』由证据目录的变异实测负责」。
  **N1–N5 五条变异全部按预测打红**，且**捕获者逐条核对**：N4（整表删空）的文案是
  「表为空 —— 判据静默失效」而**不是** `A == B` 那条 —— ③ 确实被触发，不是被顺带打红的。
  - ⚠️ **N2 第一次施加实测绿，与预测不符，照实记**：复查后查明是**变异器**的缺陷 ——
    它按「全文首次出现处」替换，而那个函数名在改准后的 §12.5 **散文里也出现了一次**，
    ⇒ 改的是散文、表格行一个字没动。第二次把施加面限定到表格行，同一条变异**立刻红**。
    **判据设计一个字未改**。教训单列进证据文件：**变异自查里的「绿」有两种读法 ——「判据盖不住」与
    「变异根本没落地」，两者退出码一模一样 ⇒ 变异器必须自证变异真的落地了。**
  - verification：`pytest tests/routing -q` → **exit 0 · `181 passed, 1 skipped`**（基线 `179`，**只增不减**）·
    `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments tests/ui` → **exit 0** ·
    `check_expected_red.py` → **exit 0 · `门禁 29 项`**（⚠️ 该脚本只跑 `tests/gates/`，本 plan 一个字不碰那里，
    「仍是 29 项」是**恒真**的，只作基线旁证，**不是本 plan 的判据**）·
    `git status --porcelain -- docs/architecture/` → **零行**。
- ⚠️ **verification scope limited**：未起 docker 栈、未调任何模型、未读任何 `AGENERP_LLM_*`、未跑任何 `-m live`、
  未跑整仓 `pytest tests -q -m "not live"`（`gates`×`tools` 环境泄漏导致基线即红，已单列
  `docs/backlog/gates-and-tools-leak-env-across-directories.md`）、**未经 CI 服务端复跑**。
  ⚠️ **并且**：新判据落在 `tests/routing/`，`gates.yml:617-618` 步骤 ④ 会跑到它，
  但 `missions/p1-insight.json:16` 的 `commands.test` 不含 `tests/routing`
  ⇒ **`GATE_VERIFY` 退 0 不等于这条判据被跑过**（plan `D3`，`missions/**` 在红线内，归人）。
- **红线自证**（`BASE = 7ed23c8`）：`git diff --name-only 7ed23c8 -- tests/gates/ .github/workflows/ docs/masterplan/DECISIONS.md docs/masterplan/02-WBS.md missions/ docker-compose.yml industry-packs/ pyproject.toml` → **无输出**；
  `git diff --numstat 7ed23c8 -- docs/masterplan/STATE.md` → **`7 0`**（删除列为 0）。
  ⚠️ **独立收口审计由收口审计步执行，本轮执行者不代跑、不代批、不预填结论。**

### 2026-08-26 · P1.9 §7.2.1 判据抽查 —— 抽中三条里名不副实 **2 条**，CP9 §1.4 重判条件已触发（plan `2026-08-26-1835-1`，三个 Phase 全部收口）

人 `STATE` §3 `[open] 2026-08-26T10:03Z` 交给 loop 的那一整项。**只领人逐字交出来的那一项，不领整行 `P1.9`**
（`02-WBS.md:91` 该行状态源逐字仍是 `人`）；**纪要本身、状态词、重判，三样都仍归人**。

- **Phase 1 · 抽样自己复算，未采信人侧写下的结果**：池 = `git log --since=2026-08-24 --name-only -- tests/`
  去重后仍存在的文件，**实测 44 条**（人侧写的是 40 条）；键 = `sha256("P1.9-spotcheck-2026-08-26" + 路径)` 取前 3
  ⇒ **前三名与人写死的三条逐字相同**，`P1-2` 的「不吻合」分支未触发。
  **池条数差异只记不处置** —— 排序键只依赖路径字符串，池增长不改前三名（第 4 名恒为 `tests/unit/serve_fakes.py`）。
  三条今天全绿（`check_expected_red.py` exit 0 · 门禁那条 `1 passed` · 另两条 `86 passed`）⇒ 抽的确实是「绿判据」。
- **独立性怎么钉（`D-1` 选 (A)，两条否决理由跑前写死）**：抽查由**独立子代理**在 fresh session 执行
  （Agent tool `general-purpose`，**task id `a0d5c65e472906df1`**），只收到**自足简报**
  （三条路径 + §7.2.1 三问 + 变异协议六款 + 承诺面锚点表整表 + 三条「重点盯」清单 + 三条变异施加面 + 判别标准 + 硬约束），
  **未拿到 plan 全文、未拿到 `B5` 那条起草期线索、未拿到任何起草期/评审期的预测与实跑结果与判词**。
  **交给它的 prompt 原文 + 它返回的报告原文 + task id 逐字落进审计文件 §2.3 / §2.4** ——
  ⚠️ 口径是 `closure-audit-prompt` 的「**不采信自报**」，一句「我不是作者」正是它禁止的形态。
  ⚠️ **限度照实说**：子代理与执行者**同属一个循环、同一个模型家族** ⇒ 独立只到**会话级与上下文级，不到实现级**，
  **不得读成「完全独立的第三方审计」**。
- **Phase 2 · 三条判词**：
  - 🔴 `tests/gates/test_insight_rule_ablation.py` → **名不副实**。缺口 **`:47`** —— 薄加载器只导出否定臂，
    **消融的阳性对照臂** `test_h1_the_full_rule_list_reports_the_finished_goods_backlog` 不在门禁面上。
    变异 **A1**（`agenerp.inspection.engine._fires` 恒 `False`，即引擎彻底失去发现力）⇒
    **门禁 exit 0 / `1 passed` / 零红**，而同一变异下开发期那份 `tests/unit/test_inspection_rules.py`
    **exit 1 / `5 failed, 11 passed`**。⇒ 「引擎彻底失去发现力」这条路在门禁面上**完全没有防线**，
    而它同时让门禁的每一条断言恒真。**不构成「已声明的边界」**：该文件 `:49-55` 的自述只声明了
    「零 LLM 调用那两条」不导出，**对阳性对照臂一个字未提**（`P2-7`(i) 不满足）。
  - 🔴 `tests/unit/test_answer_judge.py` → **名不副实**。缺口 **`:275-279`**（`test_m6_..._never_constructs_a_chat_adapter_itself`，
    def 在 `:270`）—— 名字里的 `never` 是全称否定、docstring 还把承诺加强成「F8 那条路不在本包重现」，
    而断言体只覆盖 `ast.Call` 且 `func` 是 `ast.Name`（`:275`）与 `ast.ImportFrom`（`:277`）两种语法形态，
    **属性式构造 `_a.ChatAdapter(...)` 与裸 `ast.Import` 在覆盖之外**；同族缺口 **`:310`**
    （`test_m7_..._has_a_write_path`）只认 `write_text`/`write_bytes` 两个属性名，`open(p,"w").write(...)` 绕得过。
    变异 **B3**（`Path.read_text` 补丁只对 `agenerp/judging/*.py` 生效，注入两种绕过写法 + 一条**阳性对照**）
    ⇒ **exit 1 / `1 failed, 58 passed`**，红的**只有阳性对照那条** ⇒ **注入确实抵达了 AST 扫描面，
    m6/m7 的绿不是注入没生效，是判据真的漏了**。
    ⚠️ **照实说强度**：这是 28 条里的 2 条，docstring 四条承诺里前三条由签名级判据 + B1/B2 变异实测**坐实成立**；
    但 `:270` 那条 `never` **无任何兜底**，且绕过写法是 Python 里完全常规的形态。
  - ✅ `tests/unit/test_explain_cost_ledger.py` → **名副其实但有已声明的边界**。
    `reasoning` 那一半**真的独立可红**（C1 打红 9 条，覆盖重放/链路/逐条对账/三条错误路径/洞察路径/汇总）
    ⇒ P1.7 逐字「挡住『只记 completion 不记 reasoning』这种假实现」实测成立；
    失控闸未被合并（D-18），`cached` 不在本文件（C3 下本文件零红、`test_prompt_cache_accounting.py` 4 红）。
    **`P2-7`(i)(ii) 双证据齐全**，其中 (ii)「被排除的那一半真在别处跑到」由**抽查者自己复跑**
    `python3 -m pytest tests/unit -q` → exit 0 / `839 passed, 17 skipped` 确认，**不是照抄 plan、不是采信文件自述**。
    ⚠️ `:488` 的 `pytest.raises` 型反测在 C1 下如期仍绿，**按 `P2-5`(iv) 不因此判它名不副实** ——
    它的另一半证法就在同文件 `:505`，而 `:505` 在 C1 下确实在 9 条红名单里 ⇒ 这一对成立。
- **变异形态**：10 次变异（A1 / A1-伴 / A2 / A3 · B1 / B2 / B3 · C1 / C2 / C3）
  **全部内存 monkeypatch，一次文件兜底都没用，工作树零字节改动** ——
  这是对「看门狗 60min 无输出即杀、一杀执行者与子代理俱死」这条残余风险的直接对策（内存变异随进程一起消失）。
  每次预测**均在跑前逐字写死**：**9 次吻合**；**C3 一处「部分不吻合」照实记、预测原文未改**
  （子代理跑前把 `def test_` 函数个数 21 与实际收集数 27 混为一谈，两个承重预测本身吻合）。
- **执行者对两条承重结论的独立复跑（裁判规则 3「复跑优先于分析」）**：
  A1 门禁面 → **exit 0 / `1 passed` / 零红**；A1 开发期那份 → **exit 1 / `5 failed, 11 passed`**，红名逐字一致；
  B3 无阳性对照 → **exit 0 / `59 passed`（m6/m7 全绿）**；B3 带阳性对照 → **exit 1 / `1 failed, 58 passed`**，
  红的只有 `test_no_product_module_in_the_judging_package_reads_the_annotation_set`。
  **与子代理逐字一致。** 另 `grep -n` 实读复核了 `:270`/`:275`/`:277`/`:305`/`:310` 五个行号，**逐字相同**。
- 🔴 **CP9 §1.4 的重判条件已触发**：该节逐字「若抽中的三条里**再出现一条名不副实**，则『误放行 = 1 次、且是孤例』
  不再成立，**本条须重判**」—— 本次 **2 条**，按 §7.2 原规则记为**误放行 2 次**。**重判归人。**
  loop **不代人下续用/停用的判断**，**未改** `docs/audits/2026-08-26-CP9-P1-retrospective.md` 一个字节，
  **未动** `docs/backlog/implementation-roadmap.md:38` 那一行（人 `17fc820` 写的结论面）。
- 🔴 **另一条与 CP9 §1.4 对不上的可观测事实（只登记，不改写纪要）**：§1.4 逐字「三条都不是人侧代理写的」，
  而 `git log --format='%an' -- tests/gates/test_insight_rule_ablation.py` 实测 author 是 **`lize`**（`913d515`），
  与 §1.4 自己举的人侧代理例子（`f68ae19`）**同一个 author 名**。
  ⇒ 「抽样公平」那句话在这一条上不成立。**它反而加强了「交 loop」这个安排的正当性。**
- **本轮一处缺口都没有修**（Non-Goals 8：抽查是审计，不是修理）——
  两条缺口分别落在 `tests/gates/**`（红线 1）与 `tests/unit/**`。
- **Phase 3 · 落盘**：审计文件 `docs/audits/p1-insight/2026-08-26-P1.9-gate-spotcheck.md`（五节 + 收口审计一节）
  **sha `2b70627`**（收口审计与四条 finding 的处置在后续提交）· `docs/masterplan/STATE.md` §3 **追加**一条证据行（含三条判词、命令与退出码、
  独立性依据、上面那条 author 事实、重判条件已触发）· 本条日志。
  ⚠️ **`[open] 2026-08-26T10:03Z` 那一行本身一个字都没改（红线 5）** —— **loop 无权把它标 `[resolved]`**，
  追加行里显式写明「它要的东西已交付，**标记归人**」。
  并**一并点名** `[open] 2026-08-26T09:49Z`（§7.2.2 人侧四提交独立复核）**仍敞着、本轮未做**，
  理由是 Non-Goals 5：那是另一个结果面，且它在 `02-WBS.md` 里**没有行**，loop 无权开行（红线 5 + D-24）。
- **红线自证（`<BASE>` = `29a85dc`，逐条说清哪条命令证了哪条，不许一条命令包打七条）**：
  `git diff --name-only 29a85dc -- tests/gates/ .github/workflows/ docs/masterplan/DECISIONS.md docs/masterplan/02-WBS.md missions/ docker-compose.yml agenerp/ tests/ industry-packs/`
  → **无输出**（红线 1/2/3/5 直接证到）· `git diff --name-only 29a85dc -- pyproject.toml` → **无输出**（红线 4）·
  `git diff --numstat 29a85dc -- docs/masterplan/STATE.md` → **`15  0`，第 2 列 deletions == 0**（红线 5 只追加；
  🔴 **未用** `grep -c '^-[^-]'` —— 该写法对 `- [open] …` 这种行形状**真删了也返回 0**）·
  **红线 6 本仓 diff 按构造证不了** ⇒ 逐字声明「**全程未对 `${XM_PATH}` 发起任何写操作**」，
  本轮**也未读过它**（只读了 `evidence-repo.env` 指针文件），**不拿本仓 diff 冒充** ·
  **红线 7 由「产品代码零改动」间接推出**，不是上面任何一条直接证的。
  另：`git diff --name-only 29a85dc -- docs/audits/2026-08-26-CP9-P1-retrospective.md` → **无输出**。
- **附带登记（红线 5 内不可修，只记）**：`02-WBS.md:91` 的 `P1.9` 行被 `42fa183` 改成 **6 格**，
  而表头 `:76` 只有 **5 列** ⇒ 渲染时最后那个 `人` 会被吞。**改它归人。**
- **独立收口审计（`Audit: required`）**：**独立子代理**，fresh session，**task id `a6caad8c6df08b9d1`** ——
  **既不是本 plan 的执行者，也不是那三份判据的作者**。口径逐字取自 `closure-audit-prompt.md`「不采信自报，逐条对活仓取证」。
  **总判词 `closure approved`**（1 条 `should-fix` + 3 条 `nit`，**零 `blocking`**）。
  审计者**自己复跑**了红线四条自证、`P1-1` 抽样重算（池 44、前四名逐字相同）、`P2-A` 三条锚点、`P2-7`(ii)、
  以及 **A1 / B3 / B1 / C1 / C3 五次变异** —— **红名集合与落盘逐字一致**；
  并**额外加验了一层阳性对照的有效性**（`_code_without_docstrings` 与 m6/m7 走同一条 `read_text` 路径）
  和**「误判成边界」的反向风险**（实测阳性对照臂 `test_h1_the_full_rule_list_reports_the_finished_goods_backlog`
  **不依赖任何 fixture、完全可以被导出** ⇒ 该文件自述的排除理由对它不适用，`P2-7`(i) 明确不满足）。
  **三条判词逐条独立同意。**
  四条 finding **全部已处置**（审计文件 §6.1 逐条留痕）：**F1** 补上「验收面未转红」的成因追踪
  （验收面四条 `:50`/`:130`/`:136`/`:140` 全绿，成因 `answer_judge_fixture.py:143`/`:183` 不进 `agenerp/judging/`；
  执行者已独立复跑 B1 坐实）· **F2** 行数写死改掉 · **F3** 补点名 `agenerp/explain/loop.py:89-97` 的过期注释
  （**Non-Goals 8：不修**，且照实说明执行者事实上没「撞上」，是收口时主动补查的）· **F4** §2.4 标题改为「二次落盘版」并写明不可逐字比对。
  ⚠️ **照实说：F1–F4 的处置本身未经第二轮独立复核。**
- verification（命令原文 + 退出码）：
  `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 29 项：预期红 0，绿 29，跳过 0`）·
  `python3 -m pytest tests/unit -q` → **exit 0**（`839 passed, 17 skipped`）·
  `python3 -m pytest tests/gates/test_insight_rule_ablation.py -q` → **exit 0**（`1 passed`）·
  `python3 -m pytest tests/unit/test_answer_judge.py tests/unit/test_explain_cost_ledger.py -q` → **exit 0**（`86 passed`）·
  `git status --porcelain -- agenerp/ tests/` → **无输出**（全程内存变异，未用 `git checkout -- .`）。
  ⚠️ **verification scope limited**：**未跑**整仓 `pytest tests -q -m "not live"`
  （已知基线即红，`docs/backlog/gates-and-tools-leak-env-across-directories.md`）· **未跑**任何 `-m live`
  （本轮零网络、零凭据、零 docker、零 LLM 调用）· **未经 CI 服务端复跑**，上列全部是本机结果。
### 2026-08-26 · P2.0R 头号风险：瓶颈钉在「选单据」，不在「选字段」（分支 `p2-views`）

- **本轮只回答一个问题：`75% → 90%` 这 15 个点卡在哪一层。不选路，不宣称达到 90%。**
  评测集（40 条中文问句 → 目标字段，**40/40 ground truth 在活站点验过存在**）与预测
  **在跑任何检索之前**冻结提交（`e82f73d`），结果在其之后 —— 先后由 git 自证。
- **决定性的一格 P4（oracle 消融）**：把 ground-truth DocType 直接给定，只在其字段内检索
  → Top-5 **97.5%**、Top-10 **100%**；同条件不给定 DocType 只有 **62.5%**。
  **40 条只错 1 条，而那一条错在同一张单据内部**（`Delivery Note Item.qty` 被 `actual_qty` 压住）。
  ⇒ **瓶颈在选单据。换更大的嵌入模型不是解法**（与 D-22.4 同源），
  §11 那条 Deferred 的重开条件「P4 ❌ 才重开」**判定为不重开**。
  ⇒ 下一轮主攻 **DocType 消歧（结构化召回）**。
- **⚠️ P3 预测未中，照实记，不改预测**：我事先写死的判法（「Top-5 里字段名完全相同而 DocType 不同」）
  实测只有 **21.4% / 33.3%**，远低于预测的 ≥50%。**是我的定义过窄** —— 实际错法多是
  语义邻近但字段名不同（`actual_qty` / `stock_qty` / `packed_qty` 互串），落不进那条定义。
  换一个**明确标注为事后**的口径才说清楚：miss 里 **78%（business）/ 60%（live）
  的正确 DocType 连出现在 Top-5 里都没有**。该数**不参与任何判定**。
- **⚠️ 与 Spike 07 的一处分歧，机制已查明**：「只索引有数据的 DocType」在 XM 上 +15 点，
  **在本站点净亏 2.5 点**（65.0% → 62.5%）。逐题拆开：**新中 4 条、丢掉 5 条**，
  丢掉的**恰好是起草时（§1.5）跑之前就点名的那 5 条**（答案 DocType 行数为 0）。
  ⇒ 过滤器的收益真实存在，但被**种子数据宽度**造成的结构性损失吃光。**不能无条件启用。**
- **`P0 = 0%` 是地板不是差评**：本仓现役 `schema_search` 按设计就是 DocType 召回器，
  从不返回字段。它证明的是 **P2.0R 的验收面在本仓今天是空的** ——
  那个「基线 75%」是 XM 证据仓里另一套实现的数，按 D-16 不能当本仓基线。
- **🔴 交人**：WBS §5 的验收判据 `tests/gates/test_schema_retrieval_recall.py` **仍不存在，
  红线 1 我不能建**。材料已齐（`tools/experiments/p2_schema_retrieval/`，README 有复跑命令）。
  **判据不落地 ⇒ P2.0R 不算过 ⇒ P2.3 仍被阻塞**（前置 `P2.2 + P2.0R`）。
- **成本**：**零 LLM API 调用**。全程本地 Ollama `qwen3-embedding:0.6b`（与 Spike 07 同款，
  为的是可比）。建索引 6,350 字段：raw `127s` / described `152s`。
- **局限不掩盖**：40 条无统计意义（1 条 = 2.5 点）· 评测集我出、结论我判（只做了「跑前冻结」
  这一条缓解）· 只测一个 0.6B 模型 · **oracle 是上界不是实现** · 本 plan 走 solo cold-replay。
- **红线自证**（`vs 012385a`）：`git diff --name-only 012385a -- tests/gates/ .github/workflows/ missions/ docs/masterplan/`
  → **无输出**；`agenerp/**` 一字未动（本轮是测量不是改造）；证据仓 `XM_PATH` **只读引证、未写入**；
  未生成 Server Script。**全部产出在 `p2-views`，未碰 `main`。**

### 2026-08-26 · P2.0 🚪 入口关口实验：Spike 11 端到端跑完（分支 `p2-views`）

- **结论：假设成立 —— 改标准 `Workspace` 会被 app 升级静默覆盖。**
  ⇒ **视图产物必须落 AgenERP 自有表，不得落标准 Workspace。**
  站点 `frontend`（frappe 15.118.0 / erpnext 15.119.3），标的 `Workspace/Selling`，三臂：
  控制组（不动 JSON）改动全存活 · Arm A（md5 变、`modified` 不变）改动全存活 ·
  **Arm B（`modified` 推新）`icon` 被 JSON 覆盖、shortcuts 7 → 6、canary 在
  `tabWorkspace Shortcut` 里物理删行（count = 0），只有 `is_hidden` 幸存**。
- **「静默」不是修辞**：Arm B（抹掉全部用户改动那次）与控制组（什么都没发生那次）的
  `bench migrate` 输出，去掉进度条后 **`diff` 退 0，逐字节相同**；
  `grep -inE "selling|warn|overwrit|discard|lost|delet"` **零命中**。
- **⚠️ 跑之前发现一条判据在拧不接线的旋钮**（`f7cc4bd` 里写死，早于任何一次 migrate）：
  `REF:ROADMAP-SPIKE1112` 的证伪判据是「手改 app 内该 JSON **使 md5 变化** → 改动仍在即证伪」，
  但 `import_file.py` 的 md5 比较**只对 `DocType` 生效** —— `migration_hash` 是 DocType 的字段，
  `Workspace` 没有（实测 `has_column` → `False`），`stored_hash` 恒为 `None`。
  Workspace 真正的闸门是 `is_db_timestamp_latest`（`modified` 时间戳）。
  ⇒ **照字面四步跑（Arm A）得到的结论是「证伪」，与 Arm B 实测的事实相反。**
  同 P1 复盘 §1.2 的形态：**判据绿 ≠ 判据在测它名字说的那件事**，区别是这次跑之前就发现了。
  **判据是裁判，一个字未改**；双臂并列上报，**处置交人**。
- **owner doc 订正**：`docs/architecture/module-boundaries.md` §11.4 的机制陈述
  （2026-08-20 版写的「按文件 md5 决定是否导入」）**是错的**，已按实测改写为 `modified` 时间戳，
  并补上三臂实测表。`delete_doc` + 重新插入那一半原本就是对的，保留。
- **复原核验**：`selling.json` md5 回到 `b87df5828d081999ab63ff3c2911b901`，
  `Workspace/Selling` 的 `modified`/`icon`/`is_hidden`/`n_shortcuts`/`n_links`/canary 行数
  **六项逐项等于实验前基线**。临时脚本全在容器 `/tmp`，工作树零残留。
- **成本**：全程**零 LLM 调用**，未消耗任何模型额度。
- **人 2026-08-26 裁定（同日晚些时候，补记）**：①「以 Arm B 判过，判据原样留着」
  ⇒ **P2.0 关口 → 过**，`REF:ROADMAP-SPIKE1112` 一个字不改，「该判据名不副实」转入 **P2.8 复盘**
  作为 §7.2.1 抽查制的实例（已写进 `p2-views-roadmap.md`，不会丢）；
  ②「认可换栈，自有栈才是判定面」⇒ 结论适用 frappe 15.118.0，XM 栈复跑转 Deferred But Adjudicated。
  plan → `Plan Status: completed`。
- ⚠️ **本次 closure 的局限照实写**：source-of-truth 冲突由人直接裁定（满足 Reviewer-Availability
  Fallback 的「人」那一支），**但没走独立子代理的冷复放**（本轮被明确要求不派子代理）。
  ⇒ **实验设计本身（三臂怎么分、H1–H6 怎么措辞）只有作者一个人读过。** 残余风险不掩盖。
- **红线自证**（`vs 012385a`）：`git diff --name-only 012385a -- tests/gates/ .github/workflows/ missions/ docs/masterplan/`
  → **无输出**（红线 1/2/3/5）· 未写证据仓 `XM_PATH`（红线 6）· 未生成任何 Server Script（红线 7）·
  未改项目名/包名/命名空间（红线 4）。**本轮全部产出在 `p2-views` 分支，未碰 `main`。**

### 2026-08-26 · `route()` 尊重 `AGENERP_LLM_MODEL`（plan `2026-08-26-1728-1` Phase 3 收口）

- **变异自查 M1–M10 + 补的 M6b**：**8/10 按预期形态打红**，逐条施加、记退出码与栈顶、逐条复原，
  三个文件 `sha256` 与施加前**逐字节相同**（`router.py` `f8658cd3…`、`test_router.py` `65f79bce…`、
  `capabilities.py` `3572c242…`）。⚠️ **两格没按预期形态红，照实登记不修饰**：
  **M6**（`model=profile.name` → `model=None`）对 P4 **exit 0** —— 点名分支下两者恰好同值，
  adapter 的 `or config.model` 兜出同一个串；补 **M6b** 改判 P6（空 `config.model` 走「第一个满足的胜出」）
  → **exit 1**，整个 `tests/routing` `32 failed, 147 passed` ⇒ 该变异确实被这一层挡住。
  **M8**（`KNOWN_MODEL_PROFILES` 删掉 `qwen3:14b`）红在**收集期**（`KeyError`，`test_router.py:32` 的模块级夹具），
  不是红在 P2 第二组 / P4 的断言上；**同一变异对 WBS 验收文件 exit 0（`10 passed`）** ——
  它按表参数化，表缩小时用例数从 12 掉到 10 而全绿。⇒ **「遍历某张表」的参数化判据对「表本身变短」是盲的**，
  这是普遍形态不是本 plan 的局部问题，就地登记交人（§7.25.7）。
- **owner doc 对齐**：`docs/architecture/module-boundaries.md` 新增 **§7.25**（七小节：缺陷本体 · D-1 三段 ·
  优先级 · 残余风险 · 行为变化 · 判据分工表 · 变异表）—— **纯追加**，
  `git diff … | grep -c '^-[^-]'` → **0** ⇒ §7.13–§7.24 一行未改。
  `docs/architecture/model-management.md` §12.5 两处限定（环境变量表 `AGENERP_LLM_MODEL` 一行 + 「摆放规矩」第 2 条），
  **三张 `machine-read` 表一格未动**（`tests/routing/test_capabilities.py` → exit 0，`19 passed`）。
- **D1（502 vs 503）本轮亲自实测**（未采信 plan 起草期的数）：临时探针跑出
  `STATUS = 502`、文本逐字 `点名的模型 'typo-model' 不在候选档案里；候选是 ['fake-explainer']`，
  探针跑完即删、工作树无残留。修法面在 `agenerp/serve/**` = 工作项 10（预算 `2/2` 已满）⇒ **交人**，
  已追加进 `docs/masterplan/STATE.md` §3。
- **⚠️ 执行期 HEAD 移动了，照实说**：起跑基线 `433d2ca`，执行中人侧落了 `42fa183` / `9be3007`，HEAD 变成 `9be3007`。
  本 loop 往 `STATE.md` §3 追加的那条 needs-human **在人侧提交时正在 working tree 里，被一并带进了 `9be3007`**
  ⇒ **那 8 行署在人的 commit 名下，内容是 loop 写的**。仍满足红线 5
  （`git show 9be3007 -- docs/masterplan/STATE.md | grep -c '^-[^-]'` → **0**），**但署名不准这件事不掩盖**。
- **红线自证（`vs HEAD = 9be3007`）**：
  `git diff --name-only HEAD -- tests/gates/ .github/workflows/ missions/ docker-compose.yml industry-packs/ docs/masterplan/`
  → **无输出**（红线 1/2/3/5）· 证据仓 `git -C $XM_PATH status --porcelain` → 无输出且
  `HEAD = 1c622c81… == XM_SHA`（红线 6）· 未生成 Server Script（红线 7）· 未改项目名/包名/命名空间，
  `agenerp/routing/__init__.py` 的 `__all__` 逐字未变（红线 4 + B6 第三条）。
  ⚠️ `missions/p2-views.json` 是人侧 `9be3007` 的产物，本 loop 未写、未提交。
- verification（命令原文 + 退出码）：
  `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 29 项：预期红 0，绿 29，跳过 0`）·
  `python3 -m pytest tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments -q -m "not live"`
  → **exit 0**（`1314 passed, 23 skipped, 7 deselected`，由 `1299 passed` **只增不减**，+15）·
  `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments tests/ui` → **exit 0** ·
  `python3 -m pytest tests/unit/test_configured_model_is_the_one_used.py -q` → **exit 0**（`12 passed`，由 `6 passed` 只增不减）·
  `python3 -m pytest tests/routing -q` → **exit 0**（`179 passed, 1 skipped`）。
- ⚠️ **verification scope limited**：未跑整仓 `python3 -m pytest tests -q -m "not live"`（**已知基线即红**：
  `gates`×`tools` 环境泄漏已单列立案于 `docs/backlog/gates-and-tools-leak-env-across-directories.md`）；
  未跑 `-m live`（plan 的 Prereqs 逐字不要求）；未经 CI 服务端复跑。
- 证据全文：`docs/evidence/p1-routing-configured-model/README.md`。
- **build-verify（loop 独立复跑，`full-green verification`）** —— 本步未采信上面任何一个数，四条命令原样重跑：
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 29 项：预期红 0，绿 29，跳过 0` + `839 passed, 17 skipped`）·
  `python3 -m pytest tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments -q -m "not live"`
  → **exit 0**（`1314 passed, 23 skipped, 7 deselected`）·
  `python3 -m pytest tests/routing -q` → **exit 0**（`179 passed, 1 skipped`）·
  `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments tests/ui`
  → **exit 0**（`All checks passed!`）。**四个数与上面逐字吻合，无漂移。**
  `tools/gates/expected-red.txt` 本轮**未改**：它已是 0 条目（只剩注释），没有可划掉的行。
  红线自证 `vs HEAD = 9be3007` → 无输出；`STATE.md` 未被本轮触及（那条 needs-human 已随 `9be3007` 入库）。

### 2026-08-26 · `route()` 尊重 `AGENERP_LLM_MODEL`（plan `2026-08-26-1728-1` Phase 1–2）

- **缺陷**：`route()` 不传 `requested` 时取「第一个满足该类目的档案」，用 `profile.name`
  ⇒ **`config.model`（`AGENERP_LLM_MODEL`）被整个盖掉**。`adapter.py:128` 的 `model or config.model`
  里 `model` 恒非空，那一支在走 `route()` 的路径上永远取不到。
  复现（零网络）：`config.model = qwen3:14b | adapter.model = qwen3.8-max` ⇒ **配了 A 调了 B，无声。**
  活栈后果逐字记在 `agenerp/serve/app.py:246-256`（配 `qwen3.7-flash` 走 `qwen3.8-max`，后者无免费额度 ⇒ 每次解释 403 回空答案）。
- **裁定 D-1（选 A）**：`config.model` 去首尾空白后非空即**等同点名** —— 先按名取档案（取不到按名抛），
  再过**同一条** `satisfies()` 校验；显式 `requested` 压过它。备选 B/C/D 的否决理由见
  `docs/architecture/module-boundaries.md` §7.25。
- **实现**：`route()` 体内把 config 解析**上提到挑档案之前**（原在 `for` 循环体内），
  `requested is None` 时用 `(resolved_config.model or "").strip() or None` 顶上。
  行为变化照实登记：`config is None` 且环境没配时，`RoutingError("配置不全…")` 现在**在能力校验之前**抛。
- **代价实测**：只改 `route()` ⇒ 六目录 `15 failed, 1284 passed`，**15 条全部在 `tests/routing/test_router.py`**，
  唯一成因是该文件夹具 `CONFIG` 的 `model="unused"` —— 那个字面量本身就是本缺陷的编码。
  改成 `model=""`（不点名 ⇒ 旧路径原样保留）后 `tests/routing` 回到 `170 passed, 1 skipped`，
  **一条既有断言未改、未删、未放松**（`git diff -- tests/routing/test_router.py | grep -c '^-[^-]'` → **1**，就是那行夹具）。
- **判据先红后绿**（改动前逐条实跑，退出码逐字记）：
  P2 两组 / P3 / P4 → 改动前 **exit 1**；**P8 → exit 1（`4 failed, 1 passed`，栈顶 `assert 'qwen3.8-max' == 'qwen3.7-plus-2026-05-26'`）**；
  **P9 → exit 1**。⚠️ **照实说**：P5（`requested` 压过 `config.model`）/ P6 / P6b / P7 **改动前就绿** ——
  它们钉的是优先级与空值语义，旧实现「从不读 `config.model`」时**恰好也满足**，
  是回归护栏而非缺口证据；它们真正的证据在 Phase 3 的变异 M2 / M3。
- **WBS `P1.1-fix` 的验收本体**：`tests/unit/test_configured_model_is_the_one_used.py`
  **纯新增**两条**不传 `requested`** 的用例（参数化成功面 + 未知模型失败面），
  `6 passed` → **`12 passed`**，`git diff -- <该文件> | grep -c '^-[^-]'` → **0**（既有 6 条一字未改）。
- verification（命令原文 + 退出码）：
  `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 29 项：预期红 0，绿 29，跳过 0`）·
  `python3 -m pytest tests/routing tests/unit/test_configured_model_is_the_one_used.py -q` → **exit 0**（`191 passed, 1 skipped`）·
  `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments tests/ui` → **exit 0**
- ⚠️ **本条只覆盖 Phase 1–2**；变异自查 M1–M10、owner doc 对齐与收口取证记在 Phase 3。

### 2026-08-26 · `doc.links` 子表分支补守卫（plan `2026-08-26-1618-1` Phase 1–2）

- **缺陷**：`5396e68` 把人对 C1 的裁定第 ② 条（「单个宿主查失败不整次作废」）**只落在主表支**，
  子表支的两处站点调用（`documents.py:129` 查子表行 · `:139` 逐行回溯父单据）**至今无守卫**
  ⇒ 任一抛错异常穿透整个 `scan_links()`，`doc.links` / `lineage.trace` 整次作废。
  roadmap 逐字「21 个指向 `Sales Order` 的 Link 里 14 个在子表」⇒ **无守卫的那支是多数路径**；
  活站点上 `doc.links{Item, HRD-PACK-5K}` 的 14 行里 **8 行来自子表宿主**。
- **判据先红后绿**：新增两条失败点判据在修改前各自红在**构造的异常逐字穿透**上
  （栈顶 `documents.py:129` / `:139`，**不是断言不相等**），第三条反「绿着坏掉」判据修改前就绿。
- **三条裁定**（详见 `docs/architecture/module-boundaries.md` §7.24）：
  ① 回溯父单据失败 ⇒ **丢掉那一行**，不以 `docstatus=None` 记入 —— 下游筛选逐字是
  `docstatus != CANCELLED` 而 `None != 2` 为真，记入等于把一张可能已取消的单据当成有效关联漏出去；
  `child_level_rows.append(row)` 一并挪到调用**成功之后**，`child_table_hits` 从此只算产出了命中的行。
  ② `scanned_link_levels` 的过度声称**改掉**：`scanned(level)` 挪到站点调用成功之后
  ——「零命中」仍记（扫成了），「宿主全崩」不记。既有三条相关判据实跑仍绿。
  ③ 失败**留痕**（`hosts_skipped_after_failure` / `child_rows_skipped_after_failure`），
  **只加在 `scan_links()` 的返回上**：`doc_links()` / `lineage_trace()` 的 `Outcome.facts` 一个键不加
  ⇒ 契约后置条件与活体门禁的形状不变（红线 1）。
- **变异自查 M1–M6 全部打红**，逐条施加后复原并 `sha256` 比对为
  `7e41d2d4a72b3d7ba018e462b3bdc3000a5ffe09561952cf4806f04f1b7dd5c6`（与施加前逐字节相同）。
- verification（命令原文 + 退出码）：
  `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 28 项：预期红 0，绿 28，跳过 0`）·
  `python3 -m pytest tests/unit tests/tools -q` → **exit 0**（`908 passed, 29 skipped`，由 `903 passed, 29 skipped` **只增不减**）·
  `python3 -m pytest tests/contracts tests/routing tests/context -q` → **exit 0**（`375 passed, 1 skipped`）·
  `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments tests/ui` → **exit 0**
  （顺带清掉 `test_doc_links_skips_singles.py:22` 那行随 `5396e68` 进仓的 `F401 import pytest`，
  它本会让下一次推送的 `gates.yml` `lint` job 变红）。
- ⚠️ **本条只覆盖 Phase 1–2 的离线口径**；活站点回归与活体门禁的两跑记在 Phase 3。

### 2026-08-26 · P1.8b Phase 2 —— 侧边栏本体落地 + 离线判据 7 条

- `agenerp/serve/assets/desk.js` 由「只证明自己到了」（1,193 字节）扩成侧边栏本体（**10,817 字节**），六件事全做：
  ① `Cmd/Ctrl+K` toggle 唤起/关闭 + `Esc` 关闭 + **焦点归还**（关闭时 `focus()` 回唤起前那个元素，
  元素已不在文档里就不还、不抛）；② 上下文取值链按 §7.23.2 落地；③ 同源 `fetch(EXPLAIN_PATH, {credentials:"same-origin"})`；
  ④ 请求体**只放** `question` / `task_class`，有上下文时才挂 `doctype` / `name`；
  ⑤ 九个可分辨码 + `200` 各一态 **+ 兜底态 + 网络层失败态**；⑥ `Object.freeze` / `agenerpDesk` / `)();` 收尾四格全保。
- **真浏览器烟测（12 态，`page.route` 打桩）**，逐条实读可见文本：**10 条两两全等比较全部为假**、
  **每一条都含自己那个码的字面量**、`418` 渲染成「未预期的响应（418）」、`abort` 渲染成「请求没能发出去（Failed to fetch）」
  —— **无一条空白、无一条停在 spinner**。
  ⚠️ 为此给 200 态补了一行「答出来了（200）」：`answer` 的内容是模型给的、不受控，
  不加这一行时 200 态在可见文本上与别的态**分不出来**。
- 新判据 `tests/unit/test_desk_sidebar_static.py`（**离线、零浏览器、零 JS 运行时**，7 条）：
  ① 请求路径与 `app.py` 的 `EXPLAIN_PATH` **各读一次再比**（且反向禁止别的 `/agenerp/*` 字面量）·
  ② 请求体键集 ⊆ `ALLOWED_BODY_KEYS` 且与 `CALLER_CLAIMED_KEYS` 交集为空（**两个集合都从 `app.py` 读**）·
  ③ `window.agenerpDesk` 标记（`Object.freeze` 那格归 `test_desk_asset_route.py`，不重复断言）·
  ④ 九个码字面量各出现过 + ④b 兜底/网络失败两个态有名字 ·
  ⑤ **⑤a** `innerHTML`/`outerHTML`/`insertAdjacentHTML` **各 0** · **⑤b** `document.cookie` **0** ·
  **⑤c** `JSON.stringify(` **1 次**（上限 1；拼请求体那次是正常的）· 外加「渲染只读那四个已知键」的正面一半。
  ⚠️ 五件全部逐字标注成**文本下限，不证运行时行为**；可达性由 Phase 3 的活体判据承担。
- verification（命令原文 + 退出码）：`python3 -m pytest tests/unit -q` → **exit 0**（**`814 passed, 6 skipped`**，
  由 807 只增不减）· `python3 tools/gates/check_expected_red.py` → **exit 0** ·
  `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments` → **exit 0**
  （⚠️ **本轮不把 `tests/ui` 加进参数** —— 那个目录 Phase 3 才建，此时传进去 ruff 直接报错，是一条假红）·
  既有 22 条（`test_desk_asset_route.py` / `test_desk_injection_static.py`）**`git diff --stat` 空输出 = 零改动**，且实跑 `22 passed`。
- ⚠️ **verification scope limited**：未跑整仓 `pytest tests -q -m "not live"`；未经 CI 服务端复跑。

### 2026-08-26 · P1.8b 下半 plan `2026-08-25-1743-1` Phase 1 —— 六条探针实跑 + 五条裁定落定

- 派工：`2026-08-26-094345-mission-driver` 派 `docs/plans/p1-insight/2026-08-25-1743-1-desk-sidebar-cmdk-and-live-ui-gate.md`。
  开工 sha = `1663c05`，`git status --porcelain` **无输出**。plan `Plan Status: active`（人已在 2026-08-26T01:47Z 解除 Review Hold）。
- **§0 六条重取基线全跑**（退出码逐条实取）：`check_expected_red.py` → **0**（`门禁 28 项：预期红 0，绿 28，跳过 0`）·
  `pytest tests/unit -q` → **0**（**`807 passed, 6 skipped`** —— ⚠️ plan §1.9 起草期记的是 `801 passed`，**照执行期实读为准**）·
  `pytest tests/contracts tests/tools tests/routing tests/context -q` → **0**（`456 passed, 13 skipped`）·
  `ruff check`（七目录）→ **0**（`All checks passed!`）。
- **停机分支 4 免停条件执行期实读满足，不停机**：`DECISIONS.md:373` 的 **D-25** ·
  `STATE.md:496` 的 `[resolved] 2026-08-26T01:47Z` 行（⚠️ **不是 plan 写的 `:425`，漂了 71 行** —— 按字面定位是对的）·
  `pyproject.toml` 的 `ui = ["playwright>=1.47"]` 且 `dependencies` 仍逐字只有 `certifi>=2024.2.2`。
- **`env | grep -c '^AGENERP_LLM_'` = 1**，变量名只有 `AGENERP_LLM_MODEL`（**值一个字未打印**）。`H7b` 走哪一支待 Phase 3 实测钉死。
- **六条探针（真浏览器 + 真登录，非推断）**：`H1` 目录集合与 `gates.yml:597` 的 `COVERED` **相等**（八个）·
  `H2` chromium **`145.0.7632.6`** 真起来读到 DOM · **`H2b` 默认站回落成立**（`/login` 200、`title=Login`、登录表单在）
  ⇒ `--host-resolver-rules` 对冲分支**未触发** · `H3` **`Cmd/Ctrl+K` 没被 Desk 占**（注册表 `handlers["k"]` `ABSENT`；
  真按下 `defaultPrevented=false`、modal 0、焦点不动）⇒ `D-d-4` 保留 ⌘K，plan §11 那条键位偏差**未触发** ·
  `H5` 注入**恰好 1 次**且 **`window.agenerpDesk` 读得到**（比预测强一格：§7.22 留的「标签在 ≠ 被执行」第一次被正面回答）。
- ⚠️ **`H4` 不吻合，照实记**：本机站点 `setup_complete=False` ⇒ Desk 把**任何** `/app/**` 强制改写成 `setup-wizard`
  ⇒ URL / `frappe.get_route()` / `cur_frm` **三者都取不到**。按 plan `H4` 第四列写死的处置走 = **合法的「无单据上下文」态**。
  这一格换来一条真发现：**`frappe.router.routes` 是 slug → `{doctype}` 的映射（本机 447 条）**，
  `user`→`User`、`sales-order`→`Sales Order`，而 `setup-wizard` / `home` **`ABSENT`**
  ⇒ ① URL 里的 slug **不是** doctype 名（原样发出去必然 403）；② URL 形状**分不出**单据路由与页面路由。
  上下文取值链因此写死为「URL 取哪两段（第一顺位不变）+ `routes` 表反 slug 并判类型 → `get_route()` 的 Form → `cur_frm` → 无上下文」。
- 产出：`docs/analysis/2026-08-25-1743-desk-sidebar-probe.md`（新建，六格实际值 + 五条裁定的完整理由）·
  `docs/architecture/module-boundaries.md` **§7.23**（新建，**纯追加 199 行、`git diff` 删除行计数 `0`**
  ⇒ §7.13/§7.20/§7.21/§7.22 零改动）。
- ⚠️ **verification scope limited**：未跑整仓 `pytest tests -q -m "not live"`；未经 CI 服务端复跑。

### 2026-08-26 · plan `2026-08-25-1118-1` 第三次被派回执行 —— **卡点与上一轮逐字相同，本轮零产出**

- 派工：任务 `2026-08-26-094345-mission-driver` 第三次把 plan 派回 EXECUTE。开工 sha = 上一轮收尾 sha `3b01c8a`，
  `git status --porcelain` **无输出** ⇒ **两轮之间仓库零变更**，本条不是新证据，是同一状态的又一次独立复核。
- 卡点未解：plan 等的 `STATE.md` §3 `[needs-human] 2026-08-26T03:18Z` **仍是 `[needs-human]`，人未答**
  （`git log` 实测 `HEAD` 之后没有 author 为 `lize` 的新提交）。
- 复跑优先于分析（裁判规则 3）：原样再跑卡点那条命令 —— `git diff --stat 182ef2a^ 182ef2a -- agenerp/ docker-compose.yml tools/`
  → **仍是空输出**，`P3-6` 绑定 2 **字面仍不满足**。`182ef2a` 实取全量改动为 `DECISIONS.md`(12+) · `STATE.md`(11+) ·
  `tests/unit/test_explain_service_body.py`(37+ 5-)。
- 为什么仍不打勾：打 `P3-6` 要么放宽绑定 2（**五条里的防伪条款** —— 由 loop 解释一条约束 loop 的防伪条款，正是它要防的那种事），
  要么伪造一次未发生的验收。**两条都不做。**另落一次 `agenerp/` 内的修法能让路径清单满足，但**当场撞绑定 4**
  （「修复提交的 diff 必须就是 `D-b-9` 里选中的那个修法本身」）且等于 loop 自开一次 Phase 2 裁定 ⇒ **换个形状的自批，不是出路。**
- 本机复跑（命令原文 + 退出码）：`python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 28 项：预期红 0，绿 28，跳过 0`）·
  `python3 -m pytest tests/unit -q` → **exit 0**（`807 passed, 6 skipped in 13.76s`）· `bash tools/check-masterplan-links.sh` → **exit 0**（`断链 0 条`）。
  ⚠️ **verification scope limited** —— 未跑整仓 `pytest tests -q -m "not live"`；两条有条件命令未触发（本轮未改 compose/nginx）。
- 红线自证（`BASE_4 = 3b01c8a`，两步法）：`-- tests/gates/ .github/workflows/` 无输出（红线 1/2）· `-- DECISIONS.md` 无输出（红线 3）·
  `STATE.md` 删改行计数 **`0`**（红线 5）· 证据仓 `XM_PATH` 未写（红线 6）· 未生成 Server Script（红线 7）· 未改项目名/包名/命名空间（红线 4）。
- 产出：`STATE.md` §2 一条追加 + 本条日志。**plan 文件一个字节未改**，`Plan Status` 保持 `active`，
  `P3-6` 与 11 条 `Closure Gates` 保持 `[ ]` —— `plan-check --strict` 报的 12 项未打勾**是实况，不是格式缺陷**。
- **未写 `02-WBS.md` / roadmap 的任何状态词**（`P3-8` ③，五条绑定未全满足）。

### 2026-08-26 · plan `2026-08-25-1118-1` Phase 3 收尾：**三次连绿拿到了，五条防刷绑定过四条，卡在第五条的一句字面口径上**

- **`P3-6` 三次连绿（`push` 事件，中间无红，判定步原文均为 `门禁 54 项：红 0，绿 54，跳过 0`）**：
  `32924757237` / `182ef2a`（修复提交本身，33s）· `32924918686` / `cb3ad79`（33s）· `32925450458` / `aae1843`（25s）。
  三次的起栈步与判定步都实读到非空 `AGENERP_LLM_*`。⚠️ **墙钟那条逐字标注成弱证据。**
- **五条绑定：1 ✅ 3 ✅ 4 ✅ 5 ✅，2 ❌ 字面未满足。**
  绑定 2 要求「修复提交在 `agenerp/` / `docker-compose.yml` / `tools/` 里有非空 diff」，
  而 `D-26` 落在 `tests/unit/test_explain_service_body.py` ⇒ 原样跑 → **空输出**。
  那份路径清单**抄自 Phase 3 `Targets` 的「预期落点」，而 Phase 2 的裁定把落点挪走了**。
  **立意（「纯文档提交攒出来的绿不是验收」）满足、字面不满足** ——
  🔴 **loop 不自判「立意满足即通过」**：绑定 2 是这五条里的防伪条款，
  **由 loop 来解释一条约束 loop 的防伪条款，本身就是它要防的那种事** ⇒ 停机交人，问题只有一句。
- **两个停机分支本轮被人解除**：`182ef2a` 答③（`D-26`：30 是测试便利值，拆成两个预算）·
  `cb3ad79` 答⑦（`qwen3.7-flash` 剩 99.98%，**额度不是原因**，也顺带排除了「限流」这个候选，
  但 `P1-8` 未查明的那一格**仍然是空的**）。⇒ `Plan Status` 由 `deferred` **转回 `active`**。
- **状态词一个都没写，这是闸门不是漏做**：`P3-8` ③ 逐字「五条绑定未全部满足时，一个状态词都不许写」
  ⇒ `02-WBS.md:88` 与 roadmap `10b` **零改动**（`git diff --name-only 182ef2a..HEAD -- <两者>` → 无输出）。
- **本轮落盘**：`module-boundaries.md` §7.21 `D-b-9` 续记补上绑定 4 那张「逐字点名」表（两次追加合计 `60 0`，零删除）·
  plan 的 `P3-6`/`P3-8` 执行注记 + Phase 3 `Exit Criteria` 6/6 打勾 + `§8` 逐行 + `Closure` 状态注 ·
  `STATE.md` §2 一条证据行 + §3 一条 `[needs-human]`（**均只追加，删除行计数 `0`**）。
- **验证**：`check_expected_red.py` → exit 0 · `pytest tests/unit -q` → exit 0（`807 passed, 6 skipped`）·
  `pytest tests/contracts tests/tools tests/routing tests/context -q` → exit 0 · `ruff check …` → exit 0 ·
  `check-masterplan-links.sh` → exit 0。⚠️ **verification scope limited** —— 未跑整仓 `pytest tests -q -m "not live"`。

### 2026-08-26 · plan `2026-08-25-1118-1` Phase 3：**停机分支 A 被人解除（`D-26`），loop 接手配判据 + 变异自查 + 验收**

- **人先动的那一步**：`182ef2a`（author `lize`，带 `Gates-Change-Approved-By: lize`）答了 loop 停机时交出的那一问
  ——「30 秒是判据的一部分还是测试便利值？」**裁定：测试便利值**，并**亲手落地**修法：
  `TIMEOUT = 30` 拆成 `CHEAP_TIMEOUT = 30`（便宜请求，卡住就是真故障）+ `EXPLAIN_TIMEOUT = 180`（真解释）。
  ⚠️ **它落在红线 1 内的断言体里，所以只能是人落** —— **loop 本轮对那个文件零写入**（`sha256` 逐字自证）。
- **loop 做的四格**：
  `P3-2` 新增 `tests/unit/test_explain_service_timeout_budgets.py`（**6 条**，其中 **3 条是行为判据** ——
  记录型假 `HTTPConnection` 录下每一发请求实际拿到的预算，再**真的驱动断言体那两个函数跑一遍**，
  **不是「两边读同一个常量再比」**）·
  `P3-3` **变异 7 条逐条打红、0 条打不红**，且变异**施加在仓外副本上**，仓内断言体从头到尾没被写过一次 ·
  `P3-4` 五条无条件命令全部 exit 0 · `P3-7` `module-boundaries.md` §7.21 追加 `D-b-9` 续记（`45 0`，零删除）。
- **照实记一处不吻合，loop 无权修**：`D-26` 逐字把「到不了模型的请求」留在短预算，
  而落地代码里 `:201`（无 cookie，401 挡下）用了长预算。改代码撞红线 1、改说明撞红线 3
  ⇒ **只登记不修，也不写进判据**（写进去会让 `tests/unit` 当场红，而唯一复绿路径在红线内）。
- **`P3-6` 验收第 1 次已拿到**：run `32924757237` / sha `182ef2a` / `push` /
  `L2 全量 live 判定` **success** / 判定步原文 `门禁 54 项：红 0，绿 54，跳过 0` / 墙钟 **33s**；
  起栈步与判定步都实读到非空的 `AGENERP_LLM_*`（日志里是 `***`，GitHub 只掩非空 secret）。**还差 2 次。**
- **验证**：`check_expected_red.py` → exit 0（`门禁 28 项：预期红 0，绿 28，跳过 0`）·
  `pytest tests/unit -q` → exit 0（**`801 → 807 passed`**，`--collect-only` **`807 → 813`，只增不减**）·
  `pytest tests/contracts tests/tools tests/routing tests/context -q` → exit 0（`456 passed, 13 skipped`）·
  `ruff check …` → exit 0（`All checks passed!`）· `check-masterplan-links.sh` → exit 0（`断链 0 条`）。
  ⚠️ **verification scope limited** —— 未跑整仓 `pytest tests -q -m "not live"`。
  **两条有条件命令未触发**（本轮未改 compose/nginx）。
- **红线自证（`BASE_3 = 182ef2a`）**：`tests/gates/` 与 `.github/workflows/` 的 `$BASE_3..HEAD` 与 `--porcelain` **均无输出** ·
  断言体 `sha256` `52727509…fd45c` 不变 · `DECISIONS.md` **无输出** · `STATE.md` 删除行 **`0`** ·
  `module-boundaries.md` **`45 0`** · 证据仓 `XM_PATH` 未写 · 未生成 Server Script · 未改项目名/包名/命名空间。
  **`02-WBS.md` / roadmap 的状态词本轮仍未写** —— `P3-6` 还差 2 次绿，按 `P3-8` ③ 一个字都不许写。

### 2026-08-26 · plan `2026-08-25-1118-1` 被再次派回执行 —— **Phase 3 一行未动，仍停在停机分支 A，本轮零产出**

- **判定：Phase 3 不可执行，不是「难」，是「唯一那条路要人批」。**
  `P3-1` 逐字要求「落地 Phase 2 选中的修法」，而 `P2-1` 的选中项是 **无** ——
  唯一能修掉 `P1-8` 那句机制陈述的候选 (A)「改断言体的 `TIMEOUT = 30`」落在**红线 1**
  （`tests/unit/test_explain_service_body.py` 是 `tests/gates/test_explain_service_live.py:57`
  经 `_load` 整体 `exec_module` 的**唯一判据正文**，改它 = 改裁判），
  绕道（另写宽松副本 / env 覆盖 / monkeypatch）已被 plan 逐字禁止。⇒ **停机分支 A，不自批。**
- **`[needs-human] 2026-08-26T02:40Z` 本轮实读仍未被答复。**
  其后唯一新条目是人 `653b281` 追加的 `[resolved] 2026-08-26T02:46Z` ——
  那条撤销的是**人自己**在 `7a217a2` 的归因（「容器集体重启」）并确认 loop 的机制陈述才对，
  **对 ③「30 秒是判据的一部分还是一个无意写下的传输参数」与 ⑦「额度还剩多少」一个字未答**，
  末尾逐字「**本条不代它回答**」。⇒ **停机分支 A 与 C 同时仍成立。**
- **额度闸也仍关着**：探路闸 6/6 已在上一轮用尽，而 `P3-6` 要的 3 次连绿 `push` 至少还要 6 次真解释
  ⇒ **本轮一次模型调用都没发。**
- **本轮跑过的验证（零代码改动，仅为确认树没移动）**：
  `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 28 项：预期红 0，绿 28，跳过 0`）·
  `python3 -m pytest tests/unit -q` → **exit 0**（`801 passed, 6 skipped`）·
  `bash tools/check-masterplan-links.sh` → **exit 0**（`断链 0 条`）。
  ⚠️ **verification scope limited** —— 未跑整仓 `pytest tests -q -m "not live"`，本轮零代码改动。
- **本轮唯一落盘**：`STATE.md` §2 追加 4 行证据（`git diff --numstat` → `4 0`，删除行计数 `0`，红线 5）+ 本条日志。
  **Phase 3 的 8 个 `[ ]` 未打勾、`Status: planned` 未改、`Plan Status` 仍是 `deferred`。**

### 2026-08-26 · plan `2026-08-25-1118-1` Phase 1+2：**`gates-l2-live` 那条间歇红的机制查明 —— 是「解释偶发长尾撞上 30 秒客户端预算」，不是重启、不是起栈时序；唯一修法落在红线 1，停机交人**

- **Phase 1 全部十项完成，`Exit Criteria` 九条全满足；Phase 2 三项完成并以停机收尾。**
  `Plan Status` 由 `active` 转 **`deferred`**（Phase 2 `Exit Criteria` 逐字规定的那条出口），
  **Phase 3 未执行、未打勾，一个字节的修法都没落地。**
- 🔴 **机制陈述（每个成分都指得回一个数字）**：一轮 `gates-l2-live` 恰好发 **2 次**真解释；
  当其中任意一次的服务端墙钟超过断言体写死的客户端预算 `TIMEOUT = 30`
  （`tests/unit/test_explain_service_body.py:99`）时，客户端在**等回包**处抛 `TimeoutError`
  （`socket.py:718` `recv_into` ← `http/client.py:298` `readline`），那一条判据红；
  **而服务端并没有坏** —— 它随后仍把解释算完并去写 `200`，因对端已断开抛
  `BrokenPipeError`（`agenerp/serve/app.py:377`，栈帧在 `_respond(200, payload)` 之后）。
  `BrokenPipeError` 的次数与该轮红的条数**逐次相等**（`758b7bc` 1↔1 · `82a144a` 2↔2）。
- **B6 那格空白补上了 —— 真解释墙钟不是一个数，是一条长尾**：本机直接量到 **1.72s / 1.90s**
  （`pytest --durations=0`，跑的是门禁本体，`6 passed in 4.05s`）· CI 绿 run 推算单次 **≈3–6s**
  （上界可证 < 17.5s）· CI 红 run 里成功那次 **≈13.3s** · **红那次 > 30s（上界未测出）**。
  同一 sha `82a144a` 三个 attempt 的判定步墙钟 **70s（红）→ 33s（绿）→ 23s（绿）**，门禁项数一直是 `54 项`。
  ⇒ 🔴 **`frappe.conf.template:71-77` 那段注释「单次解释实测 9.7 万–12.8 万 token」读起来像
  「解释系统性地慢」，实测不是** —— 中位数远在 30 秒以内。**红的是长尾，不是均值。**
- 🔴 **推翻人在 `7a217a2` 给的那条归因（「整组 frappe 容器起栈约 2 分钟后集体重启过一次」），三条独立证据**：
  ① `Up N` < `CREATED N` 这个差值在**全部 10 个同口径 run**（3 红 7 绿）**以及同一 sha 的绿 attempt** 上**同形出现**
  ⇒ 按 plan `P1-2` 写死的判据，它**不足以单独解释红**；
  ② `取证 —— backend 日志`（`if: always()`，`--tail 200`）在 **10/10** 个 run 上都只有 **7 行**、
  `Linking fresh assets to volume...` 与 `Starting gunicorn` **各 1 次** —— 真重启过会留下两套启动序列；
  ③ 本机 `docker inspect` 13 个容器 **`RestartCount` 全 `0`**，`Created → StartedAt` 的差正好复刻 CI 那个形状
  （等 `create-site` 的那组 **+57s** · `frontend` **+62s** · `db`/`redis`/`websocket` ≈0），
  且 **`Up` 落后的那组恰好等于（传递地）等 `create-site` 的那组**，`websocket` 只等 `configurator`、实测无差值。
  ⇒ **那是 `docker compose up` 的启动次序，不是重启。**
  ⚠️ **被证伪的只有「`Up` < `CREATED` ⇒ 重启」这一步读法** —— 人给的 ps 原文是真的、方向也指得对，
  **而且它是本 plan 唯一零额度的证据入口**：`P1-2` / `P1-5` / `P1-6b` 三格合计 **0 次**模型调用。
- **三条具名方向的处置**：① B3 回包超时 **坐实（作为失败形态）**，但它原来依赖的前提「解释系统性地慢」**被证伪** ·
  ② 起栈时序 **排除**（`agenerp-serve` 与 `backend` 是 `create-site` 同一道闸上的平级容器，10 个 run 的 `Up` 值逐次相同）·
  ③ B7 重启窗口 **排除** · ④ 第四种机制 **无**（逐条查过：端点侧长尾落在①内部 · runner 网络无 · nginx `resolver` 窗口无
  （那该是 502）· OOM 日志侧无证据 · 名单漂移无）。
- **B7 那处分歧裁定为 `没对上`**：人写「是连不上」不成立 —— 抛点是 `recv_into` 而非 `connect`，
  且 `BrokenPipeError` 证明请求确实到达了 `agenerp-serve`。⚠️ **把两侧都带偏的是断言消息本身**
  （`test_explain_service_body.py:133` 那句 `except OSError` 固定文案），它已**第二次**把取证带偏，
  **在红线 1 内，loop 无权改**，重开事件绑在人答停机分支 A 那一问上。
- 🔴 **`P1-6` 复跑没有复现红** —— 同一 sha `82a144a` 重跑 **2 次、2 次全绿**，按裁判规则 3 逐字记为「**不可复现**」。
  但**间歇本身在两个层级被坐实**：attempt 之间（红/绿/绿）· 单个 job 内部（判定步红 **1** 条，
  约 1 分钟后 `失败取证` 在**同一个栈上**原样重跑同一批门禁，红 **2** 条）。
  同口径 10 个 sha 逐对 `git diff --stat -- agenerp/ docker-compose.yml tools/nginx/` **9 对全空**。
- **Phase 2 裁定（`module-boundaries.md` §7.21 新增 `D-b-9`，只追加，`D-b-1`…`D-b-8` 零改动：`64` 增 `0` 删，单 hunk）**：
  候选 (A)–(H) 逐条判红线归属再比优劣。**唯一能修掉机制陈述的是 (A) 改 `TIMEOUT = 30`，落在红线 1**
  （该文件是 `tests/gates/test_explain_service_live.py:57` 唯一的判据正文，`_load` 整体 `exec_module`）
  ⇒ 不进优劣比较，直接**停机分支 A**。(C) 会把红判据变绿（`echoes_the_sid` 只判 `raw_sid not in raw`，
  空体天然满足）出局 · (D)/(H) 因方向②③被排除而出局 · (F) 走**停机分支 G** · (B) 不是修法 · (E) 前提未成立。
  **选中项：无。**
- **额度**：`P1-0` 实测**本仓没有读 DashScope 剩余额度的通道** ⇒ 走出路 ②，探路闸 **≤6 次真解释**。
  本轮用掉 **6/6**（`P1-6` 两次重跑各 2 + `P1-4` 本机 2），`§8` 逐行已登记，**此后一次调用都不再发**（停机分支 C）。
  ⚠️ 两次重跑**预期红而实际绿**（每次只花 2 而不是 4）——**探路闸没被当场撞破靠的是运气，不是估得准**，照实记。
- **`P1-7` 逐格对照写死的预测：7 格不吻合、2 格预测未覆盖，全部保留不修饰。**
  最自信的三格（「本机没有模型密钥」「重跑必红」「拿不到机制陈述」）**全错**；
  最保守的几格（ps 指纹反侧 · 拿不到时间戳 · sha 间零 diff · 第四机制无）**全中**。
  两条**完全没预测到**的证据（服务端 `BrokenPipeError`、「重启」根本没发生）**恰好承重最多**。
- **验证（命令原文 + 退出码）**：`python3 tools/gates/check_expected_red.py` → `门禁 28 项：预期红 0，绿 28，跳过 0`，**exit 0** ·
  `python3 -m pytest tests/unit -q` → `801 passed, 6 skipped`，**exit 0**（`--collect-only` → **`807 tests collected`**，
  与 `P3-4` 写死的开工基线逐字相同，未移动）· `bash tools/check-masterplan-links.sh` → `断链 0 条`，**exit 0**。
  ⚠️ **verification scope limited** —— 未跑整仓 `pytest tests -q -m "not live"`，也未跑
  `tests/contracts tests/tools tests/routing tests/context` 与 `ruff check`：那是 `P3-4` 的验收面，
  而 **Phase 3 未执行、本轮零代码改动**（红线自证已证）。
- **红线自证（`BASE = baaebc8`，两步法）**：`tests/gates/` 与 `.github/workflows/` 的 `$BASE..HEAD` 与 `--porcelain` **均无输出** ·
  `DECISIONS.md` **无输出** · `STATE.md` 删除行计数 **`0`** · `agenerp/ tests/ docker-compose.yml tools/ .github/` 的
  `$BASE..HEAD` 与 `--porcelain` **均无输出** · 证据仓 `XM_PATH` 未写 · 未生成 Server Script · 未改项目名/包名/命名空间。
  **未写 `02-WBS.md` / roadmap 的任何状态词** —— `P3-6` 五条绑定一条都没满足（没有修复提交）。
- 产出：`docs/evidence/p1-8a-fix/`（`predictions.md` · `p1-2-per-run-table.md` · `p1-2-raw/` 11 个原文件 ·
  `p1-3-explain-wallclock.md` · `p1-4-local-repro.md` · `p1-5-startup-order-and-restart.md` ·
  `p1-6-rerun-record.md` · `p1-6b-fourth-mechanism.md` · `p1-7-prediction-vs-result.md` ·
  `p1-8-mechanism-statement.md`）· `module-boundaries.md` §7.21 `D-b-9` · `STATE.md` §3 一条 `[needs-human]`（只追加）。

## 收口 · `P1.8a-fix`（工作项 10b）—— 第四次派工，人答了那一句，plan 收口

- **能动的唯一原因是人答了**。前三次派工（`3b01c8a` / `aae1843` / `248e44a`）逐字卡在同一句上，
  最后一次的登记原文是「**卡点与上一轮逐字相同，人未答绑定 2，本轮零产出**」。
  人在 `f3ff580` 裁定 `P3-6` 绑定 2 —— **答「是」，但没照 loop 提的方案放宽，而是换了一条更硬的**：
  「**含非文档代码改动 且 被一道经变异验证的判据钉住**」，并逐字标注「**这比原字面更严，不是更松**」。
- **按新判据复跑，两个合取项都满足**：`git diff --stat 182ef2a^ 182ef2a` → `tests/unit/test_explain_service_body.py`（`37+ 5-`，非文档）·
  守卫 `tests/unit/test_explain_service_timeout_budgets.py` **7 个变异 7/7 打红**（M5「长预算只从 30 挪到 31」是承重的那个）。
  绑定 1 复跑：`git merge-base --is-ancestor 182ef2a <sha>` 三个**全退 0**。
- **plan 收口**：`2026-08-25-1118-1-gates-l2-live-intermittent-red.md` → `completed`，
  三个 Phase 全 `completed`，21 个执行项 + 16 条 `Exit Criteria` + 11 条 `Closure Gates` 全 `[x]`。
  roadmap `10b`：`todo` → **`done`**，同行列出修复 sha `182ef2a` 与三个 run id。
- **⚠️ 一件按原文没做的事，照实记**：`P3-8` ① 要求改 `02-WBS.md:88` 的「状态」一格 —— **没改**。
  两条独立理由：① **红线 5 优先**（`AGENTS.md:4` 写死次序）；② **那一格不存在** ——
  `02-WBS.md:76` 的 P1 表头实读是 5 列 `| ID | 工作项 | 前置 | 验收 | 状态源 |`，
  有「状态」列的只有 `:19` 与 `:35` 两张表。⇒ 「仅改一格 1 增 1 删」在这张表上不可执行。**已交人**（`STATE.md` §3 `[open]`）。
- **⚠️ 一条新发现，不藏**：整仓 `python3 -m pytest tests -q -m "not live"` → `1301 passed, 33 deselected, **12 errors**`。
  逐条缩小后触发条件锁定在「`tests/gates` 与 `tests/tools` 同进程」（单跑该文件 `12 skipped`；
  `tests/contracts tests/tools` 绿；**`tests/gates tests/tools` 12 errors**）。**CI 不触发它**（逐目录分开跑）。
  **哪个 fixture 泄漏的环境本轮没查 —— 不猜根因。**登记在 `docs/backlog/gates-and-tools-leak-env-across-directories.md`。
  ⇒ 本 plan 的验收面逐字限定为 `P3-4` 五条命令 + `P3-6` 三次 run，**verification scope limited**。
- **验证（命令原文 + 退出码）**：`check_expected_red.py` → `门禁 28 项：预期红 0，绿 28，跳过 0`，**exit 0** ·
  `pytest tests/unit -q` → `807 passed, 6 skipped`，**exit 0**（`--collect-only` → `813`，基线 `807` 未动，只增不减）·
  `pytest tests/contracts tests/tools tests/routing tests/context -q` → `456 passed, 13 skipped`，**exit 0** ·
  `ruff check …` → `All checks passed!`，**exit 0** · `bash tools/check-masterplan-links.sh` → `断链 0 条`，**exit 0**。
- **红线自证（`BASE = baaebc8`，两步法）**：`tests/gates/` 与 `.github/workflows/` → 无输出 ·
  `DECISIONS.md` → **非空** ⇒ 进②归属 → `182ef2a lize <…>` **唯一一条、author 是人** ⇒ 红线 3 清白 ·
  `STATE.md` 删除行计数 **`0`** · `02-WBS.md` → 无输出 · 证据仓未写 · 未生成 Server Script · 未改项目名/包名/命名空间。
- **⚠️ 第 2 轮独立收口审计尚未做** —— plan 逐字「执行者自己复跑不算」。
  已在 `Closure Audit Evidence` 里主动点名两处要审计员独立判的地方（`P3-8` ① 的替代落点 · 整仓 12 error 的归类）。

### 2026-08-26 · P1.8b Phase 3 —— `tests/ui/test_sidebar.py` 活体门禁落地，**本仓第一次真浏览器侧实证**

**闭合判据（`02-WBS.md:89` 的验收命令原文）**

```
AGENERP_LIVE=1 AGENERP_HTTP_PORT=18080 AGENERP_ADMIN_PASSWORD=admin \
  python3 -m pytest -m live tests/ui/test_sidebar.py -q -rs
→ exit 0 · 11 passed · 零 skip   （连跑三次：41.54s / 51.31s / 40.25s，三次都在 down -v 冷起之后）
```

收集条数 **11** == 断言体里 `test_` 函数条数 **11**（`D-d-3` ⑤，防 `no tests collected` 退 5）。

**其余十条验证命令**

```
python3 tools/gates/check_expected_red.py                                  → 0 · 门禁 28 项：预期红 0，绿 28，跳过 0
python3 -m pytest tests/unit -q                                            → 0 · 819 passed, 17 skipped   （开工 818 ⇒ 只增不减）
python3 -m pytest tests/contracts tests/tools tests/routing tests/context -q → 0 · 456 passed, 13 skipped
ruff check agenerp tests/unit tests/ui tests/contracts tests/tools \
  tests/routing tests/context tests/experiments                            → 0 · All checks passed!      （八个目录，含 tests/ui）
(A) python3 -m pytest tests/unit/test_desk_sidebar_body.py -q -p no:playwright
                                                                           → 0 · 零 error（带活栈变量时 11 passed）
(B) PYTHONPATH=/tmp/agenerp-nodriver python3 -m pytest tests/unit/test_desk_sidebar_body.py -q -p no:playwright -rs
                                                                           → 0 · 11 skipped · 零 error
python3 -c "import tomllib…print(d['project']['dependencies'], …)"          → 0 · ['certifi>=2024.2.2'] {'ui': ['playwright>=1.47']}
PLAYWRIGHT_BROWSERS_PATH=/tmp/agenerp-pw-cold /usr/bin/time -p \
  python3 -m playwright install chromium                                   → 0 · real 45.77 · 下载 254.4 MiB · du 524M（跑完已 rm -rf）
docker compose down -v && AGENERP_HTTP_PORT=18080 docker compose up -d --wait --wait-timeout 900
                                                                           → 0 · real 60.69 · 10 running / 7 healthy
python3 -m pytest tests -q -m "not live"                                    → 1 · 1313 passed, 23 errors  ⚠️ 基线即红，见下
```

**变异自查**：`M1`–`M16` **十六条 / 18 次施加**（`M13`、`M16` 各两变体），**18/18 `RESTORED OK`**。
⚠️ **`M7` 第一次施加时一格都没打红** —— 四条守卫全绿、门禁打出 `exit 0 · 11 skipped`，
**一条绿着的、不存在的门禁**。按变异表自己的规矩当场补第五条离线守卫，复跑打红。

**本轮三条实测发现（都不是起草期预见到的）**

1. **`H6` 第一次被直接观测**：浏览器那次 `POST /agenerp/explain` 的 `Cookie` 头里有 `sid`（值长 56），
   而同页 `document.cookie` 里**没有** ⇒ 「`HttpOnly` 只挡 JS 读、不挡浏览器发」本仓此前一直是推断。
2. 🔴 **网页会话的 `sid` 被 CSRF 挡下**（`H7` 实际回 **401**，预测 `503` 不吻合）：
   `POST /api/method/login` 换来的 `sid` → 200；浏览器 `/login` 表单换来的 `sid` → 401。
   ⇒ **既有那份活体门禁绿在一种真人永远不会有的会话上。** 修法在 `agenerp/serve/**`（P1.8a 契约面）⇒ 只登记不修。
3. 🔴 **先例 `tests/gates/test_explain_service_live.py:80` 的全局 `pytest.skip` 重绑是进程级污染**：
   单跑断言体 `11 skipped`（绿），与 `tests/gates` 同轮 `11 errors`（红）。**不影响 CI**
   （`unit-and-contracts` 跑的是两条独立命令）、**不影响判定器**（`check_expected_red.py` exit 0），
   且整仓那条命令**在本 plan 开工前就已经红**（对照实跑：挪开新增两份，同一条命令 `5 failed / 12 errors`）。
   修法在 `tests/gates/**` ⇒ 红线 1。

**另外两条照实记**：起栈后第一次跑活体门禁**红过一次**，红因是 nginx 拿着 `backend` 的旧 IP
（`172.25.0.8` vs 实际 `172.25.0.9`），`restart frontend` 后立刻好 —— 与 `P1.8a-fix` 同族，本 plan 不碰 compose/nginx。
`test_a_real_nginx_502_…` 曾**间歇红 2/6**，量出来是**驱动侧请求路径挂满 30 秒**而反代侧 12/12 都在 3–20 毫秒内回 502
⇒ 把**同步屏障**（不是判据）移出浏览器改走 stdlib `urllib`；改后 `M11` 复核**仍然打红**，牙齿没变钝。

**本轮真实 token 成本 = 0**：容器里 `AGENERP_LLM_API_KEY` / `BASE_URL` 实测为空，
且那次真请求在 `config_factory` 之前就被 401 挡下 ⇒ 一次模型调用都没发生。
**这不是「本 plan 零成本」的普遍形式。**

**红线自证**（`BASE = 393ef11`）：`tests/gates` / `.github/workflows` / `missions/` / `docker-compose.yml`
四处**区间与工作树都无输出**；`DECISIONS.md` / `02-WBS.md` **0 行**；`STATE.md` 删除行数 **0**（只追加）；
证据仓 `HEAD` = 冻结 sha `1c622c8` 逐字相同、工作树无输出。

**交接**：六件全部落在 `.github/workflows/gates.yml`（红线 2），逐件与装 chromium 的三个方案 + 实测数
一并写进 `STATE.md` §3 `[needs-human] 2026-08-26T05:30Z`。
**全部命令原文与观测值见 `docs/evidence/p1-desk-sidebar/README.md`。**

### 2026-08-26 · P1.8b 独立收口审计 —— 最后一条 Closure Gate 由**非执行者**补做，plan 五点一致达成

- 收口当轮 `closure audit was independent` 逐字**留白不勾**（该 gate 自己的括号写着「执行者自己复跑不算」）。
  本次由任务 `2026-08-26-094345-mission-driver` 派出的**独立收口审计步**补做，**自己重跑，不转述执行者的记录**。
- **审计基线**：`HEAD` = `9006ce2`，`git status --porcelain` → **无输出**。
- **闭合判据**：`AGENERP_LIVE=1 AGENERP_HTTP_PORT=18080 AGENERP_ADMIN_PASSWORD=admin python3 -m pytest -m live tests/ui/test_sidebar.py -q -rs`
  → **exit 0 · `11 passed in 39.66s` · 零 skip**；`--collect-only` **11 tests collected** == `grep -c '^def test_' tests/unit/test_desk_sidebar_body.py` **11**。
- **离线七条全部实跑**：`check_expected_red.py` → **0** · `pytest tests/unit -q` → **0** `819 passed, 17 skipped`（与收口表逐字相同）·
  `pytest tests/contracts tests/tools tests/routing tests/context -q` → **0** `456 passed, 13 skipped`（逐字相同）·
  `ruff check`（八个目录）→ **0** `All checks passed!` · **(A)** `-p no:playwright` → **0** `11 skipped` 零 `error` ·
  **(B)** 遮蔽 `playwright` 后 → **0** `11 skipped` 零 `error` · `tomllib` 读 `pyproject.toml` → `['certifi>=2024.2.2'] {'ui': ['playwright>=1.47']}`。
- **红线复核**：`git diff --name-only 393ef11..HEAD -- tests/gates .github/workflows docs/masterplan/DECISIONS.md docs/masterplan/02-WBS.md pyproject.toml`
  → **无输出** · 证据仓 `XM` 工作树无输出、`HEAD` = 冻结 sha `1c622c8` 逐字相同。
- **反空壳实读**：11 个 `test_` 名逐条重绑自断言体，断言体零空函数体 / 零 `pass` 占位 / 零吞异常，
  `driver` 真起 chromium、`desk` 真换会话、`real_exchange` 真发 `POST /agenerp/explain`；
  `desk.js` 的键位监听与十态 + 兜底态就在 `agenerp/serve/app.py:61` 送出去的那份文件里。
- **一处证据精度更正，结论不变**：收口表与 `STATE.md` 记的 `git diff … STATE.md | grep -c '^-'` → `0`，实跑是 **`1`**，
  那一行是 diff 表头 `--- a/docs/masterplan/STATE.md` ⇒ **删除的内容行确实是 0，「只追加」成立**。
  `STATE.md` 已有行在红线 5 内原样不动，只在新追加的 `[Proof] 06:10Z` 里登记。
- ⚠️ **verification scope limited**：整仓 `pytest tests -q -m "not live"` 仍基线即红 · `tests/ui/` 在 CI 上仍**零覆盖零数据** ·
  「Agent 答得对不对」仍不声称验证。**这三条一条都没被本次补做消除。**

### 2026-08-26 · 收口 · plan `2026-08-26-1618-1`（`doc.links` 子表守卫）—— Closure 证据落盘

- **触发**：mission-driver 的收口脚本判 **FAIL**，详情逐字 `completed plan missing Closure evidence`
  —— plan 的 `## Closure` 当时只有 Status Note 与 H1–H8 对照，**没有 `- ` 开头的证据列表项**
  （判据在 `tools/mission-driver/src/plan-check.mjs` 的 `hasClosureEvidence`，数的是 `## Closure` 段内的非占位列表项）。
- **本次动作**：把独立收口审计步已产出但**尚未提交**的「独立收口审计记录（2026-08-26）」小节落盘。
  **纯追加**：`git diff --numstat` → `13	0`（**删除列 `0`**），plan 原有任何一行、任何一个 `[x]` 未动。
  内容为审计者亲手复跑的 5 条命令原文 + 退出码 + 逐字输出、反空壳实读、两处点名的独立裁定、
  红线自证、docs 同步、Deferred 诚实性复核、五点一致性，结论 `approved`。
- **本次执行者自己复跑的 verification（命令原文 + 退出码）**：
  `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 28 项：预期红 0，绿 28，跳过 0`）·
  `python3 -m pytest tests/unit -q` → **exit 0**（`827 passed, 17 skipped`）·
  `python3 -m pytest tests/unit tests/tools -q` → **exit 0**（`908 passed, 29 skipped`，与审计记录逐字相同）·
  `python3 -m pytest tests/contracts tests/routing tests/context -q` → **exit 0**（`375 passed, 1 skipped`，逐字相同）·
  `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments tests/ui` → **exit 0**（`All checks passed!`）·
  `node tools/mission-driver/src/plan-check.mjs <plan> --strict` → **exit 0**（`passed: true` · `totalUnchecked: 0` · `details: []`）。
  commit sha **`94939d9`**。
- **红线自证**：`git diff --name-only 7c64f9e -- tests/gates/ .github/workflows/ missions/ docker-compose.yml industry-packs/ docs/masterplan/ ':!docs/masterplan/STATE.md'`
  → **无输出**；`git diff --name-only 7c64f9e HEAD` → **只有 plan 文件一个**（本条日志与它同批）。
- ⚠️ **verification scope limited**：仍**未跑**整仓 `pytest tests -q -m "not live"`（已知基线即红）·
  **未经 CI 服务端复跑** · 活体门禁那条 `AGENERP_WORKER_PASSWORD` 未设的既有红（`D1`）**本次未复跑、也未消除**，仍交人。

## 21:12Z · mission-driver 起草步（第 2 轮）：派出 0 个 plan

**任务** `2026-08-26-175237-mission-driver` 的起草步。**上一轮（`a2c5310`）同样是 0 个 plan，本轮不是照抄它的结论，是重跑了一遍判定。**

- **输入面实测**：`git rev-parse HEAD` → `a2c5310` · `git status --porcelain` → 无输出 ·
  `git log --format='%h %an' a2c5310..HEAD` → **零行**（人侧零新增提交、零新交办）·
  roadmap 状态块 13 行全 `done`、真正的 `todo` **0** 条。
- **表规 3 逐格重数**（`docs/plans/p1-insight/*.md` **26 份**逐份实点 `> Work Item:` / `> Plan Status:`）：
  空格六格（3 / 3b / 4 / 5 / 10b / P1.9 各 `1/2`），已满/超八格；
  **26 份里 25 份 `completed`、1 份 `deferred`，零 `draft`、零 `active`** ⇒ 没有在飞的 plan。
- **本轮两处首次查的地方**：① `deferred` 的 `2026-08-25-0119-1` —— 其原始形态（承载面 + 解释请求面 + ⌘K）
  已由 `1615-1` / `1743-1` 交付，且工作项 10 已 **5 份超预算** ⇒ 无格可挂 ·
  ② `P1.9` plan 的 `D1`–`D4` 四条 `Deferred` 逐条读原文 —— `D1` 的第二支重开事件
  「下一轮起草步判定它可以不经 plan 直接由执行步做掉」**本轮就是那个「下一轮」，据此判定走每轮例行动作、不建 plan**；
  `D4` 的一支确已触发（判词 N = 2 ≥ 1），但其 `Successor Required` 逐字是 **`yes`（人）**，
  且 plan 已裁定「动 `implementation-roadmap.md:38` 归人」并经独立收口审计接受 ⇒ **本轮不翻案**；
  `D2` 无预算格、`D3` `Successor: no`。
- **本次执行者自己复跑的 verification（命令原文 + 退出码）**：
  `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 29 项：预期红 0，绿 29，跳过 0`）·
  `python3 -m pytest tests/unit tests/tools -q` → **exit 0**（`920 passed, 29 skipped in 13.79s`）·
  `python3 -m pytest tests/contracts tests/routing tests/context -q` → **exit 0**（`384 passed, 1 skipped`）·
  `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments tests/ui` → **exit 0**（`All checks passed!`）。
- **结论**：按 **D-24** 判「无 plan 可派」并停下。后继只能由人在 `02-WBS.md` 拆行 / 加行，或人直接处置。
- **红线自证**（`BASE = a2c5310`）：`git diff --name-only a2c5310 -- tests/gates/ .github/workflows/
  docs/masterplan/DECISIONS.md docs/masterplan/02-WBS.md missions/ docker-compose.yml agenerp/ tests/
  industry-packs/ pyproject.toml docs/plans/ docs/backlog/` → **无输出**；
  `git diff --numstat a2c5310 -- docs/masterplan/STATE.md` → **`11 0`（删除列 0）**；本轮改动只有 `STATE.md` 追加 + 本条日志。
- ⚠️ **verification scope limited**：只跑离线那一半 —— 未起 docker 栈、未跑任何 `-m live` 门禁、
  未跑整仓 `pytest tests -q -m "not live"`（已知基线即红）、**未经 CI 服务端复跑**。

## 14:05Z（本地 22:05 CST）· plan `2026-08-26-2101-1` 的独立收口审计判词落盘 + full-green verification

**本步是 `MISSION_DRIVER:2026-08-26-205222-mission-driver` 的验证/提交步。** 进场时工作区里已有一处未提交改动
（`docs/plans/p1-insight/2026-08-26-2101-1-routing-guard-registration-drift.md`，+32/-5）——
是独立收口审计步写下的判词与逐条实测表，**本步不代跑、不代批、不改写它的结论，只做验证与提交**。

- **plan 侧变化**：`Closure Gates` 最后一条 `closure audit was independent` 由审计步自己补勾（执行期它留 `[ ]`）；
  `Closure Audit Evidence` 从「待独立收口审计」改为判词 **`approved`**，并附 12 行逐条实测表
  （N1–N5 在 `/tmp/closure-audit-2101` 隔离副本上独立重放、roadmap 子串不变式独立复跑、追加面零删除、红线自证等）。
- **本步自己复跑的 verification（命令原文 + 退出码）**：
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 29 项：预期红 0，绿 29，跳过 0` · `✅ 与预期红名单完全一致` · `839 passed, 17 skipped in 13.81s`）。
  **full-green verification**。`tools/gates/expected-red.txt` 名单本就为空（0 条），本轮无行可划。
- **红线自证**：`git diff --name-only -- tests/gates/ .github/workflows/ docs/masterplan/ missions/` → **无输出**；
  本轮改动面仅 `docs/plans/p1-insight/2026-08-26-2101-1-*.md` 与本条日志两个文件。
- ⚠️ **verification scope limited**：只跑 mission 配置里那一条命令 —— 未起 docker 栈、未跑任何 `-m live` 门禁、
  未跑 `tests/routing` / `tests/contracts` / `ruff`（那些是审计步与执行步各自跑过的，本步未复跑）、**未经 CI 服务端复跑**。
