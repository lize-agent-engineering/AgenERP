# Development Log - 2026-08-25

### 2026-08-25 · plan `2026-08-25-1423-1`：**Phase 1 —— 七条裁定落 `module-boundaries.md` §7.21**

plan：[`docs/plans/p1-insight/2026-08-25-1423-1-explain-service-compose-and-same-origin.md`](../../plans/p1-insight/2026-08-25-1423-1-explain-service-compose-and-same-origin.md)

- **§0 十一处基线逐条重取，实读填进 §0.1**。十格吻合，**第 1 格不吻合且很重要**：
  工作区里已经有 `?? tests/gates/test_explain_service_live.py`（81 行）与
  `M tools/gates/expected-red.txt`（+14 行、六条判据名），**都是人做的**
  （逐字 `Gates-Change-Approved-By: lize`）⇒ §11 第一条 Deferred 的承接者**早于本 plan 动手了**。
  处置：那两处**本 plan 一个字不碰、也不 `git add`**；账本那 6 行**不划掉**
  （划掉的条件是「测试转绿」，而 §1.11 那条 skip 未拆之前它不会绿）。
  实测 `check_expected_red.py` 在 default 模式下仍是 `26 项：预期红 0，绿 26，跳过 0`、exit 0。
- **`D-b-1` 选 (A)**：在本仓维护一份 `frappe.conf.template` 副本（`tools/nginx/`），`:ro` 覆盖容器内那份。
  (B)/(D) 的否决**用执行期自己的实测**，不引起草期推测 —— 一次性探针容器（`--network agenerp_default`，
  未改动运行中的栈）实测：(D) → `[emerg] location "/agenerp/" is outside location
  "^/files/.*.(htm|html|svg|xml)"`，`nginx -t` 退 **1**；(B) → `[warn] conflicting server name
  "frontend" on 0.0.0.0:8080, ignored` 而 `nginx -t` 退 **0** ⇒ **第二个 server 块被静默丢弃**，
  失败形态是「配置测试全绿、反代根本不存在」。
  **(A) 与 (E) 逐条比价**：决定性的不是维护代价，是**可判定性** —— (E) 落盘的是一段会去改别的文件的 shell，
  判据⑧「`location` 必须在唯一那个 server 块之内」离线无从判起。
- **`D-b-2` 选 (i)** bind mount + `PYTHONPATH`；**挂载路径 / 上游端口 / 回程地址三处一律字面写死**
  （理由抄 `test_bootstrap_script_dir_is_mounted_literally`：仓根 `.env` 能在 `config` 时改掉插值，
  而判据是静态文本扫描）。`AGENERP_SITE` 是唯一例外，写成 `${AGENERP_SITE:-frontend}`。
- **`D-b-3` 选 (i)** `http://frontend:8080`，**字面写死**。逐字回答了「为什么不成环」
  与「为什么不能写成 `${AGENERP_SITE_URL:-…}`」——`gates.yml:399/403` 的 job 级 `env:` 会被 `:419`
  的 `up -d --wait` 继承，插值形式会让容器**打自己**，而 `/agenerp/health` 恒 200 ⇒ **绿着坏掉**。
- **`D-b-4`** 正式重开 §7.20 `D-a-1` 的残余风险：新变量 `AGENERP_SERVE_HOST`，
  **默认仍是 `127.0.0.1`**，只收 IP 字面量、非法值当场失败不静默回退。
  「容器内绑 `0.0.0.0` ≠ 对本机之外提供」的**唯一支点**是「compose 不发布该端口」，
  故它必须有静态判据守着。
- **`D-b-5`** 断言体默认基址改成与 `default_base_url()` 同一套三级解析（`Fix`，不许降级）；
  **边界写死**：改「去哪里判」是本 plan 的活，改 `:201` 那条 skip 的「判成什么」不是。
- **`D-b-6`** healthcheck 打 `/agenerp/health`，`start_period: 20s` 刻意**不抄 `backend` 的 60s`
  （那 60s 是给建站留的），`depends_on` 只有 `create-site`。
- **`D-b-7`** 风险档自评 **L1**（比 §7.20 `D-a-6` 的 L0 **高一档，照实记**：本 plan 让服务成为默认栈的一部分）；
  并裁定 `system-baseline.md` **§14.11** 就地追加**规则 ④**（判据要比对的 compose 值一律字面写死）。
- 红线自证：`git diff -- docs/masterplan/DECISIONS.md` → **0 行**。

### 2026-08-25 · plan `2026-08-25-1159-1`：**Phase 3 收口 —— 变异自查 M1–M11、🔴 断言体、H1–H7 逐格实测**

plan：[`docs/plans/p1-insight/2026-08-25-1159-1-explain-http-service.md`](../../plans/p1-insight/2026-08-25-1159-1-explain-http-service.md)（`Plan Status: completed`）

- **变异自查 M1–M11 逐条施加一次、记红点、复原**，一条没跳过、**一条都不需要现补断言**
  （复原后源文件逐字节比对 `RESTORED OK`）。红点表落 `module-boundaries.md` §7.20。
  **M1 与 M11 各打红 16 条**；**M11 是最值得看的一行** —— 它一个 `client_from_env` 都不用，
  走的是 `SiteClient(site, admin_password=credential_from_env(ADMIN_PASSWORD_ENV))` 这条
  **等价凭据回退**，挡住它的是「取件函数 + 四个 `*_ENV` 常量一并禁」外加
  「服务面不许自己构造 `SiteClient`」。
  ⚠️ **M10 只打红一条，照实记**：它在**请求解析那一层**就被允许清单挡掉了，
  根本走不到身份链 —— 这不是判据薄，但**两条防线是串联不是并联**，不粉饰。
- **🔴 断言体交付** `tests/unit/test_explain_service_body.py`（红线 1：**不创建** `tests/gates/**`）：
  六条经**同源前端**的活栈断言（反代通不通 / 无 cookie 401 / **伪造 `sid` 401 且不回退** /
  `user` 等于站点自己解析出的人 / 回包不回显 `sid` / 自带上下文被拒），
  写清了「人要把它按哪个路径加载、加载后跑什么命令、为什么现在还不能加载」，
  并点名了**人要做的那一处收严**：本文件够不到服务时 `skip`，门禁那份**必须改成 fail**
  （`gates-l2-live` 的契约是零 skip）。加载时机是**第 2 个 plan 落地的同一个提交**。
- **`STATE.md` §2 / §3 追加三条**（只追加，红线 5）：证据行 · `[open]` 门禁只能由人加载 ·
  `[open]` 事实行（`[open] 2026-08-25T00:35Z` 第 ① 项由 `D-a-3` 实质关到哪一步，**处置权仍在人**）。
- **§6 的 H1–H7 七格逐条填实测，预测原文一个字未改：七格全部吻合。**
  ⚠️ H2 **落点比预测更靠前**（抛点在进 `route()` 之前），照实标出；
  H7 另补一条：`git status -- agenerp/` 还有一行 ` M agenerp/site.py`，那是悬空引用的字面修正、
  **不是新增文件**。
- ⚠️ **`docs/backlog/p1-insight-roadmap.md` 工作项 10 刻意仍是 `todo`**：表规 3 的预算是
  1–2 个 plan，**第 1 个已收口、第 2 个未起草**；验收要的活栈 + nginx 同源属第 2 个 plan。
  记成 `done` 会让引擎跳过第 2 个 plan —— 那才是漏。该行已写明为什么不是漏改。
- ⚠️ **`closure audit was independent` 留白**：本轮执行环境不具备独立子代理，
  执行者自己复跑不算独立审计。处置同 P1.4–P1.7 的先例，由后续一轮补做。
- **验证（全绿）**：`check_expected_red.py` → exit 0（`门禁 26 项：预期红 0，绿 26，跳过 0`）·
  `pytest tests/unit -q` → **`756 passed, 6 skipped`**，exit 0 ·
  `pytest tests/unit -q -m "not live"` → **`756 passed, 6 deselected`**，exit 0（CI 的 L1 那一轮看到的是这个）·
  `pytest tests/contracts tests/tools tests/routing tests/context -q` → `456 passed, 13 skipped`，exit 0 ·
  `ruff check ...` → `All checks passed!`，exit 0 · `git diff -- pyproject.toml` → 无输出。
  ⚠️ **verification scope limited**：未跑整仓 `pytest tests -q -m "not live"`，未经 CI 服务端复跑。

### 2026-08-25 · plan `2026-08-25-1159-1`：**Phase 2 —— `agenerp/serve/` 落地 + 十条离线判据**

plan：[`docs/plans/p1-insight/2026-08-25-1159-1-explain-http-service.md`](../../plans/p1-insight/2026-08-25-1159-1-explain-http-service.md)（`Plan Status: active`）

- **新增三个产品模块**（H7 预测 `= 3`，实测 `= 3`）：`agenerp/serve/app.py`（请求处理器 +
  `build_server(...)` 工厂，**七个外部依赖全部可注入**）· `agenerp/serve/__main__.py`
  （`python3 -m agenerp.serve`，端口读 **`AGENERP_SERVE_PORT`**、默认 **`8330`**，
  **不读** 站点端口 `AGENERP_HTTP_PORT`）· `agenerp/serve/__init__.py`。
- **十条判据落 `tests/unit/test_explain_service.py`**，假件落 `tests/unit/serve_fakes.py`
  （在 `explain_fakes` → `tests/tools/conftest.py` 的 `FakeSite` 上**再套一层 `sid` 认人**，
  形状取自 `E-a-1` 的活站点实测：有效 `sid` → 200 `{"message": ...}`，伪造/无 cookie → 403）。
  **每条判据都指名一个可观测量**：④ 落在**传给 `explain()` 的实参**上并用两个 `sid` 参数化；
  ⑧⑩ 按**目录遍历**扫 `agenerp/serve/**` 全部 `.py`（新模块自动进扫描面）且**各带一条自证有牙**；
  ⑨ 连带把「调用方能往日志里塞 query 串」那条路一起判了。
- **一处确认的引用漂移改直（`Fix`，不降级为 follow-up）**：`agenerp/site.py:499` 与
  `tests/unit/test_site_client_sid.py:301` 两处都指向 `test_explain_service.py` **判据⑩**，
  而服务面的凭据 AST 扫是**判据⑧** ⇒ **文件名与编号一起改**，
  `grep -rn "test_explain_service.py" agenerp/ tests/` 逐条复核过。
- **两处执行期实测得来的调整，照实记**：① 假件原来的答案文本报了数量，会被 ② 门禁 L3 拦下
  ⇒ 换成不报数量的答案，否则「服务把答案交出来了」与「门禁拦住了」在服务面判据里混成一件事；
  ② `call_method()` 一律走 POST ⇒ **动词分不出读写**，判据⑩ 改为按方法名点名只读白名单
  （`frappe.auth.get_logged_user` / `frappe.client.has_permission`）。
- **`module-boundaries.md` §7.20 追加「交付的形状」与「引用漂移已改直」两小节**（追加式，不覆写）。
- **验证（全绿）**：`python3 tools/gates/check_expected_red.py` → `门禁 26 项：预期红 0，绿 26`，exit 0 ·
  `python3 -m pytest tests/unit -q` → **`756 passed`**（基线 697，**只增不减**），exit 0 ·
  `python3 -m pytest tests/contracts tests/tools tests/routing tests/context -q` → `456 passed, 13 skipped`，exit 0 ·
  `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments` → `All checks passed!`，exit 0 ·
  `git diff -- pyproject.toml` → **无输出**（H6 吻合）。
  ⚠️ **verification scope limited**：未跑整仓 `pytest tests -q -m "not live"`，未经 CI 服务端复跑。

### 2026-08-25 · plan `2026-08-25-1159-1`：**Phase 1 —— 请求面的形状与身份链（七条 Decision + 一次活站点只读探针）**

plan：[`docs/plans/p1-insight/2026-08-25-1159-1-explain-http-service.md`](../../plans/p1-insight/2026-08-25-1159-1-explain-http-service.md)（`Plan Status: active`）

- **§0 基线八处逐条重取**，实读值填进 plan §0.1。⚠️ **一处不吻合照实记**：
  `git status --porcelain` **不是无输出**，唯一那行是 plan 文件自身（未 add），
  产品代码/判据/文档面零脏。另**补出一条起草期未写的事实**：未知任务类目抛的是
  `DeclarationError`，而它是 `RoutingError` 的**子类** ⇒ 直接改变了 `D-a-4` 的 400/502 分格。
  基线：`门禁 26 项：预期红 0，绿 26` · `697 passed`。
- **`E-a-1` 活站点只读探针跑成了**（栈 `Up 27 hours`，未触发「不可复现」停机分支），
  记录落 [`docs/analysis/2026-08-25-1159-explain-service-sid-probe.md`](../../analysis/2026-08-25-1159-explain-service-sid-probe.md)。
  八行实测：有效 `sid` → **200 `{"message":"Administrator"}`**（`GET` 与 **`POST`** 两种动词同形，
  后者才是 `call_method()` 实际发的）· 伪造 `sid` 与不带 `Cookie` → **均 403**（**不是 200 空包**）·
  有效 `sid` 读 `/api/resource/Item` → **200** ⇒ `D-a-3` 的 (iii) 在活站点上可行。
  **零 `sid` 落盘**，并做了反向自证（全仓与 `/tmp` 各 `grep -rIl` 前 8 位 → **各 0 命中**）。
- **新增 `module-boundaries.md` §7.20**（本 plan 唯一写入的 owner doc），七条 Decision 各带备选与残余风险：
  `D-a-1` 标准库 `http.server`（残余：每连接一线程，**不是生产级并发形态**）·
  `D-a-2` 两条端点、前缀 **`/agenerp`** 定稿（否决 `/whoami`）·
  `D-a-3` 选 **(iii)**（字段表由服务端用调用者自己的 `sid` 现取）·
  `D-a-3b` **六格一个不留白**（`fields`/`role` 落 (A)、`view`/`actions` 落 (C)，
  `role` **绝不落 (B)** 并写明理由）· `D-a-4` 映射表**八行**（含**执行期补的两格**，照实标出）·
  `D-a-5` 新变量 **`AGENERP_SERVE_PORT`**，默认 **`8330`**（不复用站点端口 `AGENERP_HTTP_PORT`）·
  `D-a-6` 风险档自评 **L0**（`agents-and-roles.md` §9 那张表**一行未改**）。

### 2026-08-25 · plan `2026-08-25-1026-1`：**Phase 5 收口 —— §7.10 结论改准、§7.19 落地、STATE 追加证据行**

plan：[`docs/plans/p1-insight/2026-08-25-1026-1-industry-pack-live-parity.md`](../../plans/p1-insight/2026-08-25-1026-1-industry-pack-live-parity.md)（`Plan Status: completed`）

- **§7.10「活站点验证范围」改准，追加式不覆盖**：2026-08-24 那次的观测
  （「9 次只读请求」「结论是**部分一致**」与三条逐条记录）**逐字保留**，
  只加了一个「**第一次核对（2026-08-24）**」的引导句；新结论作为
  「**活站点验证范围的第二次核对（2026-08-25）—— 结论：逐字一致**」**追加在其后**。
  自查：本 plan 对该文件的**累计删除行只有 3 行**，全部落在这一小节内。
- **新增 §7.19**：比对链的落点节 —— 四条契约（尤其
  「`request_count` 只记录不判定」这条**取舍**及其正反两面）· 两条残余 ·
  一条边界（**本节判命中集合，站点侧对账判前提事实，两者不是同一件事**）·
  一条已知缺口（`tools/` 无 `ruff` 与 CI 覆盖）· M1–M9 红点逐条记名。
- **`STATE.md` §3 追加一条 `[Proof]` 证据行**（只追加，红线 5）：H1–H9 九格实测值
  （并注明 H1 是复核项、不计入吻合计数）· H2 的结论与触发的处置 · 本 plan 未做的四件事 ·
  `tools/` 那条已知缺口照实点名。
  只增不改自查：原文件 **769 行全部按顺序在案，缺 0 行**（新文件 779 行）。
- **八条验证命令逐条 exit 0**：`门禁 26 项：预期红 0，绿 26` + `697 passed` ·
  `contracts 151` · `tools 81+12s` · `routing 167+1s`（**逐字不变 ⇒ 零新增产品模块**）·
  `context 54` · `ruff All checks passed!` · `packs validate --pack discrete` ·
  `check-masterplan-links.sh`（`共校验 35 条引用，断链 0 条`）。
- **红线自证**：`git status --porcelain -- tests/gates/ .github/workflows/ missions/
  docs/masterplan/DECISIONS.md docker-compose.yml industry-packs/ agenerp/
  tests/unit/inspection_fakes.py` → **无输出**；`find agenerp -name '*.py' | wc -l` → **56**
  （与开工逐字相同）；`ls tests/` 目录集合与开工逐字相同。
- ⚠️ **两条照实记的不满足，不修饰**：
  ① **`closure audit was independent` 未做** —— 本轮由单一执行器完成，
     没有独立子代理做收口审计。这条 Closure Gate **留白**，已写进 plan `## Closure` 的 Follow-up。
     **【事后补记，同日】** 已由**独立收口审计器**（fresh session，不带执行期上下文）补做：
     八条命令自跑全绿 · 补跑整仓一次性 `pytest tests -q -m "not live"` → exit 0
     （`1186 passed, 12 skipped, 21 deselected`）· **自己重施变异 M1** 得
     `2 failed, 23 passed` 且红点测例名与记录逐字相同（⇒ 判据确实钉在出货的 `parity.py` 上）·
     按证据 README 复算 `parity.json` 逐字相等（exit 0）· 红线按
     `git show --name-only 65971aa` 独立复核。结论 `accept`，该 gate 已由审计者补勾。
     ⚠️ **时序照实记**：落盘（`65971aa`）时它是空的，本条是**事后**补的。
  ② **verification scope limited** —— 未跑 `pytest tests -q -m "not live"`（整仓一次性），
     也未拿到 CI 服务端复跑；八条是**逐目录**跑的。另 `ruff` 作用域不含 `tools/`。
     **【事后补记，同日】** 整仓一次性那条已由审计者补跑（exit 0）；
     **但「verification scope limited」不撤销** —— **CI 服务端复跑仍未拿到**，
     `ruff` 不含 `tools/` 这条**一个字没变**。
- ⚠️ **`docs/backlog/p1-insight-roadmap.md` 一个字未改**（plan Non-Goals 8：
  Work Item Status 块由引擎回写）。工作项 8 已是 `done`；它那句
  ⚠️「活站点核对（H4）实测『部分一致』」现已被 §7.10 的第二次核对结清，
  **但改那一行不在本 plan 的授权面内**。
- **逐字声明**：本 plan **零新增产品代码**，**未接线** `rule.lookup`，**未改任何一条规则**，
  **未对活站点做任何写**，**一个模型都没调**。

### 2026-08-25 · plan `2026-08-25-1026-1`：**Phase 4 —— 用那条带判据的链跑了一次两侧，结论 `identical`**

plan：[`docs/plans/p1-insight/2026-08-25-1026-1-industry-pack-live-parity.md`](../../plans/p1-insight/2026-08-25-1026-1-industry-pack-live-parity.md)
证据：[`docs/evidence/p1-pack-parity/`](../../evidence/p1-pack-parity/README.md)

- **结论：`verdict: identical`** —— 三条规则的命中集合两侧**逐字一致**，
  `rule_ids` 也逐字相同（三条、同序），两侧命中集合**都非空**（各 2 条）。
  ① `finished-goods-backlog` 各 1 条 `1010.0` · ② `subcontracting-issued-not-received` 各 0 条 ·
  ③ `closed-order-short-delivered` 各 1 条 `10.0`。
  ⇒ 2026-08-24 那次「**部分一致**」的结论被这次实测结清（成因由人在 `484c123` 修掉）。
- **落进架构文档的这个结论，来自被判据挡着的那条链** —— 不是临时另写的比对。
  Phase 3 与 Phase 4 走的是**同一份 `parity.py`**。
- **`Decision D2` 的短驱动器**（同进程、装替身、调 `run.main(argv, wiring=run.live_wiring)`）：
  替身**由观测方装**，不由被观测方自装 —— 从账本自己数账本是同义反复。
  实读 `{"driver_exit_code": 0, ..., "chat_adapter_probe_calls": 0}`。
- ⚠️ **两格预测没吻合，逐条照实记，一个字没改预测**：
  - **H5**：站点侧 `request_count` 实测 **10**，起草期预测 9（§7.10 记的 2026-08-24 值）。
    起草期就写死了「实测到 10 不是异常」。**没有为了凑那个 9 改任何一条规则或巡检器**，
    也**不猜根因**（裁判规则 3），只记下逐端点分布。
  - **零模型观测 ①**：`sorted(k for k in os.environ if "DASHSCOPE" in k or "AGENERP_LLM" in k)`
    起草期预测为空，实读 **`["AGENERP_LLM_MODEL", "DASHSCOPE_BASE_URL"]`** ——
    那个过滤器**按名字前缀抓，抓到的不全是凭据**（一个是模型名、一个是端点地址）。
    另测真正的那条并成立：以 `_API_KEY`/`_API_SECRET` 结尾的键为 `[]`。
    起草期已写死「本项只是前置条件检查，必须与替身计数合取」—— 本次正是靠 ② 承重。
- **H9 零写成立**：两种读回口径（REST 行数 · `get_count`）比对前后**逐条相等**，
  `SAL-ORD-2026-00001` 的 `modified` 与 `status` 前后**逐字相同**。
  运行期另有 `ReadOnlyTransport` 挡板：本次 11 条请求 = `GET` 10 + `POST /api/method/login` 1，
  `denied` 与 `other_verbs` 都为空。
  ⚠️ 它排除的是「本轮自己写了」，**排除不掉「别人在同一分钟写了」**，不说成「已隔离」。
- **结论可复算**：把 `offline-hits.json` + `live-hits.json` 两份报告读回、再喂给比对器算一次，
  输出与 `parity.json` **逐字相等** → **exit 0**（脚本原文在证据 README 里）。
- **README 逐字写清这次没证明什么**：一次跑不是分布 · 两侧一致证的是「站点装载忠实于数据集」
  而非「数据集本身对」· **一致不等于规则表达对** · 本节判命中集合、站点侧对账判前提事实，
  两者不是同一件事 · 外协那条两侧都零命中，**这次没有给它真实数据上的阳性对照**。

### 2026-08-25 · plan `2026-08-25-1026-1`：**Phase 3 —— 先把带判据的比对链建起来（离线，一次站点请求都不发）**

plan：[`docs/plans/p1-insight/2026-08-25-1026-1-industry-pack-live-parity.md`](../../plans/p1-insight/2026-08-25-1026-1-industry-pack-live-parity.md)

- **顺序是刻意的**：先建带判据的链，再用它跑那一次 —— 反过来会让落进架构文档的结论
  由一条没有判据的临时路径产出，而被判据挡着的那条路径从未跑过真实的两侧。**两条路径必须是同一条。**
- **新增三个文件，零新增产品代码**（`git status --porcelain -- agenerp/ industry-packs/ tests/unit/inspection_fakes.py`
  → **无输出**）：
  - `tools/experiments/p1_pack_parity/parity.py` —— **纯比对器**，两份
    `InspectionReport.as_dict()` 进、结构化差异出，**零仓内 import**（子进程实测
    `[k for k in sys.modules if k.startswith('agenerp')] == []`）。
  - `tools/experiments/p1_pack_parity/run.py` —— 只做编排（装包 / 建两侧行源 / 调比对器 / 落盘），
    离线侧**按路径加载** `tests/unit/inspection_fakes.py`（不复制夹具、不下沉它）。
    只读白名单 `ReadOnlyTransport`：任意 `GET` + `POST /api/method/login`，其余当场抛。
  - `tests/unit/test_pack_parity_harness.py` —— **25 条判据**（`tests/unit` 672 → **697**）。
- **两文件拆分不是洁癖，是被一条实测逼出来的**：按路径加载 `inspection_fakes.py`
  **会**把 `agenerp.routing` 拉进 `sys.modules`（`:39` → `explain_fakes` → routing），
  所以「import 后 routing 不在 `sys.modules`」这个主张**只有比对器担得起**，
  `run.py` 担不起。**照实分开，不假装 `run.py` 也干净，也不靠删依赖去凑它。**
- **`Decision D1`：`request_count` 只记录、不判定。** 实测依据：离线侧 **10**，
  §7.10 记的活站点侧 **9** —— 两侧本来就不同，算进判定会让比对**恒红**，
  而恒红与恒绿一样没有判别力。⚠️ 这是取舍不是最佳实践，残余风险照实登记。
- **变异自查 M1–M9 逐条施加 → 复跑 → 还原，九条无一留在绿**（基线 `25 passed`）：
  M1→⑤(2 failed) · M2→③④ · M3→③④ · M4→④ · M5→②⑥ · M6→⑦(靶心，共 6 failed) ·
  M7→⑧ · **M8→⑨(a) 与 ⑨(b) 各红一条**（两个可观测量都要红，只红一条就说明另一条恒真） ·
  **M9→③④**（改坏出货的 `parity.py`、判据文件一个字不动 ⇒ **判据测的确实是出货那份**）。
  还原自查：`diff /tmp/parity.orig.py tools/experiments/p1_pack_parity/parity.py` → 逐字相同。
- ⚠️ **照实记一处作用域限制**：`ruff` 的作用域**不含 `tools/`**
  （`docs/backlog/tools-dir-has-no-static-check-coverage.md`，`Status: deferred`，处置者是**人**），
  本轮新增的两份脚本**没有被 `ruff` 扫过一次**。钉着它们的只有判据 ⑩ + 变异 M9
  那条「按路径加载出货那份」的纪律 —— **这是缓解不是消除，本 plan 不代人处置。**
- 验证：`python3 tools/experiments/p1_pack_parity/run.py --offline-only` → **exit 0**
  （`离线命中 2 条；行源请求 10 次`）· `ruff check …` → **exit 0**（`All checks passed!`）·
  `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**（`697 passed`）。

### 2026-08-25 · plan `2026-08-25-1026-1`（行业包离线↔活站点命中集合逐字比对）：**Phase 1–2 —— 只读探测 + 漂移 A 无条件改准**

plan：[`docs/plans/p1-insight/2026-08-25-1026-1-industry-pack-live-parity.md`](../../plans/p1-insight/2026-08-25-1026-1-industry-pack-live-parity.md)

- **§0 九处基线逐条重取，无一条漂移**（实读值全部落进 plan §0.1）：
  `HEAD=ec74161` · `7.x` 最大节号仍是 §7.18（§7.19 未被占用）· `pack.json` 三条 `rule_id` 逐字相同 ·
  五个判据面 `672 / 151 / 81+12s / 167+1s / 54` 逐字相同 · 门禁 `26 项：预期红 0，绿 26` ·
  `find agenerp -name '*.py' | wc -l` → **56**（Non-Goals 1 的机械判据，收口须仍是 56）。
- **Phase 1（只读探测）**：`GET /api/resource/Sales Order` 四字段 → exit 0，载荷逐字
  `[{"name": "SAL-ORD-2026-00001", "status": "Closed", "total_qty": 1000.0, "per_delivered": 99.0}]`
  ⇒ **H1 成立，起草期写死的停机分支未触发**（不跑装载器，`--load-*` 一律停）。
  同时取了 **H9 的「前」**：两种读回口径（REST 列表行数 · `GET frappe.client.get_count`）
  逐条相等 `{Sales Order: 1, Delivery Note: 1, Stock Ledger Entry: 10, Bin: 4}`，
  `SAL-ORD-2026-00001.modified = "2026-08-25 07:30:46.926828"`。
- **Phase 2（漂移 A，无条件、不依赖任何实测）** —— 两处已被**已入仓的事实**证伪的话被改准：
  ① `docs/bugs/02-…` 状态行由「**已确认、未修**」改成「**已由人修复**（`484c123`）」，
     §4 Fix 补写人侧两条裁定（`status` 是受控字段 ⇒ 提交后调 `update_status`；判据归站点侧对账），
     §5 追加一行说明「尚缺的那一半」已由 `_trap_precondition_checks` 补上。
     **§1–§3（Problem / Diagnostic Method / Root Cause）一个字未动** ——
     自查 `git diff -U0 | grep '^-[^-]'` 的删除行只有 2 行前言 + 3 行 §4，无一落在 §1–§3。
  ② `module-boundaries.md` §7.10 里「已记 `docs/bugs/02-…`，并在 STATE §3 追加了 needs-human」
     这**一句**改指 `[resolved] 2026-08-25T02:02Z`。**结论那一句（「部分一致」）本轮一个字未动** ——
     它要等 Phase 4 的实测。自查该文件本轮删除行**仅 1 行**，正是那一句。
- ⚠️ **本轮只引用、不复跑**「干净站点从头重建也得到 `Closed`」——那是人做过的，照实标注出处。
- 验证：`python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
  （`门禁 26 项：预期红 0，绿 26` / `672 passed`）。

### 2026-08-25 · plan `2026-08-25-0850-1`（P1.0 逐格计数的单一真相源）：**Phase 3 重开 —— 独立收口审计打回的 F1（守卫在文件尾部失效）**

plan：[`docs/plans/p1-insight/2026-08-25-0850-1-p1-0-cell-tally-single-source.md`](../../plans/p1-insight/2026-08-25-0850-1-p1-0-cell-tally-single-source.md)

- **这一轮不是新功能，是修一个被独立审计实测出来的 live defect**（plan §13.1 · F1）。
  审计的复现逐字可跑：往 `docs/architecture/module-boundaries.md` **尾部**追加一行带语境的
  `⚠️ qwen3.6-plus 在两跳题上是 1/6。`，守卫**看不见**（`scan_handwritten_tallies()` 回 `[]`，
  `pytest tests/unit/test_entry_gate_tally.py -q` → **exit 0**，期望 exit 1）。
- **根因**：`tests/unit/entry_gate_tally.py` 的 `_region_mask()` 只要在一行里**看见** `REGION_OPEN`
  子串就把 `inside` 置真、直到看见闭合标记才复位。而 `module-boundaries.md:2801`（§7.18 讲区域面时
  **逐字引用**了那个起始标记）在该文件里开了一个**永不闭合**的区域，把 `:2801` 到 EOF 的 **18 行**
  整段豁免掉守卫 —— 且该豁免面**随该文件之后的任何续写单调增长**。
  ⚠️ **最危险的地方在于它是静默的**：判据⑦ 断言的是「命中为空」，**豁免面越大它越容易绿**。
- **修法**（审计逐字禁止「把 §7.18 里那个标记名删掉/藏起来」，本轮**没有**碰那处措辞）：
  ① 新增 `_region_marker_kind()` —— 标记只在**独占一行**（前后仅空白）时才算标记，散文里的行内引用不开区域；
  ② `_region_mask()` 强制**成对**：起始无闭合 / 闭合无起始 / 区域内再开区域，三种形态一律
  `UnpairedRegionMarker`（**判据自身打红**，不再静默扩大豁免面）；
  ③ `region_lines()` 改走同一份 `_region_mask` —— 区域面在「取数」与「守卫」两侧**只剩一个口径**，
  不会再出现「取数照常、守卫失效」的偏斜。
- **补的四条判据**（`tests/unit/test_entry_gate_tally.py`，判据数 42 → **46**）：
  扫描面每份文件区域标记成对 · 未闭合起始标记令判据自身红 · 行内引用不开区域 ·
  **M7′ 尾部变异跑在活文件的真实内容上**（尾部之后再续写多少行，这一条都跟着走）。
- **非空转自证**：把 `_region_marker_kind` 临时改回子串匹配（即旧语义）复跑
  `python3 -m pytest tests/unit/test_entry_gate_tally.py -q` → **exit 1 / `4 failed, 42 passed`**，
  失败的正是新增三条 + 判据⑦；改回后 **exit 0 / `46 passed`**。
- **F1 复现的收口实跑**：`printf '\n⚠️ qwen3.6-plus 在两跳题上是 1/6。\n' >> docs/architecture/module-boundaries.md`
  后 `python3 -m pytest tests/unit/test_entry_gate_tally.py -q` → **exit 1**（审计当天是 exit 0）；
  还原后 `git status --porcelain -- docs/architecture/module-boundaries.md` **无输出**。
  `_region_mask` 对该文件的 `sum(mask)` 由 **18** 变 **0**（该文件里本就没有真标记）。
- **未动的东西**（照实记）：`tests/gates/**` · `.github/workflows/**` · `docs/masterplan/**` ·
  `docs/audits/**` · `docs/evidence/**` · `tools/experiments/**` · `missions/**` · `agenerp/**`
  本轮**零改动**（`git status --porcelain` 只有 plan 文件与那两份 `tests/unit/` 文件）；
  §7.18 里那处逐字引用的标记名**一个字未改**。

**full-green verification（命令原文 + 退出码，同一轮实跑）**：

- `python3 tools/gates/check_expected_red.py` → **exit 0**（`门禁 26 项：预期红 0，绿 26，跳过 0`）
- `python3 -m pytest tests/unit -q` → **exit 0 / `672 passed`**（审计基线 668，**+4** 条新判据）
- `python3 -m pytest tests/unit tests/contracts tests/tools tests/routing tests/context -q` → **exit 0 / `1125 passed, 13 skipped`**
- `python3 -m pytest tests/routing -q` → **exit 0 / `167 passed, 1 skipped`**（§6 **H5** 逐字不变）
- `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments` → **exit 0**
- `bash tools/check-masterplan-links.sh` → **exit 0**

**验证范围**：本机。`tests/gates` 的 live 语义与 CI 服务端复跑**不在本轮范围内**。
本条日志的提交 sha 无法写进它自己命名的提交，按既有先例由下一轮回填或以 `git log` 对照。

---

### 2026-08-25 · plan `2026-08-25-0850-1`（P1.0 逐格计数的单一真相源）：**Phase 4 —— 变异自查 · §7.18 · STATE 登记**

**变异自查 M1–M9b 共 11 个，逐个施加、逐个复跑、逐个记退出码**（复跑命令一律
`python3 -m pytest tests/unit/test_entry_gate_tally.py -q`）：M1/M2（改表里某一格）·
M3（`label == "correct"` 改恒真）· M4（分母写死）· M5（同一性断言循环体删空）·
M6（标注集删一行）· M7（注入带语境的手抄）· M8（改 `machine-read` 标记名）·
M9（区域内散文数改一位）· M9b（区域内塞一个四列都没声明过的数）
—— **十个逐个被打红**；**M7b 退出码 0，那是它跑之前就写死的预期**（无语境裸计数是守卫的固有边界）。

⚠️ **M9b 第一次跑是绿的，照实记，不抹掉**：兜底那半判据**复用了守卫的数字面**，
而 M9b 的注入值不匹配那个面 —— **兜底那半在区域里根本没在兜**。当场改准（兜底侧改用更宽的取数口径）
并**另加一条判据**把「兜底口径必须比守卫的数字面宽」钉死，复跑 → 退出码 1。
**两个面本来就不该是同一个**：守卫的数字面是给「找手抄」用的（要窄，否则误报淹掉），
兜底要的是「区域里有没有冒出一个谁都没声明过的数」（要宽）。第一版把它们混成一个，是设计错误。
**这一处正是变异自查存在的理由 —— 不是「一次通过」，是「跑出来才知道」。**

⚠️ **§7.18 落地时被自己的守卫打红了一次，同样照实记**：那节初稿在讲窗口标定时顺手写了一个
逐格形状的数，而它在区域**之外** ⇒ 守卫当场红。**没有放松守卫**，改的是 §7.18 的写法
（改成不写任何逐格形状的数，并把这条约束逐字写进该节）。**判据倒逼写法，不是反过来。**

`module-boundaries.md` 新增 **§7.18**，记 D5 四条：派生器与判据的位置与职责 · 四列并置的口径与各自出处 ·
守卫的收网口径与误报面（含那条**具名的固有盲区**）· **本 plan 没有裁定哪个口径为准**。
`STATE.md` §3 **追加一行** needs-human：把「更正版与标注集在两格上差 3 条、循环自判与标注集逐条一致」
逐条量化登记，**不替人裁定**，并逐字声明本轮未改审计 / 证据 / 标注集 / `DECISIONS.md` / STATE 已有行。

**八条基线命令收口逐条复跑，全部 exit 0**：① `门禁 11 项：预期红 0，绿 11，跳过 0`（逐字节不变）·
② `668 passed`（基线 626，本 plan 新增 **42 条**）· ③ `151 passed` · ④ `81 passed, 12 skipped` ·
⑤ `167 passed, 1 skipped`（**与基线逐字相同**）· ⑥ `54 passed` · ⑦ `All checks passed!` ·
⑧ `共校验 35 条引用，断链 0 条`。
**§6 H1–H7 逐条对照：H1/H2/H3/H4/H5/H7 六条吻合，H6（目标而非假设）达标**（`N = 42 ≥ 12`）。
**H3 与 H4 两条真未知本轮双双结清** —— H4 尤其：§6.1.1 那张标定表是在**改之前**跑的，
「改完之后仍 0 误报 / 0 漏报」**今天才第一次有证据**。

⚠️ **一次并发写入撞进收口窗口，照实记**：收口复跑时 `tests/unit` 一度有 5 条
`test_seedsite_documents.py` 判据红。**复跑优先于分析** —— 原样复跑复现，在 `HEAD` 的干净 worktree
上跑同一条命令 **34 passed**，再把本 plan 改过的四个文件逐个换回 `HEAD` 版本复跑，**红的还是那 5 条**
⇒ 不是本 plan 的改动。`git diff` 显示 `agenerp/site.py` / `agenerp/seedsite.py` 被本轮之外的一次写入改动，
mtime 落在执行期内且**观察期间还在继续变**。按 RUNBOOK §5.5：**一个字节未碰、不代人处置**。
那次写入在本轮收口前自己收敛完（红 5 → 2 → 0），最终整目录绿。
⚠️ **这属于运气不是保障**，且已花掉五次额外复跑才判定归属 —— 与 `STATE.md` §2 `2026-08-24T10:08Z`
记的是同一类风险。**过程留着，不因为最后是绿的就删掉中间那段。**

⚠️ **同一次并发还造成一处归属错乱，照实记**：那两次人侧提交
（`484c123` `fix(seedsite)` 与 `913d515` `test(gates)`）**把本 plan 还没提交的三个文件一起扫了进去** ——
`agenerp/explain/loop.py` 与 `tests/unit/test_answer_judging_fixture.py` 落进前者、
`docs/masterplan/STATE.md` 的追加行落进后者。**内容逐条复核未被损坏**，但**产出 sha 变成了人的提交**
⇒ 本 plan 的产出 sha 是**三处**，不是一处。**不改写那两次提交**（改写别人的历史更糟），只指明。
**RUNBOOK §5.5 已立的纪律本轮再次没有兜住 —— 记为一次复发，不写成「已按纪律处理」。**

### 2026-08-25 · plan `2026-08-25-0850-1`（P1.0 逐格计数的单一真相源）：**Phase 3 —— 指针面与守卫**

§1.6 点名的**段外八处**手抄全部换成指向 §12.3 四列表的指针：
`model-management.md` §12.5 模型档案表**出处列**三处（`qwen3.8-max` / `qwen3.7-plus-2026-05-26` /
**`qwen-plus`** —— 最后这处此前手抄的两格取自乙列，与甲、丁两列**今天就不一致**，已逐字点明）·
§12.5 `multi_hop` 限定段三处 · `module-boundaries.md` §7.8 残余风险 4 一处 ·
`agenerp/explain/loop.py` 的 `explain()` docstring 一处（**仅注释**，D4）。
⚠️ **只动出处列**：前三列（模型名 / 能力 / 是否计 reasoning）一格不动，
`pytest tests/routing -q` 逐字不变自证；`multi_hop` 能力取值**一个字未动**（Non-Goal 7）。

⚠️ **§12.5 那段「`2/6` vs `1/6` 待人裁定」实读已被 `STATE.md:464` 的 `[resolved] 10:12Z` 结清**，
按该行改准「分歧仍敞着」这个陈述 —— **只改陈述，不裁定计数**；`DECISIONS.md` D-11 复验注记一个字未动（红线 3）。
「它在本项目实测下答对不到一半」这句按 D3 同法改成口径分述（**在乙、丙下成立，在甲、丁下不成立**）。

**判据⑦（守卫）两侧都落盘**：
- **阳性对照**（只留数字面、去掉语境面与区域面）：命中 **15 行**，其中 §1.6 那 **6 行无关面全部误报**
  （`agenerp/oob.py` · `module-boundaries.md` ×5）—— 证明「不带语境的收网」真的守不住，收窄不是空转。
- **收窄版**（数字面 ∧ 语境面 ∧ 区域面）：`agenerp/**` + `docs/architecture/**` 上**命中 0 处**，
  **误报 0 / 漏报 0**（§6 **H4 吻合** —— 这是改动**之后**第一次有证据，§6.1.1 那张标定表是在改之前跑的）。
- **非空转**另由四条判据钉住：M7 形态（带语境的手抄）当场红 · 标识在**前 4 行**内照样红 ·
  区域面**不是文件白名单**（同一文件区域外照样红）· 扫描面覆盖两棵树全部文件。
- ⚠️ **M7b 的固有盲区照实登记**：前 4 行内不含任何标识的裸计数**守卫看不见**，
  判据里逐字写着「它若打红了反而要回头查」。**不假装守得住。**

**判据⑧**：`tests/unit/test_answer_judging_fixture.py` 四次判错那张历史叙述表
**数字一个字未改**（`5/12 误判成 2/12` 原样），只加一条指向 §12.3 的指针；
判据断言该文件仍含「三份正确答案判成」原文，挡住「顺手把历史改成新口径」。

Phase 3 Exit 五条全绿：两条 grep 复跑 —— **16 行 P1.0 面全部落进 §12.3 的 `machine-read` 区域内
（`:98-162`）或消失，6 行无关面逐行原样保留**（`git diff` 里那 6 行零改动）·
`git diff -- agenerp` 的新增行**逐条是 docstring**，无非注释行 ·
`python3 -m pytest tests/unit tests/contracts tests/tools tests/routing tests/context -q`
→ **exit 0 / `1120 passed, 13 skipped`** · `python3 tools/gates/check_expected_red.py` → **exit 0** ·
`ruff check …` → **exit 0**。

### 2026-08-25 · plan `2026-08-25-0850-1`（P1.0 逐格计数的单一真相源）：**Phase 2 —— 并置面**

`model-management.md` §12.3 改准整段划进一对 `<!-- machine-read: p1-0-cell-tally -->` 起止标记，
四格表换成**四列并置表**（甲 · 循环自判 / 乙 · 人侧复核第一版 / 丙 · 人侧复核更正版 / 丁 · 人工标注集），
**每列一个出处指针**。⚠️ **表里没有「正确值」列**（D2）：四列合计逐列 `8/12` · `2/12` · `5/12` · `8/12`，
**哪个为准是人的事**，本节不替人选。

三列各自被判据钉在自己的源上，**一个数都不是手抄**：丁列 == Phase 1 的派生器（判据④）·
乙 / 丙两列 == `STATE.md` §2 `07:31Z` / `09:01Z` 两张表**机械解析**（判据⑤，§6 **H3 吻合** ——
缩进两格的 md 表与带 `**` 粗体、全角字的结论列**都解析得出来**）·
甲列 == `verdicts.md` 逐条打勾行**数出来的**（判据⑥），并与该表自报的正确率交叉核对一致。

⚠️ **区域不是免检区**（独立草案评审第 2 轮点出的洞）：新增 **判据⑨** 两半 ——
**⑨a** 把四条散文句按**逐字锚点**绑到它自称的那一列（`门禁把 0/3 提到 1/3` 等四条，
Phase 2 **逐字保留、连换行位置都没动**，只在其后另起一行追加不含数字的口径分述），
**锚点缺失即红**，另加一条**计数断言**（区域内含数字的散文行数 == 被绑定锚点数，实测 **4 == 4**）；
**⑨b** 兜住「区域里冒出一个四列都没声明过的数」。
D3 的口径分述已落进 doc：那句「强模型那半边出现反向证据」**在乙与丙下成立、在甲与丁下不成立**，
「门禁没让弱模型追上强模型」**在乙下不成立** —— **只登记，不据此改动 `被削弱` 这个结论标签**。

Phase 2 Exit 六条全绿：`python3 -m pytest tests/unit -q` **exit 0 / `658 passed`**（Phase 1 后 643，+15）·
`python3 -m pytest tests/routing -q` **exit 0 / `167 passed, 1 skipped`** —— 与基线**逐字相同**（§6 **H5 吻合**：
新增一个不同名的 `machine-read` 标记按构造干扰不了 `_table_after()`）·
`ruff check …` **exit 0** · `bash tools/check-masterplan-links.sh` **exit 0 / 共校验 35 条引用，断链 0 条** ·
`git status --porcelain -- docs/masterplan docs/audits docs/evidence` → **无输出**
（`STATE.md` / 审计 / 证据三处一个字节未改）。

### 2026-08-25 · plan `2026-08-25-0850-1`（P1.0 逐格计数的单一真相源）：**Phase 1 —— 派生面**

plan：[`docs/plans/p1-insight/2026-08-25-0850-1-p1-0-cell-tally-single-source.md`](../../plans/p1-insight/2026-08-25-0850-1-p1-0-cell-tally-single-source.md)

本仓关于「P1.0 第一轮逐格计数」有**四个口径**（甲：循环自判 8/12 · 乙：人侧复核第一版 2/12 ·
丙：同一人同日的更正版 5/12 · 丁：人工标注集派生 8/12），**四个全是手抄**，
而 `model-management.md` §12.3 今天引的是那条**已被同一人公开更正过**的 `07:31Z`
（`STATE.md:309` 逐字「`2/12` 这个数是错的，正确是 `5/12`」）—— guide Minimum Rule 14 的
`confirmed owner-doc drift`，不可降级。

新增 `tests/unit/entry_gate_tally.py`（派生器）+ `tests/unit/test_entry_gate_tally.py`（判据）。
派生器按 `(model, gate)` 分组数 `label == "correct"`，**分母由分组实际行数得出，不写死 3**；
八格实得 `qwen-plus off 0/3` · `on 2/3` · `qwen3.6-plus off 3/3` · `on 3/3` ·
`qwen3.7-plus-2026-05-26 off 3/3` · `on 3/3` · `qwen3.8-max off 2/3` · `on 3/3`，
与 plan §6 H1 跑之前写死的八格**逐格相等**（H1 吻合）。

⚠️ **「标注集标的就是那 24 次运行」这件事被变成了断言，不是自报**：24 行的
`(model, gate)` 与 `docs/evidence/p1-entry-gate*/{run_id}.json` 同名键逐条相等、
`answer` 与轨迹 `final_answer` **逐字节相等**，且**两侧各有一条计数断言**
（`== 24`）挡住「循环体一行没跑」的空实现。

Phase 1 Exit 四条全绿：`python3 tools/gates/check_expected_red.py` **exit 0**（`门禁 11 项：预期红 0，绿 11，跳过 0`）·
`python3 -m pytest tests/unit -q` **exit 0 / `643 passed`**（基线 626，+17）·
`ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments` **exit 0 / All checks passed!** ·
标注集 `shasum -a 256` = `243efdf3…f195b15`、`wc -c` = `63529`，**与 §0.1 实读逐字相同**
（Non-Goal 1 自证：本 plan 只读它、只数它）。

### 2026-08-25 · plan `2026-08-25-0554-1`（prompt 侧 `cached_tokens` 进账本）：**Phase 1 —— 解析面**

plan：[`docs/plans/p1-insight/2026-08-25-0554-1-prompt-cache-accounting.md`](../../plans/p1-insight/2026-08-25-0554-1-prompt-cache-accounting.md)

`agenerp/routing/adapter.py` 的 `Usage` 加第四项 `cached`，口径与 `reasoning` **完全对称**：
一个是 completion 侧细分、一个是 prompt 侧细分，**都不是新的桶**，
`total` 仍是 `prompt + completion`，`cached` **不进 `total`**（它是 `prompt` 的子集，
加进去当场与端点自报的 `total_tokens` 对不上）。`usage_of()` 加读
`prompt_tokens_details.cached_tokens`，缺失回 `0`（D2）。`plus()` 四项相加、`as_dict()` 出五键。

⚠️ **缺口原本有多大**：端点**每一次**都在回包里报 `prompt_tokens_details.cached_tokens`
（起草期与执行期两次实读 `docs/evidence/p1-answer-judge/` 三份证据，
`all.json` 24 + `stability.json` 18 + `controls.json` 6 = **48 个值**），
本仓从头到尾**一个字没记**。而 P1.7 已实测的那次解释里 prompt 占
`53041 / 58579 = 90.5%` —— 账本在占九成的那一栏上分辨不出贵与便宜。

判据落 `tests/unit/test_prompt_cache_accounting.py`（**不落 `tests/routing`** ——
`missions/p1-insight.json` 的 `commands.test` 只含 `tests/unit`，落别处进不了 `GATE_VERIFY`）。
五条**一律写死字面数字、整字典比较**，不写 `total == prompt + completion` 那种恒真式。

Phase 1 Exit 四条全绿：`pytest tests/unit -q` **exit 0 / `619 passed`**（基线 614，H9 预测的 Phase 1 格）·
新文件**恰 5 条** · `pytest tests/routing -q` **exit 0 / `167 passed, 1 skipped`**（H7 不变）·
`ruff check` **exit 0 / All checks passed!**。

§1.5 第 9 行的三个 `as_dict()` 转发点（`judge.py:52` / `loop.py:387,524`）开工时逐个复读：
**全仓无任何 `Usage.as_dict()` 的键集断言**（`tests/experiments` 那两条断的是
`tools/experiments/p1_entry_gate` 自己的 `Usage`，冻结面，不受影响；
`test_explain_cost_ledger.py:405` 断的是**证据文件**不是账本），**代码一行不改**。

### 2026-08-25 · plan `2026-08-25-0554-1`：**Phase 2 —— 账本面、落盘面，与一片确认漂移**

`agenerp/explain/ledger.py`：`_endpoint_numbers()` 回**三个数**（加 `endpoint_cached`），
`CallEntry` 加 `endpoint_cached` 字段与 `cached_matches_endpoint` 属性
（形状**逐字对称**于 `reasoning_matches_endpoint`：端点没报 ⇒ `False`，**「不知道」不写成「对得上」**）。

**跨出 P1.7 边界的唯一一处是 `agenerp/context/store.py`（D3），理由是它是一条静默丢数的契约漂移**：
`Usage` 一旦有第四项，`from_payload(to_payload(x))` 对 `cached > 0` 的会话就**不再相等**，
而今天测不出来只因为夹具的 `cached` 恒为 0。所以落盘/读回都改四键，
`tests/context/test_store.py:92` 的键集断言改四键、**另加一条 `cached > 0` 的 round-trip**（`53 → 54`）。
⚠️ `total` **仍不落盘** —— 它是派生量，落进去就是第二份真相；`cached` 不是派生量，两者不同类。
**无数据迁移**：`from_payload` 缺键即抛，但全仓除 `store.py` 自身与再导出外**零调用方**，
`docs/evidence/**` 下**零存量会话文件**。

⚠️ **`tests/context` 不在 `commands.test` 里**，所以 D3 的承重判据**同时钉在 `tests/unit`**
（判据 ⑪(a)/(b)，直接 import `agenerp.context.store`，不碰 `tests/context` 的夹具）——
不这么做，D3 的缓解措施就只存在于一条 `GATE_VERIFY` 复跑不到的判据上。

**「三项 / 不是第四个桶」这句话的漂移面用命令穷举，不靠逐行记忆**（plan §1.6 两条 grep）。
命中按**三类**归档：**改准 11 处**（`adapter.py:8` · `ledger.py:9-11,16,124` · `store.py:48` ·
`session.py:13-17,147` · `loop.py:240` · DocType `turns` 的 `description` ·
`module-boundaries.md:347/§7.7/§7.11` · `model-management.md:57,240`）·
**一个字不改 4 处**（`tools/experiments/p1_entry_gate/{llm,loop}.py` 冻结面 ·
`module-boundaries.md` 里 P1.5 那两行**历史记录** —— 改它等于改历史）·
**无关命中 3 处**（`seedsite.py:267/870` · `contracts.py:161`，讲的是别的「三项」）。

⚠️ DocType 声明只改 `turns` 那一项的 `description` **自由文本**，
`field_order` / 字段集 / `permissions` **一个字未动**（实测 diff：**1 insertion, 1 deletion**）
⇒ 不是模型形状变更、**不触发 L3、不需人批**；**本次没有在活站点上建任何表**。

Phase 2 Exit 全绿：`pytest tests/unit -q` **exit 0 / `626 passed`**（H9 的 Phase 2 格）·
新文件**恰 12 条** · `pytest tests/context -q` **exit 0 / `54 passed`**（H8）·
`check_expected_red.py` **exit 0 / `门禁 11 项：预期红 0，绿 11，跳过 0`**。

### 2026-08-25 · plan `2026-08-25-0554-1`：**Phase 3/4 —— 变异自查全红，活端点测出一个负结果**

**M1–M10 十个变异逐个由绿转红，没有一条需要就地补断言 ⇒ 没有 M11。**
文件级 `cp` 备份还原，全程未用 `git checkout`；还原后 `git status --porcelain` 与 Phase 2 结束时逐行相同。
⚠️ 其中 M1 的红灯**指的是判据 ⑥ 而不是 ⑧** —— ⑧ 的假实现摆在**判据侧**（`cached=0` 由测试自己钉死），
产品端有没有 M1 它都绿；这一条在起草评审里被抓过一次，执行时按改后的表跑。

**活端点实跑一次（`qwen3.6-plus`，同题，10 轮，124.3s，`answered` / `accepted=true`）——
逐次 `cached_tokens` 全为 `0`。**

> **本项目端点上，前缀缓存今天一个 token 都没省下来。**

**这是负结果，负结果同样有价值**：它是本仓关于「自己端点上前缀缓存生不生效」的
**第一个观测样本**（此前是 **0** 个），并证伪了 `model-management.md` §12.2
那句从 Spike 02（别的栈、别的站点、别的题）搬来的话在本项目上的**直接适用性**。
⚠️ 但**不足以推翻那句上位结论本身** —— 一道题、一个模型、一次运行不是分布。
§12.2 的成本表与那句结论**一个字未动**，只加了一行指针（D4）；改写与否**由人裁定**。
⚠️ **没有因为 H2 落空就扩大范围去「让它命中」**（那属 Non-Goals 2）。

**实测比起草期预想的更彻底一层，照实记**：端点**十次都报了** `prompt_tokens_details`，
但那个子对象的键集**逐次恒等于 `{"text_tokens"}` —— 根本没有 `cached_tokens` 这个键**。
所以本次的 `0` 不是「端点说命中了 0 个」，而是「**端点根本没说**」。
这恰好是 D2 残余风险（`0` 有两个含义）在实测中落地的样子，也证明
「证据必须原样落原始子对象」不是多余手续。⚠️ 与 `p1-answer-judge/` 那 48 个回包
（`{"cached_tokens": 0, "text_tokens": N}`，**键在、值为 0**）成因不同，**不猜根因**。

⚠️ **举证责任的边界，跑之前就写死的，逐字抄进证据与落点节**：
**活端点证据在这一支上不承担「记全了」的举证责任** —— 逐次全为 0 时每一次的
`cached_matches_endpoint` 都是 `0 == 0 → True`，
**一个把 `cached` 恒写 0 的假实现产出的证据文件与真实现逐字节相同**。
「记全了」由 `tests/unit` 的判据 ①（端点报 1024 ⇒ 解析得 1024）与
⑧（端点报 100 而解析成 0 ⇒ 比对属性为 `False`）**单独承担**。
同理 **H1 / H4 在这一支下恒真，不构成证据**，对照表逐格标注了。

**H1–H9 逐格对照**：H6 / H7 / H8 / H9 吻合；**H2 不吻合**（就是上面那个结论）；
**H5 不吻合（混淆已预先声明）**——判定器 48 次单发与本次多轮**两者都全为 0**，
按跑前写死的处置记「多轮 vs 单发的差异在本项目上不存在」；
H1 / H4 吻合但标注「该支下恒真」；H3 前提未成立、同样不构成证据。
**§6 原文一个字未改**，不吻合的照实记。

**账目回归全绿**：`total_matches_endpoint` **10/10**、`reasoning_matches_endpoint` **10/10**、
`cached_matches_endpoint` **10/10**，账本 `total` 与 `ConversationSession.usage_total` 逐项相等，
`total = prompt + completion` 口径未被 `cached` 污染。

**七条基线命令全部 exit 0**：`check_expected_red.py`（`门禁 11 项：预期红 0，绿 11，跳过 0`）·
`tests/unit` **626 passed**（614 → 626）· `tests/contracts` 151 · `tests/tools` 81 passed, 12 skipped ·
`tests/routing` **167 passed, 1 skipped**（H7）· `tests/context` **54 passed**（53 → 54，H8）·
`tests/experiments` 10。`ruff check agenerp tests/unit …` **exit 0**。

⚠️ **两条不在 `commands.test` 里的命令实读为红，且红在基线上就已经红，照实记**：
`ruff check agenerp tests tools` → exit 1（全部命中 `tools/gates/check_budget.py:142-143` 的 `F541`，
**本 plan 未动该文件**）· `node tools/check-doc-references.mjs` → exit 1（**77** 条）。
两条都在基线 `9dea949` 上 `git stash` 复跑过，**同样 exit 1**；doc-refs 的 77 条逐条 diff
**只有行号位移，一条新失败都没引入**。**不猜根因、不顺手修**（那属另一个结果面）。

⚠️ **verification scope limited**：`tests/context`（54）与 `tests/routing`（167）
**都不在 `missions/p1-insight.json` 的 `commands.test` 里**，它们的绿
**不代表 `GATE_VERIFY` 复跑得到**；活端点那一跑同样只在本机、CI 零覆盖。
`GATE_VERIFY` 复跑得到的是 `check_expected_red.py` 与 `pytest tests/unit -q`（626 条，含本 plan 的 12 条）。

owner doc：`module-boundaries.md` 新增 **§7.17**（口径 + 实测 + 举证责任边界 +
「与 §12.2 不是同一个量，不得互相佐证」），`model-management.md` §12.2 **只加一行指针**。

### 2026-08-25 · plan `2026-08-25-0225-2`（洞察 Agent 归因的首次活端点实跑）：**链路跑通了，答案是空的**

plan：[`docs/plans/p1-insight/2026-08-25-0225-2-insight-attribution-live-run.md`](../../plans/p1-insight/2026-08-25-0225-2-insight-attribution-live-run.md)

结清的是 `docs/backlog/p1-insight-roadmap.md` 工作项 7 那一行 ⚠️ 里逐字写着的
「**归因那一半也没有在活端点上跑过 —— 不得读成「洞察 Agent 已验证」**」。

**先说没做到的那一半**：两次活跑的归因 **`accepted = false`、`answer` 是空文本**。
`docs/evidence/p1-insight-live/` 里**一段归因文本都没有**。
所以这一轮能说的只有「巡检 → 题面 → 取证 → 门禁这条链在真站点 + 真模型上走得通、
零越权、零写、账目对得上」，**不能**说「归因能给出答案」。

**为什么没有答案 —— 根因实测定位，不是猜的**：`doc.links` 对 `Item/HRD-PACK-5K`
**必然 HTTP 500**。`scan_links` 遍历指向 `Item` 的全部 Link 宿主，其中
`Quick Stock Balance` 在本站点上是 **Single DocType**（实读 `issingle = 1`，没有实体表），
Frappe 回 `pymysql.err.ProgrammingError`，`SiteError` 一抛整次 `doc.links` 作废。
而 L1 门禁要的正是「作答前对 `HRD-PACK-5K` 调过 `doc.links`」——**这条证据在本站点上取不到**，
循环只能一路取证到上限（run-01 `max-turns` / run-02 `tool-call-runaway`）。
登记 `docs/bugs/03-doc-links-dies-on-single-doctypes.md` + `STATE.md` §3 needs-human。
**本 plan 不改它**：它是一次验证，在同一个 plan 里既当运动员又当裁判，
会让「活跑抓到的问题」变成「顺手改到跑绿为止」。

**做到的那一半（六项判据两跑全绿，脚本两跑均 exit 0）**：

- `discrete` 包在活站点上**恰好命中 1 条**（`finished-goods-backlog` / `on_hand = 1010.0` /
  `HRD-PACK-5K` / `成品仓 - HRD`），与起草期预登记逐字一致；
- **命中逐字未被改写**；**账本条数 == `chat()` 被调次数**（25==25 / 22==22，
  两个数来自**不同采集面** —— 账本在循环里记，计数探针包在 transport 上数）；
  三项 usage 全 > 0；`total_matches_endpoint` 逐条为真；取证轨迹非空可枚举；
- **零白名单外请求**。⚠️ 起草期实读改准的一处：**「只读」不等于「只发 GET」** ——
  `permission.scope` 逐个 DocType `POST frappe.client.has_permission`，
  一个完全只读的会话也会发出数百个 `POST`。判据因此按**端点语义白名单**写，不按动词写。
  `POST` 条数逐字对上算式 `1 login + 238 has_permission + 238×(system.overview + schema.search 次数) get_count`
  （run-01 **477** / run-02 **715**）。⚠️ N 实读为 **238** 而不是 `site_scope.py` 注释里那个
  2026-08-24 实读的 **239**，落在预测的「> 0 且 ≤ 239」内，**注释不改**。

**H4 的「≤ 12」不吻合，照起草期写死的处置办**：两跑 **25 / 22** 次模型调用。
先**原样复跑**一次（裁判规则 3），两次都超 ⇒ 记不吻合、照实落账、**照常收口**
（D-18：这是记账不是闸），并往 `STATE.md` §3 追加 needs-human。
**12 这个数一个字没改** —— 本项目此前仅有的两个观测是 **7** 与 **8**，本轮高出一个量级，
成因已定位在上面那个缺陷上。

**判定器（`agenerp/judging/`）这次什么都没测出来，这是好消息也是坏消息**：
喂给它的 `answer` 是空文本，它**指名抛** `JudgingError: 待判的答案是空文本` ——
没有回一个假标签，也没有把空串判成 `correct`。
⇒ **本仓没有观测到任何跨题族标签**，D-16 的「据判定器，判为 X，待复验」那句标注
**因为没有 X 而写不出来**；plan Phase 2 该项记 `[~]`，证据里落的是诚实版本。
判定器的标签取值**不构成本轮任何通过条件**，这一点有可判形式钉着：
判据断言**退出码在三个标签下逐字相同**，且把 `judge` 换成不带 `label` 键的替身退出码照样为 0。

**变异自查 M1–M6 逐条被打红**（文件级 `cp` 备份还原，全程未用 `git checkout`）。
⚠️ **M7 是变异自查当场发现的缺口并就地补的**：把 `decide()` 里的
`evidence_trace_enumerable` 改成**恒真**，原有 14 条判据**全绿** ——
原来只有一条「快乐路径上它为真」的断言，而一条恒真的判据等于没判。
补 `test_m7_an_empty_evidence_trace_fails_the_run`（剧本第一步就直接作答、一次工具都不调）
之后复跑，该变异 exit 1。

**八条基线命令全部 exit 0**：`check_expected_red.py`（`门禁 11 项：预期红 0，绿 11，跳过 0`）·
`tests/unit` **599 → 614 passed**（+15）· `tests/contracts` `151` · `tests/tools` `81 passed, 12 skipped` ·
`tests/routing` `167 passed, 1 skipped`（**逐字不变**，本轮新增 0 个 `agenerp/**/*.py`）·
`tests/context` `53` · `tests/experiments` `10` · `ruff … `All checks passed!``。
`agenerp/insight/**` 与 `agenerp/inspection/**` **一行未改**。

**verification scope limited**：两次活跑**只在本机、CI 完全没有覆盖**；
CI 复跑得到的只有那 15 条离线判据。`bench list-apps` 前后一致这条旁证的「前」值
用的是仓里已有的那份记录，**不是 run-01 之前当场取的** —— 站点零写的承重面是请求记录器。

落点节 `docs/architecture/module-boundaries.md` **§7.16**（纯新增）；
§7.9 那一句用**追加**结清（**原句一个字未改**）；`docs/design/agents-and-roles.md` §5.1
只补落点指针（**4 增 0 删**）。

### 2026-08-25 · plan `2026-08-25-0225-1`（答案判定器 v0）独立关闭审计：**`needs revision` → 六条逐条处置后收口**

plan：[`docs/plans/p1-insight/2026-08-25-0225-1-answer-judge-v0.md`](../../plans/p1-insight/2026-08-25-0225-1-answer-judge-v0.md)

**独立子代理**（fresh session，不带实现上下文）跑了关闭审计。**它不是冷复跑** ——
它自己重跑了八条命令，并**自己重施了 M9 / M9b / M10 / M3 / M1 / M5 / M8 七条变异**
（文件级 `cp` 备份，**全程未用 `git checkout`**），七条逐条 **exit 1**，
且都是被 plan §12 点名的那些判据打红的；三份证据也被逐份复核为真
（含把两条构造变换离线重算，`transformed_input` **逐字节复现**）。

**结论 `needs revision`，6 条 required change。逐条处置，一条不挑着记**：

- **F1 收口本身没做** —— plan 还是 `active`、Phase 4 全 `[ ]`、§10 全 `[ ]`、`## Closure` 空白，
  而 STATE / roadmap / commit message 三处已经在说"收口"了。**已改**。
- **F2 已提交的 owner doc 与工作区自相矛盾** —— `module-boundaries.md` §7.15 逐字写着
  roadmap 那一节「只读，本节一个字未改它」，而收口时**在该节内**加了 27 行。
  **已改**：§7.15 那一行改准，roadmap 补进 plan §4 Owner Docs 并注明是**执行期追加的 target**。
- **F3 plan 明文禁止的措辞还活着** —— §8 风险① 的执行期补记**禁止**写
  「集子外证据只有 H5 一条（强度低）」，而证据 README 与**本日志前半段**仍留着它。
  **已改**：README 就地改准；日志**原句字节保留**（追加式账本）并补了「已作废」指针。
- **F4 `STATE.md` 证据行落在 §3 不是 §2** —— **未修复，照实记为「满足但有偏差」**：
  归位需要删除已有行，**红线 5 不允许**；同文件 `04:20Z` 那条同款错位。
  Phase 4 该条与 §10 那条**记 `[~]` 不勾干净**，结构冲突**交人**。
- **F5 「no in-scope item downgraded」这条 gate 被断言无条件可勾** —— 却没处理 plan 自己记下的
  D4 nit (b)。**已改**：该条改记 `[~]`，并逐条说清三个 follow-up 各自在不在 in-scope。
- **F6（审计新发现，plan 里没有）主证盖不住产出证据的那条路** ——
  H7 ② 的泄漏判据**只跑过** `answer_judge_fixture.judge_row`，
  而 `docs/evidence/p1-answer-judge/` 里那三份**被采信的证据**是
  `tools/experiments/p1_answer_judge/run.py` 跑出来的，**另一条调用点**，
  且 `tools/` 既无判据覆盖也不在 `ruff` 作用域内。审计实读确认**当时没有发生泄漏**，
  但没有任何东西挡住后来的人把 `row["label"]` 拼进活跑题面。
  **已就地补判据**：把 H7 ② 那条谓词原样抬到实验设施上，登记为 **M12**
  —— 施加变异复跑 **exit 1**，`cp` 还原后 **exit 0 · 59 passed**。

⚠️ **M12 是在独立关闭审计之后补的，只经执行者自查，未再经独立复核**
（先例：工作项 4 的 A5 → M9 同款处置）。它把 `tests/unit` 从 **598** 抬到 **599**，
plan §6 H9 的「实际」列已按此改准，**598 那个数保留不改写**。

**收口时八条基线命令全部 exit 0**：`check_expected_red.py`（`预期红 0，绿 11`）·
`tests/unit` **599 passed** · `tests/contracts` `151 passed` · `tests/tools` `81 passed, 12 skipped` ·
`tests/routing` `167 passed, 1 skipped` · `tests/context` `53 passed` · `tests/experiments` `10 passed` ·
`ruff check …` `All checks passed!`。红线四个 pathspec **无输出**，`STATE.md` numstat **`11  0`**。

**verification scope limited**：活端点三个子命令**只在本机跑过，CI 未覆盖**；
CI 复跑得到的只有那 **59** 条离线判据。

⇒ **plan `Plan Status` 转 `completed`**，§10 十条里 **8 条 `[x]` / 2 条 `[~]`**（F4、F5，偏差写在条下）。


### 2026-08-25 · 答案判定器 v0 落地（plan `2026-08-25-0225-1`，P1.4 第 2 个交付面）

plan：[`docs/plans/p1-insight/2026-08-25-0225-1-answer-judge-v0.md`](../../plans/p1-insight/2026-08-25-0225-1-answer-judge-v0.md)

`tests/unit/test_answer_judging_fixture.py` 模块头逐字要求「**动手写判定器之前，先让它跑通那 24 条人工标注**」。
本轮把那句话变成了仓里的东西。

- **新增产品包 `agenerp/judging/`（三个模块）**：`rubric.py`（判定题面，三条判据**逐字取自
  `docs/evidence/p1-entry-gate/verdicts.md`** 那张跑之前就冻结的表，本轮**不新增任何判准**）·
  `judge.py`（`judge_one(answer: str, *, …)`，一律走 `route()`，一次 `chat()` 一条 `CallLedger` 账）·
  `__init__.py`（导出面只有 `judge_one` / `Verdict` / `JudgingError` / `LABELS` 四个名字）。
- **读集子与验收口径落 `tests/unit/answer_judge_fixture.py`，不落 `agenerp/**`**（plan `D7b`）：
  产品包依赖 `tests/fixtures/**` 是边界倒置，且会把"这次实验的验收口径"永久焊进产品导出面。
- **离线判据 `tests/unit/test_answer_judge.py`（58 条，零网络）**。`tests/unit` **540 → 598 passed**。
  主证**不是**"跑通了 24 条"，是三条**结构判据**（plan §6 H7）：
  ① 签名级 —— `judge_one` 的八个形参没有一个能承载被判那一行的 `label`/`reason`；
  ② 标签无关级 —— 24 条**逐条**构造两次（真行 / `label`+`reason` 换成噪声的同一行），
  两次生成的 `messages` **逐字节相等**；③ 回包依赖级 —— 同一段答案（**钉死取含「外协」的 `run-01`**）
  配三份不同假回包得到**三个不同标签**。
- **⚠️ 起草期实测出来、本轮原样记在账上的一件事**：收紧后的验收口径**挡不住**一个两行规则
  （`len<300 → truncated；否则 '外协' in a`，负例三分类精确 5/5、正例 18/19 —— **照样通过**）。
  它被**预注册**成对抗基线 `length-plus-substring` 并被断言成 `True`，
  另有一条判据守着「不许把这个不好看的基线悄悄拿掉」。
  ⇒ **口径达标是必要条件，不是充分条件**，这句话现在有判据守着，不靠人自觉。
- **活端点实验 `tools/experiments/p1_answer_judge/run.py`**（`--all` / `--stability` / `--controls`），
  证据落 `docs/evidence/p1-answer-judge/`。
  **第 1 轮全量 24 条：逐条一致 24/24**（负例三分类精确 **5/5**、正例 **19/19**）⇒
  `meets_acceptance = true`，**修订次数 0、全量轮数 1**，plan 写死的停机分支**未触发**。
  账本 **24 条 == `chat()` 调用次数**，三项 usage 全 > 0，`total_matches_endpoint` **24/24**，
  累计 48,683 token（其中 reasoning 14,416）。
- **判定模型 `qwen3.7-plus-2026-05-26` 由起草期点名**（plan `D2`）：集子的构造决定了
  **自评躲不掉**（四个模型各产出 6 条），所以判准取"别让它去评自己写的那条承重负例"——
  它自评的 6 条**全是正例**，5 条承重负例**一条都不是它写的**。
  ⚠️ **残余偏向照实登记**：正例准确率那一半里有 6/19 是它自己写的；逐条结果**按产出模型分组落账**，
  **不做加权、不做剔除**。

⚠️ **这份结果不能支撑什么**（与 plan §8 风险① 同一口径）：**没有留出集**（5 条负例分不动）；
集子之外的证据只有"截断"一条，而它自己就写死了"一个长度阈值也能通过它"
⇒ **对"集子之外的判别力"的实证支撑接近于零**，**不得写成"泛化已验证"**。
⚠️ **上面这句措辞已作废，以本条下半段那句为准**（收口时按独立关闭审计 F3 补上这个指针；
原句字节保留不改写，本仓日志是追加式账本）：正确的写法是
「**集子之外没有能区分「判断」与「匹配」的证据；H5 只证明判定器不会把明显截断的输入判成 `correct`**」
—— 因为 `len<300 → truncated` 这个公认不做判断的假实现**原样通过 H5**。
**已验证的适用范围只有 P1.0 那一道题**，跨题族按 D-16 只能写「待复验」。

**verification scope limited**：活端点那三个子命令**只在本机跑过，CI 未覆盖**；
CI 复跑得到的只有离线那 58 条。

**变异自查 M1–M10（含 M2b / M9b）逐条被打红**，还原后复跑仍 `58 passed`。
⚠️ **M7 暴露出一处判据缺陷，照实记**：它整体虽红，但**只有 AST 那一条打红了它**，
行为面那一条（跑前取 sha、跑后再取 sha）**是绿的** —— 那种写法**顺序依赖**：
同一会话里更早的判据已经把文件写过一遍，"跑之前"读到的就已经是被写过的内容。
已就地把参照换成**模块导入那一刻**的指纹 `PRISTINE_SHA256` 并复跑确认（登记为 **M11**），
之后 M7 由两条判据同时打红。**AST 那一条本来就不够**（换成 `open(path, 'w')` 就绕过去了）。
⚠️ M7 的写回是**对着一份逐字节相同的临时副本**做的，
`tests/fixtures/p1_entry_gate_labels.jsonl` **全程零改动**（红线自证两侧均复核）。

**独立评审对 `Decision D4`（构造对照的标签算不算"判定器产生的标签"）的裁定**：
**两半都成立** —— 截断可作证据（强度低）、剥离不可作证据（已降为观测）。
评审同时要求**改准一句措辞**，已照改：不是「集子外证据只有截断一条（强度低）」，
而是「**集子外没有能区分『判断』与『匹配』的证据**」—— 因为那个两行规则里的
`len<300 → truncated` **原样通过**截断对照（179 < 300）。
评审还记下一条 loop 该认的：那两段构造文本**让人读一遍再写标签只要两分钟**，
读完两条都能变成人工标注 —— **loop 选择了论证而不是去读**，而"每次都是读原文才发现"
正是 roadmap 那一节的教训。loop 无权代人定标签，故此条作为重开事件留给人。


### 2026-08-25 · plan `2026-08-25-0119-1`（P1.8 下半）独立关闭审计：**`deferred` 成立，不批准 `completed`**

plan：[`docs/plans/p1-insight/2026-08-25-0119-1-desk-sidebar-carrier-and-explain-request-surface.md`](../../plans/p1-insight/2026-08-25-0119-1-desk-sidebar-carrier-and-explain-request-surface.md)

- **本轮是纯文档轮：产品代码与判据零改动**（改动只有 plan 文件与本文件；`agenerp/**` · `tests/**` 一个字未动）。
- **审计标的逐字：「本 plan 停在 `deferred` 是否成立」**，不是「`completed` 是否成立」。
  **结论：`deferred` 成立，`completed` 不批准** —— Phase 3 / 4 / 5 确实一格没做，批 `completed` 就是伪造证据。
- **审计器未采信 plan 自报的任何数字**，逐条实跑复核：
  - 落地面在场：`agenerp/site.py` 的 `sid` 互斥模式（`:197/203/208/224/385/405-406`）与
    `client_from_sid()`（`:479-489`）· `python3 -m pytest tests/unit/test_site_client_sid.py -q` → **exit 0**（`20 passed`）。
  - **未落地面反向证伪**：`agenerp/serve/` · `apps/` · `tests/ui/` · `docs/evidence/p1-sidebar/` ·
    `tests/unit/test_sidebar_body.py` · `tests/unit/test_explain_service.py` → **六条 `ls` 全部不存在**。
    ⇒ Closure 里「未交付 P1.8 本体」那一串**逐条为真**。
  - 红线复核：`git status --porcelain -- tests/gates/ .github/workflows/ missions/ docs/masterplan/DECISIONS.md docker-compose.yml`
    → **无输出**；`git log --oneline -5 -- tests/gates .github/workflows docs/masterplan/DECISIONS.md`
    最近一次是 `6b07889`，**早于本 plan 的两个落地 sha（`5b675a3` / `7e7f517`）**。
- **审计器改了四处，逐条都是「记法」不是「结论」**：
  ① 状态行的定界从 `> Plan Status: deferred（…）` 同一行移到下一行 ——
  `tools/mission-driver/src/plan-check.mjs` 的 `PLAN_STATUS_RE` 要求状态词后到行尾无其它字符，
  此前把本 plan 的状态读成 **`unknown`**；改后读到 **`deferred`**，**状态语义一个字未变**。
  ② Phase 3 / 4 / 5 的 **36 个 `- [ ]` 改成 `- 🔴 **[未做 · Deferred]**` 非复选框标记** ——
  依据是 `00-plan-authoring-and-execution-guide.md`「When Executing」第 8 条逐字
  「Do not leave it unchecked in the execution list」。⚠️ **一格都没被勾成 `[x]`**。
  ③ Closure Gate `closure audit was independent` 由 `[ ]` 翻 `[x]`，`Closure Audit Evidence` 逐条回填。
  ④ `Follow-up` 新增一条非阻塞项（见下）。
- **审计实读到的一条非阻塞 follow-up**：`agenerp/site.py:485` 的 docstring 指向
  `tests/unit/test_explain_service.py` 判据⑩，而该文件随 Phase 3 一起未建 ⇒ **悬空引用**。
  不阻塞的判据：只在 docstring 里，**不进运行路径、不进任何判据**。
  触发条件：Phase 3 重开并真建出 `agenerp/serve/**` 时对齐；若那次裁定否掉整个服务面，则删掉这句引用。
- **本 plan 的三条人批 `[open]` 一条都没被代做**：`STATE.md` §3 的 `2026-08-25T04:05Z`（`sid` 接缝需重新裁定）
  与两条 `02:10Z`（激活需人批 / WBS 拆行）**原样敞着**，本轮**未追加也未改写** `STATE.md` 任何一行。

**verification（命令原文 + 退出码，同一轮实跑）**：

- `python3 tools/gates/check_expected_red.py` → **exit 0**
- `python3 -m pytest tests/unit -q` → **exit 0**（`540 passed`）
- `python3 -m pytest tests/contracts tests/tools tests/routing tests/context -q`
  → **exit 0**（`449 passed, 13 skipped` = 151 + 81 + 164 + 53，skipped = 12 + 1）
- `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments`
  → **exit 0**（`All checks passed!`）
- `node tools/mission-driver/src/plan-check.mjs docs/plans/p1-insight/2026-08-25-0119-1-desk-sidebar-carrier-and-explain-request-surface.md --strict`
  → **exit 0**（`planStatus: "deferred"` · `totalChecked: 29` · `totalUnchecked: 0`）

**验证范围**：本机。**verification scope limited** —— 未跑 `pytest tests -q -m "not live"`，
未过 CI 服务端复跑，`tests/gates` 的 live 语义不在本轮范围内。**上面五条的绿不等于全绿。**
本条日志的提交 sha 写不进它自己命名的提交，**落地 sha 由下一轮回填：`a4c54868e97fde8647decaaa5de8127e19c1f630`**（`git log -1 --format=%H` → exit 0）。

---

### 2026-08-25 · plan `2026-08-25-0119-1`（P1.8 下半）**被实测中止**：`sid` 是 `HttpOnly`，Phase 2 落地后停机

plan：[`docs/plans/p1-insight/2026-08-25-0119-1-desk-sidebar-carrier-and-explain-request-surface.md`](../../plans/p1-insight/2026-08-25-0119-1-desk-sidebar-carrier-and-explain-request-surface.md)
探测记录：[`docs/analysis/2026-08-25-0119-desk-sid-identity-probe.md`](../../analysis/2026-08-25-0119-desk-sid-identity-probe.md)
owner doc：[`docs/architecture/module-boundaries.md`](../../architecture/module-boundaries.md) **§7.14**（新增，121 insertions / 0 deletions）

**⚠️ 本轮不是「P1.8 下半交付」，是「P1.8 下半跑到第二个 phase 被自己的实测判停」。** 照实读。

- **停机的原因，一句话**：Phase 1 只读探测实测 `POST /api/method/login` 回的
  `Set-Cookie: sid=…` **带 `HttpOnly`**（plan §6 的 **H1 预测「不带」，不吻合**）。
  同批五个 cookie 里**只有 `sid` 带**（`system_user` / `full_name` / `user_id` / `user_image` 都不带）
  ⇒ 承载面 JS 用 `frappe.get_cookie("sid")` **按构造读不到它**，D3⑦ 预设的那条接缝不成立。
- **停机的依据是起草期写死的，不是执行期现编**：plan Phase 1 Exit Criteria 第 4 条逐字
  「Phase 2 跑完就停，**Phase 3 / 4 / 5 整体转 `Deferred But Adjudicated`**」，
  并逐字禁止「只停 Phase 3 而让 Phase 4 继续」（那会产出两个各自关不掉 D3 的残件）。**本轮照此执行。**
- **交付了什么**：`agenerp/site.py` 的**第三条互斥认证模式 `sid`** + `client_from_sid()` 工厂，
  判据 `tests/unit/test_site_client_sid.py` **20 条**（`tests/unit` **520 → 540 passed**，只增不减）。
- **没交付什么**：`agenerp/serve/**`（HTTP 服务面）· `apps/agenerp_desk/**`（承载面 app + ⌘K）·
  `tests/ui/test_sidebar.py` · `docs/evidence/p1-sidebar/`。**一行代码都没有。**
  **本 plan 未声称满足 `02-WBS.md` §4 P1.8 的验收命令，`bench install-app` 一次没跑。**
- **对活站点零写**：本轮发出的 `POST` 只有四次 —— 两次 `login`、两次 `logout`，
  逐条落在 plan §5.1 见即停第 5 条的两条显式白名单内，其余全是 `GET`。
  读回两种：`bench --site frontend list-apps` 探测前后逐字一致（`frappe 15.118.0` / `erpnext 15.119.3`）；
  四类可数文档计数**均 0**。⚠️ 后者的「前值」照实说清：探测**前**那次尝试用的是 HTTP Basic，
  站点回 `AuthenticationError`，**没取到数**；前值取自上半 plan Closure 的实测记录（同为 0）。
- **变异自查六条（本 phase 名下），逐条打红**：M1 → 判据①（+ 工厂那条，2 red）· M2 → 判据③（2 red）·
  M20 → 判据⑧（1 red）· M21 → 判据⑥（2 red）· M23 → 判据⑤（4 red）· M24 → 判据⑦（1 red）。
  **M3–M19 · M22 · M25–M29 记「按构造不适用」**，不记绿也不记红 ——
  它们全部指向已 Deferred 的 Phase 3 / Phase 4 判据，**判据不存在，变异无处施加**；
  记成绿会把「没有判据」伪装成「判据够强」。
- **执行期的一次定界（照实记）**：plan Phase 2 判据④ 起草期写「**三种**凭据两两同给即报错」。
  实读发现第三对（`api_key/api_secret` + `admin_password`）是模块**原有**的「token 优先、会话登录回退」，
  `tests/unit/test_site_client.py` 的 `_client()` 助手正是这么构造的；照原文实现会**打红 21 条既有判据**，
  而同一 plan 的 Exit Criteria 要求 `git diff -- tests/unit/test_site_client.py` **无输出**，
  R5 也逐字写「不碰既有两条路径」。→ **取窄：`sid` ⊥ 另两者，既有那一对一个字不改**，
  并新增一条判据 `test_the_pre_existing_token_plus_password_pair_is_untouched` 把这次定界钉成可判的。
  理由与出处记在 §7.14。
- **一次执行期事故，照实记**：跑 M1 变异自查时用了 `git checkout -- agenerp/site.py` 还原，
  而 Phase 2 的实现当时**尚未提交**，那条命令把它整段撤回了 HEAD。
  靠变异前手工留的 `/tmp/site.py.bak` 完整恢复（恢复后 `tests/unit` **540 passed**、`ruff` exit 0，与撤回前逐字一致）。
  **教训**：变异自查的还原手段必须是**文件级备份**，不能是 `git checkout` —— 除非那一轮的实现已经落过一次提交。
- **六条验证命令逐条实跑**（原文与退出码见下方「验证」）。
- **`docs/backlog/p1-insight-roadmap.md` 工作项 10 保持 `todo`，本轮未改**（plan Non-Goals 9 逐字写死）。
  ⚠️ **它此刻比上半更远** —— 上半停在「激活要人批」，本轮停在「接缝本身要重新裁定」，
  后者是更靠前的一步。**不代人处置，不重复登记 `STATE.md` §3 同日那两条 `open`。**

**验证**（六条，逐条命令原文 + 退出码）：

| # | 命令原文 | 退出码 | 输出 |
|---|---|---|---|
| ① | `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` | **0** | `门禁 11 项：预期红 0，绿 11，跳过 0` + `540 passed`（基线 520，**只增不减**） |
| ② | `python3 -m pytest tests/contracts -q` | **0** | `151 passed`，逐字不变 |
| ③ | `python3 -m pytest tests/tools -q` | **0** | `81 passed, 12 skipped`，逐字不变 |
| ④ | `python3 -m pytest tests/routing -q` | **0** | `164 passed, 1 skipped` —— ⚠️ **不是 H11 预测的 168**，因为那 4 个 `agenerp/serve/**` 模块随 Phase 3 一起没建（`find agenerp -name '*.py' \| wc -l` 实测 **53**，与起草期复核值相同）。**预测的算式没错，错的是前提。** |
| ⑤ | `python3 -m pytest tests/context -q` | **0** | `53 passed`，逐字不变（本轮一个字不改 `agenerp/context/**`） |
| ⑥ | `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments` | **0** | `All checks passed!` |

**验证范围**：`tests/gates` 的 live 语义与 CI 服务端复跑**不在本轮范围内**（verification scope limited）。

**落地 sha**：`5b675a30ce23f3ad40e7d6980511107b0f16045c`（本行的 sha 由紧随其后的纯文档回填提交带入，
见 `git log`；该回填提交只改 `docs/masterplan/STATE.md` 与本文件两个文件）。

**红线自证**：`git status --porcelain -- tests/gates/ .github/workflows/ missions/ docs/masterplan/DECISIONS.md docker-compose.yml`
→ **无输出**；`docs/masterplan/STATE.md` 本轮**纯追加**（`git diff --numstat` 的删除列为 **0**）；
`ls tests/ui` → **不存在**；`bench --site frontend list-apps` 探测前后一致。
⚠️ **`STATE.md` 的追加同样落在文件末尾、落进 §3 而不是 §2** ——
与上一条 `2026-08-25T02:25Z` 同一处既有偏差，归位需要删除已有行（红线 5 不允许），**只记不动**。

### 2026-08-25 · plan `2026-08-24-2311-2`（P1.8 上半）独立关闭审计回填落账

plan：[`docs/plans/p1-insight/2026-08-24-2311-2-desk-embed-carrier-decision.md`](../../plans/p1-insight/2026-08-24-2311-2-desk-embed-carrier-decision.md)

- **本轮是纯文档轮：产品代码与判据零改动**（`agenerp/**`、`tests/**` 一个字未动）。
- **回填的是什么**：该 plan 收口当轮唯一未勾的 Closure Gate —— §10 的 `closure audit was independent`
  —— 由 mission-driver 另派的 **fresh session 独立审计器**补做完成，结论 **approved**（无 BLOCKING / MAJOR）。
  plan 的该行由 `[ ]` 翻为 `[x]`，`## Closure` 的 `Closure Audit Evidence` 由「本轮未做独立关闭审计」
  改写为完整审计记录（审计侧当场复跑的命令原文与退出码、活容器里逐条实读复核的四处载重事实、
  五点一致性 / 反空壳 / 不可降级项 / 交接物存在 / §8 R3 残余抓手的逐条核验结论）。
  ⚠️ **归属照实记**：审计由独立审计器执行并写入 plan，**本轮只做验证与落账提交，不代其自证**。
- **本轮改的三个文件**：plan（审计器留下的 41 insertions / 4 deletions）· `docs/masterplan/STATE.md`（§2 追加一条证据行，**8 insertions / 0 deletions，纯追加**）· 本文件（本条）。
- **⚠️ `docs/backlog/p1-insight-roadmap.md` 工作项 10 保持 `todo`，本轮未改** —— 不是遗漏，是该 plan
  §3 Non-Goals 8 逐字写死、§10 Closure Gate 逐字自证的约束：本 plan 只是工作项 10 的**上半（选型）**，
  `tests/ui/test_sidebar.py` 未创建、也未声称满足（`ls tests/ui` → 不存在）。工作项 10 由 **P1.8 下半**收尾，
  而下半卡在 `STATE.md` §3 同日两条 `open` 的人批上，**本轮不代人处置**。
- **两处照实记下、本轮不返工的既有偏差**：
  · `STATE.md` 上一条 `2026-08-25T02:25Z` 的 sha 回填证据行**被追加在文件末尾、落进了 §3 而不是 §2**
    （由 `0979365` 写下）—— 归位需要删除一条已有行，**红线 5 不允许**，故只记不动。
  · 本文件里 **P1.8 前置 ① 那条日志的 owner doc 行**（本次追加前是第 92 行，追加 42 行后为第 134 行）把 `module-boundaries.md` **§7.12** 描述成
    「D1/D2/D3 三条裁定与被否决备选」，而 D1/D2/D3 实落 **§7.13**。该行在基线 `e804143` 中已存在
    （由 plan `2026-08-24-2311-1` 写下），**不属本 plan 的产物，本轮未返工**。

**验证（命令原文 + 退出码，同一轮实跑）**：

- `node tools/mission-driver/src/plan-check.mjs docs/plans/p1-insight/2026-08-24-2311-2-desk-embed-carrier-decision.md --strict`
  → **exit 0**（`passed: true` · `planStatus: completed` · `totalChecked: 42` · `totalUnchecked: 0`）
- `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q`
  → **exit 0**（`判定模式：default` · `门禁 11 项：预期红 0，绿 11，跳过 0` · `✅ 与预期红名单完全一致` · `520 passed`）
- `ruff check agenerp` → **exit 0**（`All checks passed!`）

**红线自证（对基线 `e804143` 复核）**：
`git diff --name-only e804143 HEAD -- tests/gates/ .github/workflows/ missions/ docs/masterplan/DECISIONS.md docker-compose.yml`
→ **无输出**；`git diff --numstat e804143 HEAD -- docs/masterplan/` → 仅 `27 1 docs/masterplan/STATE.md`（那 1 行删除是
原 EOF 补换行符，内容逐字未改）；`git diff --name-only e804143 HEAD -- docs/backlog/` → **无输出**。

**验证范围**：本机，且只跑了上面三条命令。`tests/context` / `tests/tools` / `tests/routing` / `tests/contracts` 本轮**未跑**
（上面几条日志里的那批数是上一轮的，不重复主张）；`tests/gates` 的 live 语义与 CI 服务端复跑**不在本轮范围内**。
本条日志的提交 sha 无法写进它自己命名的提交，按既有先例以 `git log` 对照。

---

### 2026-08-25 · plan `P1.8 上半` —— Desk 承载面选型（只读探测 + D1/D2/D3），两个 Phase 全部执行完毕

plan：[`docs/plans/p1-insight/2026-08-24-2311-2-desk-embed-carrier-decision.md`](../../plans/p1-insight/2026-08-24-2311-2-desk-embed-carrier-decision.md) ·
探测记录：[`docs/analysis/2026-08-24-2311-desk-embed-carrier-probe.md`](../../analysis/2026-08-24-2311-desk-embed-carrier-probe.md)

- **本 plan 交付的是结论，不是代码**（Non-Goals 1，授权出处 Minimum Rule 9）。**没有落地任何承载面**：
  没有自建 app、没有 ⌘K、没有侧边栏 UI、没有 HTTP 服务面，`tests/ui/test_sidebar.py` **未创建、也未声称满足**。
  **对活站点全程零写。**
- **一句话结论**：Desk 全局 JS 的**完整来源**是 `apps/frappe/frappe/www/app.py:47` 那一行两项 ——
  `hooks["app_include_js"]`（自建 app，构建期）与 `frappe.conf["app_include_js"]`（`site_config.json`，运行期）。
  **D1 选前者，但激活需人批。**
- **三条裁定**（落点 `docs/architecture/module-boundaries.md` **§7.13**，新增）：
  · **D1 承载面 = (A) 自建 Frappe app，激活需人批**。外部规则逐字引到
    `docs/design/agents-and-roles.md` §9 **风险档表 L3 行**（「系统形态变更 … **强制人批** + 落 git + 可回滚」）。
    按 plan §6 两问逐格答，(A) **六格全不触发** ⇒ D1 正常出结论、不停机。
  · **D2 身份 = (i) 当前登录用户**。(A) 白送：`www/app.py:21-26` 对 `Guest` 直接 403 + 跳 `/login`、
    对 `Website User` 直接 `PermissionError`，容器内调用帧的 `frappe.session.user` 就是那个人。
    否决 (ii) Administrator 时**逐字写明它是一次已知的信息越权**，不是「暂未实现」。
  · **D3 判据形状 7 条**逐字落盘，每条注明挡哪种假实现（含「`sid` 转发 + `frappe.auth.get_logged_user` 断言」
    与「绑定地址字面写死 `127.0.0.1`」两条硬项）。
- **四条被否决的候选，各有实读依据**：`Client Script` 的 `dt` 是 `reqd: 1` 的 `Link/DocType` +
  消费端 `desk/form/meta.py:148` 按 `dt` 过滤 ⇒ 做不出全站 ⌘K；`Website Script` 挂在
  **`web_include_js`**（`hooks.py:46`，门户页）⇒ Desk 不取；`Custom HTML Block` 真在 Desk 跑 JS
  （`create_shadow_element`）但只在一张 Workspace 页上；`Website Settings.head_html` 的消费处
  `templates/includes/head.html` 只被 `templates/base.html:24` include，而 **`www/app.html` 不 extends `base.html`**。
- **两条实测推翻了起草期的预测，照实记**：
  · **H2 不吻合** —— 预测挡住 (A) 的是 `tests/gates/test_zero_dep_boot.py` 与
    `tests/unit/test_compose_zero_dep.py`，**实读后两条都挡不住**（前者只判「空环境 config 退 0 / 全 healthy /
    首页含『AI 能力未配置』」；后者的 `test_bootstrap_script_dir_is_mounted_literally` 作用域**只有
    `bootstrap-homepage` 一个服务块**）。真正引得到的是另一条（风险档表 L3 行）。
  · **H5 不吻合** —— 「不存在不改站点不改镜像就把 JS 送进 Desk 的办法」被 userscript / 扩展反证。
    但**不改变 (C) 的结论**：userscript 是另一个承载物，且 §6 第二问三条全中 ⇒ 停机交人、loop 不选。
  · 另有 **H1 / H2b 部分吻合**：`Website Script` 实为 **Single**（`issingle: 1`），**计数对它按构造无效**
    （`get_count` 直接 500），与 §6 洞三对 `Website Settings` 说的是同一件事，起草期没认出来；
    `install-app` 对零 DocType 的 app **不发 DDL**（吻合），但**不止插两类行**（不吻合）。
- **一次对 plan 自己 §5.1 字面的越界，照实记（不是红线）**：取 `Workspace Custom Block` 计数时先用了
  `SiteClient.call_method`，它内部走 `POST /api/method/frappe.client.get_count`（`site.py:299`），
  而 §5.1 见即停含「任何 `POST` 到站点」。该方法只读、**零写**（两种读回已实证），事后改用纯 `GET` 复取同值。
  **教训：白名单按实际发出的 HTTP 动词判，不按方法名听起来是不是只读判。**
- **Minimum Rule 14 的不可降级项已落地**：`agenerp/site.py:5` 逐字「本模块仍是唯一的 HTTP 落点」
  自 P1.1 起为假（`agenerp/routing/adapter.py:196-208` 也在出网打 LLM 端点），已改准。
  **只改注释，行为代码一行未动。**
- **交人两条**（`docs/masterplan/STATE.md` §3 追加）：① D1 的激活需人批（含写下来但**未执行**的命令原文与回滚原文）；
  ② 工作项 10 的 plan 预算已用满表规 3 的 2 个，若激活卡住则 WBS 验收命令交不出来 —— **拆行只有人能做**（红线 5）。

**验证（命令原文 + 退出码，同一轮）**：

- `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q`
  → **exit 0**（`门禁 11 项：预期红 0，绿 11，跳过 0` · `520 passed`，逐字不变）
- `ruff check agenerp` → **exit 0**（`All checks passed!`）
- `docker compose exec -T backend bench --site frontend list-apps` → **exit 0**（`frappe 15.118.0` / `erpnext 15.119.3`，探测前后一致）
- **对活站点零写读回**：四类可数文档计数前后全 **0**；Single / `Code` 字段值前后逐字一致
  （`banner_html` sha256 前16位 `0d6c2fcef48a3d44`/len 399 · `head_html`/`brand_html`/`Website Script.javascript` 全 NULL）

⚠️ **本 plan 未交付任何行为代码，因此无行为判据可跑** —— 上面那条 `tests/unit` 是「基线未被碰坏」的证明，
**不是本 plan 的验证**，不许拿别的 plan 的绿冒充本 plan 的验证。

**红线自证**：`git status --porcelain -- tests/gates/ .github/workflows/ missions/ docs/masterplan/DECISIONS.md docker-compose.yml`
**无输出**；`docs/masterplan/` 整目录唯一的改动是 `STATE.md` 的追加。
⚠️ 照实记一处 diff 细节：`STATE.md` 追加前**文件末尾没有换行符**，追加时给原末行补了一个 `\n`，
故 `git diff --numstat` 显示 `25 1` 而不是 `26 0` —— **那一行的内容逐字未改**，只是不再是 EOF。

**提交**：**`babec7d`**（本条日志里的 sha 由紧随其后的纯文档提交回填 —— sha 写不进它自己命名的提交）。

**验证范围**：本机、镜像 `frappe/erpnext:v15.119.3`、这一次（D-16），不外推。
`tests/gates` 的 live 语义与 CI 服务端复跑不在本轮范围内。

---

### 2026-08-25 · plan `P1.8 前置 ①` —— 即时上下文（当前单据）接进解释循环，三个 Phase 全部执行完毕

plan：[`docs/plans/p1-insight/2026-08-24-2311-1-immediate-context-into-explain-loop.md`](../../plans/p1-insight/2026-08-24-2311-1-immediate-context-into-explain-loop.md)

- **补的是哪条缝**：① 层装配面 P1.2 就交付了、循环 P1.4 也交付了，但**产品路径上没有人把前者送进后者**。
  `open_session(immediate=...)`（`agenerp/orchestration/opening.py:162`）摆好的位置在产品路径上是**死端**。
  本 plan 只做调用方：`explain(immediate=...)` → `ExplainLoop._open()` 透传 → `_immediate_message(pack)`
  渲染成**一条独立的 `system` 消息**，插在开场可见范围之后、`user` 提问之前。
- **实现**（`agenerp/explain/loop.py`，唯一改动的产品文件）：新增常量 `IMMEDIATE_PREFIX` /
  `IMMEDIATE_BLOCK_KEY`、`ExplainLoop.__init__(immediate=...)`、`_immediate_message()`、
  `explain(immediate=...)`。取块**按 `key == "document"`**，不按下标；
  payload **原样 `json.dumps`**：不截断、不省略、不 unwrap §7.5 边界标记、不从 `document` 重拼。
- **判据**：新建 `tests/unit/test_explain_immediate_context.py`（J1–J11，**17 条**，`tests/unit` 503 → **520**）。
  **M1–M14 十四条变异逐条各施加一次，全部由本组判据打红**，且每条都红在 plan 指定的那条断言上；
  其中 **M3 只有 J2(b) 杀得掉、M12 只有 J10 杀得掉** —— 两条替身式判据都不是冗余。逐条记录在 plan Phase 2。
- **活端点两跑**（同题，唯一变量是带不带 ①）：证据落 `docs/evidence/p1-immediate/`。
  **H4 吻合**（带 ① 时对同一个 `name` 的 `doc.get` = **0**，不带 = 1）；
  **H5 部分吻合**（第 1 次模型调用 3,775 vs 1,066 高于；整跑合计 75,159 vs 101,282 低于 ——
  前提「两跑除注入外一样」错了，实测轮数 9 vs 12）。
  ⚠️ **每侧只有一跑，不与 P1.4 的 45,195 / P1.7 的 58,579 作任何优劣比较**（D-16）。
- **owner doc**：`docs/architecture/module-boundaries.md` **新增 §7.12**（D1/D2/D3 三条裁定与被否决备选、
  J1–J11 落点、「① 层不查权限」边界与它的 successor）；
  `docs/design/context-and-memory.md` §8.2 ① 行**只补落点指针**（`git diff` 实证：单行内**纯插入**，无一处改写）。
- **未动的东西**（照实记）：`docs/backlog/p1-insight-roadmap.md` 的 Work Item Status 块**一个字未改** ——
  工作项 10（P1.8）保持 `todo`，本 plan 是它的**前置**而不是它本身（`tests/ui/test_sidebar.py` 未创建、也未声称满足）。
  `tests/gates/**` · `.github/workflows/**` · `missions/**` · `docs/masterplan/DECISIONS.md` **零改动**；
  `docs/masterplan/STATE.md` **只追加**。
- **与 plan 字面的一处偏离，照实记**：plan Phase 3 写「`docs/logs/2026/08-24.md` 追加一条」，
  实际执行日是 **2026-08-25**，按 `docs/logs/00-log-writing-guide.md` 的「一天一个文件」写进本文件。
  另：plan §5 写 `DASHSCOPE_API_KEY` 在 `.env`，实读已迁到 `~/.config/agenerp/secrets.env`（0600，仓库目录外），
  活跑因此**未 blocked**。两处都记在 plan 的 `## Closure`。

**full-green 验证（命令原文 + 退出码，同一轮）**：

- `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q`
  → **exit 0**（`门禁 11 项：预期红 0，绿 11，跳过 0` · 503 → **520 passed**）
- `python3 -m pytest tests/context -q` → **exit 0**（`53 passed`，逐字不变）
- `python3 -m pytest tests/tools -q` → **exit 0**（`81 passed, 12 skipped`，逐字不变）
- `python3 -m pytest tests/routing -q` → **exit 0**（`164 passed, 1 skipped`，逐字不变）
- `python3 -m pytest tests/contracts -q` → **exit 0**（`151 passed`，逐字不变）
- `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments`
  → **exit 0**（`All checks passed!`）

**提交**：实现与判据在 **`b112d08`**；本条日志里的 sha 由紧随其后的纯文档提交回填
（sha 写不进它自己命名的提交，回填提交只改 plan / 本文件 / `STATE.md` 三个文件）。

**验证范围**：本机；`tests/gates` 的 live 语义与 CI 服务端复跑**不在本轮范围内**，残余风险照实登记在 plan。

---

### 2026-08-25 · plan `2026-08-24-2311-1` 独立关闭审计回填落账 + 本批第二个 plan 起草稿入仓

plan：[`docs/plans/p1-insight/2026-08-24-2311-1-immediate-context-into-explain-loop.md`](../../plans/p1-insight/2026-08-24-2311-1-immediate-context-into-explain-loop.md) ·
[`docs/plans/p1-insight/2026-08-24-2311-2-desk-embed-carrier-decision.md`](../../plans/p1-insight/2026-08-24-2311-2-desk-embed-carrier-decision.md)

- **本轮是纯文档轮：产品代码与判据零改动**（`git diff --stat` 只有 plan 文件；`agenerp/**`、`tests/**` 一个字未动）。
- **回填的是什么**：`2026-08-24-2311-1` 收口当轮唯一未勾的 Closure Gate
  —— `closure audit was independent` —— 由 mission-driver 另派的 fresh session 独立审计器补做完成，
  结论**通过**；plan 的 `Closure Gates` 该行由 `[ ]` 翻为 `[x]`，`Closure Audit Evidence` 与 `Follow-up`
  同步改写为「已完成」，逐条记录（六条命令复跑、§8 R4 自证防线、M1–M14 抽查四条变异、§7.12 无越界、
  红线复核、落点在场）写在 plan 的 `Closure Audit Evidence` 节。
  ⚠️ **归属照实记**：审计由独立审计器执行并写入，**本轮 build-verify 步骤只做落账提交，不代其自证**。
- **新入仓**：`2026-08-24-2311-2-desk-embed-carrier-decision.md`（P1.8 上半 · Desk 承载面选型，
  `Plan Status: active`，**尚未执行**）。它的 §0.1「执行期重取基线的实读结果」仍为空 —— 按该 plan 自己的规矩，
  本节为空即视为未重取基线，**开工前必须先填**。
- **未动的东西**（照实记）：`tests/gates/**` · `.github/workflows/**` · `missions/**` ·
  `docs/masterplan/DECISIONS.md` · `docs/masterplan/STATE.md` · `docs/backlog/p1-insight-roadmap.md`
  **本轮零改动**；`tools/gates/expected-red.txt` 已是空名单（预期红 0 条），**本轮无行可划**。

**full-green verification（命令原文 + 退出码，同一轮实跑）**：

- `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q`
  → **exit 0**（`判定模式：default` · `门禁 11 项：预期红 0，绿 11，跳过 0` · `✅ 与预期红名单完全一致` · `520 passed`）

**验证范围**：本机，且**只跑了 mission 配置里的这一条命令**。`tests/context` / `tests/tools` / `tests/routing` /
`tests/contracts` / `ruff` 本轮**未跑**（上一条日志里的那批数是上一轮的，不重复主张）；
`tests/gates` 的 live 语义与 CI 服务端复跑**不在本轮范围内**。本条日志的提交 sha 无法写进它自己命名的提交，
按既有先例由下一轮回填或在提交信息中以 `git log` 对照。

---

### 2026-08-25 · plan `2026-08-25-1159-1` 独立收口审计回填落账

plan：[`docs/plans/p1-insight/2026-08-25-1159-1-explain-http-service.md`](../../plans/p1-insight/2026-08-25-1159-1-explain-http-service.md)

- **本轮是纯文档轮：产品代码与判据零改动**（`git status --porcelain` 只有该 plan 一个文件；
  `agenerp/**`、`tests/**` 一个字未动）。
- **回填的是什么**：`2026-08-25-1159-1`（解释服务的 HTTP 面）收口当轮唯一未勾的 Closure Gate
  —— `closure audit was independent` —— 由独立收口审计者（fresh context、非该 plan 的执行者，
  基线 sha `06a2d1f`）补做完成，结论 **approved**；plan 的 `Closure Gates` 该行由 `[ ]` 翻为 `[x]`，
  `Closure Audit Evidence` 新增「补做记录」节（七条命令逐条复跑、反空壳复核、Goal 6 引用漂移复核、
  红线复核、五点一致复核、Deferred 诚实性复核），`Follow-up` 里「独立收口审计待补」划掉。
  ⚠️ **归属照实记**：审计由独立审计者执行并写入，**本轮 build-verify 步骤只做落账提交，不代其自证**；
  该节里那些数字是审计者自己跑出来的，不是我这一轮的复跑结果。
- ⚠️ 审计者本人也照实留了一格：`verification scope limited` **不因审计通过而变成 full green**
  —— 整仓 `pytest tests -q -m "not live"` 与 CI 服务端复跑，审计那一轮同样未跑。
- **未动的东西**（照实记）：`tests/gates/**` · `.github/workflows/**` · `missions/**` ·
  `docs/masterplan/DECISIONS.md` · `docs/masterplan/STATE.md` · `docs/backlog/p1-insight-roadmap.md`
  **本轮零改动**；`tools/gates/expected-red.txt` 已是空名单（预期红 0 条），**本轮无行可划**。

**full-green verification（命令原文 + 退出码，本轮实跑）**：

- `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q`
  → **exit 0**（`判定模式：default` · `门禁 26 项：预期红 0，绿 26，跳过 0` · `✅ 与预期红名单完全一致` ·
  `756 passed, 6 skipped in 8.23s`）
- 那 6 条 skip 落在 `tests/unit/test_explain_service_body.py`（断言体够不到活栈时 skip），
  **不是** `tests/gates` 里的 skip —— 门禁那 26 项本轮跳过 0。

**验证范围**：本机，且**只跑了 mission 配置里的这一条命令**。`tests/context` / `tests/tools` / `tests/routing` /
`tests/contracts` / `ruff` 本轮**未跑**（plan 补做记录里的那批数是审计者那一轮的，不重复主张）；
`tests/gates` 的 live 语义与 CI 服务端复跑**不在本轮范围内**。本条日志的提交 sha 无法写进它自己命名的提交，
按既有先例由下一轮回填或以 `git log` 对照。
