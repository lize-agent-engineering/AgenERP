# Development Log - 2026-08-21

### 2026-08-21 · plan-2026-08-21-1922-3 差集 apply B 半 · `EXECUTE` Phase 3（承重条款 live 转绿 + 变异验证 + 文档）

- **Proof：承重条款 `test_removing_from_pack_actually_deletes_on_site` 在活站点上转绿。**
  实跑落在 **fixture 自己拉起的一次性栈**上（起手时本仓的栈还跑着，先 `docker compose down` 拆掉并
  按 `_running_frontend_port()` 的口径确认无输出、端口空闲；跑完复查 `docker compose ps` 无输出，
  证实是 fixture 自己 `up` 又 `down` 的）。
  `AGENERP_HTTP_PORT=18080 AGENERP_LIVE=1 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin python3 -m pytest tests/gates/test_customization_roundtrip_delete.py -q`
  → **exit 1**，`1 failed, 3 passed in 28.88s`（导出两条 + **删除这条**）。
- **红因如预判挪了位**：唯一 FAILED 的 `test_no_orphan_column_left_behind` 逐字红在
  `agenerp/snapshot.py:328` 的 `schema_drift NotImplementedError`——**它第一次走得到那一步**
  （落地前红在 `agenerp/apply.py:107` 的 `execute_plan`）。它归工作项 6 的第二个 plan，本 plan 关闭时仍红。
- **Proof：全量 `capture` 前后对照，两处边界各自达标。**
  ① 围绕**整个 pytest 文件的运行**：前后各 10 条、逐条相同，`diff` → **exit 0 无输出**（差集为空，
  探针由 `live_site` teardown 清掉）。② 围绕**单次 `apply_pack`**：12 → 11，
  **差集·消失恰好 `[('Item','agenerp_scope_probe_item')]`、差集·新增为空**。
  这两格跑在我自己 `up` 的一套栈上（整文件前后快照要求栈在 pytest 之外也活着，一次性栈量不到 after），
  **如实分栈记录**，不含糊成「都在一次性栈上做的」。
- **Proof：作用域安全的活站点验证。** 站点上同时建 Item 与 Customer 两个探针，只导 Item 的包，
  apply 一次 → **Item 探针消失、Customer 探针纹丝不动**；WARNING 逐字列出被跳过的 11 条
  （含 `('Customer','agenerp_scope_probe_customer')` 与 10 条应用自带字段）。
  **不收窄的话这一次会连删 11 条，而门禁照样绿**——单测里的假客户端证明不了这一点，这条不能省。
- **Proof：变异验证（承重条款必须做）。** 把 `execute_plan` 的删除改成 no-op →
  该条**逐字转红**「从定制包删除并 apply 之后，字段仍在站点上 …」。还原后
  `shasum -a 256 agenerp/apply.py` 与变异前同为 `861d9f20…f1b574fd`，`git status --porcelain`
  与**变异前基线**逐字相同（不用「`git diff` 为空」当判据——Phase 2 的产物此时尚未提交）。
- **判定器：live 环境退 1 是预期，不是回归。** live 下 `check_expected_red.py` → **exit 1**，
  报「名单内 5 条却绿了」（本 plan 让其中的承重条款转绿，把第 1 顺位预告的矛盾放大了一档）。
  **名单一行未动**：默认判定环境下 L2 恒红，划了立刻假红；处置权在 `docs/masterplan/STATE.md` §3
  那行 needs-human，本 plan 不重复登记。收尾复跑**默认环境**判定器（栈已 down）→ **exit 0**
  （「门禁 19 项：预期红 7，绿 12，跳过 0 · ✅ 与预期红名单完全一致」），
  `git diff --numstat tools/gates/expected-red.txt` → 无输出。
- **Docs：** §11.6 落点表 `execute_plan` 由「`raise`，归工作项 6」改为「删除已实现；建/改显式拒绝」，
  并补进 `pack_doctypes` / `narrow_deletes` / `ApplyDirectionError` / `delete_custom_field` 四行；
  §11.1 的 Spike 06 表补上**本仓的实测反证**——「删掉字段再 sync 纹丝不动」说的是走 Frappe 那条路径，
  换成自建引擎后字段真的消失；**未被推翻的是「删 Custom Field 不删列」**（归 `schema_drift`）。
  roadmap 新增「5 现状」行，**保持 `planned`**（`done` 的定义要求划名单，此处不可满足）。
- **验证范围**：live 只在本机做过，**CI 未验证**，不得报成「CI 上也验证过」。

### 2026-08-21 · plan-2026-08-21-1922-3 差集 apply B 半 · `EXECUTE` Phase 2（`execute_plan` 落地 + 判据）

- **Fix：写动词白名单登记一条。** `tests/unit/test_site_client.py` 的 `WRITE_METHOD_ALLOWLIST`
  由空元组改为 `("SiteClient.delete_custom_field",)`，并在注释里写明理由与「不提供删任意 DocType
  文档的通用方法」这条边界。第 1 顺位那条结构断言本就是**白名单**形式（不是「一个写动词都不许有」），
  所以这是**收窄式演进**——每加一个写方法付一次 diff 和一次留痕，不是取消判据。
- **Add：`agenerp/site.py` · `SiteClient.delete_custom_field(doctype, fieldname)`**，本模块唯一写动作。
  成败判据沿用 `_request` 的 `200 <= status < 300`，不为删除另开分支——实测成功码是 **202**、
  删不存在的是 **404**，后者因此被判为失败并抛 `SiteError`（**不静默吞掉**）。
  文档名口径抽成模块级 `custom_field_name()`（`<dt>-<fieldname>`）。
- **Add：`agenerp/apply.py` · `pack_doctypes(path, scope)`** 算管辖面 = 包目录里**存在文件**的 DocType，
  口径与 `entries_from_payload` 同源（新增 `snapshot.doctype_from_payload`，两者共用
  `_normalized_payload`，避免第二套解析）。`read_pack` 的签名与返回类型**一个字没动**。
- **Add：`agenerp/apply.py` · `narrow_deletes(plan, covered)`** 纯函数收窄，两类丢弃**都不静默**：
  不在管辖面内、以及 `is_system_generated` 为真的，各发一条 WARNING 并逐条列出 `(doctype, fieldname)`
  （`logging.getLogger("agenerp.apply")` —— `agenerp/` 全树的第一处 logging）。
- **Add：`execute_plan(plan, site, client=None)` 实现删除路径**：按 `key` 排序 → 逐条删 → 任一条失败即抛且
  **不继续删后面的**（本层不做事务，`02-WBS.md` 划给 P3.1）。`creates`/`updates` 非空 → 抛
  `NotImplementedError` 并指名 successor；空删除集**连客户端都不构造**（零副作用）。
- **Fix：`_assert_direction` 的裸 `assert` 换成 `ApplyDirectionError`。** 理由不是风格：
  `python -O` / `PYTHONOPTIMIZE=1` 会把裸 `assert` 整条剥掉，而 B 半接上真删除之后它是挡住
  「desired/current 传反 → 把整站定制算成待删」的最后一道闸。判据里有一条**子进程带 `-O` 实跑**的用例。
- **Add：`agenerp/pack.py` · `apply_pack` 委派链由三步变四步**（读包 → 求差 → **收窄** → 执行），
  签名不变。收窄只能落在这里：管辖面要读包**目录**才算得出来，`execute_plan` 只拿得到条目。
- **Proof：新增 `tests/unit/test_apply_execute.py`，19 条用例**（plan 要求 ≥ 8），全部喂假客户端、不连站点。
  关键的一条是作用域收窄的**正反两断言写在同一个用例里**：恰好删 `Item.probe`（正）且对 Customer
  零请求（反）——只写反断言的话，一个什么都不删的空实现完美通过。另有「文件在、数组空」回归、
  covered 按载荷 `doctype` 而非文件名、失败即停、`-O` 下方向闸仍在、端到端反 upsert 链路。
- **Fix：`tests/unit/test_apply_plan.py` 里那条「必须红在执行未实现」如实改写**（它是 A 半的判据，
  不是门禁）：改成「建/改仍显式拒绝且消息指名 successor」，并**新增一条反向断言**
  `test_execute_plan_no_longer_reds_unconditionally`——把实现退回 `raise` 就红。
- 验证（命令原文 + 退出码，退出码单独取 `$?`）：
  - `python3 tools/gates/check_expected_red.py` → **0**（「门禁 19 项：预期红 7，绿 12，跳过 0 · ✅ 与预期红名单完全一致」）
  - `python3 -m pytest tests/unit -q` → **0**（**164 passed**，较 Phase 1 的 144 增 20）
  - `ruff check agenerp tests/unit tests/contracts` → **0**（All checks passed!）
  - `python3 -m pytest tests/contracts -q` → **0**（151 passed）
- owner-doc 更新**有意留到 Phase 3**（§11.6 落点表要等 live 实测结论才写得准），不是遗漏。

### 2026-08-21 · plan-2026-08-21-1922-3 差集 apply B 半 · `EXECUTE` Phase 1（实测删除语义 + 三条裁定）

- **Add：`docs/context/ai-autonomy-policy.md` 的 Protected Areas 表补第 9 行**——「对活站点的破坏性写
  （删除 Custom Field）」，规则 `plan-first`，Required Evidence 写明「独立草案评审 + 独立关闭审计 +
  实跑前后全量 `capture` 对照」。该表原本逐字写着数据删除 `none`「将来出现时，先在本表补行再动手」，
  本行是**加严**（此前默认 `implement`），不是放宽。
- **Explore：活站点实测四件事**（`_tmp/explore_delete_semantics.py`、`_tmp/explore_probe_flag.py`，
  一次性脚本，不 import `tests/gates/`）：
  ① Custom Field 文档名确为 `<dt>-<fieldname>`（`POST` 返回 `name = 'Item-agenerp_explore_probe'`）；
  ② `DELETE /api/resource/Custom Field/<name>` → **HTTP 202** `{"data":"ok"}`，删后 `GET` → 404；
  删一个不存在的 name → **404 `DoesNotExistError`**（不静默成功）；
  ③ 删后复读 `capture`：11 → 10，差集恰好 `[('Item','agenerp_explore_probe')]`；
  ④ 全站 Custom Field **10 条、7 个 DocType、全部 `is_system_generated = 1`**，`Item` 上 0 条；
  REST 建的探针实测 `is_system_generated = 0`。
- **不执行删除的预演（停手闸）通过**（`_tmp/rehearse_no_delete.py`）：按门禁那五行的形状走到 `plan_apply` 为止，
  `len(creates)=0 len(updates)=0 len(deletes)=11` → **creates/updates 均为空，本 plan 继续**。
  同一次输出坐实了两件事：包只含 `Item.json` 时 `deletes` 里混进了 **10 条别的 DocType 上应用自带的字段**
  （不收窄即静默误删，而门禁照样绿）；`remove_field` 之后 `Item.json` 是**「文件在、数组空」**
  （按包**条目**收窄的话 `Item` 不在集内，承重条款会红）。
- **三条 `Decision` 已写进 `docs/architecture/module-boundaries.md` §11.6**：
  ① 收窄集 = 包目录里**存在文件**的 DocType 集合（落点 `pack_doctypes` + `apply_pack` 的委派链）；
  ② `is_system_generated` 的字段**排除**在删除集外；③ 建/改**显式拒绝**，不静默跳过。
  各有备选与残余风险，逐条落在该节的 `#### apply 对活站点执行删除的三条裁定` 里。
- 本 phase **无 `agenerp/` 代码产物**（按 plan 的 Targets），实现落在 Phase 2。

### 2026-08-21 · plan-2026-08-21-1922-2 定制包导出 · `EXECUTE` Phase 1–3（`export_customizations` 落地 + live 转绿两条）

- **Proof：`test_added_field_exports_into_pack` 与 `test_export_produces_readable_diff_only` 在活站点上转绿。**
  `AGENERP_HTTP_PORT=18080 AGENERP_LIVE=1 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin python3 -m pytest tests/gates/test_customization_roundtrip_delete.py -q`
  → **exit 1**，`2 failed, 2 passed`。另两条 FAILED **逐字红在 `agenerp/apply.py:107` 的 `execute_plan` NotImplementedError**，
  与 plan 起草时的预判一字不差——本轮走不到 `schema_drift`。
- **Add：`agenerp/pack.py` 落两个函数。** `render_doctype_file`（纯函数，排版的唯一落点）+
  `export_customizations(doctype, into, source=None)`。站点读**复用 `capture` + `SiteSnapshotSource`，
  没开第二条站点查询**——往返一致因此由构造保证，关分页完整性直接继承 §11.7。
  两处 import 必须在**函数体内**（顶层即 `pack` ↔ `snapshot` 循环导入）。
- **Decision：排版取「逗号独占一行」。** 门禁那条逐行断言只放行 `line.strip() in "{}[],"`，
  而 `},` **不在其中**（`}` 与 `,` 在该串里不相邻）。行尾逗号排版在「探针插到数组末尾」那一格会红；
  今天 Item 恰好 0 条定制使它不可达，但**那是站点数据的偶然，不是产物形状的性质**，所以不取。
  空数组也不能写成 `"custom_fields": []`（从空变非空会改到那一行）。
- **Decision：属性投影不收窄**，`agenerp/snapshot.py` 一个字节未动。收窄的唯一落点
  `entries_from_site_rows` 同时喂着第 1 顺位刚在 live 转绿的快照门禁，而**默认判定环境下那条恒红、
  回归在 `GATE_VERIFY` 里一格都看不见**。用「让包文件行短一点」换一条只有 live 才看得见的回归风险，不划算。
- **Add：导出这条路径刻意不走 `snapshot.resolve_source`。** 它「无站点配置就退回离线来源」，
  在这里会写出一个空包，而**空包在 apply 眼里等同于「该 DocType 的定制全被删了」**。
  分界写死：空数组文件只在站点成功答出「零条目」时才写；站点名未配置 / 答不上话 / 认证失败一律抛
  `SiteError` 且不留任何文件。两条各有一条单测把守。
- **Proof：`tests/unit/test_pack_export.py` 新增 15 条**（往返不变量 1–5 各一格以上、邻居文件逐字节未变、
  抛错不产文件、无 `AGENERP_SITE` 抛而不退回、**排版回归 4 格**、写读两侧键名一致）。
  排版回归**不 import `tests/gates/` 的任何东西**，差分用 `difflib(n=0)`；`insert-at-end` 那一格
  把「行尾逗号排版会红」钉死，判据从此不依赖站点数据的偶然。
  `tests/unit` 由 **130 → 144**（+15 新增，−1：`export_customizations` 从 `NOT_YET_IMPLEMENTED` 搬进 `IMPLEMENTED`）。
- **Proof：变异验证有牙齿，但第一次写的两条变异里有一条空转、一条红得不对 —— 照实记，两条都重做了。**
  ① 顶层加 `"exported_at"` **常量**行 → 门禁**仍绿**（常量两次导出逐字相同，`git diff` 里根本不出现）；
  改成 `datetime.now().isoformat()` 才转红，逐字
  `AssertionError: diff 里夹带了与本次改动无关的内容：['"exported_at": "…039229",', '"exported_at": "…523794",']`。
  ② 在**排序阶段**丢 `fieldname` → 红在 `KeyError: 'fieldname'`（实现自己崩了，证明不了门禁有牙齿），作废；
  改在**渲染阶段**丢 → `test_added_field_exports_into_pack` 转红，逐字 `AssertionError: 新增的字段没有进定制包`。
  还原核对：`shasum -a 256 agenerp/pack.py` 变异前 / 还原后同为 `fa5f2747…1a6889ad`，`git status --porcelain` 无残留。
- **新事实（已写进 §11.6）：两条导出门禁各挡各的，谁都不是另一条的替身。**
  `test_export_produces_readable_diff_only` 挡的是**易变**噪声，**挡不住恒定的多余键**；
  而渲染阶段丢掉 `fieldname` 时它照样绿——条目行里的 `"name": "Item-agenerp_gate_roundtrip"` 仍含探针名。
  真正挡「多余键」与「身份键丢失」的是 `tests/unit/test_pack_export.py` 的往返不变量。
- **Add：§11.6 增「定制包写入口径」+「活站点实测」两段**，并把原「残余风险：`export_customizations`
  尚未实现，包的真实产出形状还没有活证据」改写为实测结论——**布局 (b) 未被推翻**，
  `read_pack` 一行未改、`plan_apply` 形状未动。
- **Add：roadmap 对照表第 6 行归属更正 + 新增「6 现状」行。**
  此前该行写「其余 3 条 + `test_field_addition_shows_up_as_structured_change`」，两处都不准：
  后者实由**工作项 4** 承接并已在 live 转绿；「其余 3 条」把删除那条也算了进来，而它归工作项 5。
  **工作项 6 保持 `planned`**，`tools/gates/expected-red.txt` **一行未动**——沿用人在 STATE §2（11:20Z）的裁定。
  本轮**未追加 STATE 证据行**：名单矛盾已由第 1 顺位登记，不重复登记。
- **验证范围（明写）**：live 命令**只在本机跑过**，`missions/p0-foundation.json` 的 `commands.test` 与
  CI 的 `gates-l1`（`-m 'not live'`）都跑不到。代偿控制是两条变异 + 单测里的排版回归四格。
- **限制（照实记）**：本轮执行环境明令不得调用子代理，**关闭审计是 solo cold-replay，不是独立的**，
  按 AGENTS.md 的 Reviewer-Availability Fallback 记录限制并挂进 plan 的 Follow-up。

- **Phase 1–3 验证（命令原文 + 退出码，逐条）**：
  | 命令 | 退出码 |
  |---|---|
  | `python3 tools/gates/check_expected_red.py` | **0**（门禁 19 项：预期红 7，绿 12，跳过 0） |
  | `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` | **0**（144 passed） |
  | `python3 -m pytest tests/unit/test_pack_export.py -q` | **0**（15 passed） |
  | `ruff check agenerp tests/unit tests/contracts` | **0** |
  | `… AGENERP_LIVE=1 … python3 -m pytest tests/gates/test_customization_roundtrip_delete.py -q` | **1**（`2 failed, 2 passed`；两条导出 PASSED） |
  | `… AGENERP_LIVE=1 … python3 -m pytest tests/gates/test_snapshot_diff_structured.py -q` | **0**（3 passed，加固复跑） |

### 2026-08-21 · plan-2026-08-21-1922-1 站点来源接活站点 · `EXECUTE` Phase 3（活站点实跑 · 名单矛盾实测 · 交接）

- **Proof：`test_field_addition_shows_up_as_structured_change` 在活站点上转绿 —— exit 0，3 passed。**
  这是本 plan 的头号交付物，也是本仓第一次让一条 L2 门禁在真站点上绿。
- **Proof：plan 原文那条命令实测 exit 1，原因不是实现，是起栈顺序 —— 照实记，不粉饰。**
  两条 L1 快照门禁（`test_two_snapshots_of_unchanged_site_diff_empty` / `test_diff_is_structured_not_text`）
  **不取任何 fixture**，配了 `AGENERP_SITE` 之后它们在 `compose_stack` 把栈拉起来**之前**就跑完了，
  逐字红在 `agenerp.site.SiteError: POST http://127.0.0.1:18080/api/method/login 连不上：<urlopen error [Errno 61] Connection refused>`
  （`FF.`，两次原样复跑逐字一致：25.80s / 25.92s）。**第三条 live 门禁在同一次运行里是绿的。**
  栈**预先起好**再跑同一条命令 → **exit 0（3 passed）**。这是 harness 接线问题（红线 1，只有人能动），已写进 STATE §3。
- **Proof：变异验证有牙齿。** `SiteSnapshotSource.read` 改成返回空元组 → 同一条命令 **exit 1**，
  逐字红在 `test_field_addition_shows_up_as_structured_change`（`Diff(added=(), removed=(), changed=()).is_empty`）；
  还原后 **exit 0**，`shasum -a 256 agenerp/snapshot.py` 变异前 / 还原后同为 `f3d421f7…e0496e21`。
- **Proof：完整性在 live 层证到了，但换了判据 —— plan 原定的阈值不成立，照实改写。**
  plan 写「`len(capture("doctypes"))` 必须 > 20，等于 20 就是分页没关掉」；实测该站点 Custom Field
  **总共只有 10 条**（站点自报 `frappe.client.get_count` 也是 10 —— 两数一致说明读全了，截断只会给出**恰好 20**），
  10 < 20 让原阈值无从判别。改用同一条代码路径读一张行数远超默认页长的表并做变异：
  `list_resource('DocType')` → **775 行**（站点自报也是 775）；**删掉关分页那个参数 → 恰好 20 行**；还原 → 775，
  `agenerp/site.py` 的 sha 变异前后同为 `22ae0d34…9165d424`。这正好补上「假传输证明不了 Frappe **认**这个参数」那个缺口。
- **Proof：名单矛盾实测，只出现一种形态。** 带 live env 跑判定器 → **exit 1**，
  「门禁 19 项：预期红 5，绿 14，跳过 0 / ❌ 名单内的门禁却绿了」两条：本 plan 转绿的那条 +
  `test_stack_boots_and_all_services_healthy`（人在 §2 11:20Z 已裁定过）。
  **plan 预判的第二种形态「名单外的门禁红了」在栈已起的前提下没有出现**。
  **`tools/gates/expected-red.txt` 一行未动**，沿用人的既有裁定：live 下转绿不构成划名单的理由。
- **Decision：STATE §3 追加一条补充事实行**（红线 5：只追加；`git diff --numstat` → **`8	0`**，第二列为 0）。
  照 §3 里 P0.7 那条「补充事实行，不另开条目」的先例，挂在既有那条工作项 4 / `compose_stack` 的 `[open]` 上，
  **不新开 needs-human 条目**。补三条新事实：① harness 官方 L2 跑法不设 `AGENERP_SITE`，该门禁在那条跑法下仍红，
  三条出路（conftest 补 env / 改口径为完整命令 / 自动探测）loop 不替人选；② 判定器实测退出码与输出原文；
  ③ 起栈顺序陷阱。授权链矛盾照既有两行的写法原样引出，不擅自消解。
- **Add：`docs/context/project-context.md` 验证命令表增「L2 live 门禁（快照）」一行**，写明完整命令、
  三处必须一起看的口径（端口 18080 / `AGENERP_SITE` 必须由命令给 / 必须先起栈），并明写
  **它不在 `missions/p0-foundation.json` 的 `commands.test` 里**，`GATE_VERIFY` 复跑不到，代偿控制是变异验证 + 独立关闭审计。
- **Add：roadmap 工作项 4 保持 `planned`，对照表补一行现状。** 不置 `done` 的理由**不是**「名单没划」，
  而是本行给它绑的是「提供 `live_site` fixture」、它没有一条属于自己的门禁测试，`done` 的字面定义不可满足
  —— 与工作项 7 同一情形（那条已由人以 `[resolved]` 处置过）。

- **Phase 3 验证（命令原文 + 退出码，逐条）**：
  | 命令 | 退出码 |
  |---|---|
  | `AGENERP_HTTP_PORT=18080 AGENERP_LIVE=1 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin python3 -m pytest tests/gates/test_snapshot_diff_structured.py -q`（栈未预起） | **1**（`FF.` · 2 failed, 1 passed · 25.80s；复跑同因、25.92s） |
  | `AGENERP_HTTP_PORT=18080 docker compose -f docker-compose.yml up -d --wait` | **0**（20.5 秒，**热卷**，六个服务 healthy） |
  | 同上 pytest 命令，**栈预先起好** | **0**（3 passed，3.03s） |
  | 同上，变异（`read` 返回空元组） | **1**（1 failed, 2 passed，指名 `test_field_addition_shows_up_as_structured_change`） |
  | 同上，还原后 | **0**（3 passed） |
  | `AGENERP_HTTP_PORT=18080 AGENERP_LIVE=1 AGENERP_SITE=frontend AGENERP_SITE_URL=http://127.0.0.1:18080 AGENERP_ADMIN_PASSWORD=admin python3 tools/gates/check_expected_red.py` | **1**（预期红 5，绿 14，跳过 0；「名单内的门禁却绿了」两条） |
  | `python3 tools/gates/check_expected_red.py`（默认环境） | **0**（预期红 7，绿 12，跳过 0） |
  | `python3 -m pytest tests/unit -q` | **0**（130 passed） |
  | `ruff check agenerp tests/unit tests/contracts` | **0** |
  | `AGENERP_HTTP_PORT=18080 docker compose -f docker-compose.yml down` | **0**（`agenerp-` 容器残留 0；站点上探针残留 0 条） |
- **verification scope limited（明写）**：live 实证**只在本机**做过（compose v5.0.2、端口 18080、**热卷**，
  20.5 秒不是冷起的 68 秒）。CI runner 是 compose 2.38.2，版本差沿用 plan `…-1022-1` 已登记的 watch-only residual。
  **不得报成「CI 上也验证过」。**
- **红线自查**：`git diff --name-only -- tests/gates/ .github/workflows/ missions/ docs/masterplan/DECISIONS.md` → 空；
  `tools/gates/expected-red.txt` 未动；`docs/masterplan/STATE.md` `git diff --numstat` → `8	0`（只增不改）。


### 2026-08-21 · plan-2026-08-21-1922-1 站点来源接活站点 · `EXECUTE` Phase 2（接上 `SiteSnapshotSource.read`）

- **Add：`SiteSnapshotSource.read` 不再 `raise NotImplementedError`。** 增一个**可选注入**字段
  `client`（默认 `None` → 走 `client_from_env`），既有构造式 `SiteSnapshotSource(site)` 的两处调用点一字未改。
  `read("doctypes")` → 站点上**全部** Custom Field → 过 `normalize` → 按 `key` 排序。
- **Add：`entries_from_site_rows` 是「一行站点数据变成哪些属性」的唯一落点。** 投影口径「剥易变键后全留」；
  身份取 **`dt`**（Custom Field 的归属 DocType 列，**不叫 `doctype`**——`_DOCTYPE_KEY` 是包文件那侧的键）+ `fieldname`。
  未知 scope **显式抛并指名收到的 scope**，不返回空元组（返回空会让「scope 拼错」和「没有定制」长得一样）。
- **Fix：owner-doc drift（确认的，不可降级）。** §11.5 不变量表逐字写着 `SnapshotSource` →「位置不存在 → 空元组，
  不抛异常」，`agenerp/snapshot.py` 的 Protocol docstring 复述了同一句；B 半让站点来源在站点答不上话时**抛**，
  两处随即变成陈述性错误。**同一个 phase 里改掉**：表格那格改成「离线来源 → 空元组；站点来源 → 抛 `SiteError`，见 §11.7」，
  docstring 同步。顺带修掉同批被推翻的三处陈述：§11.5「接缝」段（原写「目前抛 NotImplementedError」）、
  §11.6「`apply_pack` 现在红在哪」、§7.6「接活站点的一切都没做」。（`module-boundaries.md` 只禁改**标题**，表格与正文不在禁令内。）
- **Fix：两条下游单测的红因随之改写（都在 `tests/unit/`，不是门禁）。**
  `test_apply_pack_reds_on_the_site_half_not_on_diffing` 与 `test_contract_surface.py` 的 `apply_pack` 参数化
  原本期待 `NotImplementedError`；B 半落地后 `apply_pack` 在离线环境里**走不到** `execute_plan`，
  先撞上「没有活站点、没有凭据」而抛 `SiteError`。**没有为了让旧断言继续成立而让站点侧降级返回空**——
  那正是本仓明令禁止的假判据。两处都改成期待被登记过的失败类型，并在测试里清空凭据 env 以免判定面随本机漂移。
- **Proof：单测层变异验证（`GATE_VERIFY` 唯一看得见的层）有牙齿。** 把投影里的 `normalize(row)` 换成 `dict(row)`：
  `python3 -m pytest tests/unit -q` → **exit 1**，`FAILED tests/unit/test_snapshot_capture.py::test_site_read_strips_volatile_columns`
  （断言输出里逐字列出没被剥掉的 `modified` / `creation` / `owner` / `modified_by`）。还原后 → **exit 0，130 passed**。
- **Proof：L1 两条快照门禁在无 `AGENERP_SITE` 时行为逐字不变** —— 单跑 `2 passed`，判定器仍 exit 0、19 项分布不变。

- **Phase 2 验证（命令原文 + 退出码）**：
  | 命令 | 退出码 |
  |---|---|
  | `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` | **0**（门禁 19 项：预期红 7，绿 12，跳过 0；130 passed，Phase 1 后 120 → **+10**） |
  | `python3 -m pytest tests/unit -q`（变异：投影去掉 `normalize`） | **1**（1 failed, 129 passed，指名 `test_site_read_strips_volatile_columns`） |
  | `python3 -m pytest tests/unit -q`（还原后） | **0**（130 passed） |
  | `python3 -m pytest tests/gates/test_snapshot_diff_structured.py::test_two_snapshots_of_unchanged_site_diff_empty tests/gates/test_snapshot_diff_structured.py::test_diff_is_structured_not_text -q` | **0**（2 passed） |
  | `python3 -m pytest tests/contracts -q` | **0**（151 passed） |
  | `ruff check agenerp tests/unit tests/contracts` | **0** |
- **`agenerp/` 全树 `raise NotImplementedError` 复核（逐条，行号以实际为准）**：`agenerp/pack.py:66`（`export_customizations`，工作项 6）·
  `agenerp/snapshot.py:314`（`schema_drift`）· `agenerp/apply.py:107`（`execute_plan`）。**正好三处，`SiteSnapshotSource.read` 已不在其中。**


### 2026-08-21 · plan-2026-08-21-1922-1 站点来源接活站点 · `EXECUTE` Phase 1（站点只读传输落盘）

- **Add：新建 `agenerp/site.py`** —— `agenerp` 里第一行、也是唯一一处连活站点的产品代码
  （改动前全树 grep 无 `urllib`）。`SiteClient` / `SiteError` / `client_from_env` /
  `Transport` + `UrllibTransport`，只用标准库，**只有读方法**。
- **Decision：认证取「token 优先、会话登录回退」。** 只做会话登录会把口令带进每次运行且撞登录端点限流；
  只做 token 则零依赖 compose 栈上没有现成 key、L2 门禁跑不起来。取舍表与**残余风险**
  （回退路径把口令读进内存）写进 `docs/architecture/module-boundaries.md` **新增的 §11.7**。
- **Add：凭据口径 —— 产品代码不内置口令默认值。** 缺凭据时抛 `SiteError` 并**指名三个变量**
  （`AGENERP_API_KEY` / `AGENERP_API_SECRET` / `AGENERP_ADMIN_PASSWORD`）；半套 token 判为配错而非静默回退。
  `tests/gates/conftest.py:44` 那个 `admin` 默认值是测试脚手架，不是产品口径。
- **Proof：`tests/unit/test_site_client.py` 新增 16 条**，全部喂**注入式假传输**（不起本地 `http.server`
  —— 本机 8080 被另一套常驻栈占用是实测事实，再绑端口是自找的不稳定源）。唯一例外是
  「连不上 → `SiteError`」那条，走真 `UrllibTransport`、用一个刚释放的端口构造 connection refused。
  覆盖：非 2xx / 401 / 非 JSON / 连不上 / 缺凭据指名变量 / 半套 token / 无内置口令默认值 /
  token 跳过登录往返 / 会话登录只登一次 / `Host` 头 = 站点名 / URL 编码保留 `/` /
  **显式关分页参数** / 25 条不截断 / 载荷缺 `data` 抛 / **写动词白名单**（白名单为空且是可见字面量）。
- **写动词判据写成白名单而不是绝对禁令**：绝对禁令会在工作项 5 的删除段往同一模块加
  `delete_custom_field` 时被一条已关闭 plan 的判据当场打红，届时只剩「动别人的判据」和「卡住」两条路。
  白名单让它按**收窄**演进，每加一个写方法都要付一次 diff 与一次留痕。

- **Phase 1 验证（命令原文 + 退出码）**：
  | 命令 | 退出码 |
  |---|---|
  | `python3 -m pytest tests/unit/test_site_client.py -q` | **0**（16 passed） |
  | `python3 -m pytest tests/unit -q` | **0**（120 passed，开工基线 104 → **+16**） |
  | `ruff check agenerp tests/unit tests/contracts` | **0** |
  | `python3 tools/gates/check_expected_red.py` | **0**（门禁 19 项：预期红 7，绿 12，跳过 0） |
- **红线自查**：`git diff --name-only -- tests/gates/ .github/workflows/ missions/ docs/masterplan/DECISIONS.md`
  → 输出为空；`tools/gates/expected-red.txt` 未动。

### 2026-08-21 · plan-2026-08-21-1634-2 应用侧 healthcheck · `EXECUTE` Phase 5（文档、roadmap 写回与交接）

- **Add：`docs/context/project-context.md` 的 “Run app locally” 已改写，两件事分开说。**
  ① 起栈与健康判定**已实证**（冷起 `--wait` exit 0 / 68 秒、六个服务 healthy、首页 200、变异验证有牙齿）；
  ② 工作项 8 的 **L2 门禁仍未解锁**（两条仍在 `expected-red.txt` 名单内，`compose_stack` 仍抛 `NotImplementedError`）。
  原文里明写「①**不等于**②，不得把工作项 8 报成已验证」，并点出「全部 healthy」是收窄过的集合。
- **Decision：roadmap 工作项 8 由 `todo` → `planned`，取候选 (a)。**
  (c) `done` 是谎报——roadmap 自己给 `done` 的定义是「对应门禁测试已转绿并从预期红名单划掉」，
  而那两条一条没绿、一行没划；(b) 留 `todo` 会让引擎下一轮把它当没人做过的活再取一次
  （依据是 roadmap 自己写的「顺序即执行顺序，引擎取第一个 `todo`」与那张状态值定义表，
  **不是** `flow-loader.js` 的 `activePlans()`——那个扫的是 plan 文件的 `Plan Status`，两回事）。
  对照表第 8 行同步改写，逐字写明「两条都仍在 `expected-red.txt` 内，本次一条未划掉」与**剩下两半仍缺**。
  残余风险：工作项 8 会停在 `planned`，直到人处置 `STATE.md` §3 的红线决定 + 有人做完首页文案那一半。
- **Add：`docs/masterplan/STATE.md` §3 追加一条补充事实行**（红线 5：只追加）。
  它**不另开重复条目**，只给已有那条 `compose_stack` 的 `[open]` 补两条新事实：
  ① healthy 已可判定但门禁仍被挡着；② 「全部 healthy」是 6 个不是 11 个，三个 worker 不可判，
  附逐条弃选证据（`bench doctor` 退 0 却报 disabled、rq 心跳 405 秒、`scheduler` 不是 rq worker）。
  同时登记了两项**未做且不由本 plan 做**的事（首页文案 / CI 的 L2 job），以及 verification scope limited。
  `git diff --numstat docs/masterplan/STATE.md` → **`5	0`**，第二列为 0。

- **本 plan 的聚合验证（五个阶段合并 · 命令原文 + 退出码）**：
  | 命令 | 退出码 |
  |---|---|
  | `env -i PATH="$PATH" HOME="$HOME" docker compose -f docker-compose.yml config -q` | **0** |
  | `AGENERP_HTTP_PORT=18080 docker compose up -d --wait --wait-timeout 300`（冷起） | **0**（68 秒） |
  | 同上，变异后（`frontend` 探针指向必然失败的地址） | **1**（87 秒，`container agenerp-frontend-1 is unhealthy`） |
  | 同上，还原后 | **0**（11 秒） |
  | `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:18080/api/method/ping` | **200** |
  | `docker compose down -v` | **0**（`agenerp-` 容器残留 0 个） |
  | `python3 -m pytest tests/unit -q` | **0**（104 passed） |
  | `python3 -m pytest tests/contracts -q` | **0**（151 passed） |
  | `python3 -m pytest tests/gates/test_zero_dep_boot.py -q` | **1**（`1 passed, 2 errors`，红因与改动前逐字相同） |
  | `python3 tools/gates/check_expected_red.py` | **0**（预期红 7，绿 6，跳过 0） |
  | `ruff check agenerp tests/unit tests/contracts` | **0** |
  | `bash tools/check-masterplan-links.sh` | **0**（35 条引用，断链 0） |
  | `node tools/check-doc-references.mjs` | **1，违规集合与开工基线逐行相同**（判据是「集合不变」，不是退 0） |
  | 变异前 / 还原后 `shasum -a 256 docker-compose.yml` | 两次同为 `b779737…5b428b5` |
- **红线自查（两条都跑，`git diff` 看不见未跟踪文件）**：
  `git diff --name-only -- tests/gates/ .github/workflows/ missions/` → **输出为空**；
  `git status --porcelain -- tests/gates/ .github/workflows/ missions/ tools/gates/ docs/masterplan/DECISIONS.md` → **输出同样为空**。
  `git diff --numstat tools/gates/expected-red.txt` → **无输出**，名单**逐字未动**，本次没有任何门禁由红转绿。
  本次改动共 7 个文件，代码只有 `docker-compose.yml` 一个。
- ⚠️ **verification scope limited，三层都要说清**：
  ① 起栈实证**只在本机 compose v5.0.2** 上做过，CI runner 是 2.38.2，版本差是 `2026-08-21-1022-1` 已登记的 watch-only residual，
  **不得报为「CI 上也验证过」**；
  ② 上列命令是本仓此刻可跑的全部（无 build、无 typecheck，L2 门禁仍未解锁），**不得报为「全量验证通过」**；
  ③ **起栈那几条命令不在 `missions/p0-foundation.json` 的 `commands.test` 里，`GATE_VERIFY` 子进程复跑不到**——
  这部分绿是 loop 自己给自己判的分，代偿控制是变异验证（做过，有牙齿）+ 独立关闭审计。
- **落地方式（沿用前一个 plan 的要求，写在这里备查）**：本批改动应经 PR 落地。
  理由对本 plan 只成立一半但成立的那半更重要——本 plan 往 compose 里加的 `healthcheck:` 与
  `condition: service_healthy` 是**从未被 2.38.2 解析过的新键**，版本差在这里是真实敞口，只有 PR 能在合并前看见它。
  **开 PR 属对外动作，不由 loop 执行。**

### 2026-08-21 · plan-2026-08-21-1634-2 应用侧 healthcheck · `EXECUTE` Phase 4（Proof：起栈实证 + 变异验证）

冷起（Phase 1 收尾时 `down -v` 已把卷全删，本阶段是真的从零建站），全程 `AGENERP_HTTP_PORT=18080`。

- **核心判据**：`AGENERP_HTTP_PORT=18080 docker compose up -d --wait --wait-timeout 300`
  → **exit 0，耗时 68 秒**。
  **对照改动前的基线（Phase 1 记的 exit 0 / 4 秒）**：那 4 秒里站点还没建出来、首页还打不开，
  是一条空判据；这 68 秒里 `create-site` 真的跑完了，`backend` / `frontend` 是**探针探通了**才算 healthy。
  同一个退出码，含义完全不同——这就是本 plan 交付的东西。
- **`docker compose ps -a` 全表，与 §14.2 定的集合逐行相符**：
  | 服务 | State | Health |
  |---|---|---|
  | `db` / `redis-cache` / `redis-queue` | running | **healthy** |
  | `backend` / `websocket` / `frontend` | running | **healthy** |
  | `queue-short` / `queue-long` / `scheduler` | running | *（空——按 Decision 1 不判）* |
  | `configurator` / `create-site` | exited | `Exited (0)` |
  ⚠️ 照实记一条：`--wait` 的进度输出对三个无探针 worker **也打印 `Healthy`**。
  那是 compose 把「running 即满足」显示成了 Healthy，**不是它们真的被判过**——
  `docker compose ps` 的 Health 列是空的，那一列才是真话。这正是 §14.2 要把集合写明的原因。
- `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:18080/api/method/ping` → **200**
  （与工作项 3 那次实测同口径）。
- **变异验证（判据有没有牙齿）**：把 `frontend` 探针的端口 `8080` 临时改成 `9999`（必然无人监听）→
  `docker compose up -d --wait --wait-timeout 300` → **exit 1，耗时 87 秒**，
  末行逐字是 `container agenerp-frontend-1 is unhealthy`——**能指名道姓说出是哪个服务**。
  87 秒与预期的 `start_period 30s + interval 10s × retries 6 = 90s` 对得上，也印证了
  「翻红时间必须小于 `--wait-timeout`」这条关系取值是对的。
  `docker compose ps` 同时显示 `frontend running unhealthy` 而 `backend running healthy`——只红了被变异的那一个。
- **还原判据（按草案评审第 (2) 条，取哈希不取 `git diff`）**：
  - 变异前 `shasum -a 256 docker-compose.yml` → `b77973764df9e6e3ba4c66f1b04a0572a0ea83738f3a877ca508977aa5b428b5`
  - 还原后同一条命令 → `b77973764df9e6e3ba4c66f1b04a0572a0ea83738f3a877ca508977aa5b428b5`
  - **两者相同，逐字节还原。**
  - 还原后复跑 `up -d --wait --wait-timeout 300` → **exit 0，11 秒**；首页 ping → **200**。
- **收尾**：`docker compose down -v` → **exit 0**；`docker ps -a | grep -c '^agenerp-'` → **0**（清干净）。
  无关栈 `docker-frontend-1`（Up 8 hours）、`proof-proof-1`、`openmaic-openmaic-1`、`另一项目的本地栈`
  状态与开工前逐行一致，**未受影响**。

### 2026-08-21 · plan-2026-08-21-1634-2 应用侧 healthcheck · `EXECUTE` Phase 3（Add：落 healthcheck 并收紧依赖）

只改一个文件：`docker-compose.yml`。`grep -c "healthcheck:"` 由 **3 → 6**。

- **Add：`backend` / `websocket` / `frontend` 三条 healthcheck**，探针全部是 Phase 1 在容器内跑过、
  记过退出码的那三条原样搬进来，一律 `CMD-SHELL` + `curl -fsS --max-time 5`：
  `backend` 带 `-H "Host: frontend"`（不带就是 404，Phase 1 实测 exit 22）；
  `websocket` 打 socket.io 的 polling 握手端点（根路径实测不回应）；
  `frontend` 打 `:8080/api/method/ping`（nginx 自带站点头）。
  取值 `backend` 10s/5s/6/60s，另两个 10s/5s/6/30s。
- **Add：`x-backend-defaults` 补 `create-site: condition: service_completed_successfully`**（Phase 2 Decision 2）。
  三行就地注释写清了为什么不走「调大 `start_period`」那条路，以及「三个 worker 启动时机被一并推迟」这个代价。
- **Change：`frontend.depends_on` 两条由 `service_started` 收紧为 `service_healthy`。**
  两个依赖都真的落了 healthcheck，才敢改——`service_healthy` 指向无探针的服务时 `config -q` 仍退 0，
  但 `--wait` 永远收敛不了。三个 worker 不在 `frontend` 的依赖里，不涉及。
- **三个 worker 按 Decision 1 明确不补探针**，理由写在 §14.2，不在 compose 里重复。
- **自检与验证（命令原文 + 退出码，本阶段现跑）**：
  | 命令 | 退出码 |
  |---|---|
  | `env -i PATH="$PATH" HOME="$HOME" docker compose -f docker-compose.yml config -q`（与门禁 `CLEAN_ENV` 同口径） | **0** |
  | `python3 -m pytest tests/unit/test_compose_zero_dep.py -q` | **0**（10 passed） |
  | `python3 -m pytest tests/unit -q` | **0**（104 passed） |
  | `python3 tools/gates/check_expected_red.py` | **0**（预期红 7，绿 6，跳过 0） |
  | `python3 -m pytest tests/gates/test_zero_dep_boot.py -q` | **1，`1 passed, 2 errors`——与改动前一致** |
  - 门禁那条的红因逐字复核过，**没有变**：
    `NotImplementedError: compose_stack 尚未实现 —— 见 docs/backlog/implementation-roadmap.md 的 P0 交付表（P0 · 零依赖启动 CI）`，
    且是 **ERROR 不是 FAILED**。`test_compose_config_valid_with_empty_env` 仍绿。
  - 逐条文本自检：`grep 'AGENERP_LLM'` 只命中 `x-ai-env` 三行，**新增探针里一个 AI 变量都没有**；
    `grep '^version:'` → 0 行；`grep ':latest'` → 无命中；
    `grep -oE '\$\{[^}]*\}' | grep -v ':-'` → **无输出**（每个插值都带默认值）；
    新增的三条 `curl` 行里**一个 `$` 都没有**（避开了「单 `$` 被 compose 插成空串、两条断言都抓不到」那个陷阱，
    做法是全部写字面量，不引变量）；`git diff` 里 `image:` 行零改动、`ports:` 条目零改动。

### 2026-08-21 · plan-2026-08-21-1634-2 应用侧 healthcheck · `EXECUTE` Phase 2（Decision：「全部 healthy」到底指哪些服务）

三条 Decision 全部落进 owner doc 新增小节 `docs/architecture/system-baseline.md` **§14.2 本仓栈的健康判定口径**。

- **Decision 1：三个 worker（`queue-short` / `queue-long` / `scheduler`）选候选 (a)——不给探针，把「全部 healthy」收窄。**
  备选与弃选理由（全部有 Phase 1 的实测垫底）：
  (b) `bench` 自检——`bench doctor` 与 `bench --site frontend scheduler status` **都退 0**，
      却同时在输出里说 `Scheduler disabled / inactive for frontend`。永远绿的探针是假判据，弃。
  (c) redis 心跳——信号是真的（`rq:worker:<id>.hostname` 与容器 `$HOSTNAME` 逐字相符），但三条挡住了它：
      ① `scheduler` 不是 rq worker，不在 `rq:workers` 里，这条路对它无效；
      ② 心跳分辨率实测 **405 秒**（`DEFAULT_WORKER_TTL = 420`），死掉的 worker 会绿着将近 8 分钟；
      ③ 它是**跨服务探针**，redis 挂了会让 worker 变红，故障归错服务。弃。
  (d) `pgrep`——plan 起草时就点名警惕的那一个，进程活着不等于在消费队列。弃。
  **残余风险已写进 §14.2 并会同步进 `STATE.md` §3**：门禁将来断言的是 6 个服务不是 11 个。
- **Decision 2：`start_period` 不调大，改走编排层——给 `x-backend-defaults` 补
  `create-site: condition: service_completed_successfully`。**
  两条候选 plan 都列了，**没有默认落到第一条上**。选第二条的理由：
  此前该锚点只等 `configurator`，`backend` 与 `create-site` 并行起，站点还没建出来时探针必然 404，
  只能靠一个巨大的 `start_period` 兜住；而建站耗时随机器速度变（本机实测 **50 秒**，CI runner 上更久）。
  塞进 `start_period` 会同时产生两个坏结果：超时值要跟着机器猜，且**真的坏掉**的 backend 也要等同样久才翻红。
  改成次序约束后 `start_period` 只需覆盖 gunicorn 自身启动。
  **代价照实记**：该锚点为 `backend` 与三个 worker 共用，三个 worker 的启动时机被一并推迟——
  这是有意接受的编排语义变更，不是副作用。
  取值：`backend` 10s/5s/6/60s，`websocket` 与 `frontend` 10s/5s/6/30s，`--wait-timeout` 300s。
  硬关系：`interval × retries + start_period`（最大 ~120 秒）**必须小于** `--wait-timeout`，
  否则 `--wait` 先超时——那不是判据红了，是判据没给出结论。
  残余风险：本机与 CI runner 速度不同，这一格属工作项 8 剩下那一半，本 plan 只登记。
- **Decision 3：零依赖红线的自检项。** 新增探针里**不出现任何 AI 变量、不出现任何 `${…}` 插值**，
  一律字面量。这是既有约束（compose 文件头两处 + `tests/unit/test_compose_zero_dep.py` 的三条断言）的执行期落点，不是新决定。
- **验证（本阶段现跑）**：
  - `node tools/check-doc-references.mjs` → **exit 1**，违规集合与开工基线**逐行相同**（唯一一条仍是
    `module-boundaries.md` 那处存量 line-ref）。§14.2 一条新的都没加——写作时按规则只引小节名，不写 `文件:行号`。
  - `bash tools/check-masterplan-links.sh` → **exit 0**（35 条引用，断链 0）

### 2026-08-21 · plan-2026-08-21-1634-2 应用侧 healthcheck · `EXECUTE` Phase 1（Explore：探针实测）

本机 docker 29.2.1 / compose **v5.0.2**，全程 `AGENERP_HTTP_PORT=18080`（8080 被无关栈 `docker-frontend-1` 占着）。
下列每条都是现跑，命令原文 + 退出码照抄。

- **起栈**：`AGENERP_HTTP_PORT=18080 docker compose up -d` → **exit 0**，11 个服务全建。
  `docker compose ps -a` 抄下：`db` / `redis-cache` / `redis-queue` 三个 `Up (healthy)`，
  `configurator` `Exited (0)`，`create-site` 当时仍 `Up`（在建站），
  `backend` / `websocket` / `queue-short` / `queue-long` / `scheduler` / `frontend` 六个 `Up` 且 **Health 列为空**（无探针）。
- **⚠️ 改动前的 `--wait` 基线是本阶段最重要的一条**：
  `AGENERP_HTTP_PORT=18080 docker compose up -d --wait --wait-timeout 300` → **exit 0，耗时 4 秒**。
  但那 4 秒里 `create-site` **还在建站**（`docker inspect` 实测其生命周期 `09:44:07.870Z → 09:44:57.498Z`，约 **50 秒**），
  而 compose 对 `create-site` 与六个无探针服务**一律打印 `Healthy`**。
  也就是说**改动前的 `--wait` 是一条空判据**：它退 0 的时刻，站点还没建出来、首页还打不开。
  这正是 plan 的全部理由，逐字对上了日志里那句「应用侧服务的 healthy 根本还不可判」。
- **A/B/C 分支裁定**：基线**退 0**，不触发止损分支。顺带查实了那个未验证假设：
  `--wait` 对一次性容器**不判失败**（`configurator` 的 `Exited` 被接受，`create-site` 的 `running` 被当成 `Healthy`），
  所以不存在 A 类；下限三服务全部找到可用探针（见下），不存在 B 类。归类为 **C 类**——
  「基线之所以没有意义，就是因为应用侧没有 healthcheck 可等」，照常进 Phase 2。
- **探针工具盘点**（`docker compose exec -T <svc> sh -lc "command -v <tool>"`，逐个跑、记退出码）：
  `backend` → `curl` `/usr/bin/curl` **exit 0**｜`python3` `/usr/local/bin/python3` **exit 0**｜`bench` `/usr/local/bin/bench` **exit 0**｜
  `wget` **exit 127**｜`nc` **exit 127**｜`redis-cli` **exit 127**｜`node` **exit 127**。
  `websocket` / `frontend` / `queue-short` / `scheduler` 四个同镜像，结果逐项相同（`node` 同样 **127**，
  尽管 `websocket` 的 command 就是 `node`——它不在登录 shell 的 PATH 上）。
  → **镜像里有 curl**，这是事实不是假设；探针一律用 `curl`。
- **各服务判活端点实测**：
  | 服务 | 命令（容器内） | 结果 |
  |---|---|---|
  | `backend` | `curl -fsS --max-time 5 http://127.0.0.1:8000/api/method/ping` | **exit 22**，`HTTP 404`，正文 `127.0.0.1 does not exist` |
  | `backend` | `curl -fsS --max-time 5 -H "Host: frontend" http://127.0.0.1:8000/api/method/ping` | **exit 0**，`{"message":"pong"}` |
  | `websocket` | `curl -sS --max-time 5 http://127.0.0.1:9000/` | **exit 28**，`Operation timed out after 5002 ms with 0 bytes received`（根路径**不回应**，不给 max-time 会永久挂住——第一次跑就把 120 秒的工具超时耗光了） |
  | `websocket` | `curl -fsS --max-time 5 -o /dev/null "http://127.0.0.1:9000/socket.io/?EIO=4&transport=polling"` | **exit 0**，`200`，正文是 engine.io 握手 `{"sid":…,"upgrades":["websocket"],…}` |
  | `frontend` | `curl -fsS --max-time 5 -o /dev/null http://127.0.0.1:8080/api/method/ping` | **exit 0**，`200`，`{"message":"pong"}`（nginx 自己带 `FRAPPE_SITE_NAME_HEADER`，不用手加 Host） |
  - plan 起草时点破的那个 **Host 头坑是真的**：容器内打 `127.0.0.1` 会被 gunicorn 当成一个叫 `127.0.0.1` 的站点，
    404 且**再大的 `start_period` 也救不了**。上表第一、二行就是同一条命令加不加 `-H` 的差别。
    这一行同时是**判据有牙齿的证据**：`-f` 让 404 变成 exit 22，不会假绿。
- **三个 worker（`queue-short` / `queue-long` / `scheduler`）：逐条查实，结论是「没有可用的判活手段」。**
  - `bench doctor`（在 `backend` 里跑）→ **exit 0**，输出 `Scheduler disabled for frontend` / `Scheduler inactive for frontend` / `Workers online: 2`。
    **它在报告「调度器被禁用」的同时退 0**——`bench new-site` 建站时就 `*** Scheduler is disabled ***`（建站日志原文）。
    拿它当 healthcheck 等于永远绿。
  - `bench --site frontend scheduler status` → **exit 0**，输出 `Scheduler is disabled for site frontend`。同上，永远绿。
  - 两者还都是**全栈级**结论，不区分是哪个容器。
  - redis 侧确有真信号：`redis-cli smembers rq:workers` → 两个成员；
    `hgetall rq:worker:<id>` 里带 `hostname` 字段，实测 `c88db1953bd3` = `queue-long`、`fa31c7c243c9` = `queue-short`
    （与容器内 `echo $HOSTNAME` 逐字相符）。**但 `scheduler` 的 `4b078715d83f` 不在名单里**——它根本不是 rq worker。
  - 心跳刷新周期实测：`last_heartbeat` 从 `09:44:56.527266Z` 一直不变到 `09:51:32`，下一采样 `09:51:52` 变成
    `09:51:41.581655Z` → **间隔 405 秒**。与镜像里 `rq/defaults.py` 的 `DEFAULT_WORKER_TTL = 420` 对得上。
  - 另：worker 容器的 `python3` **没有** `redis` 模块（`ModuleNotFoundError`），要用得走
    `/home/frappe/frappe-bench/env/bin/python`（redis-py 4.5.5）。
- **收尾**：`docker compose down -v` → **exit 0**（卷全删）。`docker ps -a` 复查：无关栈
  `docker-frontend-1`（Up 8 hours）、`proof-proof-1`、`openmaic-openmaic-1` 等**状态与开工前逐行一致，未受影响**。

### 2026-08-21 · plan-2026-08-21-1634-1 种子数据 · `EXECUTE` Phase 4（roadmap 写回 · owner-doc 漂移修正 · 收尾）

- **Fix：`docs/context/project-context.md` 的 Optional Layers 漂移已改正。** 七个复选框此前全未勾，
  而七个目录全部存在且非空（2026-08-21 实测：`discussions` 2 / `audits` 4 / `testing` 3 / `skills` 16 /
  `analysis` 3 / `retrospectives` 2 / `lessons` 1）。七格全勾，并补一行说明「本仓七个可选层均在用」。
  按 plan-guide 规则 14，确认的 owner-doc 漂移不得降级为 follow-up。
- **Add：验证命令表新增一行** `python3 -m agenerp.seed --seed 42 --verify`，
  含两条如实注记：① WBS 原文写的是 `python`，本机没有这个可执行名；
  ② **它不在 `missions/p0-foundation.json` 的 `commands.test` 里，`GATE_VERIFY` 复跑不到它**——
  与 Contract tests 那一行同样的处理，理由相同。表下那句「上面五条」随之改为「上面几条」。
- **Decision：roadmap 工作项 7 置 `planned`，不置 `done`。** roadmap 自己给 `done` 的定义是
  「对应门禁测试已转绿并从预期红名单划掉」，而工作项 7 压根没有门禁测试，这个定义**在字面上不可满足**。
  写 `done` 等于 loop 自己放宽判据的定义（放松裁判），留 `todo` 是谎报——两者都不取。
  对照表第 7 行已更新为指向本次交付的验收命令与门禁提案，**但仍逐字写着「仍然没有门禁」**，
  并明说「这一格没有变松」。「当前基线」的缺口条目同步改写，说明 A/B 切分的责任在本 plan 自己。
- **验证（命令原文 + 退出码，本阶段现跑）**：
  - 红线自查 `git diff --name-only -- tests/gates/ .github/workflows/ missions/` → **输出为空**；
    连未跟踪文件一并查的 `git status --porcelain -- tests/gates/ .github/workflows/ missions/ tools/gates/ docs/masterplan/DECISIONS.md`
    → **输出同样为空**。（`git diff` 看不见未跟踪文件，只跑它不足以证明「没往红线目录里新建文件」，故两条都跑。）
  - `bash tools/check-masterplan-links.sh` → **exit 0**（35 条引用，断链 0，未登记 REF 0）
  - `node tools/check-doc-references.mjs` → **exit 1**，违规集合**与开工基线逐行相同**
    （唯一一条仍是 `module-boundaries.md` 那处存量 line-ref）。本 plan 一条新的都没加。
  - roadmap 可解析性复核：8 个工作项全部匹配 `- N. …: \`status\`` 形态 →
    `1:done 2:done 3:done 4:planned 5:planned 6:todo 7:planned 8:todo`
  - `git diff --numstat docs/masterplan/STATE.md` → **`5	0`**（红线 5：只增不删）
- **本 plan 的聚合验证结论（四个阶段合并，命令原文 + 退出码）**：
  | 命令 | 退出码 |
  |---|---|
  | `python3 -m agenerp.seed --seed 42 --verify` | **0** |
  | `python3 -m pytest tests/unit -q` | **0**（104 passed） |
  | `python3 tools/gates/check_expected_red.py` | **0**（预期红 7，绿 6，跳过 0） |
  | `ruff check agenerp tests/unit tests/contracts` | **0** |
  | `bash tools/check-masterplan-links.sh` | **0** |
  | `node tools/check-doc-references.mjs` | **1，违规集合与基线相同**（判据是「集合不变」，不是退 0） |
- ⚠️ **verification scope limited，两层都要说清**：
  ① 上列命令是本仓此刻可跑的**全部**（无 build、无 typecheck，L2 门禁未解锁），
  **不得报为「全量验证通过」**；
  ② **核心验收命令 `python3 -m agenerp.seed --seed 42 --verify` 不在 `commands.test` 里，
  `GATE_VERIFY` 子进程复跑不到它**——这条绿是 loop 自己给自己判的分。
  代偿控制两条：变异验证（Phase 2，五条全部转红且指名道姓）+ 独立关闭审计复跑。
- `tools/gates/expected-red.txt` **一行未动**，本 plan 没有让任何门禁由红转绿。
- **收尾 sha：`3024a3d`**（`feat(seed): 确定性种子数据集（内置 1,010 米积压）+ 自验 CLI`，18 个文件）。
- Next step：等人对 `docs/masterplan/STATE.md` needs-human 队列里新增那行 `[open]` 的
  (a)/(b)/(c) 作出选择；B 半（装载进活站点 + 站点侧断言）在那之前不开工。

### 2026-08-21 · plan-2026-08-21-1634-1 种子数据 · `EXECUTE` Phase 3（单测覆盖 · 判据的第二只脚）

- 新增 `tests/unit/test_seed_deterministic.py`，**31 条**。它是判据里 `GATE_VERIFY` **复跑得到**的那一半——
  `python3 -m agenerp.seed --seed 42 --verify` 不在 `missions/p0-foundation.json` 的 `commands.test` 里，
  而 `python3 -m pytest tests/unit -q` 在。
- 覆盖五组：
  - **确定性** 5 条：同 seed 两次相等 / 经 `snapshot.diff` 判 diff 为空 / **两次落盘字节相同**
    （对象相等不够，序列化层也可能引入不确定性）/ UTF-8 无 BOM + `sort_keys` 复算一致 / 落盘读回往返无差异。
  - **种子敏感性** 2 条：定稿保留了受控 RNG，故断言 `generate(42) != generate(43)`，
    且差异**只在 `Stock Entry` 的装饰性字段**（无增删单据）、`verify(generate(43))` 仍全过；
    另断言 `--help` 文本如实说明 `--seed` 只驱动装饰性字段。
  - **场景断言** 8 条：2,000 / 990 / 1,010 / ¥6,450 / `LOSS-00003` 10 米已审批 / 两笔逾期
    （并断言逾期是由 `as_of` 与 `due_date` 判出的，不是硬贴一个状态）/ 账面四项全绿。
    其中一条专门钉住 **FIFO 分层**：`10 × 5.00 + 1000 × 6.40 == 6450`，
    且移动加权均价口径 `(5000 + 6400) / 2000 × 1010 == 5757 ≠ 6450`——
    这条把 §12.1 的对照写成了可执行断言，日后有人把生成器改成均价口径会立刻发红。
  - **负例** 9 条：逐个篡改 `Bin` / `Loss Review` / `Sales Invoice` / `Purchase Invoice` / `GL Entry`，
    断言 `verify()` 返回非空**且原因文案能定位到具体哪条**；另两条覆盖 CLI 的退 1 路径与退 0 路径。
  - **纯净性** 4 条：扫 `agenerp/seed*.py` 与 `agenerp/seed/*.py` 的**并集**（拆包后写死路径会静默扫空 → 假绿），
    先断言**至少扫到 1 个文件**，再断言不出现 `datetime.now` / `time.time` / `os.environ` / `random.random(`，
    以及不出现 `.shuffle(` / `.sample(`。
- **验证（命令原文 + 退出码，本阶段现跑）**：
  - `python3 -m pytest tests/unit -q` → **exit 0**（**104 passed in 0.34s**，全量 unit；Phase 3 前是 73）
  - `python3 tools/gates/check_expected_red.py` → **exit 0**（门禁 13 项：预期红 7，绿 6，跳过 0）
  - `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**（合并跑一遍）
  - `ruff check agenerp tests/unit tests/contracts` → **exit 0**（All checks passed!）
  - `python3 -m pytest tests/contracts -q` → **exit 0**（151 passed）—— 附带复核，非本 plan 判据
  - `python3 -m agenerp.seed --seed 42 --verify` → **exit 0**
- `tools/gates/expected-red.txt` **一行未动**：本 plan 没有让任何门禁由红转绿，名单不需要变短。

### 2026-08-21 · plan-2026-08-21-1634-1 种子数据 · `EXECUTE` Phase 2（生成器与 CLI）

- 新增 `agenerp/seed/` **包**（不是单文件）。plan 允许「逼近 500 行就拆包」，
  初稿单文件写到 596 行即触线，按 §12 的分层拆成七个模块：
  `model.py`(112 常量与值对象) / `names.py`(18 单据号) / `masters.py`(144 主数据与订单) /
  `documents.py`(300 业务单据) / `ledger.py`(164 流水·结存·总账) / `dataset.py`(53 装配) /
  `store.py`(70 落盘与 `Snapshot` 桥) / `checks.py`(≈190 场景断言) / `__main__.py`(70 CLI)。
- **`checks.py` 的期望值刻意不从 `model.py` 取数**——这一处是本阶段最实质的修正：
  初稿里 `verify()` 从生成器的同一批常量推期望，变异一个构造常量时数据与期望一起动，
  **判据不会发红**（同义反复）。首轮变异实测只报出 1 条，暴露了这个缺陷。
  改为在 `checks.py` 里写一组独立的 `EXPECTED_*` 字面量（已知业务事实，出处 §12.1/§12.2）后，
  同一个变异报出 7 条。达成率的分子改为取自**数据**（发货数 + 该单据自己记的已审批损耗），不取自常量。
- ⚠️ **首轮变异验证的第 2、3 条结果作废，已重跑**：变异是等长替换（`990`→`980`），
  源文件大小不变且 mtime 在同一秒内 → CPython 复用了 `__pycache__` 里的旧字节码，
  第 2、3 次跑的其实还是第 1 次的变异体（三条输出逐字相同即为证据）。
  重跑时先 `find . -name __pycache__ -delete` 并全程 `python3 -B`。**这条坑值得记住**。
- **验证（命令原文 + 退出码，本阶段现跑）**：
  - `python3 -m agenerp.seed --seed 42 --verify` → **exit 0**（`✅ 种子 42：两次生成 diff 为空，场景断言全过`）
  - **变异验证（判据有没有牙齿）**，五条，全部 `python3 -B -m agenerp.seed --seed 42 --verify`：
    | 变异 | 退出码 | 报出条数 | 指名道姓（首条） |
    |---|---|---|---|
    | `DELIVERY_QTY 990→980` | 1 | 7 | 发货应为 990.0 米，实为 980.0 |
    | `APPROVED_LOSS_QTY 10→12` | 1 | 2 | LOSS-00003 的已审批损耗应为 10.0 米，实为 12 |
    | `SUBCONTRACT_FEE 2200→2500` | 1 | 2 | 成品仓结余价值应为 ¥6450.00 …，实为 ¥6750.00 |
    | `SALES_RATE 18.8→19.0` | 1 | 2 | 应收逾期合计应为 ¥18612.00，实为 ¥18810.00 |
    | `LOSS_REVIEW_STATUS Approved→Pending Approval` | 1 | 1 | LOSS-00003 的状态应为 Approved，实为 'Pending Approval' |
  - 还原后字节一致：`find agenerp/seed -name '*.py' \| sort \| xargs shasum -a 256 \| shasum -a 256`
    变异前后同为 `6b48864bde53ca33d20097e278eef5c0e7966f877ba53ceee9f6806bc40dc4dc`。
    （`agenerp/seed/**` 是本轮新增、尚未入库，`git diff` 对未跟踪文件为空，**不能拿它当还原证据**，改用摘要比对。）
  - `ruff check agenerp` → **exit 0**（All checks passed!）
  - `wc -l agenerp/seed/*.py` → 最大 `documents.py` **300** 行，全部 < 500：
    `17 __init__.py / 70 __main__.py / ~190 checks.py / 53 dataset.py / 300 documents.py / 164 ledger.py / 144 masters.py / 112 model.py / 18 names.py / 70 store.py`
  - **3.11 下限自检**（`pyproject.toml` 声明 `requires-python >= 3.11`，而 ruff 的 `target-version` 是 `py312`，守不住这条线）：
    `ast.parse(..., feature_version=(3, 11))` 逐文件解析 → 全部通过，未用 3.12 才有的语法。
  - 不带 `--verify` 跑一次：落盘到临时目录并打印路径，15 个 DocType 文件 + `_meta.json`，退 0。**仓库里不落生成物。**


### 2026-08-21 · plan-2026-08-21-1634-1 种子数据 · `EXECUTE` Phase 1（数值对账与数据集形状定稿）

- **开工基线 sha：`d45698a`**（plan `## Current Baseline` 起草时写的是 `07d684c`，两者之间循环另有提交；
  本阶段逐条复核 Baseline 的可验证断言，见下）。
- **Explore 结果（本阶段最大的产出，且它推翻了 plan 起草时的倾向）**：
  在冻结的证据仓（`evidence-repo.env` 的 `XM_PATH` @ `XM_SHA`，**全程只读，未写入一个字节**，红线 6）里
  **查到了更早的、含单价的原始记录**——`xm_pattern_demo/demo/business_flow.py` 的 `DEMO` 常量表
  与 `xm_pattern_demo/demo/bootstrap.py` 的工位 `operating_cost`。
  据此完成对账：自制批 1,000 米 @ ¥5.00（原料 120 Kg × ¥35 + 工序 600 分钟 ÷ 60 × ¥80）、
  外协批 1,000 米 @ ¥6.40（原料 ¥4,200 + 服务费 ¥2,200），**FIFO** 发货 990 米全部出自制批 →
  结余 = 10 × 5.00 + 1,000 × 6.40 = **¥6,450**、数量 **1,010 米**，两个数同时对上。
  对照：移动加权均价口径应得 ¥5,757 ≠ ¥6,450 —— **plan 起草时「6,450 是移动加权均价滚出来的」那句是错的**，
  实际机制是两档批次单价按 FIFO 分层。顺带对上应收 990 × ¥18.8 = ¥18,612、应付 ¥2,200，逐分相等。
  → **`## Deferred But Adjudicated` 里那条「¥6,450 与 1,010 米单价对不上的数值漂移」不成立，已对账关闭。**
- **五条 Decision 全部定稿**，连同备选与残余风险写进 `docs/architecture/module-boundaries.md` **新增 §12**
  （§12.1 单价对账 / §12.2 硬断言与派生量 / §12.3 落盘形状 / §12.4 确定性来源 / §12.5 两笔逾期 /
  §12.6 生成物不进仓 / §12.7 判据归属 / §12.8 已知漂移）。
  其中 §12.2 因 Explore 的结论**改写了 plan 的倾向**：金额 ¥6,450 由「只如实记录」升为**断言项**，
  因为它现在是可从原始单价推出来的，不再是二手数字。
  §12.3 取「目录形状对齐 + 判定器复用」，**不复用 `read_scope_dir` 的定制包解析口径**
  （业务单据的身份是单据号，塞进 `custom_fields`/`fieldname` 是为复用而说假话）。
- **判据先行的两个动作都做在实现之前**（不是收尾补票）：
  - 新建 `docs/backlog/gate-proposal-seed-dataset.md`（**提案文本，不是测试代码**；
    本 plan 不在 `tests/gates/` 下创建任何文件）。
  - `docs/masterplan/STATE.md` §3 追加一行 `[open]`，登记「工作项 7 没有绑定门禁」这个判据缺口，
    含三个只有人能选的处置项，并如实写出「验收命令不在 `commands.test` 里、`GATE_VERIFY` 复跑不到」这一点。
- **验证（命令原文 + 退出码，本阶段现跑）**：
  - `node tools/check-doc-references.mjs` → **exit 1**，违规集合逐行对照与基线**相同**
    （唯一一条仍是 `docs/architecture/module-boundaries.md` 的 line-ref 指向 `docs/analysis/2026-08-19-pre-build-validation.md`，
    该存量违规不属本 plan）。中途一度多出一条 `unresolved reference`（§12.7 引了尚未创建的门禁提案文件），
    创建该文件后消失。**本阶段未引入任何新的 line-ref 违规。**
  - `git diff --numstat docs/masterplan/STATE.md` → **`5	0`**，第二列为 0，只增不删（红线 5）。
  - `python3 tools/gates/check_expected_red.py` → **exit 0**（门禁 13 项：预期红 7，绿 6，跳过 0）。
  - `bash tools/check-masterplan-links.sh` → **exit 0**。
- 本阶段**未改任何代码**：`agenerp/**`、`tests/**`、`tools/**`、`missions/**`、`.github/**` 零改动。

### 2026-08-21 · mission 验证步 · plan-2026-08-21-1553-1 关闭审计落盘（`full-green verification`，无代码改动）

- 本步只做两件事：跑 mission 配置里的验证命令，并把上一步写好的**独立关闭审计**结论提交落盘。
  `agenerp/**`、`tests/**`、`tools/**` **一个字节未改**，工作区改动只有
  `docs/plans/p0-foundation/2026-08-21-1553-1-diff-apply-engine-pure-half.md` 一个文件。
- **验证（命令原文 + 退出码，本会话现跑）**：
  - `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
    （「门禁 13 项：预期红 7，绿 6，跳过 0 · ✅ 与预期红名单完全一致」/ **73 passed in 0.22s**）
  - `ruff check agenerp tests/unit tests/contracts` → **exit 0**（All checks passed!）—— 附带复核，非 mission 配置项
  - mission 配置里 ruff / mypy / docker 三格仍为空，本步按「空的跳过」处理，
    **verification scope limited**：上列命令即本仓当前验证面的全部，不等同于「全量绿」。
- 与 `EXECUTE` 会话（`0603ccf` 报 50 passed）相比 `tests/unit` 由 50 → **73 passed**，
  增量来自其后 `dac7a9f`（委派链与零依赖判据）与 `8718f40`，无一项由红转绿。
- 红线自查：`git diff --name-only d6672cc..HEAD -- tests/gates/ .github/workflows/ missions/ tools/gates/ docs/masterplan/DECISIONS.md`
  → **输出为空**。本步新增改动亦只落在 `docs/plans/**` 与本日志。
- `tools/gates/expected-red.txt` 一行未动：判定器报「预期红 7」与名单 7 行逐字对上，
  本轮**没有任何门禁由红转绿**，名单不需要变短（名单只能变短，本轮无可划）。
- plan 状态：`Plan Status: completed`，Closure Gates 九框全 `[x]`，Closure 节含审计人、基线 sha `8718f40`、
  逐条命令退出码与反假判据复核。审计如实记下一处 plan 措辞漂移（`execute_plan` 被写成「B 半唯一落点」，
  实为 `SiteSnapshotSource.read` + `execute_plan` 两个落点），按「矛盾原文如实保留、不擅自消解」处理，
  真相源以 `docs/architecture/module-boundaries.md` §11.6 为准，不构成阻塞。
- Next step: B 半（对站点执行）等人对 `docs/masterplan/STATE.md` §3 那行 `[open]` 作出 (a)/(b)/(c)/(d) 选择后才开；
  其 successor plan 须同时接 `SiteSnapshotSource.read`（工作项 4）与 `execute_plan`（工作项 6）两个落点。

### 2026-08-21 · plan-2026-08-21-1553-1 差集 apply 引擎 · A 半 · `EXECUTE` Phase 1

- **只交付 A 半（读包 + 求差 + 删除计划），不接活站点**。B 半（`execute_plan` 对站点执行删除）
  被红线 1 挡着——它要的 `live_site` / `pack_repo` 两个 fixture 全在 `tests/gates/conftest.py` 内。
  **roadmap 工作项 5 停在 `planned`，不置 `done`**；`tools/gates/expected-red.txt` 一行不动，本 plan 不让任何门禁转绿。
- **开工基线 sha：`d6672cc`**（Phase 3 的三条区间红线自查以它为 `<base>`）。
  ⚠️ 与 plan `## Current Baseline` 起草时写的 `494440d` 不同——起草与执行之间循环另有提交；
  草案评审记录已核实 `agenerp/**` / `tests/gates/**` / `expected-red.txt` 在两个 sha 之间无差异。

**前置检查（Phase 1 首项，命令原文 + 退出码，退出码单独取 `$?`）**

- `git rev-parse HEAD` → `d6672cc04a2eee4ab766771ac3bb0f409661ea68`
- `python3 tools/gates/check_expected_red.py` → **exit 0**（门禁 13 项：预期红 7，绿 6，跳过 0）。
  **本会话没有复现 `PATH` 缺 `/usr/local/bin` 的现象**（见本文件更早那条记录），一次即退 0，无需加 `PATH=` 复跑。
- `python3 -m pytest tests/unit -q` → **exit 0**（50 passed）
- `ruff check agenerp tests/unit tests/contracts` → **exit 0**（All checks passed!）
- `ls agenerp/apply.py` → **exit 1**（No such file or directory）。文件确不存在，不触发「别的会话在做同一件事」的停手分支。

**Phase 1 交付**

- 新增 `agenerp/apply.py`：`ApplyPlan`（不可变，`creates` / `updates` / `deletes` 三序列为判定面，
  `summary()` 只给人读）、`read_pack`、`plan_apply`、`execute_plan`（`raise NotImplementedError`，
  消息里指名工作项 6 与 `STATE.md` §3 的 `[open]` 行）。零第三方依赖，只用标准库 + 仓内模块。
- `Decision` **定制包目录布局取候选 (b)：`<root>/<scope>/<DocType>.json`**，与仓内既有
  `OfflineSnapshotSource` 同一套。理由与残余风险写进 `docs/architecture/module-boundaries.md` §11.6。
- `Fix` `agenerp/snapshot.py` 的载荷解析暴露成可复用公开入口：私有 `_entries_from_payload` →
  公开 `entries_from_payload`，并把「按 `<root>/<scope>/*.json` 走目录」抽成公开 `read_scope_dir`，
  `OfflineSnapshotSource.read` 改为委派给它。**行为一字未改**，`read_pack` 与离线来源自此同源，不存在第二口径。
  判据：改动前后 `python3 -m pytest tests/unit -q` → **exit 0**，既有 **50 条一条不少、一条不红**。
- `python3 -c "import agenerp.apply"` → **exit 0**；`ruff check agenerp tests/unit tests/contracts` → **exit 0**。


**Phase 2 判据（`tests/unit/test_apply_plan.py`，21 条测试 / 41 处 assert）**

- `python3 -m pytest tests/unit/test_apply_plan.py -q` → **exit 0**（21 passed）
- `python3 -m pytest tests/unit -q` → **exit 0**（**71 passed**，原 50 条一条不少、一条不红）
- `ruff check agenerp tests/unit tests/contracts` → **exit 0**
- `python3 tools/gates/check_expected_red.py` → **exit 0**（门禁 13 项：预期红 7，绿 6，跳过 0 —— **与开工时逐字一致**）
- 承重条款是 `test_field_removed_from_pack_lands_in_deletes`：失败信息逐字带出
  §11.1 的 Spike 06 实测结论（纯 upsert、revert 无效），让将来读到红的人一眼知道它守的是什么。
- 零依赖判据**在全新子进程里测**（`subprocess.run([sys.executable, "-c", ...])`），
  不在当前 pytest 进程里对 `sys.modules` 求差 —— 后者恒为空，是个永远绿的假判据。
  落法是「基线子进程（`pass`）+ 目标子进程（`import agenerp.apply`）」求差，再扣掉
  `sys.stdlib_module_names` 与 `agenerp`；两个子进程的 `returncode` 都进断言。
  导入方向另有两个子进程覆盖两种次序（`pack`→`apply`、`apply`→`pack`），`returncode` 同样进断言。

**变异实测：判据有没有牙齿（改真文件跑、跑完 `git checkout` 还原）**

- 变异 A `deletes=d.removed` → `deletes=()`（删除分支恒空）：`python3 -m pytest tests/unit -q` → **exit 1**，
  **6 failed**（`test_field_removed_from_pack_lands_in_deletes` / `test_delete_survives_alongside_creates_and_updates` /
  `test_direction_is_not_symmetric` / `test_positional_call_keeps_pack_first` /
  `test_summary_is_not_the_decision_surface` / `test_read_pack_and_plan_apply_compose_into_a_delete`）
- 变异 B `diff(before=current, after=desired)` → `diff(before=desired, after=current)`（参数序写反，
  plan `## 结构边界` 逐字点名的那个陷阱）：**exit 1，9 failed**
- 还原：`git checkout agenerp/apply.py` → **exit 0**；`git diff --quiet` → **exit 0**（工作区干净）；
  `python3 -m pytest tests/unit -q` → **exit 0**（71 passed）
- ⚠️ 第一轮变异时 `agenerp/apply.py` 还没进 git（`git checkout` 报 `did not match any file(s) known to git`），
  用文本回改还原，**这不构成「还原干净」的证据**。因此先把 Phase 1–2 提交为 `0603ccf`，再把变异 A 原样重跑一遍，
  这次 `git checkout` + `git diff --quiet` 双双退 0。如实记下这段返工，不当没发生过。

**Phase 3 交付与验证（commit `dac7a9f`；Phase 1–2 在 `0603ccf`）**

- `agenerp/pack.py` 的 `apply_pack` 改为委派 `read_pack` → `plan_apply` → `execute_plan`，
  **签名与导入路径一字未改**。`python3 -c "from agenerp.pack import apply_pack"` → **exit 0**。
  `agenerp.apply` 在**函数体内**导入（`apply` 顶层导入 `snapshot`，`snapshot` 顶层导入 `pack.normalize`，
  提到顶层就是循环导入），两种导入次序各有一个子进程判据。
- ⚠️ **红因与 plan 的表述有出入，如实记下不擅自消解**：plan `## 结构边界` 写「红因随之从『求差不存在』
  变成『执行未实现』」，并称 `execute_plan` 是「B 半的唯一落点」。委派后实测的红因逐字是
  `SiteSnapshotSource.read 尚未实现 …（工作项 4 · 工具契约层 v0）`——先卡在「站点现状是什么」，
  `execute_plan`（工作项 6）在它之后。**站点侧是两个落点不是一个。** 没有为了对齐 plan 的措辞去
  伪造一个空的 `current` 快照（那会让删除集恒等于全量包，是假的）。
  两处都记进 `module-boundaries.md` §11.6，B 半的 successor 两处都要接。
  plan Phase 3 第 1 项的**判据原文**（导入退 0 + 门禁红因仍在 fixture 层）两条都满足。
- `docs/architecture/module-boundaries.md` 新增 **§11.6**：落点表、包布局三候选裁定（取
  `<root>/<scope>/<DocType>.json`）与残余风险、方向约定、`execute_plan` 接缝归属、
  以及「本 plan 未让任何门禁转绿」这一事实。`git diff --numstat` → **50 增 0 删**，只追加。
- `docs/backlog/p0-foundation-roadmap.md:24` 工作项 5：`todo` → **`planned`**（1 增 1 删）。
  **不置 `done`**：其绑定门禁被红线 1 挡着，置 ✅ 即裁判规则 1 明禁的自报通过。
  `tools/mission-driver/prompts/execute.md` 第 4.a（`Plan Status` → `completed`）与 4.b（工作项 → ✅）
  **均未执行**，矛盾原文已记在 plan 的「`Plan Status` 由谁写」一节，等人裁定要不要改上游模板。
  4.c 无 `> Source Audits:` 行，整步略过。

**判据可达性复核（命令原文 + 退出码）**

- `python3 -m pytest tests/gates/test_customization_roundtrip_delete.py -q --tb=line` → **exit 1**，
  **`4 errors`**（`ERROR at setup`，逐字 `NotImplementedError: live_site 尚未实现 …`）——
  与 plan 起草评审时实测的基线形态**逐字一致，无漂移**。四条仍红、红在 fixture 层，
  本次改动没有把它改成别的红，也没有偷偷让它变绿。
- `python3 tools/gates/check_expected_red.py` → **exit 0**（门禁 13 项：预期红 7，绿 6，跳过 0）
- `python3 -m pytest tests/unit -q` → **exit 0**（**73 passed**；开工基线 50 条一条不少、一条不红）
- `python3 -m pytest tests/contracts -q` → **exit 0**（151 passed）
- `ruff check agenerp tests/unit tests/contracts` → **exit 0**
- **verification scope limited**：本仓无全量套件（无 build、无 typecheck、L2 门禁未解锁）。
  上面几条就是本仓验证面的全部，不等同于「全量绿」。

**红线自查（区间 diff，`<base>` = 开工基线 `d6672cc`）**

- `git diff --name-only d6672cc..HEAD -- tests/gates/ .github/workflows/ missions/ tools/gates/ docs/masterplan/DECISIONS.md`
  → **输出为空**（`tests/gates/**` 与 `tools/gates/expected-red.txt` 零改动）
- `git diff --numstat d6672cc..HEAD -- docs/masterplan/` → **输出为空**（`STATE.md` 也没碰）
- `git diff --numstat d6672cc..HEAD -- docs/architecture/module-boundaries.md` → `50	0`（删除列为 0）
- 区间全量改动：`agenerp/apply.py` 113/0 · `agenerp/pack.py` 16/2 · `agenerp/snapshot.py` 18/8 ·
  `module-boundaries.md` 50/0 · `roadmap` 1/1 · 本日志 · plan 文件 · `tests/unit/test_apply_plan.py` 389/0

**停手分支：未触发。** A 半全程没有需要碰 `tests/gates/**` 才能自证的地方——判据全落在
`tests/unit/`，而那正是 `missions/p0-foundation.json` `commands.test` 复跑得到的位置。
因此**没有**往 `docs/masterplan/STATE.md` §3 追加任何行，`Plan Status` 也不置 `deferred`。

**Plan Status 保持 `active`，九个 Closure Gates 保持未勾** —— 按 plan「`Plan Status` 由谁写」一节：
`EXECUTE` 只打勾执行项与 Exit Criteria，置 `completed` 与勾九框归独立的 `CLOSURE_AUDIT` 会话。
在 `EXECUTE` 里勾九框等于自证关闭（其中「closure audit was independent」在这一步为假）。

**Next step**：B 半（`execute_plan` + `SiteSnapshotSource.read`）等人对 `STATE.md` §3 那行 `[open]`
的 (a)/(b)/(c)/(d) 作出选择；工作项 5 的 successor plan 要自带 `planned → done` 的 roadmap 写入落点。

### 2026-08-21 · plan-2026-08-21-1022-2 工具契约层 v0 · `EXECUTE` Phase 1–3

- **只交付工作项 4 的 A 半（声明面）**。B 半（接活站点、`live_site` / `SiteSnapshotSource.read`）没做，
  被红线 1 挡着 —— 三个 fixture 全在 `tests/gates/**` 内。**roadmap 工作项 4 停在 `planned`，不是 `done`**，
  本阶段一行 roadmap 都没改（复核时它已经是 `planned`，Phase 1 第 5 点的幂等兜底是空操作，不是漏做）。
- 交付：`agenerp/tools_readonly.py`（十个只读工具的契约声明）、`tests/contracts/` 四个文件（151 条）、
  `docs/architecture/module-boundaries.md` 新增 §7.6（**只追加，58 增 0 删**）、
  `docs/context/project-context.md` 的 Verification Commands 表新增 Contract tests 一行、
  `docs/masterplan/STATE.md` §3 **追加一行** `[open]`。实现与文档在同一个提交 `c3bd794`。
  `agenerp/contracts.py`（Phase 1 的声明格式 + 校验器 + 求值面，320 行）在开工前已由 `091a150` 落盘。
- 开工 sha：**`091a150`**（Phase 3 的三条区间 diff 判据以它为基线）。

**前置确认（Phase 1 首项）**

- 三条基线今日实测：`python3 tools/gates/check_expected_red.py` → **exit 0**（门禁 13 项：预期红 7，绿 6，跳过 0）；
  `python3 -m pytest tests/unit -q` → **exit 0**（50 passed）；`ruff check agenerp tests/unit` → **exit 0**。
  与 plan `## Current Baseline` 里写的「预期红 8」不同 —— `…-1` 落地后名单短了一行，plan 早写明「不写死数字」。
- ⚠️ **门禁那条命令对 `PATH` 敏感，这一点值得记下来**：本会话 shell 的 `PATH` 不含 `/usr/local/bin`，
  `docker` 因此 `FileNotFoundError`，`check_expected_red.py` 先退 **1**（`test_compose_config_valid_with_empty_env`
  报「名单外的门禁红了」）。按裁判规则 3 原样复跑复现；查实 `/usr/local/bin/docker` 存在（`Docker version 29.2.1`），
  是 `PATH` 差异不是代码回归。`PATH="/usr/local/bin:$PATH" python3 tools/gates/check_expected_red.py` → **exit 0**。
  **没有改任何 `tests/gates/**` 文件去迁就它**；本条只是记录，`GATE_VERIFY` 子进程的 `PATH` 归人管。
- `tests/contracts/` 开工时确不存在；`agenerp/contracts.py` **已存在**。plan Phase 1 第 3 点的停手条件是
  「说明有别的会话在做同一件事」—— 实测不成立：`091a150` 的提交信息逐字写着「由循环产出、因迁库停机而中断在提交之前，
  人工代为落盘（内容未改一字）」，即本 plan 自己的在途产物。**判为续做，不是停手**，理由记在这里供审计复核。
- roadmap 工作项 4 复核时**已经是 `planned`**，无需改动。
- `…-1-zero-dep-boot-compose.md` 的 `Plan Status: completed`。A 半本就不需要它（全程无活站点）。

**验证（命令原文 + 退出码，退出码单独取 `$?`）**

- `python3 -m pytest tests/contracts -q` → **exit 0**（151 passed）—— `02-WBS.md` P0.2 的验收命令原文
- `python3 -m pytest tests/unit -q` → **exit 0**（50 passed），且 `tests/unit/test_contract_surface.py` **未被修改**
  （plan Phase 2 的预先裁定复跑成立：该文件是显式清单 + 参数化，无穷尽性断言，新增模块不会让它变红）
- `ruff check agenerp tests/unit tests/contracts` → **exit 0**（All checks passed!）
- `! python3 -m pytest tests -q --tb=no 2>&1 | grep -q 'import file mismatch'` → **exit 0**
  （判的是这个 grep，不是 pytest 的退出码 —— `pytest tests` 本来就退非 0，还有 7 条预期红门禁在）
- `PATH="/usr/local/bin:$PATH" python3 tools/gates/check_expected_red.py` → **exit 0**，计数与开工时实测一致
- `python3 -c "import agenerp.contracts"` → **exit 0**；零依赖判据按「导入前后的顶层模块集合求差」写，
  差集中不属于 `sys.stdlib_module_names` 且不是 `agenerp` 的为 **空** → **exit 0**
- 区间红线自查（`091a150..HEAD`）：
  `git diff --name-only 091a150..HEAD -- tests/gates/ .github/workflows/ missions/ tools/gates/` → **输出为空**；
  `git diff --numstat 091a150..HEAD -- docs/architecture/module-boundaries.md` → `58	0`（**只增不删**）。

**判据有没有牙齿：12 种变异逐条验证（改真文件跑、跑完还原，`diff -q` 确认字节一致）**

声明面 8 种：`doc.links` 丢掉 `from_is_submittable` / `lineage.trace` 只扫主表 / `doc.get` 的 §7.5 声明位翻成 `false` /
`permission.scope` 改成从 DocPerm 反推 / `permission.scope` 的 app 过滤裁剪规则换成「原样返回」/ 清单少一个工具 /
`doc.links` 的筛选改成「只要已提交」/ 证据门禁从 `query.read` 上摘掉 —— **8 种各自被抓**。
校验器与求值面 4 种：不再拒绝缺 `must_keep` / 不再拒绝只读工具要写权限 / 不再校验 `risk` 取值 /
把「上下文缺该事实」静默当通过 —— **4 种各自被抓**（最后一种红 13 条，因为它正是「没查」与「查了没有」被混成一件事）。
还原后 `python3 -m pytest tests/contracts -q` → **exit 0**（151 passed），两个源文件 `diff -q` 均字节一致。

**判据缺口，如实摆出来（一处自判，四处披露）**

`python3 -m pytest tests/contracts -q` **不在** `missions/p0-foundation.json` 的 `commands.test` 里
（那条是 `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q`），
**`GATE_VERIFY` 复跑不到本 plan 的主判据**。`missions/**` 是角色 B 禁区，loop 无权自己补。
裁定为「就地裁定、不占 `STATE.md` §3 的 `[open]` 行」，三条依据：判据本身是人写的（WBS P0.2 验收列）·
本 plan 不关闭工作项 4 · 代偿控制是独立关闭审计。人要翻案，把那条命令加进 `commands.test` 即可（一行的事）。
披露落点：本条 · `module-boundaries.md` §7.6 末段 · `project-context.md` 命令表该行 · plan 的 `## 判据缺口登记`。

**与 `tools/mission-driver/prompts/execute.md` 的两处冲突，逐字记下并给出优先级裁定**

- `execute.md:10`「a. Update the plan's `Plan Status` to `completed`」—— 它要执行会话在关闭审计**之前**就自称完成，
  与 `AGENTS.md` 裁判规则 1/2（「测试过没过由 `GATE_VERIFY` 裁定，AI 无权自报通过」）冲突。**裁判规则胜出**：
  `Plan Status` 保持 `active`，`## Closure Gates` 九框保持未勾，留给 `CLOSURE_AUDIT`。
  （`flows/plan-execution.json` 的步序是 `EXECUTE → CLOSURE_SCRIPT_CHECK →（fail）CLOSURE_AUDIT`，
  所以在 `EXECUTE` 内部「审计已通过」永远为假。`closureScriptCheck` 因未勾的框判 fail 是**预期**，
  它正是把流程送进 `CLOSURE_AUDIT` 的那一步。）
- `execute.md:11`「b. … change the work item from ❌ to ✅」—— roadmap `:9` 说该文件「由引擎在 **closure 审计通过后**回写」、
  `:35` 把 `done` 定义为「通过 closure 审计」，且工作项 4 绑定的判据是 B 半（本 plan 没做）。
  按次序（红线 > masterplan 执行协议 > `AGENTS.md` 其余 > **上游模板默认**），`execute.md` 是上游模板默认，
  **roadmap 与裁判规则胜出**：工作项 4 留 `planned`。

**本轮撞上的一件事：HEAD 在会话中途被人移动**

开工时 HEAD 是 `091a150`；执行到 Phase 2 末尾时人提交了 `d35ec2b`（迁库到 `~/Claude/Projects` 解开 TCC），
它改写了 `docs/masterplan/02-WBS.md` 与 `STATE.md` §1 的已有行，并顺手把 loop 当时已勾的 Phase 1 复选框一并收进去。
**那几行是人改的，人有权改**；记在这里是因为区间判据 `091a150..HEAD` 会把它算进来：
`git diff --numstat 091a150..HEAD -- docs/masterplan/` → `02-WBS.md 4	4`、`STATE.md 14	3`（**含人的改写**），
而只算 loop 自己那段 `git diff --numstat d35ec2b..HEAD -- docs/masterplan/` → `STATE.md 5	0`（**只增不删**），
`02-WBS.md` **零改动**。loop 侧对 `STATE.md` 的写入是用「从 HEAD 取干净版本追加后再 `git add`」做的，
索引里只有那 5 行新增，人在工作树里的改动一个字节没碰。

**B 半的交接**

`docs/masterplan/STATE.md` §3 新增**一行** `[open]`，登记 B 半的红线障碍与那条 `pytest_fixture_setup` hook 级绕道，
带四个处置项 (a)/(b)/(c)/(d) 供人选 —— **loop 不替人选**，也写明**本行不阻塞本 plan 关闭**（它挡的是 B 半与工作项 5/6/8）。
hook 绕道已写进 plan 的 `## Non-Goals` 禁止本 plan 使用，但**仓里没有任何规则禁止下一个会话这么做**，
这正是处置项 (d) 要人拍板的原因。
另给 B 半 successor 一条已查实的约束：它要**自带 `planned → done` 的写入落点** ——
`closure-audit.md` 里 `roadmap` 出现 **0** 次，没有任何引擎产物被指示写这一步；
后果此刻就在仓里看得见：roadmap 工作项 1 至今仍是 `todo`，而规范化器早已实现并转绿。

### 2026-08-21 · plan-2026-08-21-1022-1 零依赖启动（compose 语法，L1）· `EXECUTE` Phase 1–3

- 交付：仓根新增 `docker-compose.yml`（frappe/erpnext 全栈 11 服务）、`tests/unit/test_compose_zero_dep.py`
  （把 `docs/architecture/system-baseline.md` §14 的规则 ①② 与「⚠️ 附带风险」段的回环绑定固化成 10 条判据）、
  `tools/gates/expected-red.txt` 划掉已转绿的一行、`.env.example` 追加 compose 变量说明（只追加）。
  实现与划名单在**同一个提交** `ba7bdae`。
- 开工 sha：**`f4fe0ce`**（Phase 2/3 的四条区间 diff 判据都以它为基线）。

**前置确认（Phase 1 首项，起草时事实的幂等复核）**

- `gh api -H "Accept: application/vnd.github.raw" repos/actions/runner-images/contents/images/ubuntu/Ubuntu2404-Readme.md | grep -i 'Docker Compose'`
  → **exit 0**，命中原文 `- Docker Compose 2.38.2`。
- `docker compose version` → **exit 0**，`Docker Compose version v5.0.2`。
  ⚠️ **记这两个版本不是缓解措施**——runner 的 2.38.2 与本机 v5.0.2 差一个大版本，这一处在本机没有缓解办法
  （plan 起草时试过两版都被独立评审实测否掉，证据留在 plan 的 `## Current Baseline`）。记着只是为了真出事时不必重新考古。
- roadmap 工作项 3 复核时**已经是 `planned`**，无需改动。
- roadmap 的 `EXPECTED_RED` 指向漂移（plan 里记的 `:13`/`:35`/`:78`）复核时**已被 `3f6ccbb` 修掉**：
  `grep -c 'EXPECTED_RED' docs/backlog/p0-foundation-roadmap.md` → **0**，现有两处都指向 `tools/gates/expected-red.txt`。
  Phase 3 那条 `Fix` 因此是幂等空操作，**不是漏做**。

**验证（命令原文 + 退出码，退出码单独取 `$?`）**

- `env -i PATH="$PATH" HOME="$HOME" docker compose -f docker-compose.yml config -q` → **exit 0**
- `python3 -m pytest tests/gates/test_zero_dep_boot.py::test_compose_config_valid_with_empty_env -q` → **exit 0**（1 passed）
- `! grep -q ':?' docker-compose.yml` → **exit 0**
- `python3 tools/gates/check_expected_red.py` → **exit 0**（门禁 13 项：预期红 7，绿 6，跳过 0 · ✅ 与预期红名单完全一致）
- `python3 -m pytest tests/unit -q` → **exit 0**（50 passed）
- `ruff check agenerp tests/unit` → **exit 0**（All checks passed!）
- `python3 -m pytest tests/gates/test_zero_dep_boot.py -q --tb=line` → exit 1，其余 2 条仍逐字红在
  `NotImplementedError: compose_stack 尚未实现` —— **归工作项 8，未被本批影响**。
- 区间判据：`git diff --numstat f4fe0ce..HEAD -- tools/gates/expected-red.txt` → `0	1`（只删一行，删的正是本批转绿那条）；
  `git diff --numstat f4fe0ce..HEAD -- docs/architecture/system-baseline.md` → `28	0`（**只增不删**）。
- 判据有没有牙齿，做了变异验证（改真文件跑、跑完还原，`diff` 确认字节一致）：
  硬失败插值 / 缺 `:-` 默认值 / AI 变量非空默认 / 绑 `0.0.0.0` / 变量驱动的绑定地址 / `ports` 长语法 /
  顶层 `version:` / `latest` tag / 不写 tag —— **9 种缺陷各自被抓**；而 `$$` 转义（`echo $${HOME}`）**不误伤**，10 条仍全绿。

**起栈尝试（证据，不是判据）**

「栈起得来且全部 healthy」是**工作项 8** 的门禁（`test_stack_boots_and_all_services_healthy`），本 plan 不拿它当 Exit Criteria。
以下只是如实记录本机跑过什么、退出码是多少：

- `docker compose up -d` → **exit 1**。原文错误：
  `Error response from daemon: failed to set up container networking: driver failed programming external connectivity on endpoint agenerp-frontend-1 (…): Bind for 0.0.0.0:8080 failed: port is already allocated`
- 按 `AGENTS.md` 裁判规则 3 **原样复跑**：第二次 `docker compose up -d` → **exit 0**（首次那次**未复现**）。
  但栈仍不正常：`frontend` 陷入重启循环，日志逐字 `host not found in upstream "backend:8000"`。
  查到的**事实**（不是猜的根因）：`docker inspect agenerp-frontend-1 --format '{{range $k,$v := .NetworkSettings.Networks}}{{$k}}{{end}}'` → **空**，
  即该容器压根没接上网络——是首次那次 networking setup 失败留下的残骸。
- `docker compose down -v`（exit 0）后再跑 `docker compose up -d` → **exit 1**，同一个端口报错，**这次复现了**。
  占用者查实：`docker ps -a --format '{{.Names}}	{{.Ports}}'` 显示本机另有一个**与本仓无关**的 frappe 栈
  `docker-frontend-1`（Up 2 hours）占着 `0.0.0.0:8080->8080/tcp`。那个栈**没有动它**。
- 用本仓 compose 自带的逃生口换个宿主端口重跑：`AGENERP_HTTP_PORT=18080 docker compose up -d` → **exit 0**。
  终态 `docker compose ps -a`：11 个服务全部起来（`configurator` / `create-site` 两个一次性容器 `Exited (0)`，其余 `Up`）；
  `create-site` 日志末尾逐字 `Current Site set to frontend`；
  `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:18080/api/method/ping` → **200**，首页返回真实 Frappe HTML。
  端口映射 `127.0.0.1:18080->8080/tcp` —— **回环绑定在运行期也确认了**，不只是静态文本扫描说了算。
- 收尾 `docker compose down -v` → **exit 0**，本仓容器清零；无关的 `docker-frontend-1` / `proof-proof-1` 仍在跑，未受影响。
- **本条不宣称工作项 8 的门禁通过**：本仓 compose 只给 db/redis 写了 `healthcheck`，应用侧服务的 healthy 根本还不可判；
  首页也还没有「AI 能力未配置」文案（应用层，明确在本 plan 的 `## Non-Goals` 里）。这两件事归工作项 8。

**落地方式：本批改动必须经 PR 落地，不得直推 `main`**

- 理由：`.github/workflows/gates.yml` 的 `on:` 无条件含 `pull_request`，PR 会让 `gates-l1` **在真正的 compose 2.38.2 runner 上先跑一遍**，
  再决定合不合进 `main`。而**划名单是单向动作**——名单变短了 loop 自己放不回去（放回属变长，`expected-red-ratchet` 无
  `Gates-Change-Approved-By:` 即拦），只能由人恢复。先验证再合并，是把「不可验证 + 不可回退」变成「合并前可见」的唯一办法。
- 具体走法（当前分支就是 `main`）：`git push origin main:refs/heads/<分支名>` 再开 PR。
- **开 PR 属对外动作，本 plan 不执行**；这一条的交付物就是把要求写在人看得到的地方（本条 + plan 的 `## Deferred But Adjudicated`）。
- ⚠️ **起草时的一个前提已经过期，在此更正**：plan 里写「本地 `main` 已积压若干未推送提交（`f47031f` / `3d405fb` / `9ae88bf`），
  那个 PR 会一并带上前几个 plan 的活」。`git fetch origin` 后 `git log --oneline origin/main..HEAD` 实测**只有 `ba7bdae` 一条**——
  `origin/main` 已经在 `f4fe0ce`，前几个 plan 的提交都推过了。所以那次 `gates-l1` 实跑**只覆盖本批**，红了就是本批引入的，不必再分辨存量。
- 失败特征（万一仍直推 `main` 且踩中版本差）：`gates-l1` 报「名单外的门禁红了」并退 1，**恢复只能由人做**。

**一处产物冲突，如实记录，不由本 plan 消解**

- `tools/mission-driver/prompts/execute.md:10-11` 要求 `EXECUTE` 步在 plan 完成时就把 `Plan Status` 置 `completed`、
  并把 roadmap 工作项改成完成态；而 `docs/backlog/p0-foundation-roadmap.md:9` 写着该文件「由引擎在 **closure 审计通过后**回写」，
  `:35` 又把 `done` 定义为「完成，**且通过 closure 审计**」。Phase 3 跑在 `EXECUTE` 内、在独立关闭审计**之前**——
  照 `execute.md` 做就等于在审计前宣称通过审计。
- 按 `AGENTS.md` 开头声明的优先级次序（红线 > `docs/masterplan/` 执行协议 > `AGENTS.md` 其余 > **上游模板默认**），
  `execute.md` 是上游模板默认，roadmap 是本项目自己的产物且与裁判规则 1/2（「AI 无权自报通过」）同向——**roadmap 胜出**。
- 因此本轮 `EXECUTE`：`Plan Status` **保持 `active`**、`## Closure Gates` 九框**保持未勾**、
  roadmap 工作项 3 **保持 `planned`**。三者都归 `CLOSURE_AUDIT` 步落地。`closureScriptCheck` 判 fail 是**预期**，
  它正是把流程送进独立关闭审计的那一步。
- 附注（不属本 plan 范围）：工作项 1 的门禁早已转绿并划掉、状态却仍是 `todo`，那是 plan `…-2341-2` 的遗留，不由本 plan 处置。

**红线自查（区间 diff，基线 `f4fe0ce`）**

- `git diff --name-only f4fe0ce..HEAD -- tests/gates/ .github/workflows/ missions/ docs/masterplan/DECISIONS.md tools/gates/check_expected_red.py tools/gates/gate-verify.mjs | wc -l` → **0**
- `git diff --name-only f4fe0ce..HEAD -- docs/masterplan/ | grep -v '^docs/masterplan/STATE\.md$' | wc -l` → **0**
- `git diff --numstat f4fe0ce..HEAD -- docs/masterplan/STATE.md | awk '{print $2}'` → **空**（该文件本轮未被触碰）

- Next：本批经 PR 落地并回写 `gates-l1` 的实跑结果；`CLOSURE_AUDIT` 关闭本 plan 并把 roadmap 工作项 3 置 `done`。


### 2026-08-21 · plan-2026-08-20-2341-2 独立关闭审计通过 · `plan 关闭` · `无代码改动`

- `CLOSURE_AUDIT` 步（独立子代理，未参与本 plan 起草与实现）对 live repo 逐条复核
  `docs/plans/p0-foundation/2026-08-20-2341-2-customization-pack-normalizer.md`，结论 **closable**。
  第一次审计的阻塞点（只有人能做的划名单提交）已由 `920ce0e`（`Gates-Change-Approved-By: lize`）消除。
- 复跑证据（基线 HEAD `f47031f`，退出码单独取 `$?`）：
  - `python3 tools/gates/check_expected_red.py` → **exit 0**（门禁 13 项：预期红 8，绿 5，跳过 0）
  - `python3 -m pytest tests/gates/test_normalizer_idempotent.py -q` → **exit 0**（3 passed）
  - `python3 -m pytest tests/unit -q` → **exit 0**（40 passed）
  - `ruff check agenerp tests/unit` → **exit 0**（All checks passed!）
  - `python3 -m pytest tests/gates -q --tb=no` → exit 1（1 failed / 5 passed / 7 errors）——仍红的 8 条全在
    `tools/gates/expected-red.txt` 内，**未报全绿**
  - `node tools/mission-driver/src/plan-check.mjs <该 plan> --strict` → **exit 0**（`totalUnchecked: 0`）
- 红线自查：`git log f180b40..HEAD -- tests/gates/` 只有人的名单迁移两条；
  `git status --short -- tests/gates/ .github/ docs/masterplan/` → **输出为空**。本轮零改动于红线目录。
- 落地改动（仅文档）：该 plan 头 `Plan Status: active` → **`completed`**、`## Closure Gates` 九框由本次独立审计勾上、
  `## Closure` 新增第二次审计证据小节（第一次审计原文逐字保留在「历史记录」小节，一行未改）；
  `docs/backlog/p0-foundation-roadmap.md` 工作项 1 由 `planned` → **`done`**（roadmap 自述「由引擎在 closure 审计通过后回写」）。
- 按裁判规则 2，本条的宣称是：**实现、验证、关闭审计均已到位，plan 已关闭**——证据即上列命令原文与退出码，
  commit sha 见本条落盘提交。

### 2026-08-21 · plan-2026-08-20-2341-2 重开后的 `EXECUTE` 步 · `无代码改动`

- plan `docs/plans/p0-foundation/2026-08-20-2341-2-customization-pack-normalizer.md` 由 `DRAFT_PLANS` 步
  从 `deferred` 改回 `active`（重开条件已由人的 `920ce0e` 满足）。本轮 `EXECUTE` 复核后确认：
  **Phase 1–3 的执行项与 Exit Criteria 在磁盘上已全部 `[x]`，没有未完成的 Phase**，故本轮不动任何代码。
- 原样复跑（基线 HEAD `f47031f`，退出码单独取 `$?`）：
  - `python3 tools/gates/check_expected_red.py` → **exit 0**（门禁 13 项：预期红 8，绿 5，跳过 0 · ✅ 与预期红名单完全一致）
  - `python3 -m pytest tests/unit -q` → **exit 0**（40 passed）
  - `python3 -m pytest tests/gates/test_normalizer_idempotent.py -q` → **exit 0**（3 passed）
  - `ruff check agenerp tests/unit` → **exit 0**（All checks passed!）
  - `python3 -m pytest tests/gates -q --tb=no` → **exit 1**（1 failed / 5 passed / 7 errors）——其余 8 条仍红且
    全部在预期红名单内，**本轮未报全绿**（scope 限制照 plan 写明）
- 与 plan `## Closure` 那节记录的差别：`check_expected_red.py` 当时（HEAD `9889194`）**exit 1**，现在 **exit 0**。
  差别来自人的 `920ce0e`（`Gates-Change-Approved-By: lize`）把预期红名单迁出 `tests/gates/` 并划掉 5 行，
  **不是实现改动**。plan 的 `## Human Handoff` 验收条件由此满足。
- 红线自查：`git diff --name-only f47031f..HEAD -- tests/gates/` → **输出为空**；
  `git status --short -- tests/gates/ tools/gates/ .github/ docs/masterplan/` → **输出为空**。本轮改动只落在
  `docs/backlog/` `docs/logs/` `docs/plans/` 三处。
- 文档改动：
  - `docs/backlog/p0-foundation-roadmap.md` 工作项 1 由 `todo` → **`planned`**。**没有直接改成 `done`**：
    该表把 `done` 定义为「完成，且通过 closure 审计」，本轮独立审计尚未发生；按 plan 3 的先例（`f47031f`），
    `planned → done` 的回写由独立 `CLOSURE_AUDIT` 步在批准的同时落地。而 `todo`（=「未开始，没有 plan」）与事实不符，故先纠正。
  - 同文件三处指向 `tests/gates/EXPECTED_RED.txt` 的失效路径改为 `tools/gates/expected-red.txt`（该文件 `920ce0e` 起已迁移）。
  - plan 文件追加「执行步复核记录」一节（只追加，未改写既有任何一行）。
- `## Closure Gates` 九个框**保持未勾**：其中「closure audit was independent」「closure evidence exists in files」
  执行步无权自证（`AGENTS.md` §⚖️ 裁判规则 1、计划指南规则 13）。`CLOSURE_SCRIPT_CHECK` 会因此判 fail，
  按 `flows/plan-execution.json` 转入 `CLOSURE_AUDIT`——**这是设计路径，不是失败**。
- 按裁判规则 2，本轮宣称是：**实现与验证已到位，plan 关闭待独立审计**，不是「已关闭」。


### 2026-08-21 · mission 验证步 · `full-green verification`（无代码改动）

- 基线 HEAD `3d405fb`，工作区仅 3 个文档文件未提交（plan 关闭记录 / roadmap 工作项 2 → `done` / 当日日志）。
- 原样执行 mission 配置的验证命令（唯一非空的一条）：
  - `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
    - `门禁 13 项：预期红 8，绿 5，跳过 0` · `✅ 与预期红名单完全一致` · `40 passed in 0.03s`
- 预期红名单**无需变更**：本轮没有让任何门禁测试转绿，`check_expected_red.py` 报「与预期红名单完全一致」，
  `tools/gates/expected-red.txt` 保持 8 项不动（名单只能变短，本轮无可划项）。
- 红线自查：`git diff --name-only HEAD -- tests/gates/ .github/workflows/ docs/masterplan/` → **输出为空**，
  本轮改动全部落在 `docs/backlog/` `docs/logs/` `docs/plans/` 三处。
- ruff / mypy / docker 三条命令在 mission 配置里仍为空（它们本身是 P0 的交付物，装上后才会出现），按步骤跳过。


### 2026-08-21 · plan-2026-08-20-2341-3 独立关闭审计 · `approved`（无代码改动）

- 关闭 plan `docs/plans/p0-foundation/2026-08-20-2341-3-snapshot-structured-diff.md`：
  九个 Closure Gates 由执行步按设计全部留空（其中「closure audit was independent」执行步无权自证，
  `AGENTS.md` §⚖️ 裁判规则 1），本轮由独立 `CLOSURE_AUDIT` 代理（fresh session，不带实现上下文）逐条复核后勾选。
- 审计方法：**不采信 plan 里的 `[x]`**——逐条读活代码与活文档 + 原样复跑全部验证命令，退出码单独取 `$?`。
  基线 HEAD `3d405fb`，`git status --porcelain` 输出为空。
- 复跑结果（与 plan 记录逐条一致）：
  - `python3 -m pytest tests/gates/test_snapshot_diff_structured.py -q -m 'not live'` → **exit 0**（2 passed, 1 deselected）
  - `python3 -m pytest tests/unit -q` → **exit 0**（40 passed）
  - `ruff check agenerp tests/unit` → **exit 0**（All checks passed!）
  - `python3 tools/gates/check_expected_red.py` → **exit 0**（门禁 13 项：预期红 8，绿 5，跳过 0）
  - `python3 -m pytest tests/gates -q --tb=line` → 1 failed / 5 passed / 7 errors，**未报全绿**（scope 限制已在 plan 写明）
  - `test_field_addition_shows_up_as_structured_change` 单跑复核红因 → 仍是 `live_site` 的 `NotImplementedError`
  - `git diff --name-only b8aadbf..HEAD -- tests/gates/ .github/workflows/ docs/masterplan/DECISIONS.md` → **输出为空**
  - `git diff --stat 9ae88bf..3d405fb -- docs/masterplan/STATE.md` → `5 insertions(+)`、删除 0（红线 5 只追加）
- 另核查：反空壳（`capture` 真实走 glob → json → normalize → SnapshotEntry；`diff` 走集合运算；
  `SnapshotScopeMismatch` 显式 raise；无空体、无 `return None` 占位、无吞异常）、
  `test_snapshot_capture.py` 9 条全部用 `tmp_path` **非空夹具**驱动（起草评审点名的 H 类空洞判据确认已被压住）、
  未顺手做掉邻项（`schema_drift` / `export_customizations` / `apply_pack` 仍在未实现清单且仍抛 `NotImplementedError`）、
  五点一致性、Deferred 诚实性、文档同步。结论 **`approved`**。
- 落地：plan 头 `Plan Status: active` → **`completed`**、`Last Reviewed` → 2026-08-21、九个 Closure Gates 改 `[x]`、
  `## Closure` 的 `<未关闭>` / `<pending>` 占位替换为完整审计记录（审计人 / 基线 sha / 复跑结果表 / 反空壳复核 / 结论）；
  Phase 3 那条「文件头为 `active`」的 Exit Criterion 补记审计步改为 `completed`（保持五点一致）。
  `docs/backlog/p0-foundation-roadmap.md` 工作项 2 由 `planned` → **`done`**——该表定义 `done` = 完成且通过 closure 审计
  且门禁转绿并从预期红名单划掉，两条现均为真（名单由人在 `920ce0e` 划掉）。
  `parseRoadmapMarkdown` 复验：8 items 全部解析，工作项 2 → `done`，`overallProgress` 0.13。
- `node tools/mission-driver/src/plan-check.mjs <plan> --strict` → **exit 0**（`passed: true`，checked 39 / unchecked 0）。
- Next step: roadmap 工作项 1（定制包规范化器，plan 2 遗留，仍 `todo`）或工作项 4（工具契约层 v0，
  它同时解锁 `live_site` fixture 与 `SiteSnapshotSource.read`）。


### 2026-08-21 · plan-2026-08-20-2341-3 快照与结构化 diff（工作项 2）· Phase 1–3 执行完毕

- 交付：`agenerp/snapshot.py` 的实现早在 `e145e43` 已落盘（首轮循环产出、被 `--max-cycles 2` 截断在提交前、
  人工代为落盘），本轮补完 plan 剩下的两块——**Phase 2 的真实语义覆盖**与 **Phase 3 的结构边界文档**。
- 新增 `tests/unit/test_snapshot_capture.py`（9 条）：门禁那两条 L1 在**零条目**快照上恒真，
  一个恒返回空的假实现也能过。用非空夹具目录驱动 `<root>/<scope>/*.json` 的真实读取路径把它压出来——
  条目数与内容正确 / 连读两次相等 / 改夹具后不再相等（证明它真在读文件）/ 易变字段被剥掉 /
  位置不存在为零条目而非异常 / scope 各自携带 / 显式来源优先于环境。
- 新增 `tests/unit/test_snapshot_diff.py`（11 条）：增删改三类各归各位不串味、条目形状按 live 断言定稿
  （`.doctype` / `.fieldname`）、`changed` 带前后值、身份是 `(doctype, fieldname)` 而非裸字段名、
  `summary()` 空与非空均可调用、跨 scope 抛 `SnapshotScopeMismatch` 而非「全删全增」、纯函数不改入参。
- `tests/unit/test_contract_surface.py`：`capture` / `diff` 由 `NOT_YET_IMPLEMENTED` 迁入 `IMPLEMENTED`
  ——这正是 `e145e43` 提交信息里自记的「已知遗留」，也是 STATE §2 `2026-08-21T01:56Z` 记的「下一轮的活」。
  `diff` 的 `CALL_ARGS` 同步换成真实 `Snapshot` 入参（旧的 `({}, {})` 在实现到位后已是失效声明）。
  `schema_drift` / `export_customizations` / `apply_pack` 仍在未实现清单且仍抛 `NotImplementedError`
  ——没有顺手做掉别的工作项。
- 文档：`docs/architecture/module-boundaries.md` 新增 **§11.5 状态快照与结构化 diff 的结构边界**
  （`Snapshot` / `SnapshotSource` / `diff` 的职责与不变量、为什么相等性里不能有采集时刻、
  「结构化」的判定含义、两条显式拒绝、留给工作项 4 的来源接缝）。`agenerp/snapshot.py` 模块 docstring
  早已指向 §11.5，本轮把它补实。
- **plan 前提已被人变更，如实记录**：plan 起草时预判 `check_expected_red.py` 会因「名单内的门禁却绿了」
  退 1，并据此规定末步自置 `deferred` 等人划名单。实际人已在 `920ce0e`（带 `Gates-Change-Approved-By: lize`）
  把名单迁出红线 1 并划掉 5 行（规范化器 3 + 快照 diff 2）。**本 plan `## Human Handoff` 的验收条件
  （`check_expected_red.py` → exit 0）在执行前就已满足**，deferral 的理由不复存在 →
  末步按 plan 自己写的重开条件保持 `Plan Status: active`，交由独立 `CLOSURE_AUDIT` 关闭；
  `## Closure Gates` 九框**保持未勾**（其中「closure audit was independent」执行步无权自证，
  `AGENTS.md` 裁判规则 1）。`CLOSURE_SCRIPT_CHECK` 判 fail 时 `plan-execution.json` 的 fail 分支正是
  `goto CLOSURE_AUDIT`，这条路走得通，不是死循环。
- 验证（命令原文 + 退出码，均单独取 `$?`）· 代码提交 sha `9ae88bf`：
  - `python3 -m pytest tests/gates/test_snapshot_diff_structured.py -q -m 'not live'` → **exit 0**（2 passed, 1 deselected）
  - `python3 -m pytest tests/unit -q` → **exit 0**（40 passed）
  - `ruff check agenerp tests/unit` → **exit 0**（All checks passed!）
  - `python3 tools/gates/check_expected_red.py` → **exit 0**（门禁 13 项：预期红 8，绿 5，跳过 0）
  - `python3 -m pytest tests/gates -q --tb=line` → 1 failed / 5 passed / 7 errors；
    `test_field_addition_shows_up_as_structured_change` 仍红，红因复核为 `tests/gates/conftest.py:17`
    的 `live_site` `NotImplementedError`（属工作项 4/6，未被本 plan 弄成别的错）
  - `git diff --name-only b8aadbf..HEAD -- tests/gates/ .github/workflows/ docs/masterplan/DECISIONS.md`
    → **输出为空**（红线 1/2/3 区间自查通过；用区间而非 `git diff HEAD`，后者看不到已提交改动）
- **日期偏差如实记录**：plan 的 Phase 3 Targets 写的是 `docs/logs/2026/08-20.md`（起草日），
  实际执行日是 2026-08-21，按 `AGENTS.md` 操作规则 7「一天一文件」写入本文件。
- roadmap：`docs/backlog/p0-foundation-roadmap.md` 工作项 2 由 `todo` 改 **`planned`**，**不改 `done`**——
  该文件的 Status 表把 `done` 定义为「完成，**且通过 closure 审计**」，而本 plan 的 `## Closure` 仍是 `<未关闭>`。
  执行步自报 `done` 就是自证关闭（`AGENTS.md` 裁判规则 1）。`planned`（「已有执行 plan 且通过草案评审」）是这三值
  词汇表里唯一为真的值。改后 `parseRoadmapMarkdown` 复验：8 items 全部解析，工作项 2 → `planned`。
  工作项 1 仍是 `todo`（plan 2 的遗留，不属本 plan 范围，不代改）。
- Next step: 独立关闭审计（`CLOSURE_AUDIT`）；随后 roadmap 工作项 4（工具契约层 v0）——
  它同时解锁 `live_site` fixture 与 `SiteSnapshotSource.read`。


### 2026-08-21

- 关闭 plan `docs/plans/p0-foundation/2026-08-20-2341-1-agenerp-package-skeleton.md`：补齐唯一未勾选的
  Closure Gate「closure audit was independent」。该项按设计留给 CLOSURE 步的独立审计，执行步无权自报
  （`AGENTS.md` §⚖️ 裁判规则 1），本轮由独立审计代理（fresh session，不带实现上下文）完成。
- 审计方法：不采信 plan 里的 `[x]`，逐条读活代码 + 原样复跑全部验证命令。复跑结果与 plan 记录逐条一致，
  基线 `c78d877`：`import agenerp,agenerp.pack,agenerp.snapshot` → exit 0；门禁 `ModuleNotFoundError` 计数 → 0；
  `python3 -m pytest tests/unit -q` → exit 0 / 12 passed；`ruff check agenerp tests/unit` → exit 0；
  `python3 tools/gates/check_expected_red.py` → **exit 0**（门禁 13 项：预期红 13，绿 0，跳过 0）；
  `project-context.md` Verification Commands 段 `fill real command` 计数 → 0（`:84` 的规范句原样留存，计数 1）；
  `ai-autonomy-policy.md` 的 `<human | subagent | none>` 计数 → 0。
- 另核查：反空壳（六个契约面签名与 `tests/gates/` 调用处逐字对齐、无空体无吞异常、12 条 unit 断言真实执行）、
  五点一致性、Deferred 诚实性、红线未触碰（`tests/gates` / `.github/workflows` / `docs/masterplan/DECISIONS.md`
  最新提交 `5a970fe`，早于本 plan 的 `f180b40` / `c78d877`）、文档同步。结论 `approved`。
- 落地：Closure Gate 改 `[x]`，改写 Closure Status Note，把 Closure Audit Evidence 的 `<pending>` 占位
  替换为完整审计记录（审计人 / 日期 / 基线 sha / 复跑结果表 / 结论）。plan 无未勾选项。
- Next step: 后继 plan `2026-08-20-2341-2-customization-pack-normalizer.md`（实现 `agenerp.pack.normalize`）。

### 2026-08-21 · plan-2026-08-20-2341-2 关闭再入（第 2 次）· 无代码改动

- 触发：`CLOSURE_SCRIPT_CHECK` 判 fail（`9 unchecked items`）后引擎把本 plan 回灌进 EXECUTE。
  逐行核对未勾项落点：`grep -n '\- \[ \]' <plan>` → **仅 206–214 行**，全部且仅仅是 `## Closure Gates`。
  Phase 1–3 的执行项与 Exit Criteria 已全 `[x]`，**没有任何未完成的执行工作**。
- 原样复跑（HEAD `5cf0f74`，命令原文 + 退出码，均单独取 `$?`、不经管道）：
  - `python3 -m pytest tests/gates/test_normalizer_idempotent.py -q` → **exit 0**（3 passed）
  - `python3 -m pytest tests/unit -q` → **exit 0**（22 passed）
  - `ruff check agenerp tests/unit` → **exit 0**（All checks passed!）
  - `python3 tools/gates/check_expected_red.py` → **exit 1**（「门禁 13 项：预期红 10，绿 3，跳过 0 /
    ❌ 名单内的门禁却绿了」，列出本工作项三条）
  - `git diff --name-only f180b40..HEAD -- tests/gates/` → **空**（红线 1 区间自查通过）
  - `git status --porcelain` → 空（工作区干净）
- 结论沿用独立关闭审计：**not closable —— blocked on human**，实现侧无缺陷，不需要回 EXECUTE 改代码。
  三件本轮**刻意不做**的事及依据：
  1. 不勾 `## Closure Gates` 九框 —— 其中「closure audit was independent」「closure evidence exists in files」
     「in-scope behavior is complete」在划名单提交落地前不能全为真，勾上等于自证关闭（`AGENTS.md` 裁判规则 1/2）。
  2. 不划 `tests/gates/EXPECTED_RED.txt` 三行 —— 红线 1，且需 `Gates-Change-Approved-By:` trailer，只有人能做。
  3. 不把 `docs/backlog/p0-foundation-roadmap.md` 工作项 1 由 `todo` 改 `done` —— 那等于替人选定
     `needs-human-expected-red-handoff.md` 冲突 3 的选项 (d)（关闭与划名单解耦），本批 plan 明文「未采用」。
- `Plan Status` 维持 `deferred`（不改回 `active`，避免被 `activePlans()` 反复重选烧预算）。
  阻塞点已在 `docs/masterplan/STATE.md` §3 的 `[open] 2026-08-21 · P0.4` 行登记，本轮不重复追加。
- 解除阻塞的唯一路径（人做）：带 `Gates-Change-Approved-By: <姓名>` trailer 的提交划掉那三行，
  验收 `python3 tools/gates/check_expected_red.py` → exit 0，再把 plan 由 `deferred` 改回 `active` 走关闭审计；
  或人直接选定冲突 3 的选项 (d)。

### 2026-08-21 · BUILD_VERIFY 复跑判为环境缺陷 · 修 launchd PATH（无实现改动）

- 触发：mission 配置的 `commands.test` 退 1。原样复跑一次，失败稳定复现（不是抖动）：
  - `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 1**
    （「门禁 13 项：预期红 8，绿 5，跳过 0 / ❌ 名单外的门禁红了（真的坏了）：
    `tests/gates/test_zero_dep_boot.py::test_compose_config_valid_with_empty_env`」）
- 定位（读最后一层 traceback，不是第一层）：`FileNotFoundError: [Errno 2] No such file or directory: 'docker'`。
  查实 `/usr/local/bin/docker -> /Applications/Docker.app/Contents/Resources/bin/docker` **存在**，
  只是 `/usr/local/bin` 不在进程 PATH 上。补上 PATH 后同一条 compose 命令退 0：
  - `docker compose -f docker-compose.yml config -q` → **exit 0**
- 根因：`d35ec2b` 迁 launchd 时 plist 把 PATH 写死，漏了 `/usr/local/bin`。该测试在 `ba7bdae`
  被划出预期红名单时是**真绿**的——那时循环跑在登录 shell 下，PATH 完整。判据没错，实现没错，
  **是跑判据的环境退化了**。按裁判规则：没有为迁就它改动任何 `tests/gates/**` 文件。
- 修实现侧（`0f2c59a`）：
  - `tools/install-loop-agent.sh`：plist 模板 PATH 补 `/usr/local/bin`、`/usr/local/sbin`（附原因注释）
  - `tools/loopx-writeback.sh`：每轮入口做幂等 PATH 自愈。已装载的 plist 不能就地改——
    unload/load 会半路杀掉正在跑的循环——所以自愈点选在每趟开跑前
- 修后验证（命令原文 + 退出码，退出码单独取 `$?`）：
  - `export PATH="$PATH:/usr/local/bin:/usr/local/sbin"; python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q`
    → **exit 0**（「门禁 13 项：预期红 7，绿 6，跳过 0 · ✅ 与预期红名单完全一致」/ 50 passed）
- **本轮不自报 pass**：修的是 PATH，而本轮 `GATE_VERIFY` 子进程继承的是引擎**启动时**的旧 PATH，
  自愈要到下一趟经 `loopx-writeback.sh` 才生效。按「如实报 fail 比虚报 pass 便宜」，本步报 **fail**。
- `tools/gates/expected-red.txt` 一行未动：本轮没有门禁由红转绿，名单不需要变短。
- Next step: 下一趟循环起来后 PATH 已自愈，同一条命令应退 0；若仍红，说明自愈点选错，回本条重查。

### 2026-08-21 · 零依赖启动门禁转真绿 · PATH 自愈点上移到判定器（full-green verification）

- 承接上一条的 `Next step`：预测「下一趟循环起来后 PATH 已自愈」。**预测未兑现** ——
  本趟进程 PATH 仍是 `d35ec2b` 那份旧 plist 的原文（`.../opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin`），
  一字不差、不含 `/usr/local/bin`。即上一条自己写的分支：「若仍红，说明自愈点选错，回本条重查」。
- 原样复跑两次，失败稳定复现（不是抖动）：
  - `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 1**
    （「预期红 8，绿 5，跳过 0 / ❌ 名单外的门禁红了：
    `tests/gates/test_zero_dep_boot.py::test_compose_config_valid_with_empty_env`」）
- 根因与上一条同源，但**自愈点选低了一层**：`0f2c59a` 把自愈放在 `tools/loopx-writeback.sh`，
  只覆盖走那条入口的运行；已装载的 launchd plist 半路改不了（unload/load 等于杀掉正在跑的循环），
  于是本趟引擎仍以旧 PATH 启动，`GATE_VERIFY` 与本步 spawn 的 pytest 一并继承。
  查实 `/usr/local/bin/docker -> /Applications/Docker.app/...` **存在且可用**
  （`/usr/local/bin/docker version` → Client 29.2.1 / Server Docker Desktop 4.62.0，daemon 在跑）。
  **判据没错、实现没错，是跑判据的环境少了一段 PATH。**
- 修实现侧：自愈点上移到判定器自身 `tools/gates/check_expected_red.py`。
  判定器跑在谁的环境里不由它决定（launchd / CI / 人手 shell），所以由它在起 pytest 前补齐系统标准路径：
  - 新增 `healed_env()`，`run_pytest()` 的 `subprocess.run(..., env=healed_env())`
  - 只**追加**不前置，且只补实际存在的目录 —— 绝不遮挡环境里已选定的 python / docker
  - **不放松任何判据**：docker 真没装时该测试照样红
- 修后验证（命令原文 + 退出码，退出码单独取 `$?`；注意是在**未修复的原 PATH 下**跑的）：
  - `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
    （「门禁 13 项：预期红 7，绿 6，跳过 0 · ✅ 与预期红名单完全一致」/ 50 passed）
  - 这是本 mission 首次 **full-green verification**：判定器与 `tests/unit` 同时退 0，无需外部 PATH 补丁。
- 红线自查：`git diff --name-only` 仅 `tools/gates/check_expected_red.py` 一个文件，
  未触碰 `tests/gates/**`、`.github/workflows/**`、`docs/masterplan/**`、`missions/**`。
- `tools/gates/expected-red.txt` 一行未动：转绿的 `test_compose_config_valid_with_empty_env`
  本就**不在**名单内（名单 7 行，本轮判定器报「预期红 7」正好对上），名单不需要变短。

### 2026-08-21 · plan-2026-08-21-1634-1 BUILD_VERIFY 一次过（full-green verification）

- 承接 `8401ad9`（1634-1 补收尾 sha）与其后的独立关闭审计。本步只做一件事：**跑 mission 配置里的验证命令，不自报**。
- 验证（命令原文 + 退出码，退出码单独取 `$?`）：
  - `python3 tools/gates/check_expected_red.py && python3 -m pytest tests/unit -q` → **exit 0**
    （「门禁 13 项：预期红 7，绿 6，跳过 0 · ✅ 与预期红名单完全一致」/ **104 passed in 0.27s**）
  - 一次即过，无失败、无复跑、无「不可复现」条目。
  - 这是 `1634-1` 交付 `agenerp/seed/`（31 条新单测，`50 passed` → `104 passed`）之后的首次
    **full-green verification**，可作为后续调试的 known-good 基线。
- `tools/gates/expected-red.txt` 一行未动：本轮没有任何门禁由红转绿，判定器报「预期红 7」与名单逐条对上，
  名单不需要变短（红线 1 的边界说明：该文件是账本不是裁判，只在真有测试转绿时才划）。
- 红线自查：`git status --porcelain -- tests/gates/ .github/workflows/ missions/ docs/masterplan/ tools/gates/`
  → **输出为空**（零命中）。本轮工作区改动全部落在 `docs/plans/` 与本日志，无实现改动。
- 本轮成批提交的两份文档改动：
  1. `docs/plans/p0-foundation/2026-08-21-1634-1-...md` —— 独立关闭审计的证据落盘（审计由
     mission-driver `2026-08-21-171157` 的 `CLOSURE_VERIFY` 步做，fresh session、不带实现上下文），
     `Closure Gates` 最后一框由留空改为 `[x]`，`## Closure` 的「尚未跑」措辞同步改写。
  2. `docs/plans/p0-foundation/2026-08-21-1634-2-compose-healthcheck-app-services.md` —— 新建 plan
     （工作项 8 的前半：应用侧服务 healthcheck，**只让「全部服务 healthy」变可判定，不解锁 L2 门禁**），
     `Plan Status: active`，尚未开工。

### 2026-08-21 · plan-2026-08-21-1922-1 CLOSURE 落盘（独立关闭审计的证据入库）

- 承接 `cd813eb`（工作项 4 的 B 半：站点只读传输 + `SiteSnapshotSource.read`）。本步只做一件事：
  把 `CLOSURE_VERIFY` 里独立关闭审计者的结论与复跑证据**提交入库**，并复核 plan 自身一致。
- 审计改掉的唯一一处不一致（如实化，不是放宽）：Phase 3 的 `Proof` 项与其 Exit Criteria 原文写
  「实测数 **> 20**」并已打勾，而「实测回填」节落盘的实测数是 **10**、阈值不可判、实际换用了
  `list_resource('DocType')` **775 行** vs 摘掉 `PAGE_LENGTH_PARAM` 后**恰好 20 行** 的变异判据。
  两处已改写为「判据替换 + 理由 + 全部数字」，与「实测回填」节一致。换判据的事实执行会话本就写在
  plan 与本日志里，只是当时没同步到 checklist 文字。
- 本步复跑（命令原文 + 退出码，退出码单独取 `$?`；本机实测，非 CI）：
  - `python3 tools/gates/check_expected_red.py` → **0**（「门禁 19 项：预期红 7，绿 12，跳过 0 · ✅ 与预期红名单完全一致」）
  - `python3 -m pytest tests/unit -q` → **0**（130 passed in 0.42s）
  - `ruff check agenerp tests/unit tests/contracts` → **0**（All checks passed!）
  - `python3 -m pytest tests/contracts -q` → **0**（151 passed in 0.08s）
  - `node tools/mission-driver/src/plan-check.mjs --strict docs/plans/p0-foundation/2026-08-21-1922-1-site-snapshot-source-live.md`
    → **0**（`passed: true`，43 checked / 0 unchecked，`planStatus: completed`）
- 红线自查：`git diff --name-only -- tests/gates/ .github/workflows/ missions/ docs/masterplan/DECISIONS.md tools/gates/expected-red.txt`
  → **输出为空**；`git diff --numstat docs/masterplan/STATE.md` → **无输出**（本轮未动 STATE，`cd813eb` 里那条补充事实行的 `8	0` 不变）。
- **live 命令本步未复跑**（本机 8080 被占、栈未起），live 那七条退出码沿用执行会话的落盘记录；
  plan 里 `verification scope limited` 那条依然成立，不得报成「关闭时也在活站点上验过」。
- 工作项 4 在 `docs/backlog/p0-foundation-roadmap.md` **仍保持 `planned`，本轮不划**：该行绑的是
  「提供 `live_site` fixture」，没有属于自己的门禁测试，`done` 的字面定义对它不可满足（与工作项 7 同一情形）；
  理由早已写在「4 现状」行里，本轮无新事实推翻它。
