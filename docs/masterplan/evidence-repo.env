# AgenERP 证据仓指针 —— 换机器/换目录只改这一个文件
# 所有主计划文档对 XM 的引用都经由此处解析，不得在正文里写死绝对路径。
XM_PATH=/Users/lize/Documents/ChatGPT/XM
XM_BRANCH=validation/pre-build
XM_SHA=1c622c8119755b36992c54ba98fbf6840cd22ed4
# 若本机没有 XM：
#   git clone <XM 远端或本地路径> ../xm-evidence && cd ../xm-evidence && git checkout $XM_SHA
#   然后把上面的 XM_PATH 改成 ../xm-evidence 的绝对路径。
