# P2 阶段复盘 · 材料（给人用，**结论那两格留空**）

> 生成：2026-08-28 · 分支 `p2-views` @ `be523cc`
> 对应 **P2.8 · CP9 阶段复盘**（`02-WBS.md` §5 第 108 行，**状态源 = 人**）
> 模板：`03-SKILL-GATE-MAP.md` §A9 · 判据：`04-RUNBOOK.md` §7.2
>
> ⚠️ **本文件不是复盘纪要，是材料。** 第 6 项那两格（AGE / LoopX 续用还是停用）
> 由人填 —— 状态源是人，不是我。下面把**判据算出来的东西**摆出来，
> 每一格都注明「判据怎么说 / 证据是什么 / 按字面推出什么」，签字栏留空。
>
> ⚠️ **作者身份披露**：P2 的判据、实现、plan 绝大部分是我（loop 侧）写的。
> §7.2.1 逐字「抽查者不得是这些判据的作者」——**抽查那一项我没自己做**，
> 派了独立子 agent（见 §3.2）。其余各项的自述请按「作者自报」打折。

---

## 1. 对照 `00-GOALS.md` §2：这一阶段把 S1–S6 的哪一条往前推了

**S3 · 改得动且撤得回**，逐字：

> 一句话改老板首页 → `git diff` 看得见 → `git revert` 撤得回 → 同步到另一站点生效

四段的现状，**分开说**：

| S3 的第几段 | 谁交的 | 实测 |
|---|---|---|
| 「一句话改…」 | P2.3 视图 Agent | 两道真题 → 视图，落回 0（`qwen3.7-flash-2026-07-15`）；量化 主集 10/12、域外 6/6（`sonnet`） |
| `git diff` 看得见 | P2.4 第②步 | diff 只含本次改动 1 行，不夹带时间戳噪声 |
| `git revert` 撤得回 | P2.4 第③步 | 真 `git revert` → apply → 字段**真的消失** |
| 同步到另一站点生效 | P2.4 第④步 | 包 apply 到 `gitops.test`，字段从无到有，`frontend` 不受影响 |

🔴 **但四段没有被串成一条跑过。**
P2.3 证的是「一句话 → 视图 DSL」，P2.4 证的是「改 → diff → revert → 迁站点」，
而 P2.4 用的是一个**探针字段**（`ToDo.agenerp_gitops_probe`），不是视图 Agent 的产出。
⇒ **S3 的每一段都有实测，整条链没有。** 这句话请复盘时不要被上表的四个 ✅ 盖过去。

其余：S1/S2 本阶段未动；S4–S6 未开工。

---

## 2. 停机次数、触发条件、平均恢复时长

**0 次自动停机** —— 因为**自动循环本阶段一趟都没跑**（见 §6.1）。
`tools/gates/check_budget.py` 当前回「24 小时内没有循环趟次记录」。

⚠️ 人工中断有过（本会话里人两次用问题打断我的提问），但那不是 §A9 说的「停机」。

---

## 3. 门禁误判

### 3.1 判 fail 但其实是对的：**0 次**（本阶段未发现）

### 3.2 🔴 判 pass 但其实错了 —— **这是最危险的那个数字**

#### ① 抽查制（§7.2.1）· 2026-08-27 那次：**1 次**

已记在 `p2-views-roadmap.md` §「既定入账项 ②」：
`test_the_user_is_told_the_rich_text_lost_its_formatting` 名不副实，
变异实测「换成与字段无关的死文案 → 18 条活体判据全绿」。已重写并复验见血。
⚠️ **那次抽查者是作者本人（违反 §7.2.1）**，读者据此打折。

#### ② 阶段关口抽查（本次，**独立抽查者**）

抽样可复算：`python3 tools/experiments/p2_spotcheck/draw_sample.py`（种子 `20260827` 写死）。

**抽查者：独立子 agent（非判据作者）** —— §7.2.1 那条「抽查者不得是作者」这次**满足了**
（2026-08-27 那次没满足，见 ①）。

**结论：三条全部名副其实，本次抽查记到 0 次误放行。**

它先核了前置：`draw_sample.py` 的候选池是 `git diff` **现算**的、不是硬编码名单，
重跑复算出的三条与交给它的**逐字一致**（基线 `012385a`、种子 `20260827`）。
⚠️ 这一步很要紧 —— **抽查的独立性取决于池子不被作者裁剪**。

| # | 判据 | 它做的变异 | 判定 |
|---|---|---|---|
| 1 | `test_apply_execute.py::test_narrowed_out_entries_are_logged_not_silently_dropped` | ⓐ WARNING 换成 `pass`（真静默丢弃）→ 红 ⓑ 保留 WARNING 但只报条数、不列明细 → 红 | ✅ 名副其实，**且比名字更严**（名字只说「被记了」，它还要求逐条列出） |
| 2 | `test_schema_drift_oob.py::test_schema_drift_sorts_and_deduplicates` | ⓐ 去重不排序 → 红 ⓑ 排序不去重 → 红 | ✅ 名副其实 —— **名字里两个动词各砍一刀都见血**，没有一半靠另一半顺带绿 |
| 3 | `test_block_shapes.py::test_a_view_with_zero_blocks_is_rejected` | 删掉「零块即错」那两行 → 红 | ✅ 名副其实；这一刀还顺带证了**拒绝来自校验器而不是 dataclass 提前挡掉** |

**它自己指出的三处精度缺口**（都不构成名不副实，但照记）：
① 第 1 条用整串消息做子串匹配 ⇒ 管得住「报少了」，**管不住「报多了」**（把明细换成超集仍绿）；
② 第 3 条的 `any("blocks" in e)` 是宽松子串 ⇒ 换成一条恒发的错误仍绿，
   它分辨不出「零块被拒」和「什么都被拒」——**但同文件另一条会在该变异下红，判据族层面封住了**；
③ 第 2 条走假 runner，**没验 `trim_table` 在真 Frappe 上的口径**（名字没主张那一层）。

🔴 **它还留了一条给下一轮的话，正好撞上本材料 §3.2⑤**：

> 只跑了本机 pytest，没跑 CI。考虑到本场已有「目录写进 COVERED 但没有 job 真跑它」的前科，
> 「这三条在 CI 里确实被执行」这一点我没有独立取证 —— 我验的是判据本身的成色，
> **不是它在流水线里有没有被真正调起来**。建议下一轮把这一项单独查一次。

⚠️ **这句话值得单独看**：抽查制问的是「判据测的是不是它名字说的那件事」，
**它默认了那条判据会被跑**。而本阶段恰好证明了那个默认不成立（§3.2⑤）。
⇒ **抽查制本身可能缺一问：「它在 CI 里真的被执行了吗？」** —— 供复盘裁定要不要加。

#### ③ 🔴 独立收口审计另外抓到的**三条名不副实的绿**（不在抽样里，是审计撞见的）

这三条**全是我写的**，且**都写在 plan 里声称「已证明」的位置上**：

| # | 判据 / 声称 | 名字说的 | 实际验的 | 取证 |
|---|---|---|---|---|
| 1 | `test_the_postconditions_are_derived_from_behaviour_not_self_declared` | 后置事实**不是自报的** | 只断言「等于 True」 | 把实现改成**写死 `True`** → **13 条全绿** |
| 2 | P2.4 plan 声称「⑦ 组 5 条含**方向不变量仍然拦得住传反的参数**」 | creates 的方向不变量被证过 | 那 5 条**一条都没碰** `_assert_direction` | 把 creates 分支整条打掉 → **989 条全绿** |
| 3 | CI 门禁⑩ 的注释：`tests/render`「归 `gates-l2-live` 那条线」 | 它在别处被跑 | **没有任何 job 跑它**；写进 `COVERED` 只是让门禁转绿 | 逐条核 workflow 里每一个 pytest 调用 |

还有一条同族的（审计的「变异 B」）：`schema.drift` 的「只报不删」后置，
**执行体绕过记录器真的发出 `ALTER TABLE … DROP COLUMN`，事实照报 `True`，14 条全绿**。

**四条全部已修，且都复验见血**（写死 True → 红 · 打掉 creates 分支 → 红 ·
render 真接进 CI · 记录点下沉到 `Session.runner` 后变异 B → 5 红）。

⚠️ **计不计入「误放行次数」由人裁**。我的看法与理由，摆在这里供推翻：
它们**在被合并进主线之前就被独立审计抓住并修掉了**，没有一条像 P1 那次一样
「绿着好几天、靠人手动撞出来」。但**根因全在执行产物（我写的判据），不在引擎** ——
与 P1、与 2026-08-27 那次同形，**这是第三次同形**。

#### ④ 既定入账项 ①（人 2026-08-26 指定，不许丢）

`REF:ROADMAP-SPIKE1112` 的证伪判据方向相反（md5 比较只对 `DocType` 生效，`Workspace` 没有
`migration_hash`）。⚠️ **在跑之前发现的，没有变成误放行。** 人已裁定判据原样保留。

#### ⑤ 🔴 CI 门禁⑦ 红了两天没人看见（**本次新增，我认为该入账**）

`gates.yml` 那条「没有测试目录被漏在 CI 之外」**自 2026-08-26 起就是红的**：

```
COVERED 最后一次设定  2026-08-24  b0ad632
tests/dsl    出现     2026-08-26（P2.1）← 从这天起就红
tests/i18n   出现     2026-08-27（P2.7）
tests/render 出现     2026-08-27（P2.2）
tests/agents 出现     2026-08-28（P2.3）
```

**它恰恰在做它被造出来要做的事，而它连着咬了四个工作项没人看见。**
根因：mission 的 `commands.test` 是 `check_expected_red.py && pytest tests/unit`，**不跑 CI**。

⚠️ **这一条的根因不在执行产物，在判定面**（引擎侧的判定口径漏了 CI）。
它是 §7.2 续用/停用表里「根因在引擎」那一格**唯一的候选**，请复盘时单独判。
连带后果：**WBS §5 第 103 行那条 P2.3 验收命令，CI 上从来没跑过。**

---

## 4. 成本：本期消耗 vs 阈值

| | |
|---|---|
| 自动循环 | **本阶段 0 趟**（台账里只有 p0-foundation 13 趟、p1-insight 32 趟）⇒ 循环侧成本 0 |
| 百炼（活体判据） | P2.3 两道题约 22.5k token（`qwen3.7-flash-2026-07-15`，reasoning 全程 0） |
| Claude 订阅（量化评测） | `cost_usd_total` 记 **$7.212**，⚠️ **只含 Agent 侧**，判官那一层当时没进总账，缺口估 $2.5 上下（**是估算不是实测**，缺陷已修） |
| 阈值 | `tools/gates/budget.json` 管的是**循环**的 24 小时输入 token；本阶段循环没跑，该闸没有判定对象 |

⚠️ **额度盘点已过期**：交接文档 §4 那张表是 2026-08-28 之前的口径，
且本阶段新发现 **`qwen3.8-2.4t-a95b` 拒绝 `enable_thinking:false`（HTTP 400）** ——
满格额度那个模型**用不了关思考**，而关思考是本项目的预算前提（差 450 倍）。
**这一条要进模型档案表**，目前只记在 plan 与日志里。

---

## 5. 措辞审计（`00-GOALS.md` §5：不许措辞强于证据）

本阶段**主动记下的**「措辞强于证据」的自我修正，供复核：

1. **P2.0R 的 57/60**：明写「按点估计判过；95% 置信下界只有 87.6%，D-28 括号里『零失败』并未满足」，
   且明写「**不得写成 59/60 = 98.3%**」（只重跑失败项系统性偏高）。
2. **P2.3 的量化数字**：三处（plan / roadmap / log）都点名模型，并写「**与 P2.0R 那 95% 不可比**」。
3. **P2.4 第四步**：写明「Phase 4 那两个数**没有重跑**，这是**推理不是重跑**，供推翻」。
4. **CI 改动**：两次都写「**没有在真 GitHub Actions runner 上跑过**」。
5. **P2.2 的落回 = 0**：写明「**只对车间工人这一个角色成立**，推广要另量」。

⚠️ **反例也记**：§3.2③ 那三条正是「措辞强于证据」的实例 —— plan 里写着「已证明」，
而那件事根本没被判据碰过。**措辞审计能自查出来的，恰恰不是这一类**。

---

## 6. 方法论续用复评（阶段关口必答）

> `04-RUNBOOK.md` §7.2 逐字：**结论只有「续用」「停用」两种，不许写「继续观察」。**

### 6.1 AGE / mission-driver

| 判据（原文） | 证据 | 按字面推出 |
|---|---|---|
| **续用**：本阶段有 ≥1 个工作项**完整地**由循环推完（drafted → executed → 门禁绿 → 关闭），且门禁误放行 = 0 | 用量台账 `_tmp/loop-usage.jsonl` 里 **p2-views 一趟都没有**（只有 `p0-foundation` 13 趟、`p1-insight` 32 趟，最后一趟 2026-08-26T17:55）；`launchctl` 里现在也没有 agenerp 的作业 | **不成立**（第一半就没有证据） |
| **停用**：误放行 ≥1 次**且根因在引擎**；或全阶段没有任何工作项能**不靠人接管**跑完 | 误放行有（§3.2），根因在引擎的**只有 ⑤ 那一条**；而「没有工作项能不靠人接管跑完」—— 本阶段的工作项**全部由交互会话推完**，循环**没有被投入使用** | **两半都不干净**：前者取决于 ⑤ 算不算；后者是「没试过」不是「试了不行」 |

🔴 **判据在这里够不着**：它的两格都**预设了循环在跑**，而本阶段循环**未投入使用**——
这是第三种状态，规则没有为它写过。而规则又禁止「继续观察」。

**⇒ 这一格由人裁。** 可选的处置（我不替你选）：
① 按「没有任何工作项不靠人接管跑完」判**停用**；
② 判**续用**并说明「本阶段未投入使用，观察期顺延」——但那实质是「继续观察」，规则禁止；
③ **修判据**：给「未投入使用」补一格，并写清什么条件下重新计时。

**签字：____________**

### 6.2 LoopX

| 判据（原文） | 证据 | 按字面推出 |
|---|---|---|
| **续用**：跨会话恢复实际生效 ≥3 次，且与 mission-driver 无「谁说了算」的冲突 | `.loopx/registry.json` 的 `updated_at` 停在 **2026-08-20**；`.codex/goals/agenerp-goal/ACTIVE_GOAL_STATE.md` 最后修改 **2026-08-23**；**本阶段两次跨会话接续走的都是 `docs/logs/2026/` 下的交接 markdown**，不是 LoopX | **不成立** |
| **停用**：≥2 次状态冲突；或**跨会话恢复从未真正用上**（说明它只是多一层账） | 没有冲突记录；但「从未真正用上」这一条**有证据支持** | **第二半成立** |

**⇒ 按判据字面，LoopX 落在「停用」那一格。** 签字：____________

⚠️ **我能证的边界**：我证的是「本阶段这两个文件没被写过、接续实际靠的是交接 markdown」。
我**不能**证「LoopX 从来没在别的地方起过作用」。

---

## 7. 下一期只做一件最重要的事

**我的建议（供推翻）：把 S3 那四段串成一条跑一次。**

理由：§1 那张表四个 ✅，但**整条链没跑过**。P2 的验收清单项就是 S3，
而现在的证据是「每一段单独成立」——那与「这条路走得通」是两件事，
本仓自己反复记着这个区别（「渲染出来 ≠ 渲染对了」「调得通 ≠ 守约」）。

具体形态：视图 Agent 生成一个视图 → 落成定制包 → `git diff` → `git revert` → 迁站点。
⚠️ 它**卡在一笔已知欠账上**：视图定义现在**硬编码在 `agenerp/dsl/roles.py`**，
而那两个模块的模块头都点名由 P2.4 了结 —— **P2.4 没有了结它**（人裁定只交四步）。
⇒ 这件事要做，先要那笔欠账。

**次选**：P2.8 复盘本身产出的第 ⑤ 条（判定面漏了 CI）——它是唯一一条根因在引擎的。

---

## 附 · 本阶段的量化面

| | |
|---|---|
| 提交数 | 173（`d18c05c..HEAD`），其中本会话 25 |
| 判据数 | 全量离线 **1572 passed / 38 skipped**（P2 开工前的基线见各 plan） |
| 门禁 | `check_expected_red.py` **29 项，预期红 0，绿 29，跳过 0** |
| 活体 | `-m live tests/render` 18 passed · `-m live tests/agents/...` 13 passed 零 skip · `verify-gitops.sh` 退 0 |
| 未 push | `origin/p2-views` 落后 **65 个提交** —— push 是对外发布，红线⑥，等人明说 |
