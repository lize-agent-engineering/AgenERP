# CP1 · 主计划质询记录（等效 grill-me · 03 §A1 十问）

| | |
|---|---|
| 日期 | 2026-08-20 |
| 对象 | `docs/masterplan/` 全套（Day -1 定稿前） |
| 方式 | `grill-me` 本机未装 → 走 [03-SKILL-GATE-MAP.md](../masterplan/03-SKILL-GATE-MAP.md) §A1 内置十问 |
| 结果 | **两处站不住，已改**；其余通过 |

---

| # | 问 | 答 | 处置 |
|---|---|---|---|
| 1 | 验收命令是什么？跑一遍看退出码 | Day -1 / Day 0 各行**现在就能跑**（T2 已实跑：35 条引用 0 断链，退 0）。**但 P1–P5 各行的命令引用了尚不存在的包名与路径**——把未来的命令写成好像能跑，是本次质询发现的最实的一处 | ✅ **已改**：02-WBS 表规新增第 6 条——P1+ 是「占位形状」，可改字符串、不可改形状（必须仍能给出退出码），定稿时点为 CP2 |
| 2 | 做完了但项目仍失败，最可能哪步骗了我们？ | **门禁误放行**（判 pass 但其实错）。它不会触发任何停机条件，是唯一能静默通过全部防线的失败 | 已在 CP9 复盘模板第 3 项单列，且明说「后者是最危险的数字」 |
| 3 | 实测还是推断？n 多少？ | 引用的 spike 结论全部 n=1 | 00-GOALS §5 措辞纪律已写死禁用词 |
| 4 | 有没有措辞强于证据？ | 逐句查过。LoopX 一节只写本机实测到的（装成功、doctor ok、命令存在），未声称「跨会话恢复有效」——那要到 W0.12 才知道 | 通过 |
| 5 | 与北极星哪一段相关？ | P0–P5 各行均出自路线图，路线图各阶段与北极星三段对应关系在 00-GOALS §3 | 通过 |
| 6 | 谁能推翻判定？ | P0+ 是 `gate-verify.mjs` 复跑 + CI；**Day -1/Day 0 只有人**（无 CI），靠证据行约束 | 接受：Day 0 的产物之一就是 CI，鸡生蛋问题不可避免，已在 02-WBS Day 0 出口门禁把 `W0.7` 列为必绿项 |
| 7 | 失败怎么被发现？ | 四条停机条件 + 每日抽查一条证据行复跑 | 通过；但成本阈值目前是**空数**，由 `W0.0` 填 |
| 8 | 有没有「假定环境已就绪」的隐性依赖？ | **有，且不止一条**：Docker/ERPNext 演示栈、`ollama` + `qwen3:14b`、`node`、`gh` 登录、`loopx` 的 PATH | ✅ **已改**：04-RUNBOOK 新增 §0 环境前提表，并写明「缺 ollama 只 block `P1.0` 一行，先做别的」 |
| 9 | 引入了几个新活动部件？能否少一个？ | 四个：mission-driver fork、`--driver claude`、LoopX、CI。CI 不可省；fork 与 claude driver 是方案 C 的既定裁决；**LoopX 是唯一可省的那个** | 接受但设限：D-6 已写死 2 小时集成上限与无损退回路径，CP9 复评二选一 |
| 10 | 三个月后全新会话只读这套文档能否接手？ | 未知，靠 T1/T3/T4 实测判定 | 转 W-1.5 |

**质询结论**：可进入 T1–T4。第 1、8 两条的修改已落盘，第 7 条的空数留给 `W0.0`，第 9 条的风险已有事先写死的退出判据。
