# CP9 · P2 阶段复盘

> **判据事先写死**（`docs/masterplan/04-RUNBOOK.md` §7.2），本文只负责对着证据判，不重新定判据。
> **结论只有「续用」「停用」两种，不许写「继续观察」。**

日期：2026-08-28 · sha `1a4e118` · 分支 `p2-views`
模板：`03-SKILL-GATE-MAP.md` §A9 · 材料：[`2026-08-28-P2-retrospective-material.md`](./2026-08-28-P2-retrospective-material.md)

> ⚠️ **执行者声明（必须写在最前面）**
>
> 本文由 **loop 侧**执行，人 2026-08-28 明确委托（「p2.8 你来帮我审查」「是否可以一边修复和收掉」）。
> **而 loop 侧是本阶段绝大多数判据、实现与 plan 的作者。** 因此：
>
> - **§7.2.1 要求的「抽查 3 条」已整项外包给独立子 agent**（非作者），本文**不代抽、不代判**，
>   §3.2② 的结论以那次抽查为准。这是 P1 复盘那次没做到的一点（那次抽查者是作者本人）。
> - 第 6 项那两格的**结论由人 2026-08-28 当场签**（见 §6），本文只摆判据与证据。
> - 凡本文判到 loop 侧自己的地方，**逐处标注「自判」**，不混在证据里。

---

## 1. 对照 `00-GOALS.md` §2：把 S1–S6 的哪一条往前推了

**S3 · 改得动且撤得回**：「一句话改老板首页 → `git diff` 看得见 → `git revert` 撤得回 → 同步到另一站点生效」。

四段**各自**有实测（详见材料 §1）。

🔴 **但四段没有被串成一条跑过。** P2.4 那四步用的是一个**探针字段**
（`ToDo.agenerp_gitops_probe`），**不是视图 Agent 的产出**。
⇒ **S3 的每一段都有实测，整条链没有。**

⚠️ 这句话是本次复盘最该被记住的一条。本仓自己反复记着这个区别
（「渲染出来 ≠ 渲染对了」「调得通 ≠ 守约」），而这一次它出现在**验收清单项本身**上。

其余：S1 / S2 本阶段未动；S4–S6 未开工。

---

## 2. 停机次数、触发条件、平均恢复时长

**0 次** —— 因为**自动循环本阶段一趟都没跑**（§6.1）。`check_budget.py` 当前回
「24 小时内没有循环趟次记录」。人工打断有过，但那不是 §A9 说的停机。

---

## 3. 门禁误判

### 3.1 判 fail 但其实是对的：**0 次**

### 3.2 判 pass 但其实错了 —— **本阶段计 2 次**

**判定口径（先写死，再往下数）**：一条判据/门禁**绿着**，而它守的**不是**它名字说的那件事，
且**它是靠判据体系之外的手段被发现的** —— 计 1 次。
在合入前被本体系（独立审计/抽查）抓住并修掉的，**记录但不计次**，理由见下 ③。

| # | 事件 | 计次 | 根因在 |
|---|---|---|---|
| ① | `test_the_user_is_told_the_rich_text_lost_its_formatting` 名不副实（2026-08-27） | **计 1** | 执行产物 |
| ② | 阶段关口抽查（独立抽查者）：3 条**全部名副其实** | **0** | — |
| ③ | 独立收口审计撞见 4 条名不副实的绿 | **不计**（见下） | 执行产物 |
| ④ | `REF:ROADMAP-SPIKE1112` 证伪判据方向相反 | **不计**（跑之前发现） | 执行产物 |
| ⑤ | 🔴 **CI 门禁⑦ 红了两天没人看见** | **计 1** | 🔴 **引擎（判定面）** |

#### ① 已入账（`p2-views-roadmap.md`「既定入账项 ②」）

变异实测：换成与字段无关的死文案 → **18 条活体判据全绿**。已重写并复验见血。
⚠️ **那次抽查者是作者本人，违反 §7.2.1**，读者据此打折。

#### ② 阶段关口抽查 —— **这次满足了「抽查者不得是作者」**

抽样可复算（`tools/experiments/p2_spotcheck/draw_sample.py`，种子 `20260827` 写死，
候选池由 `git diff` **现算**、非硬编码 —— 独立抽查者自己核过这一点）。

| 判据 | 变异 | 判定 |
|---|---|---|
| `..._narrowed_out_entries_are_logged_not_silently_dropped` | WARNING 换 `pass` → 红；只报条数不列明细 → 红 | ✅ **比名字更严** |
| `..._schema_drift_sorts_and_deduplicates` | 去重不排序 → 红；排序不去重 → 红 | ✅ 两个动词各砍一刀都见血 |
| `..._a_view_with_zero_blocks_is_rejected` | 删掉「零块即错」→ 红 | ✅ 顺带证了拒绝来自校验器 |

**0 次误放行。** 它另记了三处精度缺口（都不构成名不副实，见材料 §3.2②）。

🔴 **它留下的一句话，本文认为是本阶段最有价值的方法论产出**：

> 只跑了本机 pytest，没跑 CI。考虑到本场已有「目录写进 COVERED 但没有 job 真跑它」的前科，
> **「这三条在 CI 里确实被执行」这一点我没有独立取证**。

⇒ **抽查制问「判据测的是不是它名字说的那件事」，却默认了那条判据会被跑。**
而本阶段恰好证明那个默认不成立（⑤）。**处置见 §7。**

#### ③ 独立收口审计撞见的 4 条 —— 记录，不计次

`..._derived_from_behaviour_not_self_declared`（写死 True 也全绿）·
plan 声称「⑦ 组含方向不变量」而那 5 条一条没碰 ·
`tests/render` 写进 CI `COVERED` 而无 job 跑它 ·
`schema.drift` 的「只报不删」（绕过记录器真发 `DROP COLUMN` 仍报 True）。

**四条全部已修且复验见血。**

⚠️ **「不计次」是本文的判断，不是规则原文，标为自判**：理由是它们**在合入主线前
就被本体系（独立审计）抓住**，没有一条像 ① 那样绿着好几天、靠人手动撞出来。
**反对这个判断的理由也写下来**：它们**全部出现在 plan 写着「已证明」的位置上**，
说明「自述已证明」这件事本身不可信 —— 而那正是误放行的实质。
**若人改判为计 4 次，本文的 §6.1 结论不变**（那一格的判据是「根因在引擎」，这四条都不在）。

#### ⑤ 🔴 唯一根因在引擎的一条

`gates.yml` 那条「没有测试目录被漏在 CI 之外」**自 2026-08-26 起就是红的**：

```
COVERED 最后一次设定  2026-08-24  b0ad632
tests/dsl    出现     2026-08-26（P2.1）← 从这天起就红
tests/i18n   出现     2026-08-27（P2.7）
tests/render 出现     2026-08-27（P2.2）
tests/agents 出现     2026-08-28（P2.3）
```

**它恰恰在做它被造出来要做的事，而它连着咬了四个工作项没人看见。**
根因：mission 的 `commands.test` 是 `check_expected_red.py && pytest tests/unit`，**不跑 CI**
⇒ **判定面漏了一整条流水线**。连带后果：**WBS §5 第 103 行那条 P2.3 验收命令，CI 上从来没跑过。**

已修（`1ac77c8` / `a752a42`，带 `Gates-Change-Approved-By:`）：三个离线目录接进 L1、
`tests/render` 真接进 live 那条线、ruff 作用域由八个补到十三个。
⚠️ **两笔 CI 改动都没有在真 GitHub Actions runner 上跑过** —— 本地验的是命令与判定逻辑。

---

## 4. 成本：本期消耗 vs 阈值

| | |
|---|---|
| 自动循环 | **0 趟** ⇒ 循环侧成本 0；`budget.json` 那道闸本阶段**没有判定对象** |
| 百炼（活体判据） | P2.3 两道题约 22.5k token（`qwen3.7-flash-2026-07-15`，reasoning 全程 0） |
| Claude 订阅（量化评测） | 记 **$7.212**，⚠️ **只含 Agent 侧**；判官那层当时没进总账，缺口估 $2.5 上下（**估算不是实测**，缺陷已修） |

🔴 **额度面的一条新事实，要进模型档案表**：`qwen3.8-2.4t-a95b` **拒绝 `enable_thinking:false`**
（HTTP 400 `restricted to True`）—— **满格额度那个模型用不了关思考**，
而关思考是本项目的预算前提（差 450 倍）。目前只记在 plan 与日志里。

---

## 5. 措辞审计（`00-GOALS.md` §5）

主动记下的自我修正 5 条（P2.0R 的置信下界、P2.3 数字的不可比、P2.4 的「推理不是重跑」、
CI 的「没在真 runner 上跑过」、P2.2 的「只对一个角色成立」）——详见材料 §5。

⚠️ **反例**：§3.2③ 那四条正是「措辞强于证据」的实例 —— plan 里写着「已证明」，
而那件事根本没被判据碰过。**措辞审计能自查出来的，恰恰不是这一类。**

---

## 6. 方法论续用复评（人 2026-08-28 当场签）

### 6.1 AGE / mission-driver：**停用**

**判据两格都够不着** —— 它们预设了循环在跑，而本阶段循环**一趟都没跑**：

```
_tmp/loop-usage.jsonl → p0-foundation 13 趟 · p1-insight 32 趟 · p2-views 0 趟
最后一趟 2026-08-26T17:55 · launchctl 里现在也没有 agenerp 的作业
```

人裁定按停用格的第二个分句判：**「全阶段没有任何工作项能不靠人接管跑完」** ——
本阶段 10 项全部由交互会话推完。

⚠️ **本文必须写清这个判定的弱点**：那句话的字面适用于「试了不行」，
而本阶段是「**没试**」。人在知情下仍判停用。

**并且它不是唯一理由**：§3.2⑤ 是本阶段唯一一条**根因在引擎**的误放行 ——
判定面漏了整条 CI，而那正是循环用来判「自己有没有把事做绿」的那把尺子。

**停用不等于废弃。重开条件（写死，不是「以后再说」）**：
① `commands.test` 的判定面**覆盖 CI**（或有等效机制能让「CI 红了」在循环内可见）；
② 且有 ≥1 个工作项在新判定面下**完整地由循环推完**（drafted → executed → 门禁绿 → 关闭）。
**两条同时成立才重开计时。**

### 6.2 LoopX：**停用**

判据的停用格逐字有一条「**跨会话恢复从未真正用上**（说明它只是多一层账）」，证据支持它：

- `.loopx/registry.json` 的 `updated_at` 停在 **2026-08-20**
- `.codex/goals/agenerp-goal/ACTIVE_GOAL_STATE.md` 最后修改 **2026-08-23**
- **本阶段两次跨会话接续走的都是 `docs/logs/2026/` 下的交接 markdown**

⚠️ **本文能证的边界**：只能证「**本仓本阶段**这两个文件没被写过、接续实际靠的是交接 markdown」，
**不能证它从来没在别处起过作用**。人在知情下仍判停用。

**重开条件**：跨会话恢复在本仓实际生效 ≥3 次，且与接替 mission-driver 的那套机制无「谁说了算」的冲突。

---

## 7. 下一期只做一件最重要的事

**把 S3 那四段串成一条跑一次。**

理由：§1 那四段各自成立，**整条链没跑过**，而 S3 就是 P2 的验收清单项。
形态：视图 Agent 生成一个视图 → 落成定制包 → `git diff` → `git revert` → 迁站点。

⚠️ **它卡在一笔已知欠账上**：视图定义现在**硬编码在 `agenerp/dsl/roles.py`**，
而那个模块头与 `agenerp/schema_snapshot.py` 的模块头**都逐字点名由 P2.4 了结** ——
**P2.4 没有了结它**（人 2026-08-28 裁定只交四步）。⇒ 要做这件事，先要那笔欠账。

### 同时建议改一条方法论（拟稿，`04-RUNBOOK.md` 在红线③内，**只能由人改**）

§7.2.1 的抽查制**加第二问**：

> 抽中的每条判据，除了答「它测的是不是它名字说的那件事」，还要答
> **「它在 CI 里真的被执行了吗？」**

理由是本阶段的实证：`tests/render` 的 18 条判据**成色是好的**（P2.2 那轮变异见过血），
但它在 CI 上**一次都没跑过**，而门禁⑩ 因为它被写进 `COVERED` 而转绿。
**一条没被执行的好判据，和一条名不副实的判据，在流水线上是同一个效果。**

---

## 8. P2 完成度审查（2026-08-28，按 WBS §5 的验收格逐条实跑）

| 项 | 验收 | 实跑 |
|---|---|---|
| P2.0 🚪 | 结论写进 `docs/architecture/` | ✅ `module-boundaries.md:1555` |
| P2.0R 🔴 | `tests/gates/test_schema_field_task_completion.py` | ✅ 5 passed |
| P2.1 | `pytest tests/dsl -q` | ✅ 51 passed |
| P2.2 | `pytest -m live tests/render -q` | ✅ 18 passed |
| P2.3 | `pytest tests/agents/test_view_agent.py -q` | ✅ 33 passed |
| P2.4 | `scripts/verify-gitops.sh` | ✅ 退出码 0 |
| P2.5 | `pytest tests/tools/test_schema_drift.py -q` | ✅ 15 passed |
| P2.6 | 🔴 `tests/gates/test_no_empty_workspace.py` | ✅ 3 passed |
| P2.7 | `pytest tests/i18n -q` | ✅ 13 passed |
| P2.8 | 纪要落 `docs/audits/` | ✅ **本文** |

审查中另发现并已收口：`2026-08-27-P2.0R-6-agent-navigates.md` 停在 `draft`、13 项未勾。
查明**不是没做完，是判据在执行途中被换掉**（人 2026-08-27 由 Top-5 改判为任务完成率 ≥95%，
D-28；P2.0R 随后按新判据由人判过，D-29）⇒ 已收为 **`superseded`**，
13 项逐条标注「随判据更换移出范围」，并记明残余风险：
**「结构化导航优先」今天的支撑是 D-29 的任务完成率，不是本 plan 设计的那次 A/B 对照。**

---

## 附 · 本阶段量化面

提交 173（`d18c05c..HEAD`）· 全量离线 **1572 passed / 38 skipped** ·
`check_expected_red.py` **29 项预期红 0 / 绿 29 / 跳过 0** ·
`origin/p2-views` **落后 66 个提交**（push 是对外发布，红线⑥，未做）
