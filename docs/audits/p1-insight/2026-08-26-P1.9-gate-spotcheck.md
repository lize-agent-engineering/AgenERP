# 2026-08-26 · P1.9 §7.2.1 判据抽查 —— 「它测的，是不是它名字说的那件事？」

> Plan: `docs/plans/p1-insight/2026-08-26-1835-1-gate-spotcheck-p1-9.md`
> Source: `docs/masterplan/STATE.md` §3 `[open] 2026-08-26T10:03Z`（人侧交办）· `docs/masterplan/04-RUNBOOK.md` §7.2.1
> Work Item: `docs/masterplan/02-WBS.md` §4 `P1.9` 行验收列逐字「**§7.2.1 的判据抽查整项交 loop**，见 STATE §3」
> 本文件只覆盖**被交出来的那一整项**。CP9 纪要本身、`P1.9` 的状态词、§1.4 的重判，三样都仍归人。

## 1 · 输入面（Phase 1）

### 1.1 基线 sha

- `<BASE>`（开工）：`git rev-parse HEAD` → `29a85dc324914cc9596377e0ea8304bf50c781ac`（exit 0）
- 开工时 `git status --porcelain` → **无输出**（工作树干净）
- `<HEAD>`（收尾）：见 §5.3

### 1.2 抽样复算（`P1-1`）

按 `STATE` §3 写死的算法自己重算一遍，**未采信人侧写下的结果**：

- 池 = `git log --since=2026-08-24 --name-only --format= -- tests/` 去重后**仍存在**的文件
- 排序键 = `sha256("P1.9-spotcheck-2026-08-26" + 路径)`，取前 3

实测结果：

| 名次 | 路径 | 键前 16 位 |
|---|---|---|
| 1 | `tests/gates/test_insight_rule_ablation.py` | `109097fcb5d13993` |
| 2 | `tests/unit/test_answer_judge.py` | `14152b051fa18a85` |
| 3 | `tests/unit/test_explain_cost_ledger.py` | `1b6ea32b59a19a5f` |
| 4 | `tests/unit/serve_fakes.py` | `1c308f8f26e627f3` |
| 5 | `tests/unit/test_desk_sidebar_static.py` | `1cdaf0321994670f` |

- **前三名与 `STATE` §3 写死的三条逐字相同** ⇒ `P1-2` 的「不吻合」分支**未触发**，抽查对象即 STATE 那三条。
- **池条数实测 44 条，人侧写的是 40 条。** 按 `P1-2` 末款：**只记不处置**（池随新增测试文件增长；
  排序键只依赖路径字符串，池增长不改变前三名 —— 本次实测第 4 名恒为 `tests/unit/serve_fakes.py`，与起草评审期一致）。
  **差异成因（照实记为待人处置）**：人侧实测时点早于本次，其后 `tests/` 下新增了文件。**本条不构成换样本的理由。**

### 1.3 三条的 git author（`P1-3`）

`git log --format='%h %an %ad %s' --date=short -- <path>` 实测：

| 文件 | 提交 | author |
|---|---|---|
| `tests/gates/test_insight_rule_ablation.py` | `913d515` 2026-08-25 `test(gates): 补齐 P1.4/P1.5/P1.7 的三个 🔴 验收件（人工批准）` | **`lize`** |
| `tests/unit/test_answer_judge.py` | `45bb8c3` 2026-08-25 · `1cbb44e` 2026-08-25 | `Email Agent Developer` |
| `tests/unit/test_explain_cost_ledger.py` | `7796ff7` 2026-08-24 | `Email Agent Developer` |

🔴 **与 `docs/audits/2026-08-26-CP9-P1-retrospective.md` §1.4 逐字那句「三条都不是人侧代理写的」对不上一条**：
`tests/gates/test_insight_rule_ablation.py` 的 git author 是 **`lize`**，与 §1.4 自己举的人侧代理例子（`f68ae19`）**同一个 author 名**。

⇒ **「抽样公平」那句话在这一条上不成立。** 本文件**只登记这个可观测事实**，
不改写 CP9 纪要、不推测意图、不代人重判 §1.4（plan Non-Goals 3/4）。
它反而**加强**了「交 loop」这个安排的正当性：三条里有一条是人侧写的，人自己抽它就是自己抽自己。
（登记为 plan 的 `D3`，`watch-only residual`。）

### 1.4 三条今天全绿（`P1-4` —— 抽查对象确实是 §7.2.1 逐字的「新增或改动过的绿判据」）

| 命令原文 | 退出码 | 摘要行原文 |
|---|---|---|
| `python3 tools/gates/check_expected_red.py` | **0** | `判定模式：default —— 按 tools/gates/expected-red.txt 判定` / `门禁 29 项：预期红 0，绿 29，跳过 0` / `✅ 与预期红名单完全一致` |
| `python3 -m pytest tests/gates/test_insight_rule_ablation.py -q` | **0** | `1 passed in 0.03s` |
| `python3 -m pytest tests/unit/test_answer_judge.py tests/unit/test_explain_cost_ledger.py -q` | **0** | `86 passed in 0.11s` |

三条今天**无一为红** ⇒ 抽查前提成立，`P1-4` 的「哪条今天就红当场交人」分支**未触发**。

### 1.5 附带登记（红线 5 内不可修，只记）

`docs/masterplan/02-WBS.md` `P1.9` 行（`:91`）被 `42fa183` 改成 **6 格**，而表头 `:76` 只有 **5 列**
⇒ 渲染时最后那个 `人` 会被吞。**这是可观测事实，改它归人。**

同一行的**状态源逐字仍是 `人`** ⇒ 本次交付**不领整行 `P1.9`**，只领同一行里人逐字交出来的那一整项。

## 2 · 方法与独立性（`D-1`）

### 2.1 选定 (A)：由一个独立子代理在全新会话里执行三条抽查

执行者（本会话）负责搭台、复算抽样、核对独立性、落盘，**不代它给三问的答案**。

**备选与否决理由（跑之前写死，不是事后补）**：

- **(B) 本会话直接读三份文件给答案。** **否决** —— §1.3 实测两条的 author 是 `Email Agent Developer`（= 本循环）。
  §7.2.1 逐字「作者读自己的断言，读到的是当初的意图，不是纸面上的字」，P1 那次误放行正是这个结构。
  **选它等于把要验的那条规矩当场违掉。**
- **(C) 交回人侧代理做。** **否决** —— 人 `[open] 10:03Z` 交出来的原因逐字就是「人侧不抽自己写的判据」，
  且 §1.3 实测 `test_insight_rule_ablation.py` 的 author 正是 `lize`。**交回去是原地循环。**
- **(D) 只做静态阅读，不做变异。** **否决** —— 三问的 ② 是「断言体**实际**验了什么」。
  只读得到的是「它看起来验了什么」，而 P1 那次误放行**恰恰是读不出来的**
  （`assert payload["answer"] or payload["accepted"] is False` 逐字摆在那儿，绿了好几天）。

**独立性怎么保证**：**不给 plan 全文，只给自足简报** —— 边界是「给判别标准与施加面，
**不给任何已有的预测与判词**」。简报原文见 §2.3，子代理返回原文见 §2.4。
⚠️ 独立性**不采信自报**（`docs/skills/closure-audit-prompt.md` 口径：不采信自报，逐条对活仓取证）——
一句「我不是作者」正是它禁止的形态，故此处贴的是 **prompt 原文 + 返回原文 + task id**，不是声明。

### 2.2 残余风险（照实登记，不修饰）

1. **独立子代理与本会话同属一个循环、同一个模型家族** ⇒ 「独立」只到**会话级与上下文级，不到实现级**。
   **本次抽查不是「完全独立的第三方审计」**，不得如此宣称。
2. `docs/masterplan/EXECUTION-PRINCIPLE.md:406` 逐字「子代理是**进程内**的……一个子代理挂住会让整个 step 子进程挂住」，
   且 `:79` 有「60min 无输出则杀」的看门狗 ⇒ **看门狗一杀，执行者与子代理一起死**，
   工作树里可能留着未复原的变异 —— 这正是变异协议默认改用**内存 monkeypatch**（工作树零字节改动）的直接原因。

### 2.3 交给子代理的 prompt 原文（逐字，不采信自报的替代物）

- **Agent tool**：`general-purpose`，fresh session
- **task / agent id**：`a0d5c65e472906df1`（ref `ae8261`）
- **给了什么**：三条路径 · §7.2.1 的三问 · 变异协议六款 · 承诺面锚点表整表（含三个第二锚点的路径与章节号）·
  三条各自的「重点盯」清单（含 `pytest.raises` 例外）· 三条的变异施加面与变异构造 · 判别标准与判词 · 硬约束 · 返回格式
- **没给什么**：plan 全文 · plan `B5` 那条起草期线索 · 起草期与两轮评审期的**任何**预测 / 实跑结果 / 判词留痕
  ⇒ 独立性由「**它收到了什么**」保证，不是由「它承诺不看什么」保证。

````markdown
# 任务：对三条判据做 §7.2.1 抽查（独立执行，不得采信任何他人结论）

你在仓库 `/Users/lize/Claude/Projects/AgenERP` 里工作。你是**独立抽查者**。
派你的那个会话**不会**把它的任何预测、任何已有实跑结果、任何判词交给你 —— 你收到的只有
判别标准与施加面。**结论必须由你自己实测得出。**

## 0 · 抽查对象（人侧写死的抽样结果，不许换、不许扩）

1. `tests/gates/test_insight_rule_ablation.py`
2. `tests/unit/test_answer_judge.py`
3. `tests/unit/test_explain_cost_ledger.py`

## 1 · §7.2.1 的三问（逐条回答，一条都不许省）

`docs/masterplan/04-RUNBOOK.md` §7.2.1 逐字：

> 从本阶段**新增或改动过的绿判据**里**随机抽 3 条**，逐条回答一个问题：
> **「它测的，是不是它名字说的那件事？」**
> **抽到一条名不副实的，即为一次误放行**，按原规则处置。

对每条给出：

- **①** 名字承诺了什么（三处锚点各抄原文，锚点见 §3）
- **②** 断言体**实际**验了什么 —— **必须由变异实测支撑，不许只靠读**
- **③** 两者有没有缺口，缺口**具体在哪一行**（给行号）

**参照实例（P1 那次真误放行长什么样）**：判据
`test_the_user_in_the_answer_is_the_person_the_real_sid_resolves_to` 的名字承诺
「答案里的人是真 sid 解析出的那个人」，而断言体里 `assert payload["answer"] or payload["accepted"] is False`
让**空答案照过** ⇒ 名副其实的那一半（`payload["user"] == expected`）真验了，
**「答案」那一半是空的**。它绿了好几天，**读是读不出来的**。

## 2 · 变异协议（`P2-0`，六款，先立规矩再动手）

**① 默认施加形态 = 内存 monkeypatch，不是改文件。**
在一个临时脚本里 `monkeypatch` / 直接改被导入模块的属性，再 `pytest.main([...])` 跑那份判据，
记退出码与红的**逐字断言名**。⚠️ **工作树零字节改动**。
（理由：本进程有 60min 无输出即杀的看门狗，**内存变异随进程一起消失，改文件不会**。）

**② 文件级施加只作兜底**（内存形态按构造做不到时），且必须：
施加前 `cp` 原字节到 `/tmp`（**不依赖 git**）→ `shasum -a 256` 记下 → 施加 → 跑 →
从 `/tmp` 复原 → `shasum -a 256` **必须同值** → 记 `RESTORED OK`。

**③ 施加面白名单**：`agenerp/**`。

**④ 黑名单（改一个字节即停机）**：
`tests/gates/**`（红线 1）· `.github/workflows/**`（红线 2）· `docs/masterplan/**`（红线 5）·
**`tests/` 下的任何夹具与断言体** —— 含 `tests/unit/answer_judge_fixture.py` 与
`tests/fixtures/p1_entry_gate_labels.jsonl`（它们**不在** `tests/gates/` 内，只有本条能拦住）。

**⑤ 复原失败分支（跑之前定死）**：若复原后 `sha256` 不同值，或 `git status --porcelain` 出现非预期条目 ⇒
**立即停手，不再动任何文件**，把现场原样报回。**不许「再试一次」。**

**⑥ 收尾**：`git status --porcelain -- agenerp/ tests/` **必须无输出**；
**不许用 `git checkout -- .` 复原**（会抹掉工作树里可能存在的人侧在飞改动，本仓 2026-08-26 已实测发生过两次）。

## 3 · 承诺面锚点表（`P2-A`，跑前定死，照它找 ①）

| 文件 | 承诺面（按 §5 的优先级） | ⚠️ 必须显式写明的事实 |
|---|---|---|
| `tests/gates/test_insight_rule_ablation.py` | 文件名 + docstring + **`docs/masterplan/02-WBS.md:85` 的 `P1.5` 行验收列** | —— |
| `tests/unit/test_answer_judge.py` | 文件名 + docstring + **`docs/plans/p1-insight/2026-08-25-0225-1-answer-judge-v0.md` §6 / §1.3** | `02-WBS.md:84` 的 `P1.4` 行验收列逐字**只有** `tests/gates/test_evidence_gate_blocks_single_hop.py`，**一字未提答案判定器** ⇒ **WBS 那一行钉的是另一个文件** |
| `tests/unit/test_explain_cost_ledger.py` | 文件名 + docstring + **`docs/plans/p1-insight/2026-08-24-2109-2-explain-cost-accounting.md` 与 `docs/plans/p1-insight/2026-08-25-0554-1-prompt-cache-accounting.md` 两份 plan** | `02-WBS.md:87` 的 `P1.7` 行那条 🔴 指的是 `tests/gates/test_explain_cost_accounting.py`，该文件 `:44` 加载的是 `test_explain_cost_accounting_body.py`，**不是**抽中的这一份 ⇒ **WBS 那一行钉的是另一个文件** |

🔴 **后两条必须在你的报告里显式写明「WBS 那一行钉的是另一个文件」** ——
否则会因为「WBS 上找不到承诺」而造出一个**假的 `名不副实`**。这是本次抽查最不能出的错。

## 4 · 三条各自的「重点盯」与变异施加面

### 4.1 `tests/gates/test_insight_rule_ablation.py`（`P2-1` + `P2-2`）

- **三问的 ②**：该文件是**薄加载器**，**必须追到 `tests/unit/test_inspection_rules.py` 里那个被导出的函数体**，
  逐行读它断了什么，并由变异坐实。
- ⚠️ 该文件**模块尾有一段自述**，说某些条目「管在别处」——
  **这是自报，按 `docs/skills/closure-audit-prompt.md` 的口径（不采信自报，逐条对活仓取证）不得采信**，
  必须由 §5 的 (ii) **实测确认**。
- **变异（至少两次，施加面 `agenerp/inspection/**`）**：
  - **(a)** 让规则**恒不命中**（判据表达求值恒 `False`）
  - **(b)** 让引擎**忽略清单**（恒命中）

### 4.2 `tests/unit/test_answer_judge.py`（`P2-3` + `P2-4`）

- 该文件 28 条，**逐条过一遍名字与断言体**（抽查抽的是文件，文件内要读全）。**重点盯三类**：
  - **(i)** 名字里有 `never` / `only` / `exactly` / `is a function of` 这类**强断言词**的，断言体撑不撑得住；
  - **(ii)** 断言里带 `or` 兜底的（上面那个参照实例正是一个 `or`）—— ⚠️ 已有
    `tests/gates/test_assertions_have_no_escape_hatch.py` 在守这一形态，**它绿不代表没有 `or`**
    （它允许带理由的 `# assert-escape-ok:` 豁免）⇒ **豁免逐条读，不采信「门禁绿了」**；
  - **(iii)** 名字说「离线 / 不读标注集」的，验的是不是**真的**没有写路径。
- **变异（至少两次，施加面 `agenerp/judging/**`）**：
  - **(a)** `judge_one` **忽略回包恒返一个标签**
  - **(b)** **未知标签静默归一**（不抛）

### 4.3 `tests/unit/test_explain_cost_ledger.py`（`P2-5` + `P2-6`）

- 该文件 21 条。**重点盯四类**：
  - **(i)** `reasoning` 那一半是不是**真的独立可红**（`P1.7` 逐字要能挡住「只记 completion 不记 reasoning」）；
  - **(ii)** `cached` 进没进 `total`（roadmap 逐字「`cached` 不进 `total`」）；
  - **(iii)** **成本判据与失控闸判据有没有被合并**（决策 `D-18` 逐字「两者的判据**分开写，不许合并**」）；
  - **(iv)** 🆕 **`pytest.raises` 型反测要单独标出来**：`:488` 的
    `test_h3_dropping_reasoning_turns_the_real_replay_red` 整体是 `with pytest.raises(AssertionError)`
    ⇒ **产品侧「也丢 reasoning」的变异只会让它更绿**。这类条目**按构造不可能被产品变异打红**，
    它们的有效性要**另找证法**，🔴 **不许因为「变异没打红」就判它名不副实**。
- **变异（至少两次）**：
  - **(a)** 把 `reasoning` **折进 `completion`**（`P1.7` 逐字点名的那种假实现）——
    🔴 **施加面是 `agenerp/routing/adapter.py:226` 的 `usage_of`（或 `agenerp/explain/ledger.py` 的端点取数），
    不是 `agenerp/explain/**` 里凭文件名直觉找的地方。**
  - **(b)** 让某一条**失败路径不落账**（账本少一条）。

## 5 · 判别标准与判词（`P2-7`，跑前写死）

三条各给**一个**明确判词：`名副其实` / `名副其实但有已声明的边界` / **`名不副实`**。

🔴 **承诺面冲突时的优先级**：**`docs/masterplan/02-WBS.md` 的验收列 > 文件名 / docstring**。
文件名与 docstring 只能**说明边界如何被声明**，**不得缩小 WBS 已经写下的承诺**。
（⚠️ 后两条文件的 WBS 行钉的是别的文件，见 §3 —— 那时承诺面**退到该表列的第二锚点**。）

🔴 **判「边界」必须同时满足两条，缺一条即判 `名不副实`**：

- **(i)** 边界在文件里**显式声明**了；
- **(ii)** 被排除的那一半**经实测确认真在别处跑到** —— **实测，不是采信文件自述**。
  （本例可查：`.github/workflows/gates.yml:587` 的「单测与契约测试」job `:604` 逐字
  `python3 -m pytest tests/unit -q`。**必须由你自己复跑确认，不得照抄本行。**）

⇒ **「什么都判成边界」的口子由 (ii) 堵死。**

**抽到一条名不副实的 ⇒ 记为一次误放行**（§7.2 原规则），并逐字点明
「CP9 §1.4 的『再出现一条名不副实』条件**已触发**，**重判归人**」。

## 6 · 硬约束（违反即本次抽查作废）

1. **每一次变异，预测必须在跑之前逐字写下来**，跑完照实记「吻合 / 不吻合」。
   **不许事后改预测。** 不吻合是有信息的结果，不是失败。
2. **不修任何你查出来的缺口。** 抽查是审计，不是修理。
   （若你在 `agenerp/**` 里撞见过期注释之类的东西，**点名，不许顺手改**。）
3. **不改任何 `tests/` 下的文件、不改 `docs/masterplan/**`、不改 `.github/workflows/**`。**
4. **不写 git commit、不改 plan、不改任何 `docs/audits/` 文件。** 你只把结果**返回**给派你的会话。
5. **不采信任何自报**（含被测文件自己的注释、docstring 里的「这在别处跑到」）。

## 7 · 返回格式

返回一份 Markdown，含：

- **每条一节**，节内：① 名字承诺（三锚点原文）· ② 断言体实际验了什么（**带变异实测**）· ③ 缺口在哪一行 · **判词**
- **每次变异一行表格**：施加形态（内存 / 文件兜底）· 施加内容 · **跑前预测原文** · 实际（**退出码 + 红的逐字断言名**）· 吻合/不吻合
  （文件兜底形态另加 `RESTORED OK` + `sha256` 同值）
- **结论一句话**：「抽中的三条里，名不副实 **N** 条」
- **收尾自证**：`git status --porcelain -- agenerp/ tests/` 的输出（应为空）+
  `python3 tools/gates/check_expected_red.py` 的退出码
````

### 2.4 子代理返回的报告（**二次落盘版** —— 原始返回已在传输层截断，不可逐字比对）

⚠️ **照实说清这一节的证据强度**：第一次返回在传输层被截断，执行者要求它**原样落盘不重跑不改字**
至 `/tmp/spotcheck_report.md`（162 行），下文即该文件全文。
🔴 **「内容一字未改」这句本身是子代理的自报**，与本节标榜的「不采信自报」口径同形态 ——
**原始返回已截断，按构造不可逐字比对**，此处不冒充「逐字原文」。
子代理自述的唯一差异：`C2` 行「`6 failed, 21 passed`」在被截断的那次输出里意外插了换行，落盘版恢复为单元格内连续文本。
⚠️ **补偿证据**：本节所有承重读数**都不靠这一节成立** —— 执行者已独立复跑 A1 / B3（§3.4），
收口审计者另独立复跑了 A1 / B3 / B1 / C1 / C3（§6），**三方结果逐字一致**。

````markdown
# §7.2.1 判据抽查 · 独立抽查报告（2026-08-26）

抽查者未读派工会话的任何预测/结论；未读工作树里那份预先存在的未跟踪文件 `docs/audits/p1-insight/2026-08-26-P1.9-gate-spotcheck.md`。

**基线（本机实读）**：`python3 -m pytest tests/gates/test_insight_rule_ablation.py tests/unit/test_answer_judge.py tests/unit/test_explain_cost_ledger.py -q` → **87 passed**，exit 0。收集数：门禁 **1** · answer_judge **59**（28 个 def）· cost_ledger **27**（21 个 def）。

**§5 (ii) 的公共前提，由我自己复跑确认**：`.github/workflows/gates.yml:604` 逐字 `python3 -m pytest tests/unit -q`；我实跑 → **839 passed, 17 skipped**，exit 0。`--collect-only` 实测确认其中含 `test_inspection_rules.py::test_h2_*`（4）· `test_prompt_cache_accounting.py`（12）· `test_explain_runaway_guard.py`（11）。

---

## 一 · `tests/gates/test_insight_rule_ablation.py` → **名不副实**

### ① 名字承诺（三锚点原文）

| 锚点 | 原文 |
|---|---|
| 文件名 | `test_insight_rule_ablation.py` —— **ablation（消融）** |
| docstring `:1` | 「🔴 P1.5 门禁 · 洞察消融：**抽掉规则就查不出**」 |
| `docs/masterplan/02-WBS.md:85`（P1.5 行验收列） | 「🔴 `tests/gates/test_insight_rule_ablation.py`（**消融：抽掉规则则查不出**）+ 🔴 巡检器在**零 LLM 调用**下跑通固定测例 —— 后者不绿即说明规则被塞进了提示词」 |

### ② 断言体实际验了什么（追到函数体 + 变异坐实）

薄加载器，`:44` 加载 `tests/unit/test_inspection_rules.py`，`:47` **只导出一个名字**（collected = 1）。追到 `tests/unit/test_inspection_rules.py:76-91` 的函数体，三条断言**全部在否定侧**：`:86` `ablated == ()` · `:87` 抽掉后零命中 · `:91` 判据表达改严后零命中。**对照臂（完整清单 → 报出一条、且那个数是算出来的）不在导出清单里。**

`tests/gates/**` 13 个文件里，**只有这一个**提到 `inspect_site` / `agenerp.inspection` —— 门禁面上没有第二处兜底。

### ③ 缺口在哪一行

**`tests/gates/test_insight_rule_ablation.py:47`** —— 导出清单只有一行，消融的阳性对照臂 `test_h1_the_full_rule_list_reports_the_finished_goods_backlog`（`tests/unit/test_inspection_rules.py:62`）没被导出。

**边界两条件（§5）**：
- **(i) 不满足。** 文件 `:49-55` 的自述**只声明了「零 LLM 调用那两条」不导出**，对**消融的阳性对照臂**一个字未提。
- （(ii) 我仍实测了自述那一半：`test_h2_*` 4 条确在 `pytest tests/unit -q` 里跑到并通过 —— 自述属实，但它管的不是缺口所在的那一半。）

⇒ 缺 (i) ⇒ **`名不副实`**。形状与 P1 那个 `or` 参照实例同族：**名副其实的那一半（否定臂）真验了，「消融」赖以成立的对照臂是空的**，且空掉的恰好是「在假实现下恒真」的那一侧。

### 变异记录

| # | 施加形态 | 施加内容 | 跑前预测（原文） | 实际 | 吻合 |
|---|---|---|---|---|---|
| A1 | 内存 monkeypatch `agenerp.inspection.engine._fires` | 恒 `False`（规则恒不命中） | 「门禁 **exit 0、1 passed、零红**。理由：门禁只导出否定侧，引擎死掉时「零命中」恒真」 | **exit 0**，`1 passed`，零红 | **吻合** |
| A1-伴 | 同上，靶子换成 `tests/unit/test_inspection_rules.py` | 同上 | 「exit 1，红：`test_h1_the_full_rule_list_reports_the_finished_goods_backlog`、`test_h1_the_reported_quantity_moves_with_the_dataset`、`test_every_shipped_rule_passes_its_own_test_case`、`test_h2_the_whole_inspection_runs_with_the_model_face_booby_trapped`、`test_the_hit_record_is_diffable`」 | **exit 1**，`5 failed, 11 passed`，红名逐字与预测**完全相同** | **吻合** |
| A2 | 内存 monkeypatch `engine._fires` | 恒 `True`（忽略判据表达） | 「exit 1，红：`test_h1_ablation_removing_that_rule_yields_zero_hits`」 | **exit 1**，红 `test_h1_ablation_removing_that_rule_yields_zero_hits`（断在 `test_inspection_rules.py:91`「判据表达没被读」） | **吻合** |
| A3 | 内存 monkeypatch `engine.run` + `agenerp.inspection.run` | 忽略传入清单，永远跑 `minimal_rules()`（引擎忽略清单） | 「exit 1，红：`test_h1_ablation_removing_that_rule_yields_zero_hits`」 | **exit 1**，同名红（断在 `:87`） | **吻合** |

**关键读数**：A2/A3 能打红 ⇒ 「忽略判据表达」「忽略清单」两条路有覆盖；**A1 打不红** ⇒ 「引擎彻底失去发现力」这条路在门禁面上**完全没有防线**，而它同时让门禁的每一条断言恒真。

---

## 二 · `tests/unit/test_answer_judge.py` → **名不副实**

### ① 名字承诺（三锚点原文）

| 锚点 | 原文 |
|---|---|
| 文件名 | `test_answer_judge.py` |
| docstring `:1-9` | 「答案判定器 v0 的**离线判据**：全部走假 transport，**零网络、零凭据**。…本文件判的是**判定器这个装置的结构性质**：它只吃一段文本、**它的标签是模型回包的函数**、它一次调用一条账、**它不回写集子**。」 |
| plan `2026-08-25-0225-1-answer-judge-v0.md` §6 H7 | 「**判定器拿不到答案，且标签必须由模型定**（结构判据，**本 plan 的主证**）」；§1.3 逐字：「**「判定器不是关键词正则」不能靠实测口径证明，只能靠结构判据证明**」 |

🔴 **`02-WBS.md:84` 的 P1.4 行验收列逐字只有 `tests/gates/test_evidence_gate_blocks_single_hop.py`，一字未提答案判定器 ⇒ WBS 那一行钉的是另一个文件。** 承诺面因此退到上表第二/第三锚点（文件名 + docstring + plan §6/§1.3），**不是**因为「WBS 上找不到承诺」而判它无承诺。

### ② 断言体实际验了什么（28 条逐条过，重点盯三类 + 变异坐实）

- **(i) 强断言词**：`exactly` 三处（`:61` 四个假实现集合相等、`:96` 两基线+两口径都在、`:351` 标签恰好三个）—— 断言体都是集合/元组相等，**撑得住**。`is a function of`（`:205`）由 B1 坐实撑得住。**`never` 两处撑不住**，见 ③。
- **(ii) `or` 兜底**：`grep 'assert .*\bor\b'` 在这三个文件里**零命中**；全仓 `tests/` 下 `assert-escape-ok:` 豁免**零条**（只有门禁文件自己的 docstring/常量提到这个串）。⇒ 这一形态本文件干净，且我**没有**靠「门禁绿了」下结论。
- **(iii) 「离线 / 不读标注集」**：`:284` 与 `:305` 两条结构判据 + `:295` 的**行为面**判据（真跑 24 行后集子指纹 == 模块导入那一刻的 `PRISTINE_SHA256`）。行为面那条是硬的；结构面两条偏窄（见 ③）。

### ③ 缺口在哪一行

- **`tests/unit/test_answer_judge.py:275-279`**（`test_m6_the_judging_package_never_constructs_a_chat_adapter_itself`，`:270`）—— 名字里的 **`never`** 是全称否定，docstring 还把承诺加强成「F8（`ChatAdapter` 可被直接构造绕过能力校验）那条路**不在本包重现**」。断言体只覆盖**两种语法形态**：`ast.Call` 且 `func` 是 `ast.Name`（`:275-276`）、`ast.ImportFrom`（`:277-279`）。**属性式构造**（`import agenerp.routing.adapter as _a` → `_a.ChatAdapter(...)`）与裸 `ast.Import` 都在覆盖之外。B3 实测：注入后该条**仍绿**。
- **`tests/unit/test_answer_judge.py:310`**（`test_m7_neither_the_judging_package_nor_the_helper_has_a_write_path`，`:305`）—— 名字承诺「没有写路径」，断言体只认 `ast.Attribute` 的 `attr in {"write_text","write_bytes"}`。B3 实测：注入 `open(p, "w").write("x")` 后**仍绿**。

**边界两条件**：**(i) 不满足** —— 文件内、plan §6 H7、plan §7 Phase 4 的 M6 措辞（「本包一律走 `route()`，不直接构造 `ChatAdapter`」）**都没有**声明「只查 `ast.Name` 形态 / 只查两个属性名」这个边界；docstring 反而把承诺写得更满。⇒ 缺 (i) ⇒ **`名不副实`**。

**照实说清强度**：这两条是文件里 28 条中的 2 条；docstring 四条承诺里的前三条（只吃一段文本 / 标签是回包的函数 / 一次调用一条账）由签名级判据与 B1/B2 变异实测**坐实成立**，「不回写集子」还有 `:295` 的行为面判据兜底。**但 `:270` 那条 `never` 无任何兜底** —— 它是全仓唯一守 F8 那条路在本包不重现的东西，而绕过写法是 Python 里完全常规的形态。按参照实例的口径（「`payload["user"]` 那一半真验了，「答案」那一半是空的」），此处同构：「`ast.Name` 形态」那一半真验了，**「属性形态」那一半是空的**。

### 变异记录

| # | 施加形态 | 施加内容 | 跑前预测（原文） | 实际 | 吻合 |
|---|---|---|---|---|---|
| B1 | 内存 monkeypatch `agenerp.judging.rubric.parse_label` + `agenerp.judging.judge.parse_label` | 恒返 `"correct"`（`judge_one` 忽略回包恒返一个标签） | 「exit 1；红 = `test_h7_3_the_label_is_a_function_of_the_model_reply`、`test_h7_3_the_same_answer_can_be_judged_truncated_when_the_reply_says_so`、`test_an_unknown_label_is_named_and_raised_not_silently_normalised` ×2、`test_a_reply_without_a_parseable_label_object_is_raised_too` ×2 ⇒ 6 红」 | **exit 1**，`6 failed, 53 passed`，红名逐字**完全相同** | **吻合** |
| B2 | 同上 | 未知标签静默归一为 `incomplete`（合法标签照常解析，空/不成形照常抛） | 「exit 1；红 = `test_an_unknown_label_is_named_and_raised_not_silently_normalised` ×2 ⇒ 2 红；`h7_3` 仍绿」 | **exit 1**，`2 failed, 57 passed`，红名逐字**完全相同**，`h7_3` 两条绿 | **吻合** |
| B3 | 内存 monkeypatch `pathlib.Path.read_text`（只对 `agenerp/judging/*.py` 生效，工作树零字节改动） | 往源码文本注入三样：① `open(p,"w").write("x")` 写路径 ② `_a.ChatAdapter(models[0], config)` 属性式构造 ③ 字面量 `"tests/fixtures/p1_entry_gate_labels.jsonl"` | 「exit 1；**只有** `test_no_product_module_in_the_judging_package_reads_the_annotation_set` 变红；`test_m7_neither_the_judging_package_nor_the_helper_has_a_write_path` 与 `test_m6_the_judging_package_never_constructs_a_chat_adapter_itself` **仍绿**」 | **exit 1**，`1 failed, 58 passed`，红的**只有** `test_no_product_module_in_the_judging_package_reads_the_annotation_set`；m6 / m7 **仍绿** | **吻合** |

**B3 的阳性对照说明**：③ 打红证明注入确实抵达了 AST/文本扫描面 ⇒ m6 / m7 的绿**不是**注入没生效，是判据真的漏了。

---

## 三 · `tests/unit/test_explain_cost_ledger.py` → **名副其实但有已声明的边界**

### ① 名字承诺（三锚点原文）

| 锚点 | 原文 |
|---|---|
| 文件名 | `test_explain_cost_ledger.py` |
| docstring `:1-8` | 「P1.7 **成本记账**那一组判据（D-18）—— 判的是「**每次模型调用的账都在，且对得上端点自报的数**」。⚠️ **失控闸的判据不在这里**，在 `tests/unit/test_explain_runaway_guard.py`。D-18 逐字「两者的判据分开写，不许合并」…**本文件不判任何阈值**」 |
| plan `2026-08-24-2109-2-explain-cost-accounting.md:303` / `:496-497` | 「成本判据落 `tests/unit/test_explain_cost_ledger.py`，**互不 import 对方文件里的夹具**」 |
| plan `2026-08-25-0554-1-prompt-cache-accounting.md` Non-Goals 8（`:236-239`） | 「…`cached` 不在那一行的验收内。**`cached` 的「缺项即红」由本 plan 的 `tests/unit/test_prompt_cache_accounting.py` ① 与 ⑤ 承担**」 |

🔴 **`02-WBS.md:87` 的 P1.7 行那条 🔴 指的是 `tests/gates/test_explain_cost_accounting.py`；我实读该门禁 `:44` 加载的是 `tests/unit/test_explain_cost_accounting_body.py`（`:46-54` 导出 9 个名字），**不是**抽中的这一份 ⇒ WBS 那一行钉的是另一个文件。** 承诺面因此退到上表第二/第三/第四锚点。

### ② 断言体实际验了什么（21 条，四类重点盯 + 变异坐实）

- **(i) `reasoning` 那一半真的独立可红。** C1（把 `reasoning` 折进 `completion`，施加面就是 §4.3 点名的 `agenerp/routing/adapter.py:226` 的 `usage_of` 端点取数）打红 **9** 条，覆盖重放（`:398`）、完整链路（`:422`）、逐条对账（`:561`）、错误路径（`:187` / `:541` / `:628`）、洞察路径（`:288`）、汇总（`:608`）。⇒ P1.7 逐字「判据要能挡住「只记 completion 不记 reasoning」这种假实现」**实测成立**。
- **(ii) `cached` 进没进 `total`。** 本文件里 `cached` **零出现**。C3（`usage_of` 丢掉 `cached`）→ 本文件 **exit 0、27 passed、零红**；同一变异下 `tests/unit/test_prompt_cache_accounting.py` **4 红**（含 `test_as_dict_is_exactly_this_dictionary_cached_never_enters_total` 所在文件的解析/账本/汇总三层）。⇒ **这一半确实在别处、且实测跑得到**，并已由 `pytest tests/unit -q` 覆盖（12 条）。边界的声明在**承诺面的第三锚点**（prompt-cache plan Non-Goals 8）里逐字写着，不在本文件 docstring 里 —— 这一点我照实登记（见 ③）。
- **(iii) 成本判据与失控闸有没有被合并。** 未合并：docstring `:3-5` 显式声明；`tests/unit/test_explain_runaway_guard.py` **11 条**经我 `--collect-only` + 全量跑实测确认在 `pytest tests/unit -q` 里跑到并通过；该文件内另有 `test_the_two_criteria_groups_do_not_import_each_other` 钉住「互不 import」。⇒ (i)(ii) 均满足，D-18 逐字要求成立。
- **(iv) `pytest.raises` 型反测。** `:488` `test_h3_dropping_reasoning_turns_the_real_replay_red` 整体是 `with pytest.raises(AssertionError)`；C1 下**如期仍绿**（`test_h3_a_proportional_...` 同）。**按 §4.3 我不因此判它名不副实。** 它的另一种证法**就在同文件**：`:505` `test_h3_the_real_replay_is_green_without_the_fake`，逐字「反测的另一半：**不加假实现时这两条判定体是绿的** —— 否则 `pytest.raises` 只是在证明「判定体永远红」」。而 `:505` 在 C1 下**确实在我的 9 条红名单里** ⇒ 这个「另一半」是真活的、真可红的，`488 + 505` 这一对成立。

### ③ 缺口在哪一行

- **无构成名不副实的缺口。** 两处照实登记，都不构成：
  - **`:1-9`（docstring）** —— 它声明了失控闸的边界，**没有**声明 `cached` 的边界。`cached` 的边界声明落在承诺面的第三锚点（`2026-08-25-0554-1-prompt-cache-accounting.md:236-239`）而非文件内。**两种读法我都摆出来**：严格按 §5 (i)「边界在**文件里**显式声明」算则此处欠一句；按 §3 锚点表（该 plan 本身**就是**本文件的承诺面之一，且它逐字把 `cached` 划给另一个文件）算则 `cached` 从来不在本文件承诺面内。我采后者 —— 因为把承诺面的一部分同时当作「未声明」是自相矛盾的。(ii) 无论哪种读法都由 C3 实测满足。
  - **`:453` `test_h2b_a_reply_without_reasoning_details_is_not_folded_into_completion`** 在 C1 下**仍绿**。但它名字带限定语「a reply **without reasoning details**」，测的正是「端点根本没回 `completion_tokens_details`」那一支，**名字诚实**，不是缺口。真正挡住折叠的是 `:398` / `:422` / `:561` / `:608`，C1 已实测打红。

⇒ **`名副其实但有已声明的边界`**（边界 = 失控闸在 `test_explain_runaway_guard.py`、`cached` 在 `test_prompt_cache_accounting.py`；两者均经我实测确认在 CI 的「单测与契约测试」job 里真跑到）。

### 变异记录

| # | 施加形态 | 施加内容 | 跑前预测（原文） | 实际 | 吻合 |
|---|---|---|---|---|---|
| C1 | 内存 monkeypatch `agenerp.routing.adapter.usage_of` + `agenerp.explain.ledger.usage_of` | `Usage(prompt, completion+reasoning, 0, cached)`（把 `reasoning` 折进 `completion`） | 「exit 1；红 = `test_h1_1b_the_empty_answer_path_keeps_the_real_tokens`、`test_the_ledger_also_covers_the_path_that_comes_in_through_insight`、`test_h2a_the_seven_real_calls_replay_entry_by_entry`、`test_h2b_a_raw_endpoint_body_survives_the_whole_chain`、`test_h3_the_real_replay_is_green_without_the_fake`、`test_m8_every_entry_carries_the_usage_of_that_very_reply`、`test_the_ledger_never_blocks_anything`、`test_d2_on_the_error_path_the_ledger_is_strictly_larger_than_the_session`、`test_d1_a_routing_error_carrying_usage_lands_on_the_ledger_as_real_numbers` ⇒ 9 红；`test_h3_dropping_reasoning_turns_the_real_replay_red` 与 `test_h3_a_proportional_...` 仍绿」 | **exit 1**，`9 failed, 18 passed`，红名集合与预测**逐字相同**（顺序不同）；两条 `pytest.raises` 型如期仍绿 | **吻合** |
| C2 | 内存 monkeypatch `agenerp.explain.ledger.CallLedger.record_error` | 照常构造 `CallEntry` 但**不 `append`**（失败路径不落账） | 「exit 1；红 = `test_h1_1a_...`、`test_h1_1b_...`、`test_the_call_count_equals_the_ledger_length_on_every_exit[model-error…]` ×2、`test_d2_on_the_error_path_...`、`test_d1_...` ⇒ 6 红」 | **exit 1**，`6 failed, 21 passed`，红名集合与预测**逐字相同** | **吻合** |
| C3 | 内存 monkeypatch 同 C1 两处 | `Usage(prompt, completion, reasoning, 0)`（丢掉 `cached`） | 「`test_explain_cost_ledger.py` **exit 0、21 passed、零红**；同一变异下 `tests/unit/test_prompt_cache_accounting.py` **变红**」 | **exit 0、零红**，但 `27 passed`（不是 21）；`test_prompt_cache_accounting.py` **exit 1、4 failed, 8 passed**，红 = `test_cached_tokens_are_parsed_from_prompt_tokens_details`、`test_the_ledger_records_both_the_parsed_and_the_endpoint_reported_cached`、`test_the_ledger_rollup_is_exactly_this_dictionary`、`test_a_huge_cached_count_still_does_not_block_the_answer` | **部分不吻合**：退出码与「零红 / 别处变红」两个承重预测**吻合**；`21 passed` 那个数**不吻合** —— 21 是 `def test_` 的**函数个数**，实际收集数 **27**（`@pytest.mark.parametrize` 展开 5+3 条），我跑前把两者混为一谈。**预测原文不改。** |

**所有变异均为内存 monkeypatch，无一次文件兜底 ⇒ 无 `RESTORED OK` / `sha256` 条目需要登记。工作树零字节改动。**

---

## 结论

> **抽中的三条里，名不副实 2 条。**

- `tests/gates/test_insight_rule_ablation.py` —— **名不副实**（消融的对照臂不在门禁面上，缺口在 `:47`；A1 实测：引擎恒不命中时门禁 exit 0 零红，而同一变异下开发期那份 5 红）
- `tests/unit/test_answer_judge.py` —— **名不副实**（`:270` 的 `never` 只覆盖 `ast.Name` 形态，属性式构造绕过后 B3 实测仍绿；同族缺口在 `:310`）
- `tests/unit/test_explain_cost_ledger.py` —— **名副其实但有已声明的边界**

### 处置（§7.2 原规则）

抽到名不副实 ⇒ 记为误放行，**本次为 2 次**。

🔴 **`CP9 §1.4` 的「再出现一条名不副实」条件已触发，重判归人。**

（依 §6-2：**我一处缺口都没有修**。`agenerp/**` 里未见需点名的过期注释。）

---

## 收尾自证

```
$ git status --porcelain -- agenerp/ tests/
                       ← 无输出
$ git status --porcelain
?? docs/audits/p1-insight/2026-08-26-P1.9-gate-spotcheck.md
   ← 会话开始前就已存在的未跟踪文件，非我所建、未读、未动
$ python3 tools/gates/check_expected_red.py
判定模式：default —— 按 tools/gates/expected-red.txt 判定
门禁 29 项：预期红 0，绿 29，跳过 0
✅ 与预期红名单完全一致
EXIT=0
```

未使用 `git checkout -- .`。所有变异脚本落在 `/tmp/mut_a.py` `/tmp/mut_b.py` `/tmp/mut_c.py`，仓内零新增文件。
````

## 3 · 三条逐条（Phase 2 —— 答案由 §2.4 的独立子代理给出；执行者只复跑承重结论）

⚠️ **本节的 ①②③ 与判词全部出自 §2.4 的独立子代理**，执行者未代它作答。
执行者另做的事只有一件：**把两条 `名不副实` 赖以成立的承重变异原样复跑一遍**（`裁判规则 3` 复跑优先）——
复跑结果见 §3.4，与子代理逐字一致。

### 3.1 `tests/gates/test_insight_rule_ablation.py` → 🔴 **名不副实**

- **① 名字承诺**：文件名 `ablation`（消融）· docstring `:1`「🔴 P1.5 门禁 · 洞察消融：**抽掉规则就查不出**」·
  `02-WBS.md:85` P1.5 行验收列「🔴 `tests/gates/test_insight_rule_ablation.py`（**消融：抽掉规则则查不出**）」
- **② 实际验了什么**：薄加载器 `:44` 加载 `tests/unit/test_inspection_rules.py`，`:47` 只导出一个名字（collected = 1）。
  函数体 `tests/unit/test_inspection_rules.py:76-91` 三条断言**全部在否定侧**（`:86` `ablated == ()` ·
  `:87` 抽掉后零命中 · `:91` 判据表达改严后零命中）。**阳性对照臂不在门禁面上。**
  `tests/gates/**` 13 个文件里只有这一个提到 `agenerp.inspection` ⇒ 门禁面上没有第二处兜底。
- **③ 缺口**：**`tests/gates/test_insight_rule_ablation.py:47`** —— 阳性对照臂
  `test_h1_the_full_rule_list_reports_the_finished_goods_backlog`（`tests/unit/test_inspection_rules.py:62`）没被导出。
- **不构成「已声明的边界」**：`P2-7`(i) **不满足** —— 该文件 `:49-55` 的自述**只声明了「零 LLM 调用那两条」不导出**，
  对**消融的阳性对照臂**一个字未提。（(ii) 子代理仍实测了自述那一半：`test_h2_*` 4 条确在
  `pytest tests/unit -q` 里跑到并通过 —— **自述属实，但它管的不是缺口所在的那一半**。）
- **形状**：与 P1 那个 `or` 参照实例**同族** —— 名副其实的那一半（否定臂）真验了，
  「消融」赖以成立的对照臂是空的，**且空掉的恰好是「在假实现下恒真」的那一侧**。

**变异**：A1（`_fires` 恒 `False`）⇒ 门禁 **exit 0、1 passed、零红**；
同一变异下开发期那份 **exit 1、5 failed**。A2（`_fires` 恒 `True`）· A3（引擎忽略清单）⇒ 均 exit 1，
红 `test_h1_ablation_removing_that_rule_yields_zero_hits`。**四次变异预测全部吻合**（逐字表见 §2.4）。

### 3.2 `tests/unit/test_answer_judge.py` → 🔴 **名不副实**

- **① 名字承诺**：文件名 + docstring `:1-9`（「它只吃一段文本、**它的标签是模型回包的函数**、
  它一次调用一条账、**它不回写集子**」）+ plan `2026-08-25-0225-1-answer-judge-v0.md` §6 H7 / §1.3。
  🔴 **`02-WBS.md:84` 的 P1.4 行验收列逐字只有 `tests/gates/test_evidence_gate_blocks_single_hop.py`，
  一字未提答案判定器 ⇒ WBS 那一行钉的是另一个文件**，承诺面因此退到第二/第三锚点，
  **不是**因为「WBS 上找不到承诺」而判它无承诺（`P2-A` 要求的显式声明，已写明）。
- **② 实际验了什么**：28 条逐条过。强断言词 `exactly` 三处、`is a function of` 一处**都撑得住**；
  `or` 兜底在本文件**零命中**，全仓 `tests/` 下 `assert-escape-ok:` 豁免**零条**
  （子代理逐条读了，**未采信「门禁绿了」**）；「不回写集子」有 `:295` 的**行为面**判据兜底（真跑 24 行比指纹），是硬的。
  **`never` 两处撑不住**，见 ③。
- **③ 缺口（两行）**：
  - **`tests/unit/test_answer_judge.py:275-279`**（`test_m6_..._never_constructs_a_chat_adapter_itself`，def 在 `:270`）
    —— 名字里的 **`never`** 是全称否定，docstring 还把承诺加强成「F8 那条路**不在本包重现**」；
    断言体只覆盖 `ast.Call` 且 `func` 是 `ast.Name`（`:275`）与 `ast.ImportFrom`（`:277`）两种语法形态。
    **属性式构造**（`import agenerp.routing.adapter as _a` → `_a.ChatAdapter(...)`）与裸 `ast.Import` 在覆盖之外。
  - **`tests/unit/test_answer_judge.py:310`**（`test_m7_neither_..._has_a_write_path`，def 在 `:305`）
    —— 名字承诺「没有写路径」，断言体只认 `ast.Attribute` 的 `attr in {"write_text","write_bytes"}`，
    `open(p, "w").write("x")` 在覆盖之外。
- **不构成「已声明的边界」**：`P2-7`(i) **不满足** —— 文件内、plan §6 H7、plan §7 Phase 4 的 M6 措辞
  **都没有**声明「只查 `ast.Name` 形态 / 只查两个属性名」这个边界；**docstring 反而把承诺写得更满**。
- **强度照实说**：这是 28 条里的 2 条；docstring 四条承诺里前三条由签名级判据 + B1/B2 变异实测**坐实成立**。
  **但 `:270` 那条 `never` 无任何兜底** —— 它是全仓唯一守「F8 那条路在本包不重现」的东西，
  而绕过写法是 Python 里完全常规的形态。按参照实例的口径同构：
  「`ast.Name` 形态」那一半真验了，**「属性形态」那一半是空的**。

**变异**：B1（`parse_label` 恒返一标签）⇒ exit 1、`6 failed, 53 passed`，红名逐字与预测相同；
B2（未知标签静默归一）⇒ exit 1、`2 failed, 57 passed`，红名逐字相同；
B3（源码文本注入三样，含阳性对照）⇒ exit 1、`1 failed, 58 passed`，**红的只有阳性对照那条，m6/m7 仍绿**。
**三次变异预测全部吻合。** 阳性对照证明注入**确实抵达了 AST 扫描面** ⇒ m6/m7 的绿不是注入没生效，**是判据真的漏了**。

🔴 **B1/B2 下「验收面」那几条未转红，成因追踪照实落盘**（`P2-4` 的 Exit Criteria 走的正是这一支）：
B1 打红的 6 条**全部是结构面**（`test_h7_3_*` ×2 · `test_an_unknown_label_is_named_and_raised_not_silently_normalised` ×2 ·
`test_a_reply_without_a_parseable_label_object_is_raised_too` ×2），而**验收面**四条
—— `:50` `test_h1_every_fake_judge_fails_the_acceptance_function` · `:130` `test_acceptance_requires_exact_three_way_labels_on_every_negative` ·
`:136` `test_acceptance_requires_at_least_seventeen_of_nineteen_positives` · `:140` `test_acceptance_is_true_at_the_exact_floor`
—— **全绿**。**成因**：`meets_acceptance` 实现在 `tests/unit/answer_judge_fixture.py:143`、
`always-correct` 是同文件 `:183` 的 lambda，**该调用链一次都不进 `agenerp/judging/`** ⇒
白名单内（`agenerp/**`）的变异按构造动不到它；要动它必须改 `tests/unit/answer_judge_fixture.py`，那是 `P2-0` ④ 的黑名单。
⚠️ **这不是「没有任何变异能动到验收面」**（那是对无穷空间的全称断言，本轮两次尝试撑不起来），
只是**本轮两次实测的适用范围 + 成因**。执行者已独立复跑 B1 坐实：`exit 1`、`6 failed, 53 passed`，验收面四条全绿。

🔴 **顺带点名一处过期注释，不修**（plan `D2` 要求「撞上了也不许顺手改，只在审计文件里点名」）：
`agenerp/explain/loop.py:89-97` 的注释块里逐字仍是 `32 = 8 × 4` / `默认值取 25 时` / `下界是 MAX_TURNS + 1 = 26（等号处不算数），32 > 26`，
而 `:99` 紧接着才写「2026-08-26 由 32 提到 50（人裁定）」，且 `:68` `MAX_TURNS = 40` · `:102` `MAX_TOOL_CALLS = 50`
⇒ **同一个注释块里新旧两套数并存**（同源漂移另见 `docs/architecture/module-boundaries.md:1012` / `:1029-1034`）。
**不变式本身仍成立**（`40 + 1 = 41 < 50`），过期的是两段算术的数。
⚠️ **本轮 `P2-6` 的施加面在第 1 轮评审后已从 `agenerp/explain/**` 改到 `agenerp/routing/adapter.py`，
执行者与子代理事实上都没「撞上」它** —— 这一条是执行者收口时**主动补查**点名的，不是撞上的。**Non-Goals 8：不修。**

### 3.3 `tests/unit/test_explain_cost_ledger.py` → ✅ **名副其实但有已声明的边界**

- **① 名字承诺**：文件名 + docstring `:1-8`（「判的是**每次模型调用的账都在，且对得上端点自报的数**」·
  「⚠️ **失控闸的判据不在这里**，在 `tests/unit/test_explain_runaway_guard.py`」· 「**本文件不判任何阈值**」）
  + plan `2026-08-24-2109-2:303/:496-497` + plan `2026-08-25-0554-1` Non-Goals 8（`:236-239`）。
  🔴 **`02-WBS.md:87` 的 P1.7 行那条 🔴 指的是 `tests/gates/test_explain_cost_accounting.py`，
  该门禁 `:44` 加载的是 `tests/unit/test_explain_cost_accounting_body.py`（`:46-54` 导出 9 个名字），
  不是抽中的这一份 ⇒ WBS 那一行钉的是另一个文件**（`P2-A` 要求的显式声明，已写明）。
- **② 实际验了什么（四类重点盯逐条）**：
  - **(i) `reasoning` 真的独立可红** —— C1 打红 **9** 条，覆盖重放 / 完整链路 / 逐条对账 / 三条错误路径 /
    洞察路径 / 汇总。⇒ P1.7 逐字「判据要能挡住『只记 completion 不记 reasoning』这种假实现」**实测成立**。
  - **(ii) `cached` 不进 `total`** —— 本文件 `cached` **零出现**；C3（丢掉 `cached`）⇒ 本文件零红，
    而 `tests/unit/test_prompt_cache_accounting.py` **4 红**。⇒ 这一半**实测确认真在别处跑到**。
  - **(iii) 成本判据与失控闸未合并**（D-18 逐字）—— docstring `:3-5` 显式声明；
    `test_explain_runaway_guard.py` **11 条**经 `--collect-only` + 全量跑实测确认在 `pytest tests/unit -q` 里跑到并通过；
    另有 `test_the_two_criteria_groups_do_not_import_each_other` 钉住「互不 import」。
  - **(iv) `pytest.raises` 型反测** —— `:488` `test_h3_dropping_reasoning_turns_the_real_replay_red` 在 C1 下如期仍绿，
    **按 `P2-5`(iv) 不因此判它名不副实**；它的另一种证法**就在同文件** `:505`
    `test_h3_the_real_replay_is_green_without_the_fake`（逐字「否则 `pytest.raises` 只是在证明『判定体永远红』」），
    而 `:505` 在 C1 下**确实在 9 条红名单里** ⇒ 这个「另一半」是真活的、真可红的。
- **③ 缺口**：**无构成名不副实的缺口。** 两处照实登记且都不构成：
  - `:1-9` 的 docstring 声明了失控闸的边界，**没有**声明 `cached` 的边界 —— `cached` 的边界声明落在
    承诺面的第三锚点（`2026-08-25-0554-1:236-239`）而非文件内。子代理**把两种读法都摆了出来**，
    采「该 plan 本身就是本文件的承诺面之一，把承诺面的一部分同时当作『未声明』是自相矛盾的」这一读法。
    **无论哪种读法，`P2-7`(ii) 都由 C3 实测满足。**
  - `:453` `test_h2b_a_reply_without_reasoning_details_is_not_folded_into_completion` 在 C1 下仍绿，
    但**名字自带限定语**「a reply **without** reasoning details」，测的正是「端点根本没回
    `completion_tokens_details`」那一支 —— **名字诚实，不是缺口**。
- **`P2-7`(i)(ii) 双证据齐全**：(i) docstring `:3-5` 显式声明失控闸在别处；
  (ii) `test_explain_runaway_guard.py` 11 条 + `test_prompt_cache_accounting.py` 12 条
  **由抽查者（Phase 2 的子代理）自己复跑 `python3 -m pytest tests/unit -q` → `839 passed, 17 skipped`, exit 0 确认**，
  **不是照抄 plan、不是采信文件自述**。

**变异**：C1（`usage_of` 把 `reasoning` 折进 `completion`，施加面 `agenerp/routing/adapter.py` +
`agenerp/explain/ledger.py`）⇒ exit 1、`9 failed, 18 passed`，红名集合逐字与预测相同；
C2（`record_error` 不 `append`）⇒ exit 1、`6 failed, 21 passed`，红名逐字相同；
C3（丢 `cached`）⇒ **部分不吻合**（照实记，不改预测）：退出码与「零红 / 别处变红」两个承重预测吻合，
但子代理跑前写的 `21 passed` 与实际 `27 passed` **不吻合** —— 21 是 `def test_` 的**函数个数**，
实际收集数 27（`@pytest.mark.parametrize` 展开 5+3 条），跑前把两者混为一谈。**预测原文未改。**

### 3.4 执行者对两条承重结论的独立复跑（`裁判规则 3`）

两条 `名不副实` 会触发 CP9 §1.4 的重判条件，故执行者**原样复跑**了它们各自的承重变异。
**全部内存 monkeypatch，工作树零字节改动。**

| 复跑 | 命令 / 施加 | 退出码 | 输出 | 与 §2.4 是否一致 |
|---|---|---|---|---|
| A1 · 门禁面 | `agenerp.inspection.engine._fires = lambda r, v: False` + `pytest -q tests/gates/test_insight_rule_ablation.py` | **0** | `1 passed`，**零红** | **一致** |
| A1 · 开发期那份 | 同上施加，靶子换 `tests/unit/test_inspection_rules.py` | **1** | `5 failed, 11 passed`；红 = `test_h1_the_full_rule_list_reports_the_finished_goods_backlog` · `test_h1_the_reported_quantity_moves_with_the_dataset` · `test_every_shipped_rule_passes_its_own_test_case` · `test_h2_the_whole_inspection_runs_with_the_model_face_booby_trapped` · `test_the_hit_record_is_diffable` | **逐字一致** |
| B3 · 无阳性对照 | `Path.read_text` 补丁只对 `agenerp/judging/*.py` 生效，注入属性式 `_a.ChatAdapter(...)` + `open(p,"w").write("x")` | **0** | `59 passed` ⇒ **m6/m7 全绿** | **一致** |
| B3 · 带阳性对照 | 同上再注入字面量 `tests/fixtures/p1_entry_gate_labels.jsonl` | **1** | `1 failed, 58 passed`；红**只有** `test_no_product_module_in_the_judging_package_reads_the_annotation_set`（断在 `:288`）；**m6/m7 仍绿** | **逐字一致** |

执行者另实读确认了两处缺口的行号：`grep -n` → `:270` def m6 · `:275` `isinstance(node.func, ast.Name)` ·
`:277` `isinstance(node, ast.ImportFrom)` · `:305` def m7 · `:310` `node.attr not in {"write_text","write_bytes"}`
—— **与子代理给的行号逐字相同**。

复跑后 `git status --porcelain -- agenerp/ tests/` → **无输出**。

## 4 · 结论

> **抽中的三条里，名不副实 2 条。**

| 文件 | 判词 | 缺口 |
|---|---|---|
| `tests/gates/test_insight_rule_ablation.py` | 🔴 **名不副实** | `:47` —— 消融的阳性对照臂不在门禁面上 |
| `tests/unit/test_answer_judge.py` | 🔴 **名不副实** | `:275-279`（`never` 只覆盖 `ast.Name` 形态）· `:310`（写路径只认两个属性名） |
| `tests/unit/test_explain_cost_ledger.py` | ✅ **名副其实但有已声明的边界** | 无（边界 = 失控闸 / `cached` 在别处，两者均经实测确认真跑到） |

**处置（§7.2 原规则）**：抽到名不副实即为误放行 ⇒ **本次 2 次**。

🔴 **`docs/audits/2026-08-26-CP9-P1-retrospective.md` §1.4 的「若抽中的三条里再出现一条名不副实」条件
**已触发** —— 该条逐字「则『误放行 = 1 次、且是孤例』不再成立，**本条须重判**」。
**重判归人。** 本次抽查**不代人下续用 / 停用的判断**，也**不动**
`docs/backlog/implementation-roadmap.md:38` 那一行（人写的结论面）。

**本轮一处缺口都没有修**（plan Non-Goals 8：抽查是审计，不是修理；且两条缺口都在 `tests/**`，
`test_insight_rule_ablation.py` 更在红线 1 内）。

## 5 · verification scope limited

### 5.1 跑过的与没跑的

**跑过（命令原文 + 退出码）**：

| 命令 | 退出码 | 何时 |
|---|---|---|
| `python3 tools/gates/check_expected_red.py` | **0**（`门禁 29 项：预期红 0，绿 29，跳过 0`） | 开工 + 收尾各一次 |
| `python3 -m pytest tests/unit -q` | **0**（`839 passed, 17 skipped`） | 子代理一次、执行者一次 |
| `python3 -m pytest tests/gates/test_insight_rule_ablation.py -q` | **0**（`1 passed`） | 开工 |
| `python3 -m pytest tests/unit/test_answer_judge.py tests/unit/test_explain_cost_ledger.py -q` | **0**（`86 passed`） | 开工 |
| `git status --porcelain -- agenerp/ tests/` | —— | 每次变异后 + 收尾，**均无输出** |

**没跑（照实写，不冒充全绿）**：

- ❌ **整仓 `python3 -m pytest tests -q -m "not live"` 未跑** —— 已知基线即红
  （`gates` × `tools` 同进程环境泄漏，已立案 `docs/backlog/gates-and-tools-leak-env-across-directories.md`）。
  **本次不把它当验收命令。**
- ❌ **未跑任何 `-m live` 判据**（零网络、零凭据、零 docker、零 LLM 调用）。
- ❌ **未经 CI 服务端复跑** —— 上表全部是本机结果。

### 5.2 独立性的限度（不许读成「完全独立的第三方审计」）

- 子代理与执行者**同属一个循环、同一个模型家族** ⇒ 独立只到**会话级与上下文级，不到实现级**（见 §2.2）。
- 独立性的证据形态是 **§2.3 的 prompt 原文 + §2.4 的返回原文 + task id `a0d5c65e472906df1`**，
  **不是任何一句「我不是作者」的自报**。
- 执行者对两条承重结论做了独立复跑（§3.4），但**执行者不是这三份判据的作者以外的第三方** ——
  `tests/unit/test_answer_judge.py` 与 `tests/unit/test_explain_cost_ledger.py` 的 git author
  正是 `Email Agent Developer`（= 本循环，见 §1.3）。**§3.4 只是复跑，不是独立判断。**

### 5.3 红线自证（逐条说清哪条命令证了哪条，不许一条命令包打七条）

`<BASE>` = `29a85dc324914cc9596377e0ea8304bf50c781ac`（`P1-0` 记下的开工 sha）。

| 红线 | 自证命令 | 结果 | 证到了什么 |
|---|---|---|---|
| **1**（gates）· **2**（workflows）· **3**（DECISIONS）· **5**（masterplan 其余） | `git diff --name-only <BASE> -- tests/gates/ .github/workflows/ docs/masterplan/DECISIONS.md docs/masterplan/02-WBS.md missions/ docker-compose.yml agenerp/ tests/ industry-packs/` | **无输出** | 这四条**直接证到**（`STATE.md` 的追加另由下一行证） |
| **4**（项目名 / 包名 / 命名空间） | `git diff --name-only <BASE> -- pyproject.toml` | **无输出** | 直接证到 |
| **5**（`STATE.md` 只追加） | `git diff --numstat <BASE> -- docs/masterplan/STATE.md` → **第 2 列（deletions）== `0`** | 见 §5.4 | 直接证到。🔴 **未用** `git diff \| grep -c '^-[^-]'` —— 该写法对 `- [open] …` 这种行形状**真删了也返回 0** |
| **6**（证据仓 `${XM_PATH}`） | 🔴 **本仓的 `git diff` 按构造证不了它** —— 执行者逐字声明：**全程未对 `${XM_PATH}`（`/Users/lize/Documents/ChatGPT/XM`，冻结在 `1c622c8`）发起任何写操作**；本轮**也未读过它**（只读了 `docs/masterplan/evidence-repo.env` 这个指针文件本身）。**不拿本仓 diff 冒充。** | —— | 声明，非 diff |
| **7**（运行时 Server Script） | **由「产品代码零改动」间接推出**（第 1 行已证 `agenerp/` 无输出），**不是上面任何一条直接证的**，照实写 | —— | 间接 |

另：`git diff --name-only <BASE> -- docs/audits/2026-08-26-CP9-P1-retrospective.md` → **无输出**
⇒ 人写的 CP9 复盘纪要**一个字节未改**（plan Non-Goals 3）。

### 5.4 收尾 sha 与 `STATE.md` 零删除自证

- `<HEAD>`（收尾）：本文件所在提交的 sha，记在 `docs/logs/2026/08-26.md` 的对应条目与 `STATE.md` §3 追加行里。
- `git diff --numstat <BASE> -- docs/masterplan/STATE.md` 的实测值同样记在该日志条目里
  （断言**第 2 列 == `0`**）。两者都必须带 `<BASE>` —— 不带的话落盘 commit 之后 diff 必空，**那是二次真空通过**。

## 6 · 独立收口审计（`Audit: required` · Closure Gate「closure audit was independent」）

- **审计者**：独立子代理，fresh session，Agent tool `general-purpose`，**task id `a6caad8c6df08b9d1`**
- **独立性**：**既不是本 plan 的执行者，也不是那三份判据的作者**（那三份的 author 见 §1.3）。
  ⚠️ 与 §2.2 同一条限度：与执行者**同属一个循环、同一个模型家族**，独立只到会话级与上下文级。
- **口径**：逐字取自 `docs/skills/closure-audit-prompt.md` —— **不采信自报，逐条对活仓取证**。
  执行者写下的每一句「已完成 / 已验证 / 已自证」，审计者**自己跑了一遍或自己读了一遍**再判。
- **总判词**：**`closure approved`**（1 条 `should-fix` + 3 条 `nit`，**零 `blocking`**）

**审计者自己复跑的取证（不是采信落盘）**：

| 复跑项 | 审计者实测 | 与落盘是否一致 |
|---|---|---|
| 红线 1/2/3/4/5 · `git diff --name-only 29a85dc HEAD -- tests/gates/ .github/workflows/ DECISIONS.md 02-WBS.md missions/ docker-compose.yml agenerp/ tests/ industry-packs/ pyproject.toml` | **无输出** | 一致 |
| 红线 5 · `git diff --numstat 29a85dc HEAD -- docs/masterplan/STATE.md` | **`15  0`**，且全文实读确认「只有一个 `@@ -465,6 +465,21 @@` 增量块，15 行全是 `+`」、`[open] 2026-08-26T10:03Z` **一个字未被触及** | 一致 |
| 产出面 · `git diff --name-only 29a85dc HEAD` 全量 | **四个文件**（审计文件 + 日志 + STATE + plan 自身），**零越界**；plan 的 diff 逐行看过，**起草期被证伪的 3 条变异留痕一个字都没删** | 一致 |
| `P1-1` 抽样**自己重算** | 池 **44 条**，前五名键 `109097fc…/14152b05…/1b6ea32b…/1c308f8f…/1cdaf032…` ⇒ 前三名逐字相同、第 4 名亦相同 | 一致 |
| `P2-A` 三条锚点**逐行核活仓** | `02-WBS.md:84` 一字未提答案判定器 ✓ · `:87` 钉的是 `test_explain_cost_accounting.py`（其 `:44` 加载 `..._body.py`）✓ · `:85` 两条 🔴 逐字吻合 ✓ | 一致 |
| `P2-7`(ii) **自己验** | `.github/workflows/gates.yml` 的 `unit-and-contracts` job 逐字 `python3 -m pytest tests/unit -q` ✓ · `--collect-only` 数出 `test_explain_runaway_guard.py` + `test_prompt_cache_accounting.py` 共 **23 条**（= 11+12）全被收集 ✓ · `test_inspection_rules.py::test_h2_*` **4 条**在列 ✓ | 一致 |
| **A1**（门禁 / 开发期两面） | `exit 0` / `1 passed` / 零红 ‖ `exit 1` / `5 failed, 11 passed`，五个红名一字不差 | **逐字一致** |
| **B3**（无对照 / 带阳性对照） | `exit 0` / `59 passed` ‖ `exit 1` / `1 failed, 58 passed` | **逐字一致** |
| **B1 / C1 / C3** | `6 failed, 53 passed` ‖ `9 failed, 18 passed` ‖ 本文件 `27 passed` 零红、`test_prompt_cache_accounting.py` `4 failed, 8 passed` | **红名集合逐字一致** |

**审计者对阳性对照又加验了一层**（落盘里没有的）：`_code_without_docstrings`（`tests/unit/test_answer_judge.py:34`）
走的正是 `path.read_text(encoding="utf-8")`，**与 m6（`:274`）/ m7（`:308`）同一条读取路径**
⇒ 阳性对照打红确实证明注入抵达了 m6/m7 所解析的那段源码文本。

**审计者对「误判成边界」的反向风险专门查过**：他额外跑了一步，确认阳性对照臂
`test_h1_the_full_rule_list_reports_the_finished_goods_backlog` **不依赖任何 fixture、完全可以被导出**
（`positive arm runs fixture-free under path-load: OK`）⇒ 该文件 `:49-55` 自述的排除理由
（fixture 跨文件加载不随函数走）**对它不适用**，`P2-7`(i) 明确不满足。**「其实是边界、被误判成名不副实」这条反向风险不存在。**

**三条判词审计者逐条独立同意**：两条 `名不副实` + 一条 `名副其实但有已声明的边界`，
并逐字确认「CP9 §1.4 的触发条件确实已触发，且 loop 正确地把重判留给了人、没动纪要、没动 `implementation-roadmap.md:38`」。

### 6.1 四条 finding 的处置（逐条，全部已改）

| # | 等级 | 内容 | 处置 |
|---|---|---|---|
| **F1** | `should-fix` | `P2-4` 的 Exit Criteria 走的是「验收面未转红」那一支，但**配套的成因追踪没落盘** | **已补**：§3.2 变异段新增一整段，含验收面四条的行号（`:50`/`:130`/`:136`/`:140`）与成因（`answer_judge_fixture.py:143` / `:183`，该调用链不进 `agenerp/judging/`），并逐字声明**这不是「没有任何变异能动到验收面」**。执行者**已独立复跑 B1 坐实**（`exit 1`、`6 failed, 53 passed`，验收面四条全绿） |
| **F2** | `nit` | 日志把审计文件写成 603 行，实为 620 行 | **已改**（本轮 F1/F3/F4 落地后行数再变，日志改为不写死行数） |
| **F3** | `nit` | plan `D2` 承诺「撞上了只在审计文件里点名」，但那处过期注释**全程没被点名** | **已补**：§3.2 末段点名 `agenerp/explain/loop.py:89-97` 与 `:68`/`:102` 的实测值，并**照实说明执行者事实上没「撞上」**（施加面第 1 轮评审后已改到 `agenerp/routing/adapter.py`），是收口时**主动补查**点名的。**Non-Goals 8：不修** |
| **F4** | `nit` | §2.4 标称「返回的报告原文（逐字）」，实为二次落盘版；「内容一字未改」本身是自报 | **已改**：标题改为「**二次落盘版** —— 原始返回已在传输层截断，不可逐字比对」，并写明该句是自报、按构造不可比对，另补「所有承重读数都不靠这一节成立，三方复跑逐字一致」 |

⚠️ **照实说的一件事**：**F1–F4 的处置本身未经第二轮独立复核** —— 审计者判词是
「收口前建议做完 F1，F2/F3/F4 可顺手改也可留；之后即可勾 Phase 3、Closure Gates 九项、`Plan Status → completed`」，
执行者把四条**全部**做了，但**没有再跑一轮审计去复核这四处改动**。这一点写在这里，不藏。

