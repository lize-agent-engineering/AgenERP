# 2026-08-28-P2.3 视图 Agent：自然语言 → DSL，且校验绕不过去

> Plan Status: draft
> Mission: p2-views
> Work Item: **P2.3 视图 Agent：自然语言 → DSL（含 `dsl.validate`/`dsl.preview`）**
> （WBS `02-WBS.md` §5 第 103 行）· 验收 `pytest tests/agents/test_view_agent.py -q` 退 0
> Last Reviewed: 2026-08-28
> Source: `docs/logs/2026/08-28-handoff-p2.md` §1（P2.3 = 下一项，判据文件尚不存在）·
> `docs/design/agents-and-roles.md` §5.1（视图 Agent 的工具面）· `p2-views-roadmap.md` 硬约束 ③④
> Related: 前置 **P2.2 已过 + P2.0R 已判过（D-29）** · P2.1（DSL 与两层校验器）·
> P2.6（角色首页与 schema 快照）· D-15 · D-16 · D-26
> Audit: required

---

## 0. 这一轮交什么

**一个把自然语言变成视图 DSL 的控制循环，外加三层判据证明它「校验绕不过去、错了不硬交」。**

**不交**：视图产物落库与 GitOps（P2.4）· `schema.drift` 巡检（P2.5）·
写操作（P3）· 生产模型上的量化数字（见 §5 D6 与「Deferred But Adjudicated」）。

---

## 1. Current Baseline（实读，2026-08-28 @ `b4a33b4`）

### 1.1 判据面现状（本机实跑，不是引旧数）

```
python3 -m pytest tests/ -q --ignore=tests/gates --ignore=tests/render --ignore=tests/ui
  → 1436 passed, 45 skipped
python3 -m pytest tests/dsl -q         → 51 passed
python3 tools/gates/check_expected_red.py → 门禁 29 项：预期红 0，绿 29，跳过 0
```

`tests/agents/` **这个目录还不存在**。

### 1.2 P2.3 要接的三个既有面，逐条实读

| 面 | 位置 | 对本项意味着什么 |
|---|---|---|
| DSL 声明格式 | `agenerp/dsl/blocks.py` | `View` / `Block`，五种块封闭；`field_refs()` 是「用到哪些字段」的**唯一答案** |
| 两层校验器 | `agenerp/dsl/validate.py:56` | `validate(view, None)` **抛 `SchemaUnavailable`**，不返回 ok |
| 落回判定 | `agenerp/dsl/fallback.py` | `plan_render()` **块粒度**落回、每次落回带理由、**零模型调用** |

### 1.3 循环零件已经全在，本项不需要造新的

`ExplainLoop.__init__`（`agenerp/explain/loop.py:314`）把 `adapter` / `client` /
`executors` 都做成了**注入位** —— 离线判据要的接缝已经有先例，照抄形状即可。

可复用：`routing.route()` · `tools.runtime.execute` · `tools_readonly` 契约 ·
`context.session` · `orchestration.open_session` · `DenialBreaker` · `explain.ledger.CallLedger`。

### 1.4 🔴 P2.0R 的四处 harness 修复**已经在产品工具层**，不是只在实验设施里

这一条是本项能站住的前提，逐条核过：

| 修复 | 落点 |
|---|---|
| hidden 字段**保留并标记**，不再无条件剔除 | `agenerp/tools/documents.py:272` |
| `description` 非空才回（分辨点） | `agenerp/tools/documents.py:305` |
| `keywords` 过滤大单据 | `agenerp/tools/documents.py:329` |
| `schema.search` 覆盖**全部**业务 DocType，没数据的标记不剔除 | `agenerp/tools/site_scope.py:190` |

⇒ 视图 Agent 直接用产品工具层，**不会把 §3④ 那一串重踩一遍**。

### 1.5 两份**真实**的 schema 快照已在仓内，离线可用

| 文件 | 覆盖 | 出处 |
|---|---|---|
| `agenerp/schema/view-schema.json` | 8 张表 / 381 字段（车间工人域 + 子表） | `agenerp/schema_snapshot.py` 从活站点生成 |
| `tests/dsl/fixtures/site-schema-subset.json` | 17 张表（含 `Sales Order` 106 字段） | `dump_schema.py` 实读导出，带 `provenance` |

⇒ **离线判据与评测集的期望字段都有真来源，不用手写 schema。**

### 1.6 上一场留下的、本项必须继承的三个数

- `RESULT_CHARS = 20000`（`agenerp/explain/loop.py:105`）—— 6000 时**7 条题的正解被切在返回前面**
- `MAX_TURNS = 40` —— 25 轮时归因类问题 1/3 撞轮数上限
- 跑评测**一道闸不设**：`--per-question-budget 0 --budget 0 --max-tool-calls 0`；
  设了闸实测 **42% 的题被闸砍掉，且失败伪装成能力不足**

---

## 2. Goals / Non-Goals

### Goals

1. 一个自然语言 → `View` 的控制循环，**校验由循环无条件执行，模型没有绕过它的路**。
2. 两条 fail-closed 各有判据：修复轮用尽**不产出视图**（不降级交付）· 拿不到 schema **不算过**。
3. 三层判据落地：离线本体（零 token）· live 端到端（draft 位置）· 量化评测（开发期模型）。
4. 量化评测的**期望集合本身有判据守着真实性** —— 否则测的是「我编得对不对」。

### Non-Goals

- 不动 `agenerp/explain/` 与 `agenerp/dsl/` 任何一行
- 不落 `tests/gates/**`（红线 ②，本项按人 2026-08-28 裁定写在 `tests/agents/`）
- 不做视图落库 / GitOps / drift 巡检 / 写操作
- **不产出生产模型上的数字**（人 2026-08-28：开发期数字就够）

---

## 3. Task Route

- Type: `app-layer design change`
- Owner Docs: `docs/design/agents-and-roles.md` §5.1 · `docs/architecture/module-boundaries.md` ·
  `docs/backlog/p2-views-roadmap.md` 硬约束 ③④ · `docs/masterplan/DECISIONS.md` D-15 / D-16 / D-26
- Skill Selection Basis: `superpowers:brainstorming`（已用，本文件即其产物）→
  `superpowers:test-driven-development`（Phase 1–2 先红后绿）→
  `superpowers:verification-before-completion`（收口）

---

## 4. Infrastructure And Config Prereqs

- **Phase 1–2 无前置**：零 token、无活栈、无凭据（这正是 WBS 那条命令的形态）
- **Phase 3–4 要活栈**：`AGENERP_SITE=frontend` / `AGENERP_SITE_URL=http://127.0.0.1:18080` /
  `AGENERP_ADMIN_PASSWORD`，凭据取自 `~/.config/agenerp/secrets.env`（仓外 0600）
- **Phase 4 要模型**：开发期走 `claude -p --model sonnet`（订阅额度，**不烧百炼**）。
  本机实测两个 CLI 都在：`claude 2.1.246` · `codex-cli 0.147.0`
- **不新增任何产品侧环境变量** —— `routing/config.py` 那三个变量一个不动

---

## 5. 设计决策（每条带备选与残余风险）

### D1 · 新写 `agenerp/views/loop.py`，复用零件**不复用** `ExplainLoop` 本体

**备选**：B 复用 `ExplainLoop` 并把证据门禁参数化 · C 抽公共基类给两个 Agent。

**否掉 B 的理由是硬的**：视图 Agent 的停止条件是「DSL 合不合法」，解释 Agent 的是
「证据够不够」。塞进 `ExplainLoop` 只有一条实现路径 —— 在证据充分性门禁上开一个
`if task_class == "view": skip`。**那就是给 P1.4 那道门禁开后门**，而它正是
P1.0 入口关口实验证明「门禁能补偿模型能力」的那个东西。

**否掉 C**：过早抽象，且要动 P1.4 已过判据的代码。

**残余风险**：两个循环会有重复逻辑（工具回注、clip、记账）。**现在不抽** ——
第三个 Agent（P3 操作 Agent）出现时再评估，那时才有三个样本判什么是真共性。

### D2 · `dsl.validate` / `dsl.preview` **不进模型可见工具面**（人 2026-08-28 裁定）

**与 owner doc 字面偏离**：`agents-and-roles.md` §5.1 第 119 行把这两个列为视图 Agent 的工具。

**理由**：D-15「规则能覆盖的流程不 Agent 化」。「这个视图合不合法」「哪块画不了、为什么」
`validate()` 与 `plan_render()` 纯规则零模型就能答。本仓已有同形先例：`permission.scope`
**刻意不进模型工具面**，因为开场注入已把那一步确定性化（`explain/loop.py` 模块头逐字写着理由）。

**备选**：都进（照字面）· 只开 `preview`。都会留下同一个缺口 ——
**模型可以不调就交**，除非再叠一层强制，那就变成重复校验。

**残余风险**：与 owner doc 字面不一致。**本决策可逆**（加两份契约 + 注册表项即可），
且 owner doc 的更新在收口清单里。

### D3–D5 · 判据分层 / 双层判分 / 评测域（人 2026-08-28 裁定，三条）

- **D3 判据分层**：离线本体（WBS 那条命令打它）+ live 门禁 + 量化评测，**三者判的不是同一件事**
- **D4 双层判分**：确定性硬判把门 + 判官只答「切题吗」
- **D5 评测域**：主集在车间工人域（含「落回 = 0」）+ 域外子集（只判 validate 过 + 落回有理由），
  **两个数分开报，不合并成一个总成功率**

### D6 · 开发期模型走 `claude -p`，产品 `routing` 一行不动；**P2.3 以开发期数字判过**

`routing/config.py` 按 `model-management.md` §12.1 ① **不许出现厂商名与端点**
⇒ Claude 这条路只能是 `tools/experiments/p2_view_agent/` 侧的适配器，与
`tools/experiments/p1_entry_gate/llm.py` 同族（模块头逐字「实验设施，不是产品代码」）。

**残余风险（照实记，不许被写成结论）**：这样拿到的数字**与 P2.0R 那 57/60 = 95.0% 不可比**，
也**不代表百炼上任何模型的表现**。凡引用本项数字处必须点名模型。
重开条件写在「Deferred But Adjudicated」。

### D7 · live 端到端先写在 `tests/agents/`，**不落 `tests/gates/`**（人 2026-08-28 裁定）

`tests/gates/**` 是红线 ②，改动要人明说指派 + `Gates-Change-Approved-By:`。

**残余风险**：交接文档 §3⑤ ——「**在最终位置跑一遍**」和「跑过」是两件事，
门禁在草稿位置 5 passed、落进 `tests/gates/` 后一格跑不了。
⇒ 将来落地 gates 时**必须在最终位置重跑**，本项不预支那句话。

### D8 · `REPAIR_ROUNDS` v0 = **3**，且这个数**现在没有实测依据**

**照 §1.6 的纪律：本仓的每个上限都要有出处。** 这个数今天没有 ——
`MAX_TURNS` 由 25→40 是实测换来的，`RESULT_CHARS` 由 6000→20000 也是。
`REPAIR_ROUNDS` 只是「先取一个最小可观测值」，**它是待测量，不是结论**。

**Phase 4 跑完必须回答一个问题**：有没有题目卡在修复轮上限上？
有 ⇒ 按实测调整并记 D-16（本项目的结论以本项目实测为准）；
没有 ⇒ 把「N 条题里最深修复轮数 = k」写进收口，这个数才算有出处。

⚠️ **不许在没跑之前把它调大** —— 那就是 §3② 那条「自己的闸停的，却记成能力问题」的反面：
先把闸放到看不见，同样量不出真实形状。

---

## 6. 模块边界（结构定义，不是实现细节）

```
agenerp/views/
  wire.py    线格式 ↔ View。**只解析形状，不判对错**
  loop.py    控制循环 + ViewProposal
```

**`wire.py` 的纪律照 `blocks.py`**：解析器要能造出**非法**的 `View`，否则校验器的拒绝路径
根本没法测。`WireError` 与 `DslError` **分开**——「模型交的不是 DSL」和「模型交的是错的 DSL」
回注话术不同，混成一个就分不清是格式没说清还是字段找错了。

**`loop.py` 的接缝**照 `ExplainLoop.__init__`：`adapter` / `client` / `executors` 都是注入位。

**`RESULT_CHARS` 复用 explain 那个 20000，不自己再定一个**（§1.6）。

### 循环形状

```
开场注入（可见范围，规则面，不靠模型想起来调）
  → 模型选工具：schema.search / meta.fields / system.overview   ← 只有这三个
  → execute → 回注（clip 20000）
  → 模型交 DSL JSON
  → 🔴 循环无条件跑 wire → validate → plan_render               ← 模型没有绕过它的路
  → 不过：错误逐条回注，强制重来（上限 REPAIR_ROUNDS）
  → 过：出 ViewProposal
```

**不给数据工具**（`query.read` / `doc.get`）：视图 Agent 要的是 schema 不是数据行。
给了它只会去查数据、烧 token，而答案不在那儿。

### 两条 fail-closed，形状不同，都要写死

1. **修复轮用尽仍不合法 ⇒ 不产出视图**（`view is None` + `stop_reason`）。
   **不许把画不了的块删掉再交** —— 那正是 `fallback.py` 模块头点名的坏选择
   （静默丢弃，用户以为那块内容本来就不存在）。
2. **拿不到 schema ⇒ `SchemaUnavailable` 冒上来，不吞。**

### ⚠️ 「落回 Desk」不是失败

块粒度落回照常交付、理由随件（P2.2 已定的正确行为）。
**评测主集要求「落回 = 0」是评测的判据，不是循环的判据** —— 两件事必须分开写死，
否则循环会长出「为了不落回而删块」的动机。

---

## 7. Execution Plan

### Phase 1 - 线格式与循环骨架

Status: planned
Targets: `agenerp/views/wire.py` · `agenerp/views/loop.py` · `agenerp/views/__init__.py`
Skill: `superpowers:test-driven-development`

- Item Types: `Add | Decision`
- Prereqs: 无（零 token、无活栈）

- [ ] `Add` `wire.py`：`view_from_json` / `view_to_json` / `WireError`。**只解析形状**，
      能造出非法 `View` 交给校验器拒
- [ ] `Add` `loop.py`：`ViewLoop`（注入 `adapter` / `client` / `executors`）+ `ViewProposal`
- [ ] `Add` 循环内**无条件**的 `wire → validate → plan_render`，错误逐条回注
- [ ] `Decision` D1 / D2 的落地形态写进模块 docstring（**理由写在代码里，不写在注释外**）

Exit Criteria:

- [ ] Phase 2 的判据能构造 `ViewLoop` 并在**零 LLM 调用、零活栈**下跑完
- [ ] `python3 -m pytest tests/ -q --ignore=tests/gates --ignore=tests/render --ignore=tests/ui`
      仍 ≥ 1436 passed（不回退既有面）
- [ ] No owner-doc update required（owner doc 的更新在 Phase 5）
- [ ] `docs/logs/` 更新（可与后续 phase 合并成一条）

### Phase 2 - 离线判据本体 + 变异验证

Status: planned
Targets: `tests/agents/test_view_agent.py` · `tests/agents/fixtures/`
Skill: `superpowers:test-driven-development`

- Item Types: `Proof`
- Prereqs: Phase 1

判据用**注入的脚本假模型** + `agenerp/schema/view-schema.json`（真快照，§1.5）。
⚠️ 假 executor 只喂 schema 行 —— **本层验的是循环，不是工具层**；工具层的真实性由 Phase 3 验。
这句话要写进文件 docstring，免得它被当成「工具层也测过了」。

- [ ] `Proof` 快乐路：出 `View`，`validate().ok`，落回 0
- [ ] `Proof` 拒绝路 · 不是 DSL：JSON 崩 / 缺段 → `WireError` 被回注，**不算过**
- [ ] `Proof` 拒绝路 · 字段不存在：交 `Sales Order.custmoer` → L2 拒 → 回注 → 第二轮改对 → 过；
      **且断言回注文本里出现了那个字段名**（否则模型改不了）
- [ ] `Proof` 🔴 fail-closed：修复轮用尽 ⇒ `view is None`，**且断言它没有降级交付**
- [ ] `Proof` 🔴 不可绕过：模型可见工具面里**没有** `dsl.validate` / `dsl.preview`，
      而 `validate()` 确实被调用了
- [ ] `Proof` schema 缺失 ⇒ `SchemaUnavailable` 冒上来，不被吞成「过」
- [ ] `Proof` **变异验证（D-26）**：① `validate` 改恒真 ② fail-closed 改成降级交付
      —— 逐条要见血；复原后实现文件 `sha256` 逐字节相同，**两个 sha 都留档**

Exit Criteria:

- [ ] 🔴 `python3 -m pytest tests/agents/test_view_agent.py -q` **退 0**（WBS 第 103 行那条命令）
- [ ] 两轮变异各自的红条数与复原后的 `sha256` 写进本 plan
- [ ] `python3 tools/gates/check_expected_red.py` 仍 29 项全绿、零跳过
- [ ] No owner-doc update required

### Phase 3 - live 端到端（draft 位置）

Status: planned
Targets: `tests/agents/test_view_agent_live.py`
Skill: `none`

- Item Types: `Proof`
- Prereqs: Phase 2 · 活栈 · 模型可达

形态照 `tests/render/test_renderer.py` 的严格模式：**凭据/站点/模型不可达即红，不跳过**
（`pytest.fail`，不是 `importorskip`）。⚠️ **按 D7 落在 `tests/agents/`，不落 `tests/gates/`。**

- [ ] `Proof` 一句真自然语言 → 真 `schema.search` / `meta.fields` → 出视图 → `validate().ok`
- [ ] `Proof` 产出接 P2.2 渲染路径**真能画**，且**落回 = 0**
- [ ] `Proof` 断言体与严格壳的关系写清楚（若拆两文件，basename 不得与既有断言体重名）

Exit Criteria:

- [ ] `python3 -m pytest -m live tests/agents/test_view_agent_live.py -q -rs` 退 0，**零 skip**
- [ ] 用的模型与 token 用量逐条留档
- [ ] 本文件**明写**「它现在不在 `tests/gates/`，将来落地必须在最终位置重跑」（§5 D7）

### Phase 4 - 量化评测（开发期数字）

Status: planned
Targets: `tools/experiments/p2_view_agent/`
Skill: `none`

- Item Types: `Add | Proof`
- Prereqs: Phase 3

- [ ] `Add` 评测集（**我拟**，人 2026-08-28 定）。每条：
      `{question, expect_doctype, must_fields[], expect_block_types[], domain, why_hard}`
      · 主集：车间工人域（`Work Order` / `Stock Entry` / `Item`），**≤ 20 条**
      · 域外子集：`Sales Order` 等，**≤ 10 条**
- [ ] `Proof` 🔴 **评测集真实性判据**：每个 `must_fields` 条目必须在 §1.5 那两份**实读快照**里
      真实存在（形态照 `tests/dsl/test_fixture_schema_is_real.py`）。
      **没有这一条，量化评测测的是「我编得对不对」。**
- [ ] `Add` 实验设施侧的 `claude -p` 适配器（**产品 `routing` 一行不动**，§5 D6）
- [ ] `Add` 硬判：wire 过 + validate 过 + DocType 命中 + `must_fields` 全在 +（主集）落回 = 0
- [ ] `Add` 判官只答「切题吗」，**不给它期望集合**（§3④：提示词引导证人 = 循环论证）；
      JSON 解析照 `test_judge_boundary.py` 的既有边界，**不贪婪**
- [ ] `Proof` 跑一轮出数，**三个 0**：`--per-question-budget 0 --budget 0 --max-tool-calls 0`

Exit Criteria:

- [ ] 主集与域外**两个数分开报**，各自点名模型；**不合并成一个总成功率**
- [ ] 结果 JSON 留档在 `tools/experiments/p2_view_agent/`
- [ ] 判重复只看 `trajectory_full`（名字 + 参数），**不按工具名数**（§3①，那个信号骗过四次）
- [ ] 归因前先自查：失败是不是**我们自己的闸**停的（§3②，五次）
- [ ] **`REPAIR_ROUNDS` 有出处了**（§5 D8）：写出「N 条题里最深修复轮数 = k」，
      或按实测调整并记 D-16

### Phase 5 - 收口

Status: planned
Targets: `docs/backlog/p2-views-roadmap.md` · `docs/design/agents-and-roles.md` · `docs/logs/2026/`
Skill: `superpowers:verification-before-completion`

- Item Types: `Proof | Decision`
- Prereqs: Phase 1–4

- [ ] `Decision` owner doc §5.1 补一句 D2 的偏离与理由（**或**由人裁定改回字面）
- [ ] `Proof` roadmap 第 160 行 P2.3 由 `todo` 改状态，附实测数字与证据链接
- [ ] `Proof` `docs/logs/2026/` 落一条
- [ ] `Proof` 独立收口审计（不得自审）

Exit Criteria:

- [ ] 全量离线判据、`check_expected_red.py`、live 三条命令逐条留档退出码
- [ ] 本 plan 的 `Plan Status` / 各 phase `Status` / `Closure Gates` / log 逐条一致

---

## 8. 这一轮**不许**踩的坑（逐条来自 `docs/logs/2026/08-28-handoff-p2.md` §3）

| # | 坑 | 本 plan 里对应的那一条 |
|---|---|---|
| ① | 「按工具名数重复」骗了四次 | Phase 4 收口项：判重复只看 `trajectory_full` |
| ② | 把自己配置造成的失败记成能力问题（五次） | Phase 4 收口项 + §4「三个 0」 |
| ③ | 「补丁 ID 匹配不上 ≠ 内容不在」 | 本 plan 不做分支操作，不适用 |
| ④ | 每次「能力失败」往下查都是 harness | §1.4：四处修复已在产品工具层，逐条核过 |
| ⑤ | 「在最终位置跑一遍」≠「跑过」 | §5 D7 残余风险 + Phase 3 Exit |

---

## Draft Review Record

- Independent draft review iteration 1: pending

---

## Closure Gates

- [ ] in-scope behavior is complete
- [ ] relevant docs are aligned（owner doc §5.1 的 D2 偏离已处理）
- [ ] verification has run：离线全量 · `check_expected_red.py` · `pytest -m live tests/agents` 逐条留档
- [ ] scoped verification is not conflated with full verification
- [ ] no in-scope item downgraded to deferred/follow-up
- [ ] independent draft review completed and recorded
- [ ] text consistency verified: status, phases, gates, and log all agree
- [ ] closure audit was independent
- [ ] closure evidence exists in files

---

## Deferred But Adjudicated

### 生产模型上的量化数字

- Classification: `watch-only residual`
- Why Not Blocking Closure: 人 2026-08-28 明确「开发期数字就够」。
- 重开条件：**P2.4 开工时**，或人要求生产数时。届时用百炼上仍有额度的模型重跑同一份评测集。
- ⚠️ 在此之前，**本项任何数字都不得被表述为「视图 Agent 在生产上的表现」**。
- Successor Required: `yes`（P2.4 或人指派）

### live 端到端落进 `tests/gates/`

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: `tests/gates/**` 是红线 ②，要人明说指派 + `Gates-Change-Approved-By:`。
- 重开条件：人指派。届时**必须在最终位置重跑**（§5 D7）。
- Successor Required: `yes`

### 两个循环的重复逻辑

- Classification: `optimization candidate`
- Why Not Blocking Closure: 现在只有两个样本，抽不出真共性。
- 重开条件：P3 操作 Agent 落地、出现第三个循环时。
- Successor Required: `no`

---

## Closure

Status Note: 未收口。

Closure Audit Evidence: 待补。

Follow-up: 待补。
