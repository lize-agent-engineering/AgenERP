# 2026-08-27-P2.7 术语层：给字段一个中文名字，且能证明它没乱起

> Plan Status: completed
> Mission: p2-views
> Work Item: **P2.7 术语层（LLM 生成 label，绕开社区翻译）**（WBS `02-WBS.md` §5 第 107 行）·
> 验收 `pytest tests/i18n -q` 退 0
> Last Reviewed: 2026-08-27
> Source: `view-dsl-and-eval.md` §10 术语层 · Spike 07 §6.3（术语层**同时是检索质量的前置条件**）
> Related: 前置 **P2.2 已过** · P2.0（载体落自有表）· `p2-views-roadmap.md` 硬约束 ③④ ·
> D-17（额度先用最先过期的）
> Audit: required

---

## 0. 这一轮交什么

**一份「字段 → 中文名」的术语层，外加一套能证明它没乱起名字的判据。**

**不交**：整站 6350 个字段的翻译（范围见 §2.0）· 界面上的语言切换 · 把它接进检索（那是 §11 的 bonus）。

---

## 1. Current Baseline（实读，2026-08-27 @ `2ecf2f8`）

### 1.1 🔴 整站 6350 个业务字段，label 含中文的是 **0**

不是「少」，是**零**。抽样看得更清楚：

| 字段 | 现在的 label |
|---|---|
| `Item Reorder.warehouse` | 「**Request for**」 |
| `Item Default.buying_cost_center` | 「Default Buying Cost Center」 |
| `Work Order.qty` | 「Qty To Manufacture」 |

第一条尤其说明问题：**字段名说的是仓库，标签说的是「请求给」。**
一个中国工人看着这张表，既读不懂英文，也猜不出这一列到底是什么。

### 1.2 Spike 07 给术语层加的第二个理由

`${XM}/spike/07-schema-retrieval/FINDINGS.md` §6.3 逐字：

> §10 的术语层原本是为了修「大师」「进行中」这类翻译事故。本次实测给它加了第二个理由：
> **术语层同时是检索质量的前置条件。** 字段有了准确中文标签之后，
> 无论结构化导航还是向量检索都会更准。

⚠️ 那是 Spike 07 的**建议**，不是实测结论。本轮 §11 会量一次，**但它不参与本项的过/不过**。

### 1.3 P2.0R 三轮之后，术语层在候选里的位置变了

第三轮钉住的那句话：**「任何不引入新信息的重排，结构上都够不着那 35 个点」**。
已被推翻的两个机制（族聚合、分数融合）共同点是**从同一批 embedding 再算一遍**。
**术语层不同 —— 它往索引里注入了这批向量里原本没有的信息。**

⚠️ **这不是选路**（D-16）。它只说明「量一下它值不值」是有理由的，见 §11。

### 1.4 载体

P2.0 已判：产物落 AgenERP 自有表。建表与 GitOps 是 P2.4 的结果面 ⇒
v0 落成仓内的 JSON 产物 + 一个加载器，**并在代码里写明这是暂时的**（形态同 `agenerp/dsl/roles.py`）。

---

## 2. 事先写死的判据与预测

### 2.0 范围：**跟着路线 C 走**，只做车间工人那 15 张表 / 317 个字段

理由两条：
1. **与 P2.2 一致** —— 路线 C 的分母就是这个。
2. **成本**。整站 6350 个字段是 20 倍量。P1 有过一次单问答烧掉 13.6 万 token 的实测，
   本仓的规矩是**有真跑判据前先算账**。

### 2.1 成本预算（先算账，D-17）

**首选本地 Ollama，API 成本 = 0。** 本机实测有 `qwen3.5:0.8b` / `qwen3:14b` / `qwen3:0.6b`。

⚠️ **若本地模型质量不过 T3 那一关，才升级到 `qwen3.6-plus`**（CI 那把 secret 现用的就是它）。
届时的账**先算再跑**：317 个字段按 20 条一批 ≈ 16 次调用，
每次约 800 in / 400 out ⇒ 合计约 **13k in / 6.4k out**。
**这是那次 13.6 万的十分之一，可承受** —— 但**只在本地不够时才花**。

### 2.2 T1 · 覆盖率 = **100%**（317/317 每个字段都有一条），且**逐条指回真实字段**

硬约束 ④ 用在术语层自己身上：一条指不回真实字段的术语，是纯粹的噪音。

### 2.3 T2 · 生成的 label **≥ 95%** 含中文字符，且**不等于**英文原 label、**不等于** fieldname

这三条都是**规则面**（硬约束 ③），不许交给模型判。

### 2.4 T3 · 🔴 决定性一格 · **15 条「不该错」清单**，预测 **≥ 13/15 命中**

**清单与可接受关键词在跑之前写死如下**，事后不许调：

| 字段 | 可接受的 label 里必须含以下任一 |
|---|---|
| `Item.item_code` | 编码 · 编号 · 代码 · 料号 |
| `Item.item_name` | 名称 |
| `Item.stock_uom` | 单位 |
| `Item.description` | 描述 · 说明 |
| `Item.brand` | 品牌 |
| `Work Order.qty` | 数量 |
| `Work Order.produced_qty` | 数量 · 产量 |
| `Work Order.status` | 状态 |
| `Work Order.company` | 公司 |
| `Work Order.source_warehouse` | 仓库 |
| `Stock Entry.posting_date` | 日期 |
| `Stock Entry.from_warehouse` | 仓库 |
| `Stock Entry.to_warehouse` | 仓库 |
| `Stock Entry Detail.qty` | 数量 |
| `Stock Entry Detail.uom` | 单位 |

⚠️ **这是一条弱判据，说清它弱在哪**：它验的是「有没有离谱地错」，
**不是「翻译得好不好」**。后者没有规则面的判法 —— 而**我不打算让模型判自己**（硬约束 ③）。
⇒ **术语层的质量上限由这条判据决定，且这个上限是低的。这句话必须写进 owner doc。**

### 2.5 判定标准（事先写死）

| 组合 | 判定 |
|---|---|
| T1 ✅ 且 T2 ✅ 且 **T3 ≥ 13/15** | 术语层 v0 可用，**P2.7 过** |
| T3 **< 13/15** | **换模型重跑**（先本地大一档，再考虑 `qwen3.6-plus`，且先算账）；仍不过则本项不过 |
| T1 或 T2 ❌ | 生成流程有问题，修完重跑 |

⚠️ **不得靠调可接受关键词去凑 T3。** 清单在 §2.4 写死并先于生成提交，git 可查。

---

## 3. Goals / 4. Non-Goals

- 交一份能指回真实字段的术语层 + `pytest tests/i18n -q` 退 0
- **不做**整站翻译 · 不做语言切换 UI · **不建 `tests/gates/**`**（红线 1）· 不改 masterplan（红线 5）

## 5. Task Route

**app-layer implementation**。Owner doc：`view-dsl-and-eval.md` §10。
Skill: `superpowers:test-driven-development`。

## 6. Prereqs

- 本地 Ollama 在跑（实读：是）
- `/tmp/schema.json`（活站点导出）

---

## 7. Execution Plan

### Phase 1 — 判据先行 · Status: completed
- [x] `Add` `tests/i18n/`：覆盖率 · 指回真实字段 · 含中文 · 不等于英文/fieldname · 15 条清单
- [x] `Proof` 此时必须红（术语层还不存在），记退出码

### Phase 2 — 生成 · Status: completed
- [x] `Add` 生成器（本地 Ollama，批量，可复跑）
- [x] `Proof` T1 / T2 / T3 逐条记数

### Phase 3 — 接进渲染器 · Status: completed
- [x] `Add` `view_plan` 把术语层的中文名交给渲染器，表头显示中文
- [x] `Proof` 活体：工人看到的表头是中文（`pytest -m live tests/render -q` 仍退 0）

### Phase 4 — 变异与落账 · Status: completed
- [x] `Proof` 变异：把「指回真实字段」的校验改成恒真 / 把「含中文」改成恒真 —— 各须有判据红
- [x] `Add` owner doc · `docs/logs/` · roadmap

---

## 8. 已知风险（事先登记）

| 风险 | 处置 |
|---|---|
| **判据只能验「没离谱错」，验不了「翻译得好」** | §2.4 已写死这条自白，且要求写进 owner doc。**不掩饰上限低** |
| 我既定清单又判结果 | 清单**先于生成提交**，git 可查；且可接受关键词只列客观词（数量/仓库/日期这一类），不含风格判断 |
| 小模型可能胡编字段含义 | T3 就是抓这个的；不过就换模型，**且换之前先算账**（§2.1）|
| 术语层错了比没有更糟 | 硬约束 ④ 的执行体是 T1（逐条指回真实字段）；**指不回的一律不入库**，不是「先留着再说」|

## 9. Draft Review Record

- 2026-08-27 · 起草（solo）。不触保护区，无 source-of-truth 冲突 ⇒ solo cold-replay 可用。
  **局限：设计只有作者一人读过。**

## 10. Closure Gates

- [x] T1–T3 逐条有「预测 / 实测 / 吻合与否」（§12.2，三条全中）
- [x] `pytest tests/i18n -q` **退 0 · 12 passed**（§12.1）
- [x] Phase 1「先红」有退出码记录（退出码 2）
- [x] 变异 8 条逐条有记录（§12.5）；⚠️ **M6 没见血，容错边界已量清**
- [x] owner doc 已更新，**且写明这套判据的上限**

## 11. Deferred But Adjudicated

### 量术语层对 schema 检索的影响（P2.0R 的 bonus）
Spike 07 §6.3 说术语层是检索质量的前置条件，**但那是建议不是实测**。
本轮生成完之后**顺手量一次**（向量已缓存，成本≈0），
**但它不参与 P2.7 的过/不过**，且**不构成 P2.0R 的选路**（D-16：要选路得另起一轮、预测先落盘）。

### 整站 6350 个字段
**重开条件**：路线 C 推广到第二个角色时，随那一轮一起扩，**且先算账**。

### 术语层进 AgenERP 自有表
**重开条件**：P2.4 开工。v0 的 JSON 产物即为其输入。

## 12. Closure

> 执行 2026-08-27 · 预测与 15 条清单落在 `a080f15`（生成之前），结果落在本提交。
> **API 成本 = 0**（全程本地 Ollama）。

### 12.1 验收命令与退出码

```
python3 -m pytest tests/i18n -q          →  退出码 0 · 12 passed     ← WBS §5 那条
python3 -m pytest -m live tests/render -q →  退出码 0 · 16 passed（表头已是中文）
python3 -m ruff check agenerp tests       →  退出码 0
```

### 12.2 逐条对照（T1–T3）

| # | 预测 | 实测 | 吻合 |
|---|---|---|---|
| **T1** | 覆盖率 = 100%，且逐条指回真实字段 | **317/317 = 100.0%** | ✅ |
| **T2** | ≥ 95% 含中文、不抄英文、不抄 fieldname | **317/317 = 100.0%** | ✅ |
| **T3** | 15 条「不该错」**≥ 13/15** | **15/15** | ✅ |

### 12.3 🔴 中途换过一次模型 —— 而**换模型是判定表指的，不是我挑的**

第一版用 `qwen3.5:0.8b`（本地，$0）。T1/T2/T3 **全过**，
**而术语层里躺着实打实的错**：

| 字段 | 0.8b 起的名 | 实际含义 |
|---|---|---|
| `Item.has_batch_no` | 「批次号」 | 它是**勾选框**，含义是「是否批次管理」 |
| `Stock Entry Detail.t_warehouse` | 「仓库名称」 | 目标仓库 |
| `Item.item_code` | 「项目代码」 | 制造业语境该是**物料**；「项目」= Project |

**T3 全中却漏掉了这些** —— 因为「项目代码」含「代码」、「仓库目标」含「仓库」，
我的关键词全放行了。⇒ **判据绿 ≠ 判据在测它名字说的那件事，发生在我自己刚写的判据上。**

**处置（补判据，不改判据）**：`Check` 是**规则能覆盖**的一整类 ——
它的值只有是/否，列名就该读成是非说法。补了
`test_a_checkbox_field_reads_as_a_yes_no_question_not_as_a_noun`，
当场红：**52 个 Check 字段里只有 38.5% 读成了是非问句。**

随后往 prompt 里补两条**领域事实**（Check 是勾选框 / 这是制造业 ERP，Item = 物料）——
**0.8b 跟不动，T3 反而跟着掉了**（Check 比例 38.5% → 34.6%，T3 也红）。
⇒ 命中 §2.5 事先写死的那一格：**「T3 < 13/15 ⇒ 换模型重跑，先本地大一档」**。

换 `qwen3:14b`（仍是本地，**$0**）后：

| 字段 | 0.8b | **14b** |
|---|---|---|
| `Item.has_batch_no` | 「批次号」❌ | **「是否批次管理」** ✅ |
| `Item.item_code` | 「项目代码」⚠️ | **「物料编码」** ✅ |
| `Stock Entry Detail.t_warehouse` | 「仓库名称」❌ | **「目标仓库」** ✅ |
| `Item Default.expense_account` | 「费用账户」 | **「默认费用科目」**（会计术语） |

### 12.4 另外两处执行期修的，都修在**生成器**这一侧，判据一个字没动

1. **覆盖率卡在 218/317 = 68.8%** —— 小模型把提示里的分隔符一起抄回来
   （`是否成品 | Is Finished Item`），被「过长」规则丢掉。
   **中文答案本身是对的** ⇒ 那是**解析问题不是质量问题**，加了 `clean()`。
2. **20/317 条带单据名前缀**（`工单工序.工作站`）。
   同样先补判据（`test_no_term_carries_a_doctype_prefix`）再修生成器。

另有一处**重试而非加规则**的选择，理由写在代码里：剩下的失败形态是
「中文 English」用**空格**分隔，按 ASCII 切会误伤「BOM 编号」这类合法答案；
本地重试成本≈0，而误伤是不可见的。

### 12.5 变异验证（D-26）

基线 `退出码 0 · 12 passed`。**变异的是产物与产品代码，不是判据** ——
术语层的产物本身就是交付物，对它下手是对的。

| 变异 | 改了什么 | 退出码 | 结果 | 复原后 sha256 相同 |
|---|---|---|---|---|
| **M1** | 删掉 10 条术语（覆盖率不再是 100%） | `1` | 1 failed, 11 passed | ✅ |
| **M2** | 把 30 条换成英文（不含中文） | `1` | 1 failed, 11 passed | ✅ |
| **M3** | 把 Item.has_batch_no 改回「批次号」（Check 起成名词） | `1` | 1 failed, 11 passed | ✅ |
| **M4** | 加一条指不回真实字段的 | `1` | 1 failed, 11 passed | ✅ |
| **M5** | 把一条改成带单据名前缀 | `1` | 1 failed, 11 passed | ✅ |
| **M6** | 把 Item.item_code 改成一个不着边的词 | `0` | 12 passed | ✅ |
| **M7** | 把术语抄成 fieldname | `1` | 1 failed, 11 passed | ✅ |
| **M8** | view_plan 不再交术语层（表头退回英文） | `1` | 2 failed, 14 passed | ✅ |

**全部复原后**：`terms.zh.json` `384e994be7d2cbff…` ·
`i18n/__init__.py` `800db18f7d50e669…` ·
`serve/app.py` `641918873086bbc5…` —— 三个都逐字节相同。

#### ⚠️ M6 没见血，原因量清楚了

把 `Item.item_code` 改成「东西」→ **12 passed，退出码 0。**
因为 T3 的阈值是 `≥ 13/15`，**刻意留了 2 条容错**。补了 M6b 把边界量出来：

| 改错几条 | 退出码 |
|---|---|
| 1 条 | **0（绿）** |
| 2 条 | **0（绿）** |
| **3 条** | **1（红）** |

⇒ **容错正好是设计的那样。** 这里**不事后收紧阈值** —— 看到结果再改阈值就是调判据。
**把容错量出来写在这里，让读者自己知道这条判据能挡到哪。**

### 12.6 P2.0R 的 bonus（§11）：**这一轮量不了，说清为什么**

术语层的范围是**路线 C 的 317 个字段**，而 P2.0R 的评测集打的是**整站 6,350 个字段**
里的目标字段 —— 两者几乎不重叠。**在这个范围上量出来的数没有意义。**
⇒ 转入 §11，重开条件：术语层扩到整站范围之后再量。

### 12.7 局限（不掩盖）

1. 🔴 **判据验的是「有没有离谱地错」，不是「翻译得好不好」。** §12.3 用一次真实的
   模型更换证明了这个上限有多低：**T3 全中的同时，`has_batch_no` 是错的。**
2. **只覆盖车间工人那 317 个字段**，整站 6,350 个未做（§11）。
3. **仍有可挑剔的译法**：`Item Reorder.warehouse` → 「仓库名称」（它其实是补货请求指向的仓库）。
   规则面抓不到这一类。
4. 我既定清单又判结果（缓解：清单先于生成提交，`a080f15`）。
5. solo cold-replay（§9）。

