# 2026-08-25-1118-1 `gates-l2-live` 间歇红 —— 取证、定位、修到连续 3 次全绿

> Plan Status: deferred
> Mission: p1-insight
> Work Item: 10b（`02-WBS.md` 的 `P1.8a-fix` 行）
> Last Reviewed: 2026-08-26
> Source: 人 2026-08-25 从 P1.8a 拆出的 `P1.8a-fix` 行（commit `82a144a`，标题逐字「给 loop 解锁」）+ `STATE.md` §3 `[open] 2026-08-25T11:15Z`
> Related: `2026-08-25-1423-1-explain-service-compose-and-same-origin.md`（交付了本缺陷所在的那一跳）· `2026-08-25-1615-1-desk-injection-seam-and-asset-route.md`
> Audit: required
> 🔴 **执行记录（2026-08-26，`2026-08-26-094345-mission-driver`）：`Plan Status` 由 `active` 转 `deferred`。**
> **Phase 1 全部完成**（取证件在 `docs/evidence/p1-8a-fix/`，机制陈述成立）；
> **Phase 2 完成并以停机收尾** —— 唯一能修掉机制陈述的候选是「改断言体的 `TIMEOUT = 30`」，**落在红线 1**
> ⇒ 按 `P2-1` 的规矩不进优劣比较，直接进**停机分支 A**，`Plan Status` 依 Phase 2 `Exit Criteria` 转 `deferred`，
> 已往 `STATE.md` §3 追加 needs-human。**Phase 3 未执行、未打勾，一个字节的修法都没落地。**
> **详见 `## Execution Record（2026-08-26）` 与 `docs/architecture/module-boundaries.md` §7.21 `D-b-9`。**
> 🆕 **第 7 轮独立评审（2026-08-26，`2026-08-26-094345-mission-driver`）：`Plan Status` 已由 `draft` 转 `active`。**转 `active` 的两个合取条件在第 6 轮已满足，本轮独立复跑复核属实（命令原文与输出见 `## Draft Review Record` iteration 7）。
> 🔴 **但本轮又抓到两条阻塞，均已就地改进**：① **人在 `7a217a2`（author `lize`，2026-08-25 21:27:13）把本工作项的缺陷描述整个换掉了，并逐字「交接给 loop」，而 plan 里一个字都没有** ⇒ 已新增 **B7**，并传播进 `P1-2` / `P1-5` / `P1-6b` / Phase 1 `Exit Criteria` / `P2-1`；② **B1 第四次被 CI 追上**（现为 3 红 5 绿 / 样本 8 个，连绿已达 **5** 次）⇒ 已就地改准并传播至 P3-6 / §8 / 裁判规则 4 / 停机分支 F。
> ✅ **Review Hold 已于第 6 轮独立评审实测解除 —— 人答了，答的是「归 loop」。以下整段 Hold 原文保留、不改写（本 plan 惯例），但自 2026-08-25 起已被本条取代，不再是有效约束。**
> **实测证据（第 6 轮逐条复跑，命令原文与输出见 `## Draft Review Record` iteration 6）**：
> ① `git log -1 --format='%H %an <%ae> %ad' --date=iso f144475` ⇒ `f1444757…` **`lize <lize-agent-engineering@users.noreply.github.com>`** `2026-08-25 20:45:24 +0800`
> —— ⚠️ **author 是人，不是 loop 的 `Email Agent Developer <developer@mailagent.com>`**（若是后者即为自行消解冲突、红线 5，本轮特意先验这一格）。
> ② `git show f144475 -- docs/masterplan/02-WBS.md` ⇒ `P1.8a-fix` 那一行的状态源 **`人` → `` `MD:p1-explain` ``**（该 diff 只动这一格，行内其余文字一字未改）。
> ③ `sed -n '88p' docs/masterplan/02-WBS.md` 的行尾一格现为 **`` `MD:p1-explain` ``**，与 P1.1–P1.8b 九行同形。
> ④ `git show f144475 -- docs/masterplan/DECISIONS.md` ⇒ **零输出**（无新增 `R-x`、无已有行被改，红线 3 未被触碰）。
> ⑤ `STATE.md` §3 新增 `[resolved] 2026-08-25T12:45Z`，标题逐字「**答复 loop 的 `§0` source-of-truth 冲突（`[needs-human] 2026-08-25T11:18Z`）—— 是人填错了，不是规则冲突**」，正文逐字「**冲突消解，plan 可以出 `draft` 了**」；
> 同节 `[open] 2026-08-25T12:55Z` 另逐字「**今晚的处置：不等这条，改推 P1.8a-fix … loop 应先做 `10b`**」。
> ⚠️ 🔴 **给第 7 轮及以后的方法论警告 —— 第 5 轮那条自查命令会给出假阴性**：人是按红线 5 **追加**一条 `[resolved]` 来答的，**`STATE.md:914` 那条 `[needs-human] 11:18Z` 原文一个字未动、至今仍写着 `[needs-human]`**。
> ⇒ **只跑 `grep -n '11:18Z' docs/masterplan/STATE.md` 再看状态词，会读成「仍无人答复」。正确的判法是看有没有一条后来的条目点名答复它** —— 本轮的判据是 §3 那条 `[resolved] 12:45Z` + `02-WBS.md:88` 那一格的实际取值 + `f144475` 的 author。
>
> ~~Review Hold: `§0` 那条 source-of-truth 冲突只有人能裁 —— `STATE.md` `[needs-human] 2026-08-25T11:18Z` 至今**无人答复**。⇒ **本 plan 保持 `draft`，一行不执行。**评审可改的都改了，改不动的就是这一条。~~
> 🔴 **第 4 轮评审那句「人还没有机会答」是错的，2026-08-25 mission-driver 起草步实测改正**（原文逐字：「该条由最近一个 commit `d6c0fa2` 产出，而最后一个人侧 commit `7af5493` 在它之前 ⇒ 人还没有机会答」）。
> **实测**：`git log --date=iso` ⇒ `d6c0fa2`（loop，产出该 needs-human）`20:07:12` → **`cc205d6`（人 `lize`，`20:12:26`）** → `caa051e`（第 4 轮评审，`20:13:21`）。
> ✅ **第 5 轮评审（`2026-08-25-203256-mission-driver`）独立复跑同一条命令，结论一致**；并实测 `grep -n '11:18Z' docs/masterplan/STATE.md` ⇒ **`[needs-human] 2026-08-25T11:18Z` 仍在、仍无人答复** ⇒ **`Review Hold` 今天依然成立，本 plan 仍保持 `draft`。**第 5 轮改的 5 主要 4 次要**全部是传播缺口与格式不合规，无一条动摇本 Hold**。
> ⇒ **人在该条产出之后确有一次提交，而且改的正是 `P1.8a-fix` 那一行本身**（`02-WBS.md` + roadmap 各 1 行，收严验收），
> **却没有动那一行的「状态源」，仍是单写 `人`。**⚠️ **这是新事实，不是裁定** —— 它既不证明「归 loop」也不证明「归人」，
> **本 plan 一如既往不自行消解 `§0` 的冲突**（红线 5 / Minimum Rule 13）。改正的只是「人没机会答」这个**已被证伪的前提**：
> 人有过机会，且那次机会里没有答这一条。**结论不变：保持 `draft`。**
> 🔴 **上面这三段（第 4 轮原文、第 5 轮复核、起草步改正）整体已被本节顶部的 ✅ 条取代** —— 它们记的是「人还没答」那个阶段的事实，**在 `f144475`（人，`20:45:24`）之后不再成立**。原文按本 plan 惯例保留，不改写历史。
> ⚠️ **仍然为真、且必须继续读的只有一句**：`cc205d6`（人，`20:12:26`）那一次确实**没有**动状态源 —— 它收严的是验收，不是归属。**归属是 `f144475` 才动的。**

## 0. 授权链（先写这一段，因为它决定本 plan 该不该存在）

- **预算**：工作项 10b 是**人**在 `02-WBS.md` 新拆的一行（`82a144a`，author `lize`），roadmap 逐字「**plan 预算独立计**，不占工作项 10 的额度」⇒ 表规 3 的额度是 **0/2**，本 plan 用掉第 1 格。**这不是 loop 自行加行**（红线 5）。
- ✅ **🔴 那处 source-of-truth 冲突已由人裁定消解（`f144475`，author `lize`，2026-08-25 20:45:24）—— 裁定结果：`人` 是笔误，这一行归 loop。**
  **本条是第 6 轮独立评审就地补的现行结论；下面那条 🔴 及其四个子条是冲突尚未消解时的原文，按本 plan 惯例保留、不改写，但已不再是有效约束。**
  - **现行事实**：`02-WBS.md:88` 的状态源现为 **`` `MD:p1-explain` ``**（与 P1.1–P1.8b 九行同形；`:90` P1.9 仍是 `人`）。`git show f144475 --stat` ⇒ 只动 `02-WBS.md`（1 行）与 `STATE.md`（+6 行，纯追加），`DECISIONS.md` 零改动。
  - **人给出的理由，逐字**：「我建 `P1.8a-fix` 行时把「状态源」填成了 `人`。**本意是让 loop 去修那个间歇缺陷**（整行的措辞、验收、以及 STATE 里那条解锁通知都是这个意思），但状态源那一格填错，语义就成了「这是人的活，loop 别碰」。」⇒ **下面 ① 那条「`人` 单写是刻意的」的推断，被人本人否掉了。**
  - ⚠️ **同时要照实记的一条**：人在同一条 `[resolved]` 里逐字确认「**loop 判得对**」—— 拒绝动一个状态源为 `人` 的工作项，正是红线 5 / Minimum Rule 13 要求的行为。⇒ **本 plan 前五轮停在 `draft` 不是空转，是判对了。**
  - ⚠️ **`MD:p1-explain` 这个名字与 `missions/` 下的文件对不上**（`missions/` 只有 `p1-insight.json`，无 `p1-explain.json`；本 plan 头部是 `> Mission: p1-insight`）。**这不是本行引入的**，P1.1–P1.8b 九行全是这个写法，而工作项 10（P1.8a）正是本 mission 交付的。⇒ **归属不因此含糊**（人已在 STATE `[open] 12:55Z` 逐字写「loop 应先做 `10b`」），**本 plan 不据此重开归属问题**，只在 `Deferred But Adjudicated` 登记一条 watch-only。
- ~~🔴 **一处 source-of-truth 冲突，本 plan 不自行消解 —— 它是本 plan 转 `active` 的前置。**~~（**已被上一条取代**）
  `02-WBS.md:88` 该行的**状态源逐字是 `人`**，不是 `MD:p1-insight`。起草时曾把它读成「`人` 只指状态词的裁定权」，**独立评审把这个读法否掉了，依据是本轮实读的两条**：
  - **① 这张表自己有「联合归属」的写法，而人没用它。**`02-WBS.md:79`（**P1.0** 行 —— ⚠️ 不是 P1.0a，那在 `:78`、状态源是 `MD:p1-insight`）状态源逐字是 **`人 + \`MD:p1-insight\``** —— 人要表达「人和 loop 各管一半」时**写得出来也确实写了**。`:88` 只写 `人`，与 `:90`（P1.9，loop 已自裁「不是 loop 的面」）**同形**。⇒ 按表规 2 的字面，`人` 单写是**刻意**的。
  - **② loop 自己的先例正好相反。**`STATE.md` 逐字「⑤ **P1.9 不是 loop 的面**：`02-WBS.md:89` 的状态源逐字是 `人`」（⚠️ **那条先例引的行号本身是错的** —— `:89` 其实是 P1.8b（`MD:p1-explain`），P1.9 在 `:90`；**但它的论证不依赖行号，只依赖「P1.9 的状态源是 `人`」，而那是真的**）—— 同一个判据，上一轮用来把工作项**排除**出 loop 的面。**本 plan 不能反过来用它把自己包含进来。**
  - **与之相反的证据同样是实读的、同样有力**：commit `82a144a` 标题逐字「**给 loop 解锁**」· 正文逐字「表规 3 逐字『超预算只能由人在 `02-WBS.md` 拆行/加行』，**这一行就是那个拆**」· `STATE.md` §3 该条逐字「**本条是给 loop 的解锁通知，不是待办**」· roadmap `10b` 逐字「**plan 预算独立计**」。
  - ⇒ **两侧都是人写的，且互相矛盾。** 表规 2 的字面说「归人」，同一次提交的三处散文说「给 loop」。**这正是 Minimum Rule 13 与 `AGENTS.md` 点名的「source-of-truth 冲突」，逐字要求「须人/独立评审裁定，否则保持 open」。**
  - **处置**：已往 `STATE.md` §3 追加一条 `[needs-human]`（只追加，红线 5），问题写死为「**`02-WBS.md:88` 的状态源 `人`，是不是笔误？本行的实现工作归不归 loop？**」。**本 plan 在人答之前停在 `draft`，一行不执行。**⚠️ **不自批的理由不是谨慎，是算术**：本 plan 的验收要烧掉相当一部分模型额度（§8），**把额度花在一份自己都写着「可能作废」的 plan 上，是拿真额度赌一个只有人能答的问题。**
  - ✅ **人已按这个问法逐字答复**（`STATE.md` §3 `[resolved] 2026-08-25T12:45Z`）：**「是笔误」+「归 loop」**。⇒ 本子条的「停在 `draft` 等人答」已履行完毕并结清。**留下的额度论证仍然成立且仍然承重** —— `§8` 的地板价 ≈22 次真解释未因本次裁定改变，`P1-0` 的探路闸与停机分支 C 一个字不放宽。

## Current Baseline

**以下每一条都是本轮实读/实跑得来的，不转述前序 plan。**

### B1. 缺陷的对外形态（`gh` 实取，非转述）

- `gh run list --workflow=gates.yml` 实取：`gates-l2-live` **连续 16 次 `failure`**（run `32820284450` / sha `f09b8f0` 起，至 `32841381171` / sha `82a144a` 止），**随后 run `32841842429`（sha `7af5493`，event `push`）转 `success`**。⚠️ **`7af5493` 就是当时的 `HEAD`** ⇒ **本 plan 一行还没写，仓库当前状态上已经有一次绿。**
  - 🔴 **2026-08-25 mission-driver 起草步实取，两处就地改准（本 plan 仍一行未执行）**：
    ① **「`7af5493` 就是当前 `HEAD`」已过期** —— `origin/main` 现为 **`cc205d6`**（其后 `caa051e` / `b02fd7a` / `25ba01d` 三个 loop 文档提交**尚未 push**，因此未触发 run，`gh run list` 里查无此三者 —— ⚠️ **第 5 轮评审实测把「两个」改准为「三个」**，`gh run list --workflow=gates.yml -L 6` 复取确认最新 run 仍是 `32846332776` / `cc205d6`）。
    ② **⚠️⚠️ 又绿了一次，而修复仍然一行没写** —— `gh run list --workflow=gates.yml` 实取 **run `32846332776`（sha `cc205d6`，event `push`，`2026-08-25T12:12:32Z`）`success`**；
    `gh run view 32846332776` 逐 job 实读，`L2 全量 live 判定（全部门禁，零 skip）` = **`success`**（14 个 job 全 `success`）。
    ⇒ ~~**同口径切分（`e3afd77` 及其之后）现为 `e3afd77` 红 · `758b7bc` 红 · `82a144a` 红 · `7af5493` 绿 · `cc205d6` 绿 = 3 红 2 绿，样本 5 个**~~ 🔴 **第 6 轮：本句已被下方那条 🔴🔴 子条取代，现行是 3 红 3 绿 / 样本 6 个 —— 不要在这里取数。**（此前记 3 红 1 绿 / 样本 4 个，**原文保留在下方那条 ✅ 子条里，不改写** —— ⚠️ **第 5 轮评审改正一处指错**：原文写「上一子条」，实际那段在**后面**，而它还带着一句无限定的「Phase 1 的一切计数以这个切分为准」⇒ 顺读的执行者会先看到本条的 5 个样本、再被后面那条 4 个样本覆盖。**已就地标注该条被本条取代**）。
    ⇒ **间歇性进一步坐实**，且 **`cc205d6` 那次绿正是人在同一次提交里收严验收所针对的形态** —— 人的 commit 正文逐字「攒够三次运气就能伪装成『修好了』」，
    而**它自己那一次 push 就贡献了第 2 次运气**。⚠️ **两次绿都在修复提交之前，按收严后的验收一次都不计**（P3-6 绑定 1）；
    **但两次都必须作为取证素材进 Phase 1 的逐 run 表**（P1-2）。**距离「三次运气」只差一次 —— 这正是 P3-6 那**五条**防刷绑定与停机分支 F 存在的理由。（⚠️ **第 5 轮评审改正**：本句原写「四条」，是第 2→3 轮加绑定时的重编号残留在本条上的复发，P3-6 实列**五条**。）**
  - 🔴🔴 **第 6 轮独立评审实取 —— 那「差的一次」已经补上了：同口径现为 3 红 3 绿、样本 6 个，而修复仍然一行没写。本条取代上方 ② 的一切计数。**
    **命令原文**：`gh run list --workflow=gates.yml -L 10 --json databaseId,headSha,conclusion,event,createdAt,status`
    **新增的那次绿**：run **`32849370852`**（sha **`f144475`**，event `push`，`2026-08-25T12:45:29Z`）`success`；`gh run view 32849370852 --json jobs` 逐 job 实读 ⇒ **`L2 全量 live 判定（全部门禁，零 skip）` = `success`**（14 个 job 全 `success`）。
    ⇒ **同口径切分（`e3afd77` 及其之后）现为：`e3afd77` 红 · `758b7bc` 红 · `82a144a` 红 · `7af5493` 绿 · `cc205d6` 绿 · `f144475` 绿 = 3 红 3 绿，样本 6 个。**
    ⚠️ **还有第 7 个正在跑**：run `32850335965`（sha `f924ac6`，event `push`，`2026-08-25T12:55:41Z`）`status = in_progress`、`conclusion` 为空 ⇒ **本轮不计入任何一侧**，执行期首次取数时必须复取它的终态。
    🔴🔴🔴 **该终态已被 mission-driver 起草步在第 6 轮评审交回后当场复取（`2026-08-25-203256-mission-driver`，非评审者本人）—— 它是 `success`。本条是 B1 现行的唯一权威计数，取代上方一切数。**
    **命令原文**：`gh run list --workflow=gates.yml -L 9 --json databaseId,headSha,conclusion,event,status,createdAt`
    **输出逐字**：`32850335965 f924ac6 push status=completed concl=success 2026-08-25T12:55:41Z`
    ⇒ **同口径切分（`e3afd77` 及其之后）现为：`e3afd77` 红 · `758b7bc` 红 · `82a144a` 红 · `7af5493` 绿 · `cc205d6` 绿 · `f144475` 绿 · `f924ac6` 绿 = 3 红 4 绿，样本 7 个，且那 4 次绿是连续的。**
    🔴 **上方 🔴🔴 子条那句「三次连绿」与「+1 in_progress 未计」自本条起过期；「3 红 3 绿 / 样本 6 个」同样过期。原文按本 plan 惯例保留不改写，但不再是计数依据。**
    ⚠️ **这一次过期只花了不到 10 分钟** —— 第 6 轮评审取数时它还在跑，交回报告时它已经绿了。⇒ **B1 是本 plan 最易腐的一节，执行期 `P1-2` 首次取数必须重取，不许照抄本文件任何一个数**（这正是本节反复出现「权威计数被取代」的原因，不是笔误）。
    🔴 **对 P3-6 绑定 1 的影响是加重不是减轻**：需排除的「修复之前的绿」由三次变**四次**（`7af5493` / `cc205d6` / `f144475` / `f924ac6`），且**连续绿已达 4 次 > 验收要求的 3 次** ⇒ 「零修复直接结案」的口子比第 6 轮记的更宽，绑定 1、绑定 5 与停机分支 F **更不得放宽**。
    🔴 **这不是「又多一个样本」，这是本 plan 预言的那个失败形态今天已经发生**：人在 `cc205d6` 的 commit 正文逐字警告「**攒够三次运气就能伪装成『修好了』**」，而 **`7af5493` / `cc205d6` / `f144475` 三次连绿现在已经躺在 `main` 上，三次全在任何修复提交之前，三次全是纯文档 push 触发的。**
    ⇒ **若没有 P3-6 绑定 1（三个 sha 必须是修复提交的后代）与绑定 5（机制陈述必须成立），本 plan 今天就能不写一行修复直接宣布达标。** 这两条绑定与停机分支 F **从「预防性」变成「已被实测触发的必需品」，任何后续轮次都不得以「太严了」为由放宽。**
    ⚠️ **对 `§8` 的额度账不产生修正**：这三次绿都不是本 plan 花的，`≈22` 次真解释的地板价（P1-6 的 8 + P3-6 的 6 + 本机 2 + 自身 push 的 6）**一个数不变**。
  - 🔴🔴🔴🔴 **第 7 轮独立评审（2026-08-26 09:45 实取）—— B1 第四次被 CI 追上。本条取代上方一切计数，是 B1 现行的唯一权威计数。**
    **命令原文**：`gh run list --workflow=gates.yml -L 12 --json databaseId,headSha,conclusion,event,status,createdAt`
    **新增的那次绿**：run **`32853424473`**（sha **`7a217a2`**，event `push`，`2026-08-25T13:27:19Z`）`success`；`gh run view 32853424473 --json jobs` 逐 job 实读 ⇒ **`L2 全量 live 判定（全部门禁，零 skip）` = `success`**（14 个 job 全 `success`）。
    ⇒ **同口径切分（`e3afd77` 及其之后）现为：`e3afd77` 红 · `758b7bc` 红 · `82a144a` 红 · `7af5493` 绿 · `cc205d6` 绿 · `f144475` 绿 · `f924ac6` 绿 · `7a217a2` 绿 = 3 红 5 绿，样本 8 个，且那 5 次绿是连续的。**
    ⚠️ **还有第 9 个正在跑**：run `32920159448`（sha `fecbd59`，event `push`，`2026-08-26T01:45:16Z`）`status = in_progress` ⇒ **本轮不计入任何一侧**，执行期首次取数必须复取其终态。
    🔴 **上方 🔴🔴🔴 子条那句「3 红 4 绿 / 样本 7 个 / 4 次连绿」自本条起过期。**原文按本 plan 惯例保留不改写，但不再是计数依据。
    🔴 **对 P3-6 绑定 1 的影响继续加重**：需排除的「修复之前的绿」由四次变**五次**（`7af5493` / `cc205d6` / `f144475` / `f924ac6` / `7a217a2`）。⚠️ **这一格从第 4 轮起每一轮都在长（1→2→3→4→5）** ⇒ **执行期照抄本文件任何一个数都是错的**，必须按 `git merge-base --is-ancestor` 现算。
  - 🔴 **第 6 轮改正一处已过期的推送状态**：上方 ① 写「`caa051e` / `b02fd7a` / `25ba01d` **三个** loop 文档提交尚未 push」——**实测已全部推出去了**。`git fetch origin` 后 `git log origin/main..HEAD --oneline` ⇒ **零输出**，`git rev-parse --short HEAD` == `git rev-parse --short origin/main` == **`f924ac6`**。
    ⇒ **`gh run list` 现在看到的就是全部本地提交的完整计数**，上方 ① 那句「查无此三者」已不成立（它们连同 `96a208f` / `f144475` / `0b99c00` / `f924ac6` 都已触发 run）。**这条不改变 ≈22 的地板价**，但它证实了 `§8` 那句「本 plan 自己的每一次 push 也烧额度」**不是假设，是当天连续发生了六次的事实**。
  - 🔴 **起草时把这个数写成「11 次」，且据此下过一个错结论 —— 第 3 轮独立评审实测推翻，照实改正并保留记录**：初稿写「**同一份代码**既红过 11 次又绿过 1 次 ⇒ 间歇性坐实，**且与代码无关**」。**两处都不成立**：① 实际是 **16** 次；② **这 16 次里代码变过** —— 判定步的门禁项数在streak 中途从 **52 项**（run `32820284450`：`门禁 52 项：红 6，绿 46`）变成 **54 项**（run `32838495432`：`54 项：红 2`），**项数变了就说明判据集变了**。⇒ **「与代码无关」是没有根据的**，删掉。
  - ✅ **改正后仍然成立、且才是真正该用的那段证据**：`e3afd77`（run `32838495432`）是**人修好 `AGENERP_LLM_BASE_URL` 变量名**的那一次 —— **在它之前，答案面那条红的原因是「服务恒回 503」，与本缺陷是两回事，且基本不烧 token**。⇒ **只有 `e3afd77` 及其之后的四次才同口径**：`e3afd77` 红 · `758b7bc` 红 · `82a144a` 红 · `7af5493` **绿**。⇒ **同口径下是 3 红 1 绿**，间歇性**仍然坐实**，但**样本只有 4 个，不是 12 个**。~~**Phase 1 的一切计数以这个切分为准。**~~
    🔴 **本条的「四次 / 3 红 1 绿 / 一切计数以此为准」已被上方 🔴 子条的 ② 取代（第 5 轮评审就地标注）** —— `cc205d6`（run `32846332776`）是同口径的**第 5 个**样本且为**绿** ⇒ ~~**现行口径是 `e3afd77` / `758b7bc` / `82a144a` 三红 + `7af5493` / `cc205d6` 两绿 = 3 红 2 绿、样本 5 个**~~。**本条文字按本 plan 的惯例保留不改写，但不再是计数依据。**切分点（`e3afd77`）本身未变，仍然成立。
    🔴 **第 6 轮再取代一次：「3 红 2 绿 / 样本 5 个」也已过期 —— 现行口径是 3 红 3 绿、样本 6 个（+1 in_progress 未计）**，唯一权威的计数写在上方那条 🔴🔴 子条里。**本节此后不再重复计数，只留一个指针**，避免第 3→5 轮反复出现的「同一份 B1 里两个互相矛盾的权威计数」。
  - 🔴 **这一格是本 plan 最危险的地方，写在这里以便判据能挡住它**：验收是「连续 3 次绿」，而**每次 `push` 都会触发 `gates.yml`** ⇒ 光靠提交文档就能攒出 3 次绿。**处置见 P3-6 与停机分支 F。**
- 取 run **`32839163713`**（sha `758b7bc`）的 `gates-l2-live` job **`97774627334`** 实读日志：
  - 判定步逐字 **`门禁 54 项：红 1，绿 53，跳过 0`**（与 WBS 行记的「54 项绿 53 红 1」逐字吻合）。
  - ⚠️ **但同一行 WBS 还说「`758b7bc` 红 `echoes_the_sid` **+ `resolves_to`**」（两条），而判定步自己打的是「红 1」、`FAILED` 也只列了 `echoes_the_sid` 一条。** ⇒ **WBS 行与它引用的日志对不上**，这是继 B4 之后**第二处**人写的行与活证据的分歧。**本 plan 以日志为准，并把这处分歧照实登记**（`02-WBS.md` 是人的文件，红线 5，loop 不改）。
  - `失败取证` 步（`if: failure()` + `continue-on-error`，人在 `758b7bc` 加的）把断言正文打了出来 —— **本 plan 的定位完全建立在它上面，不靠本机猜**。

### B2. ⚠️ 断言正文与 WBS 行记的「症状」之间有一处**实读出来的分歧**，本 plan 以断言正文为准

WBS 行与 STATE 都把症状记成断言消息的字面 **「`127.0.0.1:8080` 够不到（timed out）—— 同源前端没在跑」**，并据此把工作项命名为「**起栈时序缺陷**」。**实读那条 traceback 之后，这个字面不能直接当结论用**：

- 该消息是 `tests/unit/test_explain_service_body.py:133` 里**写死的一句话**，它是 `except OSError` 分支的**固定文案**，**不是运行时观测**。`OSError` 的任何子类都会印出同一句「同源前端没在跑」。
- traceback 实读到的真实异常是 **`TimeoutError: timed out`**，抛点在 `socket.py:718` 的 `self._sock.recv_into(b)`，上游帧是 `http/client.py:298` 的 `readline` ⇒ **`conn.request()` 已经成功、连接已建立**，超时发生在**等回包**（`getresponse()`）那一段。**「连不上」与「连上了但回包没在 30 秒内到」是两种失败形态，当前消息把它们印成同一句。**
- 同一次 run 的取证步复跑逐字 **`1 failed, 25 passed, 28 deselected in 64.49s`** ⇒ **同一个栈、几十秒后、25 条 live 判据（含大量打 `127.0.0.1:8080` 的）全过**。**「frontend 没在跑」与这条实测不相容。**
- 失败落点实读到具体请求：`method = 'POST'`, `path = '/agenerp/explain'`，payload `{'question': '这张单据现在什么情况？'}` ⇒ **红的是那条会真调模型的 POST，不是健康探针、不是 401 那几条。**

⇒ **本 plan 不把「起栈时序」当既定根因**（裁判规则 3：不许猜根因）。工作项名称沿用人写的那个，**但 Phase 1 的取证不得被这个名称牵着走** —— 起栈时序与「回包超时」都要被**证据**排除或坐实，不许靠命名。
🆕 **第 7 轮补一条指针**：本节这处分歧在 **B7** 有了新的一侧证据（人实读的 ps 重启指纹）。⚠️ **两侧仍未对上** —— 人写「连不上」，而本节实读的抛点是 `recv_into`、连接已建立。⇒ **三条方向（B3 回包超时 / 起栈时序 / B7 重启窗口）一律以证据判**，不许靠命名，**也不许因为「人写的更新」就默认采信**。

### B3. 一处**量纲不一致**，实读两侧数字（是方向，不是结论）

- `tools/nginx/frappe.conf.template:83-84`：`location /agenerp/` 给 **`proxy_read_timeout 300` / `proxy_send_timeout 300`**，同处注释逐字写明理由「单次解释实测 9.7 万–12.8 万 token」。
- `tests/unit/test_explain_service_body.py:99`：`TIMEOUT = 30`，`:125` 把它交给 `http.client.HTTPConnection(host, port, timeout=TIMEOUT)`。
- 🔴 **必须连着读的第三行 —— 起草初稿漏了它，独立评审抓出来的**：同一份模板 `tools/nginx/frappe.conf.template:77` 逐字写着「⚠️ 这是**服务端**读超时；断言体自己的 `TIMEOUT = 30` 是**客户端**侧，**两者别混**。」
  ⇒ **「300 与 30 不一致」这件事，前一个 plan 已经看见过并且已经裁定过了：两者是不同维度，刻意不同。**
  ⇒ **本 plan 因此不能把它当成「发现了一处不一致」。**B3 的性质是**重开一条既有裁定**，重开的理由必须写死并且只有一条：**那条裁定是在「所有 `/explain` 都在 503 分支上 0.02 秒返回」的观测下写的**（roadmap 工作项 10 逐字），**它从未面对过一次真正跑完的解释**。⚠️ **「维度不同」与「客户端那一侧的取值够不够」是两个问题，前者成立不蕴含后者成立。**
- ⇒ 除去上一条的限定之后，仍然为真的是：**反代允许 300 秒，判据自己只等 30 秒**，而 300 那一侧的注释明写理由是「解释很慢」。
- **两条红的判据恰好是唯二会发出真 LLM 解释 POST 的**：`test_the_user_in_the_answer_is_the_person_the_real_sid_resolves_to`（`:201`，一发）与 `test_no_response_through_the_front_ever_echoes_the_sid`（`:234`，循环里发两次 `EXPLAIN_PATH`，但**只有第一次是真解释** —— 第二次 `{"role": "System Manager"}` 是自带上下文，在调模型**之前**就被 400 掉了，同文件 `:254` 那条判据正是判这个）⇒ **一整轮 live 判定恰好发 2 次真解释**（`:201` 一次 + `:234` 一次），这个数进 `§8` 的额度算术。WBS 行记的「红的那条每轮不固定，就在这两条之间摆动」与「发真解释的恰好就是这两条」**对得上**。
- ⚠️ **以上仍是方向，不是结论。** 它解释得了「为什么是这两条」「为什么间歇」，但**没有实测数据**：本仓**至今没有任何一次真实解释在 CI 上的墙钟数**。roadmap 工作项 10 自己逐字记着「`proxy_read_timeout 300` **没有在一次真实的长解释上验证过**（本轮所有 `/explain` 都在 503 分支上 0.02 秒返回）」。**没有那个数，这条方向既不能被采纳也不能被排除。Phase 1 的头等任务就是把它测出来。**

### B4. 人写的「已知线索」有一半**已经不存在了**（实读 `docker-compose.yml`）

WBS 行与 STATE 的线索逐字：「`frontend.depends_on` 有 `agenerp-serve: service_healthy`，而 `agenerp-serve.depends_on` 只有 `create-site` 没有 `backend`」。实读：

- **前半句已不成立**：`docker-compose.yml:374-391` 的 `frontend.depends_on` 只有 `backend`(healthy) / `websocket`(healthy) / `bootstrap-homepage`(completed)，**没有 `agenerp-serve`**。`:387` 起有一段注释逐字「⚠️ **这里刻意没有 `agenerp-serve`** —— 曾经有过，实测是个缺陷，已删（§7.21 `D-b-8`）」。⇒ 该依赖已在工作项 10 的修复里删掉了。
- **后半句成立**：`:331-333` 的 `agenerp-serve.depends_on` 确实只有 `create-site: service_completed_successfully`。
- `tools/nginx/frappe.conf.template:70-87` 已是 `resolver 127.0.0.11` + `set $agenerp_serve_host` + 变量形式 `proxy_pass` ⇒ **`agenerp-serve` 不在时 nginx 不再 `[emerg]`，只有 `/agenerp/` 那一条回 502**（`D-b-8` 的修法已落地）。
- ⇒ **人给的线索是在旧状态下写的**，本 plan **不据它选路**，但**也不把它判死**（后半句仍是真的，`agenerp-serve` 起得比 `backend` 早这件事本身还在）。

### B5. 判据侧的红线地形（决定本 plan 能改什么）

- 门禁那份 `tests/gates/test_explain_service_live.py` 在 `tests/gates/**` 内 ⇒ **红线 1，一个字节不能碰。**
- 它 `:57` 用 `_load(...)` 把 `tests/unit/test_explain_service_body.py` 整体加载进来，`:82-87` 逐条改名导出 ⇒ **那份 `tests/unit/` 文件是这条门禁唯一的判据正文。** 该文件模块 docstring 第一行逐字自证：「🔴 WBS §4 P1.8a 那条验收（`tests/gates/test_explain_service_live.py`）的**断言体**」「**这个文件是交接件**」「人只需按路径加载本文件，**一行断言都不重写**」。
- ⇒ **改它 = 改裁判的断言。** 与 `STATE.md` §3 `[open] 11:07Z` ③ 对 `tests/tools/test_live_conformance.py` 的裁定**同构、同结论**（物理路径不在 `tests/gates/**` 字面内，**不构成豁免**）。⚠️ **`TIMEOUT = 30` 就住在这个文件里** ⇒ **「把 30 改大」这条最直觉的修法落在红线 1 内，loop 无权自批。** 处置写在 **停机分支 A**。
- `.github/workflows/gates.yml` 的 `gates-l2-live` job：**红线 2** —— 不得让门禁变松。job 内注释另有一条本仓自定的自查约定（「新文件以旧文件为行前缀」，改现有 job 会打掉那条机械判据）⇒ **本 plan 默认不改这个 job**；确有必要时按**停机分支 B** 处理。

### B6. 还缺的一格

- 本仓**没有**任何「一次真实解释在 runner 上耗时多少」的数据（B3）。
- 本仓**没有**任何 `agenerp-serve` 侧的请求耗时观测（`agenerp/serve/` 未见耗时日志；`agenerp/explain/loop.py` 有 `MAX_TURNS` 轮数闸但那限的是轮数，`:77-79` 逐字「**它**与 `MAX_TURNS` 各管一维，不许合并」——「它」是 `MAX_TOOL_CALLS`（`:91`），**不是墙钟闸**）。

### B7. 🆕 人在 loop 停机期间做了归因取证，并逐字「交接给 loop」—— **它把本工作项的缺陷描述整个换掉了**

⚠️ **本节是第 7 轮独立评审补的，第 1–6 轮的 plan 里一个字都没有**（证据产生于 `7a217a2`，2026-08-25 21:27:13，**晚于第 6 轮评审**）。**依据 Minimum Rule 1（从活基线起写、不靠旧 plan 的记忆），它必须进 `Current Baseline`。**

- **出处（实读，非转述）**：commit `7a217a2`，author **`lize`（人）**，`git show --stat` ⇒ 只改 `docs/masterplan/STATE.md`（+9 行，**纯追加**，红线 5 未被触碰）；新增条目标题逐字 **「`P1.8a-fix`（工作项 10b）的归因取证 —— 人侧在 loop 停机期间做的，交给 loop 接手」**，正文逐字「**本条是证据交接，不是裁定，也不占用 plan 预算**」。
- **① 人主动撤销了那条旧线索**，逐字：「`frontend.depends_on` 有 `agenerp-serve: service_healthy` 而 `agenerp-serve.depends_on` 缺 `backend`」「**今天已不成立**」「**别再顺着那条查**」。⇒ **与 B4 本 plan 独立实读的结论一致**，B4 不需要改；但这条方向现在是**人亲手撤的**，不再只是 loop 的判断。
- **② 新证据（人实读 CI run `82a144a` 的 `取证 —— 服务状态` 步输出，不是推断）**：同一时刻 `db` 与 `redis-*` 是 **`Up 4 minutes`**，而 `frontend` / `backend` / `agenerp-serve` / `queue-long` / `queue-short` / `scheduler` **全是 `Up 2 minutes`**，六个容器的 `CREATED` 都是 `4 minutes ago` ⇒ **整组 frappe 系容器在起栈约 2 分钟后集体重启过一次，`db` / `redis` 没有。**
- **③ 人据此给出的新缺陷描述，逐字**：「**`docker compose up -d --wait` 返回 healthy 之后，整组 frappe 容器还会再重启一次；判定步如果落进那个窗口，就会撞上连接超时**」。⇒ **它既不是 B3（回包超时），也不是原来那条「起栈时序 / `depends_on`」——是第三种机制。**本 plan 此后统一称它 **「重启窗口」**。
- **④ 人已排除两条**：**端口映射**（实读 `127.0.0.1:8080->8080/tcp` 正确；人侧一度误读成 `->80`，是 `ps` 列宽截断，已更正）· **OOM**（`grep -inE "oom|killed|exit code 137|signal 9"` 全日志零命中）。⚠️ **OOM 那条按人自己逐字标注的读**：「**零命中不等于没发生**，只是日志里没证据」⇒ **本 plan 不把 OOM 记成「已排除」**，记成「日志侧无证据」。
- **⑤ 人明写的「未查明」那一格**：**为什么会重启**（人补记 backend 日志末尾可见 `Linking fresh assets to volume...`）。逐字「⚠️ **不许据此猜根因**（裁判规则 3）」。⇒ **本 plan 继承这条限制：「重启窗口」是一条有证据的方向，不是根因。**
- **⑥ 人给的方向（人自己标注「是方向不是结论」）**：值得先答的问题是「**`--wait` 返回之后到判定开始之间，栈是否稳定**」；若答案为否，修法**可能**在「判定前加一道稳定性等待」而不在 `depends_on`。⚠️ 人同时逐字写「**但这两条都没在本仓实测过**」⇒ **该修法方向已在 `P2-1` 登记为候选 (H) 并预先判过红线归属，不留给执行期临场发挥。**
- 🔴 **必须连着读的一处分歧 —— 本 plan 不替任何一侧消解它**：人的措辞是「断言逐字 `timed out`，**是连不上**，不是 502」；而 **B2 实读的 traceback 抛点是 `socket.py:718` 的 `recv_into`、上游帧是 `http/client.py:298` 的 `readline`** ⇒ **`conn.request()` 已经成功、连接已建立**。**字面上这两句不一致。**
  ⚠️ **它们也不必然互斥** —— 容器重启期间宿主端口仍可能被 docker-proxy 监听着，于是 TCP 连得上、却等不到回包，落点正是 `recv_into`。**但这句话本身是一条待验的机制假设，不是裁定**（裁判规则 3）。⇒ **Phase 1 必须把这处分歧当判据用，不许二选一地采信任何一侧**：`P1-2` 的逐 run 表要同时记 **ps 重启指纹**与 **traceback 抛点**，两者**能对上同一条时间线**才算坐实。
- 🔴 **它同时推翻了本 plan 的一处已裁定结论（改正见 `P1-5`）**：本 plan 曾裁定「**CI 侧对『起栈时序』这条方向确实没有现成数据路径**」。**人用的恰恰就是 CI 上现成的那一步** —— `.github/workflows/gates.yml:349-351` 的 `取证 —— 服务状态`（`if: always()`，实读确认）⇒ **`docker compose ps` 的 `Up N` 与 `CREATED N ago` 之差就是一枚重启指纹**，**红 run、绿 run 都有，且取它一次模型调用都不花**。**这是本 plan 眼下最便宜、最该先跑的一格取证**（对 `§8` 那条「地板价 ≈22 而探路闸只有 6」的结构性风险，这是唯一一条能不花额度就推进的路）。

## Goals

- **G1.** 把 `gates-l2-live` 那条间歇红的**失败机制**取证到「可复述、可指名、有数字」的程度 —— 至少能逐条判定 B3（回包超时）与「起栈时序」两条方向**哪条成立、哪条被排除**，且判定依据是**实测数字**不是论证。
- **G2.** 在**不碰红线 1 / 红线 2** 的前提下把它修掉；若唯一可行修法落在红线内，则**停机并把选择权交回人**（**停机分支**），**不自批**。
- **G3.** 验收：**`gates-l2-live` 连续 3 次 run 全绿零跳过**（`02-WBS.md:88` 逐字）。**一次绿不算。**

## Non-Goals

1. **不做工作项 11（P1.8b）的任何事** —— 不做 ⌘K、不做侧边栏 UI、不建 `tests/ui/`、不引浏览器驱动。那格卡在 `STATE.md` §3 `[needs-human] 10:21Z` 的两条上，**本 plan 一个字不碰它**。
2. **不修上游模板自己那一行 `backend:8000`** —— 工作项 10 已逐字裁定「改它等于改上游文件内容、把副本与上游的差集撑大（K3）」。本 plan 沿用该裁定，**不重开**。
3. **不改 `tests/gates/**` 的任何文件**（红线 1），**`tests/unit/test_explain_service_body.py` 一个字节不改**（B5：门禁 `:57` 把它整体 `exec_module`，⇒ **追加常量 / 追加 helper / 追加一条测试也算改判据正文**，不只是「改断言或改参数」）。
4. **不让 `gates-l2-live` 变松**（红线 2）：不加 `continue-on-error`、不摘门禁出判定面、不缩触发范围、不把 live 契约的「零 skip」放宽。
5. **不改 `docs/masterplan/` 除 `STATE.md` 追加行以外的任何内容**（红线 5），**不碰 `DECISIONS.md`**（红线 3），**不写证据仓 `XM_PATH`**（红线 6）。
6. **不做「顺手优化」** —— 与本条缺陷无关的 compose / nginx / 服务改动一律不做，发现了写 `docs/backlog/`。
7. **不引任何第三方依赖**（`pyproject.toml` 预期 0 行 diff）。

## Task Route

- Type: `bug investigation`（Phase 1）→ `implementation-only change`（Phase 3）。**次序不可颠倒**：Phase 2 的裁定必须由 Phase 1 的数字喂养，不许先定修法再补证据。
- Owner Docs: `docs/architecture/module-boundaries.md` §7.21（`D-b-*` 的落点节，本 plan 的裁定续写在这里）· `docs/architecture/system-baseline.md` §14.2/§14.4（起栈与 live 判定口径）· `docs/masterplan/02-WBS.md:88`（验收正文，**只读**）
- Skill Selection Basis: Phase 1 用 `docs/skills/bug-diagnosis-prompt.md` —— 它正是「先取证再归因、禁止跳到修法」的形状，与裁判规则 3 同向。Phase 2/3 无对应 skill（是一次依赖实测数字的工程裁定 + 一处配置改动），记 `none`。

## Infrastructure And Config Prereqs

- **CI 侧**：`gates-l2-live` 需要 `secrets.AGENERP_LLM_ENDPOINT` / `DASHSCOPE_API_KEY` / `AGENERP_LLM_MODEL`（已配好，`e3afd77` 起答案面已能真调模型）。⚠️ **fork PR 拿不到 Secrets**，那种 run 上答案面走 503 —— 是预期不是故障（job 注释已写死），**3 次连绿必须取 `push` 事件的 run**。
- 🔴 **额度是硬约束，且 job 注释里那个数字本 plan 不照抄** —— 独立评审指出它至少有三处偏差，逐条记：
  - job 注释逐字「一次解释中位 **11 万 token**，百炼免费额度 1,000,000/模型 ⇒ **约 9 次 CI 就烧光一个模型的额度**」。
  - ⚠️ **偏差①：一轮 live 判定发的是 2 次真解释，不是 1 次**（B3 实读：`:201` 一次 + `:234` 一次）⇒ 同样的额度**只够一半的次数**。
  - ⚠️ **偏差②：红的那种 run 大约翻倍** —— `失败取证` 步（`gates.yml`，`if: failure()`）会把 `pytest -m live tests/gates/` **整个再跑一遍**，于是又是 2 次真解释。⇒ **一次红 run ≈ 4 次真解释，一次绿 run ≈ 2 次。**
  - ⚠️ **偏差③（起草时提出，第 3 轮评审已把它大部分化解，照实改正）**：初稿说「当天跑出 11 红 1 绿，按上面的算法早该超出 1,000,000 ⇒ 三个数里至少一个是错的」。**实测切分之后这条基本站不住**：`e3afd77` **之前**服务恒回 503（变量名错），**那些 run 根本没调到模型、不烧 token** ⇒ 同口径的真调模型 run 已有 **6 次**（B1；⚠️ **第 5 轮评审**把 4 改正为 5，**第 6 轮再改正为 6** —— 补入 `f144475` / run `32849370852` 那次绿；另有 `f924ac6` / run `32850335965` `in_progress` 未计），与「9 次烧光」并不矛盾。🔴 **但 6 次已经很贴近那条注释自己给的 9 次上限了** ⇒ **`P1-0` 读真实剩余额度这一步的紧迫性由此上升，不是下降**：本 plan 一次调用都还没花，池子却可能已被 loop 的六次文档 push 消掉大半。⇒ **偏差③降级为「已化解」，不再作为「job 注释不可信」的依据**；**偏差①②仍然成立**，总闸仍按它们现算。
  - ⇒ **处置**：`P1-0` 先去**读真实剩余额度**再花任何一次 CI；预算表 `§8` 以「**真解释调用次数**」为单位记，不以「run 次数」记。额度耗尽是**停机分支 C**。
- **本机侧**：⚠️ 宿主对外口**必须给 `AGENERP_HTTP_PORT=18080`**（`8080` 被另一个 compose 项目占着，实测会死在 `Bind for 0.0.0.0:8080 failed: port is already allocated`）。⚠️ **CI 上不设这个变量，走默认 8080** ⇒ **本机与 CI 的端口不同，复现命令不许照抄。**
- 无新增 env / 端口 / 密钥。**No infra prereqs beyond existing baseline。**

## Execution Plan

### Phase 1 — 取证：把失败机制测出来，不许归因

Status: completed
Targets: 只读取证（`gh` 日志 + 本机栈观测）。**本阶段不改 `agenerp/` `tests/` `docker-compose.yml` `tools/` `.github/` 任何一个字节**；落盘只写 `docs/evidence/p1-8a-fix/` 与 `docs/logs/`
Skill: `docs/skills/bug-diagnosis-prompt.md`

- Item Types: `Proof`（10 条里 9 条）+ `Decision`（P1-8）
- Prereqs: `§0` 的 needs-human 已由人答复且答复是**「归 loop」**。**人没答之前 Phase 1 不开工。**⚠️ **答复是「不归 loop」时的终局也写死**：`Plan Status` → **`cancelled`**，往 `STATE.md` §3 追加一行说明，**不改成别的工作项接着做**。
      ✅ **本条 Prereq 已于 2026-08-25 满足（第 6 轮独立评审实测）**：人在 `f144475`（author `lize`）把 `02-WBS.md:88` 的状态源改成 `` `MD:p1-explain` ``，并在 `STATE.md` §3 `[resolved] 12:45Z` 逐字答「是笔误 / 归 loop」⇒ **答复是「归 loop」，`cancelled` 那条出口未触发。**（依据全文见本 plan 顶部的 ✅ Review Hold 取代条。）⚠️ **满足的只有这一条 Prereq** —— `P1-0` 的额度前置、探路闸、停机分支 C 一个字不放宽。

- [x] **P1-0 `Proof` 先读真实剩余额度，再花任何一次 CI。**⚠️ **本仓没有现成的「读 DashScope 剩余额度」通道**（第 3 轮评审指出，实情如此）⇒ **两条出路，二选一并写明选了哪条**：① 由**人**在百炼控制台读一个数给回来（那是人的账号，loop 够不到）；② 读不到就**按探路闸（≤6 次）跑，跑完停在分支 C 等人**。⚠️ **「读不到就当额度充足继续跑」不是选项。**
      🔴 **照实记一条本 plan 的结构性风险**：若走 ②，而地板价是 **22** 次（⚠️ **第 4 轮改正**：原写 16，未计入本 plan 自己 push 触发的 run，见 `§8`）⇒ **Phase 1 会在拿到机制陈述之前就撞上探路闸**。**这是可能的结局，不是缺陷**，写在这里是为了让人在答 `§0` 那条 needs-human 时**知道这份 plan 很可能一开工就要人再介入一次**。（`§8` 的第 0 行）。⚠️ **这一条排在最前不是形式** —— 停机分支 C 整个建立在额度数字上，而 `Infrastructure And Config Prereqs` 已实证 job 注释里那个「9 次」至少三处对不上。**读不到真实额度就明写「读不到」**，并把 `§8` 的上限**按最保守的那个偏差**取值，**不许按 job 注释那个乐观数取值**。
- [x] **P1-1 `Proof` 先把预测写死，再跑**（roadmap 硬约束②：预测在前、结果在后、逐条吻合）。在 `docs/evidence/p1-8a-fix/predictions.md` 里**开跑前**逐字写死对 P1-2…P1-7 每一格的预期，**事后逐条对照，不许改写**。⚠️ **必须包含「若 B3 被证伪，预期看到什么」** —— 只写「我猜它会超时」是把取证写成了自我实现。
- [x] **P1-2 `Proof` 榨干已有的 run，先不花额度**：对**全部**近期 `failure` run 取 `gates-l2-live` job 日志，逐 run 记录：判定步的「N 项：红 R，绿 G，跳过 S」原文 · 取证步复跑的 `X failed, Y passed ... in Zs` 原文 · 红的判据名 · traceback 的**异常类型与抛点**（不是那句写死的中文文案）。~~**至少覆盖同口径的那六次**~~ 🔴 **第 7 轮改准：是同口径那八次**（B1 的切分：`e3afd77` / `758b7bc` / `82a144a` 三红 + `7af5493` / `cc205d6` / `f144475` / `f924ac6` / `7a217a2` **五绿**；⚠️ **第 6 轮的「六次三绿」与更早的「四次」「五次两绿」全部过期，一个都别照抄**；⚠️ **另须复取 `fecbd59` / run `32920159448` 的终态**，第 7 轮实测时它还是 `in_progress`），**并单独标出 `e3afd77` 之前那 13 次是不同口径**（服务恒回 503），**不许把两段混在一张表里数**。产出一张逐 run 表。
      - ⚠️ **判据**：若表里出现**任何一次**红在 `_request` 之外（比如连不上、502、5xx），B3 立即不足以解释全部，**必须并行查起栈时序**。
      - 🆕 🔴 **第 7 轮新增，且这一格排在表里其余任何一格之前**（依据 B7）：**逐 run 抄下 `取证 —— 服务状态` 步的 `docker compose ps` 全文**，按服务记 `STATUS` 的 `Up N` 与 `CREATED N ago` 之差 —— **那是一枚重启指纹**。该步是 `if: always()`（`gates.yml:349-351` 实读）⇒ **红 run 与绿 run 都有这份数据，且取它一次模型调用都不花。**
        ⚠️ **判据写死，两种结果都必须照实记，不许只记支持某一侧的那种**：**红 run 普遍出现 frappe 系容器 `Up` 远小于 `CREATED`、而绿 run 不出现** ⇒ 「重启窗口」**坐实**；**绿 run 同样出现** ⇒ 重启确实发生但**不足以单独解释红**，`P1-6b` 与 B3 两条线**必须继续查**，不许就此收工。
- [x] **P1-3 `Proof` 量出「一次真实解释在 runner 上要多久」**—— 这是 B6 那格空白，也是 B3 成立与否的**唯一判据**。取证步的日志里已有 `agenerp-serve` 容器最近 60 行，先从中找请求起止；不足则设计一次**最省额度**的观测。**必须给出秒数**，写不出秒数就明写「未测出」，**不许用「大概很慢」代替**。
- [x] **P1-4 `Proof` 本机把 B3 单独逼出来**（不烧 CI 额度）：起栈并配上 AI 变量，单独跑那两条会发真解释的判据，**测它们各自的墙钟**。⚠️ 端口用 `18080`。⚠️ **本机快 ≠ CI 快**（D-16：本项目实测为准，而 CI 是另一台机器）——本机若测出 <30s，**只能写成「本机不复现」，不得写成「B3 被证伪」**。
- [x] **P1-5 `Proof` 把「起栈时序」这条方向**证实或排除**，不许因为 B2/B4 就默认它死了**：逐条查 ① `agenerp-serve` 是否真在 `backend` 之前起（`depends_on` 实读为真，但要看**实际**有没有造成影响）· ② 判定步开跑那一刻 `frontend` 的 `State.Health.Status` 与 `RestartCount` · ③ 整个 job 期间 `frontend` 有没有重启过。🔴 **②③ 的数据源问题，起草时说错过一次，改正后照实写在这里**：初稿说「`gates.yml` 的 `取证 —— 服务状态` 步能给 `frontend` 的健康与重启计数」——**这是错的**。实读 `.github/workflows/gates.yml:349-351`，那一步只跑 `docker compose ps`，**既不打 `RestartCount`，也在判定步之后才跑**，给不出「判定步开跑那一刻」的状态。⇒ **CI 侧对「起栈时序」这条方向确实没有现成数据路径**，而 Phase 1 的 `Targets` 又禁止改 `.github/`。**这个缺口提前裁定，不留给执行期临场发挥**：
      - **② 与 ③ 在 CI 上本轮取不到 —— 这是已裁定的事实，不是执行者的借口。**要在 CI 上加探针就得改 `gates.yml`，那要单独论证红线 2（走 `P2-1 (B)`），**Phase 1 不做**。
        🔴 **第 7 轮独立评审改正 —— 上面这句「已裁定的事实」只对了一半，照旧文执行会漏掉本 plan 最便宜的一格证据**（依据 B7）：**「判定步开跑那一刻的 `RestartCount`」在 CI 上确实取不到**（这半句仍然成立）；**但「整组容器重启过没有」在 CI 上取得到** —— 人已在 `7a217a2` 用 `gates.yml:349-351` 那一步现成的 `docker compose ps`（`if: always()`）的 `Up N` vs `CREATED N ago` 之差把它读出来了。
        ⇒ **改正后的分工写死**：**「重启是否发生」这一格改为在 CI 上取**（走 `P1-2` 新增的 ps 指纹一格，零额度）；**本机只补 `RestartCount` / `StartedAt` / `State.Health.Status` 的精确值与采样时序**。⚠️ **这不需要改 `gates.yml`** ⇒ `P2-1 (B)` 那条路**仍是默认不走**，红线 2 一个字不放宽。
      - **⇒ ②③ 改由本机取**：在 P1-4 的本机栈上 `docker inspect` 取 `State.Health.Status` / `RestartCount` / `StartedAt`，并在判定跑的**同时**采样。⚠️ **本机数据不能外推到 runner**（D-16）—— 它只能**证伪**「起栈时序在任何环境下都会犯」，**不能证实** runner 上发生了什么。**这条限制必须写进结论，不许省略。**
      - **① 可以在 CI 上取**：`docker compose ps` 的输出里有各服务状态与启动次序线索，逐 run 实读。
- [x] **P1-6 `Proof` 复跑优先于分析**（裁判规则 3）：对**同一 sha** 原样重跑 `gates-l2-live`（`gh run rerun --failed`）**至少 2 次**，记录每次红的是哪条。若两次红在不同判据上 ⇒ 间歇性坐实且与代码无关；若复跑不出来 ⇒ **明写「不可复现」，不许猜根因**。⚠️ **计入 `§8` 的预算表，且必须标注「取证用，预期红」**（与「本该绿却红了」分开记，见「裁判规则 4」一节）。
- [x] **P1-6b `Proof` 查第三种机制存不存在** —— ⚠️ **B3 与「起栈时序」不是穷举**，本 plan 起草时把假设空间关成两条，独立评审指出这会让「有数字的那条」自动获胜。**至少逐条查过**：端点侧延迟尖峰（同一 sha 有红有绿，而模型调用是唯一的外部变量）· runner 网络 · nginx `resolver … valid=10s` 的重解析窗口 · **同口径那 8 个 sha 之间** `agenerp/` / `docker-compose.yml` / `tools/nginx/` **有没有变**（`git diff --stat` 逐对：`e3afd77`→`758b7bc`→`82a144a`→`7af5493`→`cc205d6`→`f144475`→**`f924ac6`**→**`7a217a2`**；⚠️ **第 6 轮由 5 扩到 6，第 7 轮再扩到 8** —— 补入的两次都是绿，**漏掉它们就等于漏掉五连绿里的后两环**）。🔴 **第 5 轮评审改正一处未传播完的结论**：本条原写「11 红 1 绿那几次 sha 之间」，而 **11 这个数第 3 轮就被实测推翻（实为 16），口径也已按 `e3afd77` 切分**（B1）。这不是措辞问题 —— **它定的是本条 `Proof` 的枚举范围**，照旧文执行会去枚举一段根本不同口径的 sha。⚠️ **另需单独查一次 `f09b8f0`…`e3afd77`^ 那段**（不同口径、服务恒回 503），但**结论不得与同口径那 5 个混在一起数**（B1 逐字）。**每条给「查了—有/无」或「没查—为什么」，不许整条略过。**
      🆕 🔴 **第 7 轮改动本条的性质，必须连着读**（依据 B7）：**「重启窗口」已经从「本条要去猜的第三种机制」升格为 Phase 1 的具名方向 ③**（人已给出 run `82a144a` 的 ps 指纹证据），⇒ **它由 `P1-2` 的 ps 指纹一格与 `P1-5` 负责，不再算在本条的「第三机制」名额里。**
      ⚠️ **本条的名额因此空了出来，而不是被填满了** —— 本条现在要查的是「**既非 B3、既非起栈时序、也非重启窗口的第四种机制**」。**不许拿「重启窗口」来交本条的差**，那会让假设空间在刚被拓宽的当天又关回三条。
      ⚠️ **同时新增一条必查项**：**为什么会重启** —— 人已逐字标注这一格「**未查明**」且「**不许据此猜根因**」。本条只负责**枚举并逐条排查**（如 runner 资源 / entrypoint 的 `Linking fresh assets to volume...` 是否触发重启 / compose `restart:` 策略 / healthcheck 抖动），**给「查了—有/无」或「没查—为什么」，不得给出未经实测的根因**。
- [x] **P1-7 `Proof` 逐条对照 P1-1 写死的预测**，吻合/不吻合**逐格照实记**，不吻合的那格**保留在记录里，不修饰**。
- [x] **P1-8 `Decision` 出「机制陈述」一段**：一句话说清「什么条件下会红」，且这句话的每个成分都能指回上面某一格的数字。**写不出来就不许进 Phase 2** —— 那说明证据还不够，继续取证或停机。

Exit Criteria:

- [x] `docs/evidence/p1-8a-fix/` 下有：`predictions.md`（开跑前写死、事后未改）· 逐 run 表 · 秒数（或明写「未测出」）· 复跑记录
- [x] 「机制陈述」成立，或**明写「证据不足以支撑任何机制陈述」并走停机分支 D**
- [x] ~~B3 与「起栈时序」两条方向~~ 🔴 **第 7 轮改为三条方向**（B7 把「重启窗口」由假设变成了有证据的第三条）：**① B3 回包超时 · ② 起栈时序（`depends_on`） · ③ B7 重启窗口**，**每一条各自**有明确处置：`坐实` / `排除` / `本轮取不到证据`，**三选一，不许留空**
- [x] ~~**第三格必答**：「是否存在既非 B3 也非起栈时序的机制」~~ 🔴 **第 7 轮改准（上一条已把「重启窗口」升为具名方向 ③，本条不能再拿它充当那个「第三格」）**：「**是否存在既非 B3、既非起栈时序、也非 B7 重启窗口的第四种机制**」→ `有（指名）` / `无（说明查过哪些）` / `未查（说明为什么）`
- [x] 🆕 **B7 那处分歧已有处置**：人写的「连不上」与 B2 实读的 `recv_into` 抛点（连接已建立）是否对上同一条时间线 → `对上（给出两侧数据）` / `没对上（照实记，两侧都不采信）` / `本轮取不到证据`
- [x] **`§8` 已按真解释调用次数逐行登记**，且未超上限
- [x] 仓内代码零改动（`git diff --name-only $BASE..HEAD -- agenerp/ tests/ docker-compose.yml tools/ .github/` **无输出**，外加 `git status --porcelain -- agenerp/ tests/ docker-compose.yml tools/ .github/` 也无输出）。🔴 **口径由第 4 轮独立评审改正**：原文只写 `git status --porcelain`，而第 3 轮 [主要4] 已实证它**只看工作区、一提交就必然空输出** —— 本阶段会提交取证件（`docs/evidence/` / `docs/logs/`），提交之后那条自证就恒真、挡不住任何东西。**两条都要跑**：`$BASE..HEAD` 管已提交的，`--porcelain` 管未提交的。`BASE` 取 `§8` 第 0 行钉住的开工基线 sha。🆕 ⚠️ **非空时按 `Closure Gates` 那条「逐 commit 归属」两步法读**（第 7 轮补，实测已撞上：评审期间人往 `$BASE..HEAD` 区间里推了与本 plan 无关的提交）。
- [x] No owner-doc update required（本阶段只取证）
- [x] `docs/logs/` 更新

### Phase 2 — 裁定修法：先判它落不落在红线内

Status: completed
Targets: `docs/architecture/module-boundaries.md` §7.21（追加 `D-b-9`）
Skill: `none`（这是一次依赖 Phase 1 数字的工程裁定，没有对应 skill）

- Item Types: `Decision`
- Prereqs: Phase 1 全部 Exit Criteria 满足

- [x] **P2-1 `Decision` 列候选修法并逐条判红线归属，再选**。⚠️ **先判归属再比优劣** —— 落在红线内的候选**不进优劣比较**，直接进停机分支。每条候选必须写：改什么文件 · 红线归属 · 它凭什么能修掉 Phase 1 那句机制陈述 · 残余风险。**至少覆盖**：
      - (A) **改 `TIMEOUT = 30`** → **红线 1**（B5）⇒ **停机分支 A，不进比较。**
      - (B) **改 `gates-l2-live` job** → **红线 2 的判断题**：加诊断步（`if: failure()` 那种）**不是变松**；动判定步、加 `continue-on-error`、摘判据**是变松**。⇒ 前者可做但要在 **P2-3** 逐条自证「不变松」，后者进**停机分支 B**。⚠️ **若真走了「加诊断步」这条**：Phase 3 的 `Targets` 要补上 `.github/workflows/gates.yml`，且 `Closure Gates` 那条红线自证 `git status --porcelain -- .github/workflows/` **会有非空输出** —— 那时**必须在收口记录里逐字写明「本轮确实改了 workflow，改的是哪一步、为什么不算变松」**，不许让那条自证从「空输出 = 清白」悄悄变成一句没人看的空话。**默认路径是不改。**
      - (C) **服务侧给 `/agenerp/explain` 一个墙钟上限**，超时回一个**明确的错误码**而不是吊着。⚠️ **这是产品面的改动，不是判据面的** —— 但它会改变门禁看到的行为，**必须论证它不是「把判据调整到迁就环境」**（`gates.yml` 注释里人写死的那条戒律）。⚠️ 还要论证它**不与** `loop.py:77-79` 的「`MAX_TOOL_CALLS` 与 `MAX_TURNS` 各管一维，不许合并」冲突（⚠️ 那一维是 `MAX_TOOL_CALLS`（`:91`），**不是** `STOP_RUNAWAY`（`:74`，那是停机原因串））—— 墙钟是**第三维**，要么独立要么不加。
        🔴 **(C) 自带一条硬规矩，违反即出局，不许靠执行者自辩**：**任何新增的错误路径都不得把一条红的判据变成绿的。** 挡两个已经看得见的形状：
          - ⚠️ `test_no_response_through_the_front_ever_echoes_the_sid`（`:234-251`）**只断言 `raw_sid not in raw`** ⇒ **一个快速返回的 500/504 空体照样让它绿**，而解释根本没跑完。
          - ⚠️ **起草时这里写错过一条，改正并保留记录**：初稿说「把超时打扮成带 `AGENERP_LLM_` 字样的 503 就能让它走 skip/绿分支」——**在门禁里这是错的**。`tests/unit/test_explain_service_body.py:222` 那句 `pytest.skip` 会被 `tests/gates/test_explain_service_live.py:80` 的 `_BODY.pytest.skip = _skip_is_a_failure_here` 换成 `pytest.fail`（⚠️ **第 6 轮实读改准行号，原写 `:79`，实际在 `:80`**；`:69` 是 `_skip_is_a_failure_here` 的定义处。机制本身实读无误） ⇒ **503 在 `gates-l2-live` 里是红，不是绿。**
          - ⇒ **真正的口子只有上面第一条**（快速 500/504 空体让 `echoes_the_sid` 转绿）。**逐字禁止**：(C) 引入的任何超时返回**都不得让 `echoes_the_sid` 由红转绿** —— 它只判 `raw_sid not in raw`，**一个空体天然满足**。
          ⇒ **(C) 只有在拿得出「一次真解释在客户端预算内真的跑完了」的实测之后才可落地**；拿不出就**不是修法**，走停机分支 D。
      - (D) **compose / nginx 侧**（起栈时序、探针、`depends_on`）。⚠️ 只有 Phase 1 **坐实**了起栈时序，本候选才进比较；未坐实即出局；⚠️ 且**不得**用 `depends_on` 当修法 —— 工作项 10 已实测裁定「`depends_on` 只管 `up` 的次序，管不到 `restart: on-failure` 的重启，**代价真、收益假**」，并已有判据⑩b 专门挡这一手。
      - (F) **「让解释跑得更快」那一族** —— 调小 `MAX_TURNS`（`agenerp/explain/loop.py:61`）或 `MAX_TOOL_CALLS`（`:91`）· 削减注入上下文 · 把 `AGENERP_LLM_MODEL` 换成更快的档 · nginx 侧重试。⚠️ **它们在 P1-3 量出秒数之后会变成最顺手的一条路，所以必须提前裁定**：这一族改的是「解释做多少事」，不是「链路有没有毛病」 ⇒ **默认判为「把判据调整到迁就环境」**（`gates.yml` 里人写死的那条戒律），**默认出局**。🔴 **第 3 轮独立评审指出：这一族是本 plan 剩下的最后一条「不修缺陷也能结案」的路，且原来的护栏拦不住它。**原护栏是「须论证不削弱 P1.4 / P1.7 的既有判据」——**实测无效**：把 `MAX_TURNS` 从 25 调到 8、或削掉注入上下文之后，`test_evidence_gate_blocks_single_hop` 与 `test_explain_cost_accounting`（**只判形状**）**照样全绿**，而解释已经变浅、门禁也跟着变绿了。⇒ **护栏换成停机**：**(F) 一律走停机分支 G，loop 不得自行采纳**，哪怕 Phase 1 坐实了「解释做得太多」。⚠️ **换模型另有一条独立禁令**：停机分支 C 禁的是「为省额度换模型」，**这里追加禁「为跑赢 30 秒换模型」** —— 动机不同，两条禁令都成立。
      - 🆕 **(H) 「判定前加一道稳定性等待」/ 让 `--wait` 真正等到栈稳定** —— 🔴 **第 7 轮独立评审新增，来源是人在 `7a217a2` 亲手给出的方向**（B7 ⑥ 逐字：「若答案为否，修法**可能**在**判定前加一道稳定性等待**，而不在 `depends_on`」）。⚠️ **字母跳过 `G`，是为了不与停机分支 G 混淆** —— 停机分支 G 挡的是候选 (F)，与本候选无关。
        ⚠️ **只有 Phase 1 坐实了方向 ③（B7 重启窗口），本候选才进比较**；未坐实即出局（与 (D) 同规）。
        **红线归属是一道判断题，必须在 `P2-3` 逐条自证，不许一句「不会变松」带过**：
          - **落在 `docker-compose.yml` / `tools/nginx/**` 侧**（例如让健康判定真正覆盖那次重启、`start_period` / healthcheck 口径）⇒ **不在红线内**，但仍受 Non-Goal 4 与 D-19（零依赖启动门禁必须仍绿）约束，且必须跑 `P3-5` 冷起自证。
          - **落在 `.github/workflows/gates.yml` 侧**（在判定步之前插入等待）⇒ **红线 2 的判断题**，按 `P2-1 (B)` 的既有规矩走：要改就得在 `P2-3` 自证不变松，且 Phase 3 `Targets` 与 `Closure Gates` 的红线自证须按 (B) 那条的要求逐字说明。**默认路径仍是不改。**
        🔴 **本候选自带一条硬规矩，违反即出局，不许靠执行者自辩**：**「稳定性等待」必须是一个可证伪的条件式等待**（等到某个**指名的、可观测的**稳定判据成立，例如「容器 `RestartCount` 在 N 秒内不再增长」），**并且必须给出它凭什么能修掉 `P1-8` 那句机制陈述**。
          - ⚠️ **逐字禁止：把它实现成一个无条件 `sleep`、或把等待时长调到「红变绿为止」。**那是**把判据调整到迁就环境**（`gates.yml` 里人写死的那条戒律），与候选 (F) 同类 ⇒ **走停机分支 B，不自批。**
          - ⚠️ **也禁止**用它掩盖「为什么会重启」这一格未查明：**等待只是绕开窗口，不解释窗口**。若 `P1-8` 的机制陈述里没有「为什么会重启」的位置，(C)/(D)/(H) 都不能算修掉了缺陷 ⇒ 按 P3-6 绑定 5 与停机分支 F 处理。
      - (E) **什么都不改**（若 Phase 1 判定红因在本仓之外，例如 runner 或端点侧）⇒ 那不是「修好了」，走**停机分支 D**。
- [x] **P2-2 `Decision` 把选中项写进 `module-boundaries.md` §7.21 的新裁定 `D-b-9`**，含：选中理由 · **被否候选逐条的否决理由**（能实测否决的就实测，不能就写「论证否决」并标明）· 残余风险。⚠️ **§7.21 既有的 `D-b-1`…`D-b-8` 一个字不改**，只追加。
- [x] **P2-3 `Decision` 判定「本修法会不会让门禁变松」并逐条自证**（红线 2）。⚠️ **这一条不许写成一句「不会」** —— 要指名它守的失败形态修前修后各是什么。

Exit Criteria:

- [x] 选中项**不落在红线 1–7 任何一条内**（🔴 **第 5 轮评审改正**：原文枚举「1/2/3/5/6/7」**漏了红线 4**（改项目名 / 包名 / 命名空间），第 4 轮已记为 [次要4]，本轮直接改为覆盖全部七条）；落在的话本阶段以停机收尾，`Plan Status` 转 **`deferred`** 并往 `STATE.md` §3 追加 needs-human。
      🔴 **第 5 轮评审改正一处格式不合规**：原文写的是 `blocked`，而 `docs/plans/00-plan-authoring-and-execution-guide.md` 的「Plan Status Flow」逐字只定义 `draft` / `active` / `completed` / `superseded` / `replaced` / `deferred` / `cancelled` —— **没有 `blocked`**。停机等人裁定正是指南给 `deferred` 的语义（「no longer owns live closure in its original form」），且它与 Phase 1 Prereqs 已写死的 `cancelled` 出口同属指南状态集，**自动化流程按状态词选路时不会撞上未定义值**。
- [x] `D-b-9` 已写进 `module-boundaries.md` §7.21，`D-b-1`…`D-b-8` 零改动 —— 以 `git diff $BASE..HEAD -- docs/architecture/module-boundaries.md` 逐行核对：新增行只落在 `D-b-9` 一节内，`D-b-1`…`D-b-8` 各节**零删除行、零修改行**。🔴 **口径由第 4 轮独立评审改正**：原文写的是裸 `git diff`，第 3 轮 [主要4] 已实证它**只看工作区**，而 `D-b-9` 一旦提交这条自证就恒真。🆕 ⚠️ **第 7 轮补**：非空时按 `Closure Gates` 那条「逐 commit 归属」两步法读。
- [x] `docs/logs/` 更新

### Phase 3 — 落地 + 验收：连续 3 次 run 全绿

Status: planned
Targets: 由 Phase 2 选中项决定（预期落在 `agenerp/serve/**` 或 `docker-compose.yml` / `tools/nginx/**`）
Skill: `none`

- Item Types: `Proof`（8 条里 6 条）+ `Fix`（P3-1 / P3-7）
- Prereqs: Phase 2 全部 Exit Criteria 满足

- [ ] **P3-1 `Fix` 落地 Phase 2 选中的修法。**
- [ ] **P3-2 `Proof` 给它配离线判据**（`tests/unit/`，**不是** `tests/gates/`）。⚠️ **判据不许只验「调得通」**（roadmap 硬约束①）：要能挡住修法被悄悄改回去 / 被改成空壳。⚠️ **不许把判据写成「两边读同一个常量再比」** —— 工作项 11 的 M5 实测过这个窟窿（判据绿着、浏览器不执行）。
- [ ] **P3-3 `Proof` 变异自查**：对修法**逐条施加**能让它失效的变异，**逐条确认判据打红**，复原后 `sha256` 自证 `RESTORED OK`。⚠️ **打不红的照实记成「打不红」，不许粉饰成「全打红」**（先例：工作项 10 的 M7、工作项 11 的 M6）。
- [ ] **P3-4 `Proof` 本机全绿**：`python3 tools/gates/check_expected_red.py` · `python3 -m pytest tests/unit -q`（**只增不减**；⚠️ **开工基线本轮起草时实测写死：`807 tests collected`**，命令 `python3 -m pytest tests/unit -q --collect-only`，2026-08-25。**执行期不许自行改这个底**——树若已移动，改底是一次独立裁定，写进 `§8` 并说明）· `python3 -m pytest tests/contracts tests/tools tests/routing tests/context -q` · `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments` · `python3 -m pytest tests/unit/test_compose_zero_dep.py -q`（改了 compose 就必跑）· `bash tools/check-masterplan-links.sh`。**每条记命令原文 + 退出码。**
- [ ] **P3-5 `Proof` 冷起自证**（只在改了 compose / nginx 时必跑）：`docker compose down -v` → `AGENERP_HTTP_PORT=18080 docker compose up -d --wait --wait-timeout 900` → 记退出码与墙钟。⚠️ **零依赖启动门禁必须仍绿**（D-19：一个 AI 变量都不配时也要起得来）。
- [ ] **P3-6 `Proof` 验收 —— `gates-l2-live` 连续 3 次 `push` run 全绿零跳过。** 逐次记 **run id + sha + 判定步原文**。⚠️ **一次绿不算**（`02-WBS.md:88` 逐字）。⚠️ **必须是 `push` 事件**（fork PR 拿不到 Secrets）。⚠️ **三次必须是连续的** —— 中间夹一次红就**从头数**，且那次红照实记录、不删。
      🔴 **五条绑定，缺一条这次验收就不算数**（B1 实证这个口子**今天就开着**：`HEAD` 上已躺着一次绿）：
      1. **三个 sha 必须都是修复提交的后代或它本身**（`git merge-base --is-ancestor <fix-sha> <run-sha>` 逐个退 0）。**修复提交之前的绿一律不计，包括 `7af5493` / `cc205d6` / `f144475` / `f924ac6` / `7a217a2` 那五次**（🔴 **第 7 轮实测再改正一次：是五次**，run `32853424473` 已 `success`，见 B1 那条 🔴🔴🔴🔴 子条）（⚠️ 🔴 **第 6 轮实测改正：是三次，不是两次、更不是一次**；🔴🔴🔴 **起草步当场复取后再改正一次：是四次** —— `f924ac6` / run `32850335965` 已 `success`，见 B1 那条 🔴🔴🔴 子条。**这四次已经构成一段长于验收要求的连续绿，而修复一行都还没写** ⇒ **本条绑定不再是预防性条款，它现在是唯一挡在「零修复直接结案」前面的东西，任何轮次都不得放宽**）。⚠️ **执行期不许照抄这五个 sha 就算数** —— 开工时须重取 `gh run list`，把「修复提交之前的绿」按 `git merge-base --is-ancestor` **现算**一遍，因为这个清单每被 push 一次就长一个。
      2. **修复提交在 `agenerp/` / `docker-compose.yml` / `tools/` 里必须有非空 diff**（`git diff --stat <fix-sha>^ <fix-sha> -- agenerp/ docker-compose.yml tools/` 非空）。**纯文档提交攒出来的绿不是验收。**
      3. **三次都必须真调过模型。**⚠️ **验证手段起草时写错过，改正**：初稿说「逐次在日志里确认走 200 分支」，但 `tools/gates/check_expected_red.py` 起的 pytest 带 `-q --tb=no` 且 `capture_output=True`，而 `失败取证` 是 `if: failure()` ⇒ **绿 run 的日志里没有逐条明细，这条验不了。**⇒ **改用两条能验的**：(i) 逐次确认该 run 的**起栈步与判定步都拿到了非空的 `AGENERP_LLM_*`**（fork PR 拿不到 Secrets ⇒ 只取 `push` 事件，已是绑定的一部分）；(ii) 逐次记该 job 的**墙钟**并与 P1-3 量出的真解释耗时对照 —— 没真调模型的 run 会明显短。⚠️ **(ii) 是弱证据，必须逐字标注成弱证据**，不许写成「已证实真调过」。
      4. **修复提交的 diff 必须就是 `D-b-9` 里选中的那个修法本身**，`D-b-9` 逐字点名它改的文件与函数/配置项。⚠️ **绑定 2 只查「diff 非空」，挡不住注释级改动或改 `expected-red.txt` 凑数** —— 本条堵的正是那个。
      5. **Phase 1 的机制陈述必须已经成立**（P1-8）。⚠️ **若 CI 自己转绿而机制陈述始终写不出来 ⇒ 那不是修好了，是这一轮没咬到** ⇒ 走**停机分支 F**，不许结案。
- [ ] **P3-7 `Fix` owner-doc 对齐**：`module-boundaries.md` §7.21 的 `D-b-9` 补上实测结果；若改了 compose/nginx 则同步 `system-baseline.md` 对应节。
- [ ] **P3-8 `Proof` 往 `STATE.md` §3 追加一条证据行**（**只追加，不改写既有行**，红线 5），含命令原文 + 退出码 + 三次 run id + commit sha。
      🔴 **第 6 轮独立评审改正本条的授权陈述 —— 原文写反了，现已过期**：原文逐字「⚠️ **不代人写 roadmap 10b 的状态词**（§0 的授权边界）—— 状态源是 `人`。」**`f144475` 之后状态源是 `` `MD:p1-explain` ``，即这一行的状态词本来就归 loop 维护**（表规 2 逐字：状态源就是这一格的定义）。⇒ **收口时 loop 应当写 `02-WBS.md:88` 与 roadmap `10b` 的状态词，不写反而是漏做。**
      ⚠️ **但这不是把闸门放开，而是把闸门挪到该在的地方 —— 三条硬约束写死**：
      ① **`02-WBS.md` 只许改 `P1.8a-fix` 那一行的「状态」一格**，行内其余文字、以及其余任何一行**一个字节不改**（红线 5 对 `docs/masterplan/` 其余文件仍然全效；`02-WBS.md` 的其他行不在本行的状态源覆盖范围内）。自证：`git diff $BASE..HEAD -- docs/masterplan/02-WBS.md` 的输出**必须只有 1 增 1 删且都在第 88 行**。
      ② **状态词与证据必须同一次提交落地** —— 改状态的那个 commit 里必须同时有三个 run id + 修复 sha（`02-WBS.md:88` 逐字要求「收口时必须点名那个 commit 的 sha，并列出 3 次 run 的 id —— 指不出来就不算达标」）。
      ③ **P3-6 五条绑定未全部满足时，一个状态词都不许写。**⚠️ **B1 已实测：`main` 上此刻就躺着三次连绿，全在修复之前** ⇒ **「CI 是绿的」从来不是写状态词的依据。**

Exit Criteria:

- [ ] 修法落地且有离线判据守着，变异自查逐条记录（含打不红的）
- [ ] P3-4 的**五条无条件命令全部 exit 0**（`check_expected_red.py` / `pytest tests/unit` / `pytest tests/contracts tests/tools tests/routing tests/context` / `ruff check` / `bash tools/check-masterplan-links.sh`），原文与退出码落盘；**两条有条件**命令（`pytest tests/unit/test_compose_zero_dep.py`、P3-5 冷起）只在改了 compose / nginx 时必跑，未触发时逐字写明「未触发，本轮未改 compose/nginx」
- [ ] **`gates-l2-live` 连续 3 次 `push` run 全绿零跳过**，三个 run id 可 `gh run view` 复核
- [ ] `module-boundaries.md` §7.21 已对齐（`D-b-1`…`D-b-8` 零改动）
- [ ] `STATE.md` §3 已追加证据行（`git diff $BASE..HEAD -- docs/masterplan/STATE.md | grep -c '^-[^-]'` → **`0`**）。🔴 **口径由第 4 轮独立评审改正**：原文是裸 `git diff`，**只看工作区** —— 而 P3-8 的追加行**必然是提交出去的**，提交之后该命令恒输出 `0`，等于一条永远为真的红线 5 自证。与 `Closure Gates` 那条同口径。🆕 ⚠️ **第 7 轮补**：计数非零时按 `Closure Gates` 那条「逐 commit 归属」两步法读，**不许因为「可能是人改的」就略过归属**。
- [ ] `docs/logs/` 更新

## Execution Record（2026-08-26）

> 执行者：loop（`2026-08-26-094345-mission-driver`）。`BASE = baaebc8`（开工时 `git rev-parse HEAD`）。
> ⚠️ **本节只记做了什么与得到了什么，结论正文在 `docs/evidence/p1-8a-fix/` 与 `D-b-9`，不在这里重复。**

### 做了什么

| 项 | 结果 | 花掉的真解释 |
|---|---|---|
| `P1-0` | **读不到真实剩余额度** ⇒ 走出路 ②，探路闸 ≤6 | 0 |
| `P1-1` | `predictions.md` 开跑前写死，事后未改 | 0 |
| `P1-2` | 同口径 **10 个 run**（比第 7 轮记的 8 个多 2 个）逐 run 表 + **ps 重启指纹** + 原始日志落盘 | **0** |
| `P1-3` | 真解释墙钟：本机直接量到 1.72s / 1.90s；CI 侧由步级墙钟反解（算式与假设写死） | 0 |
| `P1-4` | 本机跑门禁本体 `6 passed in 4.05s`，**本机不复现** | 2 |
| `P1-5` | 起栈时序**排除**；`RestartCount` 全 0 | **0** |
| `P1-6` | 同一 sha 重跑 2 次，**2 次全绿 ⇒ 逐字记「不可复现」** | 4 |
| `P1-6b` | 9 对 sha 三路径**全空**；第四机制**无（列了 5 个查过的）**；「为什么会重启」前提被推翻但仍逐条给了结论 | **0** |
| `P1-7` | 逐格对照：**7 格不吻合、2 格预测未覆盖**，全部保留不修饰 | 0 |
| `P1-8` | **机制陈述成立**（每个成分指回一个数字） | 0 |
| `P2-1` / `P2-2` / `P2-3` | 候选 (A)–(H) 逐条判红线归属并给否决理由，写进 `D-b-9`；**选中项：无** | 0 |

**合计 6 次真解释 = 探路闸上限，未超。**

### 三条本轮推翻/改准的既有说法（照实记）

1. 🔴 **B7 ② 人写的「整组 frappe 容器在起栈约 2 分钟后集体重启过一次」不成立。**
   `Up N` < `CREATED N` 这个差值在**全部 10 个 run**（红 3 绿 7）**以及同一 sha 的绿 attempt** 上同形；
   CI backend 日志 10/10 只有**一套**启动序列；本机 13 个容器 `RestartCount` **全 0**；
   `Up` 落后的那组恰好等于（传递地）等 `create-site` 的那组，`websocket` 不在其中且实测无差值。
   ⇒ 那是 `docker compose up` 的**启动次序**，不是重启。
   ⚠️ **被证伪的只有「`Up` < `CREATED` ⇒ 重启」这一步读法** —— 人给的那份 ps 原文是真的，
   而且**它是本 plan 唯一零额度的证据入口，方向指得对**。
2. 🔴 **B7 那处分歧的裁定：`没对上`，人的「是连不上」那一半不成立。** 两侧数据都在 `p1-8` 里给了。
3. 🔴 **B3 所依赖的前提「解释系统性地慢」被证伪**（中位数 1.7–13s ≪ 30s），
   **但 B3 作为失败形态坐实**（红那次 > 30s，服务端 `BrokenPipeError` 佐证）。

### 为什么停在这里（不是「查不出来」，是「唯一那条路要人批」）

机制陈述里那个 `30` 就是 `tests/unit/test_explain_service_body.py:99` 的 `TIMEOUT`，
而那份文件是 `tests/gates/test_explain_service_live.py:57` 唯一的判据正文（`_load` 整体 `exec_module`）
⇒ **改它 = 改裁判 = 红线 1。** 其余候选逐条被实测或规矩否决（`D-b-9` 那张表）。
⇒ **停机分支 A**，问题逐字交人：**「30 秒这个数是判据的一部分（『解释必须在 30 秒内回』），还是一个无意写下的传输参数？」**
本轮的实测让这个问题第一次带上了数字：**中位数 1.7–13 秒，长尾 > 30 秒。**

### 未查明的一格（不许猜根因）

**「为什么某一次会落到 30 秒之外」** —— 端点侧延迟尖峰 vs 解释 loop 自身轮数，**本轮分不开**。
分开它需要单次解释的耗时与 token 账；本仓无耗时日志、junit 未上传 artifact、加探针要改 `.github/`（红线 2）。

### 红线自证（两步法，`Closure Gates` 口径）

| 自证 | 命令 | 输出 |
|---|---|---|
| 红线 1/2 | `git diff --name-only $BASE..HEAD -- tests/gates/ .github/workflows/` + `git status --porcelain -- tests/gates/ .github/workflows/` | **均无输出 ⇒ 清白，到此为止** |
| 红线 3 | `git diff --name-only $BASE..HEAD -- docs/masterplan/DECISIONS.md` + `git status --porcelain -- …` | **均无输出** |
| 红线 5 | `git diff $BASE..HEAD -- docs/masterplan/STATE.md \| grep -c '^-[^-]'` | **`0`**（本轮对 `STATE.md` 只追加） |
| 红线 4/6/7 | 未改项目名/包名/命名空间 · 未写证据仓 `XM_PATH` · 未生成 Server Script | **均未触碰** |
| Phase 1「仓内代码零改动」 | `git diff --name-only $BASE..HEAD -- agenerp/ tests/ docker-compose.yml tools/ .github/` + `git status --porcelain -- 同路径` | **均无输出** |
| Phase 2「`D-b-1`…`D-b-8` 零改动」 | `git diff --numstat -- docs/architecture/module-boundaries.md` | **`64  0`**（64 增 0 删），单个 hunk `@@ -3817,0 +3818,64 @@` ⇒ 新增行全落在 `D-b-9` 一节内 |

### 本机验证（命令原文 + 退出码）

```
$ python3 tools/gates/check_expected_red.py
门禁 28 项：预期红 0，绿 28，跳过 0
✅ 与预期红名单完全一致
退出码 0

$ python3 -m pytest tests/unit -q
801 passed, 6 skipped in 13.66s
退出码 0

$ python3 -m pytest tests/unit -q --collect-only
807 tests collected in 0.24s          # 与 P3-4 写死的开工基线 807 逐字相同，未移动

$ bash tools/check-masterplan-links.sh
共校验 35 条引用，断链 0 条。
退出码 0
```

⚠️ **verification scope limited** —— 本轮**未**跑整仓 `pytest tests -q -m "not live"`，
也未跑 `pytest tests/contracts tests/tools tests/routing tests/context` 与 `ruff check`：
那几条是 `P3-4` 的验收面，而 **Phase 3 未执行、本轮零代码改动**（上表红线自证已证）。

## 停机分支（触发即停，写 `STATE.md` needs-human 等人，**不自批**）

> 这一节是本 plan 最重要的一节。**它列的每一条都是「拿不准就停下来」的具体形状。**

- **分支 A —— 唯一可行修法是改 `TIMEOUT = 30`（或断言体里任何其他一行）。**
  依据 B5：那份 `tests/unit/test_explain_service_body.py` 是门禁**唯一的判据正文**，其文件头逐字自证是「交接件」「一行断言都不重写」，与 `STATE.md` §3 `11:07Z` ③ 对 `tests/tools/` 那份的裁定**同构** ⇒ **改它 = 改裁判，红线 1。**
  ⚠️ **不许拿「它物理路径不在 `tests/gates/**` 字面内」当豁免** —— 那条口子上一轮已经查过并被证伪，本 plan 不重开。
  ⚠️ **也不许绕道**：不许在门禁之外另写一份「宽松版」判据、不许用 env 覆盖 `TIMEOUT`、不许 monkeypatch 它。**那些都是同一件事换个写法。**
  **处置**：停机，写 needs-human，把问题写成「**30 秒这个数是判据的一部分（『解释必须在 30 秒内回』），还是一个无意写下的传输参数？**」—— 这个问题**只有人能答**，因为它等于在问「本产品对一次解释的响应时间承诺是什么」。

- **分支 B —— 唯一可行修法要动 `gates-l2-live` 的判定步 / 加 `continue-on-error` / 摘判据出判定面。**
  **红线 2。**⚠️ 尤其挡住这一手：把那两条会真调模型的判据从 live 判定面里摘出去 —— 它能让 CI 立刻变绿，而 `gates.yml` 里人已写死戒律「摘掉它能让 CI 变绿，但那是**把判据调整到迁就环境**，正好是判定器不该做的事」。**处置**：停机，写 needs-human。

- **分支 C —— 模型额度耗尽 / 端点持续不可用。**
  job 注释逐字「约 9 次 CI 就烧光一个模型的额度」，而验收要 3 次连绿。若额度在拿到 3 次连绿之前耗尽 ⇒ **停机**，照实登记已用次数与剩余，**不许**改用更弱模型/改判据来省额度（那是分支 A/B 换皮）。**处置**：写 needs-human，问「换模型还是加额度」。

- **分支 D —— Phase 1 取不出机制陈述，或红因落在本仓之外**（runner 侧、端点侧、GitHub 侧）。
  **不许猜根因**（裁判规则 3）。**处置**：把「已排除什么、还剩什么、为什么测不到」写成一段，停机等人。⚠️ **明确不许**：不许因为「查不出来」就去改判据让它别红。

- **分支 E —— 修法看起来有效，但 3 次连绿拿不到**（例如 2 绿 1 红）。
  ⚠️ **这不是「基本修好了」，是没修好。**间歇缺陷的验收就是为这种情况定的。**处置**：那次红照实记录并回到 Phase 1 继续取证；**若第 2 格 plan 预算也用尽**（表规 3），停机等人拆行。**不许**把 `Plan Status` 写成 `completed`。

- **分支 F —— CI 自己转绿，但机制陈述写不出来。**
  ⚠️ **这是本 plan 最可能的死法，也最容易被误当成成功。**~~🔴 **第 6 轮改正并加强**：B1 实证 **同口径下 3 红 3 绿**~~ 🔴 **第 7 轮再改准：B1 实证同口径下 3 红 5 绿**（`e3afd77` / `758b7bc` / `82a144a` 红，`7af5493` / `cc205d6` / `f144475` / `f924ac6` / `7a217a2` 绿），**而修复一行都还没写** —— ⚠️ **更要命的是那 5 次绿是连续的** ⇒ **「连续 3 次 run 全绿」这个字面验收，此刻在零修复的仓库上不只被满足，还超出了 2 次。**（「3 红 2 绿」是第 5 轮口径、「3 红 3 绿」是第 6 轮口径，**均已过期**。）间歇缺陷的绿**不携带信息** —— 「修好了」与「这轮没咬到」在结果层面长得一模一样（roadmap 硬约束①挡的正是这个）。
  **触发条件**：P3-6 五条绑定里**第 5 条**不成立（Phase 1 没能给出机制陈述），而 CI 恰好连绿。
  **处置**：**不许结案，不许把 `Plan Status` 写成 `completed`。**照实记「连绿 N 次但机制未明」，停机等人裁定「这算不算修好」。⚠️ **这个判断只有人能做** —— 它等于在问「本项目接受多大的残余不确定性」。

- **分支 G —— 唯一可行修法落在「让解释跑得更快 / 做得更少」那一族（候选 (F)）。**
  ⚠️ **这一条是第 3 轮独立评审新加的，因为它是本 plan 剩下的最后一个「绿了但没修」的口子。**调小 `MAX_TURNS` / `MAX_TOOL_CALLS`、削注入上下文、换更快的模型档 —— 这些**都能让门禁转绿**，而且**能同时满足 P3-6 的全部五条绑定**（diff 非空、就是 `D-b-9` 点名的那个、机制陈述也成立）。**挡不住它的原因是**：P1.4 / P1.7 的既有判据**只判形状不判深度**（`test_evidence_gate_blocks_single_hop` / `test_explain_cost_accounting` 在解释变浅之后照样绿）⇒ **本仓今天没有任何一条判据能区分「解释变快了」和「解释变差了」。**
  **处置**：**停机，不自批。**问题写成「**把解释做浅换取门禁变绿，可不可以接受？如果可以，用什么判据保证它没变差？**」——⚠️ **后半句才是关键**：没有那条判据，(F) 就是把「答得对」换成「答得快」，**正是 roadmap 硬约束①逐字禁止的那种「调得通就算」。**

## 裁判规则 4 与本 plan 的关系（`AGENTS.md` 逐字「CI 连续 2 轮红」是停机条件）

⚠️ **本 plan 开工时，仓库已经处在这条停机条件里很深的位置**：`gates-l2-live` **16 次连续 `push` 红**（B1，第 3 轮评审实测改正的数），其后 `7af5493` 转绿、**`cc205d6` 又绿一次**（⚠️ **第 5 轮评审补上第二次绿**，B1 的 🔴 子条 ② 已记，本节此前只写了第一次）、**`f144475` 再绿一次**（⚠️ 🔴 **第 6 轮评审实取补上第三次绿**，run `32849370852`，B1 的 🔴🔴 子条已记）、**`f924ac6` 与 `7a217a2` 又各绿一次**（⚠️ 🔴 **第 7 轮评审实取补上第四、第五次绿**，run `32850335965` / `32853424473`，B1 的 🔴🔴🔴🔴 子条已记）。**照实摆出来，不绕过。**
🔴 **第 6 轮据此补一条本节此前没有的推论**（⚠️ **第 7 轮把数改准为 5**）：`gates-l2-live` 现在**连续 5 次 `push` 绿** ⇒ **裁判规则 4 的「CI 连续 2 轮红」这条停机条件，此刻在字面上已经不成立了**。⚠️ **但这不构成「缺陷已消失」**：本节第 3 条那句「不因为本 plan 正在修 CI 就自我豁免」**反过来同样适用 —— 也不许因为 CI 恰好绿着就宣布缺陷没了**。间歇缺陷的绿不携带信息（停机分支 F），**唯一能结案的仍然是 P3-6 那五条绑定全部满足。**

1. **人已经看见并处置过它** —— `82a144a` 与 `STATE.md` §3 `[open] 11:15Z` 就是那次处置，逐字「**给 loop 解锁**」。⇒ 本 plan 存在的前提正是人对那串红的裁定。⚠️ **但那次裁定没有逐字豁免裁判规则 4**，所以这一节把它摆出来，**不代人宣布豁免**。
2. **P1-6 会主动制造红**（`gh run rerun --failed`，预期红）。⚠️ 那是**取证手段不是新缺陷**，但它会让红计数继续涨。**处置**：P1-6 的每一次重跑在 `§8` 里**单独标注「取证用，预期红」**，与「本该绿却红了」**分开计数，不许混**。
3. ⚠️ **若 Phase 1/2 期间出现「非预期」的 CI 红连续 2 轮**（不是 P1-6 制造的）⇒ **按裁判规则 4 停机**，不因为「本 plan 正在修 CI」就自我豁免。

## §8 CI 次数与额度预算表（执行期逐行追加，**不许事后补写**）

> **单位是「真解释调用次数」，不是「run 次数」** —— 依据见 `Infrastructure And Config Prereqs` 记的三处偏差。
> 换算（本 plan 采用的保守值）：**一次绿 run ≈ 2 次真解释；一次红 run ≈ 4 次**（红的那种会多跑一遍 `失败取证`）。

🔴 **先算本 plan 自己的地板价，再定上限 —— 初稿把上限定得比地板价还低，是独立评审算出来的**：
- P1-6 至少 2 次同 sha 重跑，**预期红** ⇒ **≥ 8 次**
- P3-6 三次绿 run ⇒ **≥ 6 次**
- P1-3 / P1-4 的本机取证 ⇒ **≥ 2 次**（⚠️ **本机跑烧的是同一个 DashScope 额度** —— 初稿写「不烧额度」是错的，那句只对「不烧 CI 次数」成立，**额度是同一个池子**）
- 🔴 **第 4 轮独立评审补上的一项 —— 前三轮全漏了：本 plan 自己的每一次 `push` 也烧额度。**
      实读 `.github/workflows/gates.yml:6-10`：触发器逐字是 `on:` / `  push:` / `    branches: [main]` / `  pull_request:` / `  workflow_dispatch:`（⚠️ **第 6 轮补全引文** —— 原文只引了 `push` 那三行就写「逐字」，漏了后两行；**承重的那半句「`push` 到 `main` 且没有任何 `paths:` 过滤」实测无误**，结论不变）⇒ **一次纯文档 push 照样跑满 `gates-l2-live`**，照样发 2（绿）／4（红）次真解释。
      ⚠️ **这个事实本 plan 在 B1 里已经写出来过**（逐字「**每次 `push` 都会触发 `gates.yml`**」），但**只用在 P3-6 的防刷绑定上，从未传播进本节的算术** —— 同一条事实在一处承重、在另一处被忽略。
      而本 plan **必然要提交并推送**：P1-7 的取证件 · P2-2 的 `D-b-9` · P3-8 的 `STATE.md` 证据行 · 本表自己的逐行追加。**按每阶段各推一次的最省口径也是 ≥ 3 次额外 run ⇒ ≥ 6 次真解释**（若其中有红则更多）。
      ⇒ **地板价由 ≈16 修正为 ≈22 次真解释**，且这一项**不可省** —— 它不是返工余量，是本 plan 正常走完就一定会花掉的。
- **减轻手段（写死，不是建议）**：阶段内的文档提交**攒起来一次 push**，不许一条一提交一推。⚠️ 实证依据：`45ee997`（loop 的纯文档提交）与 `7af5493` 同批 push，`gh run list` 里**只出现 `7af5493` 一个 run** ⇒ **批量 push 只触发一次**，这是真能省的。**每一次 push 触发的 run 都要进本表**，用途列记 `顺带（文档 push 触发）`，**不许因为「不是我特意跑的」就漏登**。
- ⇒ **不含任何返工的地板价 ≈ 22 次真解释**（16 + 顺带的 6），而初稿上限写的是 12 ⇒ **本 plan 会在够到验收之前先把自己撞进停机分支 C。**
  ⚠️ **连带修正一处判据**：下一段「**若现算结果 < 16 ⇒ 走停机分支 C**」里的 **16 一并改为 22**；按 16 放行等于明知地板价是 22 还开工。

**改后分成两个闸**：
1. **`P1-0` 之前的探路闸**：累计真解释 **不得超过 6 次**（够 P1-3/P1-4 本机取证 + 一次 CI 取证）。**`P1-0` 没读出额度就不许越过这个闸** ⇒ 停机分支 C。
2. **`P1-0` 之后的总闸**：由 `P1-0` 读到的**真实剩余额度** ÷「一次真解释的实测 **token** 数」**现算**，写进本表第 0 行。
   ⚠️ **起草时这里指错了来源、并绕成一个死循环，第 3 轮评审抓出来，照实改正**：初稿说那个 token 数是「P1-3 的产物」，但 **P1-3 产出的是秒数不是 token 数**；而且**要量它就得先花掉几次调用**，而第 0 行又写着「先填这一行再动下面任何一行」⇒ **自锁**。**改正**：① token 数的来源是**服务自己的成本账**（P1.7 已交付：回包 `payload["cost"]["total"]` 的 `prompt`/`completion`/`reasoning`/`total` 四项），**不是 P1-3**；② **明确解锁**：**探路闸（≤6 次）之内的行允许先于第 0 行落表**，第 0 行在探路闸内量到第一个 token 数之后补齐。**「先填第 0 行」这条只约束探路闸之外的行。**⚠️ **不许沿用 job 注释那个「9 次」**（`Infrastructure And Config Prereqs` 已证它至少三处对不上）。**若现算结果 < 22 ⇒ 本 plan 在当前额度下做不完，走停机分支 C，不许开工硬撞。**（⚠️ **第 4 轮改正**：原文写 16，未计入本 plan 自己 push 触发的 run，见上一段。）

⚠️ **本机跑也进本表**（同一个额度池），`run id` 一列填 `本机`。

| # | run id / `本机` | sha | 事件 | 判定步原文 | 本次真解释次数 | 累计 | 用途（取证 / 验收）· 预期红? |
|---|---|---|---|---|---|---|---|
| 0 | — | `baaebc8` | — | **`P1-0` 走出路 ②：读不到真实剩余额度。**本仓无「读 DashScope 剩余额度」通道（`grep` 实查）；宿主 `env` 与 `.env` 里没有 DashScope 密钥（只有 `AGENERP_LLM_MODEL` / `DASHSCOPE_BASE_URL`）；未走人侧控制台那条路 ⇒ **按探路闸 ≤6 次真解释跑，跑完停分支 C**。**总闸现算不出来，逐字记「读不到」，不按 job 注释那个乐观数取值。**`BASE = baaebc8`（开工基线 sha） | — | **0** | **先填这一行 ✓** |
| 1 | `32841381171` **attempt 2** | `82a144a` | `gh run rerun --failed` | `门禁 54 项：红 0，绿 54，跳过 0`（判定步 33s） | 2 | **2** | **取证（`P1-6` 第 1 次）· 预期红 —— 实际绿** |
| 2 | `32841381171` **attempt 3** | `82a144a` | `gh run rerun` | `门禁 54 项：红 0，绿 54，跳过 0`（判定步 23s） | 2 | **4** | **取证（`P1-6` 第 2 次）· 预期红 —— 实际绿** |
| 3 | `本机` | `baaebc8`（工作区） | 本机 pytest | `6 passed in 4.05s`（`--durations=0`：1.72s / 1.90s 两条真解释） | 2 | **6** | **取证（`P1-4`）· 预期绿** |
| — | — | — | — | 🔴 **探路闸 6/6 用尽 ⇒ 停机分支 C 的额度前置就此触发；此后一次调用都不再花** | — | **6** | — |

⚠️ **重跑口径**：`gh run rerun` 产生的是**同一个 run id 下的新 attempt**，不是新 run。⇒ **取证（P1-6）可以用重跑**，**验收（P3-6）只认三个互相独立的 run id**，重跑 attempt **一律不计入那三次**。

🔴 **执行期实测，三条对本表的修正（照实记，不改上面已写死的换算）**：
1. **「一次红 run ≈ 4 次真解释」这个换算对 `e3afd77` 不成立** —— `失败取证` 步是 `758b7bc` 才引入的
   （`git log -S"失败取证" -- .github/workflows/gates.yml` ⇒ 只有 `758b7bc` 一条）⇒ **`e3afd77` 那次红只花了 2 次。**
   换算本身不改（它对 `758b7bc` 之后的红 run 成立），但**用它回溯估算历史消耗时会高估**。
2. **本 plan 预期「重跑必红 ⇒ 每次 4 次」，实测两次重跑全绿 ⇒ 每次只花 2 次**（`p1-7` 已记为预测不吻合）。
   **这是探路闸没有当场被撞破的唯一原因** —— 不是估得准，是运气。
3. **本轮的提交不 push。** 上一个 loop 步的 `baaebc8` 同样停在本地未推
   （`git log origin/main..HEAD --oneline` ⇒ 只有 `baaebc8`）⇒ **本轮不产生「顺带（文档 push 触发）」那一类 run。**
   ⚠️ **但这只是把那笔账推后**：将来任何一方把这些提交推上去，会触发**一次** `gates.yml`（批量 push 只触发一次，`§8` 已实证）
   ⇒ **届时 +2（绿）或 +4（红），记在推的那一方账上。**

⚠️ **零额度取证不进本表**（本表单位是真解释调用次数）：`P1-2` 的逐 run 表与 ps 指纹 · `P1-5` 的 `docker inspect` ·
`P1-6b` 的逐对 `git diff` · `P1-3` 的步级墙钟反解 —— **这四格合计花掉 0 次真解释**，
是本轮拿到机制陈述的主力。**`P1-2` 那条「ps 指纹应当排在最前」的开工建议，实测是对的。**

⚠️ **`7af5493` / run `32841842429`、`cc205d6` / run `32846332776`、`f144475` / run `32849370852`、`f924ac6` / run `32850335965`、`7a217a2` / run `32853424473` 那五次绿都不进本表的验收列**（⚠️ 🔴 **第 7 轮改正：是五次** —— 第 6 轮写三次、更早写两次，**均已过期**；五次全在修复提交之前，P3-6 绑定 1 已排除），但**都要作为取证素材进 Phase 1 的逐 run 表**。⚠️ **`fecbd59` / run `32920159448` 第 7 轮实测时仍 `in_progress`**，执行期首次取数须复取终态并按同一规则归类。⚠️ **这五次绿一次也不是本 plan 花的额度**，`≈22` 的地板价不变。

## Draft Review Record

- Independent draft review iteration 1: **needs revision**（独立子代理 `aa853d786c0baeb08`，**非起草者**）—— 判 **4 阻塞 / 7 主要 / 6 次要**，「可以转 active: **no**」。**已全部改进本文件**，逐条对应：
  - **[阻塞1]** 正文三处指向 `§7` / `§8` / `§9`，而这三个小节**根本不存在**（`§8` 还是停机分支 C 所依赖的预算表）⇒ 已新建 `§8`，另两处指针改指真实存在的条目。
  - **[阻塞2]** P3-6 没要求 3 次绿**发生在修复提交之后**，而 `HEAD`（`7af5493` / run `32841842429`）上**已经躺着一次绿**，且每次 `push` 都触发 `gates.yml` ⇒ **纯文档提交就能攒出 3 次绿**。已加四条绑定 + 停机分支 F。
  - **[阻塞3]** 候选 (C) 可用「快速错误码」把红判据变绿而缺陷未修（`echoes_the_sid` 只判 sid 不回显，快速 500/504 空体照样绿；`resolves_to` 接受带 `AGENERP_LLM_` 字样的 503）⇒ 已加逐字禁令 + 实测前置。
  - **[阻塞4]** `§0` 单方面把 `02-WBS.md:88` 的状态源 `人` 重新解释成「只管状态词」⇒ **已改为不自裁**：本轮实读 `02-WBS.md:79` 证明人要表达联合归属时写的是 `人 + MD:p1-insight`，⇒ `:88` 单写 `人` 是刻意的。已往 `STATE.md` §3 追加 `[needs-human]`，**plan 停在 `draft`**。
  - **[主要5]** B3 引了 `frappe.conf.template:73` 与 `:83-84`，却**漏掉 `:77`** —— 那一行逐字裁定 300 与 30「**两者别混**」⇒ 已补引，并把 B3 的性质从「发现不一致」改写成**重开一条既有裁定**，重开理由写死。
  - **[主要6]** 额度算术照抄 job 注释未复核（漏了「一轮 2 次真解释」与「红 run 翻倍」，且与「11 红 1 绿」的观测对不上）⇒ 已加 `P1-0` 与 `§8`，单位改为真解释调用次数。
  - **[主要7]** 未处理 `AGENTS.md` 裁判规则 4「CI 连续 2 轮红」与 P1-6 主动制造红之间的冲突 ⇒ 已加专节。
  - **[主要8]** Phase 1 结构让超时假设默认获胜（B3 有两个可量化项，起栈时序那项自带「取不到就登记取不到」的免死金牌）⇒ 已加 `P1-6b` 第三机制桶与第三条 Exit Criteria。⚠️ **当时同时指名 `gates.yml` 的 `取证 —— 服务状态` 为其数据源 —— 那一半已被第 2 轮 [阻塞2] 推翻并改掉（该步只跑 `docker compose ps`），本条按改后的读。**
  - **[主要9]** `tests/unit` 回归基线留空待执行期自填，违反 Minimum Rule 1 ⇒ **已实测钉死 `807 tests collected`**（2026-08-25 起草时）。
  - **[主要10]** Non-Goal 3 只禁「改断言或参数」，挡不住「追加常量 / helper」（门禁 `:57` 整体 `exec_module`）⇒ 改为**一个字节不改**。
  - **[主要11]** 候选清单漏掉「让解释跑得更快」那一族（调小 `MAX_TURNS` / `MAX_TOOL_CALLS`、削上下文、换更快的模型档）⇒ 已补为 (F) 并**默认判出局**。
  - **[次要12–18]** 行号漂移三处（`:55→:57`、`:81-86→:82-87`、`:389→:387`）· B1 已过期且漏记「WBS 说两条红 vs 判定步说红 1」这处矛盾 · 「两发真解释」实为一发 · Phase 1 `Targets` 自相矛盾 · P3-4 六条里两条其实是有条件的 · `loop.py` 引文张冠李戴（`STOP_RUNAWAY` → `MAX_TOOL_CALLS`）· 重跑 attempt 与 run id 的计数口径未定 ⇒ **逐条改正**（重跑口径已写死：**取证可用重跑，验收只认独立 run id**）。
- Independent draft review iteration 2: **needs revision**（独立子代理 `a9039b8af847acc06`，**既非起草者也非第 1 轮评审者**）—— 判 **2 阻塞 / 5 主要 / 3 次要**，「可以转 active: **no**」。⚠️ **它确认第 1 轮的 4 阻塞里有 3 条真落地了**（`807` 基线实测复核、三处行号实测复核、B3 已引 `:77` 并改写成重开裁定、`STATE.md` 追加为**只追加**且 `Plan Status` 仍 `draft`），并**独立用 `gh` 复核了 B1**（run `32841842429` / sha `7af5493` = `HEAD` 确为 `success`）。**已全部改进**，逐条：
  - **[阻塞1，第 1 轮那条没真修干净]** `§8` 建出来了，但正文仍有 4 处 `§1` / `§2` **指向不存在的小节**，其中 `§2 的停机分支 A` 还是 B5 处置红线 1 修法的落点 ⇒ **已全部改指 `Phase 1` / `停机分支 A`**，并复核全文再无悬空 `§N`。
  - **[阻塞2]** P1-5 指名的取证源**给不出它要的东西**：`gates.yml:349-351` 的 `取证 —— 服务状态` 只跑 `docker compose ps`，**不打 `RestartCount`，且在判定步之后才跑** ⇒ 「起栈时序」在 CI 上其实没有数据路径，[主要8] 想消除的不对称**又回来了**。已**提前裁定**这个缺口：②③ 改由本机 `docker inspect` 取，并**逐字写死「本机数据不能外推到 runner，只能证伪不能证实」**；要在 CI 上加探针须走 `P2-1 (B)` 单独论证红线 2。
  - **[主要3]** `§8` 的上限（12）**比本 plan 自己的地板价（≈16）还低** ⇒ 会在够到验收之前先把自己撞进停机分支 C。已改成两个闸（探路闸 6 / `P1-0` 之后现算总闸），并写死「现算 < 16 ⇒ 走分支 C，不许硬撞」。
  - **[主要4，改正一处本 plan 自己的事实错误]** 初稿称「带 `AGENERP_LLM_` 字样的 503 能让 `resolves_to` 走 skip/绿分支」——**错的**：`tests/unit/test_explain_service_body.py:222` 那句 `pytest.skip` 被 `tests/gates/test_explain_service_live.py:79` 换成了 `pytest.fail`，**503 在门禁里是红**。已改正，并把 (C) 的禁令收窄到**真正的那个口子**（快速 500/504 空体让 `echoes_the_sid` 转绿）。同时 P3-6 绑定 3「逐次在日志里确认走 200 分支」**也验不了**（`check_expected_red.py` 起的 pytest 带 `-q --tb=no` + `capture_output=True`，绿 run 日志无逐条明细）⇒ 已换成两条能验的，且**弱证据那条逐字标注为弱证据**。
  - **[主要5]** `P2-1 (B)` 说加诊断步「可做」，而 Phase 3 `Targets` 不含 `.github/`、`Closure Gates` 的红线自证又要求该路径**空输出** ⇒ 三处互相打架。已补：若真走这条，`Targets` 要补上该文件，且**必须在收口记录里逐字写明改了哪一步、为什么不算变松**；默认路径是不改。
  - **[主要6]** P3-6 绑定 2 只查「diff 非空」，**一次注释级改动或改 `expected-red.txt` 就能满足** ⇒ 已加**绑定 4**：修复提交的 diff 必须**就是 `D-b-9` 里逐字点名的那个修法本身**（原绑定 4 顺延为 5）。
  - **[主要7]** 额度账漏了本机调用 —— P1-3/P1-4 写着「不烧 CI 额度」，但它们烧的是**同一个 DashScope 池** ⇒ 已改正措辞，`§8` 增设 `本机` 行类型。
  - **[次要8]** `P3-4` 六条 / Exit Criteria 四条 / Closure Gates 七条**三处对不上**（`check-masterplan-links.sh` 被悄悄漏掉）⇒ 已统一成「五条无条件 + 两条有条件」。
  - **[次要9]** 改版后 `Item Types` 计数过期（Phase 1 已是 10 条，Phase 3 并非 Fix-heavy）⇒ 已改准。
  - **[次要10]** `§0` 两处**承重引文的行号标错**：`:79` 是 P1.0 不是 P1.0a；被引的 STATE 先例把 P1.9 记成 `:89`（实为 `:90`，`:89` 是 P1.8b）⇒ 已逐条标注改正，并写明**论证本身不依赖行号，仍然成立**。
  - ⚠️ **第 2 轮明确复核过、未发现的三件**：未发现任何授权越红线之处（除 [主要5] 那处措辞冲突）· 裁判规则 3 得到遵守（未预设根因，`P1-6b` 确实拓宽了假设空间）· Minimum Rules 1/2/5/9/13/14 满足。
- Independent draft review iteration 3: **needs revision**（独立子代理 `ad70104653a874ea6`，**与前两轮均非同一位**）—— 判 **2 阻塞 / 4 主要 / 2 次要**，「可以转 active: **no**」。⚠️ **它逐条复核并确认第 2 轮的修复全部真落地**（悬空 `§N` 已清零；`807` 复现；`:99/:125/:133/:201/:219/:223/:234/:253`、`:57/:79/:82-87`、nginx `:77/:78/:83`、compose `:332/:387`、`gates.yml:349-351` 逐条实读无误；`02-WBS.md:79` 确为 P1.0 且状态源确为 `人 + MD:p1-insight`；skip→fail 机制确实成立；`§8` 的「上限低于地板价」已消除；P3-6 确已五条绑定且绑定 4 确实堵死了注释级提交）。**已全部改进**，逐条：
  - **[阻塞1，改正本 plan 一处承重数字与一个错结论]** B1 写的「11 次连续红」**是错的，实测是 16 次**（run `32820284450` / `f09b8f0` 起至 `32841381171` / `82a144a` 止）；更要紧的是初稿据此下的「**同一份代码**红 11 次绿 1 次 ⇒ 与**代码无关**」**站不住** —— 判定步的门禁项数在 streak 中途由 **52** 变 **54**（`32820284450` 逐字 `门禁 52 项：红 6，绿 46` vs `32838495432` 逐字 `54 项：红 2`），**项数变了就是判据集变了**。已改正，并按 **`e3afd77`（人修好 `AGENERP_LLM_BASE_URL` 那次）切分**：在它之前服务恒回 503、与本缺陷是两回事且不烧 token ⇒ **同口径样本只有 4 个（3 红 1 绿）**。改正已传播到「偏差③」（**降级为已化解**）、「裁判规则 4」一节、P1-2 的枚举范围。
  - **[阻塞2]** `§8` 第 0 行自锁：总闸的除数被写成「一次真解释的实测 token 数（**P1-3 的产物**）」，但 **P1-3 产出的是秒数**，而量 token 又必须先花掉几次调用、第 0 行却写着「先填这一行再动下面任何一行」⇒ **死循环**。已改正：token 数取自**服务自己的成本账**（P1.7 已交付的 `payload["cost"]["total"]`），并**明确放开**「探路闸之内的行可先于第 0 行落表」。
  - **[主要3 —— 本 plan 剩下的最后一个『绿了但没修』的口子]** 候选 (F)「让解释跑得更快」原来的护栏是「须论证不削弱 P1.4 / P1.7 的既有判据」，**评审实测该护栏无效**：`MAX_TURNS` 25→8 或削注入上下文之后，`test_evidence_gate_blocks_single_hop` 与 `test_explain_cost_accounting`（**只判形状**）**照样全绿**，且能同时满足 P3-6 的**全部五条**绑定。⇒ **护栏由「论证」升级为「停机」**：新增**停机分支 G**，(F) 一律归人，问题写成「**把解释做浅换门禁变绿可不可以？若可以，用什么判据保证它没变差？**」。
  - **[主要4]** 四条红线自证是**空转**：`git status --porcelain` 与裸 `git diff` **只看工作区**，**一提交就必然空输出**。已改成钉住开工基线 sha 的 `git diff --name-only $BASE..HEAD -- <paths>`，`BASE` 记进 `§8` 第 0 行。
  - **[主要5]** 第 2 轮加绑定时的**重编号残留**：P3-6 标题仍写「四条绑定」而实际列了五条；停机分支 F 的触发条件仍指「第 4 条」而机制陈述已顺延为第 5 条。已改准。
  - **[主要6 + 次要7]** 本仓**没有**读 DashScope 剩余额度的现成通道 ⇒ `P1-0` 若读不到，探路闸（6）低于地板价（16），**Phase 1 会在拿到机制陈述之前先停机**。已把这一点**写成明示的可能结局**（两条出路：人去控制台读一个数 / 按探路闸跑完停在分支 C），并逐字写明「读不到就当额度充足继续跑」**不是选项**；同时补上 `§0` 答复为「不归 loop」时的终局（`Plan Status` → `cancelled`）。
  - **[次要8]** 第 1 轮 [主要8] 的答复里指名的数据源已被第 2 轮 [阻塞2] 推翻 ⇒ 已就地标注「该一半已被推翻，按改后的读」。
- Independent draft review iteration 4: **needs revision**（mission-driver 评审步，任务 `2026-08-25-135246-mission-driver`，**非起草者**）—— 判 **2 主要 / 2 次要**，「可以转 active: **no**」，但**否决理由已不是 plan 的质量，是 `§0` 那条只有人能答的前置**（见 `> Review Hold`）。⚠️ **本轮独立复跑复核过的**：`02-WBS.md:88` 状态源确为单写 `人`、`:79`（P1.0）确为 `人 + \`MD:p1-insight\``、`:90`（P1.9）确为 `人` ⇒ §0 的冲突陈述**属实**；`python3 -m pytest tests/unit -q --collect-only` → **`807 tests collected`**（P3-4 的基线**今日仍成立**）；`gh run list --workflow=gates.yml` 独立复取 ⇒ **`f09b8f0`(32820284450) 起连续 16 次 `failure`、`82a144a`(32841381171) 止、`7af5493`(32841842429) `success`** —— **B1 第 3 轮改正的那个数属实**；`tests/gates/test_explain_service_live.py` 的 `_BODY.pytest.skip = _skip_is_a_failure_here` 确在、`tests/unit/test_explain_service_body.py` 的 `TIMEOUT = 30` 与 `:133` 那句写死文案确在、`tools/nginx/frappe.conf.template` 的「两者别混」与 `proxy_read_timeout 300` 确在 ⇒ **B2 / B3 / B5 的承重引文逐条属实**。**两条主要已直接改进本文件**：
  - **[主要1 —— 前三轮全漏]** `§8` 的地板价算术**漏掉本 plan 自己 push 触发的 run**。实读 `.github/workflows/gates.yml:6-10`：`on: push: branches: [main]`，**无 `paths:` 过滤** ⇒ **纯文档 push 照样跑满 `gates-l2-live`**，照样烧 2／4 次真解释。⚠️ **这条事实 B1 里本来就写着**（逐字「每次 `push` 都会触发 `gates.yml`」），却**只用在 P3-6 的防刷绑定上，从未传播进 `§8`** —— 同一条事实一处承重、一处被忽略。而 P1-7 / P2-2 / P3-8 **必然要推**。⇒ 已把地板价 **16 修正为 ≈22**，并把「现算 < 16 停机」**一并改为 < 22**；另写死减轻手段（阶段内文档提交攒起来一次 push —— 实证依据：`45ee997` 与 `7af5493` 同批 push，`gh run list` 里只出现一个 run），且**每一次 push 触发的 run 都必须进表**，用途列记 `顺带（文档 push 触发）`。
  - **[主要2 —— 第 3 轮 [主要4] 的修复没传播完]** 第 3 轮已实证 `git status --porcelain` / 裸 `git diff` **只看工作区、一提交就恒空输出**，并据此把 `Closure Gates` 四条红线自证改成 `$BASE..HEAD`；但**三处 phase 级 `Exit Criteria` 仍是旧的空转口径**，而它们才是**每个阶段边界上先被检的那一层**（`Closure Gates` 要到收口才跑）：Phase 1「仓内代码零改动」用 `git status --porcelain`（而本阶段必然提交取证件）· Phase 2「`D-b-1`…`D-b-8` 零改动」用裸 `git diff`（而 `D-b-9` 必然提交）· Phase 3「`STATE.md` 只追加」用裸 `git diff`（而 P3-8 的追加行必然提交）。⇒ **三条逐条改成 `$BASE..HEAD` 口径**（Phase 1 另保留 `--porcelain` 一条管未提交的，两条都要跑）。
  - **[次要3]** `Closure` 节缺模板里的 `Follow-up:` 子节；**[次要4]** Phase 2 Exit Criteria 的红线枚举写「1/2/3/5/6/7」漏了红线 4（改名），`Closure Gates` 已覆盖。**两条次要留着**，按评审规则交给收口审计与深度审计。
  - ⚠️ **本轮未发现越红线之处**：`tests/gates/**` · `.github/workflows/**` · `DECISIONS.md` · `docs/masterplan/` 非追加改动 —— 本轮对本仓的写入**只有这一个 plan 文件**。
- Independent draft review iteration 5: **needs revision**（mission-driver 评审步，任务 `2026-08-25-203256-mission-driver`，**非起草者、非前四轮任何一位**）—— 判 **0 阻塞 / 5 主要 / 4 次要**，「可以转 active: **no**」，**否决理由仍不是 plan 质量，是 `§0` 那条只有人能答的前置**（见 `> Review Hold`）。
  ⚠️ **本轮把第 4 轮记的那条经验当成了方法论 —— 「专门查一遍已改正的结论有没有传播完，不要只查新事实」 —— 五条主要里有四条正是这一类，说明那条经验是对的。**
  ⚠️ **本轮独立复跑复核过、逐条属实的**：`gh run list --workflow=gates.yml -L 6 --json ...` ⇒ `cc205d6`(32846332776) `success` · `7af5493`(32841842429) `success` · `82a144a`(32841381171) / `758b7bc`(32839163713) / `e3afd77`(32838495432) `failure` ⇒ **B1 的「同口径 3 红 2 绿、样本 5 个」属实，且自第 4 轮以来无新 run**（`origin/main` 仍是 `cc205d6`）；`git log --date=iso` ⇒ `d6c0fa2` 20:07:12 → `cc205d6`（人 `lize`）20:12:26 → `caa051e` 20:13:21 ⇒ **`> Review Hold` 里那条「人有过机会但没答」的改正属实**；`sed -n '88p' docs/masterplan/02-WBS.md` ⇒ 状态源确为**单写 `人`**；`grep -n '11:18Z' docs/masterplan/STATE.md` ⇒ **`[needs-human] 2026-08-25T11:18Z` 仍在、仍无人答复** ⇒ **`§0` 的前置今天依然不成立。****五条主要已全部就地改进本文件**，逐条：
  - **[主要1 —— B1 内部自相矛盾，且矛盾的那一句是「计数依据」]** B1 的 🔴 子条 ② 已把口径改成「3 红 2 绿 / 样本 5 个」，但**后面**那条 ✅ 子条仍以无限定语气写着「同口径下是 3 红 1 绿…**Phase 1 的一切计数以这个切分为准**」⇒ **顺读的执行者最后看到的是被取代的那个数，而它自称是权威计数依据。**更要紧的是 ② 里写「原文保留在**上一子条**里」——**方向也指反了**，那段在**下面**。⇒ 已就地把 ✅ 子条那句权威语标为取代（`~~删除线~~` + 🔴 标注，**正文按本 plan 惯例不改写**），并把指针改为「下方那条 ✅ 子条」。
  - **[主要2 —— 一条 `Proof` 的枚举范围仍建立在被推翻的数上]** `P1-6b` 逐字「**11 红 1 绿那几次 sha 之间** … `git diff --stat` 逐对」。**11 这个数第 3 轮就被实测推翻（实为 16）**，口径也早已按 `e3afd77` 切分；第 3 轮自称改正「已传播到 P1-2 的枚举范围」，**`P1-6b` 被漏掉**。⇒ 这不是措辞问题，**它定的是该 `Proof` 要枚举哪些 sha**。已改为同口径那 5 个并逐个点名，另写明「`f09b8f0`…`e3afd77`^ 那段单独查、不许与同口径混数」。
  - **[主要3 —— 停机分支 F 引用了 B1 已经删掉的那个结论]** 分支 F 逐字「B1 实证：**同一份代码 11 次红之后自己绿了一次**」。**两处都已被第 3 轮推翻**：数是 16 不是 11；**「同一份代码」本身不成立** —— 判定步门禁项数在 streak 中途 52→54，B1 已据此把「与代码无关」**删掉**。⇒ **分支 F 是本 plan 自称「最重要的一节」里最可能被触发的一条**，它援引的证据必须是现行的。已改写为「同口径下 3 红 2 绿，而修复一行都还没写」——**论证不依赖那个数，改后更强**。
  - **[主要4 —— 未定义的 `Plan Status` 值]** Phase 2 Exit Criteria 写 `Plan Status` 转 **`blocked`**，而 `00-plan-authoring-and-execution-guide.md` 的「Plan Status Flow」**只定义 7 个值，其中没有 `blocked`**。⚠️ 指南 Minimum Rule 12 已逐字警告过状态词与自动化流程的耦合（`completed` 与未打勾项会让 `EXECUTE`/`VERIFY` 死循环）⇒ **一个未定义值同样是自动化选路上的坑**。已改为 **`deferred`**（指南给「停机、不再以原形态持有 closure」的那个词），与 Phase 1 Prereqs 已写死的 `cancelled` 出口同属指南状态集。
  - **[主要5 —— 第 3 轮 [主要5] 那类重编号残留在新写的段落里复发]** B1 的 🔴 子条 ② 结尾逐字「这正是 P3-6 那**四条**防刷绑定与停机分支 F 存在的理由」，而 **P3-6 实列五条**（第 2 轮加绑定 4、原绑定 4 顺延为 5，第 3 轮已把 P3-6 标题与分支 F 改准）。⇒ **该子条是第 4 轮/起草步新写的，把已改正的旧数又写了回来。**已改准并标注来由。
  - **[次要6]** 「偏差③」仍写「同口径的真调模型 run 只有 **4 次**」（未计入 `cc205d6`）⇒ 改为 5 次。**[次要7]** 「裁判规则 4」一节仍只写「其后 `7af5493` 转绿」，漏第二次绿 ⇒ 已补。**[次要8]** B1 「其后 `caa051e` / `b02fd7a` **两个** loop 文档提交尚未 push」⇒ 实测已是**三个**（新增 `25ba01d`），已改准（这条直接影响 `§8` 那笔「本 plan 自己 push 也烧额度」的账，虽不改变 ≈22 的地板价）。**[次要9]** 第 4 轮留的两条次要（`Closure` 缺 `Follow-up:` 子节、Phase 2 红线枚举漏红线 4）⇒ **本轮顺手补齐，不再往下传**。
  - ⚠️ **本轮复核过、未发现问题的**：无悬空 `§N` 指针 · 每个执行项都有 `Fix`/`Add`/`Decision`/`Proof`/`Follow-up` 标签且 `Item Types` 计数与实际条数吻合（Phase 1 = 10、Phase 3 = 8）· 三个 Phase 均 `Status: planned` 且全部 `[ ]` ⇒ Minimum Rule 12 的 `grep -B5 "\- \[ \]" | grep "Status: completed"` **空** · 指南 Anti-Slacking 那 6 个禁用词（原文见 `00-plan-authoring-and-execution-guide.md`，**此处刻意不逐字复述，免得给后续 `grep` 自查制造假阳性**）**在范围内条目中零命中** · 两条 `Deferred But Adjudicated` 均有 Classification / Why Not Blocking / Successor 三项且重开事件写死 · 模板必填小节齐全。
  - ⚠️ **本轮未发现越红线之处**：`tests/gates/**` · `.github/workflows/**` · `DECISIONS.md` · `docs/masterplan/` 非追加改动 —— **本轮对本仓的写入只有这一个 plan 文件**（`git diff --name-only` 可复核）。
- ⚠️ **收敛状态照实记，不粉饰**：**五轮**独立评审（互不相同）共提 **8 阻塞 / 23 主要 / 17 次要**，**每一条都已改进本文件**（第 4 轮留的两条次要已由第 5 轮补齐，**当前无任何一条留给收口审计**），但**五轮都判 `needs revision`，consensus 未达成** ⇒ **`Plan Status` 保持 `draft`。**
  - **逐轮阻塞数：4 → 2 → 2 → 0 → 0**（第 4、5 轮均零阻塞）。**主要数：7 → 5 → 4 → 2 → 5。**⚠️ **第 5 轮的 5 条主要不是质量回退，也不该被读成「收敛失败」** —— 逐条看，**五条里有四条是「早已改正的结论没有传播到该去的地方」**（B1 内部矛盾 · `P1-6b` 枚举范围 · 分支 F 的引文 · 「四条绑定」复发），**第 5 条是格式不合规（未定义的 `blocked`）**，**没有一条是新发现的事实错误或新的红线风险**。⇒ **事实层面已经收敛，传播层面还在漏** —— 这正是第 4 轮记下那条经验的原因，本轮把它当检查项跑了一遍就捞出四条。
  - 🔴 **但第 4 轮仍判 `needs revision`，理由与前三轮不同，必须分清**：前三轮卡的是 plan 自己的缺陷；**第 4 轮卡的是 `§0` 那条只有人能答的前置**。⇒ **再起第 5、6 轮评审也不会让它转 `active`** —— 评审改得动的部分已经改完了。
  - ⚠️ **第 4 轮同时暴露了一件值得记下的事**：它找出的两条主要**都是「前面几轮已经知道的事实没有传播到该去的地方」**（push 触发 CI 这条事实在 B1 有、`§8` 没有；空转 git 自证的修复在 `Closure Gates` 落了、三处 `Exit Criteria` 没落）。⇒ **后续任何一轮评审都应当专门查一遍「已改正的结论有没有传播完」，不要只查新事实。**🔴 **第 5 轮实测把这条经验坐实了**：本轮 5 条主要里 **4 条**是这一类，其中一条（B1 的「四条绑定」）**是第 3 轮已改正过、又在第 4 轮新写的段落里写回去的**。⇒ **凡在评审中新写、改写任何一段正文，都要回头查它有没有把旧数、旧结论重新引进来。**
  - 🔴 **本轮起草步不再起第 4 轮评审，理由不是评审收敛了，而是它已经不能改变结果**：本 plan 转 `active` 的两个条件是**合取** —— ①评审 consensus ②`§0` 那条 `[needs-human]` 有人答复。**②今天无论如何都不成立**（问题只有人能答），⇒ 再起第 4 轮**改变不了 `draft` 这个结论**，只会继续花额度。**这是把账算过之后的选择，不是评审做不下去了。**
  - ⚠️ **因此本 plan 交到人手上时的真实状态是：一份经过五轮独立评审、逐条改进过、但尚未获得评审 consensus 的 `draft`。**（⚠️ **第 5 轮改正**：本句原写「三轮…先跑第 4 轮评审」，第 4、5 轮跑完后已过期。）**不要**把它当成「评审已通过」；但**也不要**把「再跑一轮评审」当成解锁手段 —— 见下一条。
  - 🔴 **第 6 轮就地取代整个「收敛状态」小节（上面五条原文保留、不改写）。现行状态：**
    **六轮**独立评审（互不相同）共提 **11 阻塞 / 30 主要 / 20 次要**，**每一条都已改进本文件，当前无任何一条留给收口审计**。**逐轮阻塞数：4 → 2 → 2 → 0 → 0 → 3**；**主要数：7 → 5 → 4 → 2 → 5 → 7**。
    ⚠️ **第 6 轮阻塞数从 0 反弹到 3，不是质量回退，成因写清楚**：三条里 **[阻塞1] 与 [阻塞3] 都是「人在 `f144475` 裁定之后，plan 里那些以『人还没答』为前提写下的段落集体过期」** —— 是外部事实变了，不是 plan 变差了；**[阻塞2] 是 B1 第三次被 CI 追上**（同口径样本 4→5→6），**而这一次追上的方向恰好是本 plan 预言的失败形态**。七条主要**全部**是「已改正的结论没传播完」这一类，**再次坐实第 4、5 轮记下的那条经验**。
    🔴 **上面第 3 条「再起第 5、6 轮评审也不会让它转 `active`」这句判断，被第 6 轮证伪了 —— 照实记，不粉饰**：它当时成立，是因为 ② 那个条件卡在人手里；**人在 `f144475`（`20:45:24`）答了之后，第 6 轮就是那个「能改变结果」的评审**。⚠️ **教训**：「评审改不动的部分」是**相对当下事实**说的，**外部事实一变，同一份 plan 就可能需要一轮全新的评审去追**——**不要把一次「再评审也没用」的判断当成永久结论**。
    ⚠️ **本 plan 现在的真实状态：一份经过六轮独立评审、逐条改进过、第 6 轮判 `passes draft review` 且「可以转 `active`: yes」的 `draft`。**转 `active` 的两个合取条件**均已满足**（① 第 6 轮 consensus ② 人已答复），**但状态词本身由上级改，第 6 轮未改。**
- ~~🔴 **即使后续某一轮判 `accept`，本 plan 仍不得转 `active`** —— 还须等 `§0` 那条 `[needs-human]`（`STATE.md` 2026-08-25T11:18Z）有人答复。**两个条件是合取，缺一不可。**~~
  ✅ **第 6 轮实测：该合取的第 ② 项已成立，本条随之过期（原文保留不改写）。** 逐条对照：**① 评审 consensus** —— 由第 6 轮给出（见下条 iteration 6）；**② `[needs-human] 11:18Z` 有人答复** —— 人在 `f144475`（author `lize`）+ `STATE.md` §3 `[resolved] 12:45Z` 答复「是笔误 / 归 loop」。⇒ **两条都已满足。**
  ⚠️ **但转 `active` 这个动作本身仍不由评审做** —— 指南 `00-plan-authoring-and-execution-guide.md`「When Executing」第 2 条逐字「Change to `active` only after the draft-review record shows the plan is acceptable for execution」，**评审的产出是这条记录，改状态词由派出本轮评审的上级执行。第 6 轮未改 `Plan Status`。**

- Independent draft review iteration 6: **passes draft review**（独立子代理，**非起草者、非前五轮任何一位**）—— 判 **3 阻塞 / 7 主要 / 3 次要，全部已就地改进本文件**；**「可以转 active: `yes`」**。⚠️ **本轮是六轮里第一次给出 `passes`，理由必须写清楚：不是 plan 突然变好了，而是卡住前五轮的那个唯一未决项（`§0` 的 source-of-truth 冲突）被人裁掉了。** 本轮把「已改正的结论有没有传播完」当成首要检查项跑（第 4、5 轮记下的那条经验），仍捞出 7 条主要，全是这一类。
  **本轮实跑命令与关键输出（逐条，不转述）**：
  - `git log -1 --format='%H %an <%ae> %ad' --date=iso f144475` ⇒ `f1444757fddd3c259636d3e2b0e914a6f5e9d121 lize <lize-agent-engineering@users.noreply.github.com> 2026-08-25 20:45:24 +0800` ⇒ **author 是人，不是 loop 的 `Email Agent Developer <developer@mailagent.com>`**（若是后者即红线 5 自行消解冲突，本轮以此为第一道验）。
  - `git show f144475 --stat` ⇒ `docs/masterplan/02-WBS.md | 2 +-` · `docs/masterplan/STATE.md | 6 ++++++`（2 files changed, 7 insertions(+), 1 deletion(-)）⇒ **`02-WBS.md` 只动 1 行，`STATE.md` 纯追加。**
  - `git show f144475 -- docs/masterplan/02-WBS.md` ⇒ `P1.8a-fix` 行末一格 `人` → `` `MD:p1-explain` ``，**行内其余文字逐字未动**。
  - `git show f144475 -- docs/masterplan/DECISIONS.md` ⇒ **零输出**（红线 3 未被触碰，无新增 `R-x`）。
  - `sed -n '88p' docs/masterplan/02-WBS.md` 行末一格 ⇒ `` `MD:p1-explain` ``；对照 `:78` `MD:p1-insight` · `:79` `人 + MD:p1-insight` · `:89` `MD:p1-explain` · `:90` `人`。
  - `grep -n '11:18Z' docs/masterplan/STATE.md` ⇒ **`:914` 仍逐字是 `[needs-human]`（红线 5 append-only，原行不许改）**，而 **`:432` 是新增的 `[resolved] 2026-08-25T12:45Z`，标题逐字点名答复的就是它**。⚠️ **只看 `:914` 会得出「仍无人答复」的假阴性，这正是第 5 轮的读法**；已把正确判法写进顶部 Review Hold 取代条。
  - `git fetch origin` 后 `git log origin/main..HEAD --oneline` ⇒ **零输出**；`HEAD` == `origin/main` == `f924ac6`。
  - `gh run list --workflow=gates.yml -L 10 --json databaseId,headSha,conclusion,event,createdAt,status` ⇒ `32850335965`/`f924ac6` **`in_progress`** · `32849370852`/`f144475` **`success`** · `32846332776`/`cc205d6` `success` · `32841842429`/`7af5493` `success` · `32841381171`/`82a144a` `failure` · `32839163713`/`758b7bc` `failure` · `32838495432`/`e3afd77` `failure`。
  - `gh run view 32849370852 --json jobs` ⇒ 14 个 job 全 `success`，含 **`L2 全量 live 判定（全部门禁，零 skip）` = `success`**。
  - `python3 -m pytest tests/unit -q --collect-only` ⇒ **`807 tests collected in 0.18s`**（P3-4 的基线**今日仍成立**，第 6 轮独立复现）。
  - 承重引文抽验（`sed -n`）：`tests/unit/test_explain_service_body.py:99` = `TIMEOUT = 30` · `:133` = 那句写死的「够不到 …… 同源前端没在跑」 · `:222` = `assert "AGENERP_LLM_" in payload["error"]` · `tests/gates/test_explain_service_live.py:57` = `_load(...)`、`:80` = `_BODY.pytest.skip = _skip_is_a_failure_here`、`:82-87` = 六条改名导出 · `tools/nginx/frappe.conf.template:77` = 「两者别混」、`:83-84` = `proxy_read_timeout/proxy_send_timeout 300` · `.github/workflows/gates.yml:6-10` = `on:`/`push:`/`branches: [main]`/`pull_request:`/`workflow_dispatch:`。**逐条属实。**
  **三条阻塞（均已就地改进）**：
  - **[阻塞1 —— `> Review Hold` / `§0` / 记录末尾那条 🔴 全部过期，而它们是本 plan 的最高层结论]** 三处逐字写着「转 `active` 需两个合取条件，② `[needs-human] 11:18Z` 有人答复」，而 ② 已于 `f144475` + `[resolved] 12:45Z` 成立。**过期的不是措辞，是本 plan 的执行开关** —— 照旧文读，这份 plan 永远开不了工。⇒ 已按本 plan 惯例（原文保留 + 标注取代）在三处就地改准，并把「人已答且答的是归 loop」的五条实测证据写进顶部。**另补一条方法论警告**：第 5 轮那条 `grep '11:18Z'` 自查会给假阴性，正确判法已写死。
  - **[阻塞2 —— B1 又过期，且这次过期的方向正好是本 plan 最怕的那个]** B1 记「3 红 2 绿 / 样本 5 个」，实取已是 **3 红 3 绿 / 样本 6 个**（新增 `f144475` / run `32849370852`，`L2 全量 live 判定` 逐 job 实读为 `success`）。⚠️ **`7af5493` / `cc205d6` / `f144475` 三次连绿此刻已经躺在 `main` 上，三次全在任何修复提交之前，三次全由纯文档 push 触发** ⇒ **人在 `cc205d6` 正文警告的「攒够三次运气就能伪装成修好了」已经发生**，`02-WBS.md:88` 的字面验收（连续 3 次 run 全绿）**在零修复的仓库上已被满足**。⇒ 已在 B1 加 🔴🔴 现行计数条并把旧计数标为取代；**P3-6 绑定 1 与停机分支 F 就地升级为「已被实测触发的必需品，任何轮次不得放宽」**；同步改准 P1-2 / P1-6b 的枚举范围（5→6 个 sha）、`§8` 的排除清单（两次→三次）、「裁判规则 4」一节（补第三次绿 + 补一条「CI 恰好绿着不等于缺陷没了」的反向推论）。
  - **[阻塞3 —— P3-8 与 `Closure Gates` 末条的授权陈述现在是反的]** 两处逐字写「**不代人写 roadmap 10b 的状态词** —— 状态源是 `人`」，而 `f144475` 之后状态源是 `` `MD:p1-explain` `` ⇒ **按表规 2 的定义，这一行的状态词本来就归 loop 维护，不写反而是漏做。**⚠️ 改这一条等于放宽授权，因此**不是简单删掉**：已改写成「应当写，但受三条硬约束」—— ① `02-WBS.md` 只许改第 88 行的「状态」一格（自证：该文件 diff 必须只有 1 增 1 删且都在 `:88`）② 状态词必须与三个 run id + 修复 sha 同一次提交（`02-WBS.md:88` 逐字要求）③ **P3-6 五条绑定未全部满足时一个状态词都不许写**（阻塞 2 已实证「CI 是绿的」从来不是依据）。`Closure Gates` 末条同步改写为可证伪的双出口。
  **七条主要（均已就地改进）**：
  - **[主要4]** B1 的 ① 仍写「`caa051e` / `b02fd7a` / `25ba01d` 三个 loop 文档提交尚未 push、`gh run list` 里查无此三者」⇒ 实测**已全部推出、未推提交为 0**（`origin/main` == `HEAD` == `f924ac6`）。已改准，并指出这反证了 `§8` 那笔「本 plan 自己 push 也烧额度」**当天连续发生了六次**。
  - **[主要5]** 「偏差③」仍写「同口径的真调模型 run 只有 5 次」⇒ 改为 **6 次**（另有 1 次 `in_progress` 未计），并补一句：**6 已贴近 job 注释自称的「9 次烧光」上限 ⇒ `P1-0` 读真实额度的紧迫性上升而非下降**（本 plan 一次调用都没花，池子却可能已被 loop 的文档 push 消掉大半）。
  - **[主要6]** `P1-2` 的取证覆盖面仍写「那五次」⇒ 改为**六次三绿**，并写死「须复取 `f924ac6` / run `32850335965` 的终态」。
  - **[主要7]** `P1-6b` 的 `git diff --stat` 逐对枚举仍是 5 个 sha ⇒ 扩到 6 个（补 `f144475`），**漏它等于漏掉三连绿的最后一环**。
  - **[主要8]** `P3-6` 绑定 1 只点名两次前置绿 ⇒ 改为三次，并写明本条绑定的性质已由预防性变为唯一屏障。
  - **[主要9]** `§8` 逐 run 表下方的排除说明只排除两次绿 ⇒ 改为三次 + `in_progress` 那次的复取要求。
  - **[主要10]** 停机分支 F 的承重引文仍是「3 红 2 绿」⇒ 改为 3 红 3 绿，并补上最关键的那半句：**那三次绿是连续的**。（这与第 5 轮 [主要3] 是同一处、同一类复发 —— **B1 一改，分支 F 必须跟着改，已在本条写死这条联动。**）
  **三条次要（均已就地改进）**：
  - **[次要11]** `§8` 引 `.github/workflows/gates.yml:6-10` 时写「触发器逐字是 `on: push: branches: [main]`」，实读那 5 行还含 `pull_request:` / `workflow_dispatch:` ⇒ 引文补全（承重的「push 到 main 且无 `paths:` 过滤」实测无误，结论不变）。
  - **[次要12]** 两处把 `_BODY.pytest.skip = _skip_is_a_failure_here` 记在 `tests/gates/test_explain_service_live.py:79`，实读在 **`:80`**（`:69` 是该函数定义处）⇒ 正文那处已改准；`Draft Review Record` 里的历史记述按惯例不改写。
  - **[次要13]** `02-WBS.md` 的状态源 `MD:p1-explain` **在 `missions/` 下没有对应文件**（只有 `p1-insight.json`），而本 plan 头部是 `> Mission: p1-insight` ⇒ 早于本行的命名残留、非 `f144475` 引入、且工作项 10 同样是 `MD:p1-explain` 而由本 mission 交付 ⇒ **归属无歧义**，已登记为 `Deferred But Adjudicated` 的 watch-only 一条，不重开归属问题。
  ⚠️ **本轮复核过、未发现问题的**：三个 Phase 均 `Status: planned` 且全部 `[ ]` ⇒ Minimum Rule 12 的 `grep -B5 "\- \[ \]" <plan> | grep "Status: completed"` **空输出（exit 1）** · `Plan Status` 值 `draft` 落在指南定义的七个状态内 · Anti-Slacking 那 6 个禁用词全文零命中 · `§8` 地板价 **≈22 次真解释**（P1-6 的 8 + P3-6 的 6 + 本机 2 + 自身 push 的 6）**逐项重算仍成立**，探路闸 ≤6（本机 2 + 一次红 run 4）仍够用且仍低于地板价 ⇒ `P1-0` 那条「Phase 1 可能在拿到机制陈述前先撞停机分支 C」的结构性风险**依然有效，本轮未放宽** · 停机分支 A–G 七条互相自洽，**除分支 F 的引文外无一条引用被推翻的证据**（已改） · 三个 Phase 的 `Item Types` 计数与实际条数吻合（Phase 1 = 10、Phase 2 = 3、Phase 3 = 8） · Phase 3 `Exit Criteria` 5 条与 P3-4「五条无条件 + 两条有条件」、`Closure Gates` 的表述三处一致 · 无悬空 `§N` 指针 · 前五轮改正过的那五个旧数（`11 次` / `3 红 1 绿` / `四条绑定` / 地板价 `16` / `两个未 push`）**在正文中零残留**，仅存于 `Draft Review Record` 的历史记述里（合本 plan 惯例）。
  ⚠️ **本轮未发现越红线之处**：`git status --porcelain -- tests/gates/ .github/workflows/` → **无输出**（红线 1/2）· `git diff --name-only -- docs/masterplan/` → **无输出**（红线 3/5，本轮对 `docs/masterplan/**` **零写入**，连 `STATE.md` 追加都没做）· 证据仓 `XM_PATH` 未写（红线 6）· 未生成 Server Script（红线 7）· 未改项目名/包名/命名空间（红线 4）· **本轮对本仓的写入只有这一个 plan 文件**，且**未执行本 plan 的任何一个执行项**（未跑 CI、未改 `docker-compose.yml` / `tools/nginx/**` / `agenerp/**`）。
  ⚠️ **本轮未改 `Plan Status`** —— 那个动作由派出本轮评审的上级做（见上一条）。

- Independent draft review iteration 7: **passes draft review**（mission-driver 评审步，任务 `2026-08-26-094345-mission-driver`，**非起草者、非前六轮任何一位**）—— 判 **2 阻塞 / 4 主要 / 1 次要，全部已就地改进本文件**；**「可以转 active: `yes`」，并已由本轮执行该状态变更**（`draft` → `active`）。
  ⚠️ **本轮把第 4–6 轮反复记下的那条经验当首要检查项**（「专查已改正的结论有没有传播完」），**但本轮两条阻塞都不是传播缺口，而是外部事实在第 6 轮之后又变了两次** —— 这本身是对 B1「本 plan 最易腐的一节」那句判断的第二次实证。
  **本轮实跑命令与关键输出（逐条，不转述）**：
  - `git log -1 --format='%H %an <%ae> %ad' --date=iso f144475` ⇒ `f1444757fddd3c259636d3e2b0e914a6f5e9d121 lize <lize-agent-engineering@users.noreply.github.com> 2026-08-25 20:45:24 +0800` ⇒ **第 6 轮那条「人已裁定、归 loop」的承重证据独立复核属实。**
  - `sed -n '88p' docs/masterplan/02-WBS.md` ⇒ 行末状态源一格确为 **`` `MD:p1-explain` ``** ⇒ **`§0` 的授权前置今天仍然成立。**
  - `git show f144475 -- docs/masterplan/DECISIONS.md` ⇒ **零输出**（红线 3 未被触碰）。
  - `git log --oneline -8` ⇒ **第 6 轮之后 `main` 又长了两个 commit**：`7a217a2`（人，2026-08-25 21:27:13）· `fecbd59`（人，2026-08-26 09:45:11）。**两者都只改 `docs/masterplan/STATE.md`、都是纯追加。**
  - `gh run list --workflow=gates.yml -L 12 --json databaseId,headSha,conclusion,event,status,createdAt` ⇒ `32920159448`/`fecbd59` **`in_progress`** · `32853424473`/`7a217a2` **`success`** · `32850335965`/`f924ac6` `success` · `32849370852`/`f144475` `success` · `32846332776`/`cc205d6` `success` · `32841842429`/`7af5493` `success` · `32841381171`/`82a144a` `failure` · `32839163713`/`758b7bc` `failure` · `32838495432`/`e3afd77` `failure`。
  - `gh run view 32853424473 --json jobs` ⇒ 14 个 job 全 `success`，含 **`L2 全量 live 判定（全部门禁，零 skip）` = `success`**。
  - `sed -n '335,360p' .github/workflows/gates.yml` ⇒ `取证 —— 服务状态` 步逐字 `if: always()` + `run: docker compose -f docker-compose.yml ps` ⇒ **该步确实在红 run 与绿 run 上都会跑**（这一条是本轮 [阻塞1] 处置的依据）。
  - `grep -B5 "^\- \[ \]" <plan> | grep "Status: completed"` ⇒ **空（exit 1）** ⇒ Minimum Rule 12 满足。
  **两条阻塞（均已就地改进）**：
  - **[阻塞1 —— 人在 loop 停机期间做了本工作项的归因取证并逐字「交接给 loop」，而 plan 里一个字都没有]** commit `7a217a2`（author `lize`）往 `STATE.md` 追加 `[open] 2026-08-25T13:27Z`，逐字「**这把缺陷的描述整个换掉了**」：实读 run `82a144a` 的 `docker compose ps` ⇒ `db`/`redis` `Up 4 minutes` 而六个 frappe 系容器全是 `Up 2 minutes`、`CREATED 4 minutes ago` ⇒ **整组 frappe 容器在起栈约 2 分钟后集体重启过一次**。⚠️ **这是 Minimum Rule 1 意义上的活基线变更，不是一条可选参考** —— 照旧文执行，Phase 1 会拿着一份人已经亲手撤销过线索的假设空间去取证。⇒ 已新增 **B7** 逐字登记（含人自己标注的「零命中不等于没发生」「未查明为什么会重启」「不许据此猜根因」三条限制），并**传播**：`P1-2` 增设 **ps 重启指纹**一格（零额度、红绿 run 都有）· `P1-5` 改正「CI 侧没有数据路径」那条已裁定结论 · `P1-6b` 让出第三机制名额并新增「为什么会重启」必查项 · Phase 1 `Exit Criteria` 由两条方向改为**三条具名方向**并新增一条分歧处置 · `P2-1` 新增候选 **(H)**。
    🔴 **本轮特意没有做的一件事**：人写「**是连不上**，不是 502」，而 B2 实读的抛点是 `recv_into`（`conn.request()` 已成功、连接已建立）—— **两句字面不一致。本轮不替任何一侧消解**（裁判规则 3），只把分歧写进 B7 并变成 Phase 1 的一条判据。
  - **[阻塞2 —— B1 第四次被 CI 追上，且这次追上时 plan 正在被评审]** B1 的「唯一权威计数」记 3 红 4 绿 / 样本 7，实取已是 **3 红 5 绿 / 样本 8**（新增 `7a217a2` / run `32853424473`，逐 job 实读 `L2 全量 live 判定` = `success`），**连续绿达 5 次，超出验收要求的 3 次两次**；另有 `fecbd59` / run `32920159448` `in_progress`。⇒ 已在 B1 增 🔴🔴🔴🔴 现行计数条并把旧计数标为取代，**传播**至 `P1-2`（六次→八次）· `P1-6b`（6→8 个 sha）· `P3-6` 绑定 1（四次→五次）· `§8` 排除清单（三次→五次）· 「裁判规则 4」一节（连绿 3→5）· 停机分支 F（3 红 3 绿→3 红 5 绿）。
    ⚠️ **本轮实测期间 `HEAD` 从 `7a217a2` 变成 `fecbd59`**（另一个 loop 步在评审进行中提交并推送）⇒ **B1 的易腐性已由第 6、7 两轮连续实证**，`P1-2` 那条「首次取数必须重取、不许照抄本文件任何一个数」**不得放宽**。
  **三条主要（均已就地改进）**：
  - **[主要3]** Phase 1 `Exit Criteria` 的「第三格必答」原文是「既非 B3 也非起栈时序的机制」—— B7 把「重启窗口」升为具名方向 ③ 之后，**照旧文可以拿「重启窗口」交差，等于假设空间在刚被拓宽的当天又关回三条** ⇒ 已改为「**既非 B3、既非起栈时序、也非 B7 重启窗口的第四种机制**」，并在 `P1-6b` 内逐字写明「不许拿重启窗口交本条的差」。
  - **[主要4]** 人给的修法方向「判定前加一道稳定性等待」**不在 `P2-1` 的候选清单里**（A/B/C/D/E/F 六条均不覆盖它）—— 而 `P2-1` 逐字自称「**至少覆盖**」，且本 plan 别处反复写死「不留给执行期临场发挥」⇒ 已补为候选 **(H)**（字母跳过 `G` 以免与停机分支 G 混淆），**预先判过红线归属**（落 compose/nginx 不在红线内；落 `gates.yml` 是红线 2 判断题，走 (B) 的既有规矩），并写死两条出局规矩：**无条件 `sleep` 或把等待时长调到「红变绿为止」= 迁就环境 ⇒ 停机分支 B**；**等待不解释「为什么会重启」⇒ 按绑定 5 / 分支 F 处理**。
  - **[主要5 —— 四条红线自证会把人的提交算到 loop 头上，本轮实测当场撞上]** `Closure Gates` 与三个 Phase `Exit Criteria` 的自证都是 `git diff $BASE..HEAD -- <路径>`，而**那个区间装的是所有人的提交**。⚠️ **评审进行中人推了 `d69b335`（D-25 批准浏览器驱动，属工作项 11），它改了 `docs/masterplan/DECISIONS.md`** ⇒ **照旧文跑，红线 3 那条自证立刻非空，而 loop 一个字节都没碰过它**；反过来同样危险 —— 一条真正的 loop 越线也会被「可能是人改的」搪塞掉。⇒ 已补**两步法**（空输出到此为止；非空则 `git log $BASE..HEAD --format='%h %an <%ae>' -- <路径>` **逐 commit 归属**，只有 author 为人的可排除，**只要有一个 author 是 loop 就是越线、不许自辩**），并在三个 Phase 的对应 `Exit Criteria` 各加一条指针。**这是收紧，不是放宽。**
  - **[主要6]** `Last Reviewed` 仍是 `2026-08-25`，而本轮实跑日期是 **2026-08-26**（`date` ⇒ `2026-08-26 09:45:28 +0800`）⇒ 已改准。
  **一条次要（已就地改进）**：
  - **[次要7]** 顶部前言缺一条给下一轮读者的现行状态指引（前六轮的 Hold / 取代条层层堆叠，顺读极易读成「仍卡在 `draft`」）⇒ 已在 `> Audit: required` 之后补一条 🆕 现行状态条，逐字写明本轮已转 `active` 与两条阻塞的处置去向。
  ⚠️ **本轮复核过、未发现问题的**：三个 Phase 均 `Status: planned` 且全部 `[ ]` ⇒ Minimum Rule 12 的 grep **空输出** · Anti-Slacking 那 6 个禁用词在范围内条目中**零命中**（本轮新增文字同样零命中） · `Item Types` 计数与实际条数仍吻合（Phase 1 = 10、Phase 2 = 3、Phase 3 = 8 —— **本轮只扩写既有条目，未增删任何执行项**） · 模板必填小节齐全 · 无悬空 `§N` 指针 · 停机分支 A–G 七条与新增候选 (H) 的映射自洽（(H) 的两个出局出口分别指向分支 B 与分支 F，不新增分支） · `§8` 地板价 **≈22** 与探路闸 **≤6** 本轮未放宽 · `P1-0` 那条「Phase 1 可能在拿到机制陈述前先撞停机分支 C」的结构性风险**依然有效** —— ⚠️ **但 B7 的 ps 指纹一格是本 plan 第一条零额度证据路径，它把这条风险的实际概率压低了，本轮据此未改任何闸值。**
  ⚠️ **本轮未发现越红线之处**：`git status --porcelain` ⇒ **只有这一个 plan 文件被修改**；`tests/gates/**` · `.github/workflows/**` · `DECISIONS.md` · `docs/masterplan/**` **本轮零写入**（连 `STATE.md` 追加都没做）· 证据仓 `XM_PATH` 未写 · 未生成 Server Script · 未改项目名/包名/命名空间 · **未执行本 plan 的任何一个执行项**（未跑 CI、未改 `agenerp/**` / `docker-compose.yml` / `tools/**`）。
- 🔴 **第 7 轮就地取代整个「收敛状态」小节（上面各条原文保留、不改写）。现行状态：**
  **七轮**独立评审（互不相同）共提 **13 阻塞 / 34 主要 / 21 次要**，**每一条都已改进本文件，当前无任何一条留给收口审计**。**逐轮阻塞数：4 → 2 → 2 → 0 → 0 → 3 → 2**；**主要数：7 → 5 → 4 → 2 → 5 → 7 → 4**。
  ⚠️ **第 7 轮的 2 阻塞成因与第 6 轮同类，但更值得记**：第 6 轮的阻塞是「人裁定之后旧前提集体过期」，**第 7 轮的两条则是「人在 loop 停机的 12 小时里替 loop 做了取证」+「CI 在评审进行中又绿了一次」** ⇒ **两条都是外部事实变化，没有一条是 plan 自身质量回退**。⚠️ **但这恰恰说明一件事：本 plan 的 `Current Baseline` 与真实世界的耦合度高于常规 plan，`B1` / `B7` 两节必须在执行期首次取数时整体重验，不许照抄。**
  ✅ **转 `active` 的两个合取条件**（① 评审 consensus ② 人已答复 `§0`）**在第 6 轮即已满足，第 7 轮独立复核仍成立** ⇒ 本轮按指南「When Executing」第 2 条把 `Plan Status` 由 `draft` 改为 **`active`**，**实现可以开工**。
  ⚠️ **开工第一件事不是花额度**：按 `P1-0` 先处理额度前置，而 `P1-2` 的 **ps 重启指纹**一格（B7，零模型调用）是本 plan 唯一能在探路闸之外先跑的取证，**应当排在最前**。

## Closure Gates

- [ ] in-scope behavior is complete
- [ ] relevant docs are aligned（`module-boundaries.md` §7.21 / `system-baseline.md`）
- [ ] verification has run：P3-4 的**五条无条件** + **两条有条件**（含 P3-5 冷起，未触发时写明）+ **P3-6 三次 run id**
- [ ] scoped verification is not conflated with full verification —— 未跑整仓 `pytest tests -q -m "not live"` 时必须逐字写「verification scope limited」
- [ ] no in-scope item downgraded to deferred/follow-up
- [ ] independent draft review completed and recorded
- [ ] text consistency verified: status, phases, gates, and log all agree
- [ ] closure audit was independent（⚠️ **执行者自己复跑不算** —— 工作项 10/11 已两次踩到这格，其中一次留白至次日才补做）
- [ ] closure evidence exists in files
- [ ] **红线自证四条实跑。**🔴 **口径由第 3 轮独立评审改正**：初稿用的是 `git status --porcelain` / 裸 `git diff`，**它们只看工作区** —— **一旦提交就必然是空输出，无论那些提交动过什么**，等于一条永远为真的自证。**改成钉住开工基线 sha 比对**（开工时把 `BASE=$(git rev-parse HEAD)` 记进 `§8` 第 0 行）：
      - `git diff --name-only $BASE..HEAD -- tests/gates/ .github/workflows/` → **无输出**（红线 1/2；若走了 `P2-1 (B)` 的诊断步则此处非空，按那一条的要求逐字说明）
      - `git diff --name-only $BASE..HEAD -- docs/masterplan/DECISIONS.md` → **无输出**（红线 3）
      - `git diff $BASE..HEAD -- docs/masterplan/STATE.md | grep -c '^-[^-]'` → **`0`**（红线 5，只追加）
      - 证据仓 `XM_PATH` 未写（红线 6）· 未生成 Server Script（红线 7）· 未改项目名/包名/命名空间（红线 4）
      🔴 **第 7 轮独立评审补一条口径 —— 不是放宽，是补上它漏判的那一半**：`$BASE..HEAD` 这个区间**装的是所有人的提交，不只是 loop 的**。⚠️ **本轮实测当场撞上**：评审进行中，人先后推了 `fecbd59`（改 `STATE.md`）与 `d69b335`（**改 `DECISIONS.md`** + `pyproject.toml` + `docs/references/`，D-25 批准浏览器驱动，属工作项 11，与本 plan 无关）⇒ **照旧文跑，红线 3 那条自证会输出非空并把 loop 判成越线，而 loop 一个字节都没碰过它。**
      ⇒ **改后的判法写死（两步，缺一不可）**：
        ① 先照上面四条原样跑。**空输出 = 清白，到此为止。**
        ② **非空时不许直接判越线，也不许直接判清白** —— 必须逐 commit 归属：`git log $BASE..HEAD --format='%h %an <%ae>' -- <该条路径>`，**把每一个 sha 连同 author 抄进收口记录**。**只有 author 为人（`lize <lize-agent-engineering@users.noreply.github.com>`）的 commit 才可排除**；**只要有一个 commit 的 author 是 loop（`Email Agent Developer <developer@mailagent.com>`），就是越线，按红线处置、不许自辩。**
      ⚠️ **这条口径同样适用于三个 Phase 的 `Exit Criteria` 里那几条 `$BASE..HEAD` 自证**（Phase 1 的「仓内代码零改动」· Phase 2 的「`D-b-1`…`D-b-8` 零改动」· Phase 3 的「`STATE.md` 只追加」），**逐条按同一两步法读，不再各自复述。**
      ⚠️ **红线 5 那条 `grep -c '^-[^-]'` 尤其要按②读**：人的提交若删改过 `STATE.md` 的行，计数会非零 —— **那不是 loop 越线，但也不许拿它当 loop 的挡箭牌**，必须逐 commit 归属后才能下结论。
- [ ] ~~**未代人写 roadmap 10b 的状态词**（状态源是 `人`，§0）~~ 🔴 **第 6 轮改正 —— 依据已作废（`f144475` 把状态源改成了 `` `MD:p1-explain` ``）。本 gate 改为：**
      **`P1.8a-fix` 的状态词已按 P3-8 的三条硬约束写下**（`02-WBS.md:88` 仅「状态」一格 1 增 1 删 · 状态词与三个 run id + 修复 sha 同一次提交 · P3-6 五条绑定全部满足在先），**或**在收口记录里逐字写明「五条绑定未全部满足，状态词未写」并说明缺哪一条。

## Deferred But Adjudicated

### 断言消息把「连不上」与「回包超时」印成同一句

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: 该消息在 `tests/unit/test_explain_service_body.py:133`，属门禁判据正文 ⇒ **红线 1，loop 无权改**（B5）。它**误导了 WBS 行对本缺陷的命名**（B2），代价真实，但修它要人批。
- Successor Required: `yes` —— **重开事件**：人对停机分支 A 那个问题作出裁定时（无论答案是什么），**顺带处置这一句文案**。

### `02-WBS.md` 的状态源 `MD:p1-explain` 在 `missions/` 下没有对应文件

- Classification: `watch-only residual`
- Why Not Blocking Closure: 第 6 轮独立评审实测 —— `missions/` 只有 `base.json` / `demo.json` / `onboarding.json` / `p0-foundation.json` / `p1-insight.json`，**没有 `p1-explain.json`**，而 `02-WBS.md` 的 P1.1–P1.8b **九行加本行共十行**状态源全写 `` `MD:p1-explain` ``（本 plan 头部则是 `> Mission: p1-insight`，roadmap 是 `docs/backlog/p1-insight-roadmap.md`，证据目录既有 `docs/evidence/p1-explain/` 也有 `docs/evidence/p1-cost/`）。⇒ **这是一处早于本行的命名残留，不是 `f144475` 引入的**，且工作项 10（P1.8a，同样是 `MD:p1-explain`）正是本 mission 交付的 ⇒ **归属在事实层面无歧义**；人另在 `STATE.md` §3 `[open] 12:55Z` 逐字写「**loop 应先做 `10b`**」。**`02-WBS.md` 是人的文件（红线 5），loop 不改。**
- Successor Required: `no` —— 本 plan 在此逐字登记即可。**重开事件**：若将来某一行的状态源歧义**实际**导致派工冲突，由人在 `02-WBS.md` 统一命名，那是人动作。

### 人写的「已知线索」前半句已不成立

- Classification: `watch-only residual`
- Why Not Blocking Closure: `frontend.depends_on.agenerp-serve` 已在工作项 10 的修复里删掉（B4 实读），线索是在旧状态下写的。**`02-WBS.md` 是人的文件（红线 5），loop 不改。**
- Successor Required: `no` —— 本 plan 在此逐字登记即可；人若要改准那一行，改的是 `02-WBS.md`，是人动作。

## Closure

Status Note: **未收口。**本 plan 于 2026-08-26 由 `active` 转 `deferred` —— Phase 1/2 完成，Phase 2 以**停机分支 A** 收尾（唯一可行修法落在红线 1），Phase 3 未执行。收口审计**不在本轮做**：没有修法可审，也没有 `P3-6` 的三次绿可核。**重开事件：人裁定停机分支 A 那个问题之后。**详见 `## Execution Record（2026-08-26）`。

Closure Audit Evidence:

- Auditor / Agent: <独立子代理，非本 plan 执行者>
- Evidence: <task id / 复跑原文 / 退出码>

Follow-up:

- <未开始，不得预填。⚠️ **收口时只许填非阻塞项** —— 指南 Minimum Rule 14 逐字：已确认的活缺陷 / 契约漂移 / owner-doc 漂移 / 仓内已修的 CI-lint 规则**不得**降级进本节；本 plan 的两条已裁定残余在 `Deferred But Adjudicated`，不重复登记在这里。>
- <🔴 **第 5 轮评审补上本子节**（第 4 轮记为 [次要3]）：模板 `Closure` 节含 `Follow-up:`，本 plan 此前缺失。>
