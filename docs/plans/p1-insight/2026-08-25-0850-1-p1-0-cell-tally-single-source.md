# P1.0 逐格计数的单一真相源：把手抄换成可复算的派生，把三处口径分歧登记而不替人选

> Plan Status: active
> Mission: p1-insight
> Work Item: 2. 入口关口实验：门禁能否补偿模型能力（P1.0 🚪）—— **本 plan 是它的第 2 个 plan**
> （表规 3「一个工作项 = 1–2 个 plan」的预算，本 plan 用掉第 2 个；起草期实读该格为 `1/2`）
> Last Reviewed: 2026-08-25
> Source: 起草期实读发现的一处**已确认 owner-doc 漂移** ——
> `docs/architecture/model-management.md` §12.3 逐字自述「本表取 `docs/masterplan/STATE.md` §2 的
> `2026-08-24T07:31Z` 行」，而**同一个人**在同日 `09:01Z` 已就该行发布更正
> （STATE.md:309-317，逐字「`2/12` 这个数是错的，正确是 `5/12`」），**doc 没有跟上那次更正**。
> 依据 `docs/plans/00-plan-authoring-and-execution-guide.md` **Minimum Rule 14**：
> 「confirmed owner-doc drift」是**不可降级项**，不许写成 follow-up。
> Related: [`2026-08-24-P1.0-entry-gate-experiment.md`](./2026-08-24-P1.0-entry-gate-experiment.md)（P1.0 本体，`completed`；本 plan 改的是它那次实验**计数的引用面**，不改实验本身）·
> [`2026-08-25-0225-1-answer-judge-v0.md`](./2026-08-25-0225-1-answer-judge-v0.md)（P1.4 第 2 个 plan，`completed`；它把 `tests/fixtures/p1_entry_gate_labels.jsonl` 确立为本仓唯一的人工标注集，本 plan 的派生面就建在它上面。⚠️ 它 §1.7 逐字写着「不再引用任何一个具体的 `1/6` 数字……**哪个对是人的事**」—— 本 plan **继承这条定界，不推翻它**，见 §7 D2）·
> [`2026-08-24-1457-1-model-routing-v0.md`](./2026-08-24-1457-1-model-routing-v0.md)（P1.1，`completed`；漂移面 `model-management.md` §12.3/§12.5 是它写的）
> Audit: required
> Execution Order: **1 / 1**（本批只有这一个 plan）

## 0. 执行前必做：重取基线

**起草期读到的一切都可能在开工时已经变了。** 下面八处**逐条重读**，把实读值填进 §0.1，
与起草期不一致的**照实记、不改起草期原文**。

1. `git log -1 --format=%H` 与 `git status --porcelain`（判据收窄成：**除本 plan 文件外无输出**）
2. `docs/architecture/module-boundaries.md` 的 `7.x` 族**当时的最大节号**
   （起草期实读 **§7.17**，本 plan 预定落 **§7.18**；被占用就顺延，以开工时实读为准）
3. §1.6 那两条 grep 命令**逐字复跑**，命中集合与起草期表比对（§6 H2 判这一条）
4. `docs/masterplan/STATE.md` §2 的 `07:31Z` / `08:56Z` / `09:01Z` 三张表**逐格实读**
   （STATE 是追加式账本，理论上不会变；变了说明红线 5 被破，**当场停机**）
5. `tests/fixtures/p1_entry_gate_labels.jsonl` 的**行数与 sha256**
   （起草期：24 行；该文件是人工标注，**本 plan 一个字节不改**，见 §3 Non-Goal 1）
6. `tests/routing/test_capabilities.py` 的 `_table_after()` 取表口径
   （起草期实读：按 `<!-- machine-read: NAME -->` 标记名取其后的表；§6 H5 判新增标记会不会伤到它）
7. **§1.5 / §1.6 / §7 D2 / §9 里引用的每一个行号逐条复读**（起草期第一版有四处行号偏移，
   由独立草案评审逐条查出并改准，见 §10 第 1 轮）—— 行号会随任何一次文档编辑漂移，
   **以开工时实读为准，与起草期不一致的照实记**
8. 八条基线命令的开工基线数字（起草期实读见 §1.1）

### 0.1 执行期重取基线的**实读结果**

*(执行期填写)*

## 1. Current Baseline（起草期逐条实读，非记忆、非转述）

### 1.1 八条基线命令（起草期基线 `7eb31bf`，`git status --porcelain` 无输出）

| # | 命令 | 退出码 | 输出 |
|---|---|---|---|
| ① | `python3 tools/gates/check_expected_red.py` | **0** | `判定模式：default` · `门禁 11 项：预期红 0，绿 11，跳过 0` · `✅ 与预期红名单完全一致` |
| ② | `python3 -m pytest tests/unit -q` | **0** | `626 passed` |
| ③ | `python3 -m pytest tests/contracts -q` | **0** | `151 passed` |
| ④ | `python3 -m pytest tests/tools -q` | **0** | `81 passed, 12 skipped` |
| ⑤ | `python3 -m pytest tests/routing -q` | **0** | `167 passed, 1 skipped` |
| ⑥ | `python3 -m pytest tests/context -q` | **0** | `54 passed` |
| ⑦ | `ruff check agenerp tests/unit tests/contracts tests/tools tests/routing tests/context tests/experiments` | **0** | `All checks passed!` |
| ⑧ | `bash tools/check-masterplan-links.sh` | **0** | `共校验 35 条引用，断链 0 条` |

（`tests/experiments -q` 另实读 `10 passed`，本 plan 不改它，不列进收口必跑。）

### 1.2 本仓关于「P1.0 第一轮逐格计数」一共有**四个**口径（起草期逐条实读，不合并成一个数）

| # | 口径 | 出处（实读位置） | 弱 off | 弱 on | 强 off | 强 on | 第一轮合计 |
|---|---|---|---|---|---|---|---|
| **甲** | 循环自己的判定表（非盲判，自陈） | `docs/evidence/p1-entry-gate/verdicts.md` 逐条打勾 + `docs/audits/p1-insight/2026-08-24-P1.0-entry-gate.md:41/57/67` | 0/3 | **2/3** | **3/3** | **3/3** | **8/12** |
| **乙** | 人侧独立复核，第一版 | `STATE.md:281-292`（`2026-08-24T07:31Z`） | 0/3 | 1/3 | 1/3 | 0/3 | **2/12** |
| **丙** | 人侧独立复核，**更正版**（同一人，同日 90 分钟后） | `STATE.md:309-317`（`2026-08-24T09:01Z`），逐字「`2/12` 这个数是错的，正确是 `5/12`」 | 0/3 | 1/3 | **3/3** | 1/3 | **5/12** |
| **丁** | **人工标注集**（人读原文定标签） | `tests/fixtures/p1_entry_gate_labels.jsonl` 的 24 行；起草期**机械派生**得出 | 0/3 | **2/3** | **3/3** | **3/3** | **8/12** |

**起草期已实算的四件事，逐条给命令，不给形容词**：

1. **丁的派生**：按 `(model, gate)` 分组数 `label == "correct"`，得
   `qwen-plus off 0/3` · `qwen-plus on 2/3` · `qwen3.6-plus off 3/3` · `qwen3.6-plus on 3/3`；
   第二轮四格 `qwen3.7-plus-2026-05-26 off 3/3 / on 3/3` · `qwen3.8-max off 2/3 / on 3/3`。
2. **标注集的格归属不是自报的**：24 行的 `(model, gate)` 与
   `docs/evidence/p1-entry-gate*/{run_id}.json` 里同名键**逐条比对，0 处不符**；
   24 行的 `answer` 与轨迹的 `final_answer` **逐字节相同，0 处不符**。
   ⇒ 标注集**确实标的就是那 24 次运行**，不是另一批文本。
3. **甲与丁在第一轮逐条一致**：8 条 `correct`（`run-01/03/04/06/08/11/12/14`）与
   `verdicts.md` 判 `正确` 的 8 条**同一个集合**；其余 4 条两侧都不是「正确」
   （`run-05` 甲判 `错误` / 丁标 `incomplete`，`run-07` 甲判 `不完全` / 丁标 `truncated`，
   档位措辞不同、**正确/非正确的划分一致**）。
4. **丙与丁的差恰好是 3 条**，且方向与 `tests/unit/test_answer_judging_fixture.py:12` 那一行
   逐字吻合（「『没有任何销售订单来消化』『无单可发』『额外的外协收货』都不在词表里 |
   **三份正确答案判成「不完全」**」）：`弱 on` 差 1、`强 on` 差 2，合计 3。
   ⚠️ **这是算术对得上，不是「所以丁赢了」** —— 处置见 §7 D2。

### 1.3 缺口是什么（一句话）

**本仓从来没有一处可复算的逐格计数，四个口径全是手抄；手抄已经错了四次，
而 owner doc 今天引的是那条**已被同一人公开更正过**的 `07:31Z`。**

### 1.4 授权来自哪里，以及为什么这不是「顺手优化」

- **`model-management.md` §12.3 的漂移是「已确认」的，不需要任何判断**：
  该节 `:117-118` 逐字自述取 `STATE.md` §2 `07:31Z` 行，而 `STATE.md:309` 是**同一人**
  对该行的公开更正。**doc 的声明源自己发过更正，doc 没跟上** ⇒ guide **Minimum Rule 14**
  的 `confirmed owner-doc drift`，**不可降级为 follow-up**。
- **不是重开任何人的裁定**：本 plan 不主张任何一个口径为准，不改 `STATE.md` 已有行、
  不改 `DECISIONS.md`、不改 `docs/audits/`、不改标注集、不改预注册假设（§3 Non-Goals 全列）。
- **不是「顺手优化」**：mission roadmap 的硬约束 **④（D-16）**逐字要求
  「引用任何数字前先问：这个数字是在本项目、本数据集、本任务上跑出来的吗」，
  而本仓今天引用这些数字的方式是**手抄**，硬约束 ④ 在实现层没有任何机械保障。

### 1.5 已知会被这次改动碰到的地方（inventory，Minimum Rule 1 要求，不可省）

| 位置 | 今天写着什么 | 本 plan 怎么处置 |
|---|---|---|
| `docs/architecture/model-management.md:98-128`（§12.3 改准整段） | 四格表在 `:101-106`（`0/3 / 1/3 / 1/3 / 0/3`）、合计 **2/12** 在 `:109`、声明源 `07:31Z` 在 `:118` | 整段划成**单一真相源区域**（`machine-read` 起止标记），表换成**四列并置表**；结论句按 §7 D3 处置 |
| `docs/architecture/model-management.md:207` / `:208` / `:210`（§12.5 模型档案表的**出处列**） | 手抄的逐格数：`:207` `qwen3.8-max off 2/3、on 3/3` · `:208` `qwen3.7 off 3/3、on 3/3` · **`:210` `qwen-plus off 0/3、on 1/3`** —— `:210` 的 `on` 格在丁列是 **2/3**，**今天就与丁列冲突** | 三处出处列的逐格数换指针。⚠️ **能力列不动**：起草期实读 `tests/routing/test_capabilities.py:79-88` 只断言 `row[0]`（模型名）/ `row[1]`（能力）/ `row[2]`（是否计 reasoning），**出处列 `row[3]` 没有任何判据钉着** ⇒ 改它既不碰能力声明，也不可能打红 `tests/routing` |
| `docs/architecture/model-management.md:226-240`（§12.5 `multi_hop` 限定段） | `无门禁 1/3、有门禁 0/3 —— 合计 1/6`；「`2/6` vs `1/6` 待人裁定」；「答对不到一半」 | 数字换指针；那段「待人裁定」实读**已被 `STATE.md:464` 结清**（`[resolved] 10:12Z`，更正为 `4/6`），照实改准 |
| `docs/architecture/module-boundaries.md:558-560`（§7.8 残余风险 4） | `它在本项目两跳题上是 1/6` | 数字换指针，`[open] 07:50Z` 的引用**原样保留** |
| `agenerp/explain/loop.py:643-646`（`explain()` docstring） | `是 1/6（STATE §3 [open] 2026-08-24T07:50Z）` | **仅注释**，数字换指针；签名、行为、判据一律不动 |
| `tests/routing/test_capabilities.py` | 按 `machine-read` 标记名取表，判 §12.5 三张表与 `capabilities.py` 逐行相等 | **不改**；§6 H5 判新增一个标记会不会伤到它 |
| `tests/unit/test_answer_judging_fixture.py:9-12` | 四次判错的历史叙述（含 `5/12 误判成 2/12`） | **不改数字**（那是历史叙述，本身正确）；只加一条指向单一真相源的指针 |

### 1.6 手抄面的**穷举**（起草期实读，用命令定义，不用行号定义）

- **grep-1（漂移候选面）**：
  `grep -rn --exclude-dir=.git --exclude-dir=__pycache__ -E "[0-9]+/(6|12)" agenerp docs/architecture`
  起草期命中 **8 处**，其中**属 P1.0 逐格计数的 6 处**：`agenerp/explain/loop.py:645` ·
  `model-management.md:109` / `:228` / `:230` / `:232` · `module-boundaries.md:558`；
  **另 2 处与 P1.0 无关**：`module-boundaries.md:195`（`83/61 → 34/12`，权限过滤条数）·
  `module-boundaries.md:2540`（`6/6 一致`，判定器稳定性抽样）。
  ⚠️ **那 2 处正是「不许手抄」判据的误报面** —— 一条只按 `[0-9]+/(6|12)` 收网的守卫会把
  它们一起打红。误报面的处置写死在 §6 **H4**，**跑之前写死，不许执行期现编**。
- **grep-2（四格数面）**：
  `grep -rn --exclude-dir=.git --exclude-dir=__pycache__ -E "[0-3]/3" agenerp docs/architecture`
  起草期命中 **16 行**，逐行分类如下（**穷举，不省略**）：

  | 行 | 内容摘要 | 分类 |
  |---|---|---|
  | `model-management.md:103` / `:104` / `:105` / `:106` | §12.3 四格表的四行本体 | **P1.0 逐格数** |
  | `model-management.md:108` / `:109` | 结论句里内嵌的 `0/3 提到 1/3`、`1/3 → 0/3`、`2/12` | **P1.0 逐格数** |
  | `model-management.md:113` | `「0/3 vs 1/3」不得当作量化结论` | **P1.0 逐格数** |
  | `model-management.md:121` | `（该文件记弱模型门禁 on 为 2/3、强模型两格均为 3/3）` = 甲口径 | **P1.0 逐格数** |
  | `model-management.md:207` / `:208` / `:210` | 模型档案表**出处列**的手抄逐格数 | **P1.0 逐格数** |
  | `model-management.md:228` | §12.5 限定段的 `无门禁 1/3、有门禁 0/3`（**grep-1 也命中它**） | **P1.0 逐格数** |
  | `agenerp/oob.py:195` | `2026-08-22 冷起实测 3/3、CI runner 2/2` | 无关 |
  | `module-boundaries.md:1250` | `P1.6 的 0/3/4/5 四种可区分退出码` | 无关 |
  | `module-boundaries.md:2460` | `第 2/3 条仍完全有效` | 无关 |
  | `module-boundaries.md:2508` | `(a) 2/3 留出（否决，如上）` | 无关 |

  ⇒ **12 行属 P1.0 逐格数、4 行无关**（`:103-106` 四行 + `:108/:109/:113/:121` 四行
  + `:207/:208/:210` 三行 + `:228` 一行 = 12；12 + 4 = 16，与实跑行数对得上）。
  ⚠️ **本段被改过两次，两次都是数错，照实保留过程**：第一版写成「含三处 doc 命中 + 两行」
  （共 5 行），由独立草案评审逐字复跑该命令查出实为 16 行；第二版补齐 16 行时
  **又把 `:228` 漏出分类表**，导致「11 + 4 = 15」与实跑的 16 对不上 ——
  **这一处是起草侧自己重数时发现的**（§10 第 1 轮修订后自查）。
  两次都不假装从来没写错过：**本 plan 修的就是手抄计数，起草期自己手抄了两次都错，
  这恰恰是「派生优于手抄」的实证，不是反证。**
- **两条 grep 合起来的 P1.0 逐格数面 = 16 行**（grep-1 的 6 行 ∪ grep-2 的 11 行，
  `model-management.md:109` 是两边都命中的那一行）：

  | 分组 | 行 | 条数 |
  |---|---|---|
  | 只被 grep-1 命中（写的是 `x/6`） | `agenerp/explain/loop.py:645` · `model-management.md:230` / `:232` · `module-boundaries.md:558` | 4 |
  | **两边都命中** | `model-management.md:109`（`0/3` 与 `2/12` 同行）· `model-management.md:228`（`1/3` 与 `1/6` 同行） | 2 |
  | 只被 grep-2 命中 | `model-management.md:103` / `:104` / `:105` / `:106` / `:108` / `:113` / `:121` / `:207` / `:208` / `:210` | 10 |
  | **合计** | | **16** |

  **这 16 行就是 Phase 2/3 的全部处置对象**，**8 + 8 分开**：
  `model-management.md:103` / `:104` / `:105` / `:106` / `:108` / `:109` / `:113` / `:121`
  **八行落在 §12.3 改准段（`:98-128`）内** → Phase 2 整段重写并划进 `machine-read` 区域；
  `model-management.md:207` / `:208` / `:210` / `:228` / `:230` / `:232` ·
  `module-boundaries.md:558` · `agenerp/explain/loop.py:645`
  **八行在段外** → Phase 3 逐处换指针。
- **无关面 = 6 行**（grep-1 的 `module-boundaries.md:195` / `:2540` + grep-2 的
  `agenerp/oob.py:195` · `module-boundaries.md:1250` / `:2460` / `:2508`）。
  **这 6 行就是 §6 H4 的「守卫必须保持沉默」的预先写死的对照集**，一行不多一行不少。
- ⚠️ **两条 grep 的作用域刻意只有 `agenerp` 与 `docs/architecture`** —— 那是本 plan
  有权改的两个面。`docs/masterplan/**`（红线 5）· `docs/audits/**` · `docs/evidence/**` ·
  `docs/backlog/**` · `docs/plans/**` 全部在作用域外，**不是漏扫，是定界**（§3 Non-Goals）。

## 2. 一个结果面（Minimum Rule 4）

**P1.0 的逐格计数在本仓只有一处可复算的派生，其余全是指向它的指针；
四个口径并置落盘、各自被判据钉在自己的源上，孰为准由人裁定。**

一句话判据：**把任何一格的数改错，`tests/unit` 必须红；把任何一处指针改回手抄的数字，
`tests/unit` 必须红。** 两者都跑得到 `GATE_VERIFY` 与 CI（判据落 `tests/unit`）。
⚠️ **「任何一格」逐字包含区域内的散文数字**，不只是表里的四列 —— 承担这一半的是
**判据⑨**（§7 D6），实测它的是 **M9（⑨a 那半）与 M9b（⑨b 那半）**。**已知的唯一例外写在 §6.2 第 7 条**：
前 4 行内不含任何标识的裸计数，**守卫看不见**，M7b 把这个边界实测出来。

## 3. Goals / Non-Goals

### Goals

1. **派生面**：一个只读派生器，从 `tests/fixtures/p1_entry_gate_labels.jsonl` 机械算出
   八格计数（两轮 × 两模型 × 门禁 on/off），**并把「标注集标的就是那 24 次运行」
   这件事变成断言**（`(model, gate)` 与 `answer` 两侧都与轨迹逐条比对，不是自报）。
2. **并置面**：`model-management.md` §12.3 落一张 `machine-read` 表，把甲/乙/丙/丁**四列并置**，
   丁列由派生器产出、乙丙两列由 `STATE.md` §2 两张表**机械转录并断言相等**、
   甲列由 `docs/audits/` 与 `docs/evidence/verdicts.md` 转录并断言相等。
3. **指针面**：§1.6 点名的**段外八处**手抄（`model-management.md` `:207` / `:208` / `:210` /
   `:228` / `:230` / `:232` 六处 · `module-boundaries.md:558` 一处 ·
   `agenerp/explain/loop.py:645` 一处**仅注释**）改成指向 §12.3 那张表的指针。
4. **守卫面**：一条判据，使得**在 `agenerp/**` 与 `docs/architecture/**` 里新手抄一个
   P1.0 逐格数就红**；误报面按 §6 H4 的写死处置。
5. **登记面**：`STATE.md` §3 **追加一行**，把「丙（5/12）与丁（8/12）差 3 条、
   甲与丁逐条一致」这个起草期发现登记为 needs-human，**不替人裁定**。

### Non-Goals（逐条是显式定界，不是遗漏）

1. **不改 `tests/fixtures/p1_entry_gate_labels.jsonl` 一个字节。** 标签只能由人读原文定
   （roadmap 逐字），本 plan 只**读**它、只**数**它，不新增标签、不改标签、不回写判定结果。
2. **不裁定哪个口径为准。** 甲乙丙丁四列并置，`machine-read` 表里**不设「正确值」列**。
   继承 `2026-08-25-0225-1` §1.7 的定界逐字：「哪个对是人的事」。
3. **不改 `docs/masterplan/` 任何已有行**（红线 5）；`STATE.md` **只追加**一行。
   **不改 `DECISIONS.md`**（红线 3），D-11 复验注记里的 `2/6` 原样不动。
4. **不改 `docs/audits/p1-insight/2026-08-24-P1.0-entry-gate.md`。** 审计是收口判定制品，
   其措辞该不该动由人裁定（这条定界是 mission-driver 第十六次盘点已立的，本 plan 继承）。
5. **不改 `docs/evidence/**`。** `verdicts.md` 与 `run-NN.json` 是落盘证据，
   `p1-entry-gate-round2/HYPOTHESES.md` 是**跑之前冻结的预注册假设**（硬约束 ②），
   两者都改不得 —— 即使里面的数字属于乙口径。
6. **不改 `docs/backlog/p1-insight-roadmap.md`。** 它的 Work Item Status 块由引擎回写，
   其余段落的措辞归属不在本步职责内（第十三次盘点已立的定界）。
7. **不改任何模型能力声明。** `capabilities.py` 的 `qwen3.6-plus` `multi_hop` 取值
   **一个字不动**；§12.5 三张 `machine-read` 表的**前三列**（模型名 / 能力 / 是否计 reasoning）
   一格不动 —— 那三列正是 `tests/routing/test_capabilities.py:79-88` 断言的部分。
   ⚠️ **第四列（出处）改，且必须改**（见 §1.5）：它今天手抄着逐格数，其中 `:210` 已与丁列冲突。
   **把「表体一行不动」写成定界是不准确的** —— 起草期实读确认出处列无判据钉着，
   拿一条不存在的约束当挡箭牌，正好是本 plan 要修的那类毛病。
   本 plan 改的是**计数的引用**，不是**能力的声明**；能力声明归工作项 3（P1.1），不在本格。
8. **不重跑 P1.0 实验、不调任何 LLM、不碰活站点。** 全程离线，零 token 消耗。
9. **不碰 `tools/experiments/p1_entry_gate/`**（实验设施，P1.4 已立「一行未动」的先例）。
10. **不改 `tests/gates/**`（红线 1）· `.github/workflows/**`（红线 2）· `missions/**`。**

## 4. Task Route

- Type: `verification or audit work`（主）+ `stale-doc conflict`（guide 决策表把它归 Full plan）
- Owner Docs: `docs/architecture/model-management.md`（§12.3 / §12.5）·
  `docs/architecture/module-boundaries.md`（§7.8 残余风险 4，新增落点节 §7.18）
- Skill Selection Basis: **`none`**。`docs/skills/README.md` 里没有覆盖
  「owner-doc 计数漂移修复 + 派生判据」的可复用技能；本 plan 的方法由 §6/§7 写死，
  不由技能选择。逐 Phase 仍逐条记 `Skill: none`。

## 5. Infrastructure And Config Prereqs

**No infra prereqs beyond existing baseline.** 全程离线：不需要活站点、不需要
`DASHSCOPE_API_KEY`、不需要 docker 栈、不需要任何 `AGENERP_*` 环境变量。
所有输入都是仓内已入库的文件（标注集 24 行 + 24 份轨迹 JSON + `STATE.md` + 审计与证据 md）。
**无回滚脚本需求**：本 plan 不产生任何不可逆动作，回滚 = `git revert`。

## 6. 假设：**跑之前逐字写死**（硬约束 ②）

⚠️ **先照实交代举证责任的边界**：§1.2 的甲乙丙丁四列与 §1.6 的 grep 命中集合
**都是起草期已经实算过的**，因此 **H1 / H2 不是预测，是守护性回归** —— 逐字标注在下表，
**不冒充成「跑之前不知道」**。
⚠️ **起草期第一版把「真未知」写成五条（H3–H7），独立草案评审逐条查后判定是虚高，已改准**：
**H5** 的解析口径起草期自己就读过（§0 第 6 条）、**H6** 由执行者自己决定条数因而不可证伪、
**H7** 本 plan 自己都写着「理应不变」。**真未知只有 H3 / H4 两条**，其余五条按其真实性质标注。
把「不知道的有几条」说多，与把数字手抄错，是同一类毛病。

| # | 假设（跑之前写死） | 怎么判 | 性质 |
|---|---|---|---|
| **H1** | 派生器算出的八格：`qwen-plus off 0/3` · `qwen-plus on 2/3` · `qwen3.6-plus off 3/3` · `qwen3.6-plus on 3/3` · `qwen3.7-plus-2026-05-26 off 3/3` · `on 3/3` · `qwen3.8-max off 2/3` · `on 3/3` | `pytest tests/unit -q` | **守护性回归**（起草期已实算） |
| **H2** | §1.6 grep-1 命中 **8 处**（6 处属 P1.0 + `module-boundaries.md:195` / `:2540` 两处无关） | 执行期逐字复跑两条 grep | **守护性回归**（起草期已实读） |
| **H3** | 乙（`STATE.md:283-288`）· 丙（`:311-317`）· 甲（`docs/evidence/p1-entry-gate/verdicts.md` 逐条打勾行）三处的数字**可以被机械解析出来**，解析结果与 §1.2 表逐格相等 | 判据里真解析、真断言 | **真未知** —— STATE 的表是**缩进两格**的 md 表，`verdicts.md` 的结论列带 `**` 粗体与全角字，解析可能当场失败 |
| **H4** | **Phase 2/3 改完之后**，守卫在改后的文本上仍是**误报 0 / 漏报 0**：6 行无关面全部沉默，且新写的任何一处手抄（M7 变异）当场打红 | 先跑宽网阳性对照证明误报**真的发生**，再跑 §6.1 三条谓词版逐条比对两个集合 | **真未知** —— §6.1.1 的 0/0 是在**改之前**的文本上标定的，Phase 2/3 会改动行位置、内容与「标识↔数字」的距离，**改后是否仍 0/0 今天无证据** |
| **H5** | 在 §12.3 新增一对 `machine-read` 起止标记之后，`pytest tests/routing -q` **逐字仍是 `167 passed, 1 skipped`** | 基线命令 ⑤ | **守护性回归** —— 起草期实读 `tests/routing/test_capabilities.py:42-62` 的 `_table_after()` 做的是 `text.split(anchor, 1)[1]`，**一个不同名的新标记按构造不可能干扰它**；仍实测，但不冒充成未知 |
| **H6** | `pytest tests/unit -q` 由 `626` 增至 `626 + N`，**且 `N ≥ 12`** | 基线命令 ② | **目标，不是假设** —— N 由执行者写几条判据决定，**不可证伪**。照实标注：它是本 plan 给自己定的下限（8 格 + 4 列 ≥ 12 条），达不到要说明为什么 |
| **H7** | `check_expected_red.py` 输出**逐字节不变**（`门禁 11 项：预期红 0，绿 11，跳过 0`） | 基线命令 ① | **守护性回归**（本 plan 不碰 `tests/gates`；仍实测） |

### 6.1 守卫的口径：**三条谓词，跑之前写死**（硬约束 ②）

起草期第一版只写了「枚举一组数字串」，**没有把那组串枚举出来**，
而任何含 `2/3` / `3/3` 的集合都会撞上 §1.6 那 6 行无关面 —— 独立草案评审实测点出这一点。
收窄口径因此改成**三条谓词的合取**，逐条写死：

1. **数字面**：行内匹配 `[0-3]/3` 或 `[0-9]+/(6|12)`（= §1.6 两条 grep 的并集）。
2. **语境面**：**该行或其前 4 行**之内出现下列标识之一 ——
   `P1.0` · `入口关口` · `两跳题` · `门禁` · `qwen-plus` · `qwen3.6-plus` ·
   `qwen3.7-plus` · `qwen3.8-max`。
   ⚠️ **「前 4 行」这个窗口是起草期实跑标定出来的，不是拍的**，标定过程与被否决的三种
   窗口逐条记在 §6.1.1 —— **本仓关于这条谓词的全部已知失败形态都在那张表里**。
3. **区域面**：该行**不在**单一真相源区域内 —— 区域由
   `<!-- machine-read: p1-0-cell-tally -->` 与 `<!-- /machine-read: p1-0-cell-tally -->`
   一对标记界定，**整个 §12.3 改准段落落在区域内**。

**为什么第 3 条不是「只扫某几个写死的文件名」**（那是被禁止的退化形态）：
守卫仍然扫 `agenerp/**` 与 `docs/architecture/**` 的**全部**行，包括
`model-management.md` 区域之外的每一行。在同一个文件里、区域之外新写一处手抄，**照样红**。
排除的是一段**由标记显式界定、内容即真相源本身**的区域，不是一份文件白名单。

#### 6.1.1 窗口大小的标定（起草期**实跑**，四种口径逐条落账）

判据：对全部 22 行命中（16 行 P1.0 面 + 6 行无关面）算**误报**（无关面被打红）
与**漏报**（P1.0 面未被打红）两个数。

| 语境面口径 | 误报 | 漏报 | 漏报的是哪几行 | 结论 |
|---|---|---|---|---|
| 只看**本行** | **0** | **4** | `loop.py:645` · `mm:113` · `:230` · `:232` | 否决：标识常在**上一行**（如 `mm:227` 写 `qwen3.6-plus`、`:228` 才写 `1/3`） |
| **整段**（空行分隔的块） | **2** | 1 | `mm:113` | 否决：`mb:195` 整段含「门禁」、`mb:2508` 整段含三个模型名 —— 段太大，语境被稀释 |
| 本行 + **前 2 行** | **0** | 1 | `mm:232` | 否决：`:232` 与最近的标识（`:227` 的 `qwen3.6-plus`）隔了 5 行 |
| **本行 + 前 4 行** | **0** | **0** | —— | **采用** |

⚠️ **「前 4 行」不是最优解，是「本仓今天这份文本上，误报与漏报同时为 0 的最小窗口」**；
起草期另测了前 5 / 6 / 8 行，**同样是 0 / 0** ⇒ 4 是该区间的下端，**取下端更不容易误报**。
⚠️ **不得读成「这个窗口在别的文本上也 0/0」**（D-16）：它只在本仓当前 22 行上被验过。

⚠️ **H4 剩下的真未知是什么，说清楚**：上表是在**改之前**的文本上跑的。
Phase 2/3 会重写 §12.3 整段、把 8 行段外手抄换成指针 —— **行的位置、内容、
以及标识与数字的相对距离全都会变**。「改完之后仍然 0 / 0」这件事**今天没有任何证据**，
H4 判的就是它。

### 6.2 H4 不吻合时**怎么办，跑之前写死**（硬约束 ①）

若收窄后的守卫**出现任何一处误报**（§6.1.1 实测的基线是 **0**，因此第 1 处就算）：

1. **照实记「H4 不吻合」**，把误报处**逐行列出**（文件:行 + 原文）。
2. **只允许改第 2 条谓词的两个旋钮**：窗口大小、标识集合。
   改完**必须把 §6.1.1 那张四口径标定表在改后的文本上整张重跑并落账**
   （误报 / 漏报两个数都要，不许只报一个），**并在判据里写明为什么被放过的那行不是 P1.0 的数**。
3. **禁止的三种处置，逐条写死**：
   **(a)** 把 `2/3` / `3/3` 移出第 1 条谓词的数字面 —— 那是 8 格里的 5 格，守卫当场被掏空；
   **(b)** 把守卫改成只扫某几个写死的文件名 —— 那样它挡不住「新写一处手抄」，
   退化成 CP9 继承项 ① 点名的「只验调得通」；
   **(c)** 为迁就守卫去改那 6 行无关文本本身 —— 判据迁就不了实现，实现也不该迁就判据。
4. 若三条都不可行，**Phase 3 停在这里、判据⑦ 转 `Deferred But Adjudicated`**，
   Phase 1/2 的成果照常收口，**不许为了关掉一格而放松守卫**。

**漏报那一侧，同样跑之前写死**（独立草案评审第 2 轮指出：起草期第一版只写了误报的处置，
「守卫漏掉一行 P1.0」这一侧**一条路都没写**，执行者到那里同样无合法动作）：

5. 出现漏报（某行是 P1.0 逐格数却没被打红）时：**照实记「H4 不吻合」并逐行列出**，
   然后**只允许放大窗口或增补标识**，改完**整张 §6.1.1 重跑落账**。
6. **禁止**：为了让守卫看见它而去**改那行文本本身**（判据迁就不了实现，反过来也一样）。
7. ⚠️ **有一类漏报是收不掉的，跑之前就说清，不留给执行期发现**：
   **一处「前 4 行内没有任何标识」的裸计数**（例如孤零零一行 `是 1/6`）
   **对任何基于语境的守卫都不可见**。这不是本 plan 的实现缺陷，是
   **「语境面」这条谓词的固有边界**。处置：**不假装守得住** ——
   登记成 §11 的具名残余风险，并由 **M7b** 变异把这个边界**实测出来、落进账**，
   而不是让它留在「以为守得住」的状态。

## 7. Decisions（选择 / 备选 / 否决理由 / 残余风险）

### D1 · 派生器落 `tests/unit/`，不落 `agenerp/`

- **选择**：`tests/unit/entry_gate_tally.py`（派生器 + 四列常量）+ `tests/unit/test_entry_gate_tally.py`（判据）。
  先例形态：`tests/unit/answer_judge_fixture.py` / `explain_fakes.py` / `inspection_fakes.py`。
- **备选 (A) 落 `agenerp/`**：否决 —— 这是**一次历史实验的记账**，不是产品行为；
  放进产品包会让「本仓实测过的数字」变成运行时可 import 的东西，与 §12.5
  「配置种子不是厂商绑定」的分层相冲。
- **备选 (B) 落 `tools/experiments/p1_entry_gate/`**：否决 —— 实验设施已冻结
  （P1.4 先例「实验设施一行未动」），且 `tools/**` 不在 `commands.test` 与 CI 的
  pytest 作用域里，判据会复跑不到 —— 那正是前面多个 plan 反复吃到的
  `verification scope limited`。
- **残余风险**：`tests/unit/` 里放一个非 `test_` 前缀的模块，`ruff` 作用域已含它（基线命令 ⑦），
  但它**不会被 pytest 自动收集**，只有被 `test_` 文件 import 才跑得到；
  ⇒ 判据必须真 import 它并断言（H1）。

### D2 · 四列并置，**不设「正确值」列**

- **选择**：`machine-read` 表四列（甲/乙/丙/丁）+ 每列一个出处指针，**不排名、不加粗某一列**。
- **备选 (A) 取丁（标注集）为准**：否决 —— 丁的标签是人定的，但
  「把丁提升为唯一口径」是一次**裁定**，而 `2026-08-25-0225-1` §1.7 已逐字把这件事
  归给人（「哪个对是人的事」）；`STATE.md` §2 丙那一行是人写的、红线 5 只能追加。
  loop 自己给自己开这个口子，等于用一次派生推翻一条人写的更正。
- **备选 (B) 取丙（人侧最后一张显式表）为准**：否决 —— 丙与丁差的那 3 条，
  方向恰好被 `test_answer_judging_fixture.py:12` 记的**第四次判错**解释掉；
  取丙等于把一条已知有 bug 的判定方法的产物定为唯一真相。
- **残余风险，照实登记**：并置表**读起来更累**，且「到底该信哪个」在人裁定之前**没有答案**。
  这是 §12.3 自己已有的做法（`:117` 逐字「照实记，不合并成一个数」），本 plan 只是把它做彻底。

### D3 · §12.3 的**结论句**按口径分述，本 plan 不改任何结论

- **事实（起草期实算）**：「强模型那半边出现反向证据（加门禁反而变差）」这句
  在**乙**（1/3 → 0/3）与**丙**（3/3 → 1/3）下成立，在**甲/丁**（3/3 → 3/3）下**不成立**。
  ⇒ 这是一句**依赖口径**的结论，今天却被写成无条件的。
- **选择**：结论句**原文保留**，其下增一句指明它依据哪一列、在哪几列下不成立，
  并指向 §12.3 那张四列表。**「被削弱」这个结论标签一个字不改。**
- ⚠️ **它与守卫的关系，跑之前说清**：结论句里内嵌着 `0/3 提到 1/3` / `1/3 → 0/3`
  （§1.6 的 `:108` / `:109`），而它要原文保留 ⇒ **守卫必须对它沉默**。
  这正是 §6.1 第 3 条谓词把**整个 §12.3 改准段**（而不只是那张表）划进区域的理由：
  单一真相源是**一段**，不是一行表。**区域外一处手抄都不许有，区域内的数字紧挨着自己的列名**。
- ⚠️ **区域不是免检区**（独立草案评审第 2 轮的 Major：起草期第一版把「守卫豁免」
  当成了「不必被判据够到」，而 判据④⑤⑥ 只钉**表的四列**、够不到段内散文，
  于是 `:108/:109/:113/:121` 那 8 个散文数字会变成**全仓唯一既豁免又无断言**的一块）。
  **已补 判据⑨**（⑨a 位置相等承重 + ⑨b 集合归属兜底）：结论句的数**按位置逐个等于乙列**、
  `:121` 的数**等于甲列**，区域内任何新冒出的数由 ⑨b 拦下 ——
  「原文保留」因此从**豁免**变成**被引用钉死的引文**，M9 实测它。
- **备选 (A) 直接删掉那句反向证据**：否决 —— 那是替人裁定（见 D2）。
- **备选 (B) 什么都不写**：否决 —— guide Minimum Rule 14，确认的 owner-doc 漂移不可降级。
- ⚠️ **残余风险，逐字写死不许被读松**：`被削弱` 这个标签在四列下**并非同样成立** ——
  「门禁没让弱模型追上强模型」在甲/丙/丁下成立（弱 on 低于强 off），
  但在**乙**下弱 on 与强 off **相等**（均 1/3）。**本 plan 只把这件事登记，不据此动结论标签。**
- ⚠️ **一处照实说的不对称**（独立草案评审第 3 轮提出）：本 plan 落地后，
  区域内**被断言钉住的散文数只有乙列与甲列**，**丙 / 丁两列在散文里一个数都没有**
  （没有任何句子引用它们）⇒ 「这句结论在哪几列下不成立」这半的全部重量，
  **压在 D3 新增的那一句口径分述上**，没有第二处冗余。**登记，不假装它有。**

### D4 · `agenerp/explain/loop.py` **仅注释**

- **受框架约束的选择**（constrained）：该处是 `explain()` 的 docstring 残余风险段。
  改动**只允许**是注释文字；签名、行为、判据、导出面一律不动。
  先例：P1.7 的「三处代码孪生句……均**仅注释**」。
- **残余风险**：无 —— 由基线命令 ②③④⑤⑥ 逐条不变来兜（`tests/unit` 只增不减）。

### D5 · 落点节 `module-boundaries.md` §7.18

- **选择**：新增 §7.18，记四件事：① 派生器与判据的位置与职责；② 四列并置的口径与各自出处；
  ③ 守卫的收网口径与**误报面**（H4 的实测结果）；④ **本 plan 没有裁定哪个口径为准**。
- **备选：写进 §12.3 就够了**：否决 —— §12.3 是 `model-management.md` 的内容面，
  而「判据落在哪、守卫怎么收网」是结构边界，归 `module-boundaries.md`（本仓一贯分法）。
- **残余风险**：§7.18 与 §12.3 之间会有一处**叙述重叠**（两边都要提四列口径），
  两处将来可能各自漂移，而**本 plan 没有给这条重叠加任何机械判据**（守卫只判数字，不判叙述）。
  照实登记，不假装它不存在；缓解是 §7.18 只写指针式的一句、把数字全留在 §12.3 区域内。

### D6 · §2 的判据口径按 判据⑨ 补全

- **选择**：§2 那句「把任何一格的数改错，`tests/unit` 必须红」**在补 判据⑨ 之前是假的**
  （区域内散文改了不红）。**不改口号、改实现** —— 补上 判据⑨ 让那句话为真，
  而不是把口号改软成「表里的每一格」。
- **备选：把 §2 的措辞收窄成「表内每一格」**：否决 —— 那是把判据迁就实现，
  且正好放过本 plan 要修的那类漂移（散文里的手抄数）。
- **残余风险（本轮由独立草案评审第 4 轮的 Minor 3 改准，原措辞已过期）**：
  判据⑨ 判的是**位置相等（⑨a）**与**集合归属（⑨b）**。
  ⚠️ **原文写的是「本 plan 没有给『结论句是否还在』加任何判据」，那句话现在是错的** ——
  ⑨a 的**锚点缺失即红**已经把这一半收掉了，**但只对四条被绑定的锚点**。
  **改准后的残余风险，逐字**：**删掉一句本来就不带数的散文**（例如 D3 那句口径分述本身），
  判据看不见 —— 它没有锚点，也不进计数断言的两边。
  ⚠️ **起草期第一版在这里写的是「删掉四条锚点之外的任何一句带数的散文」，
  并附了一句「计数断言随之各减一、两边仍然相等」—— 两处都错，由独立草案评审第 5 轮指出**：
  ① 删掉一条**不在册的带数散文**会让散文侧减一、锚点侧不变，两边**当场不等**，不是相等；
  ② 更根本地，**那种行在一个绿着的仓里根本不存在** —— 它一被写进去，
  计数断言就已经是 `5 ≠ 4` 了。⇒ 原来登记的是一个**够不着的洞**，
  真正够得着的是「不带数的句子被删」。**照实改准，不留着一个听起来更吓人但不成立的版本。**
  缓解只有收口时的人读；**本 plan 不为此再加判据**（要挡住它得枚举「本来该有哪些句子」，
  那是另一个真相源，超出本 plan 的一个结果面）。
## 8. Execution Plan

### Phase 1 — 派生面：从标注集机械算出八格，并证明标注集标的就是那 24 次运行

Status: planned
Targets: `tests/unit/entry_gate_tally.py` · `tests/unit/test_entry_gate_tally.py`
Skill: `none`

- Item Types: `Add | Proof`（5 项：2 `Add` / 3 `Proof`；**3/5 = 60%，未达指南第 7 条的 80% 门槛**，因此逐项标注、不在 Phase 层声明统一类型）
- Prereqs: §0 的八条重取基线全部填进 §0.1

- [ ] `Add` 派生器：按 `(model, gate)` 分组数 `label == "correct"`，输出八格
      `(命中, 总数)`；分母**由分组实际行数得出**，不写死 `3`
  - Skill: `none`
- [ ] `Add` 同一性断言的输入面：24 行的 `run_id` → 轨迹文件路径的解析
      （`docs/evidence/p1-entry-gate/` 与 `p1-entry-gate-round2/` 两个目录）
  - Skill: `none`
- [ ] `Proof` 判据①：八格 == §6 H1 写死的值（**逐格独立断言，不断言一个聚合数**）
  - Skill: `none`
- [ ] `Proof` 判据②：24 行的 `(model, gate)` 与轨迹同名键**逐条相等**，
      且 24 行的 `answer` 与轨迹 `final_answer` **逐字节相等**；
      **两条都要断言「比对确实发生了 24 次」**（计数断言，挡住空循环假实现）
  - Skill: `none`
- [ ] `Proof` 判据③：标注集**行数 == 24** 且 `run_id` 互不重复；
      八格分母之和 == 24（挡住「某一格被静默漏掉」）
  - Skill: `none`

Exit Criteria:

- [ ] 八格派生可复算，`pytest tests/unit -q` exit 0 且条数只增不减
- [ ] `ruff check`（基线命令 ⑦ 原文）exit 0
- [ ] 标注集**字节数与 sha256 与 §0.1 实读相同**（本 Phase 不改它，Non-Goal 1 自证）
- [ ] `docs/logs/` 更新

### Phase 2 — 并置面：§12.3 的四列 `machine-read` 表，三列各自被钉在自己的源上

Status: planned
Targets: `docs/architecture/model-management.md`（§12.3）· `tests/unit/test_entry_gate_tally.py`
Skill: `none`

- Item Types: `Fix | Proof | Decision`（6 项：1 `Fix` / 4 `Proof` / 1 `Decision`；**4/6 ≈ 67%，未达 80% 门槛**，逐项标注。⚠️ 起草期第一版把 `Add` 也列进来，而本 Phase 一个 `Add` 项都没有，已删）
- Prereqs: Phase 1

- [ ] `Fix` 把 §12.3 改准整段（起草期实读 `:98-128`，表本体 `:101-106`）划进
      `<!-- machine-read: p1-0-cell-tally -->` … `<!-- /machine-read: p1-0-cell-tally -->`
      **一对起止标记**（§6.1 第 3 条谓词的区域面），段内的四格表换成
      四列并置表（甲/乙/丙/丁），每列一个出处指针
  - Skill: `none`
- [ ] `Proof` 判据④：丁列与 Phase 1 的派生器**逐格相等**
  - Skill: `none`
- [ ] `Proof` 判据⑤：乙列与 `docs/masterplan/STATE.md:283-288` 的表**机械解析后逐格相等**；
      丙列与 `:311-317` 的表同样（§6 **H3** 判这一条能不能做到）
  - Skill: `none`
- [ ] `Proof` 判据⑥：甲列与 `docs/evidence/p1-entry-gate/verdicts.md` 的逐条打勾行
      **机械解析后逐格相等**（该文件同时是审计 `:41/57/67` 三个数的来源）
  - Skill: `none`
- [ ] `Proof` **判据⑨（区域内也必须被钉住），两半，弱的那半不许单独用**：
      **⑨a 位置相等（承重的一半）**：对**下表每一条被绑定的句子**，
      按**逐字锚点子串**在区域内定位（**不用行号定位** —— Phase 2 重写整段，
      起草期行号在判据第一次跑起来时就已作废），把该句内嵌的数**按出现顺序**取出成元组，
      与**该条自己声明的有序列-格引用**逐位比较：

      **取数的跨度定义：锚点所在的那一整行**（一行绑定一次，不许两条锚点落在同一行 ——
      起草期第一版把 `加门禁反而 1/3 → 0/3` 与 `四格合计` 拆成两条，
      而它们**同在起草期 `:109` 一行上**，两条会争同一行的元组；已合并成一条）：

      | # | 逐字锚点子串（唯一定位；Phase 2 **必须逐字保留**） | 该行有序数元组 | 期望的列-格引用 |
      |---|---|---|---|
      | ⑨a-1 | `门禁把 0/3 提到 1/3` | `(0/3, 1/3)` | `(乙.弱off, 乙.弱on)` |
      | ⑨a-2 | `加门禁反而 1/3 → 0/3`（**同行含 `四格合计 2/12`**） | `(1/3, 0/3, 2/12)` | `(乙.强off, 乙.强on, 乙.合计)` |
      | ⑨a-3 | `「0/3 vs 1/3」不得当作量化结论` | `(0/3, 1/3)` | `(乙.弱off, 乙.弱on)` |
      | ⑨a-4 | `该文件记弱模型门禁 on 为 2/3、强模型两格均为 3/3` | `(2/3, 3/3)` | `(甲.弱on, 甲.强off)`，并另断言 `甲.强off == 甲.强on`（「两格均为」这四个字的含义） |

      ⚠️ **随之而来的一条执行约束，跑之前写死**：D3 说结论句下方要**追加**指针与口径分述句，
      而**追加的文字若与被绑定的行同行、且含匹配数字面的数，会改变该行的元组、当场打红 ⑨a**。
      ⇒ **追加的句子一律另起一行**，且**指针句里不得出现任何匹配 `[0-3]/3` / `[0-9]+/(6|12)` 的数**
      （要指数就指列名，如「乙列的强 off 格」）。**这不是绕开判据，是判据倒逼指针写法收敛到指列不指数** ——
      正是本 plan 想要的形态。
      ⚠️ **起草期实测四条锚点在 `model-management.md` 中各出现 1 次**（`四格合计` 亦为 1 次），
      定位唯一。
      ✅ **⑨a 在改动之前的文本上已被起草期完整试跑过一遍**（四条逐条）：
      锚点唯一命中 `:108` / `:109` / `:113` / `:121`，四行取出的有序元组
      `(0/3,1/3)` · `(1/3,0/3,2/12)` · `(0/3,1/3)` · `(2/3,3/3)`
      与各自声明的列-格引用**逐位相等**；计数断言的两边也对得上
      （区域内含数字的散文行 = `[108, 109, 113, 121]`，**4 条 == 4 个锚点**）。
      ⚠️ **这只证明「⑨a 在改之前跑得通」** —— Phase 2 改完之后它是否仍然跑得通，
      由 Phase 2 的 Exit Criteria 与 M9 承担，**不由这次试跑承担**。

      **Phase 2 对这四条锚点的处置，跑之前写死**：**四条全部逐字保留**，
      只在其后**另起一行追加**指针与 D3 的口径分述句，**不改写、不删除、不合并任何一条**。
      ⚠️ **「逐字保留」逐字包含「不重新折行」**（独立草案评审第 4 轮指出取数单位
      「行 / 句 / 锚点」三者不可混用后补写）：取数跨度是**整行**，
      因此把一行 markdown 重新折成两行会把元组劈开、当场打红 ⑨a。
      **这四行的字节序列必须原样**，连换行位置都不许动。
      ⚠️ **本 plan 刻意选「行」而不是「句」做取数单位**，理由写死在这里：
      区域内的句子分隔符**不止一种**（`；`（⑨a-1 句尾）· 句中 `。`（`:109` 一行内隔开两句）·
      `——` 且**跨行续写**（`:109` 续到 `:110`）），
      按「句」取数要先把这三种分隔全部枚举对，**枚举错一种就静默取错元组**；
      按「行」取数只依赖一个不会读错的边界。**代价是它对折行敏感** ——
      代价已由上一条「不许重新折行」承担，且由 M9 实测。
      ⚠️ **`⑨a-4` 那句在四列表落地后会显得冗余**（甲列已在表里）——
      **仍然保留**，理由是它记的是「审计文件与本表不同」这件事，删掉会丢掉那段来历；
      **本 plan 不因为「表里有了」就删句子**。
      **⑨a 的锚点缺失即红**：任一逐字锚点在区域内找不到 → 判据红。
      这同时把 D6 残余风险里「整句被删掉判据看不见」那一半**收掉了**（对这四条而言）；
      ⚠️ **对四条之外新写的句子仍然看不见**，那一半照实留在 D6。
      **⑨a 的覆盖完整性由计数断言保证**（与 判据② 的「比对确实发生了 24 次」同一形态）：
      **区域内含数字的「散文行」（不以 `|` 开头、即不属于任何 markdown 表格的行）
      条数 == 被绑定的锚点条数（今天是 4）**，多一行没被绑定就红 ——
      覆盖是**构造出来的**，不是靠今天数出来的四行。
      ⚠️ **表格行由 判据④⑤⑥ 按列钉住**，因此刻意排除在本计数之外，
      **不是漏掉，是分工**（重复钉一遍不会更强，只会让两处口径可能互相打架）。
      **⑨b 集合归属（兜底的一半）**：区域内**每一处**数字出现都必须落在四列取值的并集里，
      用来兜住「区域里冒出一个谁都没声明过的新数」。
      ⚠️ **⑨b 单独是不够的，跑之前就写明**：并集只有
      `{0/3, 1/3, 2/3, 3/3, 2/12, 5/12, 8/12}` 七个值，
      **把 `:103` 的 `0/3` 改成 `1/3` 仍然在集合里** —— ⑨b 对它是绿的。
      承重的是 ⑨a 与 判据④⑤⑥；**M9 必须打在 ⑨a 够得到的位置上**（`:109` 的 `2/12 → 3/12`），
      并**另加 M9b** 打在只有 ⑨b 够得到的位置上（区域内塞一个 `4/9`）以证明兜底那半也活着。
      ⚠️ **这条判据不裁定任何口径** —— 它判的是「散文里引的数与**它自称的那一列**一致」，
      即**这句话是否仍然忠实地转述它的出处**，不是「这个数对不对」。
      结论句被钉到乙列，是因为它**本来就是从乙列写出来的**（§12.3 `:118` 自述取 `07:31Z`）；
      钉住它**不等于承认乙列为准** —— 该句在哪几列下不成立由 D3 那句口径分述另行说明，
      两者一起才是完整处置。因此与 D2「不设正确值列」不冲突
  - Skill: `none`
- [ ] `Decision` 记 **D2 / D3** 的落地形态：表里**没有**「正确值」列；
      §12.3 结论句原文保留 + 一句口径分述（D3 的残余风险照抄进 doc，不缩写）
  - Skill: `none`

Exit Criteria:

- [ ] §12.3 的表由判据钉住，改错任一格即红（Phase 4 的 M1/M2 证明）
- [ ] **区域内没有任何一个数是「既豁免于守卫、又不被任何判据够到」的** ——
      判据⑨a/⑨b 合起来覆盖区域内全部数字出现，Phase 4 的 **M9 / M9b** 两个变异各证一半
- [ ] **`STATE.md` / `docs/audits/` / `docs/evidence/` 三处一个字节未改** ——
      `git status --porcelain -- docs/masterplan docs/audits docs/evidence` 无输出
- [ ] `pytest tests/routing -q` **逐字仍是 `167 passed, 1 skipped`**（§6 **H5**）
- [ ] `bash tools/check-masterplan-links.sh` exit 0（新增指针不得造成断链）
- [ ] `docs/logs/` 更新

### Phase 3 — 指针面与守卫：把段外八处手抄换成指针，并让「新写一处手抄」当场红

Status: planned
Targets: `docs/architecture/model-management.md`（§12.5 出处列 `:207/:208/:210` + `multi_hop` 限定段）·
`docs/architecture/module-boundaries.md`（§7.8）· `agenerp/explain/loop.py`（**仅注释**）·
`tests/unit/test_entry_gate_tally.py` · `tests/unit/test_answer_judging_fixture.py`
Skill: `none`

- Item Types: **`Fix`-heavy**（5/7 tagged `Fix`；另 2 `Proof`）
- Prereqs: Phase 2

- [ ] `Fix` §12.5 **模型档案表出处列**三处手抄换指针：`:207`（`2/3` / `3/3`）·
      `:208`（`3/3` / `3/3`）· **`:210`（`0/3` / `1/3` —— `on` 格与丁列的 `2/3` 今天就冲突）**。
      ⚠️ **只动第四列**；前三列（模型名 / 能力 / 是否计 reasoning）一格不动，
      收口以 `pytest tests/routing -q` 逐字不变自证（§6 H5）
  - Skill: `none`
- [ ] `Fix` §12.5 `multi_hop` 限定段：`1/6` 换指针；
      **那段「`2/6` vs `1/6` 待人裁定」实读已被 `STATE.md:464`（`[resolved] 10:12Z`）结清**，
      按该 `[resolved]` 行改准（**只改准「分歧仍敞着」这个陈述，不裁定计数**）
  - Skill: `none`
- [ ] `Fix` §12.5 那句「它在本项目实测下答对不到一半」：该判断在甲/丁列下**不成立**
      （6/6），按 D3 同法改成口径分述。**`multi_hop` 能力取值一个字不动**（Non-Goal 7）
  - Skill: `none`
- [ ] `Fix` `module-boundaries.md:558` 的 `1/6` 换指针，`[open] 07:50Z` 引用原样保留
  - Skill: `none`
- [ ] `Fix` `agenerp/explain/loop.py:645` 的 `1/6` 换指针（**仅注释**，D4）
  - Skill: `none`
- [ ] `Proof` 判据⑦（守卫）：先跑**宽网阳性对照**（只留数字面、去掉语境面与区域面），
      证明它在 §1.6 那 **6 行无关面上全部误报**（起草期实跑：6/6 误报），
      再落 §6.1 三条谓词的收窄版并断言**误报 0 / 漏报 0**；
      收窄口径与派生器**共用一份常量**。
      ⚠️ **不写「H4 预测的 2 处」** —— H4 本轮已改写成「改完之后仍 0/0」，
      **它不再预测任何误报**；「2 处」是起草期第一版从 grep-1 的无关数带过来的，已作废
  - Skill: `none`
- [ ] `Proof` 判据⑧：`tests/unit/test_answer_judging_fixture.py` 的历史叙述表
      **数字一个字不改**，只加一条指向 §12.3 的指针；判据断言该文件仍含
      「三份正确答案判成」这句原文（挡住「顺手把历史改成新口径」）
  - Skill: `none`

Exit Criteria:

- [ ] §1.6 **两条 grep 一起**在 `agenerp` + `docs/architecture` 上复跑：
      **16 行 P1.0 面全部消失或落进 §12.3 的 `machine-read` 区域内**；
      **6 行无关面逐行原样保留**（逐行比对，不比对总行数）
- [ ] 守卫的阳性对照与零误报两侧都落盘（命令原文 + 退出码）
- [ ] `agenerp/**` 的改动**逐条是注释**：`git diff -- agenerp` 里无非注释行
- [ ] `pytest tests/unit tests/contracts tests/tools tests/routing tests/context -q` 全 exit 0
- [ ] `docs/logs/` 更新

### Phase 4 — 变异自查 M1–M9b（含 M7b / M9b）、落点节 §7.18、STATE §3 追加登记

Status: planned
Targets: `docs/architecture/module-boundaries.md`（新增 §7.18）· `docs/masterplan/STATE.md`（**只追加**）· `docs/logs/2026/08-25.md`
Skill: `none`

- Item Types: `Proof | Add`
- Prereqs: Phase 3

- [ ] `Proof` **变异自查 M1–M9b（含 M7b / M9b，共 11 个），逐个施加、逐个复跑、逐个记退出码**：
      **M1** 丁列某一格 `3/3 → 2/3` · **M2** 乙列某一格改一位数 ·
      **M3** 派生器把 `label == "correct"` 改成恒真 · **M4** 派生器分母写死成 3 ·
      **M5** 同一性断言的循环体删空（计数断言必须打红） ·
      **M6** 标注集删掉一行（24 → 23） ·
      **M7** 在 `module-boundaries.md` 新写一处**带语境的** P1.0 手抄数,
      注入文本**跑之前写死**：逐字 `⚠️ qwen3.6-plus 在两跳题上是 1/6。`（守卫必须打红）·
      **M7b** 在同一位置改注入一处**不带任何语境的裸计数**，
      注入文本逐字 `是 1/6。`，且**前 4 行内不含任何标识** ——
      **预期结果跑之前写死：守卫看不见它（漏报）**，这正是 §6.2 第 7 条那条固有边界。
      ⚠️ **M7b 打不红不算判据失败，算边界被实测到**；它若**打红了**，说明窗口比
      §6.1.1 标定的更宽，**那才要回头查** ·
      **M8** 把 §12.3 的 `machine-read` 标记名改掉 ·
      **M9** 把区域**内**一个散文数字改掉（`:109` 的 `2/12 → 3/12`）—— **判据⑨a 必须打红** ·
      **M9b** 在区域内塞一个四列都没声明过的数（逐字 `4/9`）—— **判据⑨b 必须打红**。
      M9 / M9b 是独立草案评审第 2 轮点出的那个洞的实测，**两半各测一个**
  - Skill: `none`
- [ ] `Add` 落点节 §7.18，内容按 **D5** 四条
  - Skill: `none`
- [ ] `Add` `STATE.md` §3 **追加一行**：登记「丙（5/12）与丁（8/12）在两格上差 3 条、
      甲与丁在第一轮逐条一致」这一发现为 **needs-human**，附命令原文与退出码，
      **不替人裁定**，并逐字声明本 plan 未改 `docs/audits/` / `docs/evidence/` / 标注集
  - Skill: `none`
- [ ] `Proof` 八条基线命令逐条复跑，与 §6 H1–H7 逐条对照，**不吻合就照实记不吻合**
  - Skill: `none`

Exit Criteria:

- [ ] M1–M9b **逐个被打红，M7b 除外**（它的预期是漏报，见上）；
      其余有任何一个是绿的，**就地补断言并登记为 M10…**（不许放过）
- [ ] §7.18 落地；`bash tools/check-masterplan-links.sh` exit 0
- [ ] `STATE.md` 只追加：`git diff -- docs/masterplan/STATE.md` 里**无删除行**
- [ ] §6 H1–H7 逐条对照表落进**本 plan 的 §12.2**（不是 `model-management.md` §12.2，
      那一节本 plan 不碰），**假设原文一个字未改**
- [ ] `docs/logs/2026/08-25.md` 更新

## 9. 红线自查（开工前读一遍，收口时逐条复核）

| 红线 | 本 plan 的自证 |
|---|---|
| 1 `tests/gates/**` | **一个字节不改**。判据全部落 `tests/unit/`。收口复跑 `git status --porcelain -- tests/gates` 须无输出 |
| 2 `.github/workflows/**` | **不改**。本 plan 不新增测试目录（判据落进已被 CI 覆盖的 `tests/unit`），`gates.yml:535` 的 `COVERED` 集合不需要动 |
| 3 `DECISIONS.md` | **不改，也不追加 `R-x`**。D-11 复验注记里的 `2/6` 原样不动 |
| 4 项目名 / 包名 / 命名空间 | 不涉及 |
| 5 `docs/masterplan/` 其余文件 | **只在 `STATE.md` §3 追加一行**，不改写任何已有行。§12.3 那张表**读** `STATE.md`，**不写**它 |
| 6 证据仓 `${XM_PATH}` | 不写入 |
| 7 运行时 Server Script | 不生成 |

**收口时逐条复跑（命令原文）**：
`git status --porcelain -- tests/gates .github/workflows missions docs/masterplan/DECISIONS.md tools/experiments/p1_entry_gate docs/audits docs/evidence tests/fixtures`
→ 须**无输出**；`git diff -- docs/masterplan/STATE.md | grep '^-' | grep -v '^---'` → 须**无输出**。

## 10. Draft Review Record

- **Independent draft review iteration 1: `needs-revision`**（独立子代理，fresh session，
  不带起草期上下文，2026-08-25）。评审器**未采信本 plan 自报的任何数字**，逐条实跑复核：
  四列口径逐格重算（含自行从标注集派生丁列）· 24/24 `(model, gate)` 与 `answer` 同一性 ·
  八条基线命令逐条复跑（输出与 §1.1 逐字相同）· grep-1/grep-2 逐字复跑 ·
  表规 3 预算按 `> Work Item:` 重数（工作项 2 = `1/2`，本 plan 可入）·
  红线与 Protected Areas 逐条对照（**零违反**）。
  **判定：§1.2 四列口径与 §1.4 的漂移主张全部为真，无一处数字错**；
  以下 **7 条**要求修订，逐条已改：
  1. **Blocking** —— §1.5/§1.6 漏掉 `model-management.md:207` / `:208` / **`:210`** 三处
     出处列手抄，其中 `:210` 的 `qwen-plus on 1/3` **与丁列的 `2/3` 今天就冲突**；
     且 Non-Goal 7 用「§12.5 表体一行不动」当挡箭牌，而评审器实读
     `tests/routing/test_capabilities.py:79-88` 证明**出处列 `row[3]` 根本没有判据钉着** ⇒
     那条约束不存在。**已改**：三行进 §1.5 与 Phase 3，Non-Goal 7 改写成「前三列不动」并
     照实写明第一版的措辞不准确。
  2. **Blocking** —— §1.6 自称「穷举」，但 grep-2 只写了 5 行，**实跑是 16 行**。
     **已改**：16 行逐行分类落表，并补出「P1.0 面 16 行 / 无关面 6 行」的并集口径。
  3. **Major** —— 判据⑦ 的枚举集合从未被枚举，且任何含 `2/3`/`3/3` 的集合都会撞上
     至少 7 行无关文本，而**起草期第一版 §6.1** 预写的唯一补救（收窄集合）会把守卫掏空，
     另一条出路又被同段禁止 ⇒ 执行者无合法动作。**已改**：现 **§6.1**
     把守卫写成**数字面 ∧ 语境面 ∧ 区域面**三条谓词的合取（区域由一对
     `machine-read` 起止标记界定，**不是文件白名单**），现 **§6.2** 重写不吻合处置
     并写死三种禁止形态 + 一条停机分支。
     ⚠️ **改完之后起草侧实跑，发现这条比评审器指出的还要糟**：单行语境谓词
     **漏报 4 行**（`loop.py:645` / `mm:113` / `:230` / `:232` —— 标识在相邻行上），
     整段窗口则**误报 2 行**。四种窗口逐条实跑标定后取「本行 + 前 4 行」
     （**误报 0 / 漏报 0**），过程落 **§6.1.1**。**评审器的判断方向是对的，
     程度被低估了；照实记，不写成「已按评审意见修正」了事。**
  4. **Minor** —— 四处行号偏移（§12.3 表实为 `:101-106`、改准段 `:98-128`、
     「照实记」句在 `:117`、`gates.yml` 的 `COVERED` 在 `:535`）。**已改**，
     并在 §0 增第 7 条要求开工时逐条复读行号。
  5. **Minor** —— 「真未知五条」虚高：H5 由 `_table_after()` 的 `split(anchor,1)[1]`
     按构造不可能受扰、H6 由执行者自定条数因而不可证伪、H7 本 plan 自己写着「理应不变」。
     **已改**：H5/H7 标 `守护性回归`、H6 标 `目标，不是假设`，真未知收敛为 **H3 / H4 两条**。
  6. **Minor** —— Phase 2 的 `Item Types` 漏 `Decision`；D5 缺残余风险行。**两处已补。**
  7. **Minor** —— Phase 4 的「落进 §12.2」指向歧义（会被读成 `model-management.md` §12.2）。
     **已改**为「本 plan 的 §12.2」。
  ⚠️ **评审器判为「无需修订」的四项照实记下，不冒充成本 plan 的功劳**：
  Anti-Slacking Rule 零违禁词、Minimum Rule 4 单结果面成立（守卫是单一真相源契约的执行面，
  不是第二个契约）、Rule 6/8 合规、Rule 14 未降级。
- **修订后的起草侧自查（不算一轮评审，照实记）**：按第 2 条改写 §1.6 时
  **又漏掉 `model-management.md:228`**（它同时被两条 grep 命中），导致「11 + 4 = 15」
  与实跑的 16 对不上；起草侧重数时自己发现并改准为「12 + 4 = 16」，
  并把并集拆成 **8 行段内 + 8 行段外**。**过程照实写进 §1.6，不抹掉。**
- **Independent draft review iteration 2: `needs-revision`**（同一独立子代理，2026-08-25）。
  评审器**自己实现了 §6.1 的三条谓词并实跑**，不是读着判的。**三条要求，逐条已改**：
  1. **Blocking（H4 的后半在起草期就是假的 + §6.2 只写了误报一侧）** ——
     ⚠️ **照实记一处时序**：评审器测的是**单行**语境谓词（它收到的那一版），
     而起草侧在它跑的同时已把谓词换成「本行 + 前 4 行」并实测 **0 误报 / 0 漏报**
     ⇒ 它点名的 (a)(b) 两项**已被那次改动结清**，它列的 4 行漏报
     （`loop.py:645` / `mm:113` / `:230` / `:232`）与起草侧独立测到的**完全同一组**。
     **但 (c)(d) 两项确实缺**：§6.2 只写了误报侧的处置、M7 的注入文本没写死。
     **已补**：§6.2 增第 5–7 条（漏报侧处置 + 固有边界）· M7 注入文本逐字写死 ·
     新增 **M7b**（无语境裸计数，**预期漏报**）· §11 增具名残余风险一条。
  2. **Major（区域是免检区）—— 这一条起草侧没想到，评审器是对的。**
     `machine-read` 区域 `:98-128` 内有 8 行 P1.0 数，而 判据④⑤⑥ 只钉表的四列，
     `:108` / `:109` / `:113` / `:121` 那些**散文里的数**既豁免于守卫、又无任何断言够得到 ⇒
     把 `2/12` 改成 `3/12` 全仓不红，**§2 的口号当场是假的**。
     **已补**：Phase 2 增 **判据⑨**（区域内每一处数字出现必须落在四列取值集合里，
     且结论句逐个等于乙列、`:121` 等于甲列）· Phase 4 增 **M9** 实测 ·
     §7 增 **D6** 记「不改口号、改实现」的取舍与残余风险 · §2 补一句指向 判据⑨。
  3. **Minor（§1.6 三处相消的算术错）** —— ⚠️ **同样是时序**：这三处
     （grep-2 的 P1.0 数 11→12 · 重叠 1→2 行 · 段内外 9/7→8/8 · `:228` 错归行）
     **起草侧在评审器跑之前已自查发现并全部改准**，与它独立算出的数**逐条相同**。
     本轮无需再改；**两侧独立得到同一组数，本身就是这批数字第一次被两个来源交叉验证。**
  ⚠️ **评审器本轮复核为「无需动作」的七项照实记**：预测的 6 行沉默**实跑确认**
  （含 `mb:1250` 的 `P1.6` 不匹配 `P1.0`）· 守卫**非空转**（今天打红 12 行，
  区域外 5 行全是 Phase 3 的处置对象，只有修完才会归零）· 区域面**不是文件白名单**的论证成立 ·
  四处行号改准全部核对无误（且它自陈第 1 轮说的 `gates.yml:534` 是**它自己**数错，`535` 才对）·
  Non-Goal 7 的改写有据（`row[3]` 确无判据）· 并集 16 / 6 两个数正确 ·
  红线、表规 3、Anti-Slacking 与 Rule 1/2/4/6/8/14 本轮未被扰动。
- **Independent draft review iteration 3: `needs-revision`**（同一独立子代理，2026-08-25）。
  评审器**独立实现了 §6.1 的谓词并把 §6.1.1 的四行标定整张重跑**，
  **四行逐行复现，连被否决变体的失败行都逐行相同**（本行 0/4 · 整段 2/1 · 前 2 行 0/1 ·
  **前 4 行 0/0**，且前 5/6/8 同样 0/0）⇒ 「4 是 0/0 的最小窗口」**经独立复算成立**。
  它另行复核：四列取值并集正确、`3/12` 与 `4/9` 均在并集之外（M9 / M9b 选得对）、
  M7b 的注入点可行（仅 §7.8 内就有 38 个前 4 行无标识的位置）。
  **三条判定为「清白」的回答照实记**：① ⑨a 把结论句钉到乙列**不是替人裁定** ——
  乙是 §12.3 `:118` **文档自己声明的出处**，⑨a 判的是「这句话还忠实转述它自称的出处吗」，
  且若人日后裁定丁列，⑨a 不会抵抗；② ⑨a/⑨b 的拆分与 M9/M9b 各测一半是正确的变异设计；
  ③ **M7b 的「预期漏报」是合法的边界记录，不是把弱点洗成通过** ——
  评审器给的决定性理由是**它在另一个方向上可证伪**（M7b 若打红反而要回头查），
  且代价是**实测**出来的（去掉语境面 = 6 行全误报），不是断言的。
  **五条要求修订，逐条已改**：
  1. **Major（我自己上一轮改出来的）** —— 插入 D6 时把 **D3 的两条备选与残余风险
     一起裹进了 D6**，导致 D3 当场违反 Minimum Rule 9（无备选、无残余风险），
     而 D6 顶着两份不属于它的内容。**已改**：三条归位 D3，并把 D6 移到 D5 之后
     （§7 此前的顺序是 D1 D2 D3 **D6** D4 D5）。
  2. **Major** —— ⑨a **按写法不可执行**：它说「与该列的有序期望元组逐位比较」，
     而没有一行散文含四元组（`:108` 是二元、`:109` 是三元），照字面实现四条全红；
     且它**用起草期行号定位**，而 Phase 2 会重写整段、行号当场作废；
     `:121` 是否被 Phase 2 保留也没写。**已改**：⑨a 换成**五条逐字锚点子串**的绑定表
     （锚点 → 有序数元组 → 有序列-格引用），**锚点缺失即红**，
     并写死「Phase 2 五条全部逐字保留、只追加不改写」与一条**计数断言**
     （区域内含数字的散文行数 == 被绑定锚点条数）。五条锚点起草期已逐条实测存在。
  3. **Major** —— 判据⑦ 的阳性对照写「§6 H4 预测的 **2 处**误报」，两处皆错：
     H4 本轮已改写、**不再预测任何误报**；且宽网实际误报是 **6 行**不是 2 行。**已改**。
  4. **Minor** —— §6.2 的触发阈值仍写「出现第 7 处及以上」（那是把 6 行无关面
     当成误报时代的数），而 §6.1.1 实测基线是 **0** ⇒ 第 1 处就该触发。**已改**。
  5. **Minor** —— §2 只点名 M9，未点 M9b。**已改**。
  ⚠️ 另按评审器 Q1 的补充意见，在 **D3 增记一处不对称**：落地后区域内被钉住的散文数
  **只有乙列与甲列**，丙/丁两列在散文里一个数都没有 ⇒ 「在哪几列下不成立」这半
  **全压在 D3 那一句口径分述上，没有冗余**。照实登记。
- **Independent draft review iteration 4: `needs-revision`**（同一独立子代理，2026-08-25）。
  ⚠️ **照实记一处时序，这一轮尤其要说清**：评审器读到的是 ⑨a 的**五行**版本，
  而起草侧在它跑起来之前已经把 ⑨a 改成**四行、按「行」取数**
  （起因也是起草侧自查：`四格合计` 与 `加门禁反而 1/3 → 0/3` **同在一行**，
  五行版会有两条锚点争同一行的元组）。
  ⇒ 它的 **Major finding 1（计数断言在未改动的文本上就 4 == 5 当场红）
  与 Minor 2（「四条」对五行）在四行版上均已不成立** ——
  **不是被驳回，是被一次独立同源的自查提前修掉了**；两侧**各自独立**定位到同一处
  「取数单位混用」的病根，只是修法不同（它建议改成按「句」并枚举分隔符，
  起草侧选了按「行」）。**两种修法的取舍已按它的分析补写进 ⑨a**，见下。
  **本轮实际落到文件上的改动，两条**：
  1. **Minor 3（D6 的残余风险已过期）—— 完全成立，已改。** D6 原文写着
     「本 plan 没有给『结论句是否还在』加任何判据」，而 ⑨a 的**锚点缺失即红**
     已经把这一半收掉了（只对四条被绑定的锚点）。原措辞与 ⑨a 直接矛盾，
     撞 Minimum Rule 11（收口前文本必须自洽）。**已改准**：残余风险收窄成
     「删掉四条锚点之外的带数散文，判据看不见」，并写明本 plan **不为此再加判据**及理由。
  2. **按它对「行 / 句 / 锚点三种单位不可混用」的分析，补写 ⑨a 的取舍与代价**：
     逐字写明本 plan **刻意选「行」不选「句」**，理由是区域内句分隔符**有三种**
     （`；` · 句中 `。` · `——` 且跨行续写），按句取数要先枚举对这三种、
     **枚举错一种就静默取错元组**；按行取数只依赖一个不会读错的边界。
     **代价是对折行敏感**，因此把「逐字保留」逐字扩写成**连换行位置都不许动**，
     并由 M9 实测。⚠️ **这条代价是它指出来我才写下的，不是本来就想到的。**
  **它复核为「清白」的九项照实记**：D3 的备选与残余风险归位、Rule 9 恢复 ·
  D3 新增的不对称记述「是诚实版本不是软化版本」（原话）· ⑨a 的行号依赖已彻底去掉 ·
  Phase 2 对锚点的处置已写死且保留 ⑨a-4 的理由正确 ·
  `甲.强off == 甲.强on` 的侧断言对得上甲列 · 判据⑦ 已改准且与它实测的
  「宽网 22 命中、6 行无关」一致 · §6.2 触发阈值 · §2 已点名 M9/M9b ·
  M9(`3/12`) / M9b(`4/9`) 均在并集之外且各测一半 ·
  **D6 移到 D5 之后未破坏任何交叉引用**（全部按名引用，逐条复核）。
- **Independent draft review iteration 5: `acceptable-as-is`** ✅（同一独立子代理，2026-08-25）——
  **共识达成，据此把 `Plan Status` 由 `draft` 转 `active`。**
  评审器**执行了四行版 ⑨a**（不是读着判的），逐条落账：四条锚点在**全文件**范围内各命中 1 次
  （`四格合计` 全文亦仅 1 次）· 四行元组 `(0/3,1/3)` · `(1/3,0/3,2/12)` · `(0/3,1/3)` · `(2/3,3/3)`
  与声明的列-格引用**逐位相等** · `甲.强off == 甲.强on`（`3/3`）成立 ·
  计数断言 **4 == 4** · ⑨b 侧区域内无并集外的数 ·
  **并模拟了 M9**：`:109` 的 `2/12 → 3/12` 使元组变成 `(1/3, 0/3, 3/12)`，
  **位置比较失败 ⇒ M9 确实落在 ⑨a 上、没有被 ⑨b 吸收**（这正是 ⑨a/⑨b 分工的验证）。
  它并**撤回**自己第 4 轮「必须改成按句取数」的要求，原话理由：
  按句取数若把三种分隔符枚举错，会**静默取到错元组**、判据在被污染的区域上照样绿
  （承重那一半出现假通过）；按行取数只会因折行而**响亮地红**。
  「拿一个静默给错答案的失败形态，换一个吵闹误报的失败形态，对一条专门抓静默漂移的判据是对的取舍。」
  它另点名「追加的指针句不得含数、只许指列名」这条约束，评价是
  **「一条把写法推向目标、而不只是允许目标的约束」**。
  **本轮唯一的非阻塞观察已一并改掉**（它明说不必为此再开一轮，但既然要动文件就顺手改真）：
  D6 残余风险的括号算术写反了，且它登记的那个洞**够不着** —— 详见 D6 内的改准说明。
  ⚠️ **评审器的收口评语照实记两句，因为它们是对本 plan 论点的检验而不是夸奖**：
  ① 五轮里每一条可实测的事实主张**都在独立执行下成立**（四口径 · 24/24 同一性 ·
  八条基线命令逐字 · 16/6 并集 · §6.1.1 四行标定含被否决变体的失败行 · ⑨a 四条绑定与两个计数）；
  ② **「本 plan 的主题是手抄的数字，而起草过程自己就把数字手抄错了两次；
  把这件事记下来而不是藏起来，是这个 plan 自己论点最强的证据。」**

## 11. Deferred But Adjudicated

### 四个口径孰为准的裁定

- Classification: `blocked by human-only adjudication`
- Why Not Blocking Closure: 三个来源（`STATE.md` §2 · `docs/audits/` · 标注集）
  **全部是人写的或只能由人改的**；红线 3/5 与「标签只能由人读原文定」两条逐字禁止 loop 选边。
  本 plan 把四列并置、各自钉死、差异逐条量化 —— **裁定所需的全部材料都备齐了，缺的只有裁定。**
- Successor Required: `yes`。重开事件：**人在 `STATE.md` §3 就本 plan 追加的那条给出裁定**
  （或人直接改 `docs/audits/p1-insight/2026-08-24-P1.0-entry-gate.md` / `STATE.md` 的口径）。
  届时要做的只有一件：把 §12.3 那张四列表按裁定收敛，判据形状不变。

### `docs/audits/` 与 `docs/evidence/` 里同一批数字的措辞

- Classification: `watch-only residual`
- Why Not Blocking Closure: 审计是收口判定制品、证据是落盘轨迹与**预注册假设**，
  改它们分别撞在「审计措辞归人」（第十六次盘点已立）与硬约束 ②（假设跑之前写死）上。
- Successor Required: `yes`。重开事件：**与上一条同一次人侧裁定**。

### `docs/backlog/p1-insight-roadmap.md` 顶部硬约束 ④ 里的 `4/6`

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: roadmap 由引擎在 closure 审计后回写，其余段落措辞归属
  不在起草步职责内（第十三次盘点已立的定界）。⚠️ **照实记一处本 plan 发现的内部不一致**：
  同一文件的工作项 2 那一行写「弱 on **2/3** / 强 off **3/3**」（= 甲/丁口径），
  而顶部硬约束 ④ 写 `4/6`（= 丙口径），**两处口径不同**。本 plan **只登记，不代改**。
- Successor Required: `yes`。重开事件：**与第一条同一次人侧裁定**。

### 守卫对「无语境裸计数」的固有盲区

- Classification: `watch-only residual`
- Why Not Blocking Closure: 这是「语境面」这条谓词的**结构性边界**，不是实现缺陷 ——
  一处前 4 行内不含任何标识的裸计数（如孤零零一行 `是 1/6`），
  **对任何基于语境的守卫都不可见**。唯一能消除它的做法是去掉语境面，
  而 §6.1.1 实测证明那会让 `mb:195` / `mb:2508` 等 6 行无关面**全部误报**，
  守卫会因噪声被关掉 —— 那比留一个已知边界更糟。
- **本 plan 对它做了什么，逐条**：① 在 §6.2 第 7 条**跑之前**写死；
  ② 用 **M7b** 变异**实测**这个边界并落进变异账（预期漏报）；
  ③ 在 §2 与本节两处具名登记。**不假装守得住。**
- Successor Required: `yes`。重开事件：**实际发生过一次「区域外新写的裸计数逃过守卫」**
  （由人或后续 plan 在复核中发现）。届时的处置面是加一条与语境无关的判据
  （例如把四列取值集合本身做成禁止字面量），**本 plan 不预选方案**（本仓今天没测过它的误报面）。

### `qwen3.6-plus` 的 `multi_hop` 能力声明本身

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: 能力声明归工作项 3（P1.1），不在本格；
  且四列里没有任何一列**降低**该声明的支撑度（丙 4/6、甲/丁 6/6、乙 1/6 已被同一人更正），
  ⇒ 本 plan 不动它**不会让任何判据变松**。
- Successor Required: `no`。重开事件：出现一条要按 P1.4/P1.5 真实结果重划分档的工作项
  （`model-management.md:239` 逐字「这是 §12.5 三张表里第一个该被 P1.4 的真实结果推翻的格子」）。

## 12. 执行记录

*(执行期填写：12.1 八条基线命令复跑 · 12.2 H1–H7 逐格对照 · 12.3 M1–M9b 变异结果 ·
12.4 §1.6 两条 grep 的收口复核 · 12.5 红线自证)*

## Closure Gates

- [ ] in-scope behavior is complete
- [ ] relevant docs are aligned（§12.3 · §12.5 · §7.8 · 新增 §7.18）
- [ ] verification has run：八条基线命令 ①–⑧ 逐条 exit 0 且输出原文落盘
- [ ] scoped verification is not conflated with full verification —— 若只跑了部分目录，
      逐字写「verification scope limited」并说清哪些没跑
- [ ] no in-scope item downgraded to deferred/follow-up
- [ ] independent draft review completed and recorded（§10）
- [ ] text consistency verified: status, phases, gates, and log all agree
- [ ] closure audit was independent
- [ ] closure evidence exists in files
- [ ] 红线自查 §9 的两条命令逐条复跑，输出为空

## Closure

Status Note: *(pending)*

Closure Audit Evidence:

- *(pending)*
