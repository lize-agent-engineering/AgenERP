# 2026-08-26-1835-1 · P1.9 §7.2.1 判据抽查 —— 「它测的，是不是它名字说的那件事？」

> Plan Status: active
> Mission: p1-insight
> Work Item: **P1.9 · CP9 · P1 阶段复盘**（`docs/masterplan/02-WBS.md` §4 最后一行）——
> 该行验收列逐字：「纪要落 `docs/audits/2026-08-26-CP9-P1-retrospective.md`（人 2026-08-26 委托人侧代理执行；
> **§7.2.1 的判据抽查整项交 loop**，见 STATE §3）」。**本 plan 只做被交出来的那一整项，不碰纪要本身。**
> Last Reviewed: 2026-08-26
> Source: `docs/masterplan/STATE.md` §3 `[open] 2026-08-26T10:03Z`（人侧交办）·
> `docs/masterplan/04-RUNBOOK.md` §7.2.1（判据来源）·
> `docs/audits/2026-08-26-CP9-P1-retrospective.md` §1.4（本项是 CP9「AGE 续用」结论的**成立条件**）
> Related: `docs/plans/p1-insight/2026-08-26-1618-1-doc-links-child-host-guard.md`（同期 loop 交付，非依赖）
> Audit: required

## Current Baseline

### B1 · 这一项是人明确交给 loop 的，且今天**一个字都没做**

- `02-WBS.md` §4 `P1.9` 行的验收列逐字含「**§7.2.1 的判据抽查整项交 loop**」（人 `42fa183`，2026-08-26 18:06 落地）。
- `STATE.md` §3 `[open] 2026-08-26T10:03Z` 是它的正文：交办理由、抽样算法、抽中的三条、
  要 loop 逐条回答的那一个问题、参照实例、处置规则，**全部由人写死**。该行今天仍是 `[open]`。
- **实读确认未做**：`grep -rn 'test_insight_rule_ablation' docs/` 的命中里，**没有任何一条是抽查结论**
  （命中散在 WBS 行、CP9 §1.4、STATE 交办行、P1.5 的 plan、`module-boundaries.md:652`、
  `docs/analysis/2026-08-20-mission-driver-adoption-design.md:179`、`p1-insight-roadmap.md:62`、
  `STATE.md:752/753/760` —— **逐条读过，无一是抽查结论**。⚠️ 起草期第一版写的是「命中全在……」
  那五处，评审实测**枚举不全**，此处按实测改准，原文留痕于此）。
  `git rev-list --count 42fa183..HEAD` → **5**（评审期实测），逐个点名：
  `012385a`（分支/worktree 合并核查）· `17fc820`（P1 → done）· `38969b1`（1728-1 文档）·
  `a8b5489`（1728-1 实现）· `9be3007`（P2 规划）—— **无一是抽查**。
  ⚠️ **起草期第一版把 `433d2ca` 也算进"之后"，方向反了** —— `433d2ca` 17:53:59 **早于** `42fa183` 18:06:01，
  它是拆 `P1.1-fix` 那一次。按实测改准，原文留痕于此。

### B2 · 它是 CP9 一条已发布结论的**成立条件**，不是可有可无的补充

`docs/audits/2026-08-26-CP9-P1-retrospective.md` §1.4 逐字：

> **「续用」以 loop 那次抽查为条件**：若抽中的三条里**再出现一条名不副实**，
> 则「误放行 = 1 次、且是孤例」不再成立，**本条须重判**。

而 `docs/backlog/implementation-roadmap.md:38`（人 `17fc820`）已经把
「**AGE / mission-driver → 续用（以 loop 那次判据抽查为条件）**」写进了全局阶段索引。
⇒ **今天全局索引上挂着一条条件未验证的结论。** 本 plan 交付的就是那个条件的验证面。

### B3 · 抽样算法与抽中的三条（人写死，本 plan 不改、不换、不扩）

- 池 = `git log --since=2026-08-24 --name-only -- tests/` 去重后仍存在的文件（人侧实测 **40 条**；
  ⚠️ **起草评审期实测已是 44 条** —— 池随新增测试文件增长。**这不影响抽样**，见 `P1-2`）
- 排序键 = `sha256("P1.9-spotcheck-2026-08-26" + 路径)`，取前 3
- 抽中：`tests/gates/test_insight_rule_ablation.py` · `tests/unit/test_answer_judge.py` ·
  `tests/unit/test_explain_cost_ledger.py`

### B4 · 三条的规模与作者（起草期实读 `git log --format='%an'`，逐条点名）

| 文件 | 行数 | `def test_` 条数 | 首次进仓 commit | git author |
|---|---|---|---|---|
| `tests/gates/test_insight_rule_ablation.py` | 55 | **0**（薄加载器，见 B5） | `913d515` | **`lize`** |
| `tests/unit/test_answer_judge.py` | 441 | 28 | `45bb8c3` | `Email Agent Developer` |
| `tests/unit/test_explain_cost_ledger.py` | 647 | 21 | `7796ff7` | `Email Agent Developer` |

🔴 **这张表与 CP9 §1.4 逐字那句「三条都不是人侧代理写的」对不上一条**：
`test_insight_rule_ablation.py` 的 git author 是 `lize`，与 `f68ae19`（CP9 §1.4 自己举的人侧代理例子）
**同一个 author 名**。⇒ **「抽样公平」那句话的依据在这一条上不成立。**
⚠️ **本 plan 只登记这个可观测事实，不改写 CP9、不推测意图、不代人重判 §1.4**（见 Non-Goals 3/4）。
它反而**加强**了交给 loop 的正当性：三条里有一条是人侧写的，人自己抽它就是自己抽自己。

### B5 · 起草期已经看见的第一个疑点（**不是结论，是本 plan 要去查实的东西**）

`tests/gates/test_insight_rule_ablation.py` 实读 55 行，**自己一条断言都没有**，
按路径加载 `tests/unit/test_inspection_rules.py` 后**只导出一个名字**：

```
test_h1_ablation_removing_that_rule_yields_zero_hits = _BODY.test_h1_ablation_removing_that_rule_yields_zero_hits
```

而 `02-WBS.md` §4 P1.5 行的验收列写的是**两条** 🔴：
「`tests/gates/test_insight_rule_ablation.py`（消融：抽掉规则则查不出）**+** 🔴 巡检器在**零 LLM 调用**下跑通固定测例」。
该文件模块尾的注释**自己逐字承认**「零 LLM 调用」那两条**不在这里导出**，理由是 fixture 跨文件加载不随函数走，
并声明「它们仍由 `tests/unit/test_inspection_rules.py` 跑到……**不是没人管，是管在别处**」。

⇒ **三问里的 ③（缺口在哪一行）在这一条上已经有了候选。**

⚠️ **起草期第一版在这里替执行者把收窄理由写完了**（逐字「文件名承诺的是消融这一件事，不是 P1.5 整行」），
**评审判它与 Non-Goals 7「不预写结论」直接打架，已删。** 承诺面以谁为准**不由本节裁**，
由 `P2-7` 写死的优先级裁（**WBS 验收列 > 文件名/docstring**），并由抽查者实测后自己下判词。
**本节只留可观测事实：WBS 那一行写了两条 🔴，这个文件导出一条。** 是不是缺口、是不是边界，本节不表态。

### B6 · 离线基线（起草期本机实跑，一个 env 都不设）

- `python3 tools/gates/check_expected_red.py` → **exit 0**（`判定模式：default` / `门禁 29 项：预期红 0，绿 29，跳过 0` / `✅ 与预期红名单完全一致`）
- 三条抽中文件今天**全绿**（含在上面那 29 项与 `tests/unit` 里）⇒ 抽查的对象确实是 §7.2.1 逐字的「**新增或改动过的绿判据**」
- ⚠️ **整仓 `pytest tests -q -m "not live"` 已知基线即红**（`gates`×`tools` 同进程环境泄漏，已立案
  `docs/backlog/gates-and-tools-leak-env-across-directories.md`）⇒ 本 plan **不把它当验收命令**，
  收口时按「verification scope limited」照实写。

### B7 · 预算与权限逐条核过

- **plan 预算**：`docs/plans/p1-insight/*.md` 共 **24 份（不含本 plan；含本 plan 为 25）**，
  逐份 `grep -m1 '^> Work Item:'` 实点 ——
  **挂 `P1.9` 的 0 份** ⇒ 表规 3（「一个工作项 = 1–2 个 plan」）下 **`0/2`，本 plan 占第 1 格**。
  **不需要人拆行、不需要人加预算**（D-24 的「预算满了才要人加」这一前提在本格上不成立）。
- **修法面**：本 plan **不改任何产品代码、不改任何判据**。产出是**一份审计文件 + STATE §3 追加行 + 一条日志**。
  ⇒ 不触红线 1–7 任何一条（红线 5 只允许 STATE **追加**，本 plan 正是追加）。
- 🔴 **状态源必须单独说清楚**：`grep -n '^| P1.9' docs/masterplan/02-WBS.md` → `91:` 该行状态源逐字仍是 **`人`**。
  本仓有明确先例（`STATE.md:610` 逐字「状态源不是装饰字段——**它决定这一行归谁**」；
  `:1091` 逐字「loop 拒绝动一个状态源写着『人』的工作项**判得对**」）。
  ⇒ **本 plan 领的不是整行 `P1.9`**，只领**同一行里人逐字交出来的那一整项**：
  「**§7.2.1 的判据抽查整项交 loop**」。**纪要本身、状态词、重判，三样都仍归人**（Non-Goals 3/4）。
  ⚠️ 附带登记（红线 5 内不可修，只记）：`42fa183` 给 `P1.9` 行加成了 **6 格**，而表头 `:76` 只有 **5 列**
  ⇒ 渲染时最后那个 `人` 会被吞。**这是可观测事实，改它归人。**
- **谁执行**：§7.2.1 逐字「**抽查者不得是这些判据的作者**」。B4 实测两条的 author 是 `Email Agent Developer`
  （= 本循环）⇒ **执行者不得是写它们的那个会话**，独立性怎么钉见 Phase 1 的 `D-1`。

## Goals

1. **三条抽中判据逐条回答 §7.2.1 那一个问题** ——「它测的，是不是它名字说的那件事？」
   每条给出：**① 名字承诺了什么 ② 断言体实际验了什么 ③ 两者有没有缺口，缺口具体在哪一行。**
2. **②「实际验了什么」必须由实测支撑，不许只靠读。** 每条至少施加一次**可复原**的变异，
   观察该判据由绿转红/仍然绿，用退出码与红的**逐字断言名**回答 ②。
3. **抽查者的独立性可自证**：执行者不是这三份文件的作者会话，且独立性依据逐条落盘。
4. **结论落 loop 自己的提交**：审计文件 `docs/audits/p1-insight/2026-08-26-P1.9-gate-spotcheck.md` +
   `STATE.md` §3 追加一条证据行 + `docs/logs/` 一条。
   **抽到一条名不副实的即登记为一次误放行**（§7.2 原规则），**由人决定 CP9 §1.4 要不要重判**。

## Non-Goals

1. **不改 `tests/gates/**` 任何一个字节**（红线 1）—— 包括「为了让它更名副其实」补断言、
   包括为做变异而临时改它。**变异只施加在 `agenerp/**` 实现侧**（见 Phase 2 的施加面白名单）。
2. **不改 `.github/workflows/**`**（红线 2）· **不改 `DECISIONS.md` / `02-WBS.md`**（红线 3/5）·
   **不改写 `STATE.md` 任何已有行**（红线 5，只追加）· **不写证据仓**（红线 6）· **不生成 Server Script**（红线 7）。
3. **不改 `docs/audits/2026-08-26-CP9-P1-retrospective.md`** —— 那是人写的复盘纪要。
   §7.2.1 说「抽查记录落进当期复盘纪要」，而 `STATE` §3 交办行逐字说「**结论落 loop 自己的提交**」。
   **两句以后者为准**：loop 落自己的文件，**要不要并进纪要由人做**。
4. **不重判 CP9 §1「AGE 续用」的结论。** §1.4 逐字「本条须重判」的主体是人。
   本 plan 只提供「抽中的三条里有没有再出现一条名不副实」这个事实，**不代人下续用/停用的判断**。
5. **不做 §7.2.2 的人侧四提交独立复核**（`6e118c2` / `5396e68` / `40a6c33` / `f68ae19`，
   `STATE` §3 `[open] 2026-08-26T09:49Z`）—— **那是另一个结果面**（指南规则 4），且它是 RUNBOOK 立的
   **每轮例行动作**，不是本工作项。本 plan 只在 `## Deferred But Adjudicated` 登记它仍敞着。
6. **不扩样、不换样本、不改抽样算法、不"因为某条不好查就换一条"。** 复算不出同一组也不换（见 `P1-2`）。
7. **不预写结论。** B5 那个疑点是**起草者看见的线索**，抽查者必须独立判、可以判它没问题；
   若抽查者的结论与 B5 的线索相反，**照实写反面结论**，本 plan 不因此算失败。
8. **不修任何本轮查出来的缺口。** 抽查是审计，不是修理；修法面在 `tests/gates/**` 的更是红线 1。

## Task Route

- Type: `verification or audit work`（含一处 `Decision`：抽查者的独立性怎么钉）
- Owner Docs: `docs/masterplan/04-RUNBOOK.md` §7.2.1（判据来源，**只读**）·
  `docs/masterplan/02-WBS.md` §4 `P1.5` / `P1.4` / `P1.7` 三行（三条判据各自的名字承诺，**只读**）·
  产出落 `docs/audits/p1-insight/`（新增一份，不动既有任何一份）
- Skill Selection Basis: **`none`** —— 口径**逐字借用** `docs/skills/closure-audit-prompt.md` 的
  「**不采信自报，逐条对活仓取证**」（`STATE` §3 `[open] 09:49Z` 与 RUNBOOK §7.2.2 都点名这一句），
  但**不登记为技能使用**：`docs/skills/README.md:47` 给该 prompt 的 when-NOT 逐字是
  「work is still mid-flight」，而本任务正是 mid-flight；其输入列是
  「plan, verification evidence, relevant changed docs」，与本任务的对象（**判据本身**）不是一回事。
  ⇒ 指南规则 8 要求的「required inputs and expected output 已从 README 清楚」在本任务上**不成立**，
  照实写 `none`，**不假装有技能覆盖**。三问的结构由 §7.2.1 写死，工作方法由本 plan 的 Phase 2 自己写死。

## Infrastructure And Config Prereqs

**No infra prereqs beyond existing baseline** —— 全部动作零网络、零站点、零 docker、零 LLM 凭据。

- 三条抽中判据**全部可离线跑**：`test_insight_rule_ablation.py` 在 `check_expected_red.py` 的 29 项里、
  另两条在 `tests/unit`。**本 plan 不要求跑任何 `-m live` 判据。**
- ⚠️ **变异会写工作树**（`agenerp/**` 实现文件）。**回滚策略写死在 Phase 2 的 `P2-0`**：
  每次施加前记 `sha256`、施加后必复原、复原后 `sha256` 必须同值，且**整个 Phase 收尾
  `git status --porcelain -- agenerp/ tests/` 必须无输出**。⚠️ **不许用 `git checkout -- .` 兜底复原**
  —— 那会连带抹掉工作树里可能存在的人侧在飞改动（本仓 2026-08-26 已实测发生过两次人侧在飞改动）。

## Execution Plan

### Phase 1 — 复算抽样、钉死独立性

Status: planned
Targets: `docs/audits/p1-insight/2026-08-26-P1.9-gate-spotcheck.md`（新建，本 Phase 只落"输入面"一节）
Skill: `none`（理由见 `## Task Route` 的 Skill Selection Basis）

- Item Types: `Decision | Proof`
- Prereqs: 无

- [ ] `Decision` **D-1：谁来做这次抽查。**
      选定 **(A)**：**由一个独立子代理在全新会话里执行 Phase 2 的三条抽查**，
      该子代理**不读本 plan 的 B5**（B5 是线索，会污染独立判断），只拿到 §7.2.1 的三问与三条路径；
      执行者（本会话）负责搭台、复算抽样、核对独立性、落盘，**不代它给三问的答案**。
      **备选与否决理由（跑之前写死，不许事后补）**：
      - **(B) 本会话直接读三份文件给答案。** **否决** —— B4 实测两条的 author 是 `Email Agent Developer`
        （= 本循环）。§7.2.1 逐字「作者读自己的断言，读到的是当初的意图，不是纸面上的字」，
        P1 那次误放行正是这个结构。**选它等于把要验的那条规矩当场违掉。**
      - **(C) 交回人侧代理做。** **否决** —— 人 `[open] 10:03Z` 交出来的原因逐字就是「人侧不抽自己写的判据」，
        且 B4 实测 `test_insight_rule_ablation.py` 的 author 正是 `lize`。**交回去是原地循环。**
      - **(D) 只做静态阅读，不做变异。** **否决** —— 三问的 ② 是「断言体**实际**验了什么」。
        只读得到的是「它看起来验了什么」，而 P1 那次误放行**恰恰是读不出来的**
        （`assert payload["answer"] or payload["accepted"] is False` 逐字摆在那儿，绿了好几天）。
      **可行性不是推测**：`EXECUTION-PRINCIPLE.md:189` 逐字「AI 工作：读 roadmap / 写 plan.md……**spawn 子代理审查**」、
      `:388` 逐字「in-process 子代理（Task tool）」；同仓先例 `2026-08-26-1618-1` 的 `## Draft Review Record`
      就是两轮子代理评审。**引擎里做得到，不是纸上谈兵。**
      **(A) 的三条落实要求（缺一条 `D-1` 不算落地）**：
      - **不给 plan 全文，给自足简报 —— 边界不是"给多少页"，是"给判别标准与施加面，不给任何已有的预测与判词"。**
        🔴 **给（缺一项则简报不自足，`D-1` 不算落地）**：三条路径 · §7.2.1 的三问 ·
        **`P2-0` 全文（六款）** · **`P2-A` 整张锚点表**（含三个第二锚点的路径与章节号）· **`P2-7` 全文** ·
        `P2-1` / `P2-3` / `P2-5` 的"重点盯"清单（**含 `P2-5`(iv) 的 `pytest.raises` 例外**）·
        **`P2-2` / `P2-4` / `P2-6` 的施加面与变异构造**
        （`agenerp/inspection/**` · `agenerp/judging/**` · `agenerp/routing/adapter.py:226 usage_of`）。
        🔴 **不给**：`B5` 全文 · `P2-2`/`P2-4`/`P2-6` 里**任何**"起草期预测 / 评审实跑结果 / 留痕"段落 ·
        `P2-4` 末段那句成因结论 —— 那些是起草侧与评审侧的**判断与结果**，给了就污染独立判断。
        ⚠️ **`P2-A` 必须进简报，这一条是第 2 轮评审加的**：`P2-7` 的优先级逐字是
        「`02-WBS.md` 验收列 > 文件名/docstring」，而后两条文件的 WBS 行**钉的是别的文件**
        （`:84` 一字未提答案判定器 · `:87` 钉的是 `test_explain_cost_accounting.py`）。
        **子代理拿不到 `P2-A` 就会照 `P2-7` 去 WBS 找承诺面，找不到 ⇒ 造出一个假的 `名不副实`
        ⇒ 按 `P2-7` 末段去触发 CP9 §1.4 的重判。** 那正是本 plan 最不能出的错。
        ⚠️ **同理施加面必须进简报**：拿不到就会按文件名直觉去改 `agenerp/explain/**` ——
        **那条路已被第 1 轮评审实测证伪。**
        ⚠️ 起草期原写「子代理不读 B5」，第 1 轮评审判它**不可验证**（子代理要干活就得读 plan）——
        改成**根本不把 plan 全文交给它**，独立性由"它收到了什么"而不是"它承诺不看什么"来保证。
      - **预测归子代理写，不归 plan 写**：`P2-2/P2-4/P2-6` 里的变异**预测由执行的子代理在跑之前
        逐字写进审计文件**。**plan 内不再写死任何预测** —— 起草期写死的六条里，
        **评审实跑证明其中 3 条是错的**（口径 = **变异条数**，不是测试名条数）：
        `P2-4`(a) 预测证伪 · `P2-4`(b) 按构造不可施加 · `P2-6`(a) 预测证伪。
        `P2-2`(a)(b) 两条**实跑吻合**；**`P2-6`(b)（失败路径不落账）评审期未实跑，状态是「未验证」**，
        照实记，不算进「吻合」也不算进「证伪」。
        ⇒ **plan 内的预测不构成对错标准**，硬约束②（"预测在前"）约束的是**执行时刻**，不是起草时刻。
      - **独立性不许自报**：把**交给子代理的 prompt 原文 + 子代理返回的原文 + task/session id**
        逐字贴进审计文件。⚠️ `closure-audit-prompt` 的口径逐字是「**不采信自报**」——
        一句"我不是作者"正是它禁止的形态。
      **残余风险（照实登记，不修饰）**：
      ① 独立子代理与本会话同属一个循环、同一个模型家族，**"独立"只到会话级与上下文级，不到实现级**；
      ② `EXECUTION-PRINCIPLE.md:406` 逐字「子代理是**进程内**的……一个子代理挂住会让整个 step 子进程挂住」，
      且 `:79` 有「60min 无输出则杀」的看门狗 ⇒ **看门狗一杀，执行者与子代理一起死**，
      工作树里可能留着未复原的变异 —— 这正是 `P2-0` 改用内存 monkeypatch 的直接原因。
      **两条都必须逐字写进审计文件的"方法"一节，不许写成"完全独立的第三方审计"。**

- [ ] `Proof` **P1-0（记死 `<BASE>`）**：开工时 `git rev-parse HEAD` → 记具体 sha 进审计文件。
      后续所有红线自证与 STATE 删除行自证**一律带这个 `<BASE>`**
      —— ⚠️ 不带 `<BASE>` 的 `git diff` 在落盘 commit 之后必然为空，**那是二次真空通过，不是证据**。
      收尾再 `git rev-parse HEAD` 复核一次，两个 sha 都记。
- [ ] `Proof` **P1-1（抽样可复算）**：按 B3 的算法**自己重算一遍**
      （池 = `git log --since=2026-08-24 --name-only -- tests/` 去重后仍存在的文件；
      键 = `sha256("P1.9-spotcheck-2026-08-26" + 路径)`；取前 3），
      与人写死的三条**逐字比对**，池的条数一并记（人侧实测 40 条 · 评审期实测 44 条）。
- [ ] `Proof` **P1-2（复算不吻合时怎么办 —— 跑之前定死）**：若复算得到的三条与 STATE 里的**不逐字相同**，
      **仍然抽查 STATE 里写死的那三条**（人写死的样本优先），并把差异逐条落进审计文件的"输入面"一节、
      连同可能的成因（池的时间边界 / 已删除文件 / 换行符）**照实记为待人处置**。
      **绝不换样本、绝不"以复算为准"** —— 换样本的口子一开，抽查就不可复算了。
      🔴 **池条数与人侧写的 40 条不一致时，只记不处置** —— **本条只在「抽中的三条不逐字相同」时触发。**
      理由：排序键只依赖**路径字符串**，池增长不改变前三名。第 2 轮评审已在四个提交点
      （`HEAD` / `42fa183` / `433d2ca` / `9be3007`）各复算一遍，**前三名逐字恒为那三条、第 4 名恒为
      `tests/unit/serve_fakes.py`**。⚠️ 起草期把这一款写进了 B3 的前向引用却漏在正文，第 2 轮评审点出，已补。
- [ ] `Proof` **P1-3（作者与独立性逐条落盘）**：三条各跑
      `git log --format='%h %an %ad %s' --date=short -- <path>`，把 author 逐字记进审计文件；
      并把**交给子代理的 prompt 原文 + 子代理返回的原文 + task/session id** 逐字贴进审计文件
      （**不采信"我不是作者"这类自报声明**，见 `D-1` 第三条）。
      ⚠️ **B4 那条与 CP9 §1.4 对不上的事实必须原样写进审计文件**，不加修饰、不代人改纪要。
- [ ] `Proof` **P1-4（对象确实是"绿判据"）**：`python3 tools/gates/check_expected_red.py` → 退出码与摘要行原文记下；
      `python3 -m pytest tests/unit/test_answer_judge.py tests/unit/test_explain_cost_ledger.py -q` → 退出码与条数记下。
      **三条今天全绿是抽查的前提**（§7.2.1 抽的是"绿判据"）；若哪条今天就红，**那不是抽查对象，当场登记交人**。

Exit Criteria:

- [ ] 审计文件已建，"输入面"一节含：抽样复算结果 + 池条数 + 三条路径 + 三条 author + 独立性声明 + 三条今天的绿证据
- [ ] `D-1` 的选定/两条否决/残余风险**逐字落在审计文件的"方法"一节**（不只在本 plan 里）
- [ ] **No owner-doc update required**（本 Phase 不改任何 owner doc；产出是新增审计文件）
- [ ] `docs/logs/` 留到 Phase 3 一次性写（同一 sprint 同一交付面，指南 `When Executing` 第 9 条）

### Phase 2 — 三条逐条抽查（三问 + 变异实测）

Status: planned
Targets: `docs/audits/p1-insight/2026-08-26-P1.9-gate-spotcheck.md`（三条各一节）
Skill: `none`（理由见 `## Task Route` 的 Skill Selection Basis）

- Item Types: `Proof`（**本 Phase 全部 Proof，9/9** —— `P2-0` `P2-A` `P2-1`…`P2-7`）
- Prereqs: Phase 1（`D-1` 必须先裁定，否则不知道谁来答三问）

- [ ] `Proof` **P2-0（变异协议 —— 先立规矩再动手）**：写死并落进审计文件：
      **① 默认施加形态 = 内存 monkeypatch，不是改文件。** 在一个临时脚本里
      `monkeypatch` / 直接改被导入模块的属性，再 `pytest.main([...])` 跑那份判据，
      记退出码与红的**逐字断言名**。⚠️ **工作树零字节改动** ——
      评审期六条变异全部用这个形态跑通，**退出码与红断言名与改文件等价**。
      **这条是对 `D-1` 残余风险②的直接对策**：看门狗随时可能把进程杀掉（`EXECUTION-PRINCIPLE.md:79`/`:406`），
      而**内存变异随进程一起消失，改文件不会**。
      **② 文件级施加只作兜底**（内存形态按构造做不到时），且必须：施加前 `cp` 原字节到 `/tmp`
      （**不依赖 git**）→ `shasum -a 256` 记下 → 施加 → 跑 → 从 `/tmp` 复原 → `shasum -a 256` **必须同值** → 记 `RESTORED OK`。
      **③ 施加面白名单**：`agenerp/**`。
      **④ 黑名单（改一个字节即停机）**：`tests/gates/**`（红线 1）· `.github/workflows/**`（红线 2）·
      `docs/masterplan/**`（红线 5，STATE 追加除外）· **`tests/**` 下的任何夹具与断言体** ——
      ⚠️ **这一条是评审加的**：`tests/unit/answer_judge_fixture.py` 与
      `tests/fixtures/p1_entry_gate_labels.jsonl` **都不在红线 1 内**（不在 `tests/gates/`），
      **只有本 plan 自己的白名单能拦住它们**；标注集另有 roadmap 逐字「标签只能由人读原文定」顶着。
      **⑤ 复原失败分支（跑之前定死）**：若复原后 `sha256` 不同值，或 `git status --porcelain` 出现非预期条目 ⇒
      **立即停手，不再动任何文件**，把现场原样记进 `STATE` §3 并登记 `needs-human`。**不许"再试一次"。**
      **⑥ 收尾**：`git status --porcelain -- agenerp/ tests/` **必须无输出**；
      **不许用 `git checkout -- .` 复原**（会抹掉人侧在飞改动，本仓 2026-08-26 已实测发生过两次）。
- [ ] `Proof` **P2-A（承诺面锚点 —— 三条各自锚在哪，跑前定死）**：⚠️ **评审实测起草期锚错了两条**，按下表执行：
      | 文件 | 承诺面（按 `P2-7` 的优先级） | 起草期的错 |
      |---|---|---|
      | `test_insight_rule_ablation.py` | 文件名 + docstring + **`02-WBS.md:85` P1.5 行验收列** | 锚对了 |
      | `test_answer_judge.py` | 文件名 + docstring + **`2026-08-25-0225-1-answer-judge-v0.md` §6/§1.3** | ❌ 原锚 `02-WBS.md` P1.4 行 —— 实测 `:84` 该行验收逐字**只有** `tests/gates/test_evidence_gate_blocks_single_hop.py`，**一字未提答案判定器** |
      | `test_explain_cost_ledger.py` | 文件名 + docstring + **`2026-08-24-2109-2` / `2026-08-25-0554-1` 两份 plan** | ❌ 原锚 `02-WBS.md` P1.7 行 —— 实测 `:87` 那条 🔴 指 `tests/gates/test_explain_cost_accounting.py`，它 `:44` 加载的是 `test_explain_cost_accounting_body.py`，**不是**抽中的这一份 |
      **后两条必须在审计文件里显式写明「WBS 那一行钉的是另一个文件」** —— 否则会造出假的"名不副实"。
- [ ] `Proof` **P2-1（`tests/gates/test_insight_rule_ablation.py` · 三问）**：
      **①** 名字承诺什么 —— 三处锚点各抄原文。
      **②** 断言体实际验了什么 —— 该文件是薄加载器，**必须追到 `tests/unit/test_inspection_rules.py`
      里那个被导出的函数体**，逐行读它断了什么，并由 `P2-2` 的变异坐实。
      **③** 缺口在哪一行 —— 给行号。
      ⚠️ 该文件模块尾自述「零 LLM 调用那两条……**管在别处**」——
      **这是自报，按 `closure-audit-prompt` 口径不得采信**，必须由 `P2-7`(ii) 实测确认。
- [ ] `Proof` **P2-2（`test_insight_rule_ablation.py` · 变异实测）**：至少两次施加在 `agenerp/inspection/**`：
      **(a)** 让规则**恒不命中**（判据表达求值恒 False）· **(b)** 让引擎**忽略清单**（恒命中）。
      **预测由执行的子代理跑前逐字写进审计文件**（`D-1` 第二条）。
      ⚠️ **起草期写死的两条预测在这一格上是对的**（评审实跑：(a) ⇒ 该条**仍绿**、另 5 条红；
      (b) ⇒ **只有该条红**）—— **照实记为"起草期预测吻合"，但仍以子代理跑前自己写的预测为准。**
- [ ] `Proof` **P2-3（`tests/unit/test_answer_judge.py` · 三问）**：28 条，承诺面见 `P2-A`。
      **逐条过一遍名字与断言体**（抽查抽的是文件，文件内要读全）。**重点盯三类**：
      (i) 名字里有 `never` / `only` / `exactly` / `is a function of` 这类**强断言词**的，断言体撑不撑得住；
      (ii) 断言里带 `or` 兜底的（参照实例正是一个 `or`）—— ⚠️ 已有
      `tests/gates/test_assertions_have_no_escape_hatch.py` 在守这一形态，**它绿不代表没有 `or`**
      （它允许带理由的 `# assert-escape-ok:` 豁免）⇒ **豁免逐条读，不采信"门禁绿了"**；
      (iii) 名字说"离线/不读标注集"的，验的是不是**真的**没有写路径。
- [ ] `Proof` **P2-4（`test_answer_judge.py` · 变异实测）**：至少两次施加在 `agenerp/judging/**`：
      **(a)** `judge_one` **忽略回包恒返一个标签** · **(b)** **未知标签静默归一**（不抛）。
      **预测由子代理跑前写死。**
      🔴 **起草期的两条预测经评审实跑双双证伪，原样留痕（硬约束②：不许事后改预测，只许照实记错）**：
      - 起草期 (a)「判定器恒判正确 ⇒ 验收面那几条转红」——**错**。评审实测
        `3 failed, 56 passed`，红的只有 `test_h7_1_...` / `test_h7_3_the_label_is_a_function_of_the_model_reply` /
        `test_h7_3_the_same_answer_can_be_judged_truncated_when_the_reply_says_so`，**验收面全绿**。
        成因：`always-correct` 是 `tests/unit/answer_judge_fixture.py:183` 的 lambda，
        `meets_acceptance` 在 `:143`，**一次都不进 `agenerp/judging/`**。
      - 起草期 (b)「把 `label`/`reason` 混进 messages ⇒ `test_h7_2` 转红」——**在白名单内按构造做不到**。
        `judge_one(answer: str, ...)` 只收文本（签名判据 `test_answer_judge.py:150-165` 钉死），
        `judge_row` 只传 `row["answer"]`（`:135-138`）⇒ 要让它红**必须改 `tests/unit/answer_judge_fixture.py`**，
        越 `P2-0` ④ 的黑名单。
      ⇒ 🔴 **这两条留痕本身是有信息的**：**评审期尝试的那两条 `agenerp/**` 变异均未动到验收面**，
      成因追踪到 `tests/unit/answer_judge_fixture.py:143`（`meets_acceptance`）与 `:183`
      （`always-correct` 的 lambda）—— 该调用链**不进 `agenerp/judging/`**。
      ⚠️ **这不是“没有任何变异能动到验收面”** —— 那是对无穷空间的全称断言，两次尝试撑不起来。
      **适用范围以子代理本轮实测为准**；若子代理真把验收面打红了，**照实记红，本 plan 不因此算失败**。
      （⚠️ 起草期把它写成了必须逐字写进 `P2-3` ②/③ 的结论，**第 2 轮评审判它与 Non-Goals 7 同形态违规，已撤。**）
- [ ] `Proof` **P2-5（`tests/unit/test_explain_cost_ledger.py` · 三问）**：21 条，承诺面见 `P2-A`。**重点盯**：
      (i) `reasoning` 那一半是不是真的独立可红（P1.7 逐字要能挡住"只记 completion 不记 reasoning"）；
      (ii) `cached` 进没进 `total`（roadmap 逐字「`cached` 不进 `total`」）；
      (iii) **成本判据与失控闸判据有没有被合并**（D-18 逐字「两者的判据**分开写，不许合并**」）；
      (iv) 🆕 **`pytest.raises` 型反测要单独标出来**：`:488` 的
      `test_h3_dropping_reasoning_turns_the_real_replay_red` 整体是 `with pytest.raises(AssertionError)`
      ⇒ **产品侧"也丢 reasoning"的变异只会让它更绿**。这类条目**按构造不可能被产品变异打红**，
      它们的有效性要另找证法，**不许因为"变异没打红"就判它名不副实**。
- [ ] `Proof` **P2-6（`test_explain_cost_ledger.py` · 变异实测）**：至少两次施加：
      **(a)** 把 `reasoning` 折进 `completion`（P1.7 逐字点名的那种假实现）——
      ⚠️ **施加面是 `agenerp/routing/adapter.py:226` 的 `usage_of`（或 `agenerp/explain/ledger.py` 的端点取数）**，
      🔴 **不是起草期写的 `agenerp/explain/**`**（评审实测点名改正）。
      **(b)** 让某一条失败路径**不落账**（账本少一条）。
      **预测由子代理跑前写死。**
      🔴 **起草期 (a) 的预测经评审实跑证伪，原样留痕**：起草期写「`test_h2b_a_reply_without_reasoning_details_is_not_folded_into_completion`
      与 `test_h3_dropping_reasoning_turns_the_real_replay_red` 转红」——**两条都没红**。
      前者输入 `body_usage(120,40,None)`（无 details，**按构造折不到它**）；后者是 `pytest.raises` 反测（见 `P2-5`(iv)）。
      评审实跑 `9 failed, 18 passed`，实际转红的是 `test_h2a_the_seven_real_calls_replay_entry_by_entry` /
      `test_h3_the_real_replay_is_green_without_the_fake` / `test_m8_every_entry_carries_the_usage_of_that_very_reply` 等 9 条。
- [ ] `Proof` **P2-7（判别标准 + 判词）**：三条各给一个明确判词：
      `名副其实` / `名副其实但有已声明的边界` / **`名不副实`**。
      🔴 **承诺面冲突时的优先级，跑前写死**：**`02-WBS.md` 的验收列 > 文件名 / docstring**。
      文件名与 docstring 只能**说明边界如何被声明**，**不得缩小 WBS 已经写下的承诺**。
      （⚠️ 后两条文件的 WBS 行钉的是别的文件，见 `P2-A` —— 那时承诺面退到该表列的第二锚点。）
      🔴 **判"边界"必须同时满足两条，缺一条即判 `名不副实`**：
      **(i)** 边界在文件里**显式声明**了；
      **(ii)** 被排除的那一半**经实测确认真在别处跑到** —— **实测，不是采信文件自述**。
      （本例可查：`.github/workflows/gates.yml:587` 的「单测与契约测试」job `:604` 逐字
      `python3 -m pytest tests/unit -q`。**必须由抽查者（Phase 2 的子代理）自己复跑确认，不得照抄本行。**）
      ⇒ **"什么都判成边界"的口子由 (ii) 堵死。**
      **抽到一条名不副实的 ⇒ 记为一次误放行**（§7.2 原规则），并逐字点明
      「CP9 §1.4 的『再出现一条名不副实』条件**已触发**，**重判归人**」。

Exit Criteria:

- [ ] 三条各有完整的 ①②③ 三问答案，②**有变异实测支撑**（不是只读）
- [ ] 每一次变异都有：施加形态（内存 / 文件兜底）+ 施加内容 + **子代理跑前逐字写死的预测** +
      实际（退出码 + 红的逐字断言名）+ 吻合/不吻合。**文件兜底形态另加 `RESTORED OK` + `sha256` 同值**
- [ ] **预测不吻合的照实记为不吻合，不许事后改预测**（P1 硬约束②）。
      起草期已被证伪的 **3 条变异**（`P2-4`(a) · `P2-4`(b) · `P2-6`(a)）**原样留在 plan 里，不许删**；
      **`P2-6`(b) 评审期未实跑，状态“未验证”，同样照实记**
- [ ] `P2-4` 已落盘**两次施加各自的实际结果**（退出码 + 红的逐字断言名）。
      **本项对结果中立** —— 若验收面**未**转红，同时落盘成因追踪（调用链停在
      `tests/unit/answer_judge_fixture.py:143/:183`，未进 `agenerp/judging/`）；
      若验收面**转红了**，照实记红即可勾。**两种结果都能勾掉这一格。**
- [ ] `git status --porcelain -- agenerp/ tests/` → **无输出**（默认内存形态本就零改动；文件兜底形态已从 `/tmp` 复原，且**没用 `git checkout -- .`**）
- [ ] `python3 tools/gates/check_expected_red.py` → **exit 0**（收尾复跑，证明抽查没把仓弄坏）
- [ ] 三条各有一个判词，且判词与三问答案**不打架**
- [ ] 凡判 `名副其实但有已声明的边界` 的，`P2-7`(i)(ii) **两条都有落盘证据**，
      其中 (ii) 是**抽查者（Phase 2 的子代理）自己复跑的**，不是照抄 plan 或采信文件自述
- [ ] **No owner-doc update required**（本 Phase 不改任何 owner doc）

### Phase 3 — 落盘：审计文件收口 + STATE §3 追加 + 日志

Status: planned
Targets: `docs/audits/p1-insight/2026-08-26-P1.9-gate-spotcheck.md` · `docs/masterplan/STATE.md`（**只追加**）· `docs/logs/`
Skill: `none`（落盘动作，无可复用技能覆盖"往 STATE 追加一条证据行"这件事）

- Item Types: `Proof`（**本 Phase 全部 Proof，4/4**）
- Prereqs: Phase 2

- [ ] `Proof` **P3-1（审计文件收口）**：文件含五节 ——
      输入面（Phase 1）· 方法与独立性（含 `D-1` 残余风险逐字）· 三条各一节（三问 + 变异表 + 判词）·
      **结论一句话**（"抽中的三条里，名不副实 N 条"）· **verification scope limited** 一节。
- [ ] `Proof` **P3-2（STATE §3 追加，不改任何已有行）**：追加一条证据行，含：
      本 plan 路径 · 三条判词 · 命令原文与退出码 · commit sha · 独立性依据 ·
      **B4 那条与 CP9 §1.4 对不上的事实** · 若 N ≥ 1 则逐字写「CP9 §1.4 的重判条件已触发，**重判归人**」。
      ⚠️ **`[open] 2026-08-26T10:03Z` 那一行本身不许改一个字**（红线 5）——
      **loop 无权把它标 `[resolved]`**；追加行里显式写明"本 plan 交付了它要的东西，标记归人"。
- [ ] `Proof` **P3-3**（⚠️ 起草期标 `Follow-up`，评审指出它**没有 trigger** 且动作在本 Phase 就做完，按实质改标 `Proof`）：`STATE` §3 `[open] 2026-08-26T09:49Z`（§7.2.2 人侧四提交独立复核）
      在追加行里**点名仍敞着**，并写明本 plan 的 Non-Goals 5 为什么不做它。**只指明，不代做。**
- [ ] `Proof` **P3-4（日志）**：`docs/logs/` 按 `00-log-writing-guide.md` 写一条，覆盖三个 Phase。

Exit Criteria:

- [ ] 审计文件五节齐全，结论一句话可被"复跑那几条命令"复核
- [ ] `STATE.md` 的 diff **只有新增行，零删除行** —— 自证：
      `git diff --numstat <BASE> -- docs/masterplan/STATE.md`，断言**第 2 列（deletions）== `0`**。
      🔴 **不许用 `git diff | grep -c '^-[^-]'`** —— 评审实测该写法对 STATE 的行形状
      （`- [open] …`，第 2 个字符就是 `-`）**永远返回 0**，真删了也报零删除。
      🔴 **必须带 `<BASE>`**（`P1-0` 记下的 sha）—— 不带的话落盘 commit 之后 diff 必空，那是二次真空通过。
- [ ] **红线自证，逐条说清楚哪条命令证了哪条，不许一条命令包打七条**（`<BASE>` = Phase 1 `P1-0` 记下的开工 sha）：
      - `git diff --name-only <BASE> -- tests/gates/ .github/workflows/ docs/masterplan/DECISIONS.md docs/masterplan/02-WBS.md missions/ docker-compose.yml agenerp/ tests/ industry-packs/` → **无输出**
        ⇒ **直接自证红线 1（gates）/ 2（workflows）/ 3（DECISIONS）/ 5（masterplan 其余，STATE 追加另证）**
      - `git diff --name-only <BASE> -- pyproject.toml` → **无输出** ⇒ **红线 4**（项目名 / 包名 / 命名空间）
      - **红线 6（证据仓 `${XM_PATH}`）本仓的 `git diff` 按构造证不了它** —— 由执行者逐字声明
        「全程未对 `${XM_PATH}` 发起任何写操作」，并记下本轮**是否读过**它。**不许拿本仓 diff 冒充。**
      - **红线 7（运行时 Server Script）**由「产品代码零改动」间接推出，**不是上面任何一条直接证的**，照实写
- [ ] `docs/audits/2026-08-26-CP9-P1-retrospective.md` **一个字节未改** —— 自证：`git diff --name-only <BASE> -- docs/audits/2026-08-26-CP9-P1-retrospective.md` → 无输出
- [ ] **No owner-doc update required** —— 本 plan 不改任何 owner doc；产出全部是新增审计文件 + STATE 追加 + 日志
- [ ] `docs/logs/` 更新

## Draft Review Record

- **Independent draft review iteration 1: `needs revision`**（独立子代理，fresh session，task `a0ecb3ebe4ef2ef43`；
  评审者不是起草者）—— 14 条 finding，其中 **6 条 `blocking`**。评审者**逐条对活仓复跑取证**，
  并**实跑了起草期写死的六条变异预测**。逐条处置：
  - **F1 `blocking`（`P2-6`(a) 预测证伪 + 施加面点错）** → 已改：施加面改 `agenerp/routing/adapter.py:226 usage_of`，
    起草期错误预测**原样留痕**，实际转红的 9 条逐字记下。
  - **F2 `blocking`（`P2-4` 两条预测：(a) 证伪、(b) 按构造不可施加）** → 已改：换成
    「恒返一标签」「未知标签静默归一」两条（评审已实测吻合）；
    **并把「`agenerp/**` 内无任何变异能动到该文件验收面」立为本轮产物**。
  - **F3 `blocking`（STATE 零删除自证写法失效）** → 已改 `git diff --numstat <BASE>` 判第 2 列。
    评审实测：`grep -c '^-[^-]'` 对 `- [open] …` 这种行形状**真删了也返回 0**。
  - **F4 `blocking`（B1 提交清单过期且方向反了）** → 已改：`42fa183..HEAD` 实测 5 个，逐个点名；
    `433d2ca` 早于 `42fa183`，照实更正。
  - **F5 `blocking`（变异协议：白名单挡不住 `tests/**` 夹具 · 无复原失败分支 · 看门狗会中途杀进程）**
    → 已改：默认改**内存 monkeypatch**（工作树零改动）· 文件兜底走 `/tmp` 备份不依赖 git ·
    `tests/**` 夹具与断言体进黑名单 · 加"复原失败即停手报人"分支。
  - **F6 `blocking`（`P2-7` 判别标准留"什么都判成边界"的口子 · B5 替执行者预写了收窄理由）**
    → 已改：写死 **WBS 验收列 > 文件名/docstring** 的优先级；判"边界"须同时满足显式声明 + **实测确认真在别处跑到**；
    **B5 里那句预写的收窄理由已删**。
  - **F7 `should-fix`（`P2-3`/`P2-5` 承诺面锚错 owner doc）** → 已改：新增 `P2-A` 锚点表，两条改锚并要求显式写明
    「WBS 那一行钉的是另一个文件」。
  - **F8 `should-fix`（漏核 `P1.9` 状态源仍是 `人`）** → 已改：B7 补一条，写明本 plan **只领人逐字交出来的那一项**，
    不领整行；并登记 `P1.9` 行 6 格 vs 表头 5 列的渲染问题（红线 5 内不可修）。
  - **F9 `should-fix`（`D-1` 独立性只有自报 · "不读 B5"不可验证 · 预测归谁写自相矛盾）**
    → 已改：不给 plan 全文只给自足简报 · 预测归子代理跑前写 · 贴 prompt/返回原文替代自报 ·
    补"子代理与本会话同进程、看门狗一杀俱死"的残余风险。
  - **F10 `should-fix`（D2 只照引未实测 · 漏了代码内同源过期注释 · Classification 越模板枚举）**
    → 已改：三处逐字实测值写进 D2（含 `agenerp/explain/loop.py:91-97`），Classification 归回模板取值。
  - **F11 `should-fix`（§7.2.1"落进纪要"与 `implementation-roadmap.md:38` 悬空无 trigger）** → 已改：新增 `D4`。
  - **F12 `should-fix`（红线自证 over-claim：一条命令冒充证七条）** → 已改：拆成四条，
    红线 6 明写"本仓 diff 按构造证不了"、红线 7 明写"间接推出"。
  - **F13 `should-fix`（池 44 条 ≠ 40 条，`P1-2` 未覆盖该情形）** → 已改：`P1-2` 加"池条数差异单独记、不触发处置"。
  - **F14 `nit` 四小条**（plan 份数 24/25 · B1 全称句枚举不全 · Skill 越界借用 · `P3-3` 无 trigger）→ 全部已改，
    其中 Skill 由 `closure-audit-prompt` 改成 **`none`**（`docs/skills/README.md:47` 的 when-NOT 逐字覆盖不了本任务）。
  - 评审"我核过但没问题的"一节确认通过：B4 整张表逐格吻合（含 🔴 那条 author = `lize`）· B5 · B6 ·
    三条今天全绿（`87 passed`）· B7 预算 `0/2` 且"不需要人加预算"成立 · B3 前三名四个提交点复算逐字恒定 ·
    B2 · `P2-2` 两条预测按构造与实测**都成立** · 标注集黑名单必要且正确 · 红线面按字面执行不踩七条 ·
    Anti-Slacking 禁用词零命中 · 规则 12 状态/勾选一致性通过。
- **Independent draft review iteration 2: `needs revision` → 处置后可转 `active`**
  （独立子代理，fresh session，task `a95c1e0765dc9bb68`；**与第 1 轮不是同一个评审者，两者都不是起草者**）——
  复评者**逐条实测复核了第 1 轮六条 `blocking` 的落地形态，判「全部到位」**，并自跑了全部硬检查
  （禁用词零命中 · 规则 12 通过 · 三个 Phase 六项齐全 · 四条 Deferred 四项齐全且取值合模板 · Closure Gates 9 项）。
  新出 7 条，逐条处置：
  - **R1 `blocking`（`D-1` 的简报清单漏掉 `P2-A` 与三条变异施加面）** → 已改。
    这是第 1 轮 F7 与 F9 两个修法**没有互相对齐**造成的新口子：`P2-7` 在简报里，
    而它逐字引用的 `P2-A` 不在 ⇒ 子代理会照 `P2-7` 的优先级去 WBS 找承诺面，
    在 `:84`（一字未提答案判定器）/ `:87`（钉的是另一个文件）上落空 ⇒ **造出假的 `名不副实`
    ⇒ 误触 CP9 §1.4 的重判**。`D-1` 第一条已改成写死「给什么 / 不给什么」的边界：
    **给** `P2-0` 全文 + `P2-A` 整表 + `P2-7` 全文 + 三条"重点盯"清单（含 `P2-5`(iv) 例外）+ 三条施加面；
    **不给** B5 全文与任何预测 / 实跑结果 / 判词留痕。
  - **R2 `should-fix`（`P2-4` 末段把一条结论预写成必须勾掉的交付物，与 Non-Goals 7 同形态）** → 已改：
    「没有任何变异能动到验收面」是对无穷空间的全称断言，已收窄成**评审期两次尝试的实测句 + 成因追踪**，
    并去掉"必须逐字写进 `P2-3` ②/③"；Exit Criteria 那一格改成**对结果中立**（转红/未转红都能勾）。
  - **R3 `should-fix`（Phase 2 `Item Types` 计数 8/8，实为 9 条）** → 已改 `9/9`（F7 插入 `P2-A` 时漏改）。
  - **R4 `should-fix`（F13 的处置说明与正文对不上 —— `P1-2` 里并没有"池条数"那一款）** → 已改：
    该款**第 1 轮那次替换按引号形态没匹配上、静默没落地**，本轮补进 `P1-2` 正文。
  - **R5 `nit`（"四条被证伪"两处口径不一 · `P2-6`(b) 评审状态全文无交代）** → 已改：
    统一口径为**变异条数**，明写被证伪的是 **3 条**（`P2-4`(a)(b) · `P2-6`(a)），
    `P2-2`(a)(b) 实跑吻合，**`P2-6`(b) 评审期未实跑，状态「未验证」**，三种状态分开记。
  - **R6 `nit`（`## Deferred But Adjudicated` 顺序是 D1/D2/D4/D3）** → 已改：D3 与 D4 调回顺序，编号未变。
  - **R7 `nit`（`P2-7`(ii) 里"执行者"指谁不明 —— `D-1` 定义的执行者是本会话，跑 Phase 2 的是子代理）**
    → 已改：两处逐字改成「抽查者（Phase 2 的子代理）」。
- **判定**：第 2 轮的 `blocking` 只有 R1 一条，且它是两个修法之间的对齐问题、改法由评审者写明；
  R2–R7 均为 `should-fix` / `nit`。**七条已全部处置，本 plan 转 `active`。**
  ⚠️ **照实说的一件事**：**没有跑第 3 轮评审**。第 2 轮评审者对六条 `blocking` 的判词是"全部到位"，
  新出的七条本轮已逐条改完，但**这七条的处置本身未经独立复核** —— 这一点写在这里，不藏。

## Closure Gates

- [ ] in-scope behavior is complete（三条各有三问答案 + 变异实测 + 判词）
- [ ] relevant docs are aligned（本 plan 不改 owner doc，已在三个 Phase 各写 `No owner-doc update required`）
- [ ] verification has run：`python3 tools/gates/check_expected_red.py` · 三条抽中判据各自的 pytest ·
      `git status --porcelain -- agenerp/ tests/` · 四条红线自证 `git diff --name-only`
- [ ] scoped verification is not conflated with full verification —— 未跑整仓 `pytest tests -q -m "not live"`
      （B6：已知基线即红）、未跑任何 `-m live`、未经 CI 服务端复跑，**收口时逐字写 "verification scope limited"**
- [ ] no in-scope item downgraded to deferred/follow-up
- [ ] independent draft review completed and recorded
- [ ] text consistency verified: status, phases, gates, and log all agree
- [ ] closure audit was independent（**且审计者既不是本 plan 的执行者，也不是那三份判据的作者**）
- [ ] closure evidence exists in files

## Deferred But Adjudicated

### D1 · §7.2.2 的人侧四提交独立复核仍敞着

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: `STATE` §3 `[open] 2026-08-26T09:49Z` 交办的是**另一个结果面**
  （`6e118c2` / `5396e68` / `40a6c33` / `f68ae19` 四个人侧提交的逐条复核），
  与本 plan 的"抽查判据本身"不共享收口判据（指南规则 4）。且 RUNBOOK §7.2.2 把它立成
  **循环每轮的例行动作**，不是某个工作项的交付物 —— 它在 `02-WBS.md` 里**没有行**，
  loop 无权开行（红线 5 + D-24 逐字「loop 仍然不得自行加行」）。
- Successor Required: `yes`（人 —— 要么由人给它一行，要么由人确认它继续以"每轮例行"的形态跑）
- 重开事件：**人在 `02-WBS.md` 为 §7.2.2 的复核拆行 / 加行**，
  或**下一轮起草步判定它可以不经 plan 直接由执行步做掉**。

### D2 · `MAX_TOOL_CALLS` / `MAX_TURNS` 的两段算术随 `40a6c33` 过期（**两处，不是一处**）

- Classification: `out-of-scope improvement`
  ⚠️ 模板给的三值是 `watch-only residual | optimization candidate | out-of-scope improvement`，
  **本仓此前用过 `out-of-scope defect` 这个扩展值**（见 `2026-08-26-1728-1` 的 D1）。
  本条按模板取值，**性质照实写在这里：它是一条确认在线的 owner-doc 漂移，不是"改进建议"。**
- **起草评审期本 plan 自己实测（不再照引 STATE）**：
  - `grep -n 'MAX_TURNS = \|MAX_TOOL_CALLS = ' agenerp/explain/loop.py` → `68:MAX_TURNS = 40` · `102:MAX_TOOL_CALLS = 50`
  - `docs/architecture/module-boundaries.md:1012` 逐字 `` `MAX_TOOL_CALLS = 32`（`agenerp/explain/loop.py`） ``；
    `:1029-1034` 逐字 `32 = 8 × 4，四倍余量` 与 `` `MAX_TURNS = 25` `` / `下界因此是 MAX_TURNS + 1 = 26` / `32 > 26，余量 6`
  - 🆕 **同一段算术也就地过期在代码注释里**：`agenerp/explain/loop.py:91-97` 逐字仍是
    `32 = 8 × 4` / `默认值取 25 时` / `下界是 MAX_TURNS + 1 = 26（等号处不算数），32 > 26`，
    **紧接着第 99 行才写「2026-08-26 由 32 提到 50（人裁定）」** ⇒ 同一个注释块里**新旧两套数并存**。
    ⚠️ **这一处落在本 plan 自己的变异施加面（`agenerp/explain/**`）里** —— 执行者做 `P2-6` 时会正面撞上它。
    **撞上了也不许顺手改**（Non-Goals 8：本 plan 不修任何缺口），只在审计文件里点名。
  - **不变式本身仍成立**（`40 + 1 = 41 < 50`）⇒ 过期的是**两段算术的数**，不是结论。
- Why Not Blocking Closure: 修法面属**工作项 6（P1.4）/ 工作项 9（P1.7）**，两格 plan 预算均 **`2/2` 已满**
  ⇒ 无预算可挂。本 plan **一行产品代码、一行 owner doc 都不改** ——
  ⚠️ 指南规则 14 逐字「确认的 owner-doc 漂移不可降级为非阻塞 follow-up」：
  **本条不是把在范围的东西降级，是它压根不在本 plan 的结果面上**（本 plan 的结果面是"三条判据名副不副实"）。
- Successor Required: `yes`（人 —— 加预算格或拆行，D-24 逐字「loop 仍然不得自行加行」）
- 重开事件：**人给工作项 6 或 9 开新的预算格**，或人直接改准那三处（doc 两段 + 代码注释一段）。

### D3 · CP9 §1.4「三条都不是人侧代理写的」与 git 对不上一条

- Classification: `watch-only residual`
- Why Not Blocking Closure: 它是**人写的复盘纪要里的一句话**，改它归人（Non-Goals 3/4）。
  本 plan 把 B4 的实测表原样落进审计文件即完成登记责任。
  ⚠️ 它**不影响本 plan 的正当性** —— 反而加强了"交 loop"这个安排。
- Successor Required: `no`
- 重开事件：**人下一次因任何理由动 CP9 §1.4**，顺带对齐这一句。

### D4 · 抽查记录要不要并进 CP9 纪要 / 全局索引那一行谁去动

- Classification: `watch-only residual`
- Why Not Blocking Closure: §7.2.1 逐字说抽查记录落进「当期复盘纪要」，
  而 `STATE` §3 交办行逐字说「**结论落 loop 自己的提交**」。
  **两句不冲突** —— 后者规定 loop 的落点，没有取消前者对纪要的要求。
  本 plan 选"落自己的文件 + 并入归人"，**结论仍可被复跑复核**（审计文件 + STATE 追加行，命令与退出码俱全）。
  同理 `docs/backlog/implementation-roadmap.md:38` 那一行
  （逐字「**AGE / mission-driver → 续用（以 loop 那次判据抽查为条件）**」，人 `17fc820` 落地）——
  它今天挂着一条**条件未验证**的结论；本 plan 交付那个条件的**验证面**，
  但**动那一行等于改人写的结论面**，归人（Non-Goals 3/4）。
  ⚠️ `docs/backlog/` 不在红线内、loop 写得了 —— **不做是权限之外的取舍，不是做不到**，照实说。
- Successor Required: `yes`（人）
- 重开事件：**人下一次动 CP9 纪要或 `implementation-roadmap.md:38` 时**，把本次抽查记录并入；
  **或** 本 plan 的判词里 N ≥ 1（抽到名不副实的）触发 CP9 §1.4 的重判，由人一并处理。

## Closure

Status Note: <pending>

Closure Audit Evidence:

- Auditor / Agent: <pending —— 必须是独立子代理，且既不是执行者也不是那三份判据的作者>
- Evidence: <pending>

Follow-up:

- <pending>
